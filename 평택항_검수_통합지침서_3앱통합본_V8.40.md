# 평택항 검수 통합 지침서 — 3앱 통합본

> **이 문서는 이 프로젝트의 모든 작업에 우선 적용되는 규칙이다.**
> 이 프로젝트에서 작업하는 모든 Claude는 시작 전에 이 문서를 먼저 읽고 따른다.

최종 버전: **Tallyman Master V8.40** (검수앱) · 최종 업데이트: 2026-07-03
작성자: 성일 (평택항 검수 / Tallyman Master 개발)

> 📌 **출력 규칙 (사용자 확정 2026-06-16)**: 지침서를 사용자에게 전달할 때 **파일명에 최종 버전을 표기**한다. 예: `평택항_검수_통합지침서_3앱통합본_V8.05.md`. 새 버전 발행 시 파일명 버전과 위 "최종 버전" 줄을 함께 갱신한다.

---

## 이 문서의 구성 — 세 앱을 한 권에, 명확히 분리

이 프로젝트에는 **서로 완전히 독립된 세 개의 앱**이 있다. 코드베이스·배포·점검이 모두 별개이므로, 한 앱을 고쳐도 다른 앱에 자동 반영되지 않는다.

| 앱 | 파일/산출물 | 정체 | 현재 버전 |
|---|---|---|---|
| **A. Tallyman Master (검수앱)** | React/Vite PWA (greenmarinetally) | 컨테이너선 양하·선적 검수 본체 | V8.20 |
| **B. 콘앱 (콘 계산기)** | `public/cone.html` (독립 HTML, 검수앱 ZIP에 동봉) | 콘(고정장치) 가감 계산 | C7.64 / 검수앱 버전 공유 |
| **C. 벌크탤리** | `bulk_tally.html` (단일 HTML) | 벌크선 철강 코일 검수 | 2026-06-12판 |

- **공통부(0~Ⅰ부)**: 세 앱 모두에 적용되는 코딩 원칙·검수 본업·운영 규칙.
- **Ⅱ부 = 검수앱(A)** / **Ⅲ부 = 콘앱(B)** / **Ⅳ부 = 벌크탤리(C)** — 각 앱의 도메인·작업 이력.
- **Ⅴ부 = 영구 참조 자료(REF)** — 데이터로 검증 완료된 사실만. 3D 카고플랜·베이 페어링.
- **부록** — 디버깅 일지·운영 팁·메모 수신함.

> ⚠️ **앱 B(콘앱)는 앱 A와 같은 버전 번호(V7.xx)를 공유**한다(같은 ZIP·같은 build.sh). 단 cone.html은 독립 HTML이라 React 점검과 **따로 점검**해야 한다. 콘앱 자체 버전(C7.xx)은 콘앱만 단독 변경한 빌드에 쓴다.

> 🔒 **단일 지침서 운영 (사용자 확정 2026-06-15 — 절대)**: 이제 따로 된 지침서는 없다. **이 통합 지침서 한 권만 유지**한다. 세 앱이 모두 완성돼 독립 출시될 때까지는 검수앱·콘앱·벌크탤리를 전부 이 한 권으로 관리하며, **별도 지침서(검수앱지침서.md·BULK_TALLY_지침서.md·REF_*.md 등)를 새로 만들지 않는다.** 모든 새 내용은 이 문서의 해당 부에 흡수한다. 분리는 각 앱이 독립한 뒤 사용자가 지시할 때만.

═══════════════════════════════════════════════════════
# ★ 세션 표준 시나리오 — 모든 Claude 공통 (사용자 확정 2026-06-18, 절대)
═══════════════════════════════════════════════════════

> 이 블록은 이 문서에서 **가장 먼저 읽고 가장 강하게 지키는 절차**다.
> 0부 이하의 원칙들이 "왜"라면, 이 블록은 "어떤 순서로 움직이는가"다.
> 어느 Claude가 와도 **같은 길**을 걷게 하기 위한 것이다. 표현은 흔들려도 되지만, 이 순서는 흔들리면 안 된다.
> 작업 중 각 관문에 들어설 때 **"N관문: …"** 으로 번호를 밝힌다. 사용자가 진행을 그 번호로 추적한다.

---

## 0. 시작 선언 (코드를 만지기 전, 한 번)

작업을 시작하면 **세 줄**을 먼저 적는다. 이걸 적기 전에는 코드·파일을 건드리지 않는다.

1. **대상 앱**: 이번에 건드리는 앱 하나를 특정한다 (A 검수앱 / B 콘앱 / C 벌크탤리). 세 앱은 완전 별개다.
2. **끝낼 범위**: 이번 응답에서 어디까지 끝낼지 한 줄로 못박는다. (범위를 안 적으면 아무 데서나 멈추게 된다 — 작업을 남기는 가장 큰 원인.)
3. **남길 것 명시**: 이번에 안 할 부분이 있으면 "이건 이번에 안 한다, 이유는 X"라고 미리 밝힌다. 슬그머니 미루지 않는다.

---

> 🔒🔒 **"작업 시작"의 정의 (사용자 확정 2026-06-19 — 절대, 가장 자주 어기는 함정)**
> **작업 시작 = 협의·재분석·질문·확답 확보를 다 거친 "그 다음"이다.** 코드를 만지는 행위(5관문)는 작업의 출발점이 아니라, **사용자의 확답을 받고 나서야 비로소 시작되는 것**이다.
> - 협의·구상·질문·확답은 "작업 전 준비"가 아니라 **작업 시작을 이루는 구성요소 그 자체**다.
> - 0-11b의 "작업 모드 들어가면 중간에 안 되묻기"는 **확답을 받은 5관문 이후에만** 적용된다. 확답 전에 안 묻고 직진하는 것은 "작업 모드"가 아니라 그냥 **협의를 건너뛴 것**이다.
> - 배경: 2관문(소스 확인)을 끝내고 혼자 설계를 정해 5관문(코드)으로 직행하면, 거의 반드시 재작업이 난다(V8.10 규격 20/40 → 20/40/45·F/E 재작업이 실제 사례). 협의를 거쳤으면 처음부터 맞게 짰을 일이다.

---

> ⚠️ **이 7관문은 반드시 지켜진다 (사용자 확정 2026-06-19 — 절대 규칙).** 어느 Claude가 와도 2→3→4관문(점검·구상·협의)을 건너뛰고 코드로 직행하지 않는다. 4관문에서 사용자 확답을 받기 전에는 5관문(작업)에 진입하지 않는다. 통과한 관문은 **"N관문: …"** 으로 번호를 밝혀 사용자가 추적하게 한다.

## 1관문 — 진입 (버전·앱 정합)

- 세 버전을 대조한다. ① 이 지침서의 "최종 버전" ② 작업 베이스(업로드 ZIP/clone)의 `src/utils.js APP_VERSION` ③ 사용자가 마지막으로 받은 ZIP.
- 셋이 어긋나면 **코드를 만지기 전에 보고**하고 어느 게 최신인지 확인받는다. (0-11)
- 통과 조건: 세 버전 일치 확인 또는 불일치 보고 완료.

## 2관문 — 점검 (대상 앱·소스가 제대로 돼 있는지)

- "근거를 본다"가 아니라 **"제대로 되어 있느냐까지 점검한다."** 잘못된 앱·잘못된 베이스로 작업하면 안 되기 때문이다.
- 손댈 소스를 **GitHub에서 직접 확인**하고, 대상이 올바른지·현재 상태가 멀쩡한지·이미 누가 손댔는지(다른 세션 흔적)를 본다. 기억·추측으로 시작하지 않는다.
- 손댈 데이터 구조(EDI 행·매트릭스 cells·firebase 레코드·필드명 등)를 **실제로 본 뒤** 진행한다. (부록 A의 반복 실수는 전부 "안 보고 짠 것"이 원인.)
- "이렇겠지"가 끼면 그 자체로 관문 실패 — 다시 본다.
- 통과 조건: 대상 앱·소스가 올바르고 제대로 돼 있음을 확인. 손댈 소스·데이터 구조·필드명을 실제로 봤다.

## 3관문 — 구상 (방법·시나리오 도출)

- "어떻게 하면 좋을까" **방법을 구상하고, 시나리오를 여러 개 세운다.** 한 가지 안만 들고 오지 않는다.
- 결정이 갈리는 분기점(규격을 어떻게 나눌지, F/E를 구분할지, 표시를 어디에 둘지 등)을 미리 찾아 시나리오로 만든다.
- 통과 조건: 선택지(시나리오)들이 갈리는 지점과 함께 도출됐다.

## 4관문 — 협의·가상시뮬·질문·결정 (← 여기까지 통과해야 "작업 시작")

- **협의 = 구상한 시나리오들 중 나은 걸 찾는 과정.** 사용자와 함께 고른다. 혼자 고르지 않는다. (0-1)
- 나은 안을 고른 다음 **가상 시뮬레이션**을 돌려 "이렇게 하면 이런 결과가 나옵니다"를 데이터로 보여준다. (시뮬은 6관문 검증에만 있는 게 아니라, 작업 전 협의에도 "가상 시뮬"로 들어간다 — 추측이 아니라 데이터로 결정하기 위해.)
- 그 결과를 놓고 **"어떻게 할 것인지" 질문 → 사용자의 결정(확답)** 을 받는다.
- ⛔ **확답 없이는 5관문(작업)에 진입 금지.** 확답이 없으면 아직 "작업 시작"이 안 된 상태다.
- 통과 조건: 시나리오 중 하나로 합의 + (필요 시 가상 시뮬로 결과 제시) + 질문에 사용자 확답 확보.

## 5관문 — 작업 (외과적 변경)

- 요청·확답과 **직접 연결된 줄만** 고친다. 주변 코드 "개선"·무관한 리팩터링 금지. (0-3)
- 새 파일 첫 줄 한국어 헤더 주석. 한국어 문장은 `.`/`?`/`!`로 끝낸다(콜론 금지). (0-5, 0-6)
- 이 단계부터 0-11b 작업 모드 — 중간에 되묻지 않고 끝까지(단, 4관문 확답 범위 안에서).
- 통과 조건: 변경된 모든 줄이 4관문에서 합의된 내용에 직접 연결된다.

## 6관문 — 검증 (이것이 "완료"의 정의 — 비협상)

**아래 셋이 전부 끝나야 "완료"라고 말한다. 빌드 성공만으로는 완료가 아니다.**

1. **실데이터 시뮬 PASS** — `/home/claude/sim/*.mjs`로 실제 데이터를 끝까지 돌려 기대값과 일치.
2. **`bash build.sh` 성공** — `npm run build` 단독 금지.
3. **빌드된 JS에서 새 문자열 grep 확인** — 내가 넣은 변경이 번들에 실제로 박혔는지 확인.
   - **새 UI를 추가·변경했으면 시각 렌더(PNG/SVG, chrome-headless·puppeteer 등)로 눈으로 PASS까지.** (V8.10에서 새 배지를 만들고 렌더 확인을 건너뛴 것이 반성 사례 — 화면 확인을 사용자에게 떠넘기지 않는다.)

- 셋 중 하나라도 못 했으면 "완료"라 부르지 않는다. "검증을 사람에게 떠넘기지" 않는다 — 끝까지 내가 본다. (0-8, Ⅶ부)
- 통과 조건: 위 3종(새 UI면 +시각 PASS) 전부 완료 + 결과를 사용자에게 보고.

## 7관문 — 인계 (다음 Claude가 끊김 없이 잇도록)

- **ZIP**은 Ⅱ-2.5 규칙대로 — 최상위 `Tallyman_Master_Vx.xx/` 하나, dist 포함, 제외 목록(node_modules·HANDOFF·구지침서·깨진 파일명 등) 점검. 출력은 `/mnt/user-data/outputs/`.
- 새 발견·결정·검증 결과를 **이 지침서 해당 부에 흡수**한다(별도 파일 만들지 않음). 해결된 항목은 취소선으로 정리.
- **다음 Claude를 위한 3줄**을 남긴다:
  1. 했다 — 이번에 끝낸 것.
  2. 검증됐다 — 무엇이 어떻게 PASS됐는지.
  3. 주의 한 가지 — 다음이 밟지 말아야 할 함정 / 남은 작업.
- 통과 조건: ZIP·지침서 흡수·인계 3줄 완료.

---

## 트리거 단어 (사용자 → Claude, 고정 동작)

| 사용자가 말하면 | Claude는 |
|---|---|
| **메모** | 답변·분석·제안·질문 금지. 부록 C에 원문 그대로 적고 "메모 접수" 한 줄만. |
| **작업해줘** | 쌓인 메모를 몰아서 처리. 0관문부터 정식 시작(2·3·4관문 협의 거쳐 확답 후 코드). |
| **멈춰 / 스톱** | 즉시 중단. 변명·이어쓰기 금지. |
| **검증** | 6관문 결과(3종 PASS 여부, 새 UI면 +렌더)만 보고. |
| **인계** | 7관문 "인계 3줄"만 출력. |

---

## 한 줄 요약 (이 블록의 핵심)

**0 선언 → 1 버전대조 → 2 점검(앱·소스 제대로 됐나) → 3 구상(시나리오 여럿) → 4 협의·가상시뮬·질문·확답 【= 작업 시작】 → 5 외과적 수정 → 6 시뮬·빌드·grep(+새UI면 렌더) 전부 PASS여야 완료 → 7 ZIP·흡수·인계 3줄.**
확답을 받기 전에는 코드를 만지지 않는다. 빨리 끝내려 중간에 멈추지 않는다. 검증을 사람에게 떠넘기지 않는다. 끝낼 범위는 시작에 적고, 못 끝낸 건 인계에 명시한다.

# 0부 — 범용 코딩 지침 (세 앱 공통, 모든 작업의 기본 자세)
═══════════════════════════════════════════════════════

caution over speed. 사소한 작업은 판단껏.

## 0-1. 코딩 전에 생각하라 (Think Before Coding)
가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.
- 가정은 명시적으로 말한다. 불확실하면 묻는다.
- 해석이 여러 개면 제시한다 — 혼자 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 불분명하면 멈춘다. 무엇이 헷갈리는지 짚고 묻는다.

## 0-2. 단순함 우선 (Simplicity First)
문제를 푸는 최소 코드만. 추측성 기능 금지.
- 요청 안 한 기능 추가 금지. 일회용 코드에 추상화 금지.
- 요청 안 한 "유연성·설정 가능성" 금지. 불가능한 시나리오 에러처리 금지.
- 200줄 짠 게 50줄로 되면 다시 쓴다.

## 0-3. 외과적 변경 (Surgical Changes)
건드릴 것만 건드린다. 내가 만든 흔적만 치운다.
- 주변 코드·주석·포맷을 "개선"하지 않는다. 안 깨진 걸 리팩터링하지 않는다.
- 기존 스타일을 따른다. 무관한 죽은 코드는 언급만 하고 지우지 않는다.
- 내 변경이 만든 미사용 import/변수/함수만 제거한다.
- 기준: 모든 변경 줄이 사용자 요청과 직접 연결돼야 한다.

## 0-4. 목표 기반 실행 (Goal-Driven Execution)
성공 기준을 정하고, 검증될 때까지 반복한다.
- "버그 수정" → "버그를 재현하는 테스트를 만들고, 통과시킨다."
- 이 프로젝트에선: **실데이터로 재현 → 고침 → 같은 데이터로 PASS 확인**.

## 0-5. 한국어 출력 시 콜론으로 문장 끝내지 말 것
사용자가 한국어면 출력도 한국어. 문장 끝은 `.` `?` `!` — `:` 아님.
(영어 문서 습관으로 콜론이 새어 나옴. 콜론은 코드·키밸류·라벨에만.)

## 0-6. 새 파일 첫 줄에 한국어 헤더 주석
새 소스 파일을 만들면 첫 줄에 역할을 한 줄 한국어로.
예: `// 검수 완료 항차를 archive에 백업하는 함수`. (설정 파일은 제외.)

## 0-7. 큰 작업은 계획·체크리스트·결정 노트 먼저
비자명한 작업은 코드 전에 설계를 글로 정리하고 확인받는다 (예: 화면 구조 변경).

## 0-8. 완료라고 말하기 전에 테스트 (절대 원칙)
코드를 만졌으면 "완료" 전에 검증한다.
- 이 프로젝트에선 **실데이터 시뮬레이션 PASS + `bash build.sh` 성공 + 빌드된 JS 검증**이 곧 테스트다.
- 이것 없이 "완료"라고 말하지 않는다. LLM이 가장 자주 건너뛰는 단계 — 비협상.
- **빌드 성공만으로 ZIP 제공 금지. 시각적 렌더링이 필요하면 PNG/SVG 출력으로 PASS 확인 후 진행.**

## 0-9. 의미 있는 단위로 커밋
한 논리 변경이 끝나면 버전을 올리고 변경 이유를 기록한다.
- 좋음: "archive 백업 추가". 나쁨: "백업 추가하고 UI도 고치고 버그도 수정"(3개로 쪼갬).

## 0-10. 에러를 읽어라, 추측하지 마라 (절대 원칙)
실제 에러/로그를 읽는다. 기억으로 패턴 매칭하지 않는다.
- "흔한 수정"을 원인 확인 전에 적용하지 않는다. 불분명하면 로그를 찍어 상태를 확인한 뒤 고친다.
- ⚠️ 이 프로젝트에서 반복된 실수: "캐시 문제겠지", "옛 버전이겠지"로 넘겨짚기. 코드를 끝까지 읽고 실데이터로 확인할 것.

## 0-11b. 대화 모드 vs 작업 모드 (절대 원칙 — 사용자 확정 2026-06-16)
- **대화할 때는 대화·질문·대답만.** 작업 모드로 들어가면 중간에 되묻지 말고 끝까지 작업해 결과물로 보여준다. 사용자는 결과물을 보고 수정·보완하는 방식을 선호한다.
- **사용자가 "멈춰/스톱"이라고 하면 즉시 멈춘다 (최우선·비협상).** 진행 중이던 작업이 아깝거나 클로드의 방향이 옳아 보여도, 사용자의 스톱이 우선이다. 멈춘 뒤 하던 것을 밀어붙이지 말고 사용자 말을 먼저 듣는다.
- 배경: 멈추라는데 무시하고 완성한 결과물은 99% 재수정을 거쳐야 했다. 스톱 무시는 사용자 시간을 버리는 최악의 경우다.

## 0-11. 세션 시작 버전 대조 (절대 원칙 — 재발 3건의 교훈)
**작업 시작 전에 반드시 세 가지 버전을 대조한다.**
- ① 프로젝트 지식 지침서의 "현재 버전" ② 작업 베이스(업로드 ZIP/폴더)의 APP_VERSION ③ 사용자가 마지막으로 받은 ZIP 버전.
- 셋이 어긋나면 **코드를 만지기 전에** 사용자에게 보고하고 어느 것이 최신인지 확인한다.
- 배경: 2026-06-09 세션의 수정 3건(deck-only 규칙 제거·MD 정리·tier 라벨 13px)이 별도 라인이라 6-10 세션에 전부 재발. 기록은 있었으나(6/9 ZIP 안 지침서) 프로젝트 지식이 6/6판이라 다음 클로드에게 전달되지 않았음.
- **기록의 전달 경로 (사용자 확정 운영)**:
  - 프로젝트 지식 교체는 **하루 1회, 그날 마지막 작업 판으로만** (업데이트마다 X — 같은 채팅 안에서는 클로드가 기억하므로 무의미).
  - 새 채팅에서 앱 작업 시에는 **항상 최신 ZIP을 베이스로 업로드** — ZIP 안 지침서가 그 시점의 진실을 전달.
  - **우선순위: 지침서가 둘 보이면 버전 높은 쪽이 진실** (보통 ZIP 안 지침서 ≥ 프로젝트 지식). 프로젝트 지식은 ZIP 없는 채팅(검수 업무·상담)용 베이스라인.
  - **마무리 신호는 사용자가 준다**: 사용자가 "오늘 그만한다 / 지침서 정리해 달라"고 요구하면 그때 클로드가 그날 전체 작업을 반영한 최종판 지침서를 만들어 "프로젝트 지식 교체용"으로 드린다. 중간 ZIP마다 교체를 권하지 않는다.

═══════════════════════════════════════════════════════
# Ⅰ부 — 검수 업무 (본업, 세 앱 공통)
═══════════════════════════════════════════════════════

## Ⅰ-1. 정체성
위치: 대한민국 경기도 평택시 (평택항). 역할: 화물 검수 및 선박 양하 작업 관리. 언어: 한국어.

## Ⅰ-2. 자료 분석 원칙
- 제출 자료(메인플랜·베이플랜·양하 리스트·XRAY 리스트 등)를 빠짐없이 분석할 것.
- 추론이 아닌 실제 데이터만을 기준으로 답변할 것.
- ⚠️ 가정 금지: "이렇겠지" 추측 작업 X. 실데이터 직접 확인.

## Ⅰ-3. 응답 워크플로
- 컨테이너 끝자리 4자리 호출 → 실번호 + X-RAY 대상 여부 + 선내 위치.
- 컨테이너 개수("20피트 몇 대?") → 규격별(20ft/40ft) × 상태별(Full/Empty) 상세 산출.
- 특수화물 리스트 → 베이플랜 기본, 리퍼(온도)·FR·O/T·DG 등 포함.

## Ⅰ-4. 보고 형식
명확하고 간결하며 스캐닝이 쉬운 형식. 실데이터 기반 정밀 수치.

═══════════════════════════════════════════════════════
# Ⅱ부 — 앱 A: Tallyman Master (검수앱)
═══════════════════════════════════════════════════════

## Ⅱ-1. 현재 상태
앱: Tallyman Master (greenmarinetally). 현재 버전: V8.09-14.
GitHub: greenmarine26/greenmarinetally. Firebase: greenmarinetally (asia-southeast1, Spark).
작업 디렉토리: /home/claude/v2work/ (또는 세션별 작업 폴더).
스택: React/Vite PWA + Firebase RTDB. EDI 파싱(표준 ISO 9711 BAPLIE D:95B + 비표준 `00:/50:` 페리 포맷) 기반 양하/선적 관리, 카고플랜 PDF 생성, 베이 매트릭스 빌더, PORT-MIS 연동.

## Ⅱ-2. 검증·배포 원칙 (절대 원칙)

### Ⅱ-2.1 검증
- 검증 안 된 앱은 절대 ZIP으로 주지 않는다.
- 빌드 성공 ≠ 검증. 실데이터로 흐름을 끝까지 돌려 OK일 때만 검증 OK.

### Ⅱ-2.2 작업 순서 (절대 건너뛰지 않음)
```
코드 수정 → 실데이터 시뮬레이션 → PASS → bash build.sh → 빌드된 JS 검증 → ZIP
```

### Ⅱ-2.3 빌드 전 체크리스트
- APP_VERSION 갱신 (src/utils.js 한 곳만 — sw.js·cone.html 라벨은 build.sh가 자동 동기화).
- HelpModal 사용법 등록 (새 기능·변경 시).
- 이 지침서 갱신 (작업 완료 시).

### Ⅱ-2.4 버전 표기 — 3단계 통일 규칙 (사용자 확정 2026-06-19, 절대·모든 Claude 통일)

> 배경: Claude마다 버전 올리는 방식이 제각각이었다(누구는 8.1·8.2, 누구는 8.10, 버그는 8.11_01 또는 V8.10-01…). 데이터 보관·정렬·추적이 꼬여 **한 체계로 못박는다.** 어느 Claude가 와도 아래 그대로 따른다.

**형식: `V{메이저}.{마이너 두 자리}-{빌드 두 자리}`** (예: `V8.20`, `V8.21`, `V8.21-01`)

| 변경 성격 | 올리는 자리 | 예 |
|---|---|---|
| **큰 수정**(기능 추가·구조 변화·메이저) | 마이너 **십의 자리** | V8.10 → **V8.20** → V8.30 |
| **작은 변경**(소기능·UI 조정) | 마이너 **일의 자리** | V8.20 → **V8.21** → V8.22 |
| **버그 수정·정리**(기능 불변) | **빌드 번호**(`-NN`) | V8.21 → **V8.21-01** → V8.21-02 |

**절대 규칙**
1. **마이너는 항상 두 자리.** `V8.2 ❌ → V8.20 ⭕`. 한 자리면 사전식 정렬에서 V8.2 < V8.20 < V8.21처럼 큰수정·작은변경이 뒤섞인다. (큰 수정 "8.1"로 가면 다음 작은변경 "8.11"과 정렬·의미가 꼬임 — 그래서 큰 수정은 십의 자리 올림으로 통일.)
2. **빌드 구분자는 하이픈(`-`), 두 자리** (`-01`·`-02`). ⚠️ 언더바(`_`) 금지 — build.sh의 cone.html 라벨 sed가 `V[0-9.-]*` 패턴이라 `_`를 못 잡아 라벨이 누락된다(V7.91에서 겪은 류의 버그). 하이픈이면 build.sh 무수정으로 작동.
3. **버전 기준 = `src/utils.js APP_VERSION` 한 곳.** build.sh가 sw.js·cone.html 라벨을 자동 동기화. 모든 빌드는 APP_VERSION 문자열을 바꾼다 — 안 바꾸면 폰 캐시에 반영 안 됨(불변).
4. **별개 작업은 버전 문자열을 반드시 달리한다 (절대 — 사용자 확정 2026-06-19).** 한 세션에 두 작업을 해도 같은 버전으로 두면 ① 검수원이 앱 화면에서 버전업을 **시각적으로 확인 못 함** ② sw.js VERSION이 같아 **폰 캐시가 새 빌드를 안 받음**. 마지막에 올린 것이 항상 최신 버전이 되도록 올린다(예: 부두·주야간=V8.11, 이어서 LOLO=V8.12). 알파벳 접미사(V8.11b 등) 금지 — 규칙은 일단위(작은 변경)·`-NN`(버그)뿐.
5. 캐시 무효화는 문자열이 달라지기만 하면 되므로 빌드 번호로 충분.

**판단 기준(큰/작은 갈림)**: 새 화면·새 흐름·도메인 규칙 추가 = 큰 수정(십단위). 기존 화면에 항목·옵션·표시 추가 = 작은 변경(일단위). 동작 안 바뀌고 결함만 고침 = 버그(빌드번호).

### Ⅱ-2.4b ZIP 파일명 유일화 (사용자 확정 2026-06-19)
- **압축 풀리는 최상위 폴더명·지침서 파일명은 버전 그대로** (`Tallyman_Master_V8.21/`, `…_V8.21.md`) — 사용자가 늘 같은 방식으로 repo에 덮어쓰므로 폴더명은 바뀌면 안 된다.
- **ZIP 겉파일명은 유일해야 한다** — 같은 버전을 또 받으면 파일이 덮여 데이터 보관이 안 된다. 버전 문자열만으로 부족하면(같은 V8.21을 두 번 빌드) **겉파일명에 시각을 붙인다**: `Tallyman_Master_V8.21_20260619_0130.zip`. 폴더명·지침서명엔 시각 안 붙임(겉파일명에만).
- 버그 빌드번호(`-01`)까지 버전에 박히면 ZIP명도 자연히 유일해지므로, 그 경우 `Tallyman_Master_V8.21-01.zip` 그대로 써도 중복 안 남(시각 생략 가능). 같은 버전·빌드를 재패키징할 때만 시각 추가.

### Ⅱ-2.5 ZIP 배포 메커니즘 — ★이 절차를 글자 그대로 따른다(인스턴스마다 다르게 하지 말 것)

배경: 클로드마다 ZIP 만드는 법이 달라 사용자가 반복 고생함(폴더 구조 오해, 루트 빌드본 누락 등). 아래는 단일 정답 절차다. 임의로 바꾸지 말 것.

**핵심 사실 (먼저 이해할 것)**
- repo는 **평탄 구조**다 — `index.html`·`src/`·`assets/`·`dist/`·`sw.js`가 repo 루트에 바로 있다. GitHub Pages는 `repo루트/index.html`을 직접 서빙한다.
- 그러나 **사용자에게 주는 ZIP은 폴더 한 겹으로 감싼다** — 최상위 `Tallyman_Master_Vx.xx/` 하나. 풀면 그 폴더가 나오고, 사용자는 그 폴더 **안의 내용물**을 repo 루트에 덮어쓴다. (사용자 확정 워크플로 2026-06-18.)
- ★**루트 빌드본을 반드시 ZIP에 포함**한다 — `index.html`(빌드본)·`assets/`·`sw.js`·`manifest.webmanifest`·`cone.html`·`cone-sw.js`·`cone-cargoplan.js`·`bulk_tally.html`·아이콘 PNG들·`_index.entry.html`. 이게 빠지면 사이트가 안 바뀐다(과거 사고: V8.09-06 1차 ZIP에서 루트 `assets/` 누락 → 푸시해도 화면 그대로). dist만 넣고 끝내면 안 됨.

**절차 (build.sh가 루트 빌드본을 이미 만들어 둠 — 그 뒤 그대로 담는다)**
```
# 1) 빌드 (루트 index.html·assets·sw.js·cone.html 등을 dist→root 복사까지 자동 수행)
cd <repo> && bash build.sh

# 2) 폴더로 감싸 담기 — repo 루트의 모든 운영 파일을 그대로 복사. node_modules·.git·잡파일만 제외.
VER=$(grep -oE "V[0-9.]+-[0-9]+" src/utils.js | head -1)   # 예: V8.09-06
STAGE="/home/claude/Tallyman_Master_${VER}"; rm -rf "$STAGE"; mkdir -p "$STAGE"
for d in assets dist public src; do [ -d "$d" ] && cp -r "$d" "$STAGE/"; done
for f in index.html sw.js bulk_tally.html cone.html cone-sw.js cone-cargoplan.js \
         manifest.webmanifest icon-192.png icon-512.png cone-icon-192.png cone-icon-512.png \
         mixerUpload-*.js _index.entry.html build.sh package.json package-lock.json \
         vite.config.js tailwind.config.js postcss.config.js README.md \
         평택항_검수_통합지침서_3앱통합본_${VER}.md; do [ -f "$f" ] && cp "$f" "$STAGE/"; done
rm -rf "$STAGE/node_modules" "$STAGE/src/node_modules"
find "$STAGE" -name '*#U*' -delete   # 깨진 파일명 제거(폴더 통째 복사 시 섞여 들어옴)

# 2b) ★옛 빌드 번들 정리 — repo의 assets/·dist/assets/에 옛 index-*.js가 수십 개 누적돼 있음
#     (매 빌드마다 새 해시 생성, 옛것 미삭제 → 안 지우면 ZIP이 77M까지 부풀음).
#     현재 index.html이 참조하는 번들 + css + manifest + pdf.worker만 남기고 나머지 index-*/manifest-* 제거.
CUR=$(grep -o 'index-[A-Za-z0-9_-]*\.js' index.html | head -1)
CURCSS=$(grep -o 'index-[A-Za-z0-9_-]*\.css' index.html | head -1)
CURMANI=$(grep -o 'manifest-[A-Za-z0-9_-]*\.webmanifest' index.html | head -1)
for dir in "$STAGE/assets" "$STAGE/dist/assets"; do [ -d "$dir" ] || continue
  for fp in "$dir"/index-*.js "$dir"/index-*.css "$dir"/manifest-*.webmanifest; do
    [ -e "$fp" ] || continue; fn=$(basename "$fp")
    [ "$fn" = "$CUR" ] || [ "$fn" = "$CURCSS" ] || [ "$fn" = "$CURMANI" ] || rm -f "$fp"
  done
done   # pdf.worker.min-*.mjs 등 그 외 파일은 유지(필요)

# 3) ZIP
cd /home/claude && zip -rq "/mnt/user-data/outputs/Tallyman_Master_${VER}.zip" "Tallyman_Master_${VER}" -x "*/node_modules/*" "*.git*"
```

**검증 체크리스트 (ZIP 만든 직후 전부 확인 — 하나라도 실패면 다시)**
1. 최상위 단일 폴더: `unzip -l ZIP | ... | cut -d/ -f1 | sort -u` → `Tallyman_Master_Vx.xx` 하나만.
2. 루트 `assets/` 포함: ZIP 안 `Tallyman_Master_Vx.xx/assets/index-*.js` 존재.
3. 루트 `index.html`이 그 번들 참조 + 번들에 새 버전 문자열 박힘.
4. `sw.js` VERSION = 새 버전.
5. MD = 현재 지침서 + README.md만. **깨진 파일명(`#U...`)·HANDOFF_*·구버전 지침서·REF_*·*.zip·*.bak·sim_*·repo정리* 제외.**
6. node_modules 없음.
7. ★`assets/`·`dist/assets/`에 `index-*.js`가 **각 1개**만(옛 번들 정리됨). ZIP 크기 정상(3~5M대 — 70M+이면 옛 번들 안 지운 것).
8. (가능하면) 직전 배포 ZIP과 `diff -rq`로 바뀐 파일이 의도대로인지 대조.

**제외 목록 (절대 ZIP에 넣지 말 것)**: `node_modules/`, `.git*`, `docs/`(작업메모 — 배포 대상 아님), 깨진 파일명(`*#U*` — 한글 파일명이 깨진 중복본, repo에 섞여 있음), `HANDOFF_*`, `BUILD_NOTES*`, `*REPORT*`, `EMBEDDED_*`, 구버전 지침서(`*_V8.09-0[2-5].md`·`*_3앱통합본.md` 등 최신 외 전부), `검수앱지침서.md`, `REF_*.md`(살릴 내용은 지침서에 흡수 후 제외), 이전 `*.zip`, `*.bak`, `sim_*.mjs`, `parse_def_*.py`, `repo정리.*`, `aggregate_stats_*.json`. ★`cp -r`로 폴더를 통째 담을 때 그 안에 깨진 파일명이 섞여 들어올 수 있으니, ZIP 생성 후 반드시 체크5b로 `*#U*` 잔존을 확인할 것.

- 사용자 사용법: ZIP을 풀면 `Tallyman_Master_Vx.xx/` 폴더가 나온다. **그 폴더 안의 내용물**(index.html·src·assets·dist…)을 repo 루트에 덮어쓰기 → `git add -A` → commit & push. (폴더째 repo에 넣으면 루트가 안 바뀌어 "변경 없음"이 됨 — 반드시 안의 내용물을 루트로.)
- 캐시 무효화: sw.js는 network-first, VERSION이 바뀌어야 새 버전 인식 + 옛 캐시 삭제.
- **실행 quick start**: `npm install` → `bash build.sh`. (README.md는 이 두 줄 안내만.)
- **ZIP MD 정리 규칙**: ZIP에는 현재 유효한 지침서와 `README.md`(3줄 안내판)만. **살릴 내용은 먼저 이 지침서에 흡수한 뒤 삭제(정보 손실 금지).**

## Ⅱ-3. 컨선 베이 구조 도메인

### Ⅱ-3.1 짝수 단독 베이 (정상 구조)
BOW / STERN / 선원건물 앞 / 선원건물 뒤에서 정상. 한쪽 홀수만 있어도 정상. 짝수 단독을 비정상으로 일반화 금지.

### Ⅱ-3.2 베이별 자체 row 구조 (핵심)
선박 전체 단일 row max 가정 금지. 같은 선박 내에서도 베이/section마다 row 구조 다름.
deck/hold tier도 베이별로 다름 (예: DJCT BAY 27 = deck only). 단일 rowMaxOdd/Even은 부정확.
예: DJCT BAY 01 = rowCount 7, hasZero true / DJCT BAY 07 = rowCount 10, hasZero false / SWAT BAY 17 = rowCount 11, hasZero true.

### Ⅱ-3.3 베이사전 등록 현황
v2 verified 36척, v5 매트릭스 300척. v2/v5 모두 베이별 row 라벨 정보 일부 누락.

### Ⅱ-3.4 PDF override 사전
src/data/shipBayDict_pdf_override.js. 베이별: rowCount, hasZero, deckTiers, holdTiers, (권장) deckCells, holdCells.

### Ⅱ-3.5 ISO 6346 사이즈
첫 자: 2=20ft, 4=40ft (45R1 = 40ft hi-cube reefer), L/9=45ft. ⚠️ 45R1을 45ft로 분류 금지.
- 둘째 자리=높이(2=8'6", 5=9'6" HC). 길이 판정은 라벨(isoToLabel) 기준이 정답. 45GP→40HC, 45R1→40RF. 진짜 45ft는 L5xx.

### Ⅱ-3.6 cells = 00 포함 개수 (핵심)
- 사용자 매트릭스 cells = **00 포함 전체 칸 수**. 데크00 체크 = cells 안에 이미 00 포함.
- ⚠️ `+1` 금지 (00 두 번 세면 없는 09가 생김).
- ⚠️ 개수로 row 번호를 생성하지 말 것. 실제 row 라벨을 쓴다. ("갯수 보지 말고 실제 row 번호를 봐라")

## Ⅱ-4. 카고플랜 V2 약속

### Ⅱ-4.1 베이 구조
- **베이플랜은 좌표(absolute) 기반 — 격자(flex) 금지**: 격자는 한 칸 빠지면 옆 셀이 밀림. 각 셀이 자기 (row,tier) 좌표 자리를 가져야 함. 데크/홀드는 끝까지 따로 관리.
- **cells 해석을 베이플랜·카고플랜 간 함부로 통일하지 말 것**: 두 화면은 렌더 모델이 달라(좌표 vs flex) 한쪽 기준으로 "통일"하면 다른 쪽이 회귀. 통일 필요 시 실데이터 렌더로 양쪽 검증 후.
- PDF STOWAGE INSTRUCTION 정답 그대로. 베이마다 자체 deck/hold tier + row. 단독 베이 hold 없으면 hold 영역 없음. A4 한 장. 좌측 하단 mini-legend.

### Ⅱ-4.2 마크 약속
(a) PTK 일반 = 선사별 컬러 + o / (b) PTK 특수(R/D/F/T/A/E) = 글자 + 컬러 / (c) 통과 일반 = 회색 배경, 글자 없음 / (d) 통과 특수 = 글자 + 회색 / (e) 빈 슬롯(hull 안쪽) = border만 / (f) 짝수 40·45ft shadow = X(흰 배경) / (g) 짝수 20ft shadow = 회색 빈 셀(자리 차지) / (h) Hull 바깥 = 안 보임(visibility:hidden).
- ⚠️ X는 짝수 40/45ft shadow 전용. 통과화물 표시로 쓰지 말 것.
- ⚠️ **셀 배경색으로 XRAY·선사를 구분하지 말 것 — 구분은 글자(표기) 색으로만** (배경색이 XRAY 색과 혼동돼 작업 중단 유발).

### Ⅱ-4.3 컬러 시스템
양하 모드: 선사(c.op)별 10색. 선적 모드: POD 3자별. ⚠️ c.cn 첫 3자 BIC 코드 = 선사 X. 사용 금지.

### Ⅱ-4.4 Hold 중앙 정렬
CSS: width (nHoldCols/nDeckCols)*100% + margin auto. 1칸 차이 → 좌우 0.5칸씩.

### Ⅱ-4.5 Layout 공식
N개 박스 → 상단 = ⌈(N+1)/2⌉, 하단 = N − 상단, 별첨 자리 = 차이.
- **【확정】 hold 유무와 상관없이 베이 번호 순서가 절대 우선.** deck-only라고 순서를 깨고 내리면 BAY 01이 하단으로 밀림(TMPZ 재현). SWRG의 33·35·38이 하단인 것은 번호가 커서이지 deck-only라서가 아님 — 과잉 일반화였음. (deck-only 하단배치 규칙은 폐기됨.)

### Ⅱ-4.6 카운트 포맷
단독 = 총합 1숫자. 페어 = 20'/40'/45' 슬래시. 양하 = 평택분만.

### Ⅱ-4.7 인쇄 처리
height 195mm 고정. page-break-inside: avoid. body 직접 마운트(createPortal). display:none + bg white.

## Ⅱ-5. 데이터 검증·명칭 원칙

### Ⅱ-5.1 출처 우선순위 (절대 — 사용자 확정)
`사용자 매트릭스(검증) > PDF override > .def 사전(미검증) > v5_matrix.matrixBays > v2 baysSummary > EDI 실데이터`
- 근거: 사용자가 직접 검증한 데이터만이 확정. .def 사전은 자동 디코드일 뿐 사용자 검증을 거치지 않았으므로 사용자 매트릭스를 절대 덮지 못한다.

### Ⅱ-5.2 v5_matrix.cells 주의
순서 아래→위 → reverse() 필수. v5가 PDF와 불일치 가능. 일부 선박 v5 없음.

### Ⅱ-5.3 EDI 검증
has_zero = EDI rows에 0 있는지 직접. 추측 금지, 실데이터 보고 판단.

### Ⅱ-5.4 CASPI가 정답 기준
CASPI를 따른다. 잘못된 명칭 사용 금지. 사용자가 실제 쓴 약자·명칭만 인용.

### Ⅱ-5.5 선사 약자 3자 (영구 규칙)
선사 약자는 항상 3자. 4자 코드는 변환표 경유 3자(단순 slice 금지).
예: DJSC→DJS, NSSL→NSL, HASL→HAS, SNKO→SKR, EASK→EAS, TJMS→TJM, WDFC→WDF, DWIC→DWS.

## Ⅱ-6. userBayDict / 매트릭스 보호 (절대 원칙)
- **userBayDict 절대 보호**: source !== 'user'일 때만 mergeBayDef. 사용자 입력을 자동분석(v5/v2/EDI)이 덮어쓰지 않는다.
- **베이매트릭스가 기본 진실**: 매트릭스에 deck00/hold00이 명시되면 EDI가 덮어쓰지 않는다.
- **Rules of Hooks**: 모든 Hook은 최상단. 조기 반환문은 모든 Hook 선언 이후.
- **음성 출력 표준**: 컨테이너 번호는 한 글자씩, 베이·온도는 한글로 또박또박.
- **데크/홀드는 항상 별도 관리(축 통합 금지)** — 통합 시 phantom 00 생성.

## Ⅱ-7. 운영 핵심 원칙 (V7.x 확립)

### Ⅱ-7.1 평택분만 계산
- 항상 평택분만: 양하 = POD가 평택, 선적 = POL이 평택.
- 평택 판정 변종 포함: `PTK / PYT / PYOTM / PYO`로 끝나면 평택. **isPyeongtaekPort() 단일 함수로 통일** (적재·집계·모드판정·진단 전 화면이 이 함수 하나만 사용. firebase 한글 항구명 케이스만 예외).
- ⚠️ 폴백 금지: 평택분 0이면 0이 정답. 전체로 넘어가 타지역까지 세지 않는다.
- ⚠️ **모드별 판정 필수**: `isPyeongtaekPort(c.pol) || isPyeongtaekPort(c.pod)` 패턴은 모드 무관이라 버그 — 양하 EDI의 평택발 타항행(POL=PTK)이 평택분에 섞임. 이 패턴을 보면 전수 grep (과거 5곳에 복제돼 있었음).

### Ⅱ-7.2 데이터 유실 방지 (archive)
- 완료 시 `archive/{voyageKey}`에 실데이터 전체 백업 → 백업 검증 성공 후에만 삭제(fbDeleteVoyage). 백업 실패 시 삭제하지 않는다.
- 복원: fbRestoreVoyageFromArchive. 1년 후 자동 정리: fbCleanupArchive.

### Ⅱ-7.3 이중 확인 (검수사 → 수석)
- 검수사가 완료 누르면 `inspectorDone` 표시만(삭제 안 함). 수석 확인 전 검수사 취소 가능.
- 수석이 대시보드에서 최종 "완료 저장" 누를 때만 archive 백업 + 보관소 기록 + 삭제.
- 목적: 2명이 나눠 작업 중 한 명이 자기 자리 완료를 전체 완료로 잘못 눌러 삭제되는 사고 방지.
- **완료 권한 보유자 7명**: 이현규·김명보(부장 수석검수), 오승택(차장 수석검수), 성창모·이강익(과장 수석검수), 전우수(대리 수석검수), 김성일(대리 부수석). isChief 정규식은 `/수석검수|부수석/`.

### Ⅱ-7.4 라이브러리 / 보관소 분리
- 선박 라이브러리(진행 상황) = 현재 살아있는 voyages만. 완료 안 한 배만 보임. 수석용 [완료 저장] 버튼.
- 선박별 자료 보관소(완료 기록) = ships 노드, 최근 완료순(completed_at desc). 항차별(양하/선적/일자) + 누적.
- 보관소 제목은 사용자 약자(vsl, 예 PCBJ/TNJP) 우선.

### Ⅱ-7.5 계열 대체 시 신원 금지
- 베이 구조 없어 다른 선박 사전을 빌릴 때(series-substitute), 베이 구조만 빌리고 콜사인·선박명은 빌리지 않는다.
- ⚠️ 앞 2글자만 같으면 계열로 보는 대체는 느슨함. 무관한 선박 신원(콜사인)이 섞이면 PORT-MIS 오매칭(DJCT에 XIN TAI PING 표시된 버그).

### Ⅱ-7.6 알려진 운영 이슈
- **런타임 에러 발생 시 카메라 작동 멈춤** — 에러 핸들링/카메라 복구 작업 시 반드시 고려. (V7.35 루트 ErrorBoundary로 완화: main.jsx에서 `<ErrorBoundary reloadButton>`로 App 전체를 감쌈.)

## Ⅱ-8. 검수앱 작업 이력 (최신 → 과거)

> 콘앱(cone.html) 단독 작업은 Ⅲ부에, 검수앱+콘앱 동시 작업은 양쪽에 요약 표기한다.

### V8.40 + 수집기 v2.15.0 — 품질 게이트·항차 건강 대시보드·하트비트 (2026-07-03, 사용자 확정)

PCSZ 규격 누락 사건의 재발 방지 세트. 사용자 확답 3건: 게이트는 **올리되 '검증 필요' 플래그**(자료는 일단 보아야 한다) · 대시보드는 **홈 요약 + 상세 페이지** · 하트비트 끊김 기준 = **사이클 주기의 2배**.

**수집기 v2.15.0**
- 품질 게이트(autoreg.py `_qc_flags`): 업로드는 하되 이상이면 `voyages/{key}/qc/{mode} = {flags, at, ediFile}` 기록, 정상이면 null PUT으로 지움. 규칙 ① 빈규격 10% 초과(PCSZ) ② 리스트가 EDI보다 15% 초과 '부풂'(XTPG 305) — 리스트 부족은 수집 진행 중 정상이라 플래그 안 함(과잉 경보 방지, 시뮬로 확인).
- 하트비트(`write_heartbeat`): collect_week 끝에서 autoreg 성패 무관(try/finally) `collector_heartbeat = {at, cycleMin, autoreg:{done,flagged}, version}` PUT. cycleMin은 GUI 감시/완전자동 시작 시 `cfg["cycle_min"]`으로 주입.
- 검증: py_compile 3파일 PASS(⚠ C:\TALLYTEST 마운트가 스테일 절단 뷰를 줘 outputs 사본으로 검증 — 마운트 불신 시 호스트 Read가 진실), QC 시뮬 PASS(PCSZ=빈규격만·XTPG=수량불일치만·정상=무플래그).

**검수앱 V8.40**
- `src/health.js`(신규): 항차별 EDI/리스트/빈규격/부풂 계산 — 규칙은 수집기와 동일, 수동 업로드 항차도 같은 기준으로 검사. 수집기 qc 플래그도 병합 표시.
- `pages/HealthPage.jsx`(신규, #/health): 하트비트 카드(정상/끊김/기록없음) + 이상 요약 + 항차별 점검표(클릭→항차 이동).
- 홈 스트립: 3버튼 그리드 아래 — "수집기 정상·N분 전 / 끊김 / 기록 없음" + "검증 필요 N건 / 자료 정상", 클릭→점검 페이지. 30초마다 경과 재계산.
- `firebase.js` fbSubscribeHeartbeat 추가, App.jsx에 health 라우트(해시 #/health).
- 검증: build.sh PASS·번들 grep(V8.40·새 문구 4종)·jsdom 렌더 2종 PASS(HealthPage 9항목, 홈 스트립 끊김 표시·클릭 라우팅).
- ⚠ 주의: 대시보드는 앱이 이미 구독 중인 voyages 전체 트리로만 계산 — 추가 fetch 없음. 모드 키는 discharge/**loading**(load 아님).

### V8.35-01 — 자동 등록 EDI 선택 버그 (사용자 발견: PCSZ 카고플랜 규격 누락)

증상: 카고플랜 일부 규격 누락(빈 규격 180대). 원인: 폴더에 ASC와 BAPLIE가 둘 다 있고 컨 수 동률이면 알파벳순 첫 파일(ASC, 규격 일부 누락)이 채택됨. EDI·파서·카고플랜은 전부 정상 — 자동 등록의 최적 파일 선택 규칙 결함. 수정: 동률 시 규격(iso) 보유 수 우선(사실상 BAPLIE 우선). 시뮬 PASS(규격 없는 파일이 먼저 와도 BAPLIE 채택·빈 규격 0). autoreg_state 초기화로 전 항차 재업로드 유도. 진단 경로 기록: 원본 파싱 정상 → 실렌더 정상 → Firebase 저장본 이상 → raw fileName으로 ASC 확정.

### V8.35 + 수집기 v2.14.1 — 통보(신호) 시스템 폐지 (2026-07-03, 사용자 확정)

자동 항차 등록이 통보를 완전히 대체하므로 제거. **검수앱**: collector_signal 구독(fbSubscribeCollectorSignal)·등록 대기 카드(pendingSignals/dismiss/localStorage gm_dismissed_signals_v1) 전부 삭제. **수집기**: collect_week의 notify_ready 호출 삭제(함수 정의는 사장), GUI 신호 삭제 버튼 제거. **Firebase**: collector_signal 노드 삭제 완료. 검증: 홈 렌더 PASS(신호 카드 부재·자동 배지 정상), 빌드·참조 0건 grep, py_compile. ⚠ XTPG 양하 EDI 도메인: 이 배는 평택 양하 BAPLIE가 따로 없고 **인천 출항 EDI(XTPGnnnnW INC(2.2).EDI, Pan Asia Tally 이승철 nsclee7@naver.com 발신)**가 그 역할 — 제목 "V-05nnW Final Load Documents At KRINC", 수집기가 내용 판정→TDT W→E 교정으로 자동 처리(KKAK INCPA와 동일 계열).

### V8.33 + 수집기 v2.14.0 — RZOR 가상 EDI(IFCSUM) + 업로드 토큰 방어 + 오염 대청소 (2026-07-03 오전, 사용자 확답 4건)

**배경(사용자 신고)**: ①RZOR은 BAPLIE EDI가 없는 배(IFCSUM 매니페스트만) ②MAILBOX 선박 폴더에 안 맞는/지난 항차 자료 혼입. 감사 결과 RZOR/R070E·R070W 폴더에 타선박(KKAK·KKLC·KSKM SI, TNJP 옛 컨리스트) 66개 혼입 — 7/2 00:00 유입(자기치유 라우팅 v2.12.0-42 이전 잔재). Firebase엔 RZOR_R070E에 가짜 EDI 877·리스트 2296 등 오염.

**정리(확답 후 실행)**: Firebase — RZOR_R070E 양하/선적 데이터 삭제, SWCP 선적리스트 1113·TNJP 양하 262·XTPG 양하 160·OBWH_2676E 선적 250 삭제, PCSZ_261E(오타 중복) 항차 삭제(전 항차 검수기록 0 확인 후). MAILBOX — R070E 오분류 64개·R070W 폴더 전체 드라이브 휴지통(30일 복구 가능).

**V8.33(검수앱)**: autoRegApi에 `parseIfcsum` — IFCSUM 12:(POL)·13:(POD)·51:(컨번호/실번호/규격/F\/E/무게) 세그먼트를 가상 EDI 컨테이너로. 실파일 시뮬 124/124 PASS(전량 POD 평택). LOLO라 선내 위치 없음(정상). 헬퍼 재번들.

**V8.33-01 — 가상 EDI에 리스트 동시 생성 (사용자 지적: RZOR 매칭 0)**: IFCSUM은 실번호·무게를 담고 있으므로 ediContainers만 만들면 records가 비어 매칭이 안 잡힘 → 가상 EDI일 때 records(cn/sl/wt)도 함께 생성. RZOR_R070E는 장부의 가상 EDI에서 즉석 생성해 124건 반영 완료(라이브 확인). 헬퍼 V8.33-01 재배포.

**v2.14.0(수집기)**: autoreg 업로드 파일 토큰 검증 — 타선박 코드 토큰 제외, 항차 토큰 불일치 제외, 엑셀류(합본 제외)는 일치 토큰 필수. 시뮬 PASS(진짜만 선택·합본 우선 유지). 상태 v4(전체 재평가). ⚠ 다운로드 라우팅 자체는 v2.12.0-42의 자기치유(_route_foreign_by_filename, 컨번호 실측 대조)가 이미 있음 — 재수정 안 함.

### V8.32 — 수집기 자동 항차 등록 연동 (2026-07-02, 시뮬 3종+렌더 PASS — 사용자 확정 설계)

**설계(사용자 확정)**: 수집기가 MAILBOX 자료 도착 즉시 검수앱에 항차를 **가등록(수집중)** + EDI 업로드하고, 기존 완료 판정(선적 녹색)이 떨어지면 **확인 업로드 후 확정** 전환. 등록 조건은 무조건이 아니라 수집기 신호 조건과 동일(관할 선박만·마감/출항/터미널완료 제외·최신 항차만·등록부 선박만 — 유령 선박 교훈).

**검수앱 쪽(V8.32)**
- `src/autoRegApi.js` 신설 — `window.GMautoPayload(files,{vslCode,voy,mode,phase})` → `{key, info, ediContainers, ediRaw, records, counts}`. 파싱·분류(평택 POD/POL→discharge/loading/transit)는 VoyagePage 재처리 로직과 동일. **Firebase 쓰기는 안 함**(순수 함수) — 쓰기는 수집기 REST 담당.
- HomePage 항차 카드에 배지: `info.autoRegistered` + `autoStatus`(collecting→"🤖 자동(수집중)" 호박색 / confirmed→"🤖 자동(확정)" 초록).
- 검증: 실 EDI(SWCP 2606N, 843대) 페이로드 시뮬 PASS(843/843·평택 238), 빌드 9.9초, 번들 grep(GMautoPayload·배지 문자열), jsdom 렌더 PASS(두 배지+항차쌍 표기).

**V8.32-01 + 수집기 v2.13.0-03 — 라이브에서 잡은 결함 3건 (2026-07-02 밤, XTPG 305 사건)**
- 증상: 터미널 계획 선적 164대인데 자동 업로드 리스트가 305대. 원인 ①: 폴더의 모든 엑셀(VGM·컨번호리스트·MEMO BL 등)을 합집합 — 수집기가 이미 평택 기준으로 검증한 합본(…LOADLIST.xlsx, 140대)은 오히려 제외하고 있었음. → 수집기 -03: 합본이 있으면 합본만 사용.
- 원인 ②: 합본은 GMmerge 산출물(MERGED 시트, 'Cntr No' 헤더)이라 검수앱 parseListExcel이 0건 반환. → V8.32-01: GMautoPayload에 합본 전용 매핑(Cntr No/Seal/EmptySeal/Weight→cn/sl/eseal/wt) 추가, 헬퍼 재번들.
- 원인 ③(OBWH +250 사건): 폴더상 최신 선적이 지난 항차(2658W)면 그 옛 리스트가 현재 항차에 붙음. → 수집기 -03: 터미널 현재 짝보다 _vnum이 작은 폴더 항차는 제외(미래 항차는 허용 — RZOR R070E 같은 선도착).
- 오염 정리: XTPG_0530E/loading/records(305)·OBWH_2679E/loading 삭제(생성 직후라 검수 기록 없음 확인 후, 브라우저 REST DELETE). 상태 _STATE_V=3으로 전체 재평가.

**수집기 쪽(v2.13.0) — 별도 코드베이스지만 연동 계약이라 여기 기록**
- `collector/autoreg.py` 신설 + `collector/autoreg_helper.html`(V8.32 파서 번들, GMautoPayload 노출) + `auto.py` 수집 사이클 끝 훅 + `config auto_register`(기본 켜짐, false로 끄기).
- 키 결정: MAILBOX 폴더가 곧 선박코드/항차. 기존 항차(양하/선적 어느 키든) 있으면 재사용 — 수동 등록과 충돌 없음. 없으면 양하 우선 `{코드}_{양하항차}`.
- **보존 원칙(절대)**: info는 없을 때만 생성(있으면 voy_d/voy_l 빈칸 채움만, 수동 항차에 autoStatus 주입 금지). ediContainers/raw는 EDI 진실이라 교체. records는 **보수 머지**(신규 컨 추가+빈칸 채움만 — 검수 입력 절대 불변). completed 등 다른 경로 불가침.
- 중복 방지: 항차·모드별 파일 지문(autoreg_state.json, %LOCALAPPDATA%\TallymanCollector) — 같은 자료·같은 단계면 재업로드 안 함.
- 검증: 모의 MAILBOX/REST로 run() 시뮬 PASS — 조건 필터(마감·비관할 제외), 단계 판정(녹색→확정), 키 재사용(수동 항차 info 불가침), 보수 머지(기존 실번호·검수 입력 보존), 재실행 중복 0건. **미검증(PC 필요)**: CDP 크롬 실호출·Firebase 실쓰기 — PC 라이브 스모크 필요.
- 범위 제외(다음 과제): XRAY 리스트 자동 업로드(파일 판별 규칙 미확립), records 자동 업로드는 확정 단계에만.

### V8.31 — 작업 보고 해치커버 장수 수동 선택(1~3장) + 빌드 폭주 결함 수정 (2026-07-02, 시뮬·렌더 PASS)

**① 해치커버 자동계산 제거 → 수동 선택 (사용자 지시)**
- 대상: 작업 보고(WorkReportModal)의 해치 화면만. 가이드 흐름(GuidedWorkPanel)의 자동계산은 불변.
- WorkReportModal에서 `hatchPanelsOf`(매트릭스 hatchCount 합산 자동계산, V7.94-22) 제거. 베이 입력칸 아래 "장수 선택" 1장/2장/3장 버튼 추가(기본 1장), 보고 메시지 `총 N장`은 선택값 그대로. fbAddWorkReport 기록에 `panelCount` 포함.
- 검증: 시뮬 PASS(베이 3개 입력+수동 1장 → "총 1장" 등 3케이스), jsdom 실물 렌더 PASS(해치 화면 진입·1→2장 전환·옛 자동 힌트 제거 확인), 번들 grep("장수 선택" 1건).

**② 빌드 수 분 걸리던 진범 수정 — Tailwind가 1.2MB 단일라인 사전을 스캔 (V8.31 동봉)**
- 증상: `bash build.sh`가 3분+ (V8.30 기록 "3m 27s"), 코워크(45초/명령 제한)에서는 빌드 불가 판정까지 났음.
- 원인(프로파일로 확정): CPU 95.6%가 Tailwind 클래스 추출 정규식. content 글롭 `./src/**/*.{js,jsx}`가 `src/data/shipBayDict_v5_matrix.js`(단일 라인 1,248,716자) 등 데이터 사전까지 스캔 → 정규식 파국적 역추적.
- 수정: tailwind.config.js content에 `'!./src/data/**'` 제외 추가(데이터 파일에 Tailwind 클래스 0건 확인 후). 빌드 3분+ → **6.4초**.
- ⚠ 파급: **코워크에서도 검수앱 빌드 가능해짐** — "코워크 빌드 불가(45초 제한)" 결론(2026-07-02 오전)은 이 결함이 원인이었으므로 폐기. 단, 코워크 백그라운드 프로세스는 호출 종료와 함께 죽으므로(die-with-parent) 빌드는 한 호출 안에서 끝내야 함(이제 6초라 문제 없음).

### V8.30 — 수집기 대기 카드 치우기(로컬 숨김) (2026-07-01, 시뮬 5/5 PASS)

**요청**: 홈 상단 초록 대기 카드(`pendingSignals` = 메일수집기 `collector_signal/{이름}` 신호)를 사용자가 임의로 치우는 기능. 지난 항차거나 이미 등록된 항차라 화면에서 안 보이게 하려는 것.
**사용자 확정 규칙**: ① 검수앱 화면에서만 숨김(Firebase 수집기 신호 원본 불변). ② 되살아나도 숨김 유지. ③ **단, 같은 배라도 다른(새) 항차번호로 자료가 오면 다시 보여야 함.**
**설계(핵심)**: 숨김 기준을 신호 하위 키(`_key`)가 아니라 **항차번호 조합 sig**(`norm(dischargeVoy)+'|'+norm(loadVoy)`)로 잡음. `_key`로 숨기면 같은 배 새 항차 자료도 계속 숨겨져 규칙 ③ 위반. sig로 하면 항차번호가 바뀌면 sig가 달라 자동 재노출. `norm`은 기존 `pendingSignals`(340행) 정의 재사용.
**수정** (`HomePage.jsx`만, 외과적): ① `dismissedSigs` useState + `localStorage 'gm_dismissed_signals_v1'` 로드. ② `pendingSignals` useMemo에 `!dismissedSigs.has(sig)` 필터 한 줄 + 신호에 `_sig` 부착(deps에 dismissedSigs 추가). ③ `dismissSignal(sig)` 핸들러(Set에 추가 + localStorage 저장). ④ 대기 카드(410행)에 우상단 X 버튼(`lucide-react` X import 추가). **firebase.js·다른 파일 불변.** 등록 시 숨김(기존 349행 existingVoys 로직)과 OR 공존 — 충돌 없음.
**시뮬(5/5 PASS)**: ①초기 전부표시 ②A치움→B만 ③되살아나도 숨김 유지 ④SITC MOJI 새 항차(2632W→2634W)→sig 다름→재노출 ⑤등록(voy_l 일치)→기존 로직 숨김.
**검증(번들 PASS)**: `bash build.sh` 성공(`✓ built in 3m 27s`), 번들에 "이 대기 카드 치우기"·`gm_dismissed_signals_v1` 박힘, V8.30 index.js·sw.js·cone.html 동기화. HomePage.jsx esbuild JSX 구문 검증 통과.

### V8.29 — 인천발 항차 오프셋 EDI 경고 흡수 (2026-07-01, 가드 시뮬 5/5 PASS)

**배경(실사고)**: PEGASUS PROTO(콜사인 V7A5452) 양하에서 리스트 259대는 붙었는데 EDI 평택=0, 매칭=0. 원인 규명 = 데이터·파서·필터 전부 정상. EDI 항차 `2608S` ↔ 앱 등록 `2607N`. V8.28 `_voyCore`는 방향(N/S/E/W)만 무시하고 **번호는 정확 비교**라 `2608≠2607`에서 EDI가 차단됨 → 그 항차 `ediContainers`가 비어 화면 0. X-RAY·리퍼온도 경고도 전부 이 하나의 하위 증상.
**도메인 규칙(사용자 확정 2026-07-01)**: 인천 출발 선박은 평택 항차가 인천 항차보다 **한 항차 낮고 방향이 바뀐다**(`2608S`→`2607N`). 번호가 정확히 안 맞는 게 정상. **사용자 등록 항차가 진실.** 콜사인이 맞고 EDI에 평택 화물이 보이면 자동 흡수가 아니라 **경고(모달)** 를 띄워 사용자가 흡수 여부를 결정.
**수정** (`VoyagePage.jsx` 1617~ 항차 불일치 분기): 즉시 `continue` 하지 않고, `_ptkLeg > 0`(EDI에 평택분 존재) AND `_csComparable`(양쪽 콜사인 비교 가능)이면 `askChoice` 모달("항차 번호가 다릅니다") 발동. "흡수" 선택 → 가드 통과(정상 병합 흐름 진입, 등록 항차로 흡수), "적용 안 함" → `continue`. **콜사인 불일치·평택분 0·콜사인 비교불가는 기존대로 즉시 차단**(선박명만으로 흡수 승격 안 함 — 오매칭 방지). `_voyCore` 정의·방향 무시 로직은 그대로 유지.
**시뮬(5/5 PASS)**: ①실케이스 `2608S+2607N` 콜사인같음 평택259 → modal ②콜사인 다름 → block(ship) ③항차 같음 `2608==2608` → pass ④항차 다르고 평택분 0 → block(voy_no_ptk) ⑤콜사인 비교불가+항차다름+평택있음 → block(voy_ptk_no_cs).
**검증(번들 PASS)**: `_voyCs`/`_ediCs`(1596)·`askChoice`(1387 컴포넌트 스코프) 접근 정상. 이미 이 업로드 루프에서 `await file.text()`·`await askChoice`(1762) 쓰는 async 컨텍스트. `bash build.sh` 성공(`✓ built in 3m 11s`). 번들에 새 문구 3종("항차 번호가 다릅니다"·"인천발 항차 오프셋"·"로 흡수") 각 1건 박힘, V8.29 index.js·sw.js·cone.html 동기화.
**주의**: EDI 정정본 생성 불필요 — 앱이 등록 항차로 흡수. 파일명·TDT 항차 안 고쳐도 됨.

### V8.28 — EDI 항차 검증을 '번호 기준'으로 완화 (2026-06-28, 시뮬 6/6 PASS)

**배경**: V8.24-03 항차 가드가 방향문자까지 엄격 비교라, 인천 출발 선박처럼 선사가 왕복을 한 항차(예 `0529W`)로 표기하면 평택 양하분(`0529E`)이 "다른 항차"로 오차단됨.
**수정** (`VoyagePage.jsx` 1616~1617): 항차 비교를 '번호 기준'으로 완화. `_voyCore` 헬퍼 신규 — `_norm` 후 끝의 방향문자 `[EWNS]+$` 제거 + 앞 0(`^0+`) 제거, 빈 문자열이면 `_norm` 폴백. 비교를 `_norm` → `_voyCore`로 교체. leg(양하/선적)는 위에서 POD/POL 내용으로 이미 확정하므로 방향문자를 떼도 안전. 같은 번호면 통과(`0529`==`0529`), 번호 다르면(`0530` vs `0529`) 그대로 차단.
**시뮬(6/6 PASS, Claude 교차검증 완료)**: `0529W↔0529E` 통과 · `529E↔0529E` 통과 · `0530E↔0529E` 차단 · `R067W↔R067E` 통과 · `R068E↔R067E` 차단 · `2613S↔2613N` 통과. (추가 경계 `0000E↔0000W`는 폴백 `_norm`으로 떨어져 보수적 차단 — 번호가 통째로 0인 비현실 케이스라 현장 무영향.)
**검증(번들 PASS)**: `_norm`(1594)·`_regVoy`(1612)가 `_voyCore`(1616) 위 정의 — 스코프 정상. esbuild 정합 OK. vite build 성공(`✓ built in 4m 33s`). 번들에 V8.28·핵심 정규식(`EWNS`) 박힘, 누적기능(색맵·앞뒤맞교환·공지) 유지, 줌 회귀 없음(3화면 `useState(1)`). sw.js·cone.html V8.28 동기화.
**주의**: 수집기(TallymanMailCollector)는 미수정(사용자 확정: 검수앱만). 선박명 가드 등 기존 안전장치 그대로 유지.

### V8.27 — 수석 공지(흐르는 띠) + 딥링크 크래시 수정 + 콘앱 캐시 + 범례 (2026-06-24, 번들검증 PASS)

**[④ 수석 공지 — 신규 기능]**: 수석 대시보드에 "검수원 공지" 작성칸(보내기/내리기/읽음 인원·명단). 검수앱은 로그인된 검수원의 모든 화면 상단에 광고판처럼 흐르는 띠(marquee)로 표시, "확인" 누르면 그 사람 화면에서 사라짐(읽음). 새 공지 오면 다시 뜸. 실시간(Firebase `broadcast/current`, `reads/{id}/{검수원}`). 신규 파일 `components/BroadcastMarquee.jsx`, `firebase.js` 함수 5개(`fbSubscribeBroadcast`/`fbSetBroadcast`/`fbClearBroadcast`/`fbMarkBroadcastRead`/`fbSubscribeBroadcastReads`), `ChiefDashboard.jsx` 작성칸, `App.jsx` 마퀴 마운트.
**[① 딥링크 크래시 수정]** (V8.26-01 대기 항목 해결): 항차 주소 직접 진입(`/#/voyage/...`) 시 VoyagePage가 데이터 늦로딩으로 hook 순서 어긋나 React #310 크래시. → `App.jsx`에 `voyagesLoaded` 게이트 — voyages 로드 전엔 VoyagePage 미마운트하고 "불러오는 중/없음" 표시, 로드 후에만 마운트. 정상 경로(홈→항차) 영향 없음.
**[② 콘앱 카고플랜 캐시]**: V8.26 카고플랜 변경(XRAY 붉은별·PC 줌·배치)이 `cone-cargoplan.js`에 빌드되지만 콘앱은 `?v=__APPV`로 캐시. `cone.html` `__APPV` C7.65 → C7.66 상향해 콘앱도 새 카고플랜 받게 함.
**[③ 베이플랜 범례]** (`BayPlan.jsx`): 새 색 체계에 맞게 "흰 배경 · 글자색=선사(양하)/POD(선적) 지정색 · ★붉은별=X-RAY · 검정=비평택"으로 교체(옛 'POL색/노랑ring/X-RAY보라' 문구 폐지). 범례 블록만 변경, 줌·xrayList fix 미변경.

**검증(번들 PASS)**: esbuild App 진입점(신규 BroadcastMarquee→firebase 연결) + Marquee 단독 정합 OK(pdfjs는 외부 패키지). vite build 성공(`✓ built in 3m 41s`), cone-cargoplan.js(1.6M) 재생성·루트/dist 복사. 번들에 V8.27·`fbSetBroadcast`·`검수원 공지`·`흰 배경`(범례)·누적기능(색맵·앞뒤맞교환·붉은별X-RAY) 박힘. 줌 회귀 없음(베이상세·카고플랜V2 `useState(1)` 유지). cone.html C7.66, sw.js·cone.html V8.27 동기화.
**미검증(브라우저 확인 요망)**: 공지 띠 흐름·"확인" 읽음 처리·딥링크 정상 진입·범례 표시는 배포 후 브라우저에서 확인.

### V8.26-02 — 화면별 줌 기본값 정정 (2026-06-24, 번들검증 PASS)

**증상**: V8.26에서 카고플랜·베이상세까지 전부 22%로 시작됨. 본래 기본값은 카고플랜 100%·베이상세 100%·베이플랜만 22%.
**원인**: V8.25 핀치 줌 도입 시 카고플랜(기존·V2)·베이상세 줌 초기 `useState`를 일괄 `0.22`로 둠. 베이플랜(큰 베이가 한 화면에 안 들어와 22% 필요)과 달리 카고플랜·베이상세는 100%가 맞음.
**수정**: 줌 초기값 — `PrintableCargoPlan.jsx`·`PrintableCargoPlanV2.jsx`·`PrintableBayDetail.jsx` `0.22` → `1`(100%). `BayPlan.jsx`는 `0.22`(22%) 유지. (줌 clamp는 세 화면 모두 `0.15~3`이라 100% 정상 범위.)
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 1m 44s`). 소스 — 베이플랜 0.22 유지, 나머지 3개 `useState(1)` 확인. 번들에 V8.26-02·누적기능 박힘, sw.js·cone.html 동기화.

### V8.26-01 — 베이플랜 그리드 크래시 수정 (V8.26 긴급 버그픽스) (2026-06-24, 번들검증 PASS)

**증상**: 양하/선적 항차에서 "베이" 탭 클릭 시 앱 전체 크래시. 에러 `xrayMap is not defined`(React #310은 2차 증상).
**원인**: V8.26에서 XRAY 붉은별 코드를 `BayPage` 컴포넌트 셀 렌더에 넣으며 `xrayMap`을 참조했으나, BayPage는 그 맵을 `xrayList` prop명으로 받음(부모 BayPlan이 `xrayList={xrayMap}`로 전달). → ReferenceError로 렌더 중단.
**수정** (`BayPlan.jsx`): BayPage 셀 렌더의 별표 조건 2곳(1602·1607행) `xrayMap` → `xrayList`로 교정. (BayPlan 자체 스코프의 `getOpColor`·`cellColor` 내 `xrayMap`은 정상 변수라 그대로 둠.)
**검증(번들 PASS)**: BayPage 셀 렌더(1600~1610)에 `xrayMap` 잔존 0 확인. esbuild 정합 OK. vite build 성공(`✓ built in 4m 38s`). 번들에 V8.26-01 박힘, V8.26 기능(색맵·황금각137.5·PC줌 ctrlKey 20·앞뒤맞교환·⇅·bd-r1) 전부 유지. sw.js·cone.html V8.26-01 동기화.
**대기 중(별개 버그)**: 항차 딥링크(`/#/voyage/...`) 직접 진입 시 VoyagePage 데이터 늦로딩으로 hook 순서 어긋나 크래시(React #310). 정상 경로(홈→항차 선택)는 정상. 요청 시 별도 수정. → **V8.27에서 해결**(App.jsx `voyagesLoaded` 게이트).

### V8.26 — 색·PC줌·배치 통합 (V8.25-05 누적 흡수) (2026-06-24, 번들검증 PASS)

V8.25-04(베이상세 5줄 배치) 위에 5개 기능을 한 번에 얹은 기능 추가 버전. (V8.25-05 베이상세 미세조정 — 위치·온도 가운데정렬 — 포함.)

**[1] PC 줌 분리** (`BayPlan.jsx`·`PrintableBayDetail.jsx`·`PrintableCargoPlan(V2).jsx`): 터치 없는 PC에서만 +/−·맞춤 버튼 + Ctrl+마우스휠 확대/축소 노출. 폰(터치)은 기존 핀치 그대로.
**[2] 무중복 지정색** (`utils.buildContainerColorMap`): 기존 12색 유지 + 13번째부터 황금각(137.5°) 균등 분포로 안 겹치는 고유색 생성. 흰 배경 가독성 위해 명도 낮춤. (i%순환 폐지 → 색 중복 없음.)
**[3] XRAY = 붉은 별**: 카고플랜·베이상세 별 색 검정→붉은색·약간 키움. 화면 베이플랜은 보라 셀 배경 폐지 → 흰 배경 + 한쪽 구석 붉은 ★. (도메인 원칙 "셀 배경색 금지, 글자·표기 색으로 구분" 준수.)
**[4] 화면 베이플랜 그리드 흰 배경 + B안 색 + 5줄 분배 배치** (`BayPlan.jsx`, 103라인 대전환): 어두운(slate) 셀 → 흰 배경 + 검정 글자. 셀 배치 줄1 POL좌·*POD우 / 줄2 컨번호 좌 / 줄3 선사좌·무게중앙·규격우 / 줄4 특수(온도·DG) 중앙 / 줄5 위치 중앙, 굵게(노안). B안 색: 양하=선사코드, 선적=POD만 지정색, 특수표시는 그 컨 지정색 따라감, 나머지(컨번호·무게·위치)는 검정, 비평택 전부 검정. (NO TEMP 경고만 빨강 유지.)
**[5] 베이상세(검정)**: V8.25-05까지 포함(5줄 새 배치 + 위치·온도 가운데정렬).

**검증(번들 PASS)**: esbuild 5개 컴포넌트 + utils 색함수 정합 OK. vite build 성공(`✓ built in 3m 26s`). 번들 — V8.26, `buildContainerColorMap`, `137.5`(황금각), `ctrlKey`(PC줌) 20건, 누적기능(앞뒤맞교환·⇅ 각 2건, `bd-r1` 6건) 박힘. sw.js·cone.html V8.26 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 화면 베이플랜이 다크→흰색 대전환이라 변화 큼. 기기에서 색·간격·완료/쉬프팅 표시 확인 필요.

### V8.25-04 — 베이상세 셀 5줄 분배 배치 (검정) 구현 (2026-06-24, 번들검증 PASS)

**목적**: 베이상세 인쇄 셀을 확대 mock대로 5줄 구조로 재배치. 흰 배경·굵게(노안)·셀 안 고르게 분배(space-evenly).
**확정 배치**:
- 줄1 — POL좌(DLC/DLC) · *PTK 우 (양 끝)
- 줄2 — 컨테이너번호 좌측 (자간)
- 줄3 — 선사 좌 · 무게 중앙 · 규격 우 (3등분)
- 줄4 — 온도 / DG 중앙 (없으면 빈 줄)
- 줄5 — 위치 중앙
**수정** (`PrintableBayDetail.jsx`): `formatCellParts()` 신규(토큰 분리), `renderCell`·`mrRenderCellContent` 새 구조, `.bd-cell`/`bd-fill`/`bd-cell-lines` CSS 갱신(`bd-r1`~`bd-r5`).
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 3m 12s`). 번들에 V8.25-04, `bd-r1`/`bd-r5`/`space-evenly` 박힘, 누적 기능(앞뒤 맞교환·⇅) 유지. sw.js·cone.html V8.25-04 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 베이상세 인쇄가 mock과 맞는지(줄 간격·자간) 기기에서 확인 — 미세 조정 가능.
**다음(예정)**: 무중복 지정색 생성 → 카고플랜 B안 색 → 화면 베이플랜 그리드 B안 색 + XRAY 붉은별.

### V8.25-02 — 수동 트윈 앞뒤 맞교환 "화면 미반영" 수정 (2026-06-24, 번들검증 PASS)

**증상**: 수동 모드(양하/선적) 트윈에서 앞뒤 맞교환을 누르면 음성은 "바꿨다"라고 하는데 카드 위치가 그대로라 교환 여부를 화면에서 구분할 수 없음.
**시뮬(4경우)**: 자동·수동 × 양하·선적 모두 DB swap PASS — 앞↔뒤 위치가 실제로 교환됨. 베이플랜·베이상세에도 반영됨. 즉 **데이터는 정상**.
**원인**: 수동 TwinSearch의 `c1`/`c2`는 로컬 `useState`. swap 후 DB(allContainers)는 바뀌지만 `c1`/`c2`가 갱신되지 않아 카드가 옛 위치를 표시. (자동은 card가 allContainers에서 `useMemo`로 매번 재계산돼 정상 반영 — 수동만 해당.)
**수정** (`SearchPanel.jsx`): swap 직후 `c1`/`c2`의 bay/row/tier를 앞↔뒤로 즉시 맞바꿔(`setC1`/`setC2`) 카드에 바로 반영.
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 4m 40s`). 번들에 V8.25-02 박힘, 누적 기능(앞뒤 맞교환·⇅ 각 2건) 유지. sw.js·cone.html V8.25-02 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 수동 트윈 맞교환 시 카드가 즉시 앞↔뒤로 바뀌는지 폰에서 1회 확인.

### V8.25-01 — 베이플랜 핀치 줌 300% 고정 버그 수정 (2026-06-24, 번들검증 PASS)

**증상**: 베이플랜에서 두 손가락 터치하자마자 300%로 확대된 뒤 조정 불가.
**원인**: 핀치 `useEffect` 의존성이 `[zoom]`이라, 핀치 도중 줌이 바뀌면 effect가 재실행되며 `pinchStartDist`를 0으로 리셋 → 다음 이동에서 `ratio = newDist/0 = 무한대` → 줌 상한 300%에 고정. (카고플랜·베이상세는 ref 패턴이라 정상이었음.)
**수정** (`BayPlan.jsx`): 시작 줌을 `zoomRef`로 읽고(`const zoomRef = useRef(zoom); zoomRef.current = zoom;`), `pinchStartZoom = zoomRef.current`로 변경, effect 의존성을 `[]`로 비워 핀치 도중 재실행 차단. 카고플랜·베이상세와 동일한 ref 패턴으로 일관화.
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 3m 46s`). 번들에 V8.25-01 박힘, V8.25 신규기능(앞뒤 맞교환·⇅ 각 2건) 유지 확인. sw.js·cone.html V8.25-01 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 핀치 줌이 손가락 간격대로 부드럽게 따라오는지 폰에서 1회 확인.

### V8.25 — 트윈 앞뒤 맞교환 전구간 확장 + 핀치 전용 줌 + 베이상세 노안 가독성 (2026-06-24, 번들검증 PASS)

**메모1 — 트윈 앞뒤 맞교환을 자동/수동 · 양하/선적 4곳 전부에서 동작.**
- `GuidedWorkPanel.jsx`: `swapTwinPos`의 'loading 전용' 제한 해제(양하도 동작), ⇅버튼을 양하에도 표시.
- `SearchPanel.jsx`: 수동 트윈 패널에 ⇅앞뒤 맞교환 버튼 + `handleSwapPos` 신규(양하·선적 공용), `fbReassignContainerPosition` import 추가.
- 동작: 앞(c1)을 뒤(c2) 자리로 보내면 `fbReassign` swap이 뒤 컨을 앞 자리로 자동 이동. **자리만 교환, 완료 처리 안 함.**

**메모2 — 줌 핀치(핀치/휠) 전용 전환 + 베이상세 텍스트 가독성 강화.**
- `BayPlan.jsx`: 줌 +/− 버튼 제거(핀치/휠 전용), 22% 시작 유지. `ZoomIn`/`ZoomOut` import 제거.
- `PrintableBayDetail.jsx`: 줌 기본 0.22, 핀치 하한 0.4→0.15, +/−·100% 버튼 제거. 텍스트=배경색+흰글자+굵게, 폰트 키움(7→8.5pt 등). 인쇄 무관.
- `PrintableCargoPlanV2.jsx` · `PrintableCargoPlan.jsx`: 핀치 줌 신규(22% 시작), 인쇄 시 scale 무효 처리.

**검증(번들 PASS)**: esbuild 6개 컴포넌트 정합 OK(firebase external 처리 시 오류 0). vite build 성공(`✓ built in 1m 35s`). 번들 grep — `⇅` 2건·`앞뒤 맞교환` 2건(수동/자동 양쪽) 박힘, `fbReassignContainerPosition` 포함 확인. `sw.js`·`cone.html` 라벨 V8.25 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 노안 텍스트 실제 크기감·핀치 동작 손맛은 폰/기기에서 1회 확인 필요.

### V8.24-03 — 다른 선박/항차 EDI 업로드 차단 (2026-06-24, 시뮬 PASS)

**배경**: 다른 배 EDI나 항차 안 맞는 EDI를 항차에 넣어도 경고 없이 적용(집계·베이구조·info·베이사전 덮어씀)되던 위험 버그.

**수정**: VoyagePage EDI 적용 루프(파싱 직후, allCns 누적 전)에 가드 추가. ① 선박 다름 — 콜사인 둘 다 있고 다르면 차단, 콜사인 없으면 '진짜 선박명'(공백/≥7자)끼리만 비교(CNYNT 코드형은 제외해 재업로드 오차단 방지). ② 항차 다름 — 평택분으로 양하(voy_d)/선적(voy_l) 자동판정 후 대조, 평택 해당분 있으면 "파일명 항차 고쳐 재등록" 유도, 없으면 "다른 항차" 차단. ③ 첫 EDI(등록정보 없음)는 통과. continue로 적용만 막음. (V8.24-02 신원가드 유지.)

**시뮬 PASS**: 같은배/같은항차 통과, 다른배(콜사인) 차단, CNYNT 오차단 없음, 항차 불일치 재등록유도/다른항차 구분, 첫 EDI 통과.

### V8.22-01 — 작업보고 카운트 "265" fix 추가 (2026-06-21, 시뮬 PASS)

**배경**: V8.22(협작 테스트본)에 WorkReportModal의 작업보고 평택 카운트가 354 대신 265로 적게 나오던 버그가 남아 있었음(현황요약·마감점검 V8.20-01과 동일 계열, records의 가짜 POL이 EDI 평택 POL을 덮음). V8.20-02에서 이미 고친 fix를 V8.22 베이스에 얹음.

**수정**: WorkReportModal.jsx 머지에서 pol/pod 제외 보강(`{pol:_p,pod:_d,...rEnrich}`), EDI에 없을 때만 리스트 폴백. 시뮬 PASS(265→354). 빌드 후 번들·sw.js·cone.html APP_VERSION V8.22-01 동기화.

**주의**: shipStructure fuzzy 매칭 순서 수정(V8.20-02)은 V8.22가 이미 446줄 콜사인 오염 가드로 DJCT→XTPG를 사후 차단하고 있어 **중복 회피로 미적용**. 두 방식(입구 정확매칭 우선 vs 출구 선박명 검증)이 같은 버그를 다른 지점에서 막음 — 통합 검토는 추후.

### V8.20-01 — 현황요약/마감점검 분모 "265" 버그 수정 (2026-06-21, 실데이터 시뮬 PASS)

**증상**: SWDN 2606N 선적 화면에서 상단 헤더 `선적 0/265`. 같은 화면 하단은 전부 354(EDI 평택 354 / 리스트 354 / 매칭 354 / 규격합 20DC 206+40HC 140+40RF 8 = 354). 헤더 분모만 89 적게 나옴.

**원인(데이터 확정)**: `VoyageSummaryCard.jsx`·`WorkClosingChecklist.jsx`의 EDI↔records 머지가 `{...e, ...recordsAll}`로 records 전체를 덮어씀. 선적 리스트(헤더 없는 엑셀 등)에 POL이 평택 아닌 가짜값으로 든 컨 89개가 있었고, 그 가짜 POL이 EDI의 KRPTK를 덮어 `isPyeongtaekPort(c.pol)` 필터에서 탈락 → 354→265. (도메인: "헤더 없는 엑셀 fallback이 POD를 POL 자리에 넣음"과 동일 계열. Ⅵ M6.94.31 참조.) 화면리스트(`VoyagePage.containers`)는 ALLOWED_LIST_FIELDS로 pol을 막아 354 정상이라, 두 경로가 갈렸다.

**수정**: 두 컴포넌트 머지에서 `pol`·`pod`만 제외하고 보강(`k!=='pol' && k!=='pod'`). EDI를 항구 단일 진실로, 빈 경우만 리스트 폴백.

**점검 결과**: 같은 머지 패턴이 `App.jsx:297`·`VoyagePage.jsx:1141`에도 있으나 둘은 **평택 필터를 안 쓰는** ContainerDetailModal용 전체목록이라 증상 없음 → 미수정(외과적 변경).

**시뮬레이션(실데이터 = SWDN 2607S EDI 354대, 화면 조건 89개 가짜 pol 주입)**: VoyageSummaryCard 버그코드 265 → fix 354 PASS. WorkClosingChecklist 동일 265 → 354 PASS. 빌드 후 번들에 `!=="pol"&&...!=="pod"` 2건 + APP_VERSION V8.20-01 grep 확인.

**교훈(부록 A-2에 상세)**: 여러 Claude가 같은 화면을 보고도 헤맴 — 354 vs 265 차이와 머지 덮어쓰기 코드를 둘 다 봤으면서 연결 안 하고 Firebase export를 요구(Ⅶ부 "할 수 있는데 안 함"). "이미 됐겠지"·"같은 버그겠지"를 확인 없이 단정(Gate 2 생략). **본 데이터는 끝까지 연결하고, 자료 요청으로 도망치지 말 것.**

### V8.20 — 수집기 연동 입구 `mergeApi.js` 추가 (2026-06-20, 실데이터 시뮬 PASS)

**배경**: 수석검수사가 손으로 하던 "메일 검색 → 선적리스트 병합 + EDI 대조 → 검수앱 투입"을 수집기(Tallyman Mail Collector)가 자동화하려면, 검수앱의 검증된 파서를 그대로 재사용해야 함. 수집기 개발 측이 입구 파일 `mergeApi.js`(파서를 감싸는 다리)와 연동안내 MD를 보내옴.

**설계 핵심 (다리 = contract)**
- 파서 자체는 검수앱이 계속 소유(`utils.js`). `mergeApi.js`는 파서를 **감싸기만** 함 — 파서 로직 미포함, 드리프트 방지.
- 폴더의 파일들(EDI + 여러 선사 CLL 엑셀 + XRAY)을 받아 → 병합본 xlsx(base64) + EDI 대조 리포트를 반환.
- `main.jsx`에 `import './mergeApi.js'` 한 줄 → 배포본에 전역 `window.GMmerge(files) → { xlsxBase64, report }` 생성. React/Firebase 의존 없음.
- 분류: `.edi`→BAPLIE, `.asc`→참조, `.xls/.xlsx`→리스트(recap·cbf·cdl·memo 제외, xray 별도). EDI는 **가장 큰(최종) 파일을 진실**로 채택. `mergeWithEdi`: EDI=진실, 리스트=보강(실번호/엠티실/무게), 차이는 CONFLICTS, EDI에 없으면 NOT_IN_EDI.

**Gate 1·2 검증 결과 — 보내온 파일의 확인요청 3건 대조**
- ① `parseXrayList` 반환 형태 → **수정함**. 보낸 파일은 `{records}` 가정이나 실제는 `{ containers:[번호배열], _matchCount }`. 원본은 XRAY를 0개로 처리하던 버그(연동 측 검증표에 XRAY 없어 미발견). xray 처리부 한 줄만 `out.containers` 사용으로 교체(+ records 폴백 유지).
- ② `mergeWithEdi` 인라인 복사 → **그대로 둠**(import로 바꾸면 mixerUpload deps 번들 유입, 드리프트 위험).
- ③ 컨테이너 필드명(`cn/iso/op/sl/eseal/wt/fe/pol/pod`) → **일치**. parseBAPLIE=`{containers}`, parseListExcel=`{records}`, import 5함수 모두 utils.js 존재.

**발견·기록한 주의점**: `asArrayBuffer`가 ArrayBuffer를 그대로 파서에 넘기는데, 파서 내부 `XLSX.read(input,{type:'array'})`는 정석상 **Uint8Array**를 기대. Node/수집기에서 ArrayBuffer 직접 넘기면 엑셀 파싱이 `Cannot read properties of undefined`로 깨질 수 있음 → 회신 MD에 "엑셀류는 `new Uint8Array()`로 감싸 호출" 명시. (브라우저 File.arrayBuffer() 경로는 대체로 무관.)

**시뮬레이션(실데이터, 실제 mergeApi.js import)**: EDI 3대 + 리스트(충돌·미매칭 포함) + XRAY 1건. 결과 전부 기대값 일치 — 병합 3, CONFLICTS 1(MSKU 무게 18000 vs 99999), NOT_IN_EDI 1(XXXU9999999), **XRAY=Y TGHU1000001**(수정 핵심). 빌드 후 번들에 `window.GMmerge` 등록 + V8.20 박힘 grep 확인.

**산출**: `src/mergeApi.js` 신규, `main.jsx` import 1줄, `mergeApi_검수앱측_회신.md`(수집기 측 전달용 — 확인 3건 답변·입력타입 주의·검증표 포함).

### V8.13 — 자연어 텍스트 조회 음성 2버그 수정 (사용자 보고 2026-06-19, 시뮬 15+ PASS)

**증상(사용자 보고)**: 자연어 탭에서 **텍스트로** 끝4자리 조회 시 음성이 ① "끝4자리"를 "끝사자리"로 ② 컨번호 "1250"을 "천이백오십"으로 통째로 읽음.

**원인**: `describeQuery`(nlSearch.js 438행)가 `끝4자리 ${digits}` 문장을 만들어 화면·음성 공용으로 씀(SearchPanel 592·596 등 speak). TTS가 "4"→"사", 붙은 숫자→통째로 읽음. 음성 표준(spellKo/spellDigits)은 있었으나 `speak()` 경로가 좌표(spellPosString)만 변환하고 일반 컨번호 숫자는 안 풀었음.

**수정(2건, 외과적)**
- `nlSearch.js`: `끝4자리`→`끝네자리`(438행). TTS가 한글 "네"로 정확히 읽음. 화면에도 "끝네자리 1250"으로 자연스럽게 표시(숫자는 붙여 둠).
- `voice.js speak()`: 좌표 변환(spellPosString) 뒤 **4자리 이상 연속 숫자만 한 자씩** 풀기 추가 — `replace(/\d{4,}/g, ...NUM_KO)`. "1250"→"일 이 오 공". 모든 speak 경로에 일괄 적용(음성 표준 일원화). 수량(20대)·시각(17시)·온도(18도)·베이(38번)는 1~3자리라 보존 — 부작용 없음.

**검증(시뮬 `speak_digits_sim.mjs`·`voice_final.mjs`)**: 4자리+ 풀림·1~3자리 보존(수량/시각/온도/베이)·BIC+4자리·6자리, describeQuery 화면("끝네자리 1250")과 음성("끝네자리 일 이 오 공") 분리 동작. 15+ PASS. build.sh PASS, 번들에 "끝네자리"·4자리풀기 정규식·V8.13 박힘, sw.js 동기화.

**주의**: 4자리 이상만 푸므로 컨번호 끝3자리(드묾)는 안 풀림 — 끝4자리 조회가 표준이라 문제없음. 단건 조회(announceContainer)는 원래 spellKo라 정상이었고, 이번 수정은 describeQuery 경로(0건·통계·여러건 안내)를 고친 것.

### V8.12 — LOLO 자연어 조회 지원: 베이 게이트 스킵 (사용자 보고 2026-06-19, 시뮬 9/9 PASS)

**증상(사용자 보고)**: LOLO선(RZOR)은 자연어 검색·조회창이 안 됨. 자동/수동 작업 흐름이 베이를 전제하는데 LOLO는 베이가 없어 막힘.

**핵심 도메인(사용자 확정)**: LOLO는 **베이(선내 위치) 정보만 없을 뿐, 나머지(실번호·규격·적공 F/E·온도·X-RAY)는 다 있다.** 그러니 자연어 탭을 막을 게 아니라 **베이 게이트만 건너뛰고 나머지는 그대로 조회**하면 된다. 별도 화면·토글 신설 불필요 — RZOR(`isLoloShip`)면 자동 처리.

**원인(SearchPanel)**: 자연어 탭이 ① 자동/수동 토글 ② 베이 선택 게이트(`manualBay==null`) ③ 단 선택 게이트(`manualTier==null`)를 거쳐야 조회창에 도달. 셋 다 베이 전제라 LOLO는 게이트에서 막힘. `isLoloShip`이 SearchPanel로 전달조차 안 되고 있었음.

**수정(외과적 — 일반선 흐름 무변경)**
- `VoyagePage`: SearchPanel에 `isLoloShip` prop 전달(한 줄).
- `SearchPanel`: prop 수신. LOLO면 ⓐ 자동/수동 토글 숨김(`&& !isLoloShip`) ⓑ GuidedWorkPanel 분기 스킵 ⓒ 베이 게이트 스킵 ⓓ 단 게이트 스킵 → 곧장 조회창. 조회창 상단에 LOLO 안내 배너("베이는 없지만 실번호·규격·적공·온도·X-RAY는 정상 조회").
- 조회 결과 카드(BigResultCard)는 **무수정** — 이미 `c.bay` 있을 때만 위치 표시하므로, LOLO는 위치만 자동으로 빠지고 나머지 정보는 정상 출력(점검으로 확인).

**검증(시뮬 `/home/claude/sim/lolo_gate_sim.mjs`)**: LOLO는 양하·선적·완료·guideMode켜짐 전 상태에서 조회창 직행(게이트·가이드 스킵), 일반선은 베이→단→조회창 게이트 + 자동 가이드 그대로 유지. 9/9 PASS(회귀 0). build.sh PASS, 번들에 "LOLO 선박 (베이플랜 없음)" 박힘. **새 UI(LOLO 배너) 렌더 PNG 눈 확인 PASS**.

**주의**: 자연어 위치 질문(예: "20번 베이 몇 대")은 LOLO에서 결과 없음 — bay가 없으니 당연(applyNLFilter가 못 거름). 끝4자리 단건 조회·규격/실/온도 조회는 정상. LOLO 탭(LoloTab 리스트)은 그대로 병존 — 자연어 탭은 조회, LOLO 탭은 리스트.

### V8.11 — 해치 제외 4척 확장 + 부두별 호기 + 주야간 작업갯수 집계 (사용자 확정 2026-06-19, 시뮬 19/19 PASS) ★메이저

세 가지 신규 도메인 규칙을 한 빌드에 묶음(전부 사용자 확정).

**① 해치커버 계산 제외 4척 (V8.09-17 일반화)**
- V8.09-17이 `isHatchAutoShip=/TMPZ/i`(TMPZ만, 해치 자동·유압식이라 프롬프트 안 띄움)였음.
- 사용자 확정: **TMPZ·TNJP·RZOR·OBWH 4척은 해치커버 계산을 하지 않는다.** 대신 주야간 작업갯수를 기록(③).
- 수정: `isHatchAutoShip` → `isHatchSkipShip = /TMPZ|TNJP|RZOR|OBWH/i.test(vsl+' '+vslFull)`. 참조 3곳(데크완료 오픈·홀드완료 클로즈·닫기 제안) 가드 교체. 4척이면 해치 프롬프트·장수 계산(hatchPanelsOf)·카톡 보고 전부 자동 건너뜀.
- ⚠️ 지침서 과거판이 'TPMZ'로 오기한 곳 있음 — 정답은 **TMPZ**(사용자 확정).

**② 부두별 양적하 장비(호기) 수**
- 사용자 확정: **PCTC = 4대(1~4호기). PNCT = 5대(1~5호기)** — PNCT는 여객석이 RORO 작업을 해 1대 더 잡음.
- 신규 `utils.js equipNumbersForPier(pier)`: PCTC→1~4 / PNCT→1~5 / 미상→1~5(안전 최대). 부두 판정은 기존 `getPierFromBerth(info.berth)`(V8.09-09, 동부두 6~9=PCTC·13~16=PNCT) 재사용.
- 적용 3곳: GuidedWorkPanel 장비선택 UI / Header 장비탭 / WorkReportModal 장비 4곳. 기존 단일 상수 `EQUIPMENT_NUMBERS`(kakaoShare.js, 1~4호기 하드코딩) 대신 부두 동적 목록. 세 컴포넌트의 미사용 `EQUIPMENT_NUMBERS` import 정리(외과적). kakaoShare.js의 상수 자체는 보존(다른 참조 없음·회귀 방지).
- ⚠️ Header는 현재 항차(`route.voyage`)의 berth로 판정, voyage 없으면 1~5 전체. WorkReportModal은 `voyage.info.berth` 기준.

**③ 주야간 작업갯수 집계 → 작업보고(WorkReportModal)로 (①의 4척 전용)**
- 사용자 확정(2026-06-19): **주야간 집계표 = 작업보고의 일종.** 그러므로 가이드 화면 배지가 아니라 **작업보고 안**에 둔다. (최초안은 GuidedWorkPanel 배지였으나, "가이드 모드로는 완벽한 작업이 안 됨 — 현장이 수시로 바뀜"이라는 사용자 지적으로 폐기. 집계 근거는 모드와 무관한 Firebase 완료 레코드이므로 작업 모드에 묶으면 안 됨.)
- **위치/UI**: WorkReportModal `view==='daynight'`. main에 "📋 주야간 작업보고" 진입 버튼(isHatchSkipShip일 때만). 주간/야간 토글 — 진입 시 `reportShiftToShow(now)`로 자동 선택, 검수원이 눌러 수동 전환.
- **표**: 좌측 규격(20/40/45) × F·E·TOTAL, 하단 풀엠티토탈. 양하·선적 각 표.
- **표시 시각 판정(집계 경계와 별개, +30분 여유)**: `reportShiftToShow(ts)` — 주간 표시창 08:00~17:30 / 야간 17:30~익06:00 / 06:00~08:00 다음 주간 미리. 배경: 보고서는 17:00로 정리하지만 실제 보고는 17:30 전, 야간도 동일.
- **보고기준 자동선택(`buildShiftReport`)**: 완료(작업량) vs 잔여 **총합** 중 적은 쪽을 기준으로 + "○○ 기준" 표기. 이유는 비용/정산이 아니라 **세기 편의 — 적은 쪽 카운트가 빠르다**(검수원이 리스트 기준으로 적은 걸 셈). 동수면 작업량(`<=`). 잔여 0이면 "작업 완료 — 보고 제외".
- **완료/잔여 컷(초 단위, 절대)**: 주간 완료 = 완료시각 **≤ 17:00:00**, 야간 = **≤ 05:30:00**. **17:00:01에 완료해도 잔여분**. 잔여 = 전체 − 그 컷까지 완료(17:00 이후 완료분 + 미완료 모두 잔여). 마감 전엔 그 시점까지 실시간 카운트가 그대로(잠정 표시 없음), 17:00 넘으면 컷이 자연 적용돼 고정.
- **대상·모드무관**: 해치 제외 4척, 작업 모드(가이드/수동) 무관. 양쪽 모드 컨은 WorkReportModal 안에서 `voyage.discharge`/`voyage.loading`로 직접 조립(호출부 VoyagePage 미수정 — 외과적 유지). 평택분만(`isPyeongtaekPort` pod/pol).
- 신규 `utils.js`: `reportShiftToShow(ts)`, `buildShiftReport(conts, shift, now)`. (V8.10 1차의 `workShiftOf`/`tallyDayNight`는 보존하나 화면 미사용 — 향후 정리 가능.)
- **기존 코드 변경 기록**: GuidedWorkPanel의 `DayNightBadge`(정의+렌더 2곳)와 `tallyDayNight` import 제거 — 이유: 주야간 집계가 작업보고로 이전돼 가이드 화면 배지가 불필요·부적절(가이드 모드 의존이 잘못). 영향: 가이드 화면에서 배지만 사라짐, 다른 동작 무변경. 주석 1줄 갱신(이전처 명시).

**검증(시뮬 `v810_sim.mjs`·`v810_hatch.mjs`·`report_shift_sim.mjs`·`report_basis_sim.mjs`·`remain_cutoff_sim.mjs`·`v810_dn_final.mjs`)**: 부두별 호기(PCTC4·PNCT5·berth연동), 표시 판정 경계(17:29주간·17:30야간·05:59야간·06:00주간), 보고기준 자동선택(완료>잔여→잔여·완료<잔여→작업량·동수→작업량·전량완료→제외), 잔여 컷 초단위(17:00:00완료·17:00:01잔여·05:30컷), 규격×F/E 표·풀엠티토탈, 해치 제외 4척 정규식. 합계 **57건+ PASS**. `bash build.sh` PASS, 번들에 "주야간 작업보고"·"풀엠티토탈"·"야간보고 19"·"작업 완료" 박힘, **옛 가이드 배지 "주야간 작업갯수" 문구 0(제거 확인)**, sw.js=V8.11 동기화. **새 UI 렌더 PNG로 눈 확인 PASS**(6관문 새UI 렌더 — 토글·표·기준표기·제외 정상).

**다음 주의**: RZOR는 LOLO라 GuidedWorkPanel 경로를 안 타지만, 작업보고(WorkReportModal)는 모드·LOLO 무관하게 열리므로 주야간 보고가 정상 노출됨(1차안의 가이드 배지 우려가 이전으로 해소). V8.10 1차의 `workShiftOf`/`tallyDayNight`/`DayNightBadge`는 제거됐고, `tallyDayNight`는 utils에 남아있으나 미사용 — 다음에 정리 가능.

### V8.09-18 — 트윈 선적: 한 티어 전체 로우 채운 뒤 위 티어로 (메모3 재정의, 사용자 보고 2026-06-18, 시뮬 PASS)

**정확한 증상(사용자 재설명)**: 20ft 트윈 선적 시, 02 티어에 6개 로우가 있는데 다 안 채우고 1개 로우만 완료한 뒤 그 로우의 04·06 티어로 올라가 선적함. (앞서 "같은 규격" 해석은 틀렸음 — 실제는 로우/티어 순서 문제.)

**원인(guidedQueue.js buildStageOrder 트윈 정렬)**: 트윈 sort 비교가 POD → **로우 → 티어** 순이라, 같은 POD 안에서 한 로우를 골라 그 로우의 02·04·06을 먼저 쌓고 다음 로우로 감.

**수정**: 트윈 sort를 POD → **티어(바닥 02부터) → 로우(해상→육상)** 로 변경. 티어를 로우보다 먼저 비교. 같은 티어 안 로우 방향은 기존 rowRank(landToSea:false, 해상→육상) 그대로. → 02 티어 전체 로우(해상부터) → 04 전체 → 06 순서.

**확정 규칙(사용자)**: 각 티어마다 해상 쪽 로우부터, 한 티어를 다 채운 뒤 위 티어로. (02 전체 해상→육상 → 04 전체 → 06.)

**검증**: node 시뮬 PASS — 02티어 6로우 먼저·04 다음·06 마지막, 각 티어 해상(짝수row)부터. 실제 rowRank 방향 일치 확인. build.sh PASS, 번들에 티어 우선 비교 박힘.

**주의**: 40ft·싱글20·FR 순서는 무변경(트윈 스택 내부 순서만 조정). 데크도 동일 규칙 적용(자동 제시, 포트/난이도 보정은 수동).

### V8.09-17 — 메모 5건 일괄 처리 (사용자 메모 2026-06-18, 시뮬+빌드 PASS)

**메모1 — 수동 양하 '선박에 없음' 오류 재발(끝4자리 조회 후 수동 선적 시 발생, 자동은 정상)**: BigResultCard의 "🚫 선박에 없음(누락 처리)" 버튼은 양하 전용 기능인데 노출 조건이 `!isLoading && !isDone`뿐이라 모드 구분이 없었음. 선적에도 떠서 혼란. → `c._mode === 'discharge'` 추가해 양하에서만 노출. (선적은 위치수정·번호수정 버튼이 별도로 있음.)

**메모2 — 수동 싱글 작업 시 숫자패드 안 나옴(양하·선적 동일)**: SingleSearch 입력칸은 자유질문·음성도 받는 칸이라 `inputMode`가 text였음. → 수동 작업 컨텍스트(베이·단 선택 완료, `manualCtx.selectedGroup != null && selectedTier`)일 때만 `inputMode='numeric'` 적용. 자유질문 화면에선 기존 text 유지(한글 질문 보호).

**메모4 — TMPZ는 해치커버 자동이라 보고 안 함**: `isHatchAutoShip = /TMPZ/i.test(vsl+vslFull)` 플래그 신설. 해치 프롬프트 3종(deckDonePromptD·holdWorkedD·holdDonePrompt) 각 시작에 `if (isHatchAutoShip) return false` 추가 → 오픈/클로즈 제안·음성·버튼 모두 미표시. holdCloseD 음성 분기·클로즈 버튼 렌더는 holdWorkedD에 의존하므로 자동 차단.

**메모5 — 자동은 잔여 보이는데 수동은 진행상태 안 보임(자동 가이드처럼)**: 수동 "📍 Bxx번 홀드/데크 작업 중" 배너에 `잔여 N대` 배지 추가. N은 현재 단의 미완료 잔여(manualGroups의 hold/deck, `_comp` 제외 카운트).

**메모6 — 트윈 선적 시 앞뒤 바뀜 많음, 한번에 위치수정**: 트윈 짝꿍 구분선에 "⇅ 앞뒤 맞교환" 버튼(선적만). `swapTwinPos` 신설 — 앞 컨을 뒤 자리로 reassign하면 `fbReassignContainerPosition`의 swap 로직이 뒤 컨을 앞 자리로 자동 이동. confirm 후 자리만 교환(완료 처리 안 함), 음성 안내.

**보류 — 메모3**: ~~의도 확인 후 별도 처리~~ → **V8.09-18에서 해결**(트윈 티어 우선 정렬). 실제 의도는 "규격"이 아니라 로우/티어 순서였음 — 한 티어 전체 로우 채운 뒤 위 티어로.

### V8.09-16 — 실번호 중복 입력 차단 (일반·XRAY, 0000 예외) (사용자 보고 2026-06-18, 시뮬 10/10 PASS)

**보고**: XRAY 실번호를 서로 다른 컨에 같은 번호로 연속 입력했는데 전부 통과됨. 기본 중복 검사가 없었음.

**원인(GuidedWorkPanel.jsx)**: V8.09-06이 "XRAY 실번호 빈값 차단"만 넣었고 중복 검사는 없음. `saveSeal`/`saveXSeal` 둘 다 입력값을 검증 없이 바로 firebase에 저장.

**도메인 규칙(사용자 확정)**: 실번호는 같은 번호가 없다 — 중복은 완전 차단. 예외 단 하나, `0000`(실 부족 시 표기)은 중복 허용.

**수정**: `dupSealOwner(val, selfCn, kind)` 유틸 신설. 같은 항차·**같은 모드(`_mode`)** 안에서 자기 자신(selfCn) 제외하고 같은 실번호를 가진 컨을 찾는다. 일반 실은 다른 컨의 `c.sl`, XRAY 실은 `c._xraySeal.seal`과 **자기 종류끼리만** 비교. 빈값·`0000`은 검사 제외(예외 허용). 중복이면 alert("이미 ㅇㅇㅇㅇ 컨에 입력됨, 실 부족이면 0000")로 저장 차단. `saveSeal`·`saveXSeal` 양쪽 적용.

**검증**: node 시뮬 10/10 PASS — XRAY/일반 중복차단, 신규통과, 0000 연속허용, 자기자신 제외, 모드격리(discharge X100과 loading X100 별개), 빈값 통과. build.sh PASS, 번들에 차단 문구·0000 예외 박힘.

**주의**: 모드 격리이므로 양하에 쓴 실번호와 선적에 쓴 실번호가 같아도 막지 않는다(서로 다른 작업). 같은 모드 안에서만 유일성 강제.

### V8.09-15 — 가이드 모드 해치커버/베이 선택 음성 처리 (사용자 점검 2026-06-18, 시뮬 16/16 PASS)

**점검 배경(사용자 제보)**: 가이드 모드에서 해치커버를 열고닫을지·다음 베이로 갈지 선택하는 갈림길인데, 사람이 아직 고르지 않았는데 앱이 한 템포 빠르게 "다음 컨테이너"를 말해버린다. 어디로 갈지 미정인 상태에서 음성만 앞서감.

**원인(GuidedWorkPanel.jsx 정독)**: 다음 컨테이너 음성 useEffect가 `card`(다음 예측 컨)와 `voiceOn`만 보고 발화한다. 그런데 선택 배너 표시 조건이 `deckDonePromptD && card` / `holdDonePrompt && card`라, **배너가 떠 있는 순간에도 `card`는 살아 있어** 음성이 그대로 다음 컨을 읽어버린다. 또 선택지 자체의 음성 안내는 코드에 전혀 없었다(화면 텍스트만).

**수정(2건)**
1. **선택지 가드**: 선택 배너가 떠 있는 동안(`choicePrompt` 존재) 다음 컨테이너 음성을 막는다. `choicePrompt` useMemo 신설 — 'deckD'(양하 데크완료→홀드) / 'holdL'(선적 홀드완료→데크) / 'holdCloseD'(양하 홀드완료→닫기) 판정. 다음 컨 음성 effect 의존성·조건에 `choicePrompt` 추가.
2. **선택지 음성**: 갈림길을 음성으로 읽는다(사용자 확정 문구 1가). deckD="데크 양하 완료. 해치커버를 열까요, 다른 데크로 갈까요." / holdL="홀드 선적 완료. 해치커버를 닫을까요, 다른 베이 홀드로 갈까요." / holdCloseD="홀드 작업 끝. 해치커버를 닫을까요." **무응답이면 12초마다 1회 반복**(setInterval 12e3), 선택/배너 사라지면 즉시 정지(clearInterval). voiceOn 따름(음성 끄면 선택지 음성도 꺼짐).
- 음성 useEffect 2종을 프롬프트 정의(deckDonePromptD 등) 뒤로 이동 — 정의 순서상 앞에 두면 참조 불가.

**검증**: node 시뮬 16/16 PASS (배너 시 다음컨 차단·선택지 발화·문구 3종 매핑·평상시 정상 발화·voiceOff 무음·닫기완료 후 미표시). build.sh PASS, 번들에 문구 3종·12e3·V8.09-15 박힘 확인.

**다음 주의**: 12초 반복 간격은 현장 청취 후 숫자만 조정 가능. 선택 버튼의 실제 동작(해치 보고·베이 이동)은 무변경 — 이번은 음성 안내 흐름만.

### V8.09-14 — 선박 상태: ETD 지나도 작업 미완료면 '일정 미확정'(입항지연 오표시 수정) (사용자 보고, 시뮬 PASS, 2026-06-18)

**증상(사용자 보고)**: 작업 시작도 안 한 항차인데 "출항함"으로 뜸. 원인은 입항 지연 — 배가 안 왔는데 예정 출항시각(ETD)만 지나버림.

**원인(`getShipStatus`)**: `현재≥ETD`면 무조건 phase='departed'(출항함). ETD는 예정일 뿐인데 출항을 단정 → 입항 지연 시 "작업 0%인데 출항함" 오표시.

**수정**: `getShipStatus(pm, now, work)` 3번째 인자로 작업 진행률 `{done, total}` 받음. ETD 지난 경우: **작업 완료(total>0 && done>=total)면 'departed'(출항함), 아니면 'unsure'(❓ 일정 미확정)**. 출항함은 작업이 실제 끝났을 때만. 호출부(VoyagePage)는 `containers`/`compMap`로 현재 모드 완료수 계산해 전달. unsure tone=amber(❓ 일정 미확정).

**검증(시뮬 `/home/claude/sim/status_v14.mjs`)**: ETD지남+작업0%→일정미확정 / ETD지남+작업100%→출항함 / 절반→미확정 / 입항전→항해중 / 정박중 유지 / 작업데이터없음+ETD지남→미확정. 6케이스 PASS. 빌드 PASS(V8.09-14).

### V8.09-13 — PORT-MIS 같은 선박 여러 항차 중 '다음 작업 항차' 선택 (사용자 보고, 시뮬 PASS, 2026-06-18)

**증상(사용자 보고+화면)**: PACIFIC SHENZHEN(PCSZ, 9V8012)이 실제로는 6/19 19:00 입항 예정(아직 항해중)인데, 앱은 지난 항차(입항 6/17 18:00 / 출항 6/18 12:00)를 매칭해 "📍 PCTC·E6 정박중"으로 잘못 표시. PORT-MIS에 같은 선박의 두 항차(지난 6/17~18 + 다가올 6/19~20)가 공존.

**원인(`VoyagePage.jsx` PORT-MIS 매칭)**: 콜사인 prefix 후보를 **updatedAt(저장 시각) 최신** 순으로만 정렬(V8.01). 실제 입출항 일정(ETA/ETD)과 무관 → 다음 작업할 항차가 아니라 엉뚱한 항차 선택. 게다가 화면 시점이 옛 항차 출항(12:00) 직전이라 "정박중"으로 보임.

**수정**: 후보 정렬을 '다음 작업 항차' 기준으로 변경(09-11의 `parsePortMisDateTime` 재사용). 현재시각 now 대비 ① **아직 안 끝난 항차(ETD≥now) 우선 — 그중 ETA가 가장 늦은(가장 나중) 일정** (옛 항차가 출항 직전이어도 미래 일정이 따로 있으면 그쪽이 다음 작업분) ② 모두 끝났으면 ETD 최신 ③ 시각 없으면 updatedAt. 정렬 후 vesselName/berth 선택 로직이 정렬 순서대로 1순위를 집음.

**검증(시뮬 `/home/claude/sim/pm_match_sim2.mjs`)**: PCSZ old(6/17~18)+new(6/19~20) — 6/18 11:26·6/17 20:00·6/19 20:00·6/21·6/16 전 시나리오에서 new(다음 작업 항차) 선택, 과거+미래 케이스도 미래 우선 PASS. 빌드 PASS(V8.09-13).

**주의(현장)**: 코드는 PORT-MIS에 두 항차가 다 있을 때 올바른 쪽을 고르는 것. 만약 PORT-MIS에 새 항차(6/19)가 아직 안 올라왔으면, 새 PORT-MIS 엑셀을 올려야 새 일정이 잡힘.

### V8.09-12 — 수석대시보드 CNYNT 중복 제거: vsl 자리 항만코드 그룹 제외 (2026-06-18)

**증상(사용자 보고+화면)**: 수석대시보드에서 OBWH(D5MO4)와 CNYNT가 같은 항차(2665E·2663E·2661E)·같은 양하 수치(211·271·224)로 중복 표시. CNYNT는 쓴 적 없음. 사용자 확인: CNYNT=중국 옌타이(연태) 항만코드이며 OBWH의 목적지(POD)와 동일, 작업일자·항차·양하 동일(선적은 0).

**원인**: OBWH 항차의 양하 데이터가 어떤 voyage에 `info.vsl="CNYNT"`(목적항 UN/LOCODE)로 잘못 저장됨. `tallyVoyagesByShip`(firebase.js)이 vsl로만 그룹핑 → 항만코드를 별도 선박으로 오인해 OBWH와 중복 카드 생성. 선적 0은 양하 데이터만 항만코드로 그룹된 정황과 일치.

**수정**
- `utils.js`: `isPortCode(vsl)` 신규 — UN/LOCODE 항만코드 판별. 공백 없는 5자 영문 + 국가 prefix(CN/KR/JP/TW/HK/VN/TH/SG/MY/PH/ID/RU) 또는 알려진 항만 목록(CNYNT·CNTAO·KRPTK 등 30개). 선박명은 공백 포함 또는 4자 이하 약자(OBWH/RZOR/ATRP)라 오검출 없음.
- `firebase.js tallyVoyagesByShip`: vsl이 `isPortCode`면 `continue`(선박 그룹 제외). 데이터는 보존, 표시만 제외.

**검증(시뮬 `/home/claude/sim/cnynt_sim.mjs`, `port_code_sim.mjs`)**: CNYNT/KRPTK/CNTAO→항만코드 true, OBWH/RZOR/ATRP/선박명→false. 수정 후 CNYNT 카드 사라지고 OBWH·RZOR·ATRP 유지 PASS. 빌드 PASS(V8.09-12).

**주의(현장)**: Firebase에 vsl="CNYNT"로 저장된 항차 데이터 자체는 코드로 안 바뀜 — 화면에선 중복이 사라지지만, 근본 정리하려면 그 voyage의 vsl을 OBWH로 고치거나 삭제 필요. 예방: 향후 vsl 입력/EDI 추출 시 항만코드가 vsl로 새지 않게 점검(별도 과제).

### V8.09-11 — PORT-MIS 선박 현재 상태 표시: 항해중/정박/출항 구분 (실데이터 PASS, 2026-06-18)

**요청**: 인천 정박이면 인천, 평택으로 오는중이면 항해중, 평택 정박이면 부두 표시.

**구현(`utils.js`+`VoyagePage.jsx`)**: `getShipStatus(pm, now)` — ETA/ETD vs 현재시각(입출 보조). phase: sailing(현재<ETA) / berthed(ETA≤현재<ETD) / departed(현재≥ETD) / unknown. `parsePortMisDateTime`, `isPyeongtaekPortName`. 표시: 평택+정박중만 부두 배지, 그 외 상태 라벨(🚢 평택 항해중 / ⚓ 인천 정박중 / ↗ 인천 출항함). 검수앱만.
- 검증(시뮬 `status_verify.mjs`, 실데이터): TIAN HAI PING ZE 시각별 전환·인천 선박 구분 6케이스 PASS.

### V8.09-10 — 입력창 키보드 모드: 이름만 한글, 선박명 영문, 나머지 숫자패드 (2026-06-18)

**요청(사용자)**: 검수앱/콘앱/벌크앱에서 이름 칠 때 말고는 한글 자판 안 씀. 일반 텍스트창은 영문, 숫자만 필요한 곳은 숫자 패드.

**확정 기준(사용자 2026-06-18)**: ① 검수원 이름 = 한글 그대로. ② 선박명(+콜사인·CASP코드·컨번호) = 영문 강제(한글 제거 + 대문자). ③ 그 외 전부(조회창·실번호·XRAY실·엠티실·위치 bay/row/tier·개수) = 숫자 패드. ④ 온도 = decimal 패드. ⑤ 데미지·메모·사유 = 이미 한글 텍스트라 그대로(데미지 유무 등은 버튼 클릭이라 입력창 없음). ⑥ 자연어 질문창(검수앱 음성검색, 콘앱 "7번 베이 콘") = 한글 질문 가능하므로 예외로 그대로.

**구현**
- `src/inputUtils.js` 신규: `stripHangul`(자모·완성형 제거), `toEnglishUpper`(한글제거+대문자), `engChange(setter)` onChange 래퍼, `ENG_INPUT_PROPS`(영문 자판 유도: inputMode text·autoCapitalize characters·lang en), `NUM_INPUT_PROPS`(inputMode numeric), `DECIMAL_INPUT_PROPS`. 한글 차단은 onChange가 실시간 제거(강제), 자판 유도는 props가 보조.
- 검수앱 적용: 선박명/콜사인/CASP코드(ShipMatrix) 영문강제, IMO 숫자. 컨번호(ExtraContainer) 영문강제, 온도 decimal, 실번호 숫자. 실번호·XRAY실·E실(GuidedWorkPanel) 숫자. 컨번호 조회창(BigResultCard) 숫자. 위치 bay/row/tier·실번호·엠티실류(ContainerDetailModal) 숫자. SearchPanel 끝4자리 조회창은 기존 inputMode numeric 유지.
- 한글 유지 확인: 검수원 이름(StaffManager·Inspector), 사유(WorkReport), 데미지내용·메모(ExtraContainer) — 변환 없음.
- 콘앱/벌크앱: 콘앱 입력창은 zoom 슬라이더 + 자연어 질문창(cqaInput, 한글)뿐 → 영문/숫자 대상 없음. 벌크앱은 input/textarea 자체가 없음(버튼 위주). 두 앱은 작업 대상 없음.

**검증(시뮬 `/home/claude/sim/kbd_sim.mjs`)**: toEnglishUpper 6케이스(한글섞임·소문자·자모 등) PASS. 빌드 PASS(V8.09-10), 번들에 한글제거 정규식·숫자패드 반영.

### V8.09-09 — PORT-MIS 부두(berth) 파싱 수정: "계선장소" 다중컬럼 다수결 (실데이터 download.xlsx PASS, 2026-06-18)

**증상(사용자 보고+화면)**: PORT-MIS에 새 엑셀을 올렸는데도 "⚠ 부두 정보 없음 (옛 데이터)" 배너가 계속 뜸. 예: TIAN HAI PING ZE(BOPY4), 저장 06.18 10:17, 매칭 callsign 정상인데 berth 없음.

**원인(실엑셀 download.xlsx 분석으로 확정)**: PORT-MIS 엑셀 헤더의 **"계선장소"가 3개 컬럼에 걸침** — 15=시설코드(MBM), 16=선석번호(15), **17=진짜 부두명("동부두 15번선석")**. 파서는 헤더 매칭으로 15(코드)를 berthRaw로 잡고, 17로 보정하는 로직이 두 가지 결함:
- ① **첫 데이터 행 하나로만** 부두명 컬럼을 탐색. 이 엑셀 첫 행은 "백령 153호 / 남항 모래부두".
- ② 보정 정규식이 `[동서남북]부두|N번선석|...`라 **"남항 모래부두"를 못 잡음**("남항"은 "남부두"가 아님). 첫 행 매칭 실패 → 보정 안 됨 → berthRaw가 15(MBM)에 머묾 → `isValidBerth("MBM")=false` → **전 선박 berth 무효** → "부두 정보 없음".
- 데이터 확인: 현재 정규식은 100건 중 34건만 매칭. 17번 부두명 종류 27가지(동부두/남항/신항/북항/송악부두/돌핀/터미널 등).

**수정(`utils.js parsePortMisExcel`)**: berth 컬럼 보정을 "첫 행 정규식" → **"헤더 berthRaw~+4 컬럼을 여러 데이터 행(최대 40개) 스캔, 부두명 최다 컬럼 다수결 선택"**으로 변경. 부두명 정규식도 확장: `[가-힣]+부두|\d+\s*번?\s*선석|\d+\s*선석|[가-힣]+터미널|돌핀|컨테이너|[가-힣]+항\s`. 한 건도 안 잡히면 헤더 위치 유지(안전).

**검증(실엑셀 100척)**: 수정 후 berth 정상 100/100(기존 다수 무효). TIAN HAI PING ZE → "동부두 15번선석" → PNCT 15번 ✅. PCTC 12척/PNCT 10척 자동분류. 컨테이너 외 부두(모래부두·돌핀)는 berth 텍스트 보존 + pier=null → 회색 "📍부두명" 박스(경고 아님). 빌드 PASS(V8.09-09), 번들에 정규식 박힘.

### V8.09-08 — 새 선박 등록에 LOLO 토글 추가 (대체선 대응, 시뮬 PASS, 2026-06-18)

**배경(사용자 요청)**: 새 선박 등록의 3조건(엠티 실 정책 none/verify/attach)에 **LOLO 조건도 추가** — 본선이 수리·고장 시 투입되는 대체선을 검수사가 직접 LOLO로 지정할 수 있게. (V8.09-07은 RZOR만 코드에 박아 한정했는데, 대체선은 코드 수정 없이 현장에서 등록 가능해야 함.)

**수정(`ShipPolicyModal.jsx`)**
- `lolo` state + LOLO 토글 버튼 추가(엠티 mode와 독립, 항상 표시). "LOLO 선박 (베이플랜 없음)" — 대체선 지정 안내 포함.
- 저장 핸들러: 기존엔 `mode==='none'`이면 정책 미저장이었으나, **`mode==='none' && !lolo`일 때만** 미저장으로 변경. LOLO만 켜고 엠티 정책 none인 대체선도 저장됨. policy에 `lolo:true` 포함.
- `mode==='none' && lolo` 케이스 label = 'LOLO 검수 (베이 없는 명세선) · LOLO'.

**연결**: 저장된 정책은 `fbSaveShipPolicy`가 `...policy`로 lolo 보존 → Firebase `shipPolicies` → `extraPolicies` 구독 → `matchShipPolicy({...DEFAULT,...extra})` 병합 → `isLoloShipByPolicy`(V8.09-07)가 읽음. 등록 즉시 LOLO 판정 반영.

**검증(시뮬 `/home/claude/sim/lolo_register_sim.mjs`)**: 대체선 등록 전 false → LOLO 토글 켜 저장 후 true(선박명·코드힌트), 엠티 verify+LOLO 동시 등록 시 둘 다 유지. 회귀 RZOR true·TPMZ false·TNJP false. 빌드 PASS(V8.09-08), 번들에 토글·판정 박힘.

### V8.09-07 — LOLO 검수 리스트를 RZOR 전용으로 한정 (TPMZ 오판 수정, 시뮬 PASS, 2026-06-18)

**증상(사용자 보고)**: LOLO 항목은 RZOR(RIZHAO ORIENT)만 쓰는데, **TPMZ(일반 베이 선박)에도 적용**됨 — 수석대시보드에 TPMZ LOLO 리스트가 잘못 생성됨.

**원인**: LOLO 판정이 `ChiefDashboard.loloVoyages`·`VoyagePage.isLoloShip` 둘 다 **"모든 컨테이너에 bay/row/tier가 없으면 LOLO"**(위치좌표 유무)였음. RZOR는 IFCSUM이라 좌표가 없는 게 정상이지만, 일반 베이 선박(TPMZ)이 위치정보 없이 올라오면 똑같이 LOLO로 오판. 선박 식별이 전혀 없었음.

**사용자 확정(2026-06-18)**: TPMZ는 베이플랜 있는 일반 선박. LOLO는 **선박명으로 한정 — RZOR만**.

**수정**
- `shipPolicies.js`: RIZHAO ORIENT 정책에 `lolo: true` 추가(TPMZ·TNJP·ATRP엔 없음). alias에 항차 prefix `R063`도 추가(선박명 누락 리스트도 인식). 신규 헬퍼 `isLoloShipByPolicy(vsl, extraPolicies, hints)` — 정책 매칭 후 `lolo===true`만 LOLO.
- `ChiefDashboard.jsx`: `loloVoyages` 판정을 "위치좌표 없음" → `isLoloShipByPolicy(vsl, extraPolicies, [voy·voyage·callsign])`로 교체. 의존성에 extraPolicies 추가.
- `VoyagePage.jsx`: `isLoloShip` 동일 교체. LOLO 탭 노출·자동전환이 이 한 값으로 통제되므로 TPMZ는 베이 탭, RZOR만 LOLO 탭.

**검증(시뮬 `/home/claude/sim/lolo_sim.mjs`)**: RZOR(선박명/alias/R063힌트) → LOLO=true / TPMZ·TNJP·ATRP·미등록 → 모두 false PASS. 빌드 PASS(V8.09-07), 번들에 lolo 판정·R063 박힘.

**주의(현장)**: 코드 변경 후 Firebase에 이미 저장된 TPMZ 데이터는 안 바뀜 — 화면에선 LOLO 리스트가 사라지지만(새 판정), 잘못 생성된 옛 LOLO 흔적은 데이터에 남을 수 있음.

### V8.09-06 — 엠티실 완전삭제 + XRAY 실번호 미입력 시 양하확인 차단 (ATRP/RZOR 실데이터 + 시뮬 PASS, 2026-06-18)

**문제1 — 엠티실 테스트 기록 후 완전 삭제 불가 (ATRP/RZOR)**: 공컨 엠티실(eseal)을 테스트로 입력하면 지울 수 없었다. eseal 표시부에 삭제 버튼이 없고(실오류/리씰만 삭제 가능), 저장 핸들러 `handleSaveEseal`이 attach 모드(ATRP)에서 빈값을 막아 1자라도 남겨야 했다.
- 수정(`ContainerDetailModal.jsx`): `handleClearEseal` 신규 — 빈값으로 `fbSetEmptySeal` 저장(완전삭제), eseal_wrong/reseal은 보존. eseal 표시부 헤더에 `c.eseal`일 때만 보이는 "삭제" 버튼 추가. `fbSetEmptySeal`은 빈값 저장을 차단하지 않으므로 firebase 변경 불필요(이력은 자동 누적).
- 검증: 삭제 전 표시=실번호·삭제버튼 보임 → 삭제 후 표시="미입력"·삭제버튼 사라짐(완전삭제) PASS.

**문제2 — XRAY 대상 실번호 미입력인데 양하확인됨**: XRAY 대상 컨은 실 체결 후 XRAY 실번호(seal)를 입력해야 하는데, 완료 핸들러들이 검증 없이 바로 완료 처리했다.
- 사용자 확정(2026-06-18): **완전히 막기**(실번호 입력 전까지 양하확인 불가), 필수 기준은 **XRAY 실번호(seal)만**(eseal 불필요). 양하(discharge)에만 적용, 선적·비XRAY는 영향 없음.
- 수정(완료 경로 5개 파일 전부): `mode==='discharge' && _xray && !seal.trim()`이면 alert 후 완료 중단.
  - `GuidedWorkPanel.jsx`: `handleConfirm`(예측 카드), `handleFixPick`/`handleTwinFixApply`(수정 경로 — 실제 컨은 `allContainers`에서 _xraySeal 조회). `xraySealMissing`·`xrayMissingByCn`·`blockIfXrayMissing` 헬퍼.
  - `ContainerDetailModal.jsx`(상세 완료), `BigResultCard.jsx`(결과카드), `SearchPanel.jsx`(트윈 일괄), `ContainerList.jsx`(목록).
- 검증(시뮬): seal미입력→차단 / seal입력→완료 / 공백→차단 / 비XRAY→완료 / 선적모드→완료 / 트윈 한쪽 미입력→차단 전부 PASS. 번들에 가드 7곳 박힘 확인.

**빌드 PASS(V8.09-06).** 시뮬: `/home/claude/sim/xray_sim.mjs`, `eseal_sim.mjs`.

### V8.09-05 — 해치커버: 양하 후 선적할 게 있으면 닫지 않음 (STSE 2646W+2645E 실EDI PASS, 2026-06-18)

**증상**: 자동(가이드)모드 양하에서 홀드까지 끝내면 선적 여부를 확인하지 않고 해치커버를 닫으려 함. 같은 베이에 선적할 게 있는데도 닫을지 물음.

**원인(`GuidedWorkPanel.jsx`)**: `holdWorkedD`(클로즈 제안 조건)가 양하 홀드 완료분 존재만 판정하고 선적 잔여를 보지 않음.

**수정**: `holdWorkedD`에 "같은 그룹센터에 평택분 선적 잔여(`_mode==='loading' && _ptk && !_comp`)가 있으면 false(close 보류)" 추가. 선적까지 완료되면 true. 선적 EDI 미업로드면 잔여 0 → 기존대로 닫음(안전).

**검증(실EDI 합본 2646W+2645E)**: 양하홀드 있는 4그룹(8·12·16·24) 전부 선적 잔여 존재 → T1(양하완료·선적미완) close 금지, T2(모두완료) close 제안, T3(선적없음) close 제안, T4(선적 1개 잔여) 금지/0개 제안 PASS.

### V8.09-04 — 양하 규칙3: 40ft 풀 일반→풀 리퍼 (베이그룹 단위, 엠티 제외, 물리종속 우선) (STSE 2646W/2645E 실EDI + 합성 PASS, 2026-06-18)

**배경**: V8.09-03이 "40ft 먼저"는 넣었으나 40ft 안의 일반/리퍼 순서 구분이 없었음. 사용자 규칙3 = "40일반 → 40리퍼 → 20트윈 → 20싱글". V8.09-03 양하 데크 40일반/리퍼 전환 70회 → 미구현 확정.

**사용자 확정 규칙(2026-06-18)**: ① 단위 = 베이 그룹, 풀 40일반 전부 → 풀 40리퍼 전부. ② 엠티(공컨) 40ft는 순서 구분 없음 → 풀 뒤 층 순서. ③ 물리 종속 절대 우선(리퍼가 일반 위면 리퍼 먼저). ④ 선적 FR/OT 마지막은 현행 유지(홀드 FR은 데크 작업 전).

**수정(`guidedQueue.js`)**: `reorderFullReeferLast` 신규(베이그룹→로우스택, 스택 맨위 카드 부류로 풀일반→풀리퍼→엠티 정렬, 스택 내부 보존), `cardIsReefer` 로컬 헬퍼.

**검증**: 회귀(2646W) 티어단조 0·물리종속 0, 합성A(독립) 풀일반<풀리퍼, 합성B(충돌) 리퍼위 먼저, 합성C(엠티) 풀<엠티 전부 PASS. 선적 회귀 불변.

### V8.09-03 — 가이드 양하/선적 순서: 40ft 우선·싱글→트윈→40ft (STSE 2645E 실EDI PASS, 2026-06-17)

**배경**: 자동(가이드)모드 양하에서 "홀드 40피트 우선" 규칙 미준수. 같은 단 40·20 혼재 시 층마다 40↔20 순서 → 스프레더 40전용↔20트윈 반복 전환 → 작업 지연. 40ft 다 끝낸 뒤 20ft여야 함.

**원인**: `guidedQueue.js` 양하 cmp가 티어(층)를 40/20 구분보다 우선 → 층마다 40·20 재혼합. 24번 홀드 시뮬에서 t08·t06마다 전환 발생 확인.

**도메인 사실(사용자 확정 2026-06-17)**: 40ft 위에는 20ft가 절대 안 실린다(40ft 중간 콘 구멍 없음). EDI 281개 40ft 전수 — 같은 단 40ft 위 20ft 0건. → 양하 "40ft 먼저 전부", 선적 "트윈 먼저 다 싣고 40ft 나중"이 적재 종속과 충돌 없음.

**수정(`guidedQueue.js`)**
- 양하: `reorder40FirstForDischarge` — 각 단(데크/홀드) 안 40ft를 20ft보다 앞으로. 종속 안전장치 보존. FR 최우선 양하 유지.
- 선적: V7.99-6 "트윈 아래 40ft 끌어오기" 제거(불필요·순서 파괴). 단 내부 20싱글→트윈→40ft(`buildStageOrder`). 트윈은 POD→로우→바닥티어.
- 선적 데크도 홀드와 동일 통일(사용자 확정): 자동은 20싱글→트윈→40ft, 포트분리·난이도 케이스는 수동 보정.

**검증(STSE 2645E 전 베이, FR 제외)**: 양하 40ft우선 13/13, 선적 홀드 6/6, 선적 데크 7/7 PASS. 16·15·17 "FAIL" 의심은 FR(42P0/22P0) 우선양하 정상동작. 빌드 PASS(V8.09-03).

### V8.09-02 — ★긴급 핫픽스: ITEM 빈칸→Full 강제 제거 (전 선박 공컨 오집계, 실데이터 PASS, 2026-06-17)

**증상(현장 보고)**: V8.09-01 배포 후 **전 선박에서 업로드/리스트 집계 이상** — 공컨(Empty) 개수가 틀림. 일반 베이 선박(STSE 등)에서 선적/양하 집계가 어긋남.

**원인(코드+실데이터로 확정)**: V8.09-01에서 RIZHAO 선적용으로 넣은 ITEM 공컨 규칙에 `else if (itemRaw === '') fe = 'F'`(빈칸=Full 강제)가 포함됨. ITEM 헤더가 **품목·순번 등 다른 용도**인 일반 선박 리스트에서, ITEM 빈칸 행이 전부 Full로 덮어써져 그 아래 정상 판정 경로(TYPE/SIZE 끝글자, SOC seal)로 Empty가 될 컨이 모두 Full로 오집계됨. `parseListExcel`은 전 선박 공통 경로라 전체 영향.

**수정**: utils.js 1883행 `else if (itemRaw === '') fe = 'F'` 한 줄 제거. ITEM "공컨테이너"=Empty만 남김. 빈칸은 아래 정상 경로로 흘려보냄(RIZHAO 선적은 끝글자 없어 기본 Full로 동일 동작).

**검증(V8.08-06 정상본 vs V8.09-02 동일성 + RIZHAO 기능 유지)**
- 일반선 3양식(명시 F/E·ITEM 품목용도·SOC) 모두 V8.08-06과 **완전 동일** → 일반선 회귀 0 PASS.
- RIZHAO R063W: EMPTY 71건 유지, 잡시트 차단 170건 유지 → LOLO 선적 기능 보존 PASS.
- 빌드 PASS(V8.09-02), 번들 반영 확인.

**교훈**: 특정 선박(RIZHAO) 전용 규칙을 전 선박 공통 함수(`parseListExcel`)에 넣을 때, "빈칸=기본값 강제" 같은 광범위 분기는 다른 선박을 덮어쓴다. 선박 고유 규칙은 **양성 조건(공컨테이너=E)만** 잡고, 미해당은 기존 경로에 위임할 것.

### V8.09-01 — LOLO 선적 리스트 수석대시보드 표시 (RIZHAO 선적분, 실데이터 PASS, 2026-06-17)

> ⚠️ 이 버전은 ITEM 빈칸→Full 강제 버그로 전 선박 공컨 오집계 발생. V8.09-02에서 핫픽스. 아래 기능 설명은 유효(규격 매핑·잡시트 가드·LOLO 카드·ITEM 공컨=Empty는 정상).

**배경(증상)**: RIZHAO ORIENT(RZOR) 같은 LOLO 선박에서 수석대시보드에 **양하 LOLO 카드만 뜨고 선적이 안 보임**. 검수사 질문 "선적은 어디서 보나". EDI(IFCSUM/BAPLIE) 없이 LOADING LIST 엑셀만 들어오는 선적분이 카드 생성에서 누락됨.

**원인(실데이터 RD-Loading_List_R063W__FIIS.xls 분석으로 확정)**
- ChiefDashboard `loloVoyages`가 카드 생성을 `sec.ediContainers` 유무로만 판정. EDI 없는 선적 리스트는 버려짐.
- 부수 1: RIZHAO 선적 규격 표기(`20'D`/`40'H`/`40'R`/`45'H`/`20'L`)를 `deriveIso`가 인식 못 함 + `type_i`에 `ty/sz` 없음.
- 부수 2: F/E 판정 — RIZHAO는 엠티에도 실 부착 정책이라 실 유무로 못 가림. ITEM "공컨테이너"가 유일 근거.
- 부수 3: 선적 엑셀에 작업자 메모·이전 항차(062W)·낙서 잡시트 4개 → fallback 과집계(170→213).

**수정(최소 수술적)**
- `deriveIso`: RIZHAO 표기 매핑(`20'D`/`20'L`→22G1, `40'H`→45G1, `40'R`→45R1, `45'H`→L5G1, `40'D`→42G1). 작은따옴표 포함이라 타 선사 충돌 없음.
- `type_i`: 헤더 패턴에 `ty/sz`·`ty sz` 추가.
- F/E: ITEM 컬럼 "공컨테이너"=E. (V8.09-01의 빈칸=F 부분은 V8.09-02에서 제거.)
- 잡시트 가드: `formalSheetParsed` — 정식 헤더 시트가 확정되면 이후 헤더 없는 fallback 셀스캔 끔. 헤더 시트 전무 시에만 fallback 유지.
- ChiefDashboard `loloVoyages`: 카드 생성을 EDI∪records 유무 + 위치좌표 없음으로 변경. 일반 베이 선박 영향 0.

**도메인 확정**
- RIZHAO 선적 LOADING LIST: 규격은 `TY/SZ` 따옴표 표기, F/E는 `ITEM` 컬럼 "공컨테이너"가 유일 근거(실 유무 무효 — 엠티도 실 부착).
- 검수사가 올린 선적 엑셀에 작업자 메모/이전항차 잡시트가 섞여 올 수 있음 → 정식 헤더 시트 우선, 헤더 없는 시트는 정식 시트 있으면 무시.

### V8.04-02 초과 모달 스코프 오류 fix — extraModalOpen is not defined (사용자 보고, 렌더 PASS, 2026-06-16)
- **증상**: 초과 컨 버튼 클릭 안 됨 + 작업 베이→데크/홀드 선택 시 "extraModalOpen is not defined" 크래시(앱 전체 격리).
- **원인**: extraModalOpen 상태·버튼은 SearchPanel 본체에 선언했는데, `<ExtraContainerModal>` 렌더를 자식 컴포넌트 SingleSearch 안(HelpModal 옆)에 잘못 넣음. SingleSearch에서 부모 변수 접근 불가 → 미정의. 이 크래시가 SearchPanel 전체를 죽여 베이/단 선택 등 모든 조작에서 에러 화면이 떴음(두 증상 동일 원인).
- **수정**: ExtraContainerModal을 SingleSearch에서 제거하고 SearchPanel 본체 return 안(상태·버튼과 같은 스코프)으로 이동. 정적 분석으로 선언·사용·렌더가 모두 SearchPanel 함수 안에 있고 SingleSearch에 잔존 없음 확인 PASS.
- **교훈(중요)**: 빌드 성공이 런타임 무결성을 보장하지 않는다 — 미정의 변수 참조는 빌드를 통과하고 런타임에만 터진다. 새 상태/모달 추가 시 **선언·사용·렌더가 같은 컴포넌트 스코프인지 빌드 후 정적 점검**할 것.

### V8.04-01 초과 모달 React error #310 fix (사용자 보고, 렌더 PASS, 2026-06-16)
- **증상**: 초과 컨 추가 시 React error #310 (Rendered fewer/more hooks) — 화면 격리됨.
- **원인**: ExtraContainerModal 본문 안에서 Chip/Field를 정의하고 JSX에서 `<Field>`/`<Chip>` 컴포넌트로 사용. 매 렌더 새 컴포넌트 타입이 되고, 조건부 입력(RF 온도·데미지 내용)이 나타났다 사라지며 Hook 트리가 어긋나 #310 유발.
- **수정**: Chip/Field를 모듈 스코프 함수로 분리(재생성·재마운트 제거). react-dom/server 렌더 테스트 PASS(open=true 마운트 정상, #310 없음).
- 교훈: 컴포넌트 본문 안에서 다른 컴포넌트를 정의해 JSX로 쓰지 말 것 — 모듈 스코프로 뺀다(지침서 0-7 연장).

### V8.04 초과 컨테이너 — 신고정보 입력·리스트 포함·별도 집계·삭제 (사용자 요청, 시뮬·PNG PASS, 2026-06-16) ★메이저

**사용자 요구(누적)**: 초과 컨(신고 리스트에 없는데 내려진 것)을 ① 신고용 기본정보와 함께 입력 ② 검수·양하 리스트와 토탈에 포함(색만 강조) ③ 초과분만 따로 집계·제출(검수리스트처럼) ④ 잘못 넣은 것 수정(삭제).

**작업**
- 입력 모달(ExtraContainerModal.jsx 신규): 번호 + 규격(20/40ST/40HC/45)·적공·타입(일반/RF/FR/OT/TK)·리퍼온도·실번호·데미지 유무를 버튼 탭으로. 기존 window.prompt 2단계 → 모달 교체. (V8.04-03: 데미지 라벨에서 "책임소재" 문구 제거 — 검수사에게 불필요한 압박을 주므로. 데미지 기록은 사실 기록이 목적이고 책임소재는 부수적.)
- 저장(firebase.js): `fbAddExtraContainer(voyageKey,mode,cn,by,info)` — info에 size/fe/ctype/temp/seal/damage/note. completed+extras 양쪽 기록. `fbRemoveExtraContainer` 신설(삭제).
- **리스트·토탈 포함(SearchPanel allContainers)**: extras를 정식 컨으로 병합. size→iso 매핑(20→22G1, 40ST→42G1, 40HC→45G1, 45→L5G1), ctype→rf/fr/ot/tk, `_extra=true`, `_ptk=true`(실제 내려진 평택분). EDI/리스트에 이미 있으면 중복 추가 안 함.
- **색 강조만**: 리스트 행에 앰버 테두리 + "초과" 배지 + 데미지 ⚠. 집계엔 일반 컨처럼 들어감.
- **초과분 별도 집계·제출**: `extraList=allContainers.filter(_extra)`. 양하 탭에 "초과분 N건(신고 대상)" + CSV 내보내기(`초과분리스트_날짜.csv`: 번호·규격·적공·타입·온도·실번호·데미지·메모·기록자) + 행별 ✕ 삭제.

**검증**: node 시뮬 — 초과 2건이 전체/양하토탈에 합류(4대), 초과분만 2건 추출, 리퍼·데미지 보존, CSV 형식 PASS. esbuild 컴파일 무에러. 빌드 PASS(V8.04). PNG 모달·집계 UI 확인 PASS.

### V8.03-01 음성/질문 오답 6건 수정 (실사용 오답리포트 기반, 시뮬 PASS, 2026-06-16)

수석 대시보드에서 내보낸 실제 오답리포트(6건) 분석 후 nlSearch.js 수정. 전부 node 시뮬 PASS + 회귀 11건 PASS.
- **[1] "실 번호 점검"**: 띄어쓰기로 인식 실패 → 정규식에 `실\s*번호` 공백 허용.
- **[2] "리스트 점검"**: 전체 목록만 쏟아냄 → `리스트\s*점검`도 sealAuditQuery(실번호 이상 점검)로 연결.
- **[3] "당신이 하는 일이 뭐야"**: 인식 실패 → introQuery에 `당신/하는 일/할 수/무슨 기능` 추가.
- **[4] "몇 시쯤에 끝나"**: 현재 시각만 답함 → etaQuery 정규식에 "쯤에" 허용, "끝/완료" 의도면 timeQuery 끄고 종료 시각 추정(formatEta)으로. timeQuery에 `!etaQuery` 가드.
- **[5] "24번 홀드에 몇 개 남았어" → 0대 (핵심 도메인 버그)**: 짝수 베이 단독만 봐서 0. 검수사 메모대로 **"24번 홀드 = 23·24·25 트리오 전체"**, 홀수(23·25)는 개별. 짝수 베이 + 구역(홀드/데크) 명시 시 `bayTrio=[b-1,b,b+1]`로 확장, applyNLFilter가 트리오 전체 매칭. 통합 시뮬: 24번 홀드 → 23·24·25 홀드 미완료만 정확 집계 PASS.
- **[6] "특이 사항 있어"**: 인식 실패 → customsReportQuery(이상 건 정리)에 `특이\s*(사항|점|건)`, `문제\s*있/발생` 추가. 실오류·데미지 등 발생 문제를 정리.
- 회귀: 지금 몇 시·오늘 며칠·20번 베이·끝4자리 조회·리퍼·넌 뭐야 등 11건 모두 정상 유지(짝수 단일 베이는 zone 없으면 트리오 확장 안 됨).

### V8.03 오답 리포트 '저금통' 내보내기·비우기 (사용자 요청, 시뮬·PNG PASS, 2026-06-16)

**사용자 비유**: "오답을 적으면 저금통에 동전 넣듯 보관 → 저금통(파일)을 클로드에게 전달 → 빈 저금통 비우기." 음성 답변 오답을 앱이 모으긴 하는데(수석 대시보드 오답 리포트, feedback 노드) 파일로 빼서 전달할 방법이 없어 불편.

**작업 (ChiefDashboard.jsx + firebase.js)**
- 오답 리포트 섹션 헤더에 [📥 내보내기] [🧹 비우기] 버튼 추가.
- 내보내기: feedback 전체를 사람이 읽기 쉬운 .txt로 다운로드(`오답리포트_YYYY-MM-DD.txt`) — 질문Q·앱이 한 답·검수사 메모·파싱 요약·선박·검수원·버전 포함. 클로드가 바로 분석 가능한 형식.
- 비우기: **내보낸 시점의 ts 목록만 삭제**(`fbClearFeedback(tsList)`) — 안 본 오답 보호. 내보내기 전엔 비활성. firebase.js에 `fbClearFeedback(tsList=null)` 신설(목록 없으면 전체).
- 흐름: ❌오답 적힘 → 저금통 누적 → 📥 파일로 빼서 클로드 전달 → 🧹 내보낸 것만 비움 → 새 오답 재누적.

**검증**: node 시뮬(내보내기 텍스트 형식·비우기 대상 ts) PASS. 빌드 PASS(V8.03), 번들 반영 확인. PNG UI 시각 확인 PASS.

### V8.02-01 도움말 — 늘어난 기능 보강 (초보 검수사용, PNG PASS, 2026-06-16)

**사용자 지적**: V8.02에서 changelog를 통째로 빼면서, 그 안에 섞인 "신규 기능 사용법"까지 같이 사라졌다. 기능은 많이 늘었는데 도움말 내용은 옛날 그대로. 독자는 초보 검수사 — "어떤 문제도 앱이 해결해준다"는 안내가 필요.

**작업**: 버린 changelog 652항목을 재분석 → 기능 사용법 성격 222건 추출 → 중복·개발용어 제거 후 핵심만 초보 눈높이로 재작성. 도움말 137 → 177항목.
- basic: 브리핑(작업 시작 전 요약), 작업 완료 처리(컨 1개/양하·선적 분리) 추가.
- special: 리퍼·특수화물 베이별 위치 조회 추가.
- voice: 음성 오인식 자동 교정, 교정 사전 추가 안내.
- ai: 콘(고박장비) 계산 질문 추가.
- **신규 카테고리 report(보고·출력)**: 사진 보고(실오류/데미지, 2장 필수), 출력 3종(인쇄/PDF/이미지·CSV), 장비번호(1~4호기).
- **신규 카테고리 newship(신규 선박)**: EDI 자동 등록(95% 성공), 보정중 표시, 베이 그림 맞추기(칸 추가·삭제·복사, 빈 자리 점선).
- tips: "🆘 막혔을 때 — 초보 가이드"(화면 멈춤·카메라·검색·카톡 전송 대처) 추가.
- 카테고리 7 → 9개. 색 추가: report=pink, newship=teal (정적 클래스, purge 확인).

**검증**: node 시뮬 9카테고리·177항목 렌더 PASS(크래시 0). 빌드 PASS(V8.02-01), 신규 색 CSS 포함 확인. PNG 3화면 시각 확인 PASS.

### V8.02 도움말 전면 재구성 — 사용설명/용어 2갈래 + 시각 카드 (사용자 요청, PNG PASS, 2026-06-16) ★메이저

**사용자 요청**: 도움말 '팁' 탭에 개발 변경이력(changelog) 600여건이 쌓여 검수사용으로 부적합. "개발과정은 지침서에 있으니 빼고, ① 앱 사용설명·기능 ② 검수 용어·회화 두 갈래로 구분. 한눈에 눈으로만 봐도 알게."

**구조 재편 (HelpModal.jsx 1966줄 → 202줄)**
- 데이터를 `src/data/helpData.js`로 분리. changelog 612건 제거(통합지침서에 이미 보존 — 정보 손실 없음), 검수 실사용 137항목만 유지 + 검수 용어 18개 신설.
- 첫 화면: 두 갈래 큰 카드(① 사용설명·기능 ② 용어·회화).
- 사용설명: 7개 카테고리 아이콘+색상 카드 그리드(기본검색=sky, AI=violet, 음성=rose, 특수화물=amber, 항구=emerald, 트윈=cyan, 실전팁=yellow). 색만 봐도 구분.
- 용어·회화: 영어회화집(ContainerPhrasebook) 진입 + 용어 풀이 카드(POL/POD/베이/단/리퍼/DG/X-RAY 등 18개).
- 렌더는 `sec.examples || sec.items || sec.rows || []`로 방어(V8.01-01 fix 계승) — 키 혼재해도 안 터짐.

**핵심 안전장치**
- Tailwind 액센트 색은 정적 클래스(ACCENT 맵)로 작성 → purge 회피 확인(빌드 CSS에 7색 모두 포함 검증). 동적 `bg-${color}` 금지(색 누락 사고 예방).
- 미사용 import(Camera/Forklift/BarChart3/Send) 제거(0-3 orphan 정리).

**검증**
- node 시뮬: 전 7카테고리·137항목·18용어 렌더 안전 PASS(크래시 0).
- 빌드 PASS(V8.02), 번들에 신로직 반영, 액센트 7색 CSS 포함 확인.
- PNG 렌더(wkhtmltoimage) 3화면 시각 확인 PASS — 카드 색 구분·가독성 양호.
- props 호환: Header.jsx·SearchPanel.jsx 호출부(open/onClose) 무변경.

### V8.01-01 도움말 '팁' 탭 재로딩 버그 fix (사용자 보고, 시뮬 PASS, 2026-06-16)
- **증상**: 도움말(HelpModal)에서 '팁' 탭을 누르면 화면이 멈춰 재로딩해야 함.
- **원인**: 팁 탭 일부 블록(M6.91~M6.93.0, 5개)이 `examples:` 대신 `items:` 키를 쓰는데, 본문 렌더는 `sec.examples.map(...)`만 호출 → 그 블록에서 `undefined.map` 런타임 에러 → 화면 멈춤(지침서 Ⅱ-7.6 런타임 에러 부류).
- **수정(HelpModal.jsx)**: 렌더를 `const rows = sec.examples || sec.items || []`로 방어 — examples/items 둘 다 수용, 없으면 빈 배열. 향후 같은 키 실수도 무해화.
- **검증(node 시뮬)**: 전 8개 탭·163블록 렌더 안전성 PASS (크래시 0, 빈 블록 0). tips 139블록 전부 정상(items 5개 포함).
- 교훈: 같은 컬렉션을 .map 하는 렌더는 항목 스키마가 섞일 수 있으니 키를 방어적으로 받는다.

### V8.01 자동삭제 기준 = 작업활동 + PORT-MIS stale 날짜 회피 (사용자 확정, 시뮬 PASS, 2026-06-16)

**배경(사용자 의도)**: EDI를 작업 일주일 전에 미리 넣는 운영이 있다. 기존 자동삭제는 `createdAt`(항차 생성=EDI 투입 시각) 기준 7일이라, **작업 시작도 안 한 항차가 사라지는** 위험이 있었다. 또 PORT-MIS에 같은 선박의 지난 항차가 남아 있으면 EDI를 미리 넣어도 항차별 날짜가 stale(지난 날짜)로 찍혔다.

**A. 항차 자동삭제 기준 변경 (HomePage.jsx)**
- 자동삭제 기준을 `createdAt` → **마지막 작업 활동 시각**으로 변경. 공용 헬퍼 `lastWorkAt(v)`(모듈 스코프) 신설 — 양하/선적 완료 기록의 `at`, 선적 실체위치 `actual_at`, `info.lastActive` 중 최댓값.
- **안전장치 ①(핵심)**: 작업 활동 증거가 하나도 없으면(`worked===0`) 절대 삭제 안 함 → EDI만 미리 넣고 작업 안 한 항차는 영구 보호.
- **안전장치 ②**: `createdAt` 없는 옛 항차 보호(기존 유지). 백업 실패 시 삭제 보류(M7.18b 유지).
- "곧 자동삭제" 경고 표시도 같은 기준(작업활동)으로 일치 — 작업 안 한 항차엔 경고 안 띄움(불안 방지). `lastWorkAt`을 HomePage·VoyageCard 둘 다 쓰므로 **모듈 스코프 함수로 정의**(컴포넌트 내부에 두면 VoyageCard에서 미정의 런타임 에러 → 카메라 멈춤 위험. 지침서 Ⅱ-7.6).
- 검증(node 시뮬 8/8 PASS): EDI 8일전+작업안함=보호 / EDI 8일전+어제작업=보호 / 작업 8일전=삭제 / createdAt없음=보호 / 실체위치 6일전=보호·8일전=삭제 / lastActive 9일전=삭제.

**B. PORT-MIS stale 날짜 회피 (VoyagePage.jsx, 지침서 7.8)**
- 콜사인 prefix 후보가 여럿일 때(같은 선박 이번/지난 항차 공존) **최신 `updatedAt` 우선 정렬** 추가 → vesselName/berth 선택 로직이 동조건이면 항상 최신 데이터를 집어 stale 날짜가 안 찍힘.
- 검증(node 시뮬): 같은 선박 2항차(지난 8일전/이번 1일전) → 이번(최신) 선택 PASS.
- **두 번째 방향(vesselName 공유 stale 키 물리 삭제)은 미채택**: 같은 선박이라도 이번/지난·인천/평택 fallback(M5.90) 항차가 정당하게 공존할 수 있어, 물리 삭제는 정상 데이터 유실 위험(Ⅱ-7.2). A 정렬 수정으로 매칭이 최신을 집으면 stale 날짜 문제는 실질 해소되므로 무리한 삭제는 하지 않는다. → **부록 D 6번 백로그 해소.**

### V8.00 인수인계서 — 남은 작업+양하신고+특이사항 + 되묻기 (사용자 요청, 시뮬 PASS, 2026-06-16) ★메이저

**배경(사용자 의도)**: 그날 작업을 마무리 못하고 다음 검수사에게 넘길 때 "인수인계 자료 만들어줘"/"인계 해야돼"라고 하면, 남은 작업·(양하 남으면)양하신고할 것·특이사항을 한눈에 보기 좋게 정리. AI는 부탁받으면 **특이사항/더 전달할 게 있는지 되묻고**, 답을 들어 합쳐 정리.

**구현(nlSearch generateHandover + SearchPanel 2단계 흐름)**
- 파싱: handoverQuery — "인수인계", "인계 자료/서/할/해", "넘겨야", "교대", "다음 검수사", "작업 마무리 못". customs/eta보다 먼저.
- **generateHandover(allContainers, info)**: 평택분(_ptk) 양하·선적 둘 다 _mode로 집계.
  - ① 남은 작업: 양하/선적 각 남은·전체·완료 + 남은 베이 분포(어디가 남았는지).
  - ② 양하신고(양하분 있으면): formatCustomsReport와 동일 판별(누락 flag·초과 flag/_src=edi·바뀜·리씰 sl_orig≠sl·실오류 auditSeals) 요약.
  - ③ 특이사항: 미완료 중 리퍼 온도미입력(isReeferContainer & !tmp & fe≠E)·위험물(dg)·FR/OT 자동 검출.
  - ④ 검수사 메모: info.extraNote(되묻기로 받은 것) 합침.
- **2단계 되묻기(SearchPanel SingleSearch)**: handoverQuery면 localAnswer로 초안+되묻기 문구 표시 + 아래 textarea("더 전달할 내용") + [✓ 인계서 완성]/[특이사항 없음] 버튼. 버튼 누르면 handoverFinalized=true → extraNote 합친 최종본 재생성. 새 질문 시 리셋(useEffect). 음성은 conversational(부드럽게).
- state: handoverNote, handoverFinalized (SingleSearch). localAnswer 의존성에 추가. KNOWN 어휘에 인계·인수·교대·넘겨·특이사항·전달 추가.
- HelpModal 사용법 등록.

**검증(node 실함수)**: 파싱 4종("인수인계 자료/인계 해야돼/넘겨야 해/마무리 못했어") 인식. 합성(양하10[완료6+누락1+미완3]·선적8[완료3]) → 남은 양하3·선적5 + 남은베이(13,14,15/22) + 누락1 + 특이사항(리퍼 온도미입력·위험물) 정확. 메모 반영 최종본에 검수사 메모 추가 PASS.

**V8.00 의의**: V7.92~V8.00에 걸쳐 "AI 대화형 도우미" 축이 완성됨 — 자기소개·시간·날씨·입출항(7.92) → 음성 복원(7.80) → 완료 예정시각 대화체(7.99-15) → 양하신고 점검(7.99-16) → 인수인계(8.00). 모두 **로컬 계산 + 자연어 인식**(AI 답변 생성 없음, 비용 0, 환각 0) 원칙 유지. TTS 자연스러운 목소리 선택(7.99-15)도 포함.

**미연결(다음)**: 인계서 카톡/이미지 공유(kakaoShare 연동), 바뀜(swapped) 자동 기록(번호수정 흐름), 인계서 Firebase 저장해 다음 검수사 로그인 시 자동 표시.

### V7.99-16 양하신고 점검 — 그날 이상 건 정리 (사용자 요청, 시뮬 PASS, 2026-06-16)

**배경(사용자 의도)**: 양하 작업 종료 후 세관 양하신고 시, 그날 발생한 이상(실오류·리씰·컨테이너 바뀜·누락·초과)을 신고서에 적는다. "양하신고할까?" 하면 AI가 그날 이상 건을 자료 화면과 함께 정리해 준다.

**판별 기준(사용자 확정): 신고 리스트(세관 신고 목록)가 기준.**
- 누락 = 신고 리스트엔 있는데 선박에 없는 컨. **검수원이 "선박에 없음"으로 완료**(누락도 완료 상태의 하나).
- 초과 = 신고 리스트에 없는데 실제 내려진 컨. **기록할 장소가 없어 신설.**
- 둘 다 사용자 정의: "누락은 선박에 없는 컨이고 그상태에서 완료, 초과는 이미 내려져 있으므로 더 내릴게 없으면 완료, 기록할 장소는 신설."

**구현(전체 — 데이터+UI+답변)**
1. **데이터(firebase.js)**: `fbCompleteContainer`에 `flag`('normal'|'missing'|'extra'|'swapped')·`note` 인자 추가(기본값 normal → 기존 8개 호출부 회귀 0). 완료 기록이 `{by,at,flag,note}`. 초과 전용 `fbAddExtraContainer` 신설 — completed/{cn}에 flag:'extra' + extras/{cn} 별도 노드.
2. **초과 컨 합치기(VoyagePage)**: 양하 병합부에서 `sec.extras`를 컨 목록에 `_src:'extra'`로 추가(없으면 집계에서 빠짐). 의존성 배열에 sec.extras.
3. **UI**: BigResultCard 양하 미완료에 [🚫 선박에 없음(누락 처리)] 버튼(handleMissing → flag:'missing'). SearchPanel 양하 모드에 [➕ 초과 컨 추가] 버튼(prompt 번호입력 → fbAddExtraContainer). 바뀜은 기존 번호수정 흐름 + flag:'swapped'(향후 연결).
4. **답변(nlSearch formatCustomsReport)**: "양하신고할까?"/"신고할까"/"세관 신고"/"이상 건" 인식(customsReportQuery, 최우선 라우팅). 5종 분류 —
   - 누락 = flag 'missing' (+보조: 리스트에 있는데 미완료 = 작업종료 시 누락 의심 안내)
   - 초과 = flag 'extra' (+보조: _src==='edi'인데 완료)
   - 바뀜 = flag 'swapped'
   - 리씰 = sl_orig ≠ 현재 sl (정규화 비교)
   - 실오류 = auditSeals(중복·혼입·자리수) 재사용
   첫 줄 = 음성 요약("이상 5건 — 누락 1, 초과 1, 바뀜 1, 리씰 1, 실오류 1"), 이후 = 종류별 컨번호·위치·내용 목록. 이상 0건이면 "그대로 신고하세요" + 미완료 컨 있으면 누락 확인 안내.

**데이터 흐름(확인)**: compMap = sec.completed 통째 → `_comp = {by,at,flag,note}`로 주입(SearchPanel 63행). _src: 'edi'|'list'|'both'(VoyagePage 병합) + 'extra'(신설). formatCustomsReport는 평택분(_ptk) 컨에서 flag·_src·sl_orig로 판별.

**검증(node 실함수)**: 합성 양하(정상3+누락1+초과1+바뀜1+리씰1+실오류1) → 5종 정확 분류, 음성 요약 정확. 이상 없음 케이스 PASS. 파싱 회귀 8/8(신고 질문만 customsReport, 베이조회·eta·progress·컨번호 안 뺏김). KNOWN 어휘에 신고·세관·누락·초과·바뀜·리씰·이상·건 추가.

**미연결(다음)**: 바뀜(swapped)을 BigResultCard 번호수정 흐름에 flag로 자동 기록(현재는 함수만 준비). 신고 점검 화면에서 각 건 클릭→해당 컨 카드 이동. 작업 "종료" 신호와 연동해 누락 보조판별 자동 트리거.

### V7.99-15 완료 예정 시각(대화체) + 자연스러운 TTS 목소리 (사용자 요청, 시뮬 PASS, 2026-06-16)

**배경(사용자 의도)**: 검수사는 종일 숫자만 보는 단조롭고 고된 일이라, 앱이 말을 걸어주면 덜 지친다. 정밀 계산보다 "대화감"이 핵심. AI 도입 비용을 길게 논의했으나, 이 기능은 데이터에 이미 완료 타임스탬프가 있어 **AI 없이 로컬 계산으로 충분**(비용 0)하다고 결론.

**A. "몇 시에 끝나?" 완료 예정 시각 (nlSearch.js formatEta)**
- 데이터 출처: 완료 시각은 firebase.js 205행 `set(r,{at:Date.now()})`로 컨마다 기록됨. nlSearch에는 `c._comp = compMap[cn]`(= {at} 객체)로 들어옴. 즉 `c._comp.at`이 완료 시각. **사용자가 속도를 말할 필요 없이 데이터에서 실제 페이스 직접 계산.**
- 파싱: etaQuery — "몇 시에 끝", "언제 끝", "이 속도/페이스면", "얼마나 걸려", "퇴근 몇시", "점심 전에" 등. **progressQuery보다 먼저 평가**(안 그러면 "얼마나 남았고 몇시에 끝나"가 progress로 샘). 단순 "몇 개 남았어"는 그대로 progress='pending'.
- 계산: 완료된 컨의 at을 시간순 정렬 → 최근 20개(없으면 전체)의 (개수-1)÷시간차 = 시간당 페이스(초반 변동 있어 최근 페이스가 체감에 맞음). 남은 = 평택분 전체−완료. 남은시간 = 남은÷페이스. 완료시각 = 현재(기기 시계)+남은시간.
- 답변: **문자열 반환**(다른 답변과 일관 — 객체 반환은 SearchPanel localAnswer.split('\n')에서 깨짐). 첫 줄 = 음성용 대화 한 문장("40대 남았어요. 지금 페이스면 약 1시간 36분, 3시 57분쯤 끝나겠네요. 막바지네요"), 이후 = 화면 상세(예상완료/남은/페이스/남은시간). 남은 양에 따라 응원 한마디(≤10 "거의 다 왔어요" / ≤25% "막바지" / ≥75% "차근차근").
- 경계 방어: 완료0 "아직 시작 전" / 표본<2 "조금 더 진행되면" / 전부완료 "다 끝났어요 수고하셨습니다" / 페이스 0 안내. 크래시 없음.
- 검증(node 실함수): 합성 100대(완료60, 최근20대=시간당25대) → "시간당 약 25대 · 남은 40 · 1시간 36분" 정확. 5개 표현 다 인식, 회귀("몇 개 남았어"·"완료 몇 대"는 progress 유지) PASS. 경계 4종 PASS.

**B. 자연스러운 한국어 TTS 목소리 (voice.js)**
- 문제(사용자 제보 "딱딱하다"): 기존엔 `u.lang='ko-KR'`만 줘서 브라우저가 첫 번째(가장 기계적인) 목소리를 씀. Gemini 탓이 아니라 **폰 내장 TTS 목소리 미선택**이 원인.
- 수정: `getVoices()`에서 향상/네트워크 음성 우선 선택(enhanced/premium/natural/neural/네트워크 → Google → iOS 음성명 → localService=false 가점). 비동기 로드 대응(onvoiceschanged). speak()·speakContainer()에 `u.voice` 적용(없으면 기본 — 회귀 없음).
- 대화 모드: speak에 `opts.conversational` — 검수 호출은 빠르게(rate 1.3 유지), 말 거는 답변(eta)은 느긋·부드럽게(rate 1.0, pitch 1.08). SearchPanel 음성 출력에서 `parsed.etaQuery`면 conversational 적용.
- KNOWN 어휘 동기화: SearchPanel KNOWN 정규식에 끝·끝나·페이스·속도·퇴근·점심·걸려·쯤·예상·마치·종료 추가(파서 어휘 늘리면 KNOWN도 늘려 불필요한 AI 복원 방지 — 영구 원칙).
- HelpModal에 "몇 시에 끝나?" 사용법 등록.
- 비용: 0원(로컬 계산 + 내장 TTS). 향후 더 자연스러운 음성 원하면 클라우드 TTS(유료) 검토 가능하나 현 단계 불필요.

### V7.99-14 베이플랜 기본 줌 22% + 저배율 컨번호 끝4자리 표시 (사용자 제보, PNG PASS, 2026-06-16)

**A. 기본 줌 25% → 22%**
- 사용자 요청: 큰 베이를 한 화면에 보려면 22%가 적절. (V7.99-12에서 25%로 했던 것을 22%로 하향)
- 수정: BayPlan.jsx 46행 기본값·586행 리셋 모두 0.22. 하한 0.15·0.01 단위 조절은 유지.

**B. 저배율에서 컨번호 끝 4자리 표시 (컴팩트 모드)**
- 증상: 22%(셀 24×19px)에서 셀이 너무 작아 컨번호·POL/POD 등 5줄 정보가 다 뭉개져 확인 불가.
- 원인: fontSize가 `max(8, 10*zoom)`이라 저배율에서 8px로 클램프되는데 셀 폭은 줌 따라 작아져 글자가 넘침/안 보임. 게다가 셀 안에 5줄(POL/POD·컨번호·선사/중량·특수·위치)을 다 그림.
- 수정: 셀 폭(cellW)이 42px 미만이면 컴팩트 모드 — 컨번호 **끝 4자리만** 셀 중앙에 꽉 차게(`(c.cn).slice(-4)`). 폰트는 셀 폭에서 역산(`floor((cellW-4)/2.6)`, 6~13px 클램프). 나머지 줄·우상단 타입심볼·시프트·스택 배지·좌측 타입바는 컴팩트에선 숨겨 4자리에 공간 몰아줌.
- XRAY/선사 구분 유지: 컴팩트에서도 XRAY는 셀 배경(getCellBg), 선사는 글자색(getOpColor)으로 그대로 표시 — 배경색 규칙 위반 아님(XRAY 전용 배경은 허용).
- 임계 기준은 줌 값이 아니라 실제 cellW — baseW(폰 110/데스크 140)와 무관하게 일관 동작. 40%↑에선 컴팩트 해제, 기존 5줄 전체 표시.
- 검증(PNG PASS): puppeteer 렌더로 22/25/30/40% 확인. 22%(24px)에서 3147·2640·0028 등 끝4자리 또렷, XRAY 보라배경·선사 글자색 유지, 0028처럼 앞자리 0도 정확. 40%는 전체 컨번호 복귀 확인.

### V7.99-13 자연어 파서 인식 개선 + PORT-MIS 오매칭/삭제 위험 수정 (사용자 제보, 시뮬 PASS, 2026-06-16)

**A. 자연어(nlSearch) 인식 — 현장에서 "할 수 있는 질문도 못 알아듣는다" 제보**
- 증상1: "20번에 몇 개" → "20번"을 베이가 아니라 컨번호 끝4자리(digits)로 오인. "베이" 단어가 없으면 베이 인식 안 됨.
- 증상2: "18번 데크 리퍼 온도" → bay=18을 못 잡고 개수만 답함(온도 위치 미표시).
- 원인: 베이 정규식이 "N번 베이"/"베이 N" 형태만 인식(nlSearch.js 117행). "N번"만으로는 베이로 안 봄.
- 수정: "N번"(1~2자리+'번', '호기' 제외)을 베이로 인식하는 패턴 추가. hasBayCtx에도 "N번" 포함해 digits 오인 차단.
- 검증: 회귀 11케이스 PASS — 끝4자리 조회(3523·SKLU…11자리)는 digits 유지, "N번"은 bay, "2호기"는 베이 아님. KSKM 실데이터 "20번 베이"=="20번에 몇 개"=17대 일치. 합성 평택 리퍼로 "18번 데크 리퍼 온도"→위치+온도([-18°C]) 표시 확인.
- 참고: 온도 표시 필드명은 tmp(temp 아님) — formatLocationList 825행 `c.rf && c.tmp` 정상.

**B. PORT-MIS 오매칭 + 삭제 위험 (V7.30 XIN TAI PING 계열, 위험 버그)**
- 증상: DXQD(name=XIN QUN DAO, 콜사인 H3OI, 19베이 EDI215대)인데 PORT-MIS가 "그린오션호(키 131062)"로 오매칭(dict-fullname). "옛 데이터 삭제"를 누르니 또 다른 틀린 선박명 로딩.
- 원인1(매칭): VoyagePage 풀네임 매칭(673행)의 둘째 조건 `pn.includes(dictNameNorm.slice(4, 4+8))`이 너무 느슨 — 부분 슬라이스가 무관 선박과 우연히 겹쳐 오매칭.
- 원인2(삭제): 삭제 버튼이 `!pm.berth`(부두없음)면 "옛 데이터"로 보고 matchedKey 삭제 → 오매칭된 멀쩡한 다른 배를 지움. 삭제 후 매칭 로직이 또 다른 후보를 잡아 다른 틀린 선박명 로딩.
- 수정1: 풀네임 매칭을 양방향 전체 포함만 인정(`dictNameNorm.includes(pn) || pn.includes(dictNameNorm)`), 양쪽 길이≥5 가드, 부분 슬라이스 제거.
- 수정2: 삭제 confirm에 선박명 명시 — "선박명: {pm.vesselName} / ⚠ 작업 중인 배({vsl})와 다르면 잘못 매칭된 것, 삭제 말고 새 엑셀 업로드".
- 검증: portmis_match.mjs 6케이스 PASS — 정상 풀네임 일치(XIN QUN DAO, DONGJIN CONFIDENT) 유지, 약자/무관 선박(DXQD↔그린오션) 차단.
- 한계(데이터로 확인): DXQD name(XIN QUN DAO)과 "그린오션호"는 영문↔한글이라 슬라이스로도 안 겹침. 즉 실제 오매칭은 콜사인 등 다른 경로일 가능성 — 추가 로그 확인 필요. 단 슬라이스 제거 + 삭제 선박명 명시로 위험은 크게 감소.

### V7.99-12 베이플랜 기본 줌 25%·하한 15%·0.01단위 (메모8 후속, 렌더 PNG PASS, 2026-06-15)
- V7.99-11(스냅 제거)에도 큰 베이(데크 많음+홀드 14칸 등, 예 BAY 11)는 기본 줌 30%에서 한 화면에 안 들어와 홀드가 잘렸다(사용자 제보). 줌 하한이 0.3으로 막혀 더 못 줄였던 것이 원인.
- 수정: 줌 하한 0.3→0.15(휠·핀치·버튼 전부), 버튼 step 0.1→0.01(미세 조정), 기본값 0.3→0.25, 맞춤(리셋) 버튼 모바일 0.3→0.25.
- 검증(폰뷰 412×915, KSKM): 25%에서 BAY (10)11 데크+홀드 전체가 한 화면에, 위 (08)09·아래 (12)13까지 보임. 글자 가독성 유지 PASS.
- 파일: BayPlan.jsx. (콘앱 베이플랜은 자체 구현이라 별개 — 필요 시 동일 조정.)

### V7.99-11 베이플랜 — 베이 선택 시 앞·자기·뒤 한 화면에 (메모8, 렌더 PNG PASS, 2026-06-15)

**왜**
- 검수사가 진행상황을 한눈에 보는 베이플랜(BayPlan)에서, 베이를 고르면 그 베이 하나가 화면을 꽉 채우고 다음 베이는 절반만 잘려 보였다. 아래엔 여백이 남았다. 사용자: "24번 클릭하면 23 (24)25가 다 보이게, 8번이면 7 8 9가 보이게. 아래 여백을 없애면 됨. 단순 작업."
- ⚠ 처음에 메모8을 "짝꿍베이 트리오 19(20)21 미표시"로 오해해 카고플랜 evenBay/oddBay 매핑·자리이동 위험을 길게 진단했으나, 실제 화면은 BayPlan이고 짝꿍은 이미 "(24)25"로 정상 표시됨. 사용자 스크린샷으로 바로잡음 — 매핑·트리오와 무관. (교훈: 화면 증상은 코드 추측 전 실제 스크린샷으로 대상부터 특정.)

**원인**
- 베이 그리드 본체(BayPlan ~822행)가 `scroll-snap-type: y mandatory` + 각 페이지 `scrollSnapAlign:'start'`로 **한 베이 = 한 화면**으로 스냅. 이 스냅이 "여러 베이 같이 보기"를 막고 베이 하나가 뷰포트를 꽉 채워 아래 여백을 만들었다.

**어떻게**
- scroll-snap 제거(snapType·snapAlign·snapStop 삭제) → 베이들이 자연스럽게 이어 그려져 한 화면에 여러 베이가 들어옴. 페이지 간격 space-y-6→space-y-3 축소.
- 베이 점프 select onChange: scrollIntoView 대상을 선택 i가 아니라 **i-1(한 페이지 앞)**으로 → 24 선택 시 그 앞(23/(22)23)이 맨 위로 와서 23·(24)25가 한 화면에. 첫 페이지면 자기 자신(max(0,i-1)).

**검증**(크롬 폰뷰 412×915, KSKM): BAY (24)25 선택 → 화면 맨 위 (22)23, 그 아래 (24)25 전체(데크+홀드), 이어서 (26)27 시작까지 한 화면 PNG PASS. 스냅으로 한 베이가 뷰포트 꽉 채우던 현상·아래 여백 해소.
- 파일: BayPlan.jsx. 시뮬: /home/claude/sim/bayplan_test.* (BayPlan 단독 번들 — 정적 의존만이라 firebase 없이 마운트, react alias dedupe, puppeteer 폰뷰 PNG).

### V7.99-10 수동 작업에도 베이/홀드·데크 선택(A안) + 단 맥락 질문 2종 (메모6 수동, KSKM 실데이터 PASS, 2026-06-15)

**왜**
- 메모6의 가이드(자동)는 V7.99-8에서 베이→홀드/데크 선택을 넣었으나, 수동(SearchPanel)에는 단 지정이 없었다. 수동 작업 중에는 수석이 작업 위치를 알 수 없고, 자유 조회라 선박 전체가 대상이라 끝4자리 중복 오양하 위험이 남았다.
- 사용자 확정(A안): 수동도 가이드와 동일하게 **베이→홀드/데크 반드시 선택**. 그래야 수석에게 전달되고 조회가 그 단으로 좁혀진다. 단, 질문(자연어·날씨·입출항·리퍼·XRAY)은 베이 지정과 별개로 언제든 가능.
- 핵심 질문 의미 구분(사용자): "홀드 **몇 개** 남았어"(에 없음) = 작업 남은 **홀드가 몇 곳**인지(베이 수). "홀드**에** 몇 개 남았어"(에 있음) = 현재 작업 중인 홀드 **안 컨테이너 수**. 앞 답은 되묻지 않게 베이+곳수 한 번에("4, 12, 20 3곳입니다").

**어떻게**
1. SearchPanel(수동) 베이→단 게이트(V7.99-8 가이드 UI 재사용): manualBay·manualTier state, manualGroups 집계(데크/홀드×20/40), 단 선택 후 "📍 N번 홀드 작업 중 [단 변경][베이 변경]" 칩. workFilter 변경 시 리셋.
2. 수석 전달: 수동 선택 시 fbSetInspectorActivity(auto=false)로 호기(수동은 빈값)·베이·단·잔여 기록 — 가이드와 같은 inspectors 노드. ChiefBayEdit 배지(V7.99-8)가 그대로 표시.
3. 오양하 방지: SingleSearch results 정렬에 inManualTier 최우선(rank=-1) — 끝4자리 조회 시 현재 단 컨을 맨 위로. 완전 숨김은 안 함(통과화물·단 밖도 찾아줘야 함 — V7.53 회귀 방지), 우선 노출로 오선택만 방지.
4. 단 맥락 질문 2종(nlSearch): parseNaturalQuery에 tierPlaceCountQuery('홀드 몇 개'='hold'/'deck')·tierInContextQuery('홀드에 몇 개'). "에" 유무 + (개|군데|곳) + 남았 패턴, 숫자 베이 명시("20번 홀드") 제외. generateLocalAnswer가 4번째 인자 ctx(mode·bayPairs·selectedGroup·selectedTier) 받아 formatTierPlaceCount(작업 남은 단 center 모아 "베이 N곳")·formatTierInContext(선택 단 컨 수) 처리. SingleSearch가 manualCtx 전달.

**검증**(KSKM 2611N 양하): "지금 홀드 몇 개 남았어"→"4, 12, 20 3곳입니다" / "지금 홀드에 몇 개 남았어"(ctx 그룹20)→"19·20·21번 홀드에 14개 남았습니다" / 데크 버전 정확 / 맥락 없이 "홀드에"→베이 선택 안내. "에" 유무 구분 PASS.
- 파일: SearchPanel.jsx(게이트·manualCtx·활동기록·조회정렬), nlSearch.js(질문2종 파싱·답변·hasAnyCondition). 시뮬: /home/claude/sim/tierq_test.mjs.

### V7.99-9 해치 프롬프트 자동/수동 전환 시 재등장 버그 (메모10, 시뮬 PASS, 2026-06-15)

**왜 (현장 증상)**
- 해치커버를 열거나 닫은 뒤, 자동 모드 → 수동 모드 → 다시 자동 모드로 가면, 이미 처리한 해치커버를 **또 열거나 닫아야만** 다음 작업이 진행됐다.

**원인**
- SearchPanel의 `guideMode` 토글은 GuidedWorkPanel을 조건부 렌더(삼항)로 켜고 끈다 → 수동 전환 시 GuidedWorkPanel이 **언마운트**, 자동 복귀 시 **재마운트**. 이때 해치 처리 플래그 `hatchOpenDone`/`hatchCloseDone`이 `useState(false)` 초기값으로 돌아감.
- 결과: 이미 해치를 연 베이인데도 재마운트 후 플래그가 false → 데크완료·홀드잔여 조건(deckDonePromptD)이 다시 충족되어 오픈 프롬프트가 재등장. 그걸 눌러야 다음 카드로 넘어갔다.

**어떻게 고쳤나** (GuidedWorkPanel.jsx)
- 해치 처리 여부를 컴포넌트 로컬 state가 아니라 **voyage.info에 영속**(`hatchDone["{mode}_{center}"]='open'|'close'`). voyage prop은 재마운트돼도 유지되므로 프롬프트가 다시 안 뜬다.
- 헬퍼: `isHatchDoneSaved(center, action)`(읽기), `markHatchDone(center, action)`(fbUpdateVoyageInfo로 쓰기). 베이 그룹(center)·모드별로 기록 → 다른 베이는 정상적으로 다시 열어야 함.
- 판정: deckDonePromptD에 `|| isHatchDoneSaved(selectedGroup,'open')`, 양하 클로즈 조건에 `&& !isHatchDoneSaved(selectedGroup,'close')`. 해치 처리 3버튼(오픈·선적클로즈·양하클로즈)이 markHatchDone 호출.

**검증**(node 로직 시뮬): ① 데크완료 첫 도달 프롬프트 뜸 ② 처리 직후 안 뜸 ③ **수동→자동 재복귀(로컬 false 초기화)에도 영속값이 막아 안 뜸 = 메모10 수정 PASS** ④ 다른 베이(영속 없음)는 정상적으로 뜸. 4/4 정확.
- 파일: GuidedWorkPanel.jsx. 시뮬: /home/claude/sim/hatch_persist.mjs.

### V7.99-8 가이드 작업 단(홀드/데크) 선택 + 수석 작업 위치 추적 (메모6, KSKM 실데이터·렌더 PNG PASS, 2026-06-15)

**왜 만들었나 (현장 이유)**
- 수석검수사는 어느 호기가 어느 베이의 홀드/데크를 작업 중인지 알 길이 없었다. 검수사는 화면을 보며 작업하지만, 수석은 일일이 컨번호를 확인해야 작업 위치를 짐작할 수 있었다.
- 동시에 오양하·오선적 위험이 있었다. 자동 가이드 작업 중 예측과 다른 컨이 나와 끝4자리를 칠 때, 후보가 **선박 전체(평택분)**라 같은 끝4자리 중복이 잡혀 엉뚱한 컨을 양하/선적할 수 있었다. (메모4 숫자패드·메모7 선적구분과 같은 오작업 방지 계열.)
- 사용자 요구(원문 정리): 검수사가 베이 선택 후 **반드시 홀드/데크를 눌러야** 한다(자동 판단 금지 — 그래야 수석에게 작업 의도가 전달됨). 그 단의 남은 컨번호 리스트를 보여, 4자리를 치거나 리스트에서 선택해 오작업을 막는다.

**어떻게 고쳤나 (4개 파일)**
1. **단 선택 단계 신설** (GuidedWorkPanel.jsx): 기존 `장비→접안→베이 그룹→예측 카드` 흐름에 **3.5단계 단 선택**을 끼움 → `…→베이 그룹→[🔵데크/🟠홀드 선택]→예측 카드`. `selectedTier` state('hold'|'deck') 추가. 베이 그룹 변경 시 selectedTier 리셋. 단 버튼에 규격별 내역 표시 — `🔵 데크 32개 20FT:24/40FT:8` / `🟠 홀드 36개 20FT:22/40FT:14` (groups useMemo에 deck20/deck40/hold20/hold40 집계 추가).
2. **큐를 선택 단만**: queue useMemo에서 selectedTier='deck'면 t>=80만, 'hold'면 t<80만 필터. 홀드 선택 시 데크 컨은 큐에 안 나옴(검증: 첫 카드부터 끝까지 t<80).
3. **오양하 방지 — 후보 좁히기** (matchFor): "다른 컨이 나옴" 수정 시 후보를 `groupCenterOf===selectedGroup && 선택 단`으로 좁힘. 끝4자리 중복이 다른 베이·다른 단에서 잡히던 것을 차단(합성 검증: 끝4 "1234" 3곳 → 그룹20 홀드 작업 시 1건만). 추가로 q 비었을 때 **그 단 남은 컨 리스트**(tierRemainConts)를 버튼으로 노출 → 4자리 안 쳐도 리스트에서 선택.
4. **수석 작업 위치 전달** (firebase.js·GuidedWorkPanel·ChiefDashboard·ChiefBayEdit):
   - `fbSetInspectorActivity(name, voyageKey, mode, detail)` 확장 — detail={equip, bayLabel, tier, remain, auto}를 `inspectors/{name}`에 기록(workEquip·workBay·workTier·workRemain·workAt). 베이/단 미선택이면 클리어.
   - GuidedWorkPanel: selectedGroup·selectedTier 바뀔 때 활동 기록(검수사가 누른 단 기준 — 자동판단 아님). bayLabel은 그룹 베이 범위(예 19-21).
   - ChiefDashboard activeByVoyage에 위치 필드 추가 → ChiefBayEdit에 activeWorkers prop 전달.
   - ChiefBayEdit 헤더 아래 배지: 현재 모드 작업자 `홍길동 · 2호기 · 01번 홀드 · 남은 5개`(홀드=주황·데크=청색, 글자색 구분).

**검증**
- KSKM 2611N 실데이터: 그룹12(베이11·12·13) 단 선택 화면 = 데크 32(20:24/40:8)·홀드 36(20:22/40:14) 실집계 일치(PNG). 홀드 선택 후 큐 첫 카드~끝 전부 홀드 티어(t<80), 데크 미혼입(PNG). 수정창 남은 리스트 40건 전부 홀드 티어 PASS.
- 합성 중복: 끝4 "1234" 3곳(그룹20홀드·그룹20데크·그룹4홀드) → 단 미적용 3건 → 그룹20 홀드 작업 시 1건만(오선택 후보 제거) PASS.
- ChiefBayEdit 렌더 PNG: 헤더에 "홍길동·2호기·01번 홀드·남은 5개" 배지 표시 PASS.

**미처리(다음)**: 수동(SearchPanel) 작업도 베이·단 선택해 같은 활동 기록 — SearchPanel은 자연어 조회 중심이라 베이/단 선택 UI 신설이 필요(별도 작업). 사용자: "자동이 계획대로 되면 수동을 안 한다"라 가이드 우선.
- 파일: GuidedWorkPanel.jsx, firebase.js(fbSetInspectorActivity), pages/ChiefDashboard.jsx, components/ChiefBayEdit.jsx. 시뮬: /home/claude/sim/gwp_*.* (GuidedWorkPanel 단독 번들 — firebase/voice 임시 stub 교체 후 번들·복원, react alias dedupe, puppeteer PNG), cbe_*.* (ChiefBayEdit).

### V7.99-7 수석 베이상세 선적 전/완료 시각 구분 (메모7, 렌더 PNG PASS, 2026-06-15)
- 증상(PCSZ 실화면): 수석 대시보드 베이상세 편집(ChiefBayEdit)의 **선적 모드**에서 홀드에 컨이 차 있는데, 이건 EDI 계획일 뿐 실제 선적된 게 아님. 선적했는지 안 했는지 구분 불가(지침 Ⅵ-1: 선적 EDI=계획, 선적확인해야 실체).
- 원인: ChiefBayEdit가 baseBay/Row/Tier를 actual 있으면 actual·없으면 EDI로 잡아 셀을 그리는데, **완료(_placed) 구분 없이 전부 filled**로 렌더.
- 수정: ① containers에 `_placed`(선적 실체화 여부 = bay_actual 설정 ∨ completed 등록, 양하는 항상 true) ② cellExtra에 미선적 시 `cbe-unplaced` 클래스 ③ CSS: 미선적 = 점선 테두리 + 흐린 회색 글자(배경색 미사용 — Ⅱ-4.2) ④ renderCellContent 선적완료에 ✓ 접두 ⑤ 헤더에 선적 진행 배지(평택분 `선적 N/M · 점선=선적 전(계획)`).
- 검증(크롬 PNG): PCSZ BAY01 합성 — 선적완료 3컨 진한 실선, 선적 전 5컨 흐린 점선, 상단 "선적 3/8" 배지 정상 표시 PASS.
- 파일: ChiefBayEdit.jsx. 시뮬: /home/claude/sim/cbe_test.* (ChiefBayEdit 단독 번들 마운트 — firebase 정적이라 마운트 가능, puppeteer PNG).

### V7.99-6 가이드 작업 메모 6건 수정 (KSKM 2611N/2612S 실데이터 PASS, 2026-06-15)
퇴근 전 누적 메모 처리. KSKM(3E89, SUNNY KALMIA) 양하 ASC 418컨(평택122)·선적 EDI 406컨(평택110) 실데이터로 재현·검증. 6건 PASS, 4건은 구조변경/렌더검증 필요로 다음 세션 이월.
- **메모1 해치 장수 합산 버그**: `hatchPanelsOf`(GuidedWorkPanel)가 같은 해치 묶음(트리오 11·12·13) 베이의 hatchCount를 단순 합산 → 2+2+2=6장. 한 해치 패널이 여러 베이를 덮으므로 중복. 수정: 같은 groupCenter는 대표값(Math.max), 서로 다른 그룹만 합산. KSKM 트리오 6→2 PASS. ⚠ KSKM 사전엔 hatchCount 미입력 — 빌더에서 베이별 해치 장수 지정해야 정확(합성 hatchCount=2로 재현 검증).
- **메모2 자동 양하 40ft 우선 미적용**: `guidedQueue` cmp에 "같은 티어 40ft 먼저" tiebreak 부재. 짝수 베이 40ft가 양옆 홀수 20ft 트윈 위에 같은 높이로 걸치는 구조(KSKM 베이12 사이 11·13)에서, 같은 티어 안 40ft·20ft가 row 순으로 교대로 나옴. 수정: `is40ft` 헬퍼 + cmp에 **양하 한정** 같은 티어 40ft 우선. t84·t82에서 40ft 전부 선행 PASS. 선적·40단독 베이 회귀 PASS(선적은 holdTwins가 따로 처리하므로 양하에만 적용).
- **메모3 트윈 55톤 경고 미표시**: 55톤 점검 로직(TWIN_MAX_TOTAL_KG·twinDiffLimit)은 nlSearch에 있으나 **질문 답변 전용** — 가이드 트윈 카드엔 자동 경고 없음. 수정: GuidedWorkPanel에 `twinWtWarn` useMemo(합계>55톤=over 불가 / 무게차>부두한계=imbal 수평주의) + 카드에 경고 배지(over=적색, imbal=황색). 부두는 voyage.info.pier(미상 14톤 보수적). KSKM 트윈 33쌍 중 16쌍 55톤초과(최대58.3t)·1쌍 무게차초과(17.5t) 정확 검출 PASS.
- **메모4 수동 컨번호 숫자패드**: 단독 수정칸(587)은 inputMode=numeric인데 트윈 동시 수정칸(621)만 누락된 비대칭. 추가.
- **메모5 해치 포트 무시 전부 오픈**: `groupBaysOf`가 데크만 평택분인 베이까지 해치 대상에 포함. 해치는 홀드 접근 물리작업이므로 홀드 평택분 있는 베이만 열어야 함. 수정: `groupBaysOf(center, holdOnly)` 옵션 + sendHatchReport가 holdOnly=true(홀드 평택 0이면 보고 안 함). KSKM 통과화물 홀드 베이(7·9·15·17·23·25)가 대상에서 제외 PASS.
- **메모9 선적 바닥부터 한 로우 먼저**: 선적 트윈 `holdTwins.sort`가 POD를 안 봐서(cmp엔 podRank 있으나 holdTwins 자체 정렬엔 누락) 포트가 row 순으로 교대로 나옴. 수정: holdTwins를 **POD 우선(그룹 전체 등장순) → row → tier 오름차순(바닥부터)**. KSKM 그룹4·12·20에서 POD 연속블록 + 같은 row 티어 바닥부터 PASS.
- **이월(다음 세션)**: 메모8(베이플랜 "19(20)21" 트리오 3칸 — 렌더 PNG 검증 필요, REF Ⅴ-B 비대칭) · 메모10(수동/자동 전환 시 해치 버그 1409.png — 증상 구체화 필요). (메모6=V7.99-8, 메모7=V7.99-7에서 완료.)
- 파일: guidedQueue.js(is40ft·cmp·twinPodOrder), GuidedWorkPanel.jsx(hatchPanelsOf·groupBaysOf·twinWtWarn·621칸). 시뮬: /home/claude/sim/*.mjs (실파서 parseAscFile·parseBAPLIE + 실 guidedQueue·twin).

### V7.98 통합 (3D 뷰 + 수석 베이상세 편집)
- 깃허브 V7.97(3D뷰·BayDetailEdit·위젯픽스)과 세션 V7.97(ChiefBayEdit) 통합 → V7.98.
- 채택: BayPlan3D(베이탭 3D 토글, 기본OFF, 검증됨) · BayDictVerifyWidget 통계픽스 · ChiefBayEdit(수석 편집). 폐기: BayDetailEdit(거부된 마크 방식).
- 원칙: 편집은 수석 대시보드(ChiefBayEdit), 베이 탭 3D는 뷰. 두 기능 충돌 없음.

### V7.98-02 베이상세 빈칸 도배 픽스 (row99 OOG 오염)
- 증상: 베이상세가 전 베이를 56칸(01~99) 격자로 그려 빈칸 도배(데이터 손실은 없음).
- 원인: bay99 OOG placeholder(row99)가 PrintHubModal globalRowRange.maxRight=99로 오염 → BayDetailPage STD_ROWS가 이 전역값을 전 베이에 적용.
- 해결(방법 A, 최소수술): ① PrintableBayDetail bayList에서 bay>=99 제외(3D와 동일) ② BayDetailPage STD_ROWS를 베이별 매트릭스 row(rowMaxEvenLocal/OddLocal ∪ 이 베이 실제 컨, OOG row>=90 제외)로 ③ ChiefBayEdit도 동일 방어. 인쇄 CSS/tier라벨/해치 무변경.
- 검증(크롬 PNG): 오염된 maxRight=99를 넘겨도 BAY(06)07이 8칸 정상, bay99 페이지 없음. PASS.

### V7.98-07~11 베이상세 BayBoxV2 일원화 + 인쇄 버그 + 페어 표시 (2026-06-15)
페어 모델 상세는 Ⅴ부 REF_베이페어링 참조.
- **V7.98-07/08 일원화**: 카고플랜·편집 베이상세·인쇄 베이상세 모두 BayBoxV2 단일 컴포넌트. 그림=BayBoxV2, 셀 내용만 주입(카고=마크, 편집=컨번호, 인쇄=5줄: POL/POD·컨번호·선사 F/E 무게 타입·특수·위치). 7.98-07이 renderCellContent prop 추가.
  - 절대 주의 ①: BayBoxV2 래퍼는 `display:flex;flex-direction:column` + 높이 확보 필요(셀이 `flex:1 1 0` 높이 상속). **고정 height 금지(넘침 유발)**.
  - 절대 주의 ②: 인쇄용 전체화면 모달은 **document.body로 portal**해야 CARGO_V2_CSS 인쇄 격리를 통과한다.
- **V7.98-09**: 편집 베이상세 격자 찌부러짐 — `cbe-cargo-wrap height:72vh`.
- **V7.98-10**: 인쇄 베이상세 백지·페이지 미분리. print-to-pdf A/B로 확정. 원인 ① CARGO_V2_CSS @media print의 격리 선택자가 portal 없는 `.bd-print-modal`을 숨김 → 백지 ② bd-cargo-wrap 고정 height+page-break가 bd-page와 겹쳐 이중 페이지. 수정 — 격리 선택자에 `:not(.bd-print-modal)` + 모달 createPortal(body) + bd-cargo-wrap 고정높이 제거(flex:1 1 0). pdftoppm 5페이지 PASS.
- **V7.98-11**: 카고플랜 페어 "3 (4)5"→"3 5" 붕괴(가운데 짝수 누락, "7 (8)9"는 정상). node 실함수 시뮬 재현. 원인 — 매트릭스 빌더는 페어를 홀수 엔트리 `pairEven`(짝수번호)로 저장(짝수 별도 키 없음)인데, `autoPairBays`는 트리오를 짝수 베이 별도 존재로만 판단(pairEven 무시) + matrixBays 생성부가 pairEven 미전파. detectMissingBays는 M6.94.36에서 보정받았으나 autoPairBays만 빠진 **비대칭**. 수정 2곳 — autoPairBays에 pairEven 페어 루프(usedEvens 중복가드) + matrixBays map에 pairEven 전파. ALL PASS.
- **ATRP 베이상세가 CASPI와 다르게 큰 박스로 보임 (진단만, 미해결)**: ATRP는 매트릭스가 **cells 없는 PDF 자동본**이라 matrixRender가 hasCells=false→null → 옛 bd-grid 폴백(큰 빈 박스). cells 있는 선박은 BayBoxV2(컴팩트) 정상. 해결 방향 — cells 없을 때 EDI 실데이터로 단면 골격 생성(BayPlan 패턴)해 BayBoxV2 렌더 → 전 선박 CASPI형 통일. [다음 세션]
- **교훈(절대)**: ① ZIP은 Ⅱ-2.5대로 dist 포함, HANDOFF_* 제외(내용은 지침서 흡수, 정보 손실 금지). ② 페어 함수는 여러 개 — 한 곳만 고치면 비대칭 재발. 페어 수정 시 REF_베이페어링 §2 표 전부 점검.

### V7.94-24 자리 교환(swap) + 위치수정창 작업 단 필터 (2026-06-13)
- **현장 규칙**: 위치 수정창은 작업 중인 베이의, 작업 중인 단(홀드/데크)의 빈자리만 순서대로 표시. 자리 교환: A가 B 자리로 선적되면 B는 A의 원래 자리로 이동해 대기(미배정 떠돌이 X). 같은 포트 엠티끼리 베이 교환 가능.
- 수정: ① fbReassignContainerPosition swap화(A 원위치 캡처 → displaced B를 A 원자리로, 반환에 swappedTo) ② PositionEditModal workTier 필터('hold' tier<80 / 'deck' tier>=80). 검증: swap·workTier 10항목 + BAY38 선적 큐 순서 대조 + 회귀 127 PASS.
- 미처리: 연쇄 자리 교환(다단계), 조회 시 예측 위치 자동 제안 강화, 수동 선적 베이→홀드/데크 게이트(SearchPanel).

### V7.94-23 가이드 베이 홀드/데크 구분 + 장비선택 1회화 + 선적 POD별 묶기 (2026-06-13)
- 현장 지적 4건(SWRG 2605N): ① 베이 선택 화면에 홀드/데크 구분 추가(데크=하늘·홀드=주황 잔여) ② 장비 선택 매번 나옴 → equipStep 초기값 `!getEquipNumber()` ③ 자동 선적이 단일 포트처럼 → buildGuidedQueue 선적 시 같은 베이 안 POD별 묶기(podRank) ④ 수동 선적 베이→홀드/데크 선택 후 조회 = **미착수(다음 작업)**.
- 검증: 10항목(SWRG BAY2·10·14) + 회귀 117 PASS.

### V7.94-22 해치커버 보고 "장" 수 = 매트릭스 hatchCount 합 (2026-06-13)
- 버그: 베이 그룹인데 장수가 베이 개수로 나옴(매트릭스는 2장 지정). 원인: buildHatchMessage가 `총 {bays.length}장` — 매트릭스 hatchCount 무시.
- 수정: buildHatchMessage에 panelCount 파라미터(없으면 베이 개수 폴백) + hatchPanelsOf(bays)로 사전 hatchCount 합산(페어 흡수 짝수는 대표 홀수에 포함) + fbAddWorkReport에 panelCount 기록. 검증 10항목 + 회귀 107 PASS.
- 주의: 사전에 hatchCount 지정돼야 정확. 신규 선박은 빌더에서 베이별 해치 장수 지정 권장.

### V7.94-21 컨번호 조회 숫자 키패드 (2026-06-13, 메모 처리)
- 끝4자리 조회 입력칸 3곳에 `inputMode="numeric"`(SearchPanel·GuidedWorkPanel). 자유질문칸은 음성·한글 받으므로 text 유지. 검증: 빌드 JS 6곳 + 회귀 97 PASS.
- 미처리 메모: 로우 목록 화면 스크린샷(타 검수앱) — 신설인지 단순 참고인지 **사용자 확인 필요**.

### V7.94-20 끝4자리 중복 시 수동작업 오선택 + 위치창 베이 자동선택 (2026-06-13)
- 현장 발견(SWRG 2606S BAY38): 끝4자리 3523 2대. 선적 평택 전체 끝4자리 중복 8종.
- 문제1: matchFor가 끝4자리로 후보 찾는데 같은 베이 중복 시 엉뚱한 컨 선택 → 원래 주인 displaced 잔존. 수정: 현재 카드 위치(card.pos) 같은 컨 우선 정렬 + 2개↑면 ⚠ 중복 경고 배지.
- 문제2: 미배정 재배정 시 작업 베이 또 골라야 함. 수정: PositionEditModal workBay prop(작업 베이 자동 선택). 검증 8항목 + 회귀 97 PASS.

### V7.94-19 베이매트릭스 페어(짝수) 인라인 변경 (2026-06-13)
- 짝수(페어) 지정을 베이 목록에서 바로 변경 가능하게. updatePairEven(빈값=단독, 짝수=짝꿍, 홀수·범위밖 경고) + 인라인 number 입력(placeholder "단독", step 2). 검증 9항목 + 회귀 89 PASS.

### V7.94-18 수석검수 완료 권한 + 비수석 경고 (2026-06-13)
- 버그: 비수석도 완료 저장 가능(권한 체크 없음). 수정: isChief 확장(`/수석검수|부수석/`), ChiefDashboard에 inspector 전달, doComplete 권한 차단 + 경고, 버튼 권한별 분기([✓ 수석 완료 저장] / [🔒 수석 전용]). 검증 14항목 + 회귀 80 PASS.

### V7.94-17 PWA 홈화면 설치 404 수정 + 항만 아이콘 (2026-06-13)
- 증상: 홈화면 설치 후 아이콘으로 열면 404(QR 첫 접속은 정상). 원인: manifest `start_url:"./"` + vite `base:'./'` 상대경로 → GitHub Pages `/greenmarinetally/` 하위라 루트로 잘못 풀림.
- 수정: manifest start_url·scope = `/greenmarinetally/`(절대경로). 아이콘을 항만 크레인+컨테이너 PNG(base64 인라인). iOS는 `apple-touch-icon`(PNG data URI)을 `_index.entry.html`에 추가(build.sh가 루트 index를 dist로 덮으므로 진입 HTML에 넣어야 생존).
- 빌드 함정: 루트 index.html 직접 넣으면 빌드 시 사라짐 — 진입점은 `_index.entry.html`. 검증 7항목 + 회귀 66 PASS.
- 참고: 움직이는 아이콘은 iOS/안드로이드 모두 OS가 차단 — 정적 PNG만.

### V7.94-16 해치커버 프롬프트 가이드 통합 + 작업보고 항차 오표시 수정 (2026-06-13)
- 해치커버 보고를 가이드 작업 흐름의 베이 완료 시점 선택창으로 통합(GuidedWorkPanel): 양하 데크 완료 시 [🔓 오픈→홀드], 선적 홀드 완료 시 [🔒 클로즈→데크], 양하 그룹 완료 시 [🔒 클로즈]. 보고는 buildHatchMessage(실제 베이번호·장비·모드별 항차) + fbAddWorkReport + 카톡.
- **작업보고 항차 오표시 수정**: handleHatch/handleConBox가 `activeByEquip[equip][0].mode` 고정 → 활성 없으면 voy_d 폴백(선적인데 양하 항차). → equipModeOf: 장비 활성 모드 1개면 그것, 아니면 화면 선택 모드(selectedMode). 검증 SWRG 12항목 + 회귀 54 PASS.

### V7.94-15 SWRG(SAWASDEE RIGEL) 풀 테스트 — 실데이터 전수 검증 (2026-06-13)
- 양하 2605N(909대, 평택 402) → 선적 2606S(890대, 평택 384). IMO 9943803, SMDG22.
- 전수 교차: XRAY 10건 정확·오탐0 / 선적 선사 3종(HAS116+HSL1+SKR267=384) EDI 완전 일치 / 양하 LIST 404행 = EDI 402 + **광양분 1건 혼입(SKLU1981464, POD KRKAN)** + 중복 1건 → 현장 주의 / 가이드 양하 402·트윈 61쌍, 선적 384·트윈 12쌍.
- 수정 2건: ① guidedQueue 선적 "FR·OT 마지막"이 oog 필드 미인식 → `c.oog||twin?.oog` 추가(홀드 OT는 물리 제약 예외 유지) ② fillEmptyBaysSequential — 사전 없는 신규선박 4의배수 전무(트리오 구조)면 추정 채움 금지(SWRG 가짜 14→5). 검증 10 + 회귀 44 PASS.
- 참고: SWRG는 사전 미등록 — 작업 후 빌더 [저장]하면 v3 등록.

### V7.94-14 검수원 로그인/작업중 상태 구분 (2026-06-13)
- 증상: 로그아웃해도 "●작업중" 남음. 원인: 로그아웃이 Firebase 미기록 + 배지가 "최근 60초 활동"뿐.
- 수정: fbSetInspector에 loggedIn/loginAt, 신규 fbLogoutInspector. 공용 유틸 inspectorStatus.js(working/online/null, loggedIn 없는 과거데이터 하위호환). 표시 3곳 교체. 검증 8항목 PASS.

### V7.94-13 MCSN 95베이의 진짜 원인 — BAPLIE 위치 미정 코드 + PDF 워커 404 (2026-06-12)
- MCSN 실EDI 781대 중 1대(HASU1411466)가 `LOC+147+0999999` = **99-99-99 위치 미정 관례 코드**. 이 1대가 max를 99로 끌어올려 95베이 생성. 나머지 780대는 1~44로 .def와 일치.
- 수정 3종: ① buildMatrixFromEdi에서 bay·row·tier 모두 99인 컨 제외 ② dictBaySet 기록 → fillEmptyBaysSequential이 사전 없는 베이 추정 금지 ③ PDF 워커 404 — public/pdf.worker.min.mjs 정적 파일 + BASE_URL 참조(pdfjs 4.10.38). 최종 검증: 34베이·추정0·max45 .def 일치, 회귀 36 PASS.
- 교훈: BAPLIE에 99-99-99(위치 미정) 컨 존재 가능 — 매트릭스·통계 산정 시 항상 제외.

### V7.94-12 MCSN 베이 매트릭스 오염 사건 (2026-06-12)
- 증상: MCSN 622N 빌더가 96베이(추정 74). 화면 "사전: SPIL NIKEN".
- 진상(.def 바이너리): MCSN.def 헤더 = MCSN / SPIL NIKEN / 3E3921 / IMO 9273947 → **MCSN 실선박명이 SPIL NIKEN, 매칭 자체는 정상**. 진짜 원인 = V2 사전 MCSN entry가 PDF box-region 자동파싱(M6.71) 오염(트리오 3번째 베이 누락 + bayNo 00, 24베이). 같은 오염 8척: KKLC, LYTJ, MAMP, MCSN, NBTD, NSFR, SWSP, TNJP. .def 파서(M4.4)는 정상(34베이).
- 수정 3종: ① V2 MCSN entry를 .def 결과로 교체 ② augmentMatrixFromBayDict에 오염 패턴 자동 거부(00 존재+트리오 3번째 결손≥2) ③ 정합성 게이트(EDI 실측 max 베이 > 사전 max면 미적용). 검증 5 + 회귀 31 PASS.
- 미해결: 사용자가 본 "EDI에서 99베이"의 출처 — MCSN EDI 미수령(다음 세션).

### V7.94-11 위치 선택창 — 베이 우선 선택 + 완료 자리 비활성 (2026-06-12)
- 원칙: 전체 베이 자리를 한꺼번에 보이지 말 것(오선적 유발). 베이 먼저 선택 → 그 베이 자리만. 완료된 자리도 표시하되 선택 불가(✓회색). 검증 8 + 회귀 28 PASS.

### V7.94-10 컨테이너 번호 수정 버튼 + 다른 베이·목적지 구역 경고 (2026-06-12)
- 두 기능 병행: [위치 수정](같은 컨 자리만) / [컨테이너 번호 수정](다른 컨이 옴 — 검색·선택 후 남은 자리 창).
- 경고 2종: ⚠ 다른 베이에서 오는 컨 / 🚨 목적지 구역 이탈(슬롯 EDI 계획 컨 POD ≠ 옮기는 컨 POD). 트윈 짝꿍 자리도 검사.
- React 훅 함정 2건(렌더 검증 적발): useMemo가 아래 선언 state 참조→TDZ / 조기 return null 뒤 훅 추가→"Rendered more hooks". **훅 추가 시 조기 반환 앞·의존 선언 뒤.** 검증 8 + 회귀 28 PASS.

### V7.94-09 위치 수정 — 남은 자리 선택창 + 트윈 짝꿍 자동 (2026-06-12)
- PositionEditModal 기본 화면을 남은 자리 그리드로(같은 모드·미완료·크기 일치). 트윈 짝꿍 자동(20ft+findTwinCandidate → 앞 선택 시 뒤 자동: 21-00-04→23-00-04). 배정 후 바로 선적확인 체크. 검증 10 + 회귀 28 PASS.
- 주의: import 누락은 esbuild가 못 잡음 — 새 식별자 쓰면 import 직접 확인.

### V7.94-08 선적 가이드 — 현장 메모 5건 (2026-06-12)
선적 상황 규칙. 양하는 기존 유지(FR만 우선).
- ① 트윈 스택 연속(혼재 베이 20ft 트윈은 같은 로우 바닥부터 연속, 아래 깔린 40ft는 직전에 끌어옴). 최종: 싱글→트윈(스택)→40ft→데크→FR·OT ② 홀드 완료 프롬프트 ③ FR·OT 선적 마지막 ④ 수정 시 위치 꼬임 = fbReassignContainerPosition(예측 컨 자동 미배정+완료취소) ⑤ 트윈 동시 수정(바뀐 쪽만 선택). 검증 9 + 회귀 11+FR8 + 실렌더 7 PASS.

### V7.94-06 가이드 순서 — 40/20 혼재·FR 특수화물 규칙 (2026-06-12)
- 40/20 혼재 시 40ft 먼저 = 적재 구조에서 자연 발생(양하 위층부터라 트윈 위 40ft 먼저, 선적 아래층부터라 바닥 40ft 먼저). 바닥 20ft면 일반 순서 — 코드 무변경.
- FR(플랫랙): 우선 양하 / 마지막 선적. 물리 제약 예외(같은 줄 위 비FR 존재 / 홀드 FR인데 데크 잔여). 트윈 한쪽 FR이면 카드 전체 FR. 검증 8 + 회귀 11 PASS.

### V7.94-05 가이드 카드 정보 전면 보강 (2026-06-12)
- 카드에 규격·FULL/EMPTY·무게(formatWt)·선사·항로·OOG 배지. 실번호 줄(미입력 ⚠경고, 탭→인라인 입력 fbUpdateRecordSeal). XRAY 줄(대상만, 탭→XRAY 실번호+E-실 fbSetXraySeal). 무게는 반드시 formatWt(EDI wt는 kg — 직접 표기 시 7200t 오표기). 검증 8 PASS.

### V7.94-04 X-RAY 미매칭 잔존 키 정리 버튼 (2026-06-13)
- 잔존 출처: handleXrayUpload이 누적(merge) — 새 파일에 없는 옛 키 안 지움. 수정: [🧹 미매칭 삭제] 버튼(매칭분만 남기고 교체 저장). 누적 업로드 자체는 유지(분할 업로드). 검증 3키 중 미매칭 1키만 제거 PASS.

### V7.94-03 X-RAY 상단 0/3 vs 리스트 2 불일치 (STMJ 2627E)
- 원인: VoyageSummaryCard는 xrayList 원본 키 전부(3), ListTab은 매칭분만(2). 미매칭 1건 = EDI/리스트 어디에도 없는 컨번호(검사 누락 위험) → 숨기지 않고 드러냄. 수정: 요약 칩 매칭분 기준 `0/2` + ⚠N 미매칭, 업로드 탭에 미매칭 전체 나열. 검증 4 PASS.

### V7.94 자동 가이드 모드 — 앱이 양하/선적 순서를 예측 제시 (핵심 비전 1차 구현)
- 목적: 앱이 크레인 순서대로 "다음 컨테이너" 카드 제시 → 검수사는 [확인]/[수정]만. 몸은 검수사, 소프트웨어는 앱.
- **순서 규칙 (ATPR 2627E/2628W 실데이터 PASS)**:
  - 양하: 데크→해치→홀드, 맨 위 티어부터, 같은 티어는 육상→해상 로우.
  - 선적: 홀드→해치→데크, 맨 아래 티어부터, 같은 티어는 해상→육상 로우.
  - 육상/해상 = 접안 방향(우현 접안 = 짝수 로우가 해상쪽). 항차당 1회 입력, `voyage.info.berthSide` 저장.
  - 트윈: findTwinCandidate 그대로(홀수+홀수 한 카드). 40ft는 자기 층 차례. 싱글모드(짝 없는 20ft) = 양하 맨 마지막 / 선적 맨 처음. 쉬프팅은 감지 안 함(수동).
- UI: SearchPanel 상단 🤖자동/✋수동 토글. GuidedWorkPanel(접안→베이 그룹→예측 카드+트윈 동시+배지+음성). 수정 3연속 = 자동 수동 전환.
- 파일: guidedQueue.js(순수 정렬), GuidedWorkPanel.jsx. 검증 11 + puppeteer 3화면 PASS.
- **V7.94-02 (오선택 방지+장비 단계)**: 설정 흐름 = 선박→양하/선적→장비(호기)→좌/우현→베이. 접안 방향 confirm 재확인. 설정 칩 바([🏗 N호기][우현 접안][B20 변경]).

### V7.93 트윈 작업 무게 점검 (현장 안전 도메인)
- 도메인: ① 카고 크레인 트윈 두 컨 합계 **55톤 초과 불가** ② 무게 차이 급격하면 수평 안 맞아 불가.
- **트윈 무게차 한계(V7.94 확정)**: 부두별 — **동방아이포트(PNCT) 14톤, 평택컨테이너터미널(PCTC) 20톤.** 초과 = 불가. TWIN_DIFF_LIMITS + twinDiffLimit(pier). 부두 출처 = voyage.info.pier. 미상이면 보수적 14톤.
- 단독 질문: twinCheckQuery → 불가 쌍 위치+합계 무게, 불균형 차이값. 짝 판정은 twin.js getBayPairs 단일 규칙. 상수 TWIN_MAX_TOTAL_KG=55000. 검증 18 + 실렌더 3 PASS.

### V7.92 챗봇형 일상 질문 — 자기소개·시간·날씨·입출항
- 4종 신설: introQuery(자기소개) / schedQuery(입출항 — portMisMatch.js 신규, 최신 updatedAt 우선·V7.30 선박명 가드) / timeQuery(기기 시간) / weatherQuery(Open-Meteo 무키, 평택항 36.967/126.822 — 풍향·초속).
- ⚠ 우선순위: 입출항 > 시간("입항 시간"이 timeQuery에도 걸림). ⚠ 검증 적발 버그: 패치를 SearchPanel(바깥)만 적용 → 답변 로직은 내부 SingleSearch라 런타임 에러. **props 추가 시 선언부+모든 호출부 동시 수정**(SearchPanel 안에 SingleSearch·TwinSearch 별도 존재). 검증 27 + 마운트 6 PASS.

### V7.92-02 챗봇 집계 = 평택분 통일
- 증상: 양하 탭 개수 ≠ 자연어 질문 답. 원인: SearchPanel allContainers = EDI 전체(통과화물 포함). 수정: `_ptk` 플래그(양하 POD평택·선적 POL평택). ⚠ 예외: **컨번호 단건 조회는 전체 유지**(통과화물 스캔 시 찾아주는 게 정답). 검증 7 PASS.

### V7.91 검수앱 음성 질문 자동 복원 (콘앱 동시 — Ⅲ부 참조)
- (검수앱 측은 V7.80에서 구현, 7.91은 콘앱 이식 — Ⅲ부.)

### V7.90 완료 분리 + 조회 오염 해결 + 콘앱 자동 분리
- 완료 분리: info.inspectorDone(단일) → dischargeDone/loadingDone 모드별. VoyageCard에 [⬇ 양하 완료]/[⬆ 선적 완료]. 보유 모드 전부 완료 = 수석 대기(구 데이터 하위호환).
- 조회 오염 해결(V7.53 보완): SingleSearch 결과를 main(현재 모드·미완료)과 others(반대 모드+완료분)로 분리. main만 기본, others 접이식.
- ⚠ 검증 교훈: node_modules 없는 상태 `esbuild | grep -c "✘"`는 0 출력(가짜 통과) — **컴파일 체크 전 esbuild 실존 확인.**

### V7.90-02/-03 베이 분포 질문 + 상세 확장
- "리퍼가 몇 번 베이에 있어?" 유형 신설. bayDistQuery + posQuery 결과>5면 분포 라우팅. 첫 줄 음성용("리퍼 56대 — 베이 3, 7, 20", ≤8 나열). 베이별 줄에 갑판/홀드·규격·온도 분포·끝4 나열. 게이트 3곳에 포함 필수.

### V7.90-04 작업 브리핑
- briefingQuery("브리핑/요약"). 첫 줄 음성 한 문장 + 상세(F/E·규격·베이범위·진행률·리퍼 온도·위험물·XRAY·FR/OT/TK). generateBriefing.
- ⚠ 리퍼 판정은 isReeferContainer 단일 함수(자체 정규식 `/R[FT1-9]?$/`이 40FR 끝 R에 매칭돼 오인).

### V7.90-05 실번호 오류 사전 점검
- auditSeals: 풀씰·엠티실 전수 — 중복 / 컨번호 혼입 / 특수문자·4자 미만 / 자리수 부족(같은 접두 그룹 최빈 길이 대비, 표본≥3·최빈≥70%). 브리핑 통합 + 단독 질문. 검증 6/6 검출·오탐0.

### V7.90-06 실번호 스왑 즉시 경고
- 시나리오: 두 컨 실번호가 리스트에서 서로 바뀜 → 수정 시점이 스왑 드러나는 유일한 순간. checkSealSwap(입력값이 다른 컨 sl/eseal/reseal과 일치 시 confirm 경고). **기록은 막지 않음**(실물이 진실). 검증 PASS.

### V7.90-07 브리핑 재구성 (7.41 정정)
- 문제: 통과화물 포함 전체 집계(7.1 위반) + 일반 통계 나열. 재구성: 평택분/통과화물 분리. **⚠ 주의사항(행동 지향)** — 리퍼 온도 미입력→"조회 시 입력", 통과화물 혼재→"내리지 말 것". 일반 통계는 한 줄 압축. **브리핑 집계는 평택분, 통과화물은 숫자가 아니라 주의사항으로.**

### V7.80 음성 질문 자동 복원 — AI 질문 번역기 (검수앱)
- **원칙: AI는 답하지 않는다 — 질문만 복원한다.** 음성 오인식 교정만 AI(fixQuestionWithAI, temp0·60토큰·4초), 답은 복원 문장을 로컬 파서(nlSearch)에 재투입 — 환각 차단.
- 트리거: 음성 입력 + 미해석 단어 잔존(KNOWN 어휘 제거 후 한글 2자+). 부분 인식("20번 베이 잇퍼 몇대야")도 복원. 같은 문장 1회만. 음성 답변 핵심 한 문장.
- ⚠ 진단 교훈: 텍스트 직접 입력 16문항은 다 정답 — 병목은 엔진이 아니라 STT. 증상 재현은 반드시 입력 경로(음성)까지 포함.

### V7.61 검수앱 — (콘앱 홀드콘 ×2 버그, Ⅲ부 참조)

### V7.57 카고플랜 해치커버 등분 복구
- V7.05 원복 때 computeBayRenderData return의 hatchCount 누락 → 해치 등분 항상 1로 폴백. return에 hatchCount 복구.

### V7.58 카고플랜 해치커버 수평 정렬
- 원칙: 홀드 있는 베이들은 해치커버 선이 같은 수평선(데크는 아래 82가 해치선에 붙고 부족분 위 spacer, 홀드는 위가 붙고 부족분 아래 spacer). deck-only는 해치선 없어 제외. globalHatch 계산. 검증 KSKM PASS.

### V7.53 검색/질문(챗봇) 수리 — A방식(규칙 기반) 확정
- 사용자 확정: 챗봇은 A방식 — AI 없이 있는 데이터로 정확히. 실결함 2건: ① 탭 격리(filteredContainers→ allContainers 전체 + 현재 모드 우선 정렬) ② 45ft 오인(`/^45/`→ 45='^L5'∨라벨45, 40='^4'). nlSearch.js는 순수 모듈 — node 단위테스트.

### V7.54 질문 카탈로그 + 미응답 기록 — 파서 확장 백로그
- 매뉴얼 [AI 질문] 탭 개편(검증된 질문만 카탈로그). 미응답 [📌 기록] 버튼 → fbReportWrongAnswer. **미지원 백로그(20문항 중 X10·△2)**: 선사별 집계 / 미입력 필터(온도·씰) / 시프팅 / 통과화물 / 최대최소 정렬 / 날짜 필터 / 서수→베이 / 위험물 클래스. 오답 위험 2종(선사명·미입력) 매뉴얼 경고 표기.

### V7.52 베이 간 정렬 (검수앱 BayPlan + 콘앱 동시 — Ⅲ부도 참조)
- 증상(TMPZ): BAY01(deck-only 4칸)의 04가 BAY(02)03(6칸)의 06 위에. **홀드 없는 베이가 특히 어긋남 — 반복 실수 지점.**
- 원칙 ④ 베이 간 정렬: 기준 폭 = 전체 베이 최대 폭. 모든 베이를 그 가운데(0.5칸 margin)에. 같은 row는 어느 베이에서나 같은 세로선.
- 검수앱(BayPlan.jsx): globalGridCols 신설(dictBaysSummary 선언 뒤 — TDZ). 검증 TMPZ 렌더 PASS.
- ⚠ 본체 검증법(영구): BayPlan을 esbuild 단독 번들 + 합성 containers 마운트(bayplan_test.entry.jsx) — Firebase 의존 있어도 마운트 가능.

### V7.42~43 엠티실 보고서 정렬·서식
- 정렬: 20ft 먼저 → 40ft, 규격 안 컨번호순. 규격 경계 소계 + 맨 아래 총합(sortBySizeThenCn).
- 서식: 출력 전용 loadSheetJSStyled(xlsx-js-style@1.2.0). **기존 loadSheetJS(파서용)는 절대 교체 금지.** 공용 빌더 buildSealReportSheet.
- 버그: 선적일자 Invalid Date → fmtDateSafe.
- **V7.43 해소**: 원인 = 선사마다 규격 표기 다름(22GP/45GP) → 엠티실 칸 오염. ① 규격 컬럼 병기 `20DC(22GP)` ② 엠티실 ISO형 값 제거 ③ 입력 있으면 표시·없으면 빈칸(수기 기입 공간) ④ 선적위치(베이/열/단) 컬럼 ⑤ 시각 압축 → 8컬럼 A4 한 장.

### V7.40 수석 대시보드 전폭 점검 — 실시간 작업 보드
- **⚓ 실시간 작업 보드(신설, 최상단)**: 살아있는 항차당 카드 1장. 진행바+누락 / 지금 작업 중 검수원(lastActive 90초) / 오늘 데미지·실오류 배지 / 최근 작업 보고 / 클릭→항차.
- 버그 fix — 평택분 과대 집계(7.1 위반, 전수 5곳): 모드 인자 추가(양하=POD, 선적=POL)를 5곳 일괄. **교훈: `isPyeongtaekPort(c.pol)||isPyeongtaekPort(c.pod)` 패턴을 전수 grep.**
- 정리: "항차별 진행" 섹션 제거(보드 대체), 죽은 코드 490줄 제거. ⚠ 함수 블록 제거 시 시그니처 `{`를 본체 `{`로 오인 주의.

### V7.38 카고플랜 V2 다듬기 — override 우선순위 수술 + deck-only 규칙 재제거
- ① cargoPlanCore 우선순위 수술(5.1 정합): non-user 분기에서 자동 사전(변수명 "userBay"지만 v2/v5 포함)이 override보다 먼저 평가됨 → KSKM bay01이 4행 직사각 대신 8행 피라미드, bay27에 유령 홀드. 수정 4건(rowMax·hasZero·tiers·cells override 우선, 빈 holdTiers=[]는 "홀드 없음" 존중). **isUserSource 분기는 절대 불변.**
- ② deck-only 하단배치 규칙 재제거(6/9 확정 — 재발 1호). autoPageLayout deckOnlyKeys 자리바꿈 제거. **교훈: 세션 간 수정은 같은 ZIP 라인에서 이어가야 함.**
- ③ 해치선 CSS(calc(100%-18px)+margin). ④ tier 라벨 고정 13px 재제거(V7.39 — 재발 3호, flex 균등분배 1:1).

### V7.36 .def 단면 디코더 — 베이매트릭스 자동 생성 (336척 실데이터 확립)
- .def(CASP SHIP DEFINE FILE)에서 베이별 행 라벨·데크/홀드 tier·hasZero·홀드 유무 직접 디코드. 329척 중 314척(95.4%) 자동 성공. 파이썬↔JS(defSectionParser.js) 314척 전수 일치.
- **디코드 구조(실데이터 확립)**:
  - 6.00/6.10/6.30: 베이테이블 ~60,500(144B 간격) + 단면 레코드 ~89,089(120B 간격). 레코드=[홀드행][홀드tier+데크tier][데크행]. 포트측(짝수) 행이 이전 레코드 꼬리로 wrap. 베이테이블 byte+121=0이면 홀드 없음.
  - 6.50/6.60: 베이테이블 ~135,900(188/189 혼재 간격 — 고정 보폭 금지) + 단면 234B 창. 함정: 라벨 공백없이 붙음('0911'), tier열 잡음 라벨.
  - 행 시퀀스: 짝수 내림+(00)+홀수 오름, ≤30. tier: 짝수 오름, 홀드 02~20+데크 82~98.
  - .def 행 리스트는 영역별 최대폭. holdCells = 홀드 행 수로 자동 채움.
- **검증(pdftoppm 대조)**: DJCT(6.30) 22베이 일치(기존 override bay01 82단 누락 발견) / KSKM(6.50) 22베이 / NBTD(6.50) bay01 데크6·홀드4. ⚠ **PDF 파일명 믿지 말 것**(SWAT__STOW_PLAN.PDF 실내용은 PCSZ — 헤더에서 추출).
- 앱: defSectionParser.js + augmentMatrixFromDef + ShipMatrixBuilderModal ".def 업로드" 버튼. ⚠ 콜사인은 6.50/6.60만 자동(6.10/6.30은 오염값 — PORT-MIS 오매칭 위험).
- 미해결(잔여 15척): PCSZ류(중앙부 셀 데이터가 .def에 없음 — PDF 보강), CNAF/XISH/RZIN(베이테이블 다른 위치).

### V7.37 .def 내장 사전 289척 — 우선순위 원칙
- 314척 중 289척 앱 내장(shipBayDict_def.js, ~370KB). EDI만 올려도 vsl 자동 매칭. 우선순위 Ⅱ-5.1대로(사용자 매트릭스 > PDF override > .def 사전 > v5 > v2 > EDI).
- 오염 제외 25척(자동 휴리스틱): 최대 데크행≥17 且 (최대−최소)≥10 = 선체외곽 템플릿 의심(PCSZ·ACVG류·SWAT·PCBJ 등). 위젯 3상태: 📐매트릭스 확정 / 🚢.def 검증전 / ⚠확정 필요.

### V7.35 안정성 보완 2건 (루트 ErrorBoundary + SW 오프라인 캐시)
- ① 루트 ErrorBoundary 부재(인쇄 모달에만) → main.jsx에서 App 전체를 `<ErrorBoundary reloadButton>`로. 런타임 에러 1건이 트리 전체 언마운트 = 흰 화면 + 카메라 멈춤(현장 사고 패턴) 방지.
- ② SW 오프라인 폴백 죽은 코드(cache.put 없음) → network-first 유지하되 같은 출처 GET 성공만 캐시. 검증 4건 PASS.
- 확인된 기수정: XRAY 셀 배경색·작업보고 항차·트윈 짝꿍은 V7.32~33에서 이미 수정됨을 코드로 확인.

### V7.34 빈 카고플랜 PDF 파서 + 자체 검증 (SWRG 2605N)
- 목표: PDF(CASPI STOWAGE)에서 빈 카고플랜(베이매트릭스) 자동 추출. 빈 프레임은 선박 고정 구조.
- 판독 원칙: 베이마다 확대해 셀 테두리(rect)만 센다(화물 마크 무시). "빈 곳 위에 화물 못 올라간다"(위 화물 있는데 아래 비면 선체 구조물). hold는 역피라미드. deck도 맨 아래 좁아질 수 있음. row 시작: deck 10·hold 08. tier는 2자리 문자열.
- 구현: emptyPlanVerify.js(verifyEmptyPlan — EDI 화물이 빈 프레임 칸 안에 있는지 교차검증, SWRG 909화물 0충돌). PDF 벡터 파서(pdfplumber). 미세조정 잔여: 94 tier 분류, deck 폭 ±1.

### V7.33 트윈 짝꿍 3대 수정 (twin.js / SearchPanel.jsx)
실데이터(DXQD=XIN QUN DAO, H3OI) 시뮬 PASS.
- ① 짝꿍 못 찾음 진짜 원인: 짝꿍 판정에 사이 짝수 베이의 **EDI 적재 여부**를 조건으로(`baySet.has(짝수)`) → 40ft 안 실린 항차는 짝수가 baySet에서 빠져 23(24)25 구조인데도 23↔25 놓침. 수정: 짝수 베이 유무는 실제 베이 구조(매트릭스→사전→EDI) 기준. 짝수 베이 조건 자체는 유지(통로/슬롯 구분). ⚠ 통째로 빼면 3↔5 무관 베이를 묶음.
- ② 앞 완료 시 뒤 사라짐: useEffect 재실행 + excludeCns. → 같은 앞 컨 조회 중 재계산 가드, excludeCns 제거.
- ③ 뒤부터 완료 후 조회창 안 닫힘: 최신 allContainers에서 두 컨 완료 여부 재조회. 둘 다 완료면 자동 클리어.
- 교훈: 사용자가 짚어준 정답(짝수 조건이 EDI 적재 여부에 묶임)을 추측으로 덮지 말 것.

### V7.30 PORT-MIS 오매칭 진짜 원인 (DJCT에 XIN TAI PING)
- V7.27 ⑧(계열대체 신원 제거)은 원인이 아니었음. 실제 = 베이사전(Firebase) **오염 레코드**. 과거 DJCT 코드로 올린 EDI가 잘못된 헤더(BSDU/XIN TAI PING)를 달고 있었고 검증 없이 박힘. 정상 EDI는 callsign='' → 저장 조건 거짓 → 병합이 기존 BSDU 보존 → 재업로드로도 안 지워짐.
- 3겹 수정(실데이터 PASS): ① 화면 가드(VoyagePage — PORT-MIS 선박명이 EDI 풀네임과 다르면 그 매칭 버림) ② 저장 조건 완화(`if(r.callsign||r.vsl)`) ③ 병합 교정(firebase.js — 새 선박명이 기존과 다르면 오염 콜사인 교체).
- 교훈: "한 곳 보고 정상 단정" 금지. 데이터 흐름은 저장 시점까지 역추적.

### V7.27 코드 정합성 점검 — "곳곳에서 안 맞음" 6건 일괄 수정
- ① 평택 판정 통일(isPyeongtaekPort 단일 함수로 8파일 14지점) ② 양하 선사 색 3자(normalizeCarrierCode — 변환표 경유, ⚠ 단순 slice 금지: SNKO→SKR) ④ 베이매트릭스 확정 배지 ⑥ V1 통과화물 X 제거(shadow X와 구분 불가) ⑦ 리퍼·특수 판정 통일(isReeferContainer + c.fr/tk/ot/oog) ⑧ PORT-MIS 오매칭(계열 대체 시 bayDef callsign·name·vesselName 제거).
- 빌드 교훈: 루트 index.html은 빌드본이라 `npm run build` 직접 시 src 미반영 — 반드시 `bash build.sh`.

═══════════════════════════════════════════════════════
# Ⅲ부 — 앱 B: 콘앱 (콘 계산기, cone.html)
═══════════════════════════════════════════════════════

> 콘앱은 **검수앱 ZIP에 동봉되는 독립 HTML**(`public/cone.html`). build.sh가 dist·루트로 복사 + 버전 라벨을 APP_VERSION과 자동 동기화한다. **검수앱과 별개 앱이므로 React 점검과 별도로 cone.html도 따로 점검할 것.**

## Ⅲ-1. 콘앱 정체·목적
- 콘(고정장치)을 컨테이너 구멍 위치에 미리 갖다 놓기 위한 가감 계산기. 입력 = 양하/선적 플랜(EDI/PDF/사진), 출력 = 베이별 추가/반납 개수.
- 검수앱 EDI 연동(fbFetchEdiContainers)도 평택분만 필터(양하=POD평택, 선적=POL평택).
- Gemini 키는 검수앱과 같은 키·같은 localStorage('master_gemini_api_key_v1') 공유(같은 도메인).

## Ⅲ-2. 콘 계산 도메인 규칙 (정확도는 생명)

### Ⅲ-2.1 콘 규칙 (컨 1개당)
- Deck 40ft = 데크콘4 / Deck 20ft = 데크콘2 + 코끼리콘2 / Hold 40ft = 일반선4·컨테이너선0 / Hold 20ft = 홀드콘1.

### Ⅲ-2.2 받침 판정
- 콘은 2단부터. 바로 아래 단 같은 row에 컨(우리 화물이든 잔류분이든)이 있으면 받침 성립.
- **받침은 자기 plan 전체화물(ediRowsAll) 기준** — 평택분만 보면 통과화물 받침 컨의 콘 누락(반대 plan은 받침 아님, 양하 후 선적 시간순).
- **받침 ±1(짝꿍 베이) = 40ft만** — 두 베이에 걸쳐 실리는 물리. 옆 베이 20ft는 다른 슬롯(받침 오인 금지).

### Ⅲ-2.3 트윈 합본 계산
- 트윈 1쌍 = 데크콘4 + 코끼리콘4. 홀드콘은 두 베이 합산(23콘6 + 25콘6 = 12). ⚠ **합산은 각자 센 값의 합이지 자기 값의 2배가 아님**(V7.61 ×2 버그 교훈).

### Ⅲ-2.4 최종 계산
- 추가필요 = 선적 소요 − 양하 회수(양수=장착, 음수=반납). 곳당 + 총개수 둘 다 표시(데크콘 4곳, 코끼리콘 2곳, 홀드콘 1곳).
- 입력 3종 자동감지: EDI(.edi/.txt, 가장 정확)·PDF·사진. 양하=POD평택, 선적=POL평택만.

### Ⅲ-2.5 양하/선적 분리 작업 (ATPR형 도메인)
- 일부 선박(ATPR 등)은 양하 완료 후 콘 전량 반납 → 하루 이상 지나 선적 = **분리 작업**. 이 경우 상계가 틀림(회수분이 배에 없음) → 선적 콘 전량 콘박스에서.
- 자동 분별: 양하·선적 둘 다 로드 + 작업일 차이 ≥24h → confirm. 완료 기록 있으면 확인창 없이 자동 분리(V7.90).

## Ⅲ-3. 콘앱 핵심 원칙

### Ⅲ-3.1 질문기는 계산하지 않는다
- 콘앱 질문기는 작업표(window.__coneQA.rows)를 **읽기만** 한다. 계산과 답이 항상 일치, 도메인 로직 자동 반영, 회귀 위험 0.

### Ⅲ-3.2 단위 도메인 (검증에서 발견한 충돌)
- **콘은 '개', 컨테이너는 '대'로 센다.** 베이 질문 컨테이너 대수 분기에 '몇 개'를 추가하면 "데크콘 몇 개"가 컨테이너 질문으로 샘 — `/컨테이너|몇\s*대|대수/` 유지.

### Ⅲ-3.3 콘앱 시뮬 방법 (영구)
- 합성 BAPLIE(LOC+147+pos / EQD+CN / LOC+9 POL / LOC+11 POD / TDT 선박명) 생성 → puppeteer로 cone.html 열고 state 주입 → runCalc()/renderCargoView()/renderBayView() 호출 → fullPage 스크린샷.
- ⚠ state 주입 형식 필수: `state.stow={ediRows:parseEDI(txt).rows, isEDI:true, ...}` (parseEDI 원본은 rows라 직접 넣으면 ediRows undefined 크래시). 비대칭 데이터(선적 전용 베이)로 동선표 경로 검증.

### Ⅲ-3.4 콘앱 카고플랜 = 검수앱 카고플랜 V2 그대로
- 콘앱 카고플랜은 본체 PrintableCargoPlanV2 + cargoPlanCore + 베이사전(.def 내장 289척)을 React째 esbuild iife 번들(`cone-cargoplan.js`, ~1.6MB). build.sh가 빌드마다 재생성·복사. 콘앱은 동적 로드(loadCargoPlanV2) → `window.ConeCargoPlan.open(...)` 한 줄. 자체 카고뷰 만들지 말 것.

## Ⅲ-4. 콘앱 작업 이력 (최신 → 과거)

### C8.34 작업표 트리오 합본 표시 (2026-07-03 — 사용자 확정, DPRT 실EDI 검증)
- 문제(사용자 제보): 콘 작업자는 트리오(예 33·34·35)를 "34번"으로 보는데, 작업표 그룹 규칙(짝수 e↔e+1 페어, 홀수는 앞 짝수 없으면 단독)이 **선행 홀수(33·25·17·13·09)를 별도 행으로 고아화** → 작업자가 짝수 행(34-35 필요 26)만 보고 홀수 몫(33 필요 2)을 누락.
- 수정: 계산·그룹은 불변, **표시 계층만 병합** — mergeTrioRows(표시·질문기·동선표 공용). 큰 숫자 = 트리오 합산(합 방식: 28=2+26, 사용자 확정 — 행별 반올림 합이라 잉여 방향으로 안전), 작은 글씨 = 소그룹 분해("33:2 · 34-35:26"), 필요 숫자 강조(need-hl). 홀수 단독 베이(21 등)는 그대로.
- 검증: DPRT 2607N/2608S 실EDI jsdom 시뮬 — 33-35 필요 D28·E8, 분해·강조 렌더 4항목 PASS. 컨별 판정 계측으로 34번 콘대상 24대(1단 7대 제외)·트리오 32대 확인.


### C7.64 설치 배너 사라짐 원인(scope 충돌) 수정 (2026-06-13)
- 증상: 콘앱 설치 문구 사라지고 실행만 됨. 원인: 콘앱 매니페스트 scope가 `/greenmarinetally/` — **검수앱과 동일 scope** → 검수앱 설치된 기기에서 콘앱을 같은 앱으로 판정 → 배너 억제.
- 수정: 매니페스트 `id:"/greenmarinetally/cone"` 고유 식별자 + scope를 `/greenmarinetally/cone.html`로 좁힘. 검증 puppeteer 6항목 PASS.
- **교훈: 같은 도메인 하위 PWA 2개는 scope·id를 반드시 분리.**

### C7.63 홈화면 설치(PWA) 지원 (2026-06-13)
- 콘앱도 검수앱처럼 홈화면 추가. 매니페스트 start_url 절대경로 + 고정 콘(트위스트락) PNG 3종 + apple-touch-icon. **콘앱 전용 SW cone-sw.js 신설**(안드로이드 Chrome은 SW 있어야 설치 배너). 배포 시 cone-sw.js를 cone.html과 같은 폴더에 함께 올릴 것. 검증 7항목 PASS.

### C7.62 카고플랜 "새 기기에서 특정 선박 안 보임" (2026-06-13)
- 증상: YKTD 카고플랜이 기존 폰은 보이고 새 폰은 "선박 정보 없음". 원인: 카고플랜 모듈 사전 탐색이 window.__fbShipBayDict 전역(검수앱 런타임이 채움)에 의존하는데 **콘앱은 이 전역을 안 채움** → Firebase v3 사전 미전달. 로컬 사전 보유 기기만 성공.
- 수정: openCargoPlanV2에서 모듈 호출 전 `window.__fbShipBayDict = await fbFetchBayDict()` 주입. 검증 puppeteer 3항목 PASS.
- 운영 처방: "안 보이는 선박 = 보이는 폰에서 빌더 재저장" + 콘앱 C7.62 이상 필수.

### C7.61 카고플랜 "못 본다" (2026-06-13)
- 원인: ① 모듈 로더가 `?v=&r=Date.now()` — 열 때마다 캐시 무력화로 1.6MB 재다운로드, 약신호에서 실패 ② 항차 목록 fbFetchVoyages가 전체 다운로드(수 MB).
- 수정: `r=Date.now()` 제거 → `?v=__APPV`만(window.__APPV='C7.61' 상수, 모듈 갱신 시 같이 올릴 것), 재시도 2회(백오프), fbFetchVoyages 경량화(shallow 병렬, 수십 KB). 효과: 첫 1회만 다운로드. 검증 5 + puppeteer PASS.

### V7.46 콘앱 카고플랜 = 검수앱 카고플랜 V2 그대로 (V7.45 정정)
- V7.45 오해: "진짜 카고플랜"을 베이그리드로 해석해 자체 셀 뷰 제작. 사용자 의도 = 카고플랜 V2(A4 가로 한 장, o·X 마크, 검수앱과 동일 구조).
- 해법 = 같은 코드 번들: coneCargoPlan.entry.jsx 신규 → cone-cargoplan.js(~1.6MB) 동적 로드. 검증 SUNNY KALMIA(=KSKM) 합성 EDI 15베이 PASS. 자체 카고뷰 ~350줄 제거.
- ⚠ 콘앱 EDI에는 선사(op) 없어 별첨 선사별이 UNK(BAPLIE 한계). 검수앱 연동(fb ediContainers)은 op 포함이라 정상.

### V7.45 콘앱 카고플랜 본체급 시각화 (V7.46으로 대체 — 기록 보존)
- 자체 셀 뷰(88×56px, mono 4줄+특수 컬러바). parseEDI rowsAll 반환, 기본 줌 100%. V7.46이 본체 번들로 완전 대체.

### V7.44 콘앱 동선표 — 콘 종류 명확화
- 종류 칸 풀네임+색 배지(데크콘=노랑·코끼리콘=파랑·홀드콘=초록), 받는 곳 "베이NN→N개". 버그 fix: computeRoutes가 totalSupply 미반환 → 요약 코끼리콘·홀드콘 "배 안 이동" 줄 미표시.

### V7.41 콘앱 대폭 보강 — 결과 화면 정돈 ("급하게 만들어 조잡함")
- ① 결과 모드 신설(안내 3카드 자동 접힘 + 고정 결과 탭바 작업표|카고플랜|베이플랜, 자동 스크롤) ② "Failed to fetch" → friendlyErr 한국어 ③ 작업표 "회수N→필요N" + 범례 ④ 동선표 빈 상태에도 카드 유지 ⑤ 베이뷰 컨번호 **끝 4자리 노란 강조**(현장 호출 관행). 비활성 계산 버튼 opacity .35.

### V7.61 콘앱 홀드콘 ×2 버그 — ATPR 실데이터로 확정
- 진단(ATPR_017 선적 실EDI, 445대): 19번 홀드 콘 18 정답인데 작업표는 36 — 전부 정확히 2배. 그룹 계산(ediToBayGroups)은 정확, renderResults 작업표 변환의 `g.stow.hold*2`가 범인. "트윈 두 베이 합산"을 ×2로 오역.
- 수정: ×2 제거(need/have/diff 원값). 베이뷰·질문기·동선표는 원값 기반이라 자동 정합. 검증 ATPR PASS.
- 교훈: 진단은 "순수 함수 단위 추적 → 화면값 비교"로 2배 지점 좁힘. 실데이터 오니 10분에 끝남.

### V7.60 콘앱 카고플랜 로드 실패 + 트윈 중복 (검수앱 동시 — Ⅱ부도)
- 콘앱 카고플랜(1.6MB) 동적 로드 누구는 되고 안 됨 → 3중 방어: build.sh 번들 `--target=es2017`(구형 웹뷰), loadCargoPlanV2 실패 시 1.2초 후 1회 재시도 + 원인 구분 안내, sw.js install에 cone-cargoplan.js precache.
- (검수앱 측) 트윈 끝4자리 중복 시 선택 안 됨 — Ⅱ부 참조.

### V7.59 콘앱 콘 계산 정밀화 3건 (정확도는 생명)
- ① 받침 ±1 = 40ft만(sizeAt 맵, 옆 베이 20ft 받침 오인 제거) ② 전용 데크(20ft 데크콘4) 판정 = **구조 기준**(베이사전 ∪ 전체화물, 화물 유무로 구조 판정하는 패턴 재발 — "구조 판정은 구조 데이터로") ③ 받침 = 자기 plan 전체화물(통과화물 받침 인정). runCalc async화. 검증 3케이스 PASS.

### V7.56 음성인식 항만 용어 보정 (검수앱+콘앱)
- 증상: "양하"를 6번 말해야 인식(크롬 STT가 양아/양화/향하로). 후처리 보정이 정답. 수정 2축: maxAlternatives=5(후보 전부서 항만 용어 최고 채택) + 오인식 교정 사전(coneFixSpeech 동일 내장 — 양아·양화·향하→양하, 홀두콘→홀드콘, 댁콘→데크콘, 니퍼→리퍼 등 보수적 치환만). 운영: 현장 오인식 단어는 🎙 표시 글자 그대로 수집 → 사전 추가.

### V7.55 콘앱 질문기 — 콘 작업 전용
- 범위 확정: 콘 작업에 관한 것만("이 베이는 어떤 콘이 몇 개 남는다/모자란다"). 검수앱 질문 엔진 통째 이식은 과잉 → 작업표(rows) 읽는 초경량 내장 파서. 🎤 음성 입출력(+/−를 플러스/마이너스). 베이 매칭: 페어 라벨("06-07") split. X-RAY·실번호·진행률은 콘앱 데이터에 없어 미지원.

### V7.52 콘앱 베이플랜 — 베이 간 정렬 (검수앱 동시 — Ⅱ부도)
- 콘앱(cone.html): 골격 계산을 computeSkel로 추출해 2패스 — 1패스 전 베이 골격·globalMaxW 수집, 2패스 centerMargin 기준 globalMaxW. 검증 TMPZ 일직선 PASS.

### V7.51 콘앱 베이플랜 — 자리 표시 3원칙 (영구 원칙)
V7.49·V7.51 1차 연속 오답 — 사용자가 검수앱 화면(TMPZ) 비교로 정정. **영구 명문화.**
- ① 자리는 항상 표시: 컨테이너 없다고 자리가 없는 게 아니다. 빈 슬롯=점선, 빈 단도 통째 표시. 반대 작업 슬롯(off)도 보이는 점선.
- ② 가상 칸 금지: 데크8·홀드7이면 홀드 7칸 그대로. 가상 칸·투명 공백·null 칸 삽입 금지. 정렬은 좁은 쪽 0.5칸 margin(피치 60px → 30px).
- ③ 폴백 연속 보간: 사전 없는 선박 EDI 폴백 골격은 보인 범위 내 연속 보간. 컨테이너 있는 칸만 수집하면 빈 row·단 누락(① 위반).
- **교훈: 정렬 방식을 세 번 갈아엎었다. 처음부터 Ⅱ-4.4와 검수앱 실화면(TMPZ)을 대조했으면 한 번에 끝났다. 화면 약속은 본체 화면이 정답지다.**

### V7.50 콘앱 베이플랜 — phantom 00 제거 + 카고플랜식 정중앙
- 사용자 지적: 강제 삽입 00 row(컨테이너 불가 자리). 원인: 공통 hasZero를 데크·홀드 양쪽 적용 + EDI 폴백 deckRowPos=holdRowPos=합집합(Ⅱ-6 축 통합 phantom 00 재발). 수정: 데크/홀드 row 완전 분리(deckHasZero/holdHasZero 각자), 칸 삽입 폐지, 정렬 = 좁은 쪽 table margin-left=(maxCols−cols)×60÷2. 검증 PASS.
- **교훈: 데크/홀드 분리 관리 원칙은 본체만 아니라 콘앱 등 모든 화면에 적용. 같은 실수가 앱을 바꿔 재발.**

### V7.49 콘앱 베이플랜 — 진짜 "빈 화면" 제거
- V7.48 오해: "빈화면"을 점선 격자로 해석해 압축만. 사용자 의도 = 격자도 없이 공백으로 비는 영역. 진짜 원인: cells가 양하+선적 합집합 — 반대 작업 전용 단의 슬롯이 `bv-slot off`(배경과 거의 동일색)로 안 보이게 그려져 영역만 차지. 수정: 패널별 tier 필터(tiersWith — 그 작업 컨 있는 단만). 검증 PASS.
- **교훈: 시각 증상 단어("빈화면")는 코드 용어 번역 전 재현 화면으로 확인. "그려지지만 안 보이는" 요소는 점선/격자 어휘에 안 잡힌다.**

### V7.48 콘앱 베이플랜 — 홀드 쏠림 fix + 빈 구역 압축
- 홀드 쏠림: 데크/홀드 별도 `<table>`인데 row 라벨 행이 열폭 흔듦. 수정: `table-layout:fixed; width:max-content` + 전 칸 58px 고정(tier 라벨 18px). **교훈: 별도 테이블 두 개 세로 정렬엔 table-layout:fixed + 전 칸 고정폭 필수.** 빈 구역(컨 0)은 점선 격자 대신 "갑판 · 화물 없음" 한 줄.

### V7.47 콘앱 작업 목록 = 검수앱과 1:1 (숨김 필터 제거)
- 증상: 검수앱엔 있는 항차가 콘앱에 안 보임. 원인: 콘앱 fbFetchVoyages에 3일 필터 + EDI 없는 항차 제외. 수정: 3일 필터 제거(살아있는 항차 전부) + EDI 없는 항차 noEdi 회색 표시. **원칙: 콘앱 목록은 검수앱 voyages와 1:1. 콘앱이 따로 숨기는 조건 두지 말 것**(숨김은 검수앱 완료/자동삭제 담당).

### V7.91 콘앱 음성 질문 자동 복원 + build.sh 라벨 누적 수정
- 검수앱 V7.80 동일 이식(coneFixQuestionWithAI, temp0·60토큰·4초). **AI는 답하지 않는다 — 질문만 복원**, 답은 coneQaAnswer(작업표 읽기)에 재투입. Gemini 키 검수앱과 공유.
- build.sh 버그: cone.html 라벨 sed `V[0-9.]*`가 하이픈 미포함 → 빌드 번호(-07) 도입 후 라벨 누적(`V7.90-07-06-05…`). `V[0-9.-]*`로 수정(오염 라벨 자가 치유). 검증 6케이스 PASS.

### V7.91-02 질문기 일상 동의어 확장 (검수앱+콘앱)
- "전체"는 되는데 "전부/다"는 안 됨. 검수앱: 전부·모두·몽땅·죄다 + 단독 "다"(토큰 경계 매칭 "남았다" 오인 방지) + '데크' 추가. 콘앱: 남았·남을·뺄 / 필요·가져가야.
- ⚠ 단위 도메인: 콘앱 베이 질문 컨테이너 대수 분기에 '몇 개' 추가 금지(Ⅲ-3.2). **복원 어휘 동기화: 파서 어휘 늘리면 KNOWN(콘앱 CONE_QA_KNOWN)도 같이 늘릴 것(영구).**

### V7.70 콘앱 양하/선적 분리 작업 자동 분별 — ATPR형 (도메인 신설)
- Ⅲ-2.5 도메인 구현. 양하·선적 둘 다 로드 + 작업일 차이 ≥24h → confirm "양하 이미 완료해 콘 전부 반납?". 작업일 출처 = EDI 작성일(extractEdiDocDate: DTM+137 우선, 폴백 UNB). 검증 ATPR_017 3케이스 PASS.

═══════════════════════════════════════════════════════
# Ⅳ부 — 앱 C: 벌크탤리 (벌크선 코일 검수, bulk_tally.html)
═══════════════════════════════════════════════════════

> 최종 업데이트: 2026-06-12 (양하 모드+펄프 / 시작 화면 2026-06-13)
> **이 앱은 검수앱(Tallyman Master)·콘앱과 완전 별개 — 따로 점검.** 범용 코딩 원칙(0부)은 동일하게 적용한다.

## Ⅳ-1. 앱 개요

### Ⅳ-1.1 정체
- 벌크선(예: WESTWOOD 계열) **철강 코일 검수** 단일 HTML 앱. 파일 1개(bulk_tally.html)로 동작.
- React 18.3.1 + Tailwind를 CDN(esm.sh, cdn.tailwindcss.com) importmap으로 로드. **빌드 없음.**
- 기능: 바코드 스캔(html5-qrcode) + 한글자판 오타 복원, 수동/블루투스 입력, PORT MIX 차단, 취소 모드, 통계, 특수화물(데미지·HR·탑중량·Eye-to-Sky), 적부(베이플랜), 보고서(PDF/엑셀/CSV/공유), Supabase 실시간 다검수자 동기화 + 오프라인 큐, 모니터 탭.

### Ⅳ-1.2 진입 흐름 (2026-06-11 확정)
- **시작 화면 → ① 검수자명 입력 → ② 항차 선택(또는 + 새 항차 생성) → [시작하기] → 작업 화면.**
- 검수자명·활성 항차가 둘 다 있으면 시작 화면 생략. **헤더 선박명 탭** = 시작 화면 재진입.
- 신규 설치 기기에 데모 항차 자동 생성 안 함(과거 WESTWOOD VICTORIA 935코일 임베드 노출 문제). 새 항차는 빈 마스터(0 COILS)로 시작 → ⚙ 데이터 관리에서 엑셀 업로드.

## Ⅳ-2. 항차 보관함 (항차별 분리 관리)

### Ⅳ-2.1 저장 구조 (localStorage)
| 키 | 내용 |
|----|------|
| `bulk_voyages_v1` | 항차 레지스트리 {항차키: {vessel, voyage, pol, pod, updatedAt}} |
| `bulk_active_voyage_v1` | 활성 항차 키 |
| `bulk_master_<항차키>` | 항차별 마스터 (coils, bls, damaged) |
| `bulk_scans_<항차키>` | 항차별 스캔 기록 |
| `tally_settings_v1` | 전역 설정(검수자명·shift·hold/raw/tier/seq·pod·autoSeq) — 항차 무관 |
| `supabase_config_v1` / `supabase_queue_v1` | 클라우드 설정 / 오프라인 전송 큐 |

- 항차키 = `선박명-항차` 대문자·공백제거(예: WESTWOODVICTORIA-148E). Supabase voyage 컬럼과 동일 키.
- **레거시 자동 이전**: 구버전 키(voyage_v1 등)가 실제 존재할 때만 첫 실행 시 항차 1건으로 복사(원본 보존). 빈 "[]"는 부재로 취급(마이그레이션 가드).

### Ⅳ-2.2 항차 운영 규칙
- **새 항차 엑셀 업로드 자동 분기**: 업로드 파일의 선박·항차가 현재와 다르면 "새 항차로 등록?(현재 항차 보관)" confirm → 확인 시 새 항차 생성·전환. **이전 항차 스캔 섞임 원천 차단.**
- 항차 정보 수정으로 키가 바뀌면 마스터·스캔 데이터 키도 함께 이동. 활성 항차는 삭제 불가. 삭제는 기기에서만(클라우드 기록 유지).
- 초기화·스캔삭제 버튼은 현재 항차 범위만. 보고서·모니터·현황은 항차별 키를 읽음(구 단일 키 `tally_scans_v1` 잔존 참조 3곳 수정 완료).

## Ⅳ-3. 스캔·동기화 핵심 동작

### Ⅳ-3.1 스캔 흐름
- handleScan 전체가 try/catch 보호 — 처리 중 예외가 나도 **스캔 루프/카메라가 죽지 않음**(검수앱 "에러 시 카메라 멈춤" 패턴 예방).
- 중복 디바운스 2초. 마스터 미매칭/중복/PORT MIX는 진동·비프·플래시로 구분.
- **Seq 자동+1 토글**(기본 꺼짐): 켜면 스캔 성공마다 SEQ +1. ※ 증가 규칙이 현장과 다르면(TIER 우선, HOLD별 리셋) 규칙 확인 후 조정 — 미확정.
- 스캔 카드 time 필드 방어 처리(time 없는 비정형 레코드도 크래시 없음).

### Ⅳ-3.2 클라우드 큐 (데이터 유실 방지 — 절대 보존)
- 전송 실패 항목은 큐에 **보존**(과거: 실패해도 통째 clearQueue → 유실). 중복(23505)은 성공 취급.
- **삭제도 큐잉**: 오프라인/실패 시 `{_op:"delete", voyage, pid}` 큐 보관(과거: 취소 코일이 재접속 머지로 부활).
- 30초 주기 자동 재시도(선상 네트워크 플랩 대비) + 동시 실행 가드. 원격 DELETE 수신 시 항차 검증. CSV escape 처리.

## Ⅳ-4. 작업·검증 원칙 (이 앱 전용)

### Ⅳ-4.1 수정 원칙
- 파일 안 JS는 **컴파일된 jsx()/jsxs() 호출 형태** — UI 추가·수정도 같은 형태(JSX 문법 금지).
- 배포본은 CDN importmap(esm.sh 9곳 + tailwind) 원본 유지. 수정 후 md5로 출력본 동일성 확인. 새 localStorage 키는 `bulk_` 접두사.

### Ⅳ-4.2 검증 방법 (CDN 차단 환경 실 브라우저 테스트)
1. **검증용 사본**(bulk_tally_TEST.html)만 importmap을 로컬 번들로 치환 — 배포본은 손대지 않음.
2. 로컬 번들: react/jsx-runtime/react-dom-client/lucide를 하나의 all.mjs로 함께 번들(esbuild, react 단일 인스턴스) 후 specifier별 shim 분배. CJS `export *` 재수출은 named export 누락 → 명시적 export 필수. external 방식 금지(ESM throw).
3. Tailwind는 @tailwindcss/browser(npm) 대체.
4. python http.server + puppeteer-core(`--no-proxy-server`). 첫 로드 60초+ 가능(워밍업 후 재시도).
5. **테스트 레이스 주의**: localStorage.clear() 후 곧바로 reload하면 구 페이지 effect가 키 재오염 → clear 전후 1초+ 대기.
6. 회귀 스위트(27항목): 시작 화면 12 + 항차 흐름 8 + 스캔 기능 7. 수정 시 전체 재실행.

## Ⅳ-5. 작업 이력

### 버그 수정 9건 (2026-06-11)
1. 오프라인 큐 유실(실패 시 통삭제) → 실패분 보존 / 2. 취소 코일 부활(오프라인 삭제 미전파) → 삭제 큐잉 / 3. 스캔 콜백 예외로 카메라 정지 → 전체 보호 / 4. CSV 콤마 깨짐 → escape / 5. 큐 재시도 부재 → 30초 타이머 / 6. 타 항차 원격 삭제 전파 → 항차 검증 / 7. 보고서/현황 구 단일 키 참조 3곳 → 항차별 키 / 8. time 없는 레코드 크래시 → 방어 / 9. 마이그레이션 가드가 빈 "[]"를 데이터 오인 → 부재 취급.

### 기능 추가
- Seq 자동+1 토글 / 항차 보관함(분리 저장·전환·삭제·새 항차·자동 마이그레이션·업로드 자동 분기) / 시작 화면(검수자명+항차 게이트, 헤더 탭 재진입, 신규 설치 데모 차단).

### 양하 모드 + 펄프 화물 (2026-06-12)
- 항차에 **workMode**('loading'|'discharge')·**cargoType**('coil'|'pulp') 추가. 새 항차 폼에 토글.
- **양하 항차 키 = `선박-항차-DIS`**(선적은 무접미 — 하위호환). Supabase voyage도 같은 키라 자동 분리. 항차 정보 수정 시 병합 보존.
- 라벨 동적화: WORK_MODE/CARGO_TYPE/MODE_KO/UNIT_EN(COILS↔UNITS)/CARGO_KO(코일↔펄프) — VOYAGE 로드 시 결정.
- **마스터 0건 자유 기록**: 0건 항차는 스캔·수동 입력을 자유 기록(_free, 펄프면 품명 PULP). 1건 이상이면 기존대로 미매칭 차단.
- 미확정: **펄프/양하 마스터 엑셀 형식 미확보** — 샘플 수령 시 parseFile에 파서 추가. (WW_COLUMBIA DONGBU PPG 시트는 제품번호 없는 PO 수량이라 대조 부적합.) 검증 12항목 PASS.

### 시작 화면 상시 표시 (2026-06-13)
- (신) 앱을 새로 켜면 항상 시작 화면(검수자·항차 확인) — 검수앱과 동일한 의식적 출근 체크. 기존 검수자명·항차는 미리 채워져 [시작하기] 한 번. 구현: enter()에서 sessionStorage `bulk_entered_session=1` → 세션 플래그 없으면 게이트 true. 같은 세션 리로드는 게이트 생략, 재시작이면 표시. 검증 5항목 PASS.

## Ⅳ-6. 알려진 한계 / 다음 후보
- **CDN 의존**: 첫 로드에 인터넷 필요(브라우저 캐시 후 대체로 동작, SW 오프라인 보장 없음) → PWA화 후보.
- Seq 자동 증가 규칙 미확정(TIER/RAW/HOLD 연동 확인 후).
- 항차별 클라우드 과거 항차 복원(기기 교체 시 Supabase 불러오기) 미구현.
- 항차 자동 보관 한도/정리 정책 없음.

═══════════════════════════════════════════════════════
# Ⅴ부 — 영구 참조 자료 (REF, 검증 완료 사실만)
═══════════════════════════════════════════════════════

> 이 부는 **데이터로 PASS 검증된 사실만** 담는다(추측·미확정은 Ⅱ~Ⅳ부 작업 이력에). 해당 작업 전 이 부를 먼저 읽는다.

## Ⅴ-A. 3D 입체 카고플랜 구현 기준
작성: 2026-06-14 (MCSN 624S 실데이터 검증). 상태: 기반 검증 완료 / 3D 프로토타입 미완.

### A-1. 절대 원칙 — 3D 격자의 진실은 `buildEmptyBayRenderData` 하나뿐
3D 좌표를 EDI 슬롯에서 임의 역산하지 **않는다**. 카고플랜·베이플랜·매트릭스 미리보기가 전부 `src/cargoPlanCore.js`의 `buildEmptyBayRenderData(bayEntry, bayKey, isPair)`를 쓴다. 3D도 반드시 이 출력을 좌표로 써야 화면 3자(카고플랜/베이플랜/3D)가 100% 일치.
- 과거 단독 프로토타입(EDI LOC+147에서 임의 매핑)은 **폐기**(빈 슬롯 안 보이고 tier/row 어긋남).
- 출력 구조: `{ deckRows:[{tier,invisible,cells:[{active,rowLbl,mark,isXray,colorKey,...}]}], holdRows:[…], deckRowPos:[…], holdRowPos:[…], nDeckCols, nHoldCols, deckTiers, holdTiers, hatchCount, deckAlign, holdAlign }`.
- 3D 매핑: 각 active 셀 = 컨테이너 한 자리. **bay=Z축(선수~선미), 칸 index(또는 rowLbl)=X축, tier=Y축(데크=갑판 위, 홀드=데크선 아래)**. EDI/Firebase 슬롯이 (bay, rowLbl, tier)에 매칭되면 색 채움. 빈 active 셀 = 아직 안 실린 자리.
- 검증: MCSN 3E39 34베이 active 셀 합 = cells 합 100% PASS.

### A-2. 베이매트릭스 데이터 — 어디서 오나
- 저장 경로: Firebase `ship_bay_dict_v3/{code}` → `window.__fbShipBayDict` + `localStorage['gm_fb_baydict_cache']` 미러.
- 정적 사전(v2/v5/def)에는 deck/hold tier 분리 구조 없음(v2=rowsMax만, v5=cells 합본·deckRows:0). 3D에 쓸 건 사용자가 매트릭스 빌더로 입력해 Firebase 저장한 것뿐.
- 빌더 저장 베이 필드: `{bayNo, deckTiers[], deckCells[], holdTiers[], holdCells[], deckHasZero, holdHasZero, hasZero, hatchCount, pairEven, deckAlign, holdAlign, deckPadLeft/Right, holdPadLeft/Right, rowCount, source}`.

### A-3. 정렬 규칙 (화면 = 진실)
- 정상 저장값은 전 베이 `deckAlign='center' / holdAlign='center'`(MCSN·KSKM 확인). center는 % 단위라 홀수 diff도 0.5칸씩 좌우 균등 = 정중앙.
- **중앙 기준은 베이마다 다름**: 홀수 row만→01 중심 / 00 있으면→00 중심 / 짝수 폭→01·02 사이. `getRowPositions(nCols, hasZero)`가 처리하므로 3D는 `rowPos` 그대로 따름(재계산 금지).
- "우측 정렬" 규정 언급이 나와도, 실제 카고플랜 표시(center)가 진실.

### A-4. 페어(트윈) 베이 — 카고플랜 묶음 vs 베이 나열
- 카고플랜은 짝수+다음 홀수 한 페이지로 묶어 그림(02-01). 단, **베이 나열 자체는 개별 번호**(1,2,3,5,6,7…). 3D 베이 선택 UI는 개별, 격자 렌더만 페어.

### A-5. EDI 슬롯 좌표 — LOC+147 BBBRRTT
- `LOC+147+{7자리}` = bay(3)+row(2)+tier(2). 컨번호는 같은 그룹 `EQD+CN`(curLoc을 EQD까지 유지). tier≥80=데크, <80=홀드.
- ⚠ **미해결 검증 포인트**: EDI row는 선형(1,2,3…), 격자 rowPos는 중앙정렬(…02,01,03…). 단순 숫자 비교 매칭은 11.9%만 맞음 — **BayPlan의 EDI→좌표 변환 로직을 그대로 가져와 재검증**(다음 세션 1순위, 목표 100% PASS).

### A-6. 검증된 테스트 선박
| 코드 | 선박명 | 구조 | 용도 |
|---|---|---|---|
| 3E39 | MCSN (SPIL NIKEN) | 34베이 완전 | 대형선 |
| 3E89 | KSKM | 22베이 완전 | 일반선 비교 |
| 3E47 | MAMP | 36베이 완전 | 추가 |

- 전체 89키 중 deck/hold tier 구조 완비 = 27키(3E39 3E47 3E89 3EHZ 5LRE 9HA3 BSDU D5MO D5MP D5QW D5RR D7XF DCON H3OI PCSZ SWCP SWDN SWRG TMPZ TNJP V2EE V7A2 V7A4 V7A5 VRKS VRSC YKTD). 나머지는 ASC/자동본이라 구조 없음 → 3D 불가.

### A-7. 발견 문제 (MCSN 624S 세션)
- **MCSN 두 키 분열(해결)**: 같은 배가 `MCSN`(ASC 자동본 0/22)·`3E39`(콜사인·matrix_builder 34베이) 두 키. EDI는 MCSN으로 들어와 빈 자동본과 충돌. 해결: 조회 시 source='user'/sourceFile='matrix_builder' 우선(DXQD↔H3OI 패턴과 동일).
- **3D 좌표 EDI 역산 금지(원칙 신설)**: A-1로 확정.
- **정렬 = 화면이 진실(확정)**: A-3.
- **페어 묶음 vs 베이 나열(확정)**: A-4.

### A-8. 검증 환경
- 매트릭스 데이터: `fb_baydict_full.json`(콘솔 export — `window.__fbShipBayDict` 또는 캐시를 JSON 다운로드).
- 시뮬: `node --input-type=module`로 cargoPlanCore.js의 buildEmptyBayRenderData 직접 import. 렌더 PASS: SVG → puppeteer-core(`/opt/google/chrome/chrome`, `--no-proxy-server`) PNG 캡처 → view 확인. 빌드/생성만으로 완료 금지.

---

## Ⅴ-B. 베이 페어링 (pairEven)
근거: V7.98-11 카고플랜 페어 "3 (4)5"→"3 5" 붕괴 수정(node 실함수 시뮬 ALL PASS).

### B-1. 페어 저장 모델 (진실)
40ft 컨테이너는 짝수 베이 1개를 차지하고 물리적으로 양옆 홀수 2개 자리를 덮는다. 표기는 "홀 (짝)홀" 트리오("3 (4)5") 또는 "(짝)홀" 페어("(04)05").
- 매트릭스 빌더는 페어를 **홀수 베이 엔트리 1개**로 저장하고 `pairEven` 필드에 짝수 번호. **짝수 베이는 별도 키를 만들지 않는다.**
  - 예: "(04)05" → 키 `005`, pairEven='04'. `004` 키 없음. 트리오 "3 (4)5" → 홀수 `003`(top) + 홀수 `005`(pairEven='04').
- 단, .def/v5 PDF 파싱본은 **짝수를 별도 엔트리**로 갖는 경우가 있음 → 같은 배 안에서도 두 표현(pairEven 흡수 vs 짝수 별도) **공존 가능**. 이게 비대칭의 근원.

### B-2. 페어를 다루는 모든 함수 (수정 시 전부 점검)
| 함수 | 파일 | pairEven 처리 |
|---|---|---|
| createEmptyBayEntry | shipMatrixBuilder.js | 생성자(정의처) ✓ |
| matrixToBayDictEntry | shipMatrixBuilder.js | 전파 ✓ |
| detectMissingBays | shipMatrixBuilder.js | 인식 ✓ (M6.94.36) |
| autoPairBays | cargoPlanCore.js | 인식 ✓ (V7.98-11, 짝수-별도 루프 뒤 pairEven 루프) |
| matrixBays 생성부 | PrintableCargoPlanV2.jsx | 전파 ✓ (V7.98-11) |
| pairEven 리졸버 헬퍼 | cargoPlanCore.js (~880행) | 인식 ✓ |
| buildBayPages | PrintableBayDetail.jsx / PrintableCargoPlan.jsx | (n-1,n) 휴리스틱 — **점검 권장** |
| splitForeAft | PrintableBayDetail.jsx | 짝수 별도 기준 — **점검 권장** |
| getBayPairs / findTwinCandidate | twin.js | **점검 필요**(실데이터 미확인) |

### B-3. 절대 원칙
1. **페어 함수는 여러 개다. 한 곳만 pairEven 보정하면 비대칭 버그 재발.**(V7.98-11: detectMissingBays는 보정됐으나 autoPairBays가 빠져 "7 (8)9"는 되고 "3 (4)5"는 붕괴.) 수정 시 §B-2 표 전부 점검.
2. **짝꿍 판정을 짝수 베이의 EDI 적재 여부에 의존시키지 말 것.** 그 슬롯에 40ft 안 실린 항차는 EDI/baySet에 짝수가 안 나타나 페어 붕괴. 판정은 매트릭스 구조 / pairEven 기준만.
3. pairEven은 항상 해당 홀수의 좌측 짝수(=홀수-1). 트리오의 top(반대편 홀수)은 짝수-1.

### B-4. 검증 (V7.98-11, node 실함수)
- pairEven 저장 페어(`005` pairEven='04') → 트리오 `["03","(04)05"]` PASS / 짝수-별도 저장 → `["07","(08)09"]` 불변 PASS / 단독 "03"/"05" 잔존 안 함 PASS / 레거시 회귀0·중복0 PASS.

═══════════════════════════════════════════════════════
# Ⅵ부 — 운영 매뉴얼·실증 데이터 (검수앱)
═══════════════════════════════════════════════════════

## Ⅵ-1. 선적 작업 워크플로

**양하 vs 선적의 본질**
| 구분 | 양하 | 선적 |
|---|---|---|
| EDI 위치 의미 | 실체(이미 적재됨) | **계획**(적재 예정, 실체 없음) |
| 검수원 역할 | 실제 화물 검수 | **계획 → 실체 전환** |
| 위치 변경 | 의미 없음 | 빈번(현장 적치 다름) |

- **실체 발생 시점**: EDI 계획 → 컨번호 조회 → (위치 확인/수정) → **선적확인 버튼** → 그때 실체.
- **위치 두 박스**: 선내 위치(계획) = EDI 단일 진실, 변경 금지 / 실체 위치 = 검수원 입력(`01/00/02 → 11/11/11 (검수원명)`), "삭제"로 계획 복귀. 양하에는 실체 박스 없음.
- **자리 뺏긴 컨테이너**: 컨 X가 위치 수정해 컨 Y의 계획 자리 점유 → 베이 탭 상단 노란 박스에 Y 자동 표시. 처리: (A) [📦 이동] → 빈 셀 클릭(짝수↔짝수/홀수↔홀수만) (B) 수동 입력.
- **보관함(선적 전용)**: bay_actual='__STG__' 컨 모음. [📦 이동] 복귀, [↻ 일괄 복원]. PC 영역 선택([🔲 선택]).
- **마감 점검([🏁 마감 점검])**: 미완료 컨 / 리퍼 온도 미입력(Full만) / ISO403 사진 / X-RAY 미처리(양하만) / 자리 뺏긴 컨(선적만) / 풀씰 미입력(선적만) — 모두 0이면 ✅.
- **양하리스트 매핑**: 풀씰 → c.sl, 엠티실 → c.eseal 자동 분리(헤더 자동 인식).
- **데이터 모델(선적)**: `bay/row/tier`=effective(actual 우선) / `_bay_planned`=계획 보존 / `bay_actual`+`actual_at/by`=실체 / `_position_moved`,`_displacedBy`. Firebase: `voyages/{voyageKey}/loading/records/{cn}`. 함수: fbSetActualPosition, fbClearActualPosition.

## Ⅵ-2. CASP .def 블록 크기 매핑 (323척 실증)
| CASP 버전 | 표준 블록 | 예외 | 척수 |
|---|---|---|---|
| 6.00 | 144 | — | 5 |
| 6.10 | 144 | 80(4척) | 185 |
| 6.30 | 144 | 80(2척) | 11 |
| 6.50 | 189 | 102(3척), 201(1척) | 120 |
| 6.60 | 189 | — | 2 |
- 블록 크기는 CASP 버전이 결정(TNJP 189를 전체 적용하던 옛 가정 폐기). V7.36~38 단면 디코더가 이 매핑 위에 구축됨.

## Ⅵ-3. 라이브러리/보관소 분리 설계 (Ⅱ-7.3/7.4 근거)
- 라이브러리(진행 상황) = 살아있는 voyages. `선박명 · 양하 N · 선적 M`(평택분). 완료하면 사라짐. 수석용 [완료 저장].
- 보관소(완료 기록) = ships 노드, 최근 완료순. 항차별 줄(`2608N · 양하 452 · 선적 465 · 일자`) + 누적.
- 완료 흐름: ① archive/{voyageKey} 전체 백업 ② ships에 항차 통계 ③ 백업 확인 후 voyages 삭제. 이중 확인(항차 화면=검수사, 라이브러리=수석).

## Ⅵ-4. [REF] 신규 선박 복제 파이프라인 (V7.99~V7.99-5, 검증 완료)
진본 .def 없는 신규 선박을 기존 등록 선박 복제·보강해 베이매트릭스를 만드는 4단계. 실데이터(EVER CONFORM EDI·fb_baydict 89척·Define.zip 329척) 검증 PASS.

- **4단계 흐름**: ① 기존 선박 매칭에서 구조 우선 추천 → ② 그 매트릭스에 현재 화물(EDI) 실어봄 → ③ 중력 무시 셀 처리 → ④ 100% 확인 후 제시(그대로 저장 / 수정 후 저장).
- **우선순위(절대)**: 확정 베이매트릭스 > 복제(보정중) 구조 > EDI. 복제본도 source='user'(EDI보다 우선·자동분석 차단). 단 "🛠 보정중" 표시(provisional 플래그, 동작 영향0).
- **매칭 = 구조 우선(절대 교훈, 2회 반복 실수)**: 수용률만으로 정렬 금지(더 큰 배는 작은 배 컨테이너를 다 받아 99~100%지만 빈 베이·단 다수로 카고플랜 어긋남). 정렬 = 구조점수 우선, 수용률은 동률 tiebreak. 구조 지문(shipMatchFinder.js extractFingerprint): ① 홀짝 정규화 베이집합(40ft 페어 정규화 — 안 하면 같은 배도 0.68로 깎임) ② 부분집합 ③ capacity 수용성 ④ deckonly 패턴 ⑤ 데크높이등급 ⑥ 끝베이 근접. 검증: EVER CONFORM → SAWASDEE 계열·CNC SATURN 91% 공동최상위. 교차검증 89척 top-1 74%·top-3 96%.
- **화물 실어보기(verifyMatrixFit)**: tier 기준 수용률, 누락 시 missByBay. "실린 컨테이너 다 보이면 복제 성공."
- **중력 위반 셀 보정(detectBlockedCells, 확실한 것만)**: 기준 A만 — 한 베이·한 row에서 적재 최저단보다 아래 빈 단 = 채울 수 없음 = blocked. 위가 비면 미적재일 뿐(손대지 않음, 오탐0). 기준 B(여러 베이 같은 위치)는 단일 항차 오탐 위험으로 제외. 검증: EVER CONFORM 40곳 탐지, R9·R10 T2 막힘률 100%(구조적)·R1·R5 14~20%(정상) 정확 구분.
- **EVER CONFORM 구조 규칙(실측 확정)**: 베이 28개(1,2,3,5,6,7,9…38), 풀빔 06,10,14,18,22,26,30,38. **홀드 00 전혀 없음 / 데크 00은 베이별로 있음**(deckHasZero·holdHasZero 분리 필수). 홀드 있는 베이 데크 시작 84 / 선미 데크전용(33,35) 시작 82. 진본 .def는 Define.zip에 없음(동형: SAWASDEE 계열·CNC SATURN).
- **관련 파일**: shipMatchFinder.js, shipMatrixBuilder.js(blockedCells/provisional 직렬화), cargoPlanCore.js buildEmptyBayRenderData, ShipMatrixBuilderModal.jsx(🔁 복제 버튼).
- ⚠ 빌드 주의: `npm run build` 단독 금지 — 반드시 `bash build.sh`. 후 번들에 신규 한글 문자열 grep.

═══════════════════════════════════════════════════════
# Ⅶ부 — 검증 주체 원칙 (절대, 잊지 말 것)
═══════════════════════════════════════════════════════

**AI가 만들고 AI가 검증해야 한다. 사람이 검증자 자리에 앉으면 AI와 인간의 역할이 뒤바뀐 것이다.**
- 실패 패턴: ① 빨리 결론 내려고 끝까지 안 본다 ② 검증을 건너뛴다 ③ 어려우면 "한계"로 포장해 사람에게 떠넘긴다("정답지 필요"는 사실 끝까지 안 본 것). → **"할 수 있는데 안 한다."** 능력이 아니라 주의·끈기의 문제.
- 교정: 느리더라도 **끝까지·한 칸씩·검증까지**가 기본. 결과 후 반드시 스스로 교차검증(emptyPlanVerify 등). 사람은 최종 승인만.
- **경고: 이 원칙이 무너지면 AI와 인간의 역할이 바뀐다.**

═══════════════════════════════════════════════════════
# 부록 A. 디버깅 일지 (2026-05-23, 보관 — 반복 실수 경계)
═══════════════════════════════════════════════════════

핵심 원인: 데이터 구조를 추측으로 짠 것. 실데이터 안 보고 알고리즘 작성.
> "있는 데이터를 안 보고 이렇겠지 하고 만드는 거하고는 많이 틀리죠"

**잘못된 가정 모음**:
- 단일 rowMaxOdd/Even 통일 가능 → 베이마다 다름.
- rowMaxOdd=7 = "7개 row" → "가장 큰 row 번호"일 수 있음.
- has_zero = rowMax % 2 → EDI에 0 있는지 직접.
- v5_matrix.cells 신뢰 → DXQD 부정확. 위→아래 → 사실 아래→위(reverse).
- 45R = 45ft → 40ft hi-cube reefer.
- hull 바깥 강제 active → 단면 깨짐(hull 안쪽만).
- cells 개수 추측 → 실제 row 번호 봐야.
- 인쇄 visibility:hidden → 자리 차지로 밀림(display:none).
- 베이플랜 격자(flex) → 한 칸 빠지면 옆 밀림(좌표 absolute).

**핵심 교훈**: 알고리즘 작성 전 데이터 구조 직접 확인. 단일 값 가정 금지(rowMax vs rowCount 명확히). 사용자가 짚어준 정답을 추측으로 덮지 말 것. 정답 알고리즘은 새로 짜지 말고 그대로 포팅.

═══════════════════════════════════════════════════════
# 부록 B. 운영 팁
═══════════════════════════════════════════════════════

- **"메모" 규칙 (절대)**: 사용자가 "메모"로 시작하는 글을 보내면 **즉시 작업·분석·제안·질문하지 않는다.** 기억만 하고 부록 C에 그대로 옮겨 적은 뒤 "메모 접수"처럼 한 줄로만 응답한다(사용자가 연속으로 메모를 적는 중이라 흐름이 끊기면 안 됨). 사용자가 "작업해줘"(주로 퇴근 전)라고 하면 그때 쌓인 메모를 몰아서 처리한다.
- **문제 누적 규칙 (사용자 지시)**: 작업 중 발견된 문제는 반드시 해결법과 함께 이 지침서에 항시 누적할 것. 완벽하게 구현·검증 완료된 것은 영구 참조 자료(Ⅴ부 REF)로 승격해 다른 Claude가 참고하게 할 것. 추측·미확정은 작업 이력에, 데이터로 PASS 확인된 것만 REF로.
- **세 앱 완전 별개 원칙 (머리말급 — 2026-06-15 확정)**: 검수앱(A)·콘앱(B)·벌크탤리(C)는 코드베이스·배포·점검이 모두 독립이다. 한 앱을 고쳐도 다른 앱에 자동 반영되지 않는다. 이 원칙은 운영 팁이 아니라 **문서 머리말에 명시된 최상위 전제**이며(문서 첫머리 "세 앱을 한 권에, 명확히 분리" 표 참조), 모든 작업은 어느 앱을 건드리는지 먼저 특정하고 시작한다. 특히 콘앱(cone.html)은 검수앱과 버전 번호(V7.xx)를 공유하지만 독립 HTML이라 React 점검과 **반드시 따로 점검**한다.
- **콘앱 이력 분리 기록 규칙 (2026-06-15 확정)**: 콘앱(B) 작업은 앞으로도 **검수앱(A) 작업 이력(Ⅱ-8)과 섞지 말고 Ⅲ부에 별도 기록**한다. 검수앱+콘앱 동시 작업이면 양쪽에 요약하고 "(상대 부 참조)"로 교차 표기한다. 과거 지침서가 7부에 두 앱을 시간순으로 뒤섞어 추적이 어려웠던 것을 이번 통합에서 바로잡았으므로, 같은 혼선을 재발시키지 않는다.
- **단일 지침서 운영 (사용자 확정 2026-06-15 — 절대)**: **이 통합 지침서 한 권만 유지한다. 별도 지침서를 새로 만들지 말 것.** 세 앱이 모두 완성돼 독립 출시될 때까지는 검수앱·콘앱·벌크탤리 전부 이 한 권으로 간다. 과거처럼 `검수앱지침서.md`·`BULK_TALLY_지침서.md`·`REF_*.md`를 따로 분리·생성하지 않는다(따로 된 지침서는 이제 없음). 새 발견·결정·검증 결과는 모두 이 문서의 해당 부(Ⅱ~Ⅵ·Ⅴ부 REF)에 흡수한다. 앱별 분리는 각 앱이 독립한 뒤 사용자가 지시할 때만.
- 한 채팅 = 한 작업 원칙. 앱 개발 채팅과 검수 업무 채팅 분리. 세 앱(검수앱·콘앱·벌크탤리)은 완전 별개 — 따로 점검.
- 이 지침서를 프로젝트 지식에 두면 새 채팅마다 자동 참조. 교체는 하루 1회 그날 마지막 판으로(0-11). 지침서가 둘이면 버전 높은 쪽이 진실.
- 작업 완료 시 이 지침서 갱신본 제공. 세션 종료 시 당일 픽스·결정사항·새 운영 흐름을 지침서 업데이트 항목으로 제안.
- Firebase 베이사전 export 경로: `ship_bay_dict_v3` → `window.__fbShipBayDict`/`localStorage['gm_fb_baydict_cache']`.

═══════════════════════════════════════════════════════
# 부록 C. 메모 수신함 (사용자 메모 — 답변 금지, 기록만)
═══════════════════════════════════════════════════════

> 사용자가 "메모"로 보낸 내용을 날짜와 함께 그대로 적는다. 처리는 사용자가 지시할 때만.

- ~~2026-06-10: ATRP 홀드 19번·(20)21번 같이 계산~~ → V7.61 수정 완료(홀드콘 ×2, Ⅲ부)
- ~~2026-06-10: 트윈 선적 중 끝4자리 같은 게 선택 안 됨~~ → V7.60 수정 완료
- ~~2026-06-10: 콘앱 QR 사용 시 누구는 되고 안 됨(로드 실패)~~ → V7.60 수정 완료

═══════════════════════════════════════════════════════
# 부록 D. 미해결 백로그 (2026-06-15 기준)
═══════════════════════════════════════════════════════

**검수앱(A)**
1. **앱 내 신규 선박 입력 메뉴 + PDF/.def 자동 파싱** (핵심 목표) — 빈 카고플랜 PDF 파서(V7.34)·.def 디코더(V7.36)·복제 파이프라인(Ⅵ-4)이 기반. 통합 메뉴 미완.
2. 끝자리 4자리 조회를 베이상세/3D 하이라이트.
3. ATRP 등 cells 없는 PDF 자동본 — EDI에서 단면 골격 생성해 BayBoxV2 렌더(전 선박 CASPI형 통일).
4. 3D 카고플랜 EDI row↔격자 rowPos 매칭 100% PASS(Ⅴ-A-5) 후 프로토타입.
5. .def 미해결 15척(PCSZ류 중앙부·CNAF/XISH/RZIN 베이테이블 위치).
6. ~~PORT-MIS 날짜 동결 패턴 영구 수정 2종~~ → V8.01에서 해소. VoyagePage 콜사인 후보 최신 updatedAt 우선 정렬로 stale 날짜 회피(시뮬 PASS). fbSavePortMisBatch vesselName 공유 키 물리 삭제는 정상 데이터 유실 위험으로 미채택(정렬 수정으로 실질 해소). 추가로 항차 자동삭제 기준을 createdAt→작업활동으로 바꿔 EDI 미리 투입 항차 보호.
7. 미지원 질문 백로그(V7.54 표): 선사별 집계·미입력 필터·시프팅·통과화물·최대최소·날짜·서수·위험물 클래스.

**콘앱(B)**
8. 로컬 사전 → 서버 일괄 동기화 버튼. 콘앱 텍스트 복사·이미지 저장에 동선표 포함 여부(현장 피드백 후).

**벌크탤리(C)**
9. 펄프/양하 마스터 엑셀 형식 확보 후 parseFile 파서 추가. PWA화(SW+의존성 인라인). 과거 항차 클라우드 복원.
