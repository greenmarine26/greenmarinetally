# 검수앱 V8.24-02 변경 인계 (PORT-MIS 미매칭 — vslFull 오염 가드 수정)

> 소스 변경은 **이미 이 repo에 적용 완료**(VoyagePage.jsx·utils.js·sw.js). 다른 Claude는 **빌드·커밋·푸시만**.

## 증상
OBWH(OCEAN BLUE WHALE) 항차에서 "⚠ PORT-MIS 매칭 미확인 · 콜사인 D5MO4" 경고. 재업로드·베이사전 수정해도 그대로.

## 원인 (실제 Firebase 데이터로 확정 — 추측 아님)
- `voyages/OBWH_2671E/info.callsign = "D5MO4"` (정상), `port_mis_data/D5MO4` 존재(OCEAN BLUE WHALE, 평택) — **둘 다 멀쩡, D5MO4로 일치**.
- 그런데 `voyages/OBWH_2671E/info.vslFull = "CNYNT"` — EDI 자동추출이 **항구코드(CN-YNT, 중국 옌타이)**를 vslFull 자리에 잘못 넣음.
- VoyagePage 624줄 매칭은 콜사인 일치 + 신원가드(`_nameMatchesPm`) 둘 다 통과해야 하는데, 가드가 vslFull "CNYNT" vs port-mis "OCEAN BLUE WHALE"을 비교 → 불일치 → **올바른 D5MO4 매칭을 버림**. 이후 선박명 fallback도 "CNYNT"/"OBWH"로 검색해 전부 실패 → 미매칭.
- 즉 업로드·베이사전 callsign·숨은문자 문제가 아니라, **항차 vslFull 오염** 한 가지.

## 손본 것
1. **src/pages/VoyagePage.jsx** `_nameMatchesPm` (약 618행): vslFull이 신뢰할 만한 선박명이 아니면(공백 없는 코드형·7자 미만) **검증 불가 → 통과**로 처리. 진짜 풀네임(공백 다단어 또는 ≥7자)일 때만 가드 적용.
   - 효과: OBWH(vslFull="CNYNT") → 가드 통과 → D5MO4 정확 매칭 성공.
   - 보존: DJCT 오염콜사인(BSDU→XIN TAI PING)은 vslFull이 "DONGJIN CONFIDENT"(공백 있음) → 가드 그대로 적용되어 차단(기존 V7.30 의도 유지).
   - node 시뮬 6케이스 PASS(OBWH 매칭, DJCT 차단 보존).
2. **src/utils.js** APP_VERSION → `V8.24-02`
3. **public/sw.js** VERSION → `V8.24-02`

## 할 일
```
bash build.sh
git add -A && git commit -m "V8.24-02 PORT-MIS 미매칭 — vslFull 코드형 오염 시 신원가드 우회" && git push
```
빌드 후 OBWH 항차 열면 PORT-MIS 자동 매칭(키 D5MO4)됩니다. vslFull 데이터 교정 없이도 풀립니다.

## 남은 근본(선택)
vslFull에 항구코드(CNYNT)가 박히는 **EDI 자동추출 지점**은 별도 버그. 이번 가드 수정으로 매칭은 더 이상 안 깨지지만, 데이터를 깨끗이 하려면 그 추출부도 손봐야 함(다음 작업 후보).
