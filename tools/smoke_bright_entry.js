// 2.40 밝기 연막검사용 진입점 — utils 와 nlSearch 의 실함수를 CJS 로 내보낸다.
export { applyBrightness, getBrightness, setVolumeStep, currentVolume, runDeviceCmd } from '../src/utils.js';
export { parseNaturalQuery } from '../src/nlSearch.js';
