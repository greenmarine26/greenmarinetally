# M7.22 설계 — 선박 라이브러리 / 선박별 자료 보관소 분리

## 배경
- 기존 "선박 라이브러리"는 ships 노드(과거 누적)만 표시 → 현재 작업 중인 배가 안 보임.
- 사용자 의도: 라이브러리 = **진행 상황**(현재 작업 중), 보관소 = **완료 기록**(항차별 누적).

## 데이터 흐름 (이미 구현됨 — 변경 없음)
완료 저장 버튼 누르면:
1. archive/{voyageKey} 에 실데이터 전체 백업 (fbArchiveVoyageBeforeDelete)
2. ships/{shipId}/voyages/{voyageKey} 에 항차 통계 기록 (fbAddShipVoyage + fbAddShipStats)
3. 백업 성공 확인 후 voyages/{voyageKey} 삭제 (fbDeleteVoyage)

→ 흐름은 그대로. **화면 표시만 분리**한다.

## 화면 1 — 선박 라이브러리 (진행 상황)
- 데이터 출처: **현재 살아있는 voyages** (tallyVoyagesByShip).
- 표시: 선박명 + 양하 진행 + 선적 진행 (평택분 기준).
  - 예: `PACIFIC BEIJING · 양하 85 · 선적 53`
- 베이 구조 / 정밀등록 배지 = **제거** (베이플랜에서 봄).
- 완료(저장) 안 한 배만 보임. 완료하면 여기서 사라짐.
- 과거 누적(ships)만 있고 현재 작업 없는 배(기존 9척)는 **여기 안 나옴**.

## 화면 2 — 선박별 자료 보관소 (완료 기록)
- 데이터 출처: **ships 노드** (완료 시 기록된 voyages).
- 선박별로:
  - 항차별 줄: `항차번호 · 양하 N · 선적 M · 일자`
    - 예: `2608N · 양하 452 · 선적 465 · 2026-06-05`
  - 맨 아래 누적: `누적 항차 X · 누적 양하 ΣD · 누적 선적 ΣL`
- 평택분 기준.
- 일자 = completed_at (완료 누른 시각) 또는 analyzed_at.

## 평택분 기준 (확정)
- 양하 = POD가 평택(PTK/PYT/PYOTM/PYO), 선적 = POL이 평택.
- _isPtk / isPyeongtaekPort 와 동일.

## 변경 범위
- ChiefDashboard.jsx: ShipLibrarySection 을 두 섹션으로.
  - LiveProgressSection (voyages 기준) — 신규/대체.
  - ShipArchiveSection (ships 기준, 항차별+누적) — 신규.
- firebase.js: 데이터 흐름 변경 없음. (조회 함수만 필요시 추가)
- HomePage.jsx: 완료 흐름 변경 없음.

## 확정 — 이중 확인 구조
- 항차 화면(홈): **검수사**가 1차 완료(작업 마무리). 기존 완료 버튼 유지.
  - 단, 여기서 누르면 바로 삭제가 아니라 "검수사 완료" 표시만? 또는 기존대로 완료저장?
  - → 사용자 의도: 항차=검수사, 라이브러리=수석. 이중 확인.
  - 구현: 홈 완료 버튼은 그대로 두되, 최종 archive+삭제는 수석이 라이브러리에서.
    (※ 단순화: 홈 완료 버튼 동작은 변경하지 않고, 라이브러리에 수석용 완료 버튼 추가.
       두 곳 어디서 눌러도 동일한 완료 흐름 실행. 운영상 역할 분담은 사람이.)
- 보관소 정렬: **최근 완료순** (completed_at desc).

## 화면 1 최종 — 선박 라이브러리 (수석 대시보드, 진행 상황)
- voyages 기준. 선박명 + 양하/선적 진행(평택분).
- 각 배에 **[완료 저장]** 버튼 (수석이 최종 확인 후 누름).
  → fbArchiveVoyageBeforeDelete + fbDeleteVoyage (홈 완료와 동일 흐름).
- 베이 구조/정밀등록 배지 제거.

## 화면 2 최종 — 선박별 자료 보관소 (완료 기록)
- ships 노드 기준. 최근 완료순 정렬.
- 선박별 항차 줄 + 누적.
