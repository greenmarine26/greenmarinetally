평택항 검수 & Tallyman Master 통합 지침서

> **이 문서는 이 프로젝트의 모든 작업에 우선 적용되는 규칙이다.**
> 이 프로젝트에서 작업하는 모든 Claude는 시작 전에 이 문서를 먼저 읽고 따른다.

최종 업데이트: 2026-06-06
작성자: 성일 (평택항 검수 / Tallyman Master 개발)
현재 버전: V7.28

구성: **0부 = 범용 코딩 지침**(LLM 공통 실수 방지) · **1~6부 = 검수 업무·앱 도메인 원칙** · **7부 = V7.x 최신 작업 원칙** · **부록 = 디버깅 일지(보관)**.

═══════════════════════════════════════════════════════
0부 — 범용 코딩 지침 (모든 작업의 기본 자세)
═══════════════════════════════════════════════════════

caution over speed. 사소한 작업은 판단껏.

## 0-1. 코딩 전에 생각하라 (Think Before Coding)
가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.
- 가정은 명시적으로 말한다. 불확실하면 묻는다.
- 해석이 여러 개면 제시한다 — 혼자 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 불분명하면 멈춘다. 무엇이 헷갈리는지 짚고 묻는다.

## 0-2. 단순함 우선 (Simplicity First)
문제를 푸는 최소 코드만. 추측성 기능 금지.
- 요청 안 한 기능 추가 금지. 일회용 코드에 추상화 금지.
- 요청 안 한 "유연성·설정 가능성" 금지. 불가능한 시나리오 에러처리 금지.
- 200줄 짠 게 50줄로 되면 다시 쓴다.

## 0-3. 외과적 변경 (Surgical Changes)
건드릴 것만 건드린다. 내가 만든 흔적만 치운다.
- 주변 코드·주석·포맷을 "개선"하지 않는다. 안 깨진 걸 리팩터링하지 않는다.
- 기존 스타일을 따른다. 무관한 죽은 코드는 언급만 하고 지우지 않는다.
- 내 변경이 만든 미사용 import/변수/함수만 제거한다.
- 기준: 모든 변경 줄이 사용자 요청과 직접 연결돼야 한다.

## 0-4. 목표 기반 실행 (Goal-Driven Execution)
성공 기준을 정하고, 검증될 때까지 반복한다.
- "버그 수정" → "버그를 재현하는 테스트를 만들고, 통과시킨다."
- 이 앱에선: **실데이터로 재현 → 고침 → 같은 데이터로 PASS 확인**.

## 0-5. 한국어 출력 시 콜론으로 문장 끝내지 말 것
사용자가 한국어면 출력도 한국어. 문장 끝은 `.` `?` `!` — `:` 아님.
(영어 문서 습관으로 콜론이 새어 나옴. 콜론은 코드·키밸류·라벨에만.)

## 0-6. 새 파일 첫 줄에 한국어 헤더 주석
새 소스 파일을 만들면 첫 줄에 역할을 한 줄 한국어로.
예: `// 검수 완료 항차를 archive에 백업하는 함수`. (설정 파일은 제외.)

## 0-7. 큰 작업은 계획·체크리스트·결정 노트 먼저
비자명한 작업은 코드 전에 설계를 글로 정리하고 확인받는다 (예: 화면 구조 변경).

## 0-8. 완료라고 말하기 전에 테스트 (절대 원칙)
코드를 만졌으면 "완료" 전에 검증한다.
- 이 앱에선 **실데이터 시뮬레이션 PASS + `bash build.sh` 성공 + 빌드된 JS 검증**이 곧 테스트다.
- 이것 없이 "완료"라고 말하지 않는다. LLM이 가장 자주 건너뛰는 단계 — 비협상.

## 0-9. 의미 있는 단위로 커밋
한 논리 변경이 끝나면 버전을 올리고 변경 이유를 기록한다.
- 좋음: "archive 백업 추가". 나쁨: "백업 추가하고 UI도 고치고 버그도 수정"(3개로 쪼갬).

## 0-10. 에러를 읽어라, 추측하지 마라 (절대 원칙)
실제 에러/로그를 읽는다. 기억으로 패턴 매칭하지 않는다.
- "흔한 수정"을 원인 확인 전에 적용하지 않는다. 불분명하면 로그를 찍어 상태를 확인한 뒤 고친다.
- ⚠️ 이 앱에서 반복된 실수: "캐시 문제겠지", "옛 버전이겠지"로 넘겨짚기. 코드를 끝까지 읽고 실데이터로 확인할 것.

═══════════════════════════════════════════════════════
1부 — 검수 업무 (본업)
═══════════════════════════════════════════════════════

## 1.1 정체성
위치: 대한민국 경기도 평택시 (평택항). 역할: 화물 검수 및 선박 양하 작업 관리. 언어: 한국어.

## 1.2 자료 분석 원칙
- 제출 자료(메인플랜·베이플랜·양하 리스트·XRAY 리스트 등)를 빠짐없이 분석할 것.
- 추론이 아닌 실제 데이터만을 기준으로 답변할 것.
- ⚠️ 가정 금지: "이렇겠지" 추측 작업 X. 실데이터 직접 확인.

## 1.3 응답 워크플로
- 컨테이너 끝자리 4자리 호출 → 실번호 + X-RAY 대상 여부 + 선내 위치.
- 컨테이너 개수("20피트 몇 대?") → 규격별(20ft/40ft) × 상태별(Full/Empty) 상세 산출.
- 특수화물 리스트 → 베이플랜 기본, 리퍼(온도)·FR·O/T·DG 등 포함.

## 1.4 보고 형식
명확하고 간결하며 스캐닝이 쉬운 형식. 실데이터 기반 정밀 수치.

═══════════════════════════════════════════════════════
2부 — Tallyman Master 앱
═══════════════════════════════════════════════════════

## 2.1 현재 상태
앱: Tallyman Master (greenmarinetally). 현재 버전: V7.28.
GitHub: greenmarine26/greenmarinetally. Firebase: greenmarinetally (asia-southeast1, Spark).
작업 디렉토리: /home/claude/v2work/ (또는 세션별 작업 폴더).

## 2.2 검증 원칙 (절대 원칙)
- 검증 안 된 앱은 절대 ZIP으로 주지 않는다.
- 빌드 성공 ≠ 검증. 실데이터로 흐름을 끝까지 돌려 OK일 때만 검증 OK.

## 2.3 작업 순서 (절대 건너뛰지 않음)
```
코드 수정 → 실데이터 시뮬레이션 → PASS → bash build.sh → 빌드된 JS 검증 → ZIP
```

## 2.4 빌드 전 체크리스트
- APP_VERSION 갱신 (src/utils.js 한 곳만 — sw.js·cone.html 라벨은 build.sh가 자동 동기화).
- HelpModal 사용법 등록 (새 기능·변경 시).
- 이 지침서 갱신 (작업 완료 시).

## 2.5 버전 표기
큰 변화/기능 추가 = 마이너 버전업. 작은 수정 = 서브 버전. 모든 변경마다 버전 올림.

## 2.6 ZIP 배포 메커니즘
- 누적본 ZIP을 repo 루트에 덮어쓰기 → commit & push.
- 루트 index.html은 빌드본 그대로 (소스형 두면 vite 에러). build.sh가 dist→root로 sw.js·cone.html·manifest까지 복사.
- 캐시 무효화: sw.js는 network-first, VERSION이 바뀌어야 새 버전 인식 + 옛 캐시 삭제.

═══════════════════════════════════════════════════════
3부 — 컨선 베이 구조 도메인
═══════════════════════════════════════════════════════

## 3.1 짝수 단독 베이 (정상 구조)
BOW / STERN / 선원건물 앞 / 선원건물 뒤에서 정상. 한쪽 홀수만 있어도 정상. 짝수 단독을 비정상으로 일반화 금지.

## 3.2 베이별 자체 row 구조 (핵심)
선박 전체 단일 row max 가정 금지. 같은 선박 내에서도 베이/section마다 row 구조 다름.
deck/hold tier도 베이별로 다름 (예: DJCT BAY 27 = deck only). 단일 rowMaxOdd/Even은 부정확.
예: DJCT BAY 01 = rowCount 7, hasZero true / DJCT BAY 07 = rowCount 10, hasZero false / SWAT BAY 17 = rowCount 11, hasZero true.

## 3.3 베이사전 등록 현황
v2 verified 36척, v5 매트릭스 300척. v2/v5 모두 베이별 row 라벨 정보 일부 누락.

## 3.4 PDF override 사전
src/data/shipBayDict_pdf_override.js. 베이별: rowCount, hasZero, deckTiers, holdTiers, (권장) deckCells, holdCells.

## 3.5 ISO 6346 사이즈
첫 자: 2=20ft, 4=40ft (45R1 = 40ft hi-cube reefer), L/9=45ft. ⚠️ 45R1을 45ft로 분류 금지.

## 3.6 cells = 00 포함 개수 (V7.18 확정 — 핵심)
- 사용자 매트릭스 cells = **00 포함 전체 칸 수**. 데크00 체크 = cells 안에 이미 00 포함.
- ⚠️ `+1` 금지 (00 두 번 세면 없는 09가 생김).
- ⚠️ 개수로 row 번호를 생성하지 말 것. 실제 row 라벨을 쓴다. ("갯수 보지 말고 실제 row 번호를 봐라")

═══════════════════════════════════════════════════════
4부 — 카고플랜 V2 약속
═══════════════════════════════════════════════════════

## 4.1 베이 구조
PDF STOWAGE INSTRUCTION 정답 그대로. 베이마다 자체 deck/hold tier + row. 단독 베이 hold 없으면 hold 영역 없음. A4 한 장. 좌측 하단 mini-legend.

## 4.2 마크 약속
(a) PTK 일반 = 선사별 컬러 + o / (b) PTK 특수(R/D/F/T/A/E) = 글자 + 컬러 / (c) 통과 일반 = 회색 배경, 글자 없음 / (d) 통과 특수 = 글자 + 회색 / (e) 빈 슬롯(hull 안쪽) = border만 / (f) 짝수 40·45ft shadow = X(흰 배경) / (g) 짝수 20ft shadow = 회색 빈 셀(자리 차지) / (h) Hull 바깥 = 안 보임(visibility:hidden).
- ⚠️ X는 짝수 40/45ft shadow 전용. 통과화물 표시로 쓰지 말 것.

## 4.3 컬러 시스템
양하 모드: 선사(c.op)별 10색. 선적 모드: POD 3자별. ⚠️ c.cn 첫 3자 BIC 코드 = 선사 X. 사용 금지.

## 4.4 Hold 중앙 정렬
CSS: width (nHoldCols/nDeckCols)*100% + margin auto. 1칸 차이 → 좌우 0.5칸씩.

## 4.5 Layout 공식
N개 박스 → 상단 = ⌈(N+1)/2⌉, 하단 = N − 상단, 별첨 자리 = 차이.

## 4.6 카운트 포맷
단독 = 총합 1숫자. 페어 = 20'/40'/45' 슬래시. 양하 = 평택분만.

## 4.7 인쇄 처리
height 195mm 고정. page-break-inside: avoid. body 직접 마운트(createPortal). display:none + bg white.

═══════════════════════════════════════════════════════
5부 — 데이터 검증·명칭 원칙
═══════════════════════════════════════════════════════

## 5.1 출처 우선순위
PDF override > v5_matrix.matrixBays > v2 baysSummary > EDI 실데이터.

## 5.2 v5_matrix.cells 주의
순서 아래→위 → reverse() 필수. v5가 PDF와 불일치 가능. 일부 선박 v5 없음.

## 5.3 EDI 검증
has_zero = EDI rows에 0 있는지 직접. 추측 금지, 실데이터 보고 판단.

## 5.4 CASPI가 정답 기준
CASPI를 따른다. 잘못된 명칭 사용 금지. 사용자가 실제 쓴 약자·명칭만 인용.

## 5.5 선사 약자 3자 (영구 규칙)
선사 약자는 항상 3자. 4자 코드는 앞 3자만.
예: DJSC→DJS, NSSL→NSL, HASL→HAS, SNKO→SKR, EASK→EAS, TJMS→TJM, WDFC→WDF, DWIC→DWS.

═══════════════════════════════════════════════════════
6부 — userBayDict / 매트릭스 보호 (절대 원칙)
═══════════════════════════════════════════════════════

- **userBayDict 절대 보호**: source !== 'user'일 때만 mergeBayDef. 사용자 입력을 자동분석(v5/v2/EDI)이 덮어쓰지 않는다.
- **베이매트릭스가 기본 진실**: 매트릭스에 deck00/hold00이 명시되면 EDI가 덮어쓰지 않는다.
- **Rules of Hooks**: 모든 Hook은 최상단. 조기 반환문은 모든 Hook 선언 이후.
- **음성 출력 표준**: 컨테이너 번호는 한 글자씩, 베이·온도는 한글로 또박또박.

═══════════════════════════════════════════════════════
7부 — V7.x 최신 작업 원칙 (M6.91 이후 추가)
═══════════════════════════════════════════════════════

## 7.1 평택분만 계산
- 항상 평택분만: 양하 = POD가 평택, 선적 = POL이 평택.
- 평택 판정 변종 포함: `PTK / PYT / PYOTM / PYO`로 끝나면 평택 (KRPTK·KRPYO·KRPYOTM 등). **isPyeongtaekPort() 단일 함수로 통일** (V7.27: 코드에 isPtkPort는 없음. 적재·집계·모드판정·진단 전 화면이 이 함수 하나만 사용. firebase 한글 항구명 케이스만 예외).
- ⚠️ 폴백 금지: 평택분 0이면 0이 정답. 전체(rowsAll)로 넘어가 타지역까지 세지 않는다.

## 7.2 데이터 유실 방지 (archive)
- 완료 시 `archive/{voyageKey}`에 실데이터 전체 백업 → 백업 검증 성공 후에만 삭제(fbDeleteVoyage). 백업 실패 시 삭제하지 않는다.
- 복원: fbRestoreVoyageFromArchive. 1년 후 자동 정리: fbCleanupArchive.

## 7.3 이중 확인 (검수사 → 수석)
- 검수사가 항차 화면에서 완료 누르면 `inspectorDone` 표시만 (삭제 안 함). 수석 확인 전 검수사 취소 가능.
- 수석이 대시보드 진행 상황에서 최종 "완료 저장" 누를 때만 archive 백업 + 보관소 기록 + 삭제.
- 목적: 2명이 나눠 작업 중 한 명이 자기 자리 완료를 전체 완료로 잘못 눌러 삭제되는 사고 방지.

## 7.4 라이브러리 / 보관소 분리
- 선박 라이브러리(진행 상황) = 현재 살아있는 voyages만. 완료 안 한 배만 보임.
- 선박별 자료 보관소(완료 기록) = ships 노드, 최근 완료순. 항차별(양하/선적/일자) + 누적.
- 보관소 제목은 사용자 약자(vsl, 예 PCBJ/TNJP) 우선.

## 7.5 계열 대체 시 신원 금지
- 베이 구조 없어 다른 선박 사전을 빌릴 때(series-substitute), 베이 구조만 빌리고 콜사인·선박명은 빌리지 않는다.
- ⚠️ 앞 2글자만 같으면 계열로 보는 대체는 느슨함. 무관한 선박의 신원(콜사인)이 섞이면 PORT-MIS 오매칭 발생 (DJCT에 XIN TAI PING 표시된 버그).

## 7.6 cone 앱 (콘 계산기)
- 검수앱 EDI 연동(fbFetchEdiContainers)도 평택분만 필터. 양하=POD평택, 선적=POL평택.
- 화면 버전 라벨은 build.sh가 APP_VERSION과 자동 동기화 (신·구버전 구분용).
- cone은 독립 HTML(`public/cone.html`). build.sh가 dist·루트로 복사 + 버전 라벨 동기화. **본체와 별개 앱이므로 React 점검과 별도로 cone.html도 따로 점검할 것.**
- **V7.28: cone 베이뷰 평택 판정 통일.** 메인 isPtkPort()는 정식 기준(`/(PTK|PYT|PYOTM|PYO)$/`)인데, 베이뷰 인라인 isPtk 2곳이 `/PTK|PYT|PYOTM/.test(p)||p===''`로 달라 PYO 누락 + 끝고정 없음 + 빈값을 평택 오인 → 콘 계산값과 베이뷰 그림 불일치. 베이뷰 2곳을 메인 isPtkPort() 호출로 통일. (본체 ①번과 같은 종류. cone 안에서도 판정이 흩어져 있었음.)

## 7.7 V7.27 코드 정합성 점검 (2026-06-06) — "곳곳에서 안 맞음" 6건 일괄 수정
전수 점검에서 발견한 정합성 문제 6건. 모두 실데이터 시뮬레이션 PASS 후 build.sh 빌드 검증.
- **① 평택 판정 통일**: 같은 함수가 화면마다 따로 구현돼 평택 표기(KRPYOTM 등)를 일부만 인정 → 카고플랜엔 보이는데 카운트엔 빠지는 불일치. isPyeongtaekPort() 단일 함수로 8파일 14지점 통일. (firebase 한글 항구명 케이스만 예외.)
- **② 양하 선사 색 3자**: 컬러키가 op를 trim만 해 DJSC/DJS 갈라짐. utils.normalizeCarrierCode 신설 — 변환표 경유 3자. ⚠️ 단순 slice(0,3) 금지(SNKO→SNK 틀림, 정답 SKR).
- **④ 베이매트릭스 확정 배지**: BayDictStatusWidget 검증 배지 옆에 매트릭스 확정 여부 표시. tier가 매트릭스(user/override) 확정이면 "📐 매트릭스 확정", 미확정 베이 있으면 "⚠️ 베이매트릭스 확정 필요"(EDI 추정). 펼치면 미확정 베이 목록.
- **⑥ V1 통과화물 X 제거**: 구버전 카고플랜이 통과화물을 X로 표시 → shadow X와 구분 불가(4.2 위반). V2와 동일 회색 배경으로. X는 짝수 shadow 전용. V1은 V2 안전망이라 유지.
- **⑦ 리퍼·특수 판정 통일**: V2 셀 마크는 isReeferContainer, 카운트는 iso[2]===R 약식 → 그림·카운트 불일치. 카운트도 셀 마크 기준(isReeferContainer + c.fr/tk/ot/oog)으로 통일.
- **⑧ PORT-MIS 오매칭 fix (DJCT에 XIN TAI PING)**: 계열 대체 시 최상위 callsign만 비우고 bayDef 내부는 안 비워, PORT-MIS가 bayDef.callsign으로 폴백해 빌려온 선박 콜사인(BSDU)을 집어옴. V7.26 반쪽 수정 보완 — 계열 대체 시 bayDef의 callsign·name·vesselName도 제거. (7.5 보강.)
- **⑤ 45R1=40RF 표시**: 메인 isoToLabel이 이미 정확. 45피트 리퍼 없음(있으면 L5). 보류.
- **빌드 교훈**: 루트 index.html은 빌드본(고정 assets 참조)이라 `npm run build` 직접 실행 시 src 변경이 반영 안 됨. **반드시 `bash build.sh`** — 빌드 직전 소스형 진입점(_index.entry.html)을 루트로 임시 복사 후 빌드, 끝나면 빌드본 복원. (지침 2.3 재확인.)

═══════════════════════════════════════════════════════
부록 A. 2026-05-23 디버깅 일지 (보관 — 반복 실수 경계용)
═══════════════════════════════════════════════════════

핵심 원인: 데이터 구조를 추측으로 짠 것. 실데이터 안 보고 알고리즘 작성.
> "있는 데이터를 안 보고 이렇겠지 하고 만드는 거하고는 많이 틀리죠"

단계별 진단: ①통과화물 X 표시 → shadow X와 혼동 / ②인쇄 시 앱화면 → createPortal+@media print / ③검정 바탕 → bg white 강제 / ④좌우대칭 → M6.81 정답 알고리즘 포팅 / ⑤홀드 중심정렬 → 자체폭+CSS center / ⑥데크 빈곳 → shadow X 40/45ft만 / ⑦20ft 충돌 → 짝수 shadow 회색 빈셀 / ⑧1칸 차이 → nHoldCols/nDeckCols 비율 / ⑨분석 그만 사전 → v2 직접 조회 / ⑩00 유무 → EDI rows로 has_zero / ⑪DXQD 홀드 08 없음 → rowMaxOdd/Even 분리 / ⑫데크 08 사라짐 → cells reverse 복원 / ⑬45R=45ft 잘못 → ISO 6346 첫자 / ⑭사용못하는 셀 → hull 안쪽만 active / ⑮사전 잘못 → v2 베이별 row 누락 / ⑯PDF override 신설.

잘못된 가정 모음:
- 단일 rowMaxOdd/Even 통일 가능 → 베이마다 다름.
- rowMaxOdd=7 = "7개 row" → "가장 큰 row 번호" 의미일 수 있음.
- has_zero = rowMax % 2 → EDI에 0 있는지 직접.
- v5_matrix.cells 신뢰 → DXQD 부정확. 위→아래 → 사실 아래→위(reverse).
- 45R = 45ft → 40ft hi-cube reefer.
- hull 바깥 강제 active → 단면 깨짐.
- cells 개수 추측 → 실제 row 번호 봐야.
- 인쇄 visibility hidden → 자리 차지로 밀림 (display:none 정답).

핵심 교훈:
- 알고리즘 작성 전 데이터 구조 직접 확인.
- 단일 값 가정 금지. 변수명 명확히 (rowMax vs rowCount).
- 사용자가 짚어준 정답을 추측으로 덮지 말 것.
- 빌드 전 실데이터로 검증. 정답 알고리즘은 새로 짜지 말고 그대로 포팅.

═══════════════════════════════════════════════════════
부록 B. 운영 팁
═══════════════════════════════════════════════════════

- 한 채팅 = 한 작업 원칙.
- 앱 개발 채팅과 검수 업무 채팅 분리.
- 이 지침서를 프로젝트 지식에 두면 새 채팅마다 자동 참조.
- 작업 완료 시 이 지침서 갱신본 제공.
