# ★ 클로드 질문 답지 — 묻기 전에 여기서 찾는다

> **이 문서의 목적**: 검수사님이 지난 몇 달간 **여러 클로드에게 같은 질문을 반복해서 답해 주신 것**을 한곳에 모은 답지다.
> 세션 84개(실제 대화 37개)를 훑어 뽑았다. 작성 2026-08-01.
>
> **사용법 — 이것만 지키면 된다.**
> 1. 질문을 하기 전에 **이 문서에서 먼저 찾는다.** 여기 있으면 묻지 말고 그대로 한다.
> 2. 여기에도 없고 데이터로도 알 수 없으면 그때 묻는다.
> 3. 새로 답을 받으면 **이 문서에 추가한다.** 추가하지 않으면 다음 클로드가 또 묻는다.
>
> 관련 문서: 작업 절차 = `평택항_검수_통합지침서_3앱통합본_V*.md`(7관문) · 배포 = `DEPLOY.md` / `CLAUDE_SHARED\★배포_표준절차_모든클로드_필독.md`

---

## 0. 가장 자주 반복된 질문 — 이건 절대 묻지 마라

| 클로드가 자꾸 묻는 것 | 정답 (이미 여러 번 답하셨다) |
|---|---|
| "이제 `git push` 하시면 됩니다 / ZIP 받아서 덮어써 주세요" | **배포는 클로드가 끝까지 직접 한다.** "배포도 직접하는걸로 바뀐지 오래 되었습니다." |
| "제가 지울까요, 안내만 드릴까요?" | **직접 한다.** "아까도 직접 하셨습니다. 제컴을 조정해서" |
| "이 버튼 한 번만 눌러주시겠습니까?" | **직접 한다.** "그것도 직접하세요 어제도 했었으니까요" |
| "①부터 갈까요 ②부터 갈까요?" | **목적을 먼저 이해하고 스스로 정해 통보한다.** "질문을 하기전에 먼저 생각 해주세요. 이과정이 왜 필요 했는지와…" |
| "여기서 멈출까요?" | **끝까지 한다.** "끝까지 해보시고 나중에 한번에 하시죠" |
| "다음 세션에 하시겠습니까?" | **그날 끝낸다.** "간단 하다면 그건 지금 해주셨으면 좋겠습니다." |
| "컨텍스트가 길어져서…" | 핑계 금지. "이번세션 이제 4% 사용 했는데 길어진건가요?" |
| "권한이 막혀서 못 합니다" | 승인 절차를 밟아 직접 한다. "권한을 제가 줘도 안된다는건가요?" |
| "환경 문제로 안 됩니다" | "여태 다른 클로드들은 다 했는데 안된다는게 이해가 안되요" |
| 항차 기준이 뭔지 | **선석배정 목록.** "PORT-MIS는 항차랑 전혀 상관없습니다." |
| 이 배가 우리 관할인지 | **터미널 선석배정표 + 항로 칸.** 아래 1-2 참조. |

> **한 줄 원칙**: 물어야 할 것은 **검수사님만 답할 수 있고, 그 답이 내 다음 행동을 바꾸는 것**뿐이다.
> 되돌릴 수 없는 행위(삭제·발송·과금·외부 게시)만 확인받는다. 나머지는 보수적으로 정해 진행하고 **"이렇게 했습니다, 다르면 말씀 주세요"** 로 보고한다.

---

## 1. 업무 규칙 (도메인) — 검수사님이 정하신 기준

### 1-1. 항차·기항

- **항차의 진실은 선석배정 목록이다.** PORT-MIS는 항차와 무관하다. 표기(0533E vs 533E)도 배정표 표기가 정본이다.
- **한 기항 = 한 키.** 양하(E/N) + 선적(W/S)은 한 항차 카드로 묶인다. 같은 기항이 두 카드로 갈라지면 버그다.
- 짝 규칙은 선박마다 다르다 — **동일 번호형**(ATPR 2635E/2635W, RZOR R081E/R081W)과 **nE+(n+1)W형**(OBWH 2701E/2702W, DJCT 0220E/0221W, YKTD 2612E/2613W)이 있다. **추론하지 말고 배정표·마감텔리 제목으로 확인한다.**
- 배가 출항하면 **다음 기항이 배정표에 뜨고 수집기가 예정으로 미리 가등록한다.** 홈에 '예정'이 보이는 것은 정상이다.
- 지난 항차는 지운다. "지난건 지워 주세요"

### 1-2. 관할 판정 — 선박명이 아니라 **항로**

- 터미널 선석배정표에 있는 배만 우리 관할이다.
- **항로가 PXS·PQS·JWKP 등이면 그 주는 우리 메일로 오지 않는다.** 같은 배라도 주마다 항로가 바뀐다.
  > "그런 선박이 2-3척 됩니다 항상 항로를 보아야 합니다"
- 관할이 아닌 배를 신호·부족 목록에 올리지 않는다.

### 1-3. 카운트

- **평택분만 센다.** 양하 = POD 평택, 선적 = POL 평택. 컨번호 끝자리 조회만 예외(제한 없음).
- 집계 답변은 **반드시 구분해서** 한다. "20피트 몇 대?" → **20 풀 n대 / 20 엠티 n대**. 리퍼 n대. XRAY n대 + **XRAY 대상 위치**.
- 컨번호 끝 4자리를 부르시면 → **실번호(전체) + XRAY 대상 여부 + 선내 위치** 세 가지를 답한다.
- 카고플랜은 **베이 그림 · 별첨표 · 베이별 카운트가 서로 일치**해야 한다.
- 부족분이 음수면 미완으로 잡지 않는다. "완성이라고 녹색을 표시하고 부족하다고 또 표시되는건" 모순이다.

### 1-4. EDI·자료 우선순위

- **확정(정식) EDI가 없으면 수치 불일치를 판단하지 않는다.** 확정 EDI가 온 뒤 결정한다.
- 파일명 개정 마커 서열: **최종/FINAL > REVISED/RE)/REVISE/수정/개정 > n차·끝자리 숫자 > 무표시.** mtime은 믿지 않는다(수집기가 매 사이클 다시 저장한다).
- **선박이 다른 EDI는 경고와 함께 차단**한다. 적용하지 않는다.
- **항차가 다른 EDI도 차단**한다. 단 양하 EDI에 다른 항차수가 왔을 때는 **평택분 양하가 있는지 확인**하고, 있으면 "파일명을 바꿔 재등록"하도록 유도한다.
- **완료일자 이전에 온 자료는 완료 선박이라도 수집한다.**
- 검수사님은 **모든 파일명 끝에 선사명을 표기**하신다. 선사별로 파일명 패턴이 겹친다(SKR·HAS·HSL 동일, 남성·동영 동일) — 파일명만으로 선사를 가르지 말 것.
- 파일 내용의 선박코드(`$604` 헤더 등)가 진실이고, 폴더명은 헤더가 빈 경우의 폴백이다.
- **EDI는 확장자로 판별하지 않는다.** DXQD·STMJ는 EDI가 `.txt`로 온다(실측 7파일/2척). "EDI가 없다"고
  말하기 전에 **내용**을 봐라(`UNB+/UNH+/BGM+/TDT+/EQD+CN`). 2026-08-02 사용자 지적으로 발각 —
  클로드가 `.edi/.asc`만 스캔해 "DXQD는 EDI 없음"이라 오판했다.
- **세그먼트 순서도 선사마다 다르다.** LOC-first(`LOC+9/LOC+11`이 `EQD+CN` 앞)와 EQD-first가 공존한다.
  파서는 **블록 단위**로 모아 다음 `EQD+CN`에서 확정해야 한다. EQD-first 전제로 짜면 매 파일 1대씩 밀린다.

### 1-4B. TMPZ 플랜 = 가상 EDI 원료 (사용자 확정 2026-07-11, 인계서 106~110행)

**묻지 마라. 답은 이것이다.**

- TMPZ(TIAN HAI PING ZE)는 **선적 EDI가 오지 않는다.** 두우 프리스토우 플랜(`TIANHAI PINGZE.xls`
  격자 엑셀) + NOLIST만 온다 -> **가상 EDI 대상.**
- 확답 4건: (1)격자까지 파싱(위치 포함) (2)마커 `e`=20MT·`2`=20F·`E`=40HQ MT·`4`=40HQ F
  (3)평택 선적분 = SHANGHAI(CNSHA) + BUSAN(KRPUS) 시트 (4)**자리만 등록(`__SLOT_` 키,
  컨번호 임의 배정 금지 — 컨번호는 NOLIST 리스트 담당).**
- **"합본 대체 보호"의 뜻**: *업로드* 경로에서 LOADLIST가 낱개 엑셀을 대체할 때 **플랜은 예외**로
  살린다는 뜻이다. **merge(합본)에 넣으라는 뜻이 아니다** — 플랜엔 컨번호가 없어 합본 재료가 아니고,
  가상 EDI는 업로드 경로 `autoreg_helper.parsePlanGrid`가 만든다.
- 플랜 파일명엔 **항차 토큰이 없는 게 정상**이다("TIANHAI PINGZE.xls" = 선명만). 그래서
  '엑셀=일치 토큰 필수' 규칙의 예외다(`fileselect._PLAN_HINTS`).
- 교집합 게이트는 플랜을 **배제할 수 없다**(컨번호 0 -> `not c` -> 통과). 구조적으로 안전하다.
- 실측 2026-08-02: 2023W는 LOADLIST가 있는데도 플랜 채택 · 라이브 `TMPZ_2023E/loading` 370자리.
  **규칙대로 작동 중 — 손대지 마라.**

### 1-4A. ★★ 작업일시(planDate)의 순위 — **2026-08-02 최종 확정. 옛 규칙을 덮는다**

> ⛔ **폐기됨**: V9.35의 "작업일시의 진실은 선석배정이다. 도선만 해놓고 대기하는 경우도 있으니까요."
> 그 확답은 **2026-08-02에 사용자가 직접 뒤집었다.** 지침서·코드에 남은 옛 문장을 근거로 되돌리지 말 것.

```
(1) 도선 예보      (2) 선사 일정 메일      (3) PORT-MIS 신고      (4) 선석배정
```

**왜 도선이 1순위인가.** 배의 **실제 움직임**을 가장 먼저 반영한다. 사용자 설명 그대로:
"선박이 조금 일찍 도착해서 작업시작이 빨라지고 더불어 출항시간이 빨라집니다. 도선시간과 메일이 맞아 떨어지죠."

**부두별 갱신 속도 (사용자 확정)**
- **PNCT** — "닥치지 않는 한 배정목록이 거의 바뀌지 않는 단점" -> 배정이 낡을 수 있다
- **PCTC** — "거의 실시간 적용"
  ※ 다만 최종 규칙에서는 **부두로 가르지 않는다.** 도선이 있으면 부두와 무관하게 도선을 쓴다(단순화).

**★ 자력선 조건 (사용자 확정 2026-08-02)**
"자력선입니다. 도선예보에 올라오지 않는 선박 반드시 메일로 확인해야 합니다. 안전장치는 포트미스 입니다."
- 자력선은 도선을 안 쓰므로 **예보에 아예 안 뜬다** -> (1)이 통째로 비고 **메일이 사실상 1순위**가 된다.
- 메일마저 없으면 **PORT-MIS 입출항 신고**가 받친다. 신고는 자력선도 반드시 하기 때문이다.

**⚠ PORT-MIS 를 쓸 때 반드시 걸러야 하는 것 — 지난 기항**
`port_mis_data` 는 **콜사인당 레코드가 하나**라 옛 신고가 그대로 남는다. 거르지 않으면 8월 항차에
7월 날짜가 박힌다(실측 2026-08-02: MCAT·NSDC 07-05, STMJ·SWSP 07-21 — 파급검증이 잡았다).
-> **출항(etd)이 아직 안 지난 신고만** 이번 기항으로 본다. 앱에서도 같은 사고가 있었다(V9.34).

**실측 근거 (TMPZ 2023E, 2026-08-02)**

| 소스 | 입항/작업시작 | 출항 | 비고 |
|---|---|---|---|
| 도선 | 18:30 | **16:00** | 도선점 도착(POB) |
| 메일 | ETA/POB 18:30 · **ETB 20:30** | **16:00** | **작업시작(EST COMMENCE)은 메일에만 있다** |
| 배정(PNCT) | 21:30 | 12:00 | 옛 계획 — 넷 중 혼자 다르다 |

도선과 메일이 **소수점까지 일치**했고 배정만 4시간 어긋났다. 이것이 순위를 뒤집은 실측이다.

**구현 위치**: 수집기 `collector/autoreg.py: decide_plan()` **한 곳**. 결과 출처를 `info.planSrc`
(`pilot|mail|portmis|plan`)에 남기고 앱 카드가 `도선/메일/신고/배정` 으로 표시한다.

### 1-4F. 선사 일정 메일 — **가장 완전한 자료인데 서식이 선사마다 다르다** (2026-08-02)

일정·마감·MRN이 한 메일에 다 온다. 특히 **작업 시작 시각은 여기에만 있다.**

```
***ETA IPPADO P/S    : 1830LT/02/AUG/2026     <- 도선점 도착
***ETB(MBM-15/PNCT)  : 2030LT/02/AUG/2026     <- 접안
***EST COMMENCE DISCH: 2030LT/02/AUG/2026     <- 작업 시작 (메일에만 있음)
***ETD PYEONGTAEK    : 1600LT/03/AUG/2026     <- 출항
***CDL/CLL CLOSING TIME · MRN NO.
```

- **제목 키워드**: `VESSEL MOVEMENT` · `VSL MOVEMENT` · `BERTHING SCHEDULE` · `BERTHING SCDL`
  (`(REVISED)` 가 붙어 갱신본이 온다 — 새 메일이 파일을 덮으므로 자동 반영)
- ⚠ **같은 메일에 다음 항·이전 항 일정이 섞여 있다**: `DEP SHANGHAI`(이전 항 출발),
  `ETA NINGBO`(다음 항 도착). 라벨만 보고 잡으면 엉뚱한 항의 시각이 들어간다 -> **평택 것만** 골라야 한다.
- ⚠ **서식이 선사마다 다르다.** 두우/TJM은 `2030LT/02/AUG/2026`, 코리아해운(ATPR)은
  `ETA PTK : 03RD AUG`처럼 **날짜만 있고 시각이 없다.** 지금 파서는 두우/TJM 형식만 읽는다.
  다른 선사 서식은 **실물을 보고** 추가해야 한다 — 추측으로 정규식을 늘리지 말 것.
- 구현: `collector/auto.py: parse_schedule_mail()` -> `{voy}_일정.json` -> `autoreg._mail_plan()`.
  **수집 조건(collect_week 화이트리스트·밴)은 건드리지 않았다** — 리퍼드라이(v2.17.17)와 같이
  *이미 여는 메일의 본문만* 본다. 그곳은 손댈 때마다 오수집·미수집 사고가 났다(7월에만 3건).

### 1-4C. 출항 배지 — 퍼센트가 아니라 **남은 개수** (사용자 확답 2026-08-02)

- ⛔ **진행률 %는 틀린 자다.** "600개의 10%는 60개, 2갱이면 한 시간 이상 / 100개의 10%면 10개,
  2갱이면 5분이면 끝남." 같은 10%가 한 시간도 5분도 되므로 준비 시간 확보에 못 쓴다.
- **처리 속도는 갱당 시간당 20개.**
- ⚠ **갱수로 나누지 마라.** 마무리 단계에는 갱이 하나씩 빠지고 **마지막 한 갱**이 남은 걸 처리한다.
  그래서 4갱이라고 40개를 10분에 끝내지 않는다. -> **잔여 20개 = 약 1시간**(마지막 갱 기준).
- **기준: 선적 잔여 <= 20개 -> 출항 배지.** 양하는 먼저 끝나므로 출항 시점을 정하는 건 선적이다.
  선적이 없는 항차(ATPR 2635E 등)는 양하 잔여로 본다.
- **터미널이 DEPARTED면 잔여와 무관하게 출항.** 검수 완료 버튼을 미처 못 눌러 잔여가 남아도 넘어간다.
- **한 번 뜨면 유지.** 자료가 늦게 와 잔여가 다시 늘어도 작업일시로 되돌아가지 않는다(깜빡임 방지).

### 1-4D. ★ 참조는 참조일 뿐 — **검수앱이 1순위** (사용자 지적 2026-08-02)

> "클로드의 단점 중에 하나가 뭘 참조하라고 하면 그곳에 집중합니다. 참조는 참조일 뿐입니다."
> "검수앱이 잘 돌아가면 그걸 볼 필요가 없습니다. 검수앱에 실시간 작업중이니 잔여분도 바로 볼 수가 있으니까요."

- 표기의 **최우선 근거는 검수앱 자신의 데이터**다(잔여 = 총 − completed). 검수 중이면 이게 실시간이다.
- 터미널(트레드링스 pct · PNCT 잔여량 · PCTC 갱수)은 **검수앱이 모를 때 쓰는 폴백**이지 기준이 아니다.
- ⛔ **수집기 하나에 다 몰아넣지 마라.** "그걸 굳이 수집기가 할 필요가 있습니까? 수집기 하나로 다
  할려고 하죠?" 앱이 실시간으로 아는 것을 수집기가 5~20분 뒤에 다시 계산해 돌려주면 **멀쩡한 값을
  일부러 늦추는 것**이다. 수집기는 **앱이 모르는 재료**만 넣어준다.
- 개수를 세는 것 자체가 목적이 아니다 — **언제 출항 배지를 붙일지 정하려는 것**이다. 목적을 잊지 말 것.

### 1-4E. 터미널 화면 — 어디서 무엇을 보는가 (2026-08-02 실측)

| 화면 | 주는 것 |
|---|---|
| PCTC 선석배정 목록/차트 | 갱수 `(2G)(4G)` · 계획 수량(양하/선적/환적) · 상태(tr 클래스 plan/work/**done**) |
| PNCT 선석 스케줄(텍스트) | 접안·출항 예정 · **상태 PLANNED/DEPARTED** |
| PNCT 본선 작업 현황 | **QC별 총작업량·완료량·잔여량** · QC 대수 · **접안 방향(우현/좌현)** |
| 트레드링스(terminal_work) | `pct` 하나 — 97%에서 정체하곤 한다 |

- **부두가 갈린다**: PCTC 배는 PCTC 목록, PNCT 배는 PNCT 목록에 있다. 둘 다 봐야 전 항차가 커버된다.
- 트레드링스가 97%인데 **PCTC·PNCT는 이미 완료 처리**한 경우가 많다 — 지연·완료는 작업현황을 봐야 한다.
- **접안 방향(우현/좌현)은 PNCT 본선작업 현황에 이미 나온다.** 검수앱은 이 값을 유도 작업
  (`GuidedWorkPanel`: 우현 접안 = 짝수 로우가 해상쪽)에 쓰는데 **지금은 검수사님이 손으로 고르신다** —
  수집기가 넣어주면 자동이 된다(미구현, 다음 판 후보).

### 1-4G. ★ 로그인 이름 잠금 — 누가 잠기고 누가 안 잠기는가 (사용자 확정 2026-08-02, V9.45)

> "이제 로그인시 타인의 이름으로 못들어 가게 해야 합니다."  ·  (범위) "수석검수·부수석까지"
> (초기 비번) "1번인데 전 전 접근이 가능해야 합니다" = **본인이 정하되, 소유자는 전(全) 접근**

| 구분 | 로그인 |
|---|---|
| 관리자(admin_guard/admins) | 비밀번호 + 신뢰 기기 3대 — V9.05부터 |
| **수석검수·부수석 6명**(이현규·김명보·오승택·성창모·이강익·전우수) | **V9.45부터 동일하게 잠김** |
| 일반 검수원 22명 | 종전대로 이름만 고르면 통과 (현장 속도 우선) |

- 판정 기준은 `staffList.isChief()` = 직책에 `수석검수|부수석` — **명단이 곧 권한**이다. 직책을
  고치면 잠금 범위도 같이 바뀐다.
- ⛔ **수석 비밀번호를 `admins/` 아래 저장하지 마라.** `getAdminNames()`가 admins의 키를 그대로
  관리자로 읽는다 — 비번을 저장하는 순간 그 사람에게 관리자 권한이 딸려 붙는다.
  그래서 노드를 나눴다: 관리자 `admin_guard/admins/{이름}` · 그 외 잠금 대상 `admin_guard/locks/{이름}`.
- **첫 로그인 때 본인이 정한다.** 아무도 대신 정해주지 않으므로 기존 비번을 알려줄 일이 없다.
  그 기기가 신뢰 기기 1호가 되고, 이후 그 기기에서는 비번 없이 선택된다(최대 3대).
- **잊었을 때의 유일한 출구는 소유자(김성일)다.** 게이트 아래 "비밀번호를 잊으셨나요?"를 누르고
  소유자 비번을 넣으면 **이번 접속만** 열린다(남의 기기를 소유자 신뢰 기기로 등록하지 않는다).
  그 자리에서 "초기화" 확인을 누르면 그 사람 비번·신뢰기기가 지워져 다음에 본인이 새로 정한다.
- V9.44(수석 대시보드 진입 차단)와 **짝이다.** 화면만 막고 이름을 안 잠그면 수석 이름으로
  들어와 그대로 열린다 — 문을 잠그고 창문을 열어둔 꼴이 된다.
- ⚠️ 아직 **화면 가드**다. Firebase 보안 규칙으로 실제 읽기·쓰기 권한을 나누는 일은 미착수.

### 1-4H. ★ RZOR 덱플랜(카고플랜) — 어디서 오고 왜 놓쳤나 (2026-08-02 실측)

**RZOR의 EDI에는 선내 위치가 아예 없다.** IFCSUM 매니페스트라 `bay`·`row`·`tier`가 전부 빈칸이다
(R082E 191건 전수 확인). 그래서 **선사 덱플랜이 카고플랜의 유일한 원천**이다 — 이게 없으면 LOLO 리스트뿐이다.

| | 내용 |
|---|---|
| 보내는 곳 | 外理装拆箱 `wailizcx@163.com` (리자오동방 선사) |
| 파일 | `rzdf_ship_<타임스탬프>.xls` · 정정본은 `- 수정` 붙음 |
| 같은 내용 PDF | 제목 `日照东方船图 R082E` · 첨부 `2甲.PDF`/`3甲.PDF`/`4甲.PDF` (甲=갑판) |
| 시트 | `A~E-DECK-PTK` · 컨 1대 = 병합 블록(컨번호↵무게↵`40 HC F`) |
| 자체 정답지 | 시트 안 **SUB TOTAL** 행(20'/40'/45'/TTL) — 파싱 결과를 이걸로 대조한다 |
| 색 의미 | 메일 제목에 적힘 — 노랑=双背, 초록=落地 |

**왜 수집기가 못 잡았나 (원인 2겹, v2.17.41에서 수리)**

1. rzdf 메일은 **제목·파일명 어디에도 항차가 없다**(`rzdf_ship_1785641434834 黄色为双背…`).
   R081E 때는 같은 메일에 `CNTR_List_R081E`가 함께 붙어 항차가 잡혔을 뿐, 단독으로 오면 미아가 된다.
   → 파일 **내용**에 `R082E`가 있으므로 `_voy_in_excel()`이 읽어야 했는데,
2. 그 함수가 **확장자로 형식을 판단**했다. rzdf는 **이름만 `.xls`이고 내용은 xlsx(zip)** 이라
   바이트를 훑어봐야 압축 덩어리라 아무것도 안 나왔다. (openpyxl도 경로를 그대로 주면
   파일명 확장자만 보고 거부한다 — **바이트로 읽어 BytesIO로 넘겨야** 열린다.)

**교훈(다른 자료에도 적용)** — 파일 형식은 **확장자가 아니라 시그니처(`PK`)로 판단**한다.
항차가 파일명에 없으면 **파일을 열어본다**(v2.17.18에서 이미 세운 원칙인데 형식 판별에서 막혀 있었다).

**개수 대조** — 덱플랜 192 vs 앱 EDI 191. 차이 1대(`SPSU2019220` 20GP)는 **여객 화물용**이라
적하목록에 없는 게 정상이다(사용자 확답 2026-08-02). 덱플랜이 EDI보다 1~2대 많은 건 이 때문일 수 있다.

### 1-4I. ★ 선박 단위 자료를 항차에 붙일 때 — 배정과 대조한다 (2026-08-02 실측, v2.17.42)

**도선 예보와 PORT-MIS 신고는 선박(콜사인)당 레코드 1개다.** 어느 항차 것인지 표시가 없다.
반면 **선석배정은 항차별**로 온다. 그래서 선박 단위 값을 그냥 쓰면 **그 선박의 모든 항차에 같은 시각**이 붙는다.

**사건(사용자 지적)** — *"항차가 틀린데 한선박이 두항차 동시에 들어 오나요?"*

| 선박 | 이번 기항(도선) | 다음 기항 | 버그 당시 다음 기항에 붙던 값 |
|---|---|---|---|
| TMPZ | 2023E 08-02 18:30 | 2025E 배정 **08-09 19:00** | 08-02 21:30 ❌ |
| RZOR | R082E 08-03 07:30 | R083E 배정 **08-05 13:00** | 08-03 07:30 ❌ |
| OBWH | 2703E 08-03 08:30 | 2705E 배정 **08-05 10:00** | 08-03 08:30 ❌ |

세 척에서 동시에 나고 있었다. 홈 카드만 보면 한 선박이 두 항차로 같은 날 들어오는 것처럼 보인다.

**규칙(확정)** — 선박 단위 출처(도선·PORT-MIS)는 **그 항차의 배정 시각과 36시간 안쪽일 때만** 채택한다.
- 어긋나면 다른 기항으로 보고 **배정값을 그대로 둔다**(잘못된 값을 붙이는 것보다 계획값이 낫다).
- **배정이 아예 없으면 종전대로 도선을 쓴다** — 없는 근거로 막지 않는다.
- 판정 함수: `autoreg.py::_same_call` / `_voy_scoped`.

**어느 항차가 이번 기항인지 확인하는 법** — `MAILBOX/{선박}/{항차}/_예정.txt` 에 수집기가 적어 둔
**예정작업일자와 콜번호**를 본다(예: 2023E=콜 027 08-02, 2025E=콜 029 08-09). 자료가 실제로 들어오는
폴더가 이번 기항이다.

### 1-5. 리퍼·특수화물

- **리퍼 온도는 메일 지시가 EDI보다 우선**이다. 선사 메일에 적힌 온도로 수정한 뒤 양하한다.
- **통과화물 리퍼**는 카고플랜에 R/F로 보이되 **촬영 대상 카운트에서는 제외**한다.
- 양하 작업 순서: **일반 화물 우선, 냉동(리퍼) 후속.**
- 특수화물 리스트는 **반드시 베이 플랜을 기본 참고 파일로** 만든다. 항목 = 리퍼(컨번호·위치·온도), FR, O/T, DG 등.

### 1-6. 콘·해치커버 (콘앱)

- **30번 데크 바닥에는 콘이 필요 없다.** 다만 **커버 없는 홈(파진 곳)에는 콘이 필요하다.**
- 데크 기본은 82단이지만 **80단으로 오는 선박이 많다.** 1번 데크에 20ft 두 개가 들어가는 홈이 종종 있다.
- **해치커버는 자동계산 금지 — 1~3장 수동 선택**이다. 적용 화면은 **'작업 보고'** 한 곳뿐이고 다른 화면의 해치커버는 건드리지 않는다.
- 콘 규칙은 지침서에 있다. **모르면 추측 패치하지 말고 규칙을 먼저 물어라.** "규칙중에 이해가 안되는부분을 알려주세요"
- 한 곳만 고치지 마라. "자 이런게 코드 곳곳에 있을것입니다. 홀드도 같은 계산을 해야 합니다"

### 1-7. 적재 물리 규칙

- **40ft 위에 20ft를 못 얹는다**(콘 홀 없음). 40ft가 짝수 베이에 있으면 **홀수 베이에 X 표기** — 카고플랜과 베이플랜(편집기)에 **똑같이** 나와야 한다.
- 20ft 트윈이면 두 자리 다 컨번호가 있고, **싱글이면 한쪽이 흰색**이다.
- 선적 순서: 20ft 싱글 → 트윈 → 40ft (홀드·데크 동일).

### 1-8. 세관·XRAY

- 터미널 컨테이너 목록 조회는 **세관 신고 리스트와 XRAY 대상 리스트**를 얻기 위한 것이고, **메일 수집기의 최종 목표 중 하나**다.
- 다만 **자동화는 검수사님이 앱을 신뢰하실 때 지시하신다.** 임의로 착수하지 않는다.
  > "앱이 온전치 않아서 방금 보여드린 작업은 당분간 수작업 할 예정입니다. 제가 앱을 믿을수 있을떄 접목 예정입니다."

### 1-9. 수집기의 역할

- **메일 수집기는 검수앱에 자료를 대는 역할**이다. 수집기 작업을 할 때는 "검수앱의 어느 기능을 먹여 살리는가"를 먼저 이해한다.
- 수집기는 **조회 기간을 스스로 잡지 못한다.** 그래서 첫 메뉴가 '크롬 열기'다. **검수사님이 잡아 둔 크롬 화면(선석배정 조회 결과)을 절대 새로고침하지 마라.**

---

## 2. 작업 방식 — 가장 많이 교정받은 영역

### 2-1. 끝까지 직접 (압도적 1위, 최소 8회 반복)

- 배포·정리·실행·삭제·업로드 전부 클로드가 끝낸다. **사용자에게 명령어를 주고 끝내지 않는다.**
- 사용자 몫은 **로그인·비밀번호 입력뿐**이다.
- 클릭을 대신해 달라는 방식 자체가 실패다.
  > "일을 맡기고 다른일을 할수 없다는 이야기인데 제가 자리를 지키고 있어야 한다면 ai를 비싸게 주고 사용할 메리트가 없습니다."
  → 해법은 "사용자가 클릭하게 만드는 것"이 아니라 **클릭이 필요 없는 무인 구조로 바꾸는 것**이다.

### 2-2. 실데이터로 검증

- 목업·부분 검증 금지. 실제 EDI·실제 화면·실제 폴더로 확인한다.
- **검증 조건도 실제와 같아야 한다** — 본인 폰(로컬 사전 보유)만이 아니라 **사전이 없는 새 기기** 조건에서 확인.
- 증상은 코드가 아니라 **실제 폴더·화면**에서 확인한다.
- 테스트는 마음껏 해도 된다. "실제 사용이 아니고 테스트중이니 맘노코 테스트 해보시면 됩니다. 그래야 버그가 뭔지도 나올테니까요"

### 2-3. 판단은 근거 2개 이상 (2026-08-01 추가)

- 근거 하나로 단정하지 않는다. 하나뿐이면 **"이것만 봐서는 X로 보입니다"** 라고 불확실성을 밝힌다.
- 검수사님이 **"맞습니까?" "확실합니까?"** 라고 되물으시면 그건 **신호**다. 같은 답을 반복하지 말고 근거를 더 찾아라.
  > 실패 사례: PORT-MIS 한 줄만 보고 "지난 항차 맞습니다" → 실제로는 어제 작업 완료된 배. `completed 0` 한 칸만 보고 "검수 기록 없음" → 실번호는 213/213 기록돼 있었다.

### 2-4. 파급 — 한 곳만 고치지 않는다

- 고칠 함수·상수를 **저장소 전체 grep**해 호출부와 복제본을 전부 확인한다.
- 특정 선박·특정 케이스 전용 로직을 **공용 경로 앞단에 두지 않는다.** 게이트를 먼저 건다.
- 수정 **전/후 같은 기준표**를 찍어 대조한다(항차별 EDI·records·완료 수, 수집기 하트비트·autoreg 건수·사이클 시간).
- **"완료" 로그·반환값은 검증이 아니다.** 결과 상태를 다시 읽어 확인한다.

### 2-5. 미루지 않는다

- 원인을 알고 수정 지점까지 특정했으면 **그 세션에서 수정·검증·배포까지** 간다.
- 컨텍스트·사용량·시간을 핑계로 미루지 않는다. 간단한 건은 **그 자리에서** 처리한다.
- 이월 가능한 것은 둘뿐: 검수사님이 미루라고 하신 것, 검수사님 결정을 기다리는 것.

### 2-6. 발견은 모으고 수정은 한 번에

- 라이브 테스트 중 발견한 것은 모아 두었다가 한 번에 고친다. 중간중간 끊어 묻지 않는다.
- **"메모"** 로 시작하는 글은 즉시 작업하지 않는다. "메모 접수" 한 줄만. **"작업해줘"** 때 쌓인 메모를 일괄 처리한다.

### 2-7. 인계·산출물

- 파일은 **ZIP**으로 드린다. **지침서와 인계서를 버전에 맞춰 ZIP 안에 동봉**한다.
- ZIP은 겹폴더 없이 — 최상위 `Tallyman_Master_Vx.xx/` 하나.
- 결과물은 **검수사님이 실제로 여는 경로**에 둔다. 세션 임시 폴더에만 두면 못 여신다.
- 다른 세션 클로드가 막힌 건은 대신 처리하지 말고 **재시도용 체크리스트를 CLAUDE_SHARED에 남겨** 그쪽이 다시 하게 한다.
- 콘앱 불편신고는 검수사님이 보시는 게 아니라 **다음 클로드가 직접 읽고 고친다** — 세션 시작 시 feedback 노드를 먼저 조회한다.

### 2-8. 태도

- **"완벽합니까?"** 라는 물음이 나오면 검증 범위를 안 붙이고 완료 선언한 것이다. 완료에는 항상 무엇을 어떻게 확인했는지 붙인다.
- 진행 상황이 보이게 한다. "진척 사항을 알지 못하니 진행중인지 아닌지를 모르겠네요"
- 호기심으로 물으신 것과 작업 지시를 구분한다. "안하셔도 됩니다. 그냥 궁금했을 뿐입니다."
- 폴더명 표기가 일정하지 않다(`마스터V6` vs `마스터7.00`). **이름 패턴 하나로 검색하고 끝내지 마라.**
- 제출된 자료는 **빠짐없이 전부 분석**한다. 추론이 아니라 실제 파일 내용 기준으로 답한다.

---

## 3. 표시 취향

- **호칭은 "검수사"** 다. (초기에는 "성일님"이었고, 이후 "저를 부르는 이름을 바꿉니다 검수사로 불러주세요"로 바뀌었다.)
- 카고플랜 셀 마크: **좌상단 ◆ 파랑 = 쉬프팅 · 우상단 ★ 빨강 = XRAY · 좌하단 ▲ 빨강 = 긴급 · 보라 안쪽 테두리 = 수화물.** 긴급과 XRAY가 겹쳐도 **둘 다 보여야** 한다.
- 리스트 화면은 배지를 나란히(🔍 XRAY + ▲ 긴급).
- X 표기는 **배경색이 아니라 글자로** 보여야 한다. "회색은 보이는데 x표기가 안보입니다."
- 별첨 표의 이름칸(선사·종류·규격·합계)은 **중앙 정렬**.
- 트윈 위치 지정은 좌표 입력이 아니라 **빈자리 선택 그리드**(완료 자리는 회색·선택 불가).
- 콘앱 업데이트는 **자동감지 배너 + 항상 보이는 🔄 강제 새로고침 버튼**(서비스워커·캐시까지 비움) **둘 다**.
- 수집 후 강조색은 **새로 수집한 '메일'**에 준다(새 '선박'이 아니다).
- 잘못 등록된 선박을 지울 **삭제 버튼**이 필요하다.
- 소스 코드를 보여드리지 말고 **설명**으로 답한다. "소스를 보여 줄 필요는 없습니다. 설명을 해주세요 왜 52개인지?"
- PC·폰 겸용 화면은 별도 주소 없이 **같은 링크에서 화면 크기로 자동 전환**.

---

## 3-B. 수량은 무엇을 기준으로 세는가 (2026-08-01 확답 — 이걸 몰라 헛질문이 나왔다)

- **PCTC 부두: 선석배정목록이 기준.** 배정표에 `XTPG-0028-(P) 61/117/0` 처럼 **양하/선적/환적**이 그대로 나오고, **황색 표시가 수량 확정**을 뜻한다. 원문: *"PCTC는 배정목록에서 보면 나와 있습니다. 황색표시는 수량이 양하/선적 확정된것입니다."*
- **PNCT 부두: EDI에서 평택분을 찾는 것부터 시작한다.** 원문: *"PNCT는 지금 처럼 EDI에서 평택분을 찾는것부터 시작합니다."*
- **EDI 총량과 평택분이 다른 것은 정상이다 — 나머지는 통과화물이다.** 선적 EDI 227대인데 리스트가 117대여도 결함이 아니다. 원문: *"이질문이 조금만 확인 했다면 이질문이 안나왔을겁니다. 나머지 110대는 통과화물일테니까요."*
  → **"리스트에 없는 컨이 N대 있습니다"를 문제로 보고하기 전에, 그 배 부두가 어디인지·통과화물인지부터 확인한다.**
- 평택분 필터: **양하 = POD 평택 / 선적 = POL 평택.** (컨번호 끝자리 조회만 예외 — 제한 없음.)

---

## 4. 도메인 지식 (사실 모음 — 물어볼 필요 없는 것)

- 세 앱은 완전히 별개다: **검수앱(Tallyman Master)** / **콘앱(cone.html)** / **벌크탤리(bulk_tally.html)**. 저장소는 하나(`greenmarine26/greenmarinetally`).
- 기준 자료 4종: **메인 플랜 · 베이 플랜 · 양하 리스트 · XRAY 리스트.**
- 베이 페어링: 짝수 + 뒤 홀수(6+7), 표기는 항상 (작은 짝수)(큰 홀수). **데이터로 추론하지 말고 규칙 고정.**
- 데크/홀드는 항상 분리 관리한다. 축을 합치면 유령 슬롯이 생긴다.
- 메일박스: `G:\내 드라이브\MAILBOX`. 클로드 간 공유 문서: `G:\내 드라이브\CLAUDE_SHARED`. 로컬 작업 폴더: `C:\TALLYTEST`.
- G: 는 구글 드라이브 가상 드라이브라 셸 샌드박스에 마운트되지 않는다 — 파일 도구(Read/Write/Glob)나 PC에서 도는 스크립트로 접근한다.
- 선박명 이중등록이 잦다(DRACO = NSDC = NAMSUNG DRACO). 약자·풀네임 조각이 따로 등록되면 정리 대상.
- 26척 명단에 없는 배는 UNKNOWN으로 떨어진다 — 새 선박은 등록부에 추가해야 한다.

---

## 4-A. 기능 요청을 받으면 '왜'부터 묻는다 (사용자 지적 2026-08-01)

- 사용자 원문: *"왜 그걸 표기해야 하는지는 묻질 않았습니다. 그러니 사용자의 의도를 모른 채
  만들기만 하니 의도에 맞지 않는 결과가 나오고 수정에 수정을 거칩니다."*
- "답을 아는 질문 금지"는 데이터로 알 수 있는 걸 묻지 말라는 뜻이지 **'왜'를 삼키라는 뜻이 아니다.**
  '왜'는 데이터 어디에도 없고 사용자만 답할 수 있으며, 그 답이 설계를 통째로 바꾼다.
- 순서: 기능 요청 → **① 이 표시/기능은 현장에서 어떤 판단에 쓰입니까?(왜)** → ② 그 다음에 구현
  사양(위치·기준·범위). ①을 건너뛴 구현 질문 서너 개는 질문이 아니라 사양 받아쓰기다.
- 실례: "출항시간 표기" — 마감서류 때문이면 출항 전 알림, 다음 배 승선 계획이면 다음 항차와 나란히,
  콘 회수 판단이면 콘앱이 주 화면. 같은 요청이 의도에 따라 세 가지 다른 물건이 된다.
- **실제 답(2026-08-01 확인): TNJP·OBWH·RZOR·SITC 계열은 그린마린이 입·출항 작업(줄잡이)을 하는
  선박이다.** 입항은 30분 전까지 부두 대기, 출항은 검수 종료 후 준비 시간이 필요하다.
  → 출항시간 표기 = 이안 작업 준비 시간 확보용. 이 의도에서 파생되는 미구현 후보:
  ① 입항 쪽도 같은 급으로 중요 — 도선예보 입항시각 기준 '30분 전' 표시/알림은 아직 없다.
  ② 대상 선박은 '그린마린 줄잡이 계약선 명단'으로 관리해야 계약 변경 시 코드 수정 없이 따라간다.

## 4-A2. "맞나요?"에 즉답하지 마라 — 확인이 먼저다 (사용자 지적 2026-08-01)

> 사용자 원문: *"제가 자꾸 맞느냐고 물어 보는건 클로드가 확인을 안하기 때문에 물어 보는겁니다.
> 맞는것인데 맞냐고 물어 보면 제가 틀렸습니다 라고 답부터 합니다. 확신을 가져야 할곳에서는
> 확신을 못갖고 틀린것에 대해선 확신을 갖고 대답을 합니다. 확인부터 하는 습관을 가지셔야 합니다."*

- **"맞나요?"는 틀렸다는 신호가 아니라 "확인했느냐"는 신호다.** 반사적 사과·번복 금지.
  사과부터 하는 것은 검증이 아니라 비위 맞추기이며, 실제로 오답을 확신 있게 만들어 낸다.
- **순서: 확인 → 답변.** 확인 전에는 "확인해보겠습니다"만 말한다.
- **확인한 것은 근거와 함께 단정하고, 근거 없이 뒤집지 않는다.** 번복하려면 **새 근거**가 있어야 한다.
- **확인 못 한 것은 "모른다"고 말한다.** 추론을 결론처럼 말하지 않는다.
- **⚠ 대조 전에 '같은 대상인가'부터 확인한다.**
  사고(2026-08-01 ATPR): 화면 카드는 **2635W**(선적·검수완료·수석대기)인데 클로드는 DB에서
  **ATPR_2635E**(다른 기항 키)를 꺼내 "화면과 DB가 어긋난다"고 3회 연속 오판했다. 정상을 결함으로
  만든 것. 항차 키가 여럿인 선박에서는 키를 먼저 맞춘다.
- **홈 카드에서 사라진 것은 정상일 수 있다** — 검수 완료 후 수석 확인 처리되면 홈에서 빠진다.
  카드 유무를 결함 근거로 쓰기 전에 완료·수석대기 상태를 확인할 것.
- **선박마다 운항 형태가 다르다**(ATPR: 기항마다 양하 따로·선적 따로). 데이터에 안 적혀 있고
  사용자만 아는 사실이다 — 추론하지 말고 **물어라**(4-A의 '왜'와 같은 계열).

## 4-A3. ⛔ 수집기 크롬(포트 9222)에 브라우저 자동화로 붙지 마라 (사고 2026-08-01)

- 수집기는 **전용 디버그 크롬**(프로필 `%LOCALAPPDATA%\tallyman-chrome`, `--remote-debugging-port=9222`,
  탭 3개: 메일·PCTC 선석배정목록·PNCT)에 CDP로 붙어 일한다. **그 크롬은 수집기 것이다.**
- 사고: 클로드가 배정목록 표를 읽으려고 Claude-in-Chrome MCP로 같은 크롬에 붙어 탭을 열고/이동/닫았다.
  → CDP 세션이 엉켜 `<ws connected>` 후 `connect_over_cdp` 타임아웃. 수집기가 사이클 진입 자체를 못 하고
  ⚡ 즉시 처리도 전부 막혔다. "탭이 사라졌다"는 MCP 오류가 반복된 것도 같은 원인.
- **규칙**: 웹 자료가 필요하면 **별도 크롬 창/프로필**을 쓰거나, 수집기 코드가 읽게 한다.
  9222 크롬은 열지도, 탭을 만들지도, 이동시키지도 않는다.
- **`크롬 열기` 버튼은 사용자가 하루 한 번만 누른다**(사용자 확정 2026-08-01).
  이후에는 `완전 자동 수집`만 누르면 된다. 클로드가 이 버튼을 반복해 누르지 말 것.

## 4-B. 배포 효과확인의 기준 (2026-08-01 사고 — "검증을 사용자에게 떠넘긴 결과")

- **배포 검증은 사용자가 실제로 받는 파일 기준이다.** 최소 3개를 라이브에서 되읽는다:
  ① `index.html`이 가리키는 **번들 해시**(이번 빌드 정본과 일치) ② `sw.js` 버전 ③ 콘앱 건드렸으면 `cone.html`.
- sw.js 하나만 보고 "배포 완료"라고 한 결과: V9.36-01/-02 payload에 루트 index.html·assets가 빠졌는데
  이틀 치 배포 두 판이 "완료"로 보고됐고, 사용자가 스크린샷 두 장으로 그걸 대신 검증했다.
- **"새로고침 해보세요"는 검증이 아니다.** 사용자 화면이 안 바뀌면 캐시 탓하기 전에
  라이브 파일을 내가 먼저 되읽는다. 캐시는 데이터로 배제한 뒤에만 입에 올린다.
- payload를 만들 땐 **직전 성공 커밋의 파일 목록과 대조**한다(이번 사고: 이전 판 payload에는
  있던 루트 index.html·assets가 새 판에서 빠졌다 — 목록 대조 한 번이면 잡혔다).
- **내가 고친 컴포넌트를 그려보지 않은 연막검사는 그 수정의 검증이 아니다** (지침서 V9.23-06
  "렌더 중에만 터지는 오류는 렌더해야 잡힌다" + V9.28-03 "연막검사는 그리는 컴포넌트만 잡는다").
  build.sh 연막검사는 BayGridEditor 계열만 그린다 — ChiefDashboard·콘앱 카드·기타 화면을 고쳤으면
  그 화면을 픽스처로 직접 렌더하거나(jsdom) 최소한 해당 로직을 실DB 형상 그대로 단위검증한다.
  사용자 화면은 검증 도구가 아니다: "콘앱의 버그는 제가 보는 게 아닐 것입니다" (사용자, 2026-08-01).

---

## 5. 이 문서를 갱신하는 법

1. 검수사님께 **새로 답을 받았으면** 해당 절에 한 줄 추가한다. 원문 표현을 살린다.
2. 같은 질문을 또 하게 됐다면 **이 문서에 그 항목이 없거나 찾기 어려운 것**이다 — 표현을 고쳐 찾기 쉽게 만든다.
3. 배포 시 이 파일도 저장소·드라이브 양쪽에 반영한다.

> 이 답지는 **세션 84개에서 기계적으로 뽑은 것**이라 초반부가 잘린 세션이 있다. 빠진 규칙이 보이면 발견 즉시 여기에 채워 넣는다.
