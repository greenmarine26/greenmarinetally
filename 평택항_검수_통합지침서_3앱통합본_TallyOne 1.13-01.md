> (저장소 사본) 정본은 G:\내 드라이브\CLAUDE_SHARED\★앱_통합지침서.md

# ★ 앱 통합지침서 — TallyOne · MailPilot · ConeOne (재탄생 1.0 세대)

> **한 권 원칙 (사용자 확정 2026-08-03)**: 세 앱의 지침서는 이 문서 하나로 대체한다. 구판(평택항_검수_통합지침서 V9.x 계열)의 방대한 이력은 저장소 git 히스토리에 보존돼 있고, 유효한 규칙은 전부 이 문서에 흡수됐다.
> **기록 의무 (비협상)**: 앱에 손을 댔으면 — 지시·허락 여부와 무관하게 — **반드시 아래 "작업 기록부"에 기록한다.**
> 작업 방법(배포·검증·질문 규칙)은 `★클로드_작업표준.md`를 따른다. 정본 위치는 둘 다 `G:\내 드라이브\CLAUDE_SHARED\`.

작성 2026-08-03 (재탄생일). 작성자: 검수사(김성일) + Claude.

---

## 1. 버전 정본표 — 버전을 말하기 전에 소스 파일을 직접 읽는다

| 앱 | 정체 | 현재 버전 | 버전 소스 (여기를 읽는다) |
|---|---|---|---|
| **TallyOne** (구 Tallyman Master, 검수앱) | React/Vite PWA + Firebase RTDB | **TallyOne 1.13-01** | `src/utils.js` → `APP_VERSION` — 단일 진실원 |
| **MailPilot** (구 수집기) | PC 로컬 Python (`C:\TALLYTEST\TallymanMailCollector_v2.17.15\` — 폴더명은 버전 아님) | **MailPilot 1.0-09** | `app_gui.py` → `VERSION` |
| **ConeOne** (구 콘앱) | `public/cone.html` 독립 HTML | **ConeOne 1.5-01** | `cone.html` → `window.__CONEV` — 콘앱 단일 소스(검수앱 버전과 분리) |
| 벌크탤리 | `bulk_tally.html` 독립 HTML | TallyOne 버전 동기 | 라벨은 build.sh가 APP_VERSION에서 동기화 |

**버전 규칙**: V9.xx·v2.17.xx·C7.xx 체계는 2026-08-03 종료. 기능 추가 = 0.1 올림(1.3→1.4), 버그픽스 = `-NN`(1.3-01). 언더스코어 금지. TallyOne·벌크탤리는 build.sh가 sw.js·라벨·`__APPV`를 자동 동기화 — **모든 배포는 APP_VERSION을 바꾼다**(안 바꾸면 폰 캐시 미갱신). ConeOne은 `__CONEV`를 직접 올린다(cone.html 수정 시). cone-cargoplan.js 번들이 바뀌면 APP_VERSION도 올려 `__APPV` 캐시키를 갱신한다.

저장소: `greenmarine26/greenmarinetally` (TallyOne+ConeOne+벌크탤리). 배포 정본: 저장소 `DEPLOY.md`. Firebase: `greenmarinetally-default-rtdb.asia-southeast1` (Spark).

## 2. TallyOne (검수앱) — 구조

### 2-1. 3화면 구조 (2026-08-03 개편)
- 시작은 **항상 로그인 화면**(#/login). 역할별 진입: 검수사 → 검수 홈(#/), 수석·소유자 → 수석 대시보드(#/chief).
- **소유자(김성일)만 전 화면 열람.** #/chief·#/search는 수석·소유자 전용. 보조기능은 #/aux(매뉴얼·용어집·영어회화집·맛집·건강점검·AI키·브리핑·장비번호·클로드 메모).
- 30분 무조작 자동 로그아웃. 접속 상태 배지: working(90초 내 활동)/online.
- 완료 권한(수석) 7명: 이현규·김명보·오승택·성창모·이강익·전우수·김성일(부수석). isChief = /수석검수|부수석/.
- ⚠ gemini API 키 하드코딩은 보안상 제거됨 — AI 검색은 보조기능 'AI 검색 키 설정'에서 키 등록 후 사용.

### 2-2. Firebase 노드 계약 (누가 쓰고 누가 읽는가)
- `voyages/{VSL_VOY}/info` — 앱·MailPilot 공동(PATCH·부분갱신 원칙, 통째 교체 금지). terminalStatus·planDate·planSrc(pilot|mail|portmis|plan)·departBadgeAt 등.
- `{mode}/ediContainers` — EDI 단일 진실(MailPilot PUT — 단 RZOR 좌표·표시 필드는 보존 머지). `records` — 리스트. `completed/{cn}={by,at}` — 검수 완료(현장 기록, 덮지 마라). `raw/edi` — 원문.
- `{mode}/stowagePlan` — RZOR 덱플랜(decks/slots: line·col·lolo·dbl·empty).
- `claude_inbox` — 클로드 메모함. `activity_log/{YYMMDD}` — 활동 로그(로그인·열람·조회, 30일 자동 정리, 수석 화면 소유자 전용 열람). `feedback` — 콘앱 불편신고(다음 클로드가 읽고 고친다).
- `pilot_forecast`·`terminal_work` — MailPilot 단독 소유(앱은 구독만). `port_mis_data` — 콜사인당 1레코드(옛 신고 잔존 — etd 안 지난 것만 이번 기항). `ship_bay_dict_v3` — 베이사전(콜사인·IMO는 수집기, 구조는 앱). `archive/{key}` — 완료 백업(백업 검증 후에만 삭제).
- collector_heartbeat — MailPilot 하트비트(끊김 기준 = cycleMin×2).

### 2-3. 데이터 소유권 원칙
- **검수앱이 1순위.** 잔여 = 총 − completed가 실시간 진실. 터미널 수치는 앱이 모를 때의 폴백. **수집기 하나에 다 몰아넣지 마라** — 수집기는 앱이 모르는 재료만 넣는다.
- 베이 구조 출처 우선순위: 사용자 매트릭스(검증) > PDF override > .def > v5 > v2 > EDI. **userBayDict 절대 보호.**

## 3. MailPilot (수집기) — 구조

- 역할: **검수앱에 자료를 대는 것.** 메일 수집 → 합본 → 항차 자동등록 → 터미널·도선·PORT-MIS 보강.
- 사이클 오케스트레이션은 `collector/master.py` 단일 소스. 일정 전용 루프(3분, 크롬 불사용)와 컨테이너 사이클(5분+α) 분리. pilot·terminal_work는 일정 루프 소유.
- 전용 디버그 크롬(9222, 프로필 tallyman-chrome, 탭: 메일·PCTC·PNCT)에 CDP로 붙는다. **이 크롬은 수집기 것 — 다른 자동화로 붙지 마라**(작업표준 §3 금기). '크롬 열기'는 사용자가 하루 1회.
- 파서 단일 진입점 `parseBAPLIE`(BAPLIE·숫자코드·IFCSUM 라우팅). **EDI는 확장자로 판별하지 않는다** — DXQD·STMJ는 .txt로 온다. 세그먼트 순서 선사별 상이(LOC-first/EQD-first) — 블록 단위 파싱.
- 재시작: `C:\TALLYTEST\_restart_v21747.bat` (프로세스 kill → run.bat). 효과 확인은 하트비트 버전 되읽기.
- 설정 실사용 경로는 `%LOCALAPPDATA%\TallymanCollector\config.json` 하나(기동 시 누락 키 자동 병합). 저장소 config.json은 참고용.
- 2026-08-03 전수조사 수리판(구 v2.17.47) 기반 — 상세: `C:\TALLYTEST\메일수집기_전수조사_보고서_v2.17.46_2026-08-03.md`. 백업: `_backup_v21746_prev\`.

## 4. ConeOne (콘앱) — 구조·콘 규칙

- 파서는 마스터(parseBAPLIE, window.ConeParse) 단일 소스 — 폴백(약식) 사용 시 "간이 해석 모드" 경고 표시. 베이뷰 격자는 검수앱 BayPlan 규칙(window.ConeBayGrid) 단일 소스 — 유령 칸 금지.
- **콘 기본 규칙**: 홀드 콘 = 컨선 20ft 1개·40ft 0개(복합선은 곳당 2/4), 콘은 2단부터, 받침은 전체 화물 기준. 데크 = 40ft 콘4 / 20ft 트윈 자리당 데크2+코끼리2 / 20ft 전용데크 4 / 20ft 단독 데크2+코끼리2. 배분 표시 = 데크÷4·코끼리÷2.
- 30번 데크 바닥 콘 불필요(단 커버 없는 홈은 필요). 데크 기본 82단이나 80단 선박 많음. **해치커버는 자동계산 금지 — '작업 보고' 한 곳에서 1~3장 수동 선택.**
- 작업표 트리오 병합: 선행 홀수 베이를 짝수 트리오로 합산 표시(28=2+26 방식), 질문기는 구간 매칭("6번"=05-07).
- **RZOR(RIZHAO ORIENT) LOLO 규칙 (사용자 확정 2026-08-03)**: RORO+LOLO 혼용선 — 콘 대상은 LOLO(갠트리) 적재분만. 1곳 최대 8열 3단, 열마다 1단(콘 없이)→2단 콘→3단 콘. **콘 = (LOLO 반수 − 1단 바닥 수) × 4** (바닥 입력식, 기본 ⌈N/3⌉). **LOLO 최대 67반**(R081W 실측 MID49+STERN18, 40H 엠티) 초과 시 자료 오류 경고. 덱플랜(줄×칸)에는 단수 정보가 없다 — 데이터에서 찾으려 헤매지 말 것. 화면은 검수앱 LOLO 탭과 동일한 덱플랜 격자.
- 콘 규칙이 이해 안 되면 추측 패치 금지 — 규칙을 먼저 물어라. 한 곳만 고치지 마라(홀드도 같은 계산).

## 5. 도메인 규칙 (검수 업무 — 사용자 확정 모음)

### 5-1. 항차·기항·관할
- **항차의 진실은 선석배정 목록.** PORT-MIS는 항차와 무관. 표기도 배정표가 정본.
- 한 기항 = 한 키(양하 E/N + 선적 W/S 한 카드). 짝 규칙은 선박마다 다름 — 동일 번호형(ATPR·RZOR)과 nE+(n+1)W형(OBWH·DJCT·YKTD). **추론 금지, 배정표·마감텔리 제목으로 확인.**
- 관할 판정은 선박명이 아니라 **항로** — PXS·PQS·JWKP면 그 주는 비관할. 지난 항차는 지운다. 출항하면 다음 기항이 '예정'으로 미리 뜨는 것은 정상.

### 5-2. 카운트·평택분
- **평택분만 센다**: 양하 = POD 평택, 선적 = POL 평택(+리스트 등재 `_inList`). 컨번호 끝4 조회만 예외. 폴백 금지 — 평택분 0이면 0이 정답.
- 집계 답변은 구분해서: "20피트?" → 20풀 n / 20엠티 n. 끝4 호출 → 실번호 전체 + XRAY 여부 + 선내 위치.
- **EDI 총량 ≠ 평택분은 정상**(나머지는 통과화물). PCTC 부두는 배정목록 수량(황색=확정)이 기준, PNCT는 EDI 평택분부터.
- 카고플랜은 베이 그림·별첨표·베이별 카운트 일치. 부족분 음수는 미완이 아니다.

### 5-3. EDI·자료
- 확정(정식) EDI 전에는 수치 불일치를 판단하지 않는다. 개정 마커 서열: 최종/FINAL > REVISED/수정 > n차 > 무표시(mtime 불신).
- 선박·항차 다른 EDI는 차단. 완료일자 이전 자료는 완료 선박이라도 수집.
- 파일명 끝 선사명은 검수사 표기 관례. 파일 내용 헤더($604 등)가 진실, 폴더명은 폴백.
- TMPZ = 선적 EDI 없음, 두우 프리스토우 플랜+NOLIST → 가상 EDI(자리만 `__SLOT_`, 컨번호는 NOLIST). 플랜 파일명에 항차 토큰 없는 게 정상. 규칙대로 작동 중 — 손대지 마라.

### 5-4. 작업일시(planDate) — 2026-08-02 최종 확정 (옛 "배정이 진실" 규칙 폐기)
```
① 도선 예보 ② 선사 일정 메일 ③ PORT-MIS 신고 ④ 선석배정
```
- 자력선은 도선에 안 뜸 → 메일이 사실상 1순위, 안전장치는 PORT-MIS. PORT-MIS는 etd 안 지난 신고만.
- 작업 시작 시각(EST COMMENCE)은 선사 일정 메일에만 있다. 서식은 선사마다 다름 — 실물 보고 추가(추측 정규식 금지). 같은 메일에 타 항 일정 섞임 — 평택 것만.
- 출항 배지: 퍼센트 금지 — **선적 잔여 ≤ 20개**(마지막 갱 기준 ≈1시간). 터미널 DEPARTED면 무조건. 한 번 뜨면 유지. PCTC '작업완료(초록)'도 출항 취급(확정 2026-08-03).

### 5-5. 리퍼·특수·적재 물리
- 리퍼 온도는 메일 지시 > EDI. 통과 리퍼는 촬영 카운트 제외. 양하 순서: 일반 먼저, 리퍼 후속.
- 특수화물 리스트는 베이 플랜 기본(리퍼 온도·FR·OT·DG). 리퍼드라이(rfdry)=넌플러그, 제작컨(mkcon)=컨 자체가 상품 — 온도 경고 제외.
- **40ft 위에 20ft 불가**(콘 홀 없음) — 짝수 베이 40ft면 홀수 베이 X 표기(카고플랜·베이플랜 동일). 선적 순서 20싱글→트윈→40. 베이 페어링 = 짝수+뒤홀수(6+7) 고정. 데크/홀드 분리 관리(축 통합 = 유령 슬롯).
- 세관·XRAY 자동화는 검수사가 앱을 신뢰할 때 지시 — 임의 착수 금지.
- **선적은 홀드를 다 채우고 데크를 올린다** (검수사 확정 2026-08-05). 그래서 **데크에 실렸다 = 그 아래 커버가 닫혔다**이고, 더 나아가 **그 모드가 완료 처리됐다 = 열었던 커버는 다 닫혔다**이다. 커버를 안 닫으면 데크에 못 싣고, 못 실으면 완료가 안 되기 때문이다.
  → 클로즈 보고가 없어도 **완료 처리된 항차라면 닫힘으로 기록**한다. 단 **시각은 지어내지 않고 비운다**(수기 보완). 앞뒤 클로즈 사이에 놓아 순서만 맞춘다.
- **실오류와 자료 불일치는 다르다** (2026-08-04 확정, TallyOne 1.8-03).
  - `🔴 실오류` = 검수원이 **실물 봉인을 보고** 서류와 다르다고 판정. **세관 신고 대상.** 판정 근거는 `sl_history`(수정 이력)다.
  - `⚠ 자료 불일치` = **리스트끼리** 실번호가 다름. 아무도 실물을 안 봤다. 검수 전에 확인할 신호. 근거는 `sl_conflict`(두 값과 출처를 모두 보관).
  - 실증: STMJ 2643E `SKHU8912132` — 같은 BL(SNKO024260703119) 같은 컨인데 선사 리스트 `482869` / 세관 CDL `549844`. **자료가 실제로 어긋나 있었다.**
  - ⚠ 종전 판정은 `sl !== sl_orig` 하나뿐이라 리스트끼리 달라도 '세관 신고 대상'으로 붉게 떴다. 그 반대(조용히 덮어 감추기)도 안 된다 — **둘 다 남기고 표시**가 검수사 확정 원칙이다.
- **리퍼 온도는 세 값이 따로다** (2026-08-04 확정): `EDI 온도`(리스트/EDI 원본, 수정 불가) · `실제 셋팅온도`(rfSet) · `실제온도`(rfAct). 뒤 둘은 검수원이 리퍼 메모에서 확인·수정한다. 텔리 RF condition report 의 `Setting`←rfSet, `Actual`←rfAct. **확인 안 한 Actual 은 빈칸으로 둔다** — EDI 온도로 채우면 재보지도 않은 값이 '실측'으로 서류에 박힌다.
- 리퍼가 많으면 일일이 못 잰다. **본선 선원이 체크한 온도 리스트를 사진으로 받아 앱이 읽는다**(Gemini). 판독은 초안이고 확정은 사람이 한다.
- **설정온도 대비 ±5℃ 는 이상 없음** (검수사 확정 2026-08-04). 그 안이면 경고하지 않는다. 실측: SITC 모니터 로그 24대 중 최대 편차는 SEGU9783740 의 2.0℃(설정 -23 → -21.0)였고 **정상**이다.
- **선원 온도 기록표 양식** — 실물은 `SITC REFRIGERATED CONTAINER MONITOR LOG` 형태다.
  `NO. | CONTAINER NUMBER | SLOT | PLUG-IN DATE | POL | POD | SET TEMP | (날짜)0 4 8 12 16 20 | (다음날)0 4 8 12 16 20 …`
  ⚠ **`ACTUAL` 이라는 열이 없다.** 설정은 `SET TEMP` 한 칸이고 실제 온도는 **시각별 격자**다 —
    실제 온도 = **가장 오른쪽에 값이 적힌 칸**(가장 최근 관측값). 1.8 첫 판 프롬프트가 "ACTUAL 열을 찾아라"여서 판독 기준이 없었다.
  컨번호는 `HALU 8503321` 처럼 띄어 적혀 온다 — 붙여서 읽는다.
- **리퍼 풀/공 판정 정본 (검수사 확정 2026-08-04)** — 판단 순서가 곧 신뢰 순서다.

  | 순위 | 근거 | 판정 |
  |---|---|---|
  | 1 | **F/E 열** | `F`·`FULL`·`L`·`LOADED`→풀 / `E`·`EMPTY`·`MT`·`M`→공 |
  | 2 | ITEM 열 "공컨"·EMPTY | 공 |
  | 3 | `RF`·`RE`·`RD` (= `R/F`·`R/E`·`R/D`, 하이픈·공백 허용) | 풀 / 공 / **리퍼드라이** |
  | 4 | TYPE·SIZE 끝 글자 (`20F`·`40E`) | 풀 / 공 |
  | 5 | **온도가 적혀 있음** | **풀** — 온도를 적었다는 건 가동 중이라는 뜻 |
  | 6 | SOC 양식 + 실 유무 | 실 있음 풀 / 없음 공 |

  ⛔ **`45RE`·`43RF` 같은 규격형에서 풀/공을 읽지 않는다.** `RE` 는 **REEFER 타입 코드**지 Empty 가 아니다.
     검수사 확인: *"F 45RE 온도 이렇게 올수도 있습니다. 이건 풀리퍼라는 이야기 입니다."*
     1.8-05 가 `45RE`를 무조건 엠티로 읽었다가 1.8-06 에서 **5분 만에 되돌렸다** — 풀 리퍼를 온도 점검에서 빼는 오판이라 원래 문제보다 나쁘다.
     실물 엠티 10대가 공으로 맞았던 것도 `45RE` 때문이 아니라 **F/E 열이 `E`** 였기 때문이다.
  ⚠ 3순위 붙여쓰기는 **규격 접두 없이 정확히 두 글자 R+X 일 때만** 인정한다. 진짜 ISO 코드(`45R1`·`22R1`)는 끝이 숫자라 안 걸린다.
  ⚠ 종전 파서는 3순위 표기를 어느 경로에서도 못 읽어 **미판정 → 기본 Full** 로 흘렀다. 공 리퍼가 풀로 집계되고 리퍼드라이가 온도 경고에 잡히던 원인이다.
- **온도 점검 대상은 네 곳이 같은 기준이어야 한다** — 리퍼 메모 화면 · 상단 `❄ 리퍼` 버튼 · 텔리 RF condition report · 출항 임박 경고. 식: `리퍼 && !rfdry && !mkcon && (fe==='F' || fe 미상)`. 1.8 첫 판은 메모만 공 리퍼를 포함해 **메모엔 뜨는데 텔리엔 안 실리는 컨**이 생겼고, 텔리만 rfdry·mkcon 을 안 걸러 **온도 못 재는 컨이 온도 보고서에 실렸다**(둘 다 시뮬에서 잡음).

### 5-6. 표시 취향
- 호칭 "검수사". 셀 마크: 좌상◆파랑=쉬프팅·우상★빨강=XRAY·좌하▲빨강=긴급·보라 테두리=수화물(겹쳐도 둘 다). X는 배경색 아닌 글자로. 별첨 이름칸 중앙 정렬. 트윈 위치는 빈자리 선택 그리드. 강조색은 새 '메일'에. 소스 대신 설명. PC·폰 같은 링크 자동 전환.

## 6. 작업 기록부 (앱에 손댄 기록 — 최신이 위, 반드시 추가)

> 범용화 프로젝트(TallyUni·MailPilot Uni, 저장소 tallyone-universal)의 작업 기록은 여기가 아니라 `★범용화_프로젝트_별도관리.md` §8에 적는다 — 검수사 지시 "따로 관리"(2026-08-05). 이 기록부는 현장 3앱(TallyOne·MailPilot·ConeOne) 전용이다.

### 2026-08-06 — TallyOne 1.13-01 + MailPilot 1.0-09: 양하만 왔는데 '자료 확정' (커밋 `__H5__`)

**신고 (검수사, 1.13 배포 직후).** "확정된 선박 중 **양하만 확정인데 자료 확정으로 되어 있습니다. 양하 선적이 이루어지는 선박인데도.** 그럴 경우 별도 표기가 있어야 할 것입니다. **선적자료 대기중** 같은 표기."
그리고 — "**그것까지 처리하셨어야 했습니다. 그래야 수정이 된 거죠**"(수집기 dataAt 미대응 건).

**원인.** 1.13 이 예정 섹션을 **섹션 노드 유무**로 판단했다. 선적 섹션이 아직 안 만들어졌으면 계산에서 통째로 빠져 "양하 하나만 100%" = 전체 100% 가 됐다.
**예정 섹션의 진실은 `info.voy_d` / `voy_l` 이다.** RTDB 17항차 실측 —

```
TNJP_26356E  voy_d 26356E · voy_l 26356W  보유=discharge     ⚠ 선적 예정인데 양하만
SWDN_2608N   voy_d 2608N  · voy_l 2608N   보유=discharge     ⚠ 같은 경우
ATPR_2636E   voy_d 2636E  · voy_l 없음                        ← 양하만 하는 배 (검수사 확인)
SWBT_2614S   voy_d 없음   · voy_l 2614S                       ← 선적만 하는 배
```

**수리.**
1. 예정 섹션 = `voy_d` 있으면 양하 · `voy_l` 있으면 선적(둘 다 없으면 `info.mode` 폴백).
2. 완성율 = **예정 섹션들의 완성율 평균.** 자료가 아직 없는 예정 섹션은 0 — 몇 대일지 모르므로 가중치는 균등하다. TNJP 는 100%→**50%** 가 됐다.
3. 일부만 끝났으면 `⏳ 선적자료 대기중` 표기(검수사 문구 그대로).

**이미 찍힌 잘못된 확정 기록도 앱이 지운다.** 1.13 이 17:14 에 TNJP·SWDN 에 `dataFixedAt` 을 써 버렸다 — 코드만 고치면 그 기록이 남아 계속 '확정'으로 뜬다. 지우는 조건을 **좁게** 잡았다: **예정 섹션 중 자료가 한 번도 안 들어온 것이 있을 때만.** 정상적인 '수정본'(모든 섹션에 자료가 있는데 매칭이 잠깐 어긋난 상태)에서 지우면 수정본 표기가 사라진다.

**MailPilot 1.0-09 — 수집기도 `dataAt` 을 쓴다.** 1.13 은 앱 업로드 경로만 고쳐, 수집기가 REST 로 직접 쓰는 자동등록분은 갱신 시각이 안 남았다. `collector/autoreg.py` 의 ediContainers PUT(883행)·records PUT(973행) 뒤에 `dataAt` PUT 을 붙였다. **리스트는 실제로 바뀐 때만** 올린다(`if added or filled or removed…` 안) — 변화 없이 도는 사이클마다 올리면 '수정본'이 계속 뜬다. `py_compile` PASS.

**검증 (RTDB 17항차 실측 렌더).**

```
100%  PCSZ_2623E   [양+선]  · ✅ 자료 확정
100%  SWSP_2606N   [양+선]  · ✅ 자료 확정
 50%  TNJP_26356E  [양+선]  · ⏳ 선적자료 대기중 · 갱신 08-06 15:02
 50%  XTPG_535E    [양+선]  · ⏳ 선적자료 대기중 · 갱신 08-06 10:57
 50%  SWDN_2608N   [양+선]  · ⏳ 선적자료 대기중 · 갱신 08-03 18:21
 33%  NSFR_2615N   [양+선]  · 갱신 08-05 11:54
  0%  ATPR_2636E   [양]     · 자료 없음          ← 양하만 하는 배: 양하만 채우면 확정
```

시뮬 8항목 PASS.

**교훈.** *"섹션 노드가 있으면 그 섹션이 예정된 것"* 이라고 **구조에서 의도를 추론**했다. 의도는 `voy_d`/`voy_l` 에 명시돼 있었다. 있는 데이터를 안 보고 있는 구조로 짐작한 것이 원인이다.
그리고 **앱과 수집기가 같은 노드를 쓰면 한 판에서 둘 다 고쳐야 한다.** 앱만 고치고 "수집기는 별도 판"으로 넘긴 것은 반쪽 수정이었다 — 검수사 지적대로다.

### 2026-08-06 — TallyOne 1.13: 홈 카드 **등록일자 → 작업일시 + 자료 상태** (커밋 `3db2826`)

**요청 (검수사).** "등록일자는 사실상 무의미하다. 등록일자 대신 작업일자를 표기하고 자료 갱신일시를 그 옆에. 자료가 완성되면 갱신일시를 없애고 '자료 확정'이라고, 확정 후 수정 자료가 있으면 '수정본'이라고. 작업일시도 변경될 수 있으니 변경 시점으로 표기."

**범위 확정.** "**EDI + 리스트 100매칭까지.** 그러면 관리자가 X-RAY 리스트·선적 보충자료 추가 작업합니다." → 갱신·확정 판정은 **EDI와 리스트만** 본다. X-RAY는 확정 뒤 붙는 자료라 세지 않는다.

**표기.**

```
🗓 작업 08-06 06:42 ~ 08-07 05:00 · 갱신 08-06 03:40      ← 자료 모으는 중
🗓 작업 08-07 06:00 ~ 21:00        · ✅ 자료 확정           ← 100% 매칭
🗓 작업 08-07 06:00 ~ 21:00        · ✏ 수정본 08-06 17:20   ← 확정 뒤 또 들어옴
🗓 작업 08-09 19:00 ~ 08-10 12:00  · 자료 없음
```

같은 날 끝나면 종료 시각의 날짜를 생략한다(`06:00 ~ 21:00`). '곧 자동삭제' 경고는 그대로 남겼다.

**작업일시는 따로 보관하지 않는다.** `_etaMs`/`_etdMs` 를 **매 렌더 다시 고른다**(V9.35: 선석배정 > 도선 예보 > PORT-MIS, 지나지 않은 일정 우선). 일정이 바뀌면 저절로 바뀐 값이 나온다 — 검수사가 말한 "변경 시점으로 표기"가 이미 이 구조로 충족된다.

**새로 저장하는 것 둘.**

| 노드 | 언제 | 누가 |
|---|---|---|
| `voyages/{키}/{mode}/dataAt` | EDI·리스트 저장할 때마다 | `firebase.js _touchDataAt` (`fbSaveEdiContainers`·`fbSaveListRecords`). 자동등록은 `autoRegApi` payload 의 `dataAt` |
| `voyages/{키}/info/dataFixedAt` | 완성율이 **처음** 1.0 이 된 순간 | `HomePage` useEffect 가 자동 기록. **이미 있으면 절대 안 덮는다** — 덮으면 수정본 판정이 영영 안 뜬다 |

수정본 판정에 **1분 여유**를 뒀다. 확정 기록과 자료 저장이 거의 동시에 일어나면 확정하자마자 수정본으로 뒤집힌다.

**한계 (정직하게).** `dataAt` 은 1.13 부터 쌓인다. 그 전에 등록된 항차는 `raw/edi.uploadedAt` 으로 폴백하므로 **EDI 시각만** 보인다. 리스트만 있는 항차(TMPZ 2025E 실측)는 새 자료가 올 때까지 `갱신 —` 로 나온다. 수집기(MailPilot)가 REST 로 직접 쓰는 경로는 payload 의 `dataAt` 을 수집기가 써 줘야 반영된다 — 안 쓰면 EDI 폴백으로 떨어진다. **수집기 대응은 별도 판.**

**검증 (RTDB 17항차 실측 렌더 시뮬).**

```
TNJP_26356E  🗓 작업 08-07 06:00 ~ 21:00        · 갱신 08-06 15:02   (100%)
PCSZ_2623E   🗓 작업 08-05 23:18 ~ 08-06 20:00  · 갱신 08-06 16:59   (100%)
SWSP_2606N   🗓 작업 08-06 06:42 ~ 08-07 05:00  · 갱신 08-06 03:40   (100%)
NSFR_2615N   🗓 작업 08-07 19:00 ~ 08-08 06:00  · 갱신 08-05 11:54   ( 46%)
TMPZ_2025E   🗓 작업 08-09 19:00 ~ 08-10 12:00  · 갱신 —             (  0%)  ← 리스트만, EDI 미도착
ATPR_2636E   🗓 작업 08-09 23:00 ~ 08-10 08:00  · 자료 없음           (  0%)
```

100% 항차가 `✅ 자료 확정` 으로 바뀌는 것은 **배포 후 앱이 `dataFixedAt` 을 처음 기록한 뒤부터**다(기존 항차엔 그 필드가 없다). `bash build.sh` 성공 · 번들 grep `TallyOne 1.13`·`자료 확정`·`수정본`·`dataFixedAt`·`dataAt` 확인.

### 2026-08-06 — TallyOne 1.12: 홈 항차 목록을 **자료 완성율 순**으로 (커밋 `38be071`)

**요청 (검수사 2026-08-06).** "자료가 완성된 걸 맨 위에 올리고 완성율을 기준으로 순서를 정해 주세요. 금일 작업 선박이라도 자료가 없으면 자료 있는 선박 밑에 놓고, 자료가 올라오면 작업순서 순으로 위로 올라오게요." · "양하/선적 둘 다 완성된 게 우선. 단 양하만·선적만 있는 것은 그에 해당하는 것이 완성되면 그것도 우선."(ATPR류)

**규칙 (확정).**

1. **1차 — 자료 완성율 내림차순.** 완성 = **EDI와 리스트가 맞아떨어짐** → 완성율 = 매칭 / 모수.
2. **2차 — 자료 유무.** 완성율이 같아도(대개 둘 다 0) 자료가 하나라도 있는 배가 위. EDI만 오고 리스트가 아직인 항차가 자료 한 장 없는 항차와 섞이면 안 된다.
3. **3차 — 기존 작업시간 근접순**(V9.01) 그대로.
4. 부두 그룹(현 부두 우선)은 종전대로 이 위에 얹는다.

**계산 (섹션마다, `computeStats`와 같은 규칙 — 카드 숫자와 어긋나면 안 된다).**

```
ptk   = EDI 평택분(양하=POD 평택 / 선적=POL 평택)
rec   = ownDirCns(records, mode)            ← 1.11의 반대 방향 리스트 제외
den   = max(|ptk| − 플랜슬롯(__SLOT_), |rec|)   ← 한쪽만 있으면 낮게, 둘 다 맞아야 1.0
항차 완성율 = Σmatched / Σden   (섹션이 있는 것만 합산)
```

**수정 지점.** `src/pages/HomePage.jsx` — `voyagesWithPier` 의 `.map()` 에 `_ready`·`_hasData` 를 실어 보내고, 바로 아래 `.sort()` 맨 앞에 두 키를 넣었다. 카드 계산(`computeStats`)은 **안 건드렸다** — 순서만 바뀌고 숫자는 그대로다.

**검증 (RTDB 17항차 실측).**

```
100%  TNJP_26356E [양하]      116/116          ← 한쪽만 있어도 100% (ATPR류 규칙 확인)
100%  PCSZ_2623E  [양하+선적]  600/600 · 83/83
100%  SWSP_2606N  [양하+선적]  371/371 · 407/407
100%  SWDN_2608N  [양하]      395/395
 66%  NSFR_2615N  [양하]      145/219  (EDI 145 · 리스트 219)
 44%  XTPG_535E   [양하+선적]  96/96 · 0/121 (선적 EDI 미도착)
  0%  NSDC_2607N  [양하]      0/231 (EDI 231 · 리스트 0)   ← 자료 있음 → 자료없음 무리보다 위
  0%  TMPZ_2025E  [양하]      0/104 (EDI 0 · 리스트 104)
  0%  나머지 9항차 — 자료 없음
```

시뮬 5항목 PASS. **2차 키는 이 시뮬에서 나왔다** — 처음엔 완성율만으로 정렬해 NSDC(EDI 231대 보유)가 자료 한 장 없는 배들 사이에 섞였다. 검수사 문장("자료가 없으면 자료 있는 선박 밑에")을 만족하려면 자료 유무가 따로 필요했다.

**파급.** 정렬만 바꿨으므로 카드 숫자(평택·매칭·완료 n/N)는 수정 전후 동일. `bash build.sh` 성공 · 연막검사 통과 · 번들 grep `TallyOne 1.12`·`_hasData` 확인.

**범용 검수앱 동시 적용.** 절차서를 `CLAUDE_SHARED\★수정절차_카고플랜정본판정+홈정렬_20260806.md` 에 만들어 뒀다(검수사 지시 — 두 곳 동시 수정). 범용 앱은 테넌트 개념이 있어 `isPyeongtaekPort` 자리에 그 앱의 자기항만 판정을 넣어야 한다.

### 2026-08-06 — TallyOne 1.11-01: 매트릭스 정본을 **조회 경로로 판정**하던 결함 (커밋 `8b0126e`)

**신고.** NSFR이 오랜만에 들어왔다. 검수사가 엣지에서 베이매트릭스를 CASP 플랜과 대조해 다시 맞추고 저장했는데, 크롬에서 열면 옛 그림 그대로였다. "다 맞춰논 매트릭스인데 틀어져 있다."

**서버는 멀쩡했다.** `ship_bay_dict_v3/NSFR` 실측 — `source:'user'`·`_userOwned:true`·22베이, `BAY01 holdCells[3,1,1]`·`holdTiers[8,6,4]`까지 온전. 엣지 화면과 같다.

**찾은 결함 — 겉껍데기로 정본을 판정한다.** 조회 결과의 `source`는 *어느 경로로 찾았나*(`user`=로컬 localStorage / `firebase` / `v2`)이지 *누가 만들었나*가 아니다. 그런데 아홉 곳이 `source === 'user'` 로 "정본인가"를 물었다. 로컬 사본이 없는 브라우저는 같은 매트릭스를 Firebase로 받으면서 **자동 사전 취급**을 당한다.

- `shipStructure.js:537` — `result.source === 'firebase'` 면 v2 자동본과 union. **정본이 v2 STOWAGE PDF 파싱본과 섞였다.** (NSFR 실측: BAY 27·28 에 검수사가 없앤 홀드가 `holdTiers[8,6,4]`·`[8,6]` 로 되살아남)
- `shipStructure.js:559` · 각 화면의 `enrichBayDef(..., source)` — 정본이면 보강 금지인데 Firebase 경유면 EDI 자동 보강이 걸린다
- `PrintableCargoPlanV2.jsx:493` — `_userOwned: baseDict.source === 'user'` 로 **항목의 정본 표식을 조회 경로로 덮어썼다.** 이 한 줄이 하류의 `cargoPlanCore.isUserSource` 까지 false로 만든다
- 같은 판정이 `PrintableCargoPlanV2:540·871·873`, `PrintableBayDetail:587`, `BayGridEditor:194·218·685`, `planedit.entry:136·288·330·698·758`, `BayPlan:272`, `coneCargoPlan.entry:149` 에 복제돼 있었다

**수리.** `utils.isUserOwnedBayDict(d)` 단일 소스 신설 — **항목 안쪽**(`_userOwned` / `bayDef.source`)만 본다. 위 열두 지점을 이 함수로 교체. `source` 는 표시용(조회 경로)으로 그대로 두고, 조회 결과에 `_userOwned` 정규화 플래그를 실어 보낸다.

**함께 고친 조용한 실패 (3금지 ③ 위반).** `fbSaveShipBayDict` 다기기 충돌 가드가 서버 항목이 더 최신이면 저장을 건너뛰면서 **`true`(성공)를 돌려줬다.** 빌더 화면엔 "☁ 동기화됨 (다른 기기에서도 보임)"이 떴는데 서버엔 안 올라갔다. → `false` 반환 + 콘솔 경고(서버/이번 저장 시각 함께)로 바꿔 "⚠ 동기화 실패"가 뜨게 했다. 기기 시계 차이를 의심할 근거를 남긴다.

**검증 (정직하게).**
- 실 Firebase NSFR 로 크롬 경로 시뮬 15항목 PASS — 수리 전엔 BAY 27·28 에 v2 홀드 2건이 주입됐고, 수리 후 22베이 전 필드 무손실.
- **그러나 이 2건은 `hasHold:false` 라 화면에 안 나타난다.** 즉 고친 것은 진짜 결함이지만 **이번 화면 증상 전체를 설명하지는 못한다.** NSFR은 우연히 v5 번들 베이목록이 검수사 목록과 같아(둘 다 22베이) 목록 오염이 없었다. v5 목록이 다르거나 v2에 `parsedAt`이 있는 배였다면 그림이 통째로 갈렸을 것이다.
- 남은 유력 후보는 **저장이 서버에 안 올라갔던 것**(위 조용한 실패). 검수사가 크롬에서 저장하자 바로 맞게 나온 것과 맞물린다. 재현 자료가 없어 확정 못 했다 — 다음에 같은 일이 나면 콘솔 경고가 증거를 남긴다.
- `bash build.sh` 성공 · 연막검사 통과 · 번들 grep `TallyOne 1.11-01`·`isUserOwnedBayDict`·"동기화 실패" 확인.

### 2026-08-06 — TallyOne 1.11: **리스트 합산 오류** — 양하·선적 리스트가 한쪽으로 합산됐다 (커밋 `6b34f9d`)

**신고.** 검수사가 홈 카드 두 장을 나란히 보였다 — 같은 배 SWSP 2606N인데 한 장은 `양하 평택 778 · 매칭 371 · 부분 EDI 371`, 다른 한 장은 `양하 평택 371 · 매칭 371`. "내용을 안 보고 양하와 선적 리스트를 같이 양하 리스트로 합산하는 오류입니다."

**숫자가 먼저 말했다.** 371 + 407 = 778. 양하 리스트 371 + 선적 리스트 407 이 통째로 양하 모수가 돼 있었다.

**왜 이 배만.** 다른 배는 양하 항차와 선적 항차의 번호가 달라(2643E · 2634W) 메일함 폴더가 저절로 갈린다. 그런데 **항차번호가 방향까지 같은 배(N_N 타입 — 양하 2606N · 선적 2606N)** 는 폴더가 하나로 묶여 양하 리스트와 선적 리스트가 한 폴더에 들어온다. 그 폴더를 통째로 자동등록하면 두 리스트가 한 mode 의 `records` 로 합쳐졌다.

**실데이터로 확정 (RTDB 실측 2026-08-06).** 두 리스트는 자기 방향을 스스로 들고 있었다 —
`discharge/records` 371건 전부 `pod=KRPTK`(pol=VNSGN/THBKK/THLCH), `loading/records` 407건 전부 `pol=PTK02`(pod=KRKAN/CNNKG/…).
**그런데 `isPyeongtaekPort('PTK02')` 가 false 였다.** 접미 정규식 `/(PTK|PYT|PYOTM|PYO)$/` 이 부두번호를 못 넘었다. 전 항차 records 를 훑으니 미인식 표기가 더 있었다 — `PTK02` 407건 · `PYEONGTAEK` 71건 · `PYONGTAEK` 25건 · `PYEONGTAEK,KOREA` 5건 · `PYONGTAEK,KOREA` 3건 = **511건이 평택인데 평택이 아닌 것으로 판정되고 있었다.** 이걸 먼저 고치지 않으면 방향 판정 자체가 불가능했다.

**수리 (5지점 — 전수 grep 후 복제본까지).**

1. `src/utils.js` `isPyeongtaekPort` — 부두번호 접미(`\d{0,2}$`)와 철자형(`PYONGTAEK`/`PYEONGTAEK`, 뒤 국가명 절단) 흡수.
2. `src/utils.js` **신설 단일 소스** `isOppositeDirRecord(r, mode)` · `ownDirCns(records, mode)` — 레코드의 POL/POD 로 **반대 방향이 확정된 것만** 뺀다. POL/POD 가 없거나 둘 다 평택(환적)이면 근거가 없으므로 **유지**한다(정보 손실 금지).
3. **유입 차단** — `autoRegApi.buildAutoPayload`(수집기 자동등록) · `VoyagePage.handleListUpload`(수동 업로드) 양쪽. 조용히 버리지 않는다: `counts.recordsDropped`·`perFile`·콘솔·화면 문구로 제외 건수를 드러낸다.
4. **표시 모수** — `HomePage.computeStats` · `ChiefDashboard.computeStats` 의 `recordCns`(둘 다 같은 결함의 복제본), `VoyagePage` 의 `recMap`. 이미 저장된 오염분도 화면에서 바로잡히게.
5. **메일함 폴더** — `mailboxFs.listVoyageFiles(root, vessel, voy, mode)` 가 `2606N(D)` / `2606N(L)` 를 먼저 찾고 기존 `2606N` 로 폴백. `MailboxFilePicker` 가 mode 를 넘긴다.

**검수사 확정 규칙 (2026-08-06).** N_N 타입은 **폴더만** `2606N(D)_2606N(L)` 로 나눈다 — 다른 배가 양하 폴더·선적 폴더를 따로 갖는 것과 같은 취급. **서류 표기는 `2606N` `2606N` 그대로**(파일명 규칙 `fileNameFor` 는 손대지 않았다).

**검증.**
- 실데이터 시뮬 43항목 PASS — 실 records 로 778 재현 → 수리 후 양하 371 / 선적 407, 집합까지 원본과 일치.
- **파급**: RTDB 17항차 전 섹션에서 제외 0대(현재 저장분은 이미 깨끗 — 수리가 멀쩡한 데이터를 건드리지 않는다). EDI 평택분 카운트 8섹션 전부 수리 전후 동일(231/600/83/145/371/407/96/395).
- `bash build.sh` 성공 · 연막검사 통과 · 번들 grep `TallyOne 1.11`·`isOppositeDirRecord`·`ownDirCns`·`P(Y|YE)ONGTAEK` 확인.

**남은 몫 (MailPilot).** 폴더를 실제로 `2606N(D)` / `2606N(L)` 로 만드는 것은 수집기(`tallybox.py`·항차 폴더 생성부) 몫이다. 앱은 양쪽 이름을 다 찾도록 먼저 열어 뒀으므로 수집기를 고쳐도 기존 메일함이 깨지지 않는다. → `★인계함_밀린작업.md`.

### 2026-08-05 — TallyOne 1.10-01: 인쇄 허브 마커 유실 수리 — **최초의 Fable 지휘·Opus 실행 배포** (커밋 `f92664c`)

- **무엇**: 인계함의 확정 건 — PrintHubModal이 컨테이너 병합을 복제하며 `tagForecastMarks` 호출을 빠뜨려 인쇄물(카고플랜·검수 리스트)에 ▲긴급·🧳수화물 마커가 안 찍히던 버그. 함수를 `utils.js`로 승격(export)하고 VoyagePage(import 교체)·PrintHubModal(게이트 규칙 동일 적용: `_fc.mode===modal mode`) 배선.
- **작업 방식 (작업표준 6-B 첫 실전)**: Fable이 호출부 전수 확인 후 작업지시서 2장으로 Opus 하위 작업자에게 위임 — ①구현+문법+순수함수 시뮬 ②버전 올림+빌드+payload+워처 배포+blob 대조. Fable은 지시·대조 검수·라이브 확인만.
- **⚠ Opus가 잡은 사고**: 지시서의 payload 명령(`git diff --name-only`)이 **untracked 새 해시 자산 6개를 놓침** — 그대로면 index.html이 없는 청크를 가리켜 라이브 백지. Opus가 범위 내 판단으로 6건 추가(19개 패키징) 후 보고. 지시서 템플릿 교훈: payload 목록은 `git status --porcelain`(untracked 포함)이 정본, `git diff`는 쓰지 않는다.
- **검증**: 순수함수 시뮬 11항목 PASS · esbuild 3파일 0에러 · build.sh 연막검사 통과 · blob 19/19 · 라이브 sw `TallyOne 1.10-01`·새 청크 `index-D-NN1-ru.js` 확인. LoadingPlanEdit·planedit는 안 건드림(사용자 결정 대기 유지).
- **사용량**: Opus 3위임 합계 약 115k 토큰(실증 24k+구현 43k+배포 48k) — 전부 Fable로 했다면 Fable 소모였을 몫.

### 2026-08-05 — TallyOne 1.10: 손상 기록은 **있었는데 텔리가 안 읽었다** (커밋 `966f406`)

**사건.** 1.9 로 남의 손상을 지웠더니 이번엔 서류가 통째로 비었다. 검수사 신고 — "다시 만들었는데 데미지 리포트가 작성 안 되었습니다."
이어서 사진 카드 두 장을 보이며 물었다 — "이것이 앱에 기록 안되어 있나요?"

**기록은 되어 있었다.** `archive/STMJ_2643E/photos` 실측 — 5장, 컨 3대.
`SKRU9904609`(08-04 21:26) · `CAAU9599861`(08-04 21:46, LEFT SIDE) · `SKHU6312247`(08-05 07:21·07:22·07:29, LEFT SIDE), 전부 DENTED.
필드는 `cn·damageTypes[]·damageParts[]·note·equip·mode·message·data`. 저장에는 아무 문제가 없었다.

**끊긴 곳은 텔리였다.** 셋 다 코드로 확인했다.
1. `tallyReport.js:499` 가 `voyage.reports` 만 넘긴다 — 그것도 **타임시트용**이다. 손상은 `photos` 에 있는데 텔리 경로가 그 노드를 **한 번도 안 읽었다.**
2. `tallyTemplateMap.js` 에 `DM-IN`·`DM-OUT`·`DAMAGE-*` 키가 0건 — 본문을 쓰는 코드가 없었다.
3. `tallyFormats.js` 의 `damage: 'each'|'report'` 는 **읽는 곳이 0건인 죽은 필드**였다.

**★ 방향은 컨 소속으로 판정한다 — 카톡 표시를 믿지 마라.**
사진 카드에는 세 대 모두 `📍 STMJ 2644W`(선적)로 찍혀 있고 `photos.mode` 는 `'unknown'` 이다.
그런데 실자료를 뒤지니 **셋 다 2643E 양하 컨**이었다(`STMJ 2643E KRPTK.ASC` — 전부 `TAO→PTK`, 40HC/40HR, F).
그래서 `loadSet/disSet` 에 컨이 있는지로 먼저 가르고, 없을 때만 `photos.mode` 를 본다.

**고친 것.**
- `tallyReport.js` — `buildDamage(voyage, disCs, loadCs)` 신설, `computeTallyData` 에 `damage` 배선.
  같은 컨의 같은 자리·같은 종류는 **사진만 여러 장**이므로 한 건으로 묶는다(실측: SKHU6312247 세 장 → 1건).
- `tallyExcel.js` — DM-IN·DM-OUT 본문 + 개별 손상보고서 기입. **좌표를 박지 않는다.**
  DM 시트는 19개 템플릿이 전부 같은 구조(머리글 10행, A PORT·B B/L NO.·C:E MARKS·F:I CONTENTS·J PKGS·K TYPE·L EXCEPTION)임을 전수 확인해 머리글을 찾아 쓴다.
  DAMAGE 블록은 8개 템플릿 실배치를 대조해 두 규칙으로 통일했다 —
  ① 값 칸 = **라벨 병합이 끝난 다음부터 첫 병합 마스터**(없으면 바로 다음 칸). `A8:B8→C8` · `A8+C8:E8→C8` · `A8+병합없음→B8` 셋 다 실물과 일치.
  ② FULL/EMPTY 칸 = `SEAL NO` 라벨 병합이 끝난 **바로 다음 칸**.
  ⚠ **씰번호는 쓰지 않는다.** 실물(2639E)에서 `SEAL NO.` 뒤는 곧바로 FULL/EMPTY 칸이고 씰 값 칸 자체가 없다 — 여기 쓰면 FULL 을 덮는다(1차 시뮬에서 실제로 덮었다).
- `PhotoReportModal.jsx` — **POINT 수·치수(가로 x 세로 x 깊이) 입력 추가.** 둘 다 선택이고 비우면 그 부분만 문구에서 빠진다.
  적으면 `L/SIDE PANEL 1 POINT DENTED ( 80 x 120 x 20 )`, 비우면 `L/SIDE PANEL DENTED`. 화면에 서류 문구 미리보기를 함께 띄운다.
  검수사 확인 — 제미나이 자동 판독은 **호출 횟수 초과(비용)로 제거**했고 되살리지 않는다. 1,078개 커밋 전체를 뒤져도 판독 함수 흔적은 0건이다.
- 부위 표기 매핑 `DMG_PART_LABEL` — 앱 코드값(`LEFT SIDE`)은 화면용이고 서류는 `L/SIDE PANEL` 이다. 오라클은 2639E 실물 문구.

**검증.** 실데이터 시뮬(STMJ 2643E 실 photos 5장 + 실 ASC 컨정보) — 양하 3건·선적 0건 분류 정확, 사진 3장 1건 병합 PASS,
DM-IN r12~14 · DAMAGE 블록 3장(C8 컨번호·G8 FULL·C9 OPERATOR·B35 문구) 전부 2639E 실물과 같은 배치.
**19개 템플릿 전 선박 회귀 — 예외 0**(템플릿 없는 배는 STANDARD 폴백까지 확인). LibreOffice PDF 렌더 육안 확인.
blob 78/78 · 라이브 `sw.js VERSION = TallyOne 1.10` · 번들에 `L/SIDE PANEL` 2건 · `손상 개소` UI 1건 · tallyExcel 청크에 DM 기입 코드 확인.

**남은 것.** 이미 찍은 3건에는 치수가 없다(입력 칸이 없던 때라 비어 있다) — 그 건은 치수 없이 나간다. 다음 촬영분부터 채워진다.

### 2026-08-05 — TallyOne 1.9: 텔리 템플릿에 **남의 항차 자료가 박혀 있었다** (커밋 `cff5b4f`)

**★ 다음 클로드 필독 — 템플릿은 실물 리포트를 그대로 떠서 만든 것이라 이전 항차 데이터가 남아 있다.**

**사건.** 검수사가 STMJ 2643E 마감 텔리의 리퍼 시트를 보고 물었다 — "양하분 24대 외에 다른 리퍼는 어디서 온 건가요?"
RF 시트 2/2쪽 25대 + 별지 1대 = **26대가 이번 항차 자료 어디에도 없었다.** MAILBOX 전체를 훑으니
`STMJ/2639E/STMJ 2639E&2640W PTK TALLY REPORT.xlsx`(7/22 마감)에 26대가 통째로 있었다.
온도·`"` 표기·SKR/SIT 리마크까지 1바이트 동일. 진짜 뿌리는 **`public/tally_templates/STMJ.xlsx` 파일 자체**였다.

**왜 안 지워졌나 — 구조.**
- 본문은 `tallyTemplateMap.js`에 정의된 시트만 쓴다. STMJ `rf: {dataStart:12, dataEnd:47}` = **1페이지 범위뿐.** 61행 아래는 손대지 않는다.
- `DM-IN`·`DM-OUT`·`DAMAGE-EACH`·`DAMAGE REPORT`는 **템플릿맵에 키가 0건** — 본문을 아예 안 쓴다.
- 그런데 **V9.19-06 「전 시트 헤더 자동 기입」**(`tallyExcel.js:944`)이 모든 시트의 `VOY. NO. :`·`DATE :` 라벨을 찾아 값만 갈아끼운다.
  → **머리글은 이번 항차로 갱신되고 본문은 이전 항차 그대로.** 겉보기엔 이번 항차 서류라 더 위험했다.
  STMJ 2643E 서류에 2639E 손상 3건(FBIU5201557·SKHU6432073·HLHU8207222)이 실려 나갔다.

**전수 점검 결과 — 19개 중 15개 오염, 남의 컨테이너 246대.** `STANDARD.xlsx` 37대는 템플릿 없는 전 선박 폴백이라 파급이 가장 컸다.
깨끗한 것은 NSDC·OBWH·SWSP·TNJP 4개뿐이었다.

**고친 것 (1429칸 비움 + 미관 정리).**
1. 전 템플릿에서 이전 항차 잔재 제거 — 컨번호·온도·씰·손상내역·OPERATOR 선사코드. 양식 라벨·SYMBOLS 범례·서명란·보관지침은 보존.
2. **실번호 시트 A:B 병합 코드 신설**(`tallyExcel.js` seal 블록) — 머리글 `A11:B11`은 병합인데 데이터 행은 아니어서
   `SKHU8912132`가 `HU8912132`로 앞 두 글자가 잘려 나갔다(검수사 사진 신고). RF 시트 D:E와 같은 스타일 보존 병합.
3. 박스(테두리) 있는 데이터 칸 1563칸 중앙정렬 — 검수사 확정 "셀 안에 있는 것(박스가 있는 것)만 중앙정렬, 서식은 유지".
4. 머리글 값 831칸 **왼쪽정렬+들여쓰기 1** — 값이 병합 칸 가운데 떠서 라벨과 벌어져 보이던 것. 들여쓰기 없이 왼쪽만 주면 이번엔 라벨에 바짝 붙는다(렌더로 확인 후 교정).
5. Time Sheet `C:H` 병합 결손 33개 채움(데이터 블록 마지막 2~3행이 빠져 있었다).
6. 행높이 145행 확보 — DM-IN 머리글이 **폰트 12pt에 행높이 12.0**이라 `NO. OF PKGS`가 선에 붙었다(검수사 사진). 폰트×1.35로.

**⚠ 함정 — XML 직접 편집 시 네임스페이스 접두사를 반드시 보존한다.**
템플릿 7개(DJCF·DXQD·STMJ·STSE·TMPZ·TNJP·YKTD)에 컨테이너 도면이 들어 있어 openpyxl 재저장은 쓸 수 없다(도면 손실).
XML을 직접 고쳤는데 `ET.register_namespace`로 기본 NS만 등록해 `<drawing r:id="rId1"/>`가 `ns3:id`로 바뀌었고,
**exceljs가 `drawingRel.Target`을 못 읽어 도면 있는 템플릿 7개를 통째로 열지 못했다.** 앱이 텔리를 아예 못 만들 상태였다.
→ 원본 XML 선두의 `xmlns:*` 선언을 전부 `register_namespace`로 등록한 뒤 직렬화한다.
**빌드는 성공했다. 실데이터 시뮬(exceljs 19개 읽기)이 아니었으면 그대로 나갔다.**

**검증.** exceljs 19/19 읽기 OK · `r:id` 보존 확인 · 잔여 컨번호 0건 · 도면 7파일 보존 ·
LibreOffice PDF 렌더로 수정 전/후 눈 확인(컨번호 `HU8912132`→`SKHU8912132`, 손상 3건 사라짐, `NO. OF PKGS` 선에서 떨어짐) ·
blob 76/76 일치 · 라이브 `sw.js VERSION = TallyOne 1.9` · 번들에 seal 병합 코드 1건 · 라이브 STMJ.xlsx 277,142바이트 = 로컬 일치.

**남은 것 (인계함에 등록).** 앱은 손상을 `voyages/{key}/photos`에 **기록하고 있다**(STMJ 2643E 실측 5장·컨 3대).
그런데 텔리는 `reports`만 읽고(`tallyReport.js:499`, 타임시트용) `photos`는 한 번도 안 읽는다.
`tallyFormats.js`의 `damage: 'each'|'report'` 필드는 정의만 있고 **읽는 코드가 0건인 죽은 필드**다.
드로잉 폴백 `sheetDM`(`tallyExcel.js:183`)도 빈 표만 그린다. → `photos` → DM·DAMAGE 시트 연결은 신규 기능이라 관문을 타야 한다.

### 2026-08-05 — TallyOne 1.8-19: 해치커버는 **카톡이 정본**이다 (커밋 `f942078`)

**★ 오늘 가장 중요한 규칙. 다음 클로드는 이것부터 읽어라.**

검수사 확정:
> "실제 연시간은 22:47 입니다. 그후에 00:51 은 텔리 테스트 하기위해 제가 앱기록을 한것입니다.
>  그런데 **자동으로 테스트를 할려고 하니 14번커버를 안열었다고 열어야 한다는것입니다.**"
> "그래서 카톡 자료를 넣었을때 **순서별로 정리 해주는게 클로드의 할일** 입니다, 중복안되게"

**사건의 뿌리 — 앱은 카톡 보고를 모른다.** 검수사들은 해치커버를 카톡 작업방에 손으로 보고한다.
그 기록은 마감 때 「카톡 가져오기」로 들어오기 전까지 앱 안에 **없다.** 그런데 유도(자동) 모드는
모르는 상태로 *"이 커버를 여세요"* 라고 **단정**했고, 사람은 앱이 시키는 대로 눌렀다.
STMJ 2643E 실측 — **자동 모드가 낸 해치 보고 네 건이 전부 오보**다.

| 앱이 찍은 것 | 실제 (카톡) |
|---|---|
| 00:51 `13,14,15` 오픈 | 22:47 에 이미 열림 |
| 00:52 `09,10,11` 오픈 | 23:15 에 이미 열림 |
| 03:05 `14` 오픈 | 03:01 에 닫음 |
| 08:19 `05,06,07` · 08:20 `01,03` 오픈 | 03:17 · 02:28 에 닫음 |

⛔ **앱이 요구하지 않았으면 아무도 누르지 않았다.** 사람 탓이 아니다.

**규칙 (buildTimeSheet 0단계).**
> **카톡 기록이 있는 그룹은 카톡만으로 상태를 정한다. 그 그룹의 앱 보고는 메아리라 뺀다.**
> 카톡 기록이 **없는** 그룹은 손대지 않는다 — 앱 보고가 유일한 진실이다(STMJ 26번·18번).
> 조용히 버리지 않는다 — `rows._echo` 로 몇 줄 뺐는지 돌려준다. 앱 기록 자체는 지우지 않는다.

**STMJ 2643E 결과** — 오픈 6·클로즈 5, 시각 미기재는 **09&11 하나**뿐.
검수사: *"이건만 시간 미기재로 남으면 됩니다. 검수사의 오류이니까요"* (최검수사 보고 누락, 서류 정리 때 이미 확인된 건)

**덤 — 데크 화물 오픈 차단.** 검수사: *"화물이 있는상태에서 커버를 연다고 열리면 안되니까요"*
커버 위 데크에 컨테이너가 있으면 오픈을 권하지 않고, 눌러도 몇 대인지 보여주고 되묻는다.
⚠ 종전엔 **평택분만** 셌다 — 통과화물이 얹혀 있으면 못 여는데 "데크 비었다"고 봤다.
(평택분 필터 미적용 오집계 9.2-② 의 **거울상**: 카운트엔 평택분만, 물리 판단엔 배에 있는 것 전부.)

---

### 2026-08-05 — TallyOne 1.8-18: 끝난 작업에 앱이 해치 보고를 권했다 (커밋 `721b730`)

**사건.** STMJ 2643E 타임시트에 `08:19 OPEN 05,06,07` · `08:20 OPEN 01,03` 두 줄이 있었다.
그 커버들은 이미 02:28·03:17 에 닫힌 것이라 "닫고 다시 연 채로 출항"이 된다 — 불가능한 서류다.
검수사: *"08시건은 오염 자료 입니다."* / *"06:30 에 퇴근을 했습니다. 그이후에 앱이 혼자 기록이 가능할수가 없습니다"*

**누가 썼나 — 추측하지 말고 `inspectors` 노드를 읽는다.**
```
최관식  loginAt 08:19:08 · workAt 08:20:42 · loggedOutAt 08:21:07
        lastVoyage STMJ_2643E · lastMode discharge
보고     08:19:54 open 05,06,07 · 08:20:25 open 01,03   ← 그 2분 안
```
다른 검수사가 2분 로그인한 사이에 찍혔다. 클로드 아님(그 시간대 브라우저·화면 조작 0건, 전사 확인).
`sendHatchReport` 는 버튼을 눌러야만 도는 경로다 — 앱이 혼자 쓸 수는 없다.

**왜 앱이 권했나 (진짜 원인).** 유도 프롬프트가 **남은 컨테이너 수만** 봤다.
검수원이 앱으로 컨테이너를 일일이 체크하지 않아 `remaining` 이 늘 남아 있다
(검수사 확정: *"검수원이 이 앱을 사용 안 한다는 이야기입니다"*).
그래서 양하가 02:16 에 끝난 뒤에도 앱은 "홀드가 남았다"며 오픈을 권했고, 들어온 사람이 눌렀다.

**고침 — 판단 근거를 하나 더 둔다.** `modeFinished` = 완료 플래그 **또는 `{mode}_done` 보고**.
사람이 명시적으로 누른 것이라 믿을 수 있다. 끝난 모드면 프롬프트를 띄우지 않고,
버튼이 어떤 경로로든 눌리면 **조용히 막지 않고 확인을 묻는다**(진짜 뒤늦은 보고일 수 있다).

**그리고 지우개.** 이미 들어온 오염 기록을 앱에서 지울 수단이 없었다.
「작업 기록 보강 · 정리」 모달에 **기록 정리** 탭 추가 — 시각·출처(앱/카톡)를 보여 주고 한 건씩 삭제(확인 1회).
판단은 수석이 한다. 앱은 진짜와 오염을 구분하지 못한다.

⚠ **실 Firebase 쓰기 금지 원칙대로, 클로드가 밖에서 지우지 않았다.** 지우개를 앱에 넣고 수석이 지운다.

---

### 2026-08-05 — TallyOne 1.8-17: 타임시트 시각순·중복 접기·재오픈 / RF 시트 분리 (커밋 `d8ef61e`)

검수사가 실제 출력물을 붙여 넣어 네 가지가 드러났다.

1. **시각이 뒤죽박죽** — `COMMENCED DISCH'G` 등 work_status 줄에 **`ts` 를 안 넣었다.** 정렬이 `(a.ts||0)` 라 전부 0으로 맨 위에 몰렸다. 해치 줄만 `ts` 가 있었다. → work_status 줄에도 `ts` 부여.
2. **중복** (검수사: "중복이 많은듯 한데요?") — 앱 보고와 손으로 친 카톡 보고가 같은 사건을 두 번 적는다. **지우지 않고 접는다** — 사이에 반대 동작이 없으면 같은 사건이다. STMJ 4줄 접힘(OPEN 26·13,15·9,11 재보고 + COMMENCED DISCH'G 재보고).
3. **재오픈 커버를 놓쳤다** — 1.8-16 은 `closed` **Set** 으로 봐서 "한 번이라도 닫혔으면 끝"이었다. 그런데 03:05 BAY 14, 08:19 BAY 05,06,07, 08:20 BAY 01,03 은 닫았다가 **아침에 다시 열었다**. → **마지막 상태**로 판단한다. 추론 1건 → **4건**으로 늘었다.
   추론 표기는 **닫힌 뒤 처음 열린 보고**의 베이 표기를 쓴다(화면에 찍힌 `BAY 09,11` 과 어긋나지 않게).
4. **베이 자릿수** — 앱은 `9,11`, 손 카톡은 `09,10,11`. 두 자리로 통일.

덧: 완료 처리됐는데 `COMPLETED` 줄이 없는 모드도 커버와 같은 이치로 **시각만 비워** 채운다.

**RF 시트** (검수사: "양하 리퍼 점검 대상은 24대인데 다른건 어디서 온것인가요?")
`sheetRF` 가 `[...D.rfIn, ...D.rfOut]` 을 한 덩어리로 쏟아부어 **양하 공리퍼 + 선적 리퍼**까지 섞였다.
→ 양하 **풀 리퍼**만 본문(`fe !== 'E'`), 선적 리퍼는 버리지 않고 `LOADING` 구분줄을 넣어 따로.
선사 양식 경로·리퍼 메모 화면·출항 임박 경고와 **같은 기준 — 네 곳 일치**(지침서 5-5).

---

### 2026-08-05 — TallyOne 1.8-16: 완료 처리는 곧 커버가 닫혔다는 뜻 (커밋 `79476c7`)

**검수사 확정 — 추론의 근거와 한계를 함께 주셨다.**
> "기본 선적은 홀드 다 채우고 데크를 채웁니다. 6번 해치를 닫고 14번도 닫았으면 10번은 닫았다는 보고가 없습니다. 그런데 앱상에서 10번 데크 컨테이너가 실렸습니다. 이럴땐 아 10번이 닫혔구나. 그런데 앱은 시간을 모릅니다. … 그럼 보고서엔 **시간만 빼고 기록**합니다. 수기로 나중에 시간 기록을 할 수 있게"
> "그건 찾을 필요가 없습니다. **완료처리 했다는게 중요** 합니다."

**규칙**: 커버를 안 닫으면 데크에 못 싣고, 못 실으면 완료가 안 된다.
→ **완료 처리된 모드**에서 오픈 기록만 있고 클로즈가 없는 그룹은 **닫힌 것으로 본다.**
→ 데크 적재를 일일이 뒤질 필요 없다. 완료 플래그(`dischargeDone`·`loadingDone`·`inspectorDone`)가 근거다.

⚠ **시각은 지어내지 않는다.** 순서만 맞춰 마지막 클로즈 뒤에 놓고 시각 칸은 **비운다**(`inferred:true`).
   `HATCH COVER CLOSE (BAY 9,11)   ※ 시각 미기록` — 수석이 나중에 수기로 채운다.
**검증**: STMJ 2643E 실기록으로 돌려 **09&11 하나만** 추론됐다 — 실제 보고 누락과 일치.

**세 층으로 메운다.** ①앱 기록(가장 정확) → ②카톡 기록 보강(1.8-15) → ③완료 처리 추론(1.8-16, 시각 없음).

---

### 2026-08-05 — TallyOne 1.8-14 / 1.8-15: 마감 텔리 타임시트·리퍼 시트 + 카톡 기록 보강 (커밋 `7366f1a` · `6994e10`)

**신고(검수사)**: 마감 텔리 타임시트에 **커버가 열린 상태로 마감**됐다 — 불가능한 서류다. 리퍼 시트는 어수선하다.

**1.8-14 — 타임시트에 하역 시작·완료가 통째로 빠져 있었다.**
`buildTimeSheet` 이 `action === 'start'`·`'complete'` 를 찾았는데 실제 저장값은
`discharge_start`·`discharge_pause`·`discharge_done`·`loading_*` 여섯 가지다
(`kakaoShare.buildWorkStatusMessage` 의 labels 가 단일 소스).
→ STMJ 2643E 실측: 타임시트에 **해치 10줄만** 있고 COMMENCED/COMPLETED 가 0줄. 마지막이 08:20 오픈이라 열린 채 끝나 보였다. 수정 후 하역 시작·완료 4줄이 살아났다.

**1.8-14 — 리퍼 시트 결함 셋.** ①양하·선적 리퍼를 한 표에 섞음 ②공 리퍼·리퍼드라이 포함(온도 없는 빈 줄) ③Actual(G열)이 비고 REMARKS(I열)에 **선사코드**를 넣음.
→ 양하 F 리퍼만, 실제온도 채움, I열은 DG 표기. 시험 6줄 → 3줄.

**1.8-15 — 카톡 작업방 기록으로 타임시트를 메운다.**
검수사 확정: "마감텔리후 부족할걸 카톡 메시지 복사로 해결. 타임시트를 카톡메시지로 보강, 예보처럼."
`src/kakaoWorkLog.js` 신설 + 수석 대시보드 마감 텔리 줄에 「📋 카톡」.
붙여넣으면 파싱 → 앱 기록과 대조 → **빠진 것만** 골라 `archive/{key}/reports` 에 넣는다(`fbAddReportsAt` — 과거 시각 그대로 저장. `fbAddWorkReport` 는 ts 를 '지금'으로 덮어 못 쓴다).

**베이 표기 정본 (검수사 확인 2026-08-05) — 검수사마다 다르다.**

| 방식 | 예 | 뜻 |
|---|---|---|
| 짝수 단독 | `26번베이` `18번 베이` | 그 슬롯 |
| 홀수 묶음 | `13&15` `13/15` `13-15` `13_15` `13,15` `13 15` | 사이 짝수가 중앙(13-14-15 = 슬롯 14) |
| 세 개 이상 | `17,18,19` `1 2 3` `1-3` | 같은 슬롯 |
| 장수 | `2장` — **번호 앞에도 뒤에도 온다** | 적힌 대로 쓴다 |

⚠ 장수는 **추론하지 않는다**. 같은 홀수 쌍이라도 슬롯마다 다르다(실측: `01&03`=1장, `13&15`·`09&11`·`05&07`=2장).
⚠ 띄어쓰기 표기(`1 2 3`)가 있어 **장수·장비를 먼저 걷어내고** 남은 숫자만 베이로 읽는다. 안 그러면 "커버 2장"의 2 를 베이로 읽는다.
해치 낱말: `오픈`·`H/O`·`OPEN` / `클로즈`·`크로즈`·`H/C`·`CLOSE`. 장비가 **다음 메시지에 따로** 오기도 한다(22:16 "2호기") → 2분 이내면 직전 항목에 물려준다.

**검증 — 실물로 확인했다.** STMJ 2643E 카톡방 전수(47줄): 읽음 17건·무시 19건(사진·잡담), 앱에 없는 것 9건. 표기 변형 25건 전건 통과.
**결정적 대조**: 카톡 기록을 넣으면 열린 채 남는 슬롯이 **15개 → 1개(09&11)** 로 준다. 그 하나는 **실제 보고 누락**이었고 검수사가 "최검수사의 실수로 서류 정리 때도 이야기가 나왔다"고 확인했다. 사람이 나중에 발견한 누락을 대조가 그대로 짚어낸 것이다.
→ 별도 경고 화면은 만들지 않는다. **타임시트에 오픈만 있고 클로즈가 없으면 그게 이미 그 정보다**(검수사 판단).

### 2026-08-05 — TallyOne 1.8-09: 수동으로 닫은 해치를 자동 유도가 몰랐다 (커밋 `7df2f2b`)

**신고(검수사)**: 해치커버 CLOSE 를 **수동으로** 카톡 보냈는데, 자동모드로 돌리니 "커버를 닫을 건지" 되물었다. 수동으로 닫은 걸 인식 못 함(STMJ 2644W 베이 18).

**원인 — 두 경로가 서로 다른 곳에 썼다.**

| | 카톡 | `reports` | **`info.hatchDone`** |
|---|---|---|---|
| 자동 유도(GuidedWorkPanel) | ✓ | ✓ | **✓ markHatchDone** |
| 수동 보고(WorkReportModal) | ✓ | ✓ | **✗** |

자동이 "닫을까요?"를 안 묻는 근거는 오직 `voyage.info.hatchDone["{mode}_{center}"]` 다. 수동 보고는 그 표시를 안 찍었다.

**고친 것**: 수동 보고도 같은 표시를 찍는다. 그룹 계산은 `swapGrade.bayGroupCenter` 하나로 합쳐 두 곳이 각자 계산하다 갈라지는 일을 없앴다(종전엔 `GuidedWorkPanel` 안에만 있어 수동 쪽이 같은 키를 만들 수 없었다).

**베이 그룹 규칙(실측 확인)**: `twin.js buildBayPairs` 는 **홀수 → 홀수** 사전이다(`pairs['17']='19'` — 17·18·19 가 한 슬롯). 그래서 중앙값이 곧 사이의 짝수 베이이고 그것이 그룹 대표다.
검증 13건 PASS — 베이 18/17/19 → `18`, 05/6/07 → `6`, 0패딩 입력(`05`)도 같은 키.
⚠ 처음 시뮬에서 사전을 '홀수→짝수'로 **지어내** 2건이 FAIL 났다. 코드는 맞았고 전제가 틀렸다.

**교훈 — 오늘 하루 반복된 것**: 시뮬 데이터를 **실제 구조를 열어보지 않고 지어내면**, 표본을 아무리 늘려도 전제가 틀린 채 전건 통과한다. 사전·자료 구조는 반드시 원본을 열어 확인하고 만든다.

### 2026-08-04/05 — TallyOne 1.8-08: 카톡 사진 보고가 27바이트 문자열로 가던 것 (커밋 `d854253`)

**신고(검수사)**: 손상 사진을 앱에 올려 카톡으로 보내면 **이미지가 깨져서** 온다(갤럭시). 글자도 안 갔다.

**원인 — 인터페이스 불일치를 조용한 catch 가 덮고 있었다.**
`M5.77` 에서 사진을 2장 보내도록 **호출부만** 바꿨고 함수는 그대로였다.
```
PhotoReportModal:86   shareWithPhoto(message, [cnBlob, detailBlob], ...)   ← 배열
kakaoShare:25         URL.createObjectURL(photoBlob)                       ← 단일 blob 기대 → TypeError
                      catch → composedBlob = photoBlob (배열 그대로)
                      new File([[blob, blob]], 'report.jpg', {type:'image/jpeg'})
```
`File` 생성자는 내부 배열을 BlobPart 로 못 보고 **문자열로 변환**한다.
**재현 실측**: 사진 4.0 MB 두 장 → `27 B`, 내용 `"[object Blob],[object Blob]"`. 카톡이 이미지로 못 연다.

**고친 것**: 배열·단일 둘 다 받아 **장마다 따로 합성해 files 에 나란히** 담는다(`report1.jpg`·`report2.jpg`).
합성 실패해도 원본을 `image/jpeg` 로 **타입 위조하지 않고** 무엇이 실패했는지 로그에 남긴다.
자막을 사진에 이미 넣었으므로 `text` 는 넘기지 않는다 — 카톡이 텍스트를 우선 처리하면 사진이 첨부로 안 붙는다(검수사 실측: 글자는 안 가고 이미지만 갔다).

**교훈 3가지 — 오늘 하루에 같은 계열이 여러 번 나왔다.**
1. **호출부만 고치고 함수를 안 고치면 조용히 깨진다.** 고치기 전 저장소 전체 grep(3금지 1번)이 이걸 막는다.
2. **빈 catch 는 결함을 감춘다**(3금지 3번). 여기선 TypeError 를 삼켜 "깨진 파일 전송"으로 바꿨다.
3. **타입을 단정하지 마라.** `new File([x], 'report.jpg', {type:'image/jpeg'})` 처럼 내용과 무관하게 타입을 박으면 받는 쪽이 열다가 실패한다.


### 2026-08-04 — TallyOne 1.8 / 1.8-01: 리퍼 온도 확인 + 텔리 RF 3칸 + 리스트 재업로드 병합 (커밋 `f2fa5d4` · `6f2e134`)

**요구(검수사)**: "리퍼 리스트 안에 실제 온도를 기록할 수 있게 만들고 텔리 리포트 안에 실넘버와 셋팅 온도와 실제 온도가 기록되게 해야 함."
설계 확정: "작업전 먼저 선박을 선택합니다. 그러면 앱은 리퍼 유무를 판단하고 있으면 리퍼메모 화면을 띄워 줍니다. 메모화면엔 컨테이너 넘버 EDI온도 실제 셋팅온도 실제온도 … EDI 온도는 수정불가 나머지 2개는 수정가능."
그리고 "리퍼가 많으면 일일이 확인 불가함 → **선원이 체크한 리스트를 받아서 앱이 읽어서 기록**"(사진 촬영 방식으로 확정).

**관문 2에서 드러난 것 — 요구 셋 중 둘은 신규가 아니라 결함이었다.**
- 텔리 `ptkContainers()`가 **ediContainers 만** 읽고 records 를 안 봤다. BAPLIE엔 실번호도 리퍼 온도도 없다 — 그 둘은 리스트에서 와서 records 에 있다. 화면(VoyagePage 271·624행)은 진작부터 병합해 보여 주는데 텔리만 안 봤다. 그래서 RF condition report 의 **SEAL NO·Setting 이 상시 공란**이었다. Lug 키 사고(1.3-02)와 같은 계열 — 화면과 텔리가 서로 다른 소스를 보던 문제.
- 표준 RF 시트는 `Actual` 열이 **헤더만 있고 값을 안 넣고** 있었다.

**실측 근거**: TNJP 26355E `SEKU9206423` — ediContainers `sl:"" tmp:""` / records `sl:"LYG438353" tmp:"-18"`.
**RF 시트 좌표(실물 템플릿 병합 실측)**: `A`컨번호 `B`실번호 `C`SIZE `D:E`LOCATION `F:G`TEMPERATURE(**F=Setting, G=Atual**) `H`TIME `I`REMARKS.

**넣은 것**:
1. `components/ReeferMemoModal.jsx` 신설 — 항차에 들어가면 **미확인 리퍼가 있을 때만** 자동으로 뜬다. 확인 완료 후엔 안 뜨고 상단 `❄ 리퍼 온도 확인 · N대` 버튼으로 다시 연다. 채우는 길 셋: ①선원 리스트 **사진 판독** ②`전부 리스트대로`(EDI 온도 일괄 인정) ③개별 수정.
2. `mixerUpload.js ocrReeferTemps()` — Gemini 2.5 Flash. 영하 표기(`18-`·`△18`) 정규화, 숫자로 안 읽히면 **빈칸으로 두고 지어내지 않는다**, 손글씨가 인쇄값 위에 있으면 손글씨 우선(선원이 고친 값).
3. `firebase.js fbSetReeferTemp / fbSetReeferTempBulk` — `records/{cn}.rfSet`(실제 셋팅온도)·`rfAct`(실제온도)·`rfSrc`·`rfCheckedAt`.
4. `tallyReport.js` — `ptkContainers()` 가 records 를 **필드만 보강**(컨 추가 금지 — 추가하면 Final Work·OS·PORTPERFORMANCE 집계가 흔들린다). `buildRF()` 가 `setting`(rfSet ?? tmp)·`actual`(rfAct) 분리. **Actual 은 확인한 것만 찍는다** — tmp로 채우면 재보지도 않은 값이 '실측'으로 서류에 박힌다.
5. `tallyExcel.js` — 페리 템플릿 `getCell(7)=actual`, 표준 시트 `F열=actual`.

**같이 잡은 치명 결함 — 리스트 재업로드가 현장 입력을 지우고 있었다.**
검수사 지적: "앱이 저장을 할때 병합 저장하면 틀린것만 남을텐데 **완전 교체** 하는것 같습니다. 그래서 제가 다시 저장함."
`fbSaveListRecords` → `chunkedReplace` → `set()` 로 records 노드를 통째 갈아엎고 있었다. 온도 열이 없는 리스트를 한 번 올리면 있던 리퍼 온도가 `""`로 덮여 사라지고, 실번호 수정 이력·엠티실·리퍼 확인값도 함께 날아간다. **리퍼 메모를 만들어도 리스트 재업로드 한 번이면 지워질 상태였다.**
→ 병합 규칙 셋: ①새 값이 비면 기존 유지(**무게 `0`도 빈 값으로 취급** — 컨 무게 0은 그 열이 없었다는 뜻) ②현장 입력 전용 필드는 리스트가 못 덮음 ③리스트에서 빠졌어도 검수 흔적이 있으면 안 지움.
⚠ '검수 흔적' 판정은 KEEP 목록과 **다르다**. `sl_orig`·`mkcon` 은 리스트 업로드가 자동으로 붙이는 값이라 흔적이 아니다. 그걸 흔적으로 세면 리스트에서 빠진 컨이 영영 안 지워지고 재업로드마다 죽은 컨이 쌓인다(시뮬에서 잡음).

**검증**: 병합 규칙 실데이터 시뮬 9/9 PASS(첫 판에 2건 FAIL → 무게 0·흔적 판정 교정). RF 빌드 시뮬 6/6 PASS(수리 전 전부 공란 → 수리 후 SEAL·Setting 채워지고 Actual 은 확인분만). 빌드 PASS, 번들 grep(`선원 리스트 촬영`·`전부 리스트대로`·`rfSet`×10·`ocrReeferTemps`), 라이브에서 `❄ 리퍼 온도 확인 · 24대 / 미확인 24대` 렌더 확인(STMJ 2643E).

**1.8-01 (같은 날 즉시 수리)**: 1.8에서 모달을 파일 아래쪽 **`DataTab`(업로드 탭) 안에** 넣어 상태는 켜지는데 그릴 곳이 없어 아무 일도 안 났다. VoyagePage 트리 안(800행)으로 옮김. **한 파일에 컴포넌트가 여럿이면 렌더 위치를 행 번호로 확인할 것** — 배포 스크립트에 범위 검사(82~1486)를 넣어 재발을 막았다.

---

### 2026-08-04 — TallyOne 1.7: 마감 서류를 TALLYBOX에 직접 저장 (커밋 `bdb5e46`)

**요구(검수사)**: "후 …크롬은 폴더를 지정해야 하고 엣지는 다운로드로 가고 일관성이 없음."

**실측**: 검수사 크롬은 「다운로드 전 각 파일의 저장 위치 확인」이 켜져 있어 저장창이 뜨고 **기본 폴더가 MAILBOX 루트**다. 엣지는 말없이 다운로드 폴더로 떨군다. 브라우저 다운로드는 앱이 통제할 수 없다. (이것이 마감텔리가 MAILBOX에 쌓여 있던 원인이기도 하다.)

**넣은 것**: `tallyboxFs.js` 신설 — File System Access API 로 TALLYBOX 루트를 **한 번만** 지정받아 IndexedDB 에 보관(`mailboxFs.js` 와 같은 DB·다른 키). 그 뒤로는 앱이 `TALLYBOX/{선박}/{항차}/` 를 스스로 만들고 파일을 그 자리에 쓴다. 저장창 없음·다운로드 폴더 안 거침·크롬과 엣지가 같게 동작.
`data/tallyBoxRules.js` — 선박별 파일명·폴더명 규칙을 앱에도 이식(수집기 `tallybox.py` 와 **같은 규칙, 둘 다 고칠 것**). `generateTallyExcel(D, {download:false})` 옵션 추가. 마감 텔리·실선적 EDI·ASC 셋 다 이 경로를 쓰고, 미지원 브라우저·미지정 시 **기존 다운로드로 폴백**.
⚠ Chrome/Edge 데스크톱 전용(사파리·파이어폭스 미지원). 검수사 확정 "폰에서는 생성 안 한다"와 맞는다.

**검증**: 실측 11척 조합으로 경로·파일명 전건 생성. 라이브에서 `📁 TALLYBOX 폴더 지정 (처음 한 번만)` 렌더 확인.

---

### 2026-08-04 — TallyOne 1.6 / 1.6-01: 마감 텔리는 수석 완료 저장한 항차만 (커밋 `2a487c7` · `23d7fdd`)

**사고**: 수석이 마감 텔리 「엑셀 생성」을 눌렀는데 **엉뚱한 항차**가 나왔다. 목록이 `info`만 있으면 전부 넣고 `createdAt`(등록 시각) 내림차순으로 세웠다. 그래서 **TNJP 26356E&26356W**(오늘 12:22 예정 등록, 입항 8/6, 작업한 적 없음)가 맨 위, 실제 작업·완료한 **TNJP 26355E&26355W**는 13번째. 같은 TNJP 두 줄 중 위엣것을 눌렀다.

**검수사 확정 흐름 — 「수석 완료 저장」이 마감의 방아쇠다.**
`작업중/검수 완료 → (잠김) ─ 수석 완료 저장 ─→ 보관소 이동 → 마감 텔리 활성`
"완료 처리가 안되면 눌리면 안됩니다. 수석이 완료로 저장하면 마감텔리에 불이 들어 오게."
최종형은 **완료 저장 즉시 텔리·마감자료가 TALLYBOX로 자동 생성**이고 보관은 **1년**(`fbCleanupArchive(365)` 이미 있음). 지금은 기능별 시험 중이라 조각을 떼어 둔 상태이므로 **순서만 강제**했다(자동 생성은 당기지 않음).

**⚠ 완료 기록(`discharge/loading.completed`)을 신호로 쓰면 안 된다.** TNJP 26355E 실측 — `dischargeDone`·`loadingDone` 둘 다 true(12:34)인데 `completed` 는 **null**. 현재는 시험 운용이라 컨을 하나씩 확인하지 않고 완료만 누른다(검수사 확인). 앱이 완성되면 한 대라도 남을 때 완료 버튼이 잠기므로 done 플래그가 곧 전량 확인을 뜻하게 된다. 즉 completed 는 지금은 항상 0이고 나중엔 중복이다.

**판정(`tallyTargetState`)**: ①`dischargeDone`/`loadingDone` → `done`(잠김) ②터미널 실적이 작업창에 잡혔거나 작업 시작 시각이 지남 → `working`(잠김) ③보관소에 있음 → `ready`(생성 가능). 그 외 = 입항 전, **목록에 없음**.
**실측 결과**: 등록 20척 → **2척**(TNJP 26355E `done`, DXQD 2630E `working`). 나머지 18척은 예정일 8/5~8/10.

**1.6-01 (같은 날 긴급)**: 1.6이 대시보드 열 때마다 `fbListArchive()`를 돌렸는데 그 함수는 **보관소 키 1건당 get 7회**다. 보관소 160건 = **요청 1,120건**이 한 번에 나가 화면이 멈췄다. → 「수석 완료 저장」 때 `tally_pending/{key}` 에 한 줄 남기고 대시보드는 **그 노드 하나만** 읽는다. 항차 본문은 실제로 엑셀을 만들 때 `fbGetArchiveVoyage` 로 그 한 건만 읽는다.
⚠ `fbListArchive` 에 필드를 늘리지 마라 — 키 1건당 get 이 그만큼 늘어난다.

---

### 2026-08-04 — MailPilot 1.0-06 ~ 1.0-08: 마감 서류 모으기 3픽스 + 폴더 표기 확정

1. **파일명 항차 우선(둔갑 방지)** — `_pair_from_fb` 가 파일명의 항차를 Firebase **현재** 항차로 덮어써서, 다운로드 폴더에 남은 지난 항차 파일이 현 항차 서류로 둔갑했다. 실측: `TNJP_26349W_ACTUAL_BAPLIE.edi` → `TNJP 26356W PTK.EDI`, `SWDN_2608S…` → `SWDN 2608N…`. 마감 서류에서 이건 치명적이다. → 파일명 항차가 정본, Firebase 는 **반대편 다리를 채울 때만** 쓴다.
2. **다폴더 감시** — 크롬 저장창 기본값이 MAILBOX 루트라 다운로드 폴더만 보면 영영 못 잡는다. 다운로드·MAILBOX 루트(move)·바탕화면·문서(copy) 감시. ⚠ MAILBOX는 **최상위만** 본다 — `MAILBOX\{선박}\{항차}\` 의 수집 자료는 건드리지 않는다.
3. **확장자 원본 유지** — 규칙표의 `.xls` 는 선사가 보내온 옛 파일의 것이라, 앱이 만든 xlsx 를 `.xls` 로 이름 붙이면 엑셀이 "형식이 다릅니다" 경고를 낸다.

**항차 폴더 표기 (검수사 확정 — 직접 만든 `TALLYBOX\TNJP\26355E_W` 를 정본으로)**

| 상황 | 폴더 | 예 |
|---|---|---|
| 양하만 / 선적만 | `2643E` / `2634W` | ATPR `2636W` |
| **숫자 같고 방향만 다름** | `26355E_W` | DXQD `2630E_W` · XTPG `535E_W` · RZOR `R083E_W` |
| 숫자가 다름 | `2643E_2644W` | OBWH `2705E_2706W` · DJCF `0149N_0150S` |
| **완전히 같음** | `2608N_2608N` | SWDN · SWRG · KBTR |

축약하는 이유 = 선사가 보낸 실제 서류 제목이 이미 그렇다(`TNJP 26353E&W`, `RIZHAO ORIENT R079E&W`). 같은 항차를 안 줄이는 이유 = 줄이면 한쪽만 있는 항차와 구분이 사라진다(실증 `SWRG 2606N&2606N PTK DEP. TALLY REPORT`).
⚠ 1.0-06에서 클로드가 "중복이니 하나로"라고 임의로 줄였다가 1.0-07에서 되돌렸다. **검수사가 아니라고 한 것을 판단으로 뒤집지 말 것.**

**검증**: 실측 항차 17건 폴더명 전수 PASS. STMJ 3종 세트가 한 폴더에 모이고 선박별 파일명 규칙 적용됨을 실파일로 확인.

---

### 2026-08-04 — TallyOne 1.5: 화면 데이터만 새로고침 + 터미널 자료 신선도 (커밋 `5ef4943`)

**요구(검수사)**: "검수앱에서 리프레쉬 하면 로그인 화면으로 돌아가는데 보고 있는 화면 데이터만 리프레쉬 되게 할 수 없나요?" · 이유 = **터미널 실시간 자료**와 **입출항 자료(수시로 바뀜)**.

**관문 2에서 밝혀진 것 — 데이터는 원래 실시간이었다.** 항차·검수원·PORT-MIS·도선예보·터미널작업·베이사전·하트비트가 전부 `onValue` 구독이고, `terminal_work.{선박}.updatedAt`도 몇 분 전 값이었다. **진짜 문제는 화면이 "이게 언제 것인지" 말해주지 않는 것**이었다 — 믿을 근거가 없어 새로고침하게 되고, `App.jsx:68 const [inspector] = useState('')`가 메모리 전용이라 로그인이 풀린다.

**실측(관문 4)**: 같은 화면에 **9분 전(TNJP·DXQD)과 12시간 전(OBWH)·2일 전(XTPG)**이 구분 없이 섞여 있었다. 죽은 값을 산 값으로 보게 되는 쪽이 더 위험하다.

**넣은 것**:
1. `firebase.js fbReconnect()` — `goOffline()` → 300ms → `goOnline()`. 열린 구독을 전부 끊었다 붙여 최신값 재수신. **페이지를 다시 불러오지 않으므로 로그인이 유지된다.**
2. `components/RefreshDataButton.jsx` 신설 — 진행 중 스피너 + 마지막 실행 시각.
3. **검수사앱(HomePage)** 「📋 예보」 옆, **수석 대시보드** 바로가기 그리드 위 — 두 곳.
4. 수석 대시보드 터미널 줄에 **신선도**: 10분 이내 초록 / 1시간 이내 주황 / 그 이상 빨강 + `← n분 전`. 수집기 사이클이 3분이라 10분이면 이상 신호.

**건드리지 않은 것**: V9.13 30분 무조작 자동 로그아웃. 활동 로그가 검수원 이름으로 남는 구조라 의도가 분명하다.
**성격**: 이 버튼은 "더 최신 것을 당겨오는" 용도가 **아니다**(3분 주기라 화면은 이미 최신). **연결이 끊겼을 때 되살리는** 용도다. 평소엔 누를 일이 없어야 정상이고, 신선도가 빨강일 때 누른다.
**검증**: 빌드 PASS, 번들 grep(`데이터 새로고침`×2 · `goOffline`/`goOnline`×2 · `시각 미상` · `터미널 자료 갱신`), blob 24/24, 라이브 `sw.js` VERSION `TallyOne 1.5`.

### 2026-08-04 — MailPilot 1.0-03: 단독 양하리스트(CDL) 제외 해제 (검수사 확정, 라이브 검증 완료)

**바꾼 것**: `auto.py:3286` 한 줄. `lists_need_edi = is_discharge and (_classify(제목)=="CDL")` → `lists_need_edi = False`.

**종전 규칙(V2.12.0-29)**: 양하 + 제목이 CDL이면 '같은 메일에 EDI 동봉' 또는 '폴더에 이미 EDI 있음'일 때만 엑셀 저장.
**당시 근거(검수사 회고)**: "EDI에 양하 리스트가 다 있는데 굳이 CDL까지 받을 필요가 있나. 그때는 수집기가 정리가 안 돼 **간소화가 목적**이었다."
**해제 근거(검수사 확정 2026-08-04)** — 전제가 바뀌었다: ① 수집기 완성도가 올라와 잡음 걱정이 줄었다 ② **예정은 있는데 자료가 없는** 상황이 실제로 생기고, 그때 CDL이라도 모여야 **총 양하가 대략 몇 개인지 파악**된다.

**실사건**: STMJ 2643E — 8/3 17:17 `EAS SITC MOJI 2643E CDL입니다.` 수신. 판독은 전부 정상(선박 STMJ · 항차 2643E · 분류 CDL)이었으나 폴더에 EDI가 없어 버려졌고 `STMJ\2643E\` 에는 `_예정.txt`만 남았다. 진단 로그 `수집: STMJ 2643E 양하 (메일 1건) → 신규 EDI=X Excel=X`. 같은 배 2644W(선적) CLL은 이 제한이 없어 정상 저장돼 있었다.

**시뮬(관문 4)**: 4개 경우 대조 — 2643E CDL만 '버림→저장'으로 바뀌고 나머지(폴더에 EDI 있는 CDL·선적 CLL·EDI 동봉 양하) 3건은 변화 없음. **회귀 0.**
**라이브 검증**: 재시작(10:01) 후 `STMJ\2643E\` 에 CDL **4장** 수집됨 — `CDL STMJ EAS 2643E.xlsx` / `CDL STMJ 2643E CSC-DWS.xlsx` / `CDL STMJ 2643E DWIC-DWS.xlsx` / `CDL STMJ 2643E TJM.xlsx`. 이전엔 `_예정.txt` 하나뿐이었다.
**호출부 전수**: `_grab` 안 네 분기(일반·zip·eml·클라우드)가 모두 같은 플래그를 보므로 호출부 한 곳으로 전부 덮인다. 복제본 없음. **되살리려면 그 한 줄만 되돌린다** — `_grab`의 파라미터는 남겨 뒀다.

### 2026-08-04 — 등록 경로 도메인 확정 (코드 확인, 변경 없음)

같은 건을 조사하며 확정한 사실들. **다음 클로드는 여기서 재조사하지 말 것.**

- **리스트만 있어도 항차는 등록된다.** `_upload_mode`(autoreg.py:833)에서 `if counts.get("edi")`(EDI 업로드)와 `if counts.get("records")`(리스트 업로드)는 **독립**이다. EDI가 없어도 records는 올라간다.
  실증: `voyages/STMJ_2643E/loading` 아래에 **`records`만** 있고 `ediContainers`가 없다. `MAILBOX\STMJ\2644W\` 폴더에도 EDI 파일이 하나도 없다(CLL 엑셀 3장뿐). 그 상태로 선적이 등록돼 화면에 **"EDI 대기"**로 뜬다.
- **등록 로직은 양하/선적 대칭이다.** autoreg 전체에서 mode로 갈리는 곳은 **두 군데뿐** — `autoreg.py:825`(평택분 판정 `_ptk(pod)` vs `_ptk(pol)`)와 `autoreg.py:1452`(가상 EDI 승격). records 업로드 경로는 mode를 보지 않는다.
  → **양하가 안 되던 것은 등록이 막혀서가 아니라 수집 단계에서 CDL을 안 받았기 때문**이다. 1.0-03으로 해소.
- **가상 EDI 승격은 선적 + RZOR·OBWH 전용**이다(`autoreg.py:1452` `if mode == "loading" and vcode in ("RZOR","OBWH")`). 리스트를 `ediContainers`로 올려 카드에 총 대수를 잡아주는 장치.
  **양하에는 넣지 않기로 확정(검수사 2026-08-04)** — "리스트로 갯수만 파악하면 됩니다. 나머지는 EDI 오면 확인되니까요."
- **수집과 등록은 별개 단계다.** `master.cycle = produce(collect_week) → register(merge_all → merge_priority → run_autoreg)`. **수동 「선택수집」 버튼은 `produce`만 돈다** — 파일은 폴더에 들어오지만 앱에는 안 올라간다. 등록은 자동 사이클이나 「병합·대조」 버튼. *(2026-08-04 검수사가 이 지점에서 혼동. 오해가 잦으면 버튼 문구에 명시하는 방안 있음 — 미결정)*

### 2026-08-04 — 갱신 주기 정본 (수치 확정)

| 대상 | 주기 | 소유 |
|---|---|---|
| 도선 예보 · 터미널 작업현황 · PORT-MIS | **3분** | 일정 루프(`schedule_min=3`). v2.17.47에서 단독 소유로 분리, 크롬 미사용(urllib 직접) |
| 메일 수집 사이클 | **5분** (`cycle_min=5`) | 완전자동 루프 |
| 신착 없을 때 | 검색 **생략**. **1시간마다 안전망 풀 사이클** | `auto.py:4396` |
| ⚡ 즉시 처리 | 대기 중 **30초**마다 명령 확인 | `commands.POLL_SEC=30` |

→ 앱 화면 지연은 **최악 1시간, 보통 5분 안, 급하면 30초**. 입출항·터미널은 수집과 무관하게 항상 3분.

### 2026-08-04 — MailPilot 1.0-02: 제목만 보고 버리던 메일을 내용까지 확인 (검수사 지적, Claude 코워크)

**사건**: 2026-08-03 19:46 수신. 제목 `RE: DXQD 2629W PTK PRE PLAN`(평택신항플래너), 첨부 `DXQD 029 선적 EDI.EDI`, 내용은 `TDT+20+2630W` — **현 항차 선적 EDI**. DXQD 2629는 7/28 마감(자사 DEP.TALLY 발송 완료), 현 항차는 2630W. 발신자가 옛 스레드에 `RE:`로 답장해 제목이 전 항차를 가리켰다.
**결과**: 전 항차 폴더에도 현 항차 폴더에도 저장되지 않음. 앱은 아침까지 "EDI 대기"를 띄우고 있었고, **검수사가 손으로 파일을 넣어 메웠다.** 종전에도 계속 수동으로 메워 온 결함이다.

**원인 (코드 확정)**: `auto.py:4538` `continue`.
- 항차 판독이 **제목 전용**(`auto.py:1311 _all_voys(up)`, `up`=제목+행텍스트). 본문·첨부내용은 안 봄.
- `_gate_voy`(`auto.py:3817`): `2629 >= min(2630)` 실패 → `"drop"`.
- 구제 `_rescue_voy_by_attach`(`auto.py:3842`)는 **첨부 파일명만** 봄. `_VOY = (?<!\d)(\d{3,5}[EWNS])(?!\d)` 라 방향문자 없는 `029`를 못 잡음.
- 콜번호 해석기 `_voy_by_call`(`auto.py:3977`)은 **제목 경로(`auto.py:4493`)에만** 연결돼 있었음.
- `_app_alive`(`auto.py:4503`)는 **제목 항차**를 앱 활성 항차와 대조 — 앱이 2630W를 기다리는 걸 알면서 2629W로 비교해 실패.

**"그전에도 고쳤는데 완벽하지 않았다"의 정체**: v2.16.1 주석이 **"파일 내용/첨부 항차 == 현재로만 교정"**을 선언했는데, v2.17.11-06(7/12 EAS 20121E)·v2.17.11-19(7/19 NSFR 2614S/2615S, CLL 65대 3일 결손)·v2.17.11-20/-23(7/20~22 콜번호)이 전부 **파일명까지만** 구현했다. 선언은 "내용", 구현은 "이름". 백업 3본 전수 grep으로 확인 — 이 지점은 내용 판독으로 확장된 적이 한 번도 없다.

**왜 미수집 리포트에도 안 떴나**: `dropped`가 `(code, leg)` 키다(`auto.py:4535`). 같은 날 `DXQD 2630W CNTR NO.LIST`가 정상 수집돼 `done_legs`에 `("DXQD","선적")`이 들어가자 `expected.py:71-74`에서 걸러졌다. **한 leg에서 항차 하나만 성공하면 그 leg의 다른 항차 드롭이 전부 은폐된다.** 리포트에 오른 OBWH 2706W·PCSZ 2625W·XTPG 535W도 같은 코드가 만든 것 — 그 leg가 통째로 비어서 드러났을 뿐이다. 진단 파일에는 07/26부터 325회 찍혀 있었으나 `(선박,항차,leg)` 세 값뿐이라 어느 메일인지 알 수 없었다.

**수정 (검수사 확정 규칙)**: **"마감된 항차의 메일이 마감일 이후에 왔다 = 무조건 파일을 받아 점검한다."** 결손 신호를 기다렸다 되돌아가는 게 아니라 **그 메일을 본 그 자리에서** 본다.
1. `_rescue_voy_by_attach` 3단 확장 — ① 파일명 항차 ② **파일명 콜번호**(`_voy_by_call` 연결) ③ **첨부 내용 `TDT+`**(`browser.fetch_bytes`로 EDI류만·메일당 2개·8초 타임아웃). 어느 단계든 **배정표 현재 항차와 정확히 일치할 때만** 교정.
2. `_tally_date`로 **마감 이후 도착 신호**를 로그·진단에 명시.
3. 드롭 로그에 **첨부 파일명** 기재 + `drops` 상세 리스트 신설 → 미수집 리포트에 **「버린 메일」 절**을 별도로 출력(`expected.format_report`). 미수집과 별개 사실이라 `done_legs`에 가려지지 않는다.
4. `mail.py._attach_links` 신설 — 파일명+URL 동시 반환(다운로드 없음).

**검증**: 실사건 데이터로 6종 시뮬 PASS — ① 파일명 판독 실패 재현 ② 내용에서 `2630W` 판독 ③ 구제 전체 경로 `2630W` 반환 ④ 배정표에 없는 항차면 교정 안 함(안전성) ⑤ 콜번호 `029` → `2630W` ⑥ 리포트에 버린 메일 은폐 없이 노출 + 마감 이후 도착 경고. 라이브 하트비트 `MailPilot 1.0-02 (2026-08-04)` 확인.

**같은 결함의 다른 발현 (미수정 — 인계함)**: 제목에 항차가 아예 없는 등록 선박 메일(`_PENDING_VOY="_미상"` → `_gate_voy`가 `maybe_typo` → 구제 실패 시 drop, 보류함에 도달 못 함), `dropped[(code,leg)]` 덮어쓰기로 마지막 사유만 남음, `collect_days=7` 밖 메일은 제목 없이 숫자만 로그.

### 2026-08-04 — 배포 인프라 2건 (Claude 코워크)

**① 워처가 죽어 있었다.** 재시작 배치의 `start "X" cmd /c run.bat` 이 워처의 stdout 핸들(`_deploy_out.txt`)을 자식에게 물려줘, 수집기가 도는 내내 그 파일을 잡는다. 다음 실행부터 리다이렉트가 실패하고 `call` 자체가 안 돌아 **워처가 영구 먹통**이 된다(07:43 재시작 직후 발생, 이관 작업이 3회 헛돌았다).
→ 워처 v4: 실행마다 고유 출력 `_deploy_out_{시각}.txt` + 최신본 `_deploy_out_last.txt` 복사. 구본은 `_deploy_watcher_backup_v3.bat`.
→ 재시작 배치도 `start "X" /min cmd /c "run.bat >nul 2>&1"` 로 교정(자식이 핸들 상속 안 함). `_restart_mp1002.bat`.

**② bat 한 줄 길이 8191자 제한.** PowerShell을 `-EncodedCommand` base64로 bat 한 줄에 넣으면 17,500자가 잘려 조용히 실패한다. **ps1 파일로 분리**하고 bat은 `-File`로 호출할 것. ps1은 한글 경로 때문에 **UTF-8 BOM** 필수.

**③ PowerShell 배열 후행 쉼표 금지** — `@( ... },\n)` 는 `',' 뒤에 식이 없습니다` 파서 오류.

### 2026-08-03 — TallyOne 1.3-02: 마감 텔리 수화물(Lug) 키 불일치 수리 (Claude 코워크, 커밋 `6570df4`)

**증상**: 페리 계보(TNJP·OBWH) 마감 텔리의 `(Lug)` 4행 × IN/OUT = 8칸과 PORTPERFORMANCE `flug`/`elug` 열이 **상시 공란**. 수화물 1대가 `20ft Empty` 행에 흡수돼 실물 대비 +1/−1로 어긋났다.

**원인 (데이터로 확정)**: `src/tallyReport.js:174`가 `fc.lugg`를 읽는데, 저장 경로(`HomePage.jsx:690` → `fbUpdateVoyageInfo`)는 `luggageCns`로 저장한다. **`lugg`를 저장하는 코드는 저장소 어디에도 없다**(전수 grep 확인) — 읽기 전용 유령 키. `Array.isArray(fc.lugg)`가 항상 false여서 `luggSet`이 영원히 비었다. `VoyagePage.jsx:257`의 컨테이너 플래그 이름(`c.lugg`)을 forecast 키로 착각한 것으로 보인다.

**수정**: `luggageCns` 우선, 구 데이터 호환을 위해 `lugg` 폴백. 외과적 1개소.

**검증 (실물 오라클)**: MAILBOX 실 발송 텔리 9회차 + 실 선적 EDI 3건 확보. 앱 `parseBAPLIE` + `buildFerry`를 그대로 태워 대조 — **3회차 × 6행 = 18칸 전부 일치**(2692W 221 / 2696W 292 / 2698W 187). 수정 전 `e20` +1, `e20lug` −1이던 것 소멸. blob 18/18, 라이브 `sw.js` VERSION `TallyOne 1.3-02`.

**같이 확인된 미수정 결함 (인계함 등록)**:
- `PrintHubModal.jsx`가 `VoyagePage`의 컨테이너 병합 로직을 복제해 두고 `tagForecastMarks` 호출만 빠뜨림 → **인쇄 허브 경로의 카고플랜·검수 리스트에 ▲긴급·🧳수화물 마커가 통째로 안 찍힌다.** 베이플랜 경로는 정상.
- `tallyFormats.js`의 `OBWH: variant:'voucher'`는 **죽은 필드**(아무 데서도 안 읽음). 실제 분기는 `tallyExcel.js:372`의 `M.variant === 'ferry'`(templateMap 쪽).
- `bulkClose` 가드(30분 내 20대+)가 OBWH엔 역효과 — 작업창이 3~5시간뿐이라 일괄 마감 시 주야열이 빈칸이 되는데, 실물 9/9 회차는 채워져 있다.

### 2026-08-03 — 마감 텔리 도메인 확정 (실물 전수 조사, 코드 변경 없음)

MAILBOX 마감 메일 **58통 / 19척**(선박 폴더는 29척) 전수 + 실물 텔리 파일 13개(12척) 내부 구조 분석.

**`Final Work` 축이 3계보로 갈린다** — 이것이 "선박마다 텔리 내용이 다르다"의 정체다.

| 계보 | 행 축 | 열 축 | 선박 | 앱 |
|---|---|---|---|---|
| A. 선사×포트 | OPERATOR/PORT/F·E | DISCH·LOAD·SHIFT × 20'·40'·HC·45' | DXQD ATPR STSE TMPZ YKTD DJCT PCSZ SWAT (8) | 처리됨 |
| B. **시간대** | 화물유형 9종 × IN/OUT | Total·주간·야간(1700~0700)·시간외·휴일 | **OBWH(xls), RZOR(pdf)** | 미처리 |
| C. 선사×작업구분 | 선사 13개 알파벳 고정 × F·E | DISCH·LOAD·SHIFT1T·SHIFT2T × 20·40·45 | **XTPG(pdf)** | 미등록 |

- **OBWH와 RZOR은 같은 계보다.** 형식만 엑셀/PDF로 다르다. 시간대축 템플릿 하나로 두 척(23통, 전체 40%)이 동시에 커버된다.
- **기존 `TNJP` 템플릿 좌표가 OBWH와 완전히 동일**하다(행 17~24/27, 30~37/40, 열 E/F/G, 항차셀 C6/E6). 신설이 아니라 복사면 된다.
- **포트 순서는 고정 규칙이 아니다.** DJCT·YKTD는 선사 세트가 같은데(SKR/HAS/HSL/DJS/DYS) 포트 순서가 `SHK→HPH→INC` vs `INC→SHK→HPH`로 다르고, YKTD는 같은 파일 안에서 DYS 행만 어긋난다. **배별 실측값을 박아야 한다 — 추론 금지.**
- **RF·OT·FR·DG 전용 칸이 Final Work에 없다.** OS/Performance의 `REMARKS` 자유텍스트로만 존재(`"RF x 2 ,OT x 1 ,TK x 5(DG x 3)"`).
- **OS 시트: 값 0인 행은 지우고 보낸다** (8/9 회차. 첫 회차 2679E만 예외 — 0행 잔존·LUG 행 순서 역전). 확정 순서: OS-IN `20'F → 20'(LUG)F → 40'F → 45'F`, OS-OUT `20'F → 20'E → 20'(LUG)E → 40'F → 40'E → 45'F → 45'E`. `MANIFESTED = DISCHARGED/LOADED`, `OVER/SHORT = NIL/NIL`(9/9).
- **OS는 40'과 45'를 나누고 Final Work는 합친다.** 검산: 2691E OS-IN 83+1+117+33=234 = Final Work 27행 234. 40'F 117 + 45'F 33 = 150 = `40'/HC/45' Full` 150.
- **TDR(STSE·STMJ 전용)은 다른 문서다.** SITC 선사 양식, 단일 시트, 규격 13종(20GP·20'DG·20RF·40'HC·40'HR…), TEU·중량 열 보유, 포트가 UN/LOCODE(CNTAO). TALLY REPORT는 이를 4규격으로 압축한 것 — **TDR 자동생성은 REMARKS 파싱 없이 불가능.** 롤업 실검산: `20'` = 20GP+20'DG+20'HG+20RF+20'Special(26+3+0+1+2=32 ✓), `HC` = 40'HC+40'HR(88+13=101 ✓).

**수화물(Lug) 정보 출처 (사용자 확답)**: 카카오톡. 선사 담당자가 두 갈래로 보낸다 — ① 컨번호+실번호+F/E(예: `SPSU2042317` / `BHGJ048280` / `EMPTY`) ② 예상물량 전문(`FULL 20GPX5 + 40HQX9` / `EMPTY 20GPX31 + …` / `EMPTY 20GPX1은 수화물 컨테이너입니다` / 긴급선적분 수량 / 20자 하이큐 엠티 컨번호). **앱에 붙여넣기 입력 경로는 이미 있다**(HomePage 「📋 물량 예보 붙여넣기」, V9.02). 실측 결과 `parseCargoForecast`는 규격별 개수는 정확히 잡지만 **수화물 컨번호(`luggageCns`)와 수화물 수량 지목을 0건으로 흘린다** — 컨번호가 머리말보다 **먼저** 오는 형식을 가정하지 않았기 때문. 긴급선적분 수량과 20자 하이큐 컨번호도 미수용. `calc`는 대수가 아니라 **TEU**다(FULL 14대 → calc.full 23).

**★ 수화물(Lug) 식별 — CLL이 이미 표시하고 있다 (2026-08-03 전수 검증 확정, 11/11 반증 0)**

카톡은 **확인용**이지 유일 소스가 아니다. OBWH CLL 4회차 11리비전(2698W·2700W·2702W·2704W) 전수 검증 결과:

| 판별식 | 적중 |
|---|---|
| **맨 끝 화주 열 공란 (단독)** | **11/11** ← 1순위 |
| 셀 채움색 `#CCCCFF`(연보라) | 11/11 ← 2순위, 눈으로도 보인다 |
| `Weight공란 ∧ VGM공란 ∧ F/E='E' ∧ Tp/Sz='DC20'` | 11/11 ← 시트1만 올 때 |
| 화이트리스트 `{SPSU2042317, SPSU2042322}` | 11/11 ← 최후 보루 |
| 중량 공란 단독 | ✕ 리비전 초기 최대 37건 — **노이즈, 단독 사용 금지** |

- **`Seal No.` 열이 실번호다.** CLL의 `Cntr. No`(SPSU20423xx)는 선사 내부 가번호, 실물 번호는 Seal 열. 일반 엠티는 Seal이 `'W'`, 풀컨은 실제 씰번호.
- **수화물 컨은 2대 고정 왕복** — `SPSU2042317` / `SPSU2042322`. ASC 슬롯 항상 `030582`, 항상 `20DC`+`E`.
- **1차 CLL에 이미 있다.** 2704W 1차↔2차 수화물 행 완전 동일(1바이트 불변). 리비전마다 바뀌는 건 일반 화물 중량·VGM뿐 — **2차를 기다릴 필요 없다.**
- ⚠ **`BC20`은 수화물이 아니다.** 화주=연태훼리주식회사인 자사 엠티(2200kg, Seal='W'). 카톡의 "20자 하이큐 엠티 3대"가 이것.
- CLL 시트 구성: `'CLL '`(17열, 화주 없음) + `'CLL  (2)'`(19열 = 17열 + 빈 열 + 화주). 11/11 동일. 숨은 행·열·굵게·Remark 열은 전부 없음.
- 카톡 예보 ↔ CLL 집계는 완전히 맞는다(2704W 실검산): DC20 33+BC20 3=36 → 20GP 5F+31E, DC43 89 → 40HQ 9F+80E, DC45 18, RF43 12, FR40 4, RF20 1, F14/E146.

**`Holiday` 열 규칙 (사용자 확답)**: 공휴일이 아니라 **국경일** 기준. 7/17 제헌절(비공휴일)에 기입돼 있다. 주간·야간을 나누는 게 아니라 **총계를 그대로 겹쳐 적는다**(I27=255=E27, I40=284=E40). `Non office hours` 열은 9/9 전부 0 — 규칙 미확인.

**유통 중인 실물의 기존 오류(자동 생성하면 사라짐)**: DPRT 텔리 파일명 코드 오타 `DRPT`, OBWH `2682W`(2692W 오타)·`2694`(방향접미사 W 누락), XTPG `532E`(앞자리 0 누락), TMPZ `.xls.xlsx` 이중 확장자(4/4), PCSZ·SWAT·DJCT·YKTD의 `M/V:`·`BERTH:`·`DATE:` `#NAME?` 수식 오류, SWAT RF장 선박명 `SAWASDEE ALTAIR`(실제 ATLANTIC), STSE SHIFTING 시트 머리글 이전 항차(2571E/2572W) 잔존.

### 2026-08-03 — TallyOne 1.3-01: 유령 로그인 배지 수리 (사용자 신고, Claude 코워크)
- 증상: 이종현·이인철이 각각 3시간·12시간 전 마지막 활동인데 "로그인" 배지 유지. 원인: 앱을 로그아웃 없이 닫으면 기기 쪽 30분 자동 로그아웃(V9.13)이 돌 수 없어 loggedIn=true 영구 잔존 — 화면 판정이 loggedIn만 봄.
- 수정: inspectorStatus(단일 소스, 호출부 LoginPage 요약·목록/StaffManagerModal)에 신선도 추가 — loggedIn=true라도 lastActive 30분 초과면 로그아웃 취급. 실데이터 6케이스 시뮬 PASS. 커밋 3eb738e.

### 2026-08-03 — 재탄생일 (전수조사 + 개편 + 리브랜딩, Claude 코워크 세션)
- **MailPilot 1.0** (구 v2.17.47): 수집기 전수조사 — 치명 6·높음 19·중간 20·낮음 11 수정, 미사용 790줄 삭제(signal·badge 모듈 등). 상세 보고서 C:\TALLYTEST\메일수집기_전수조사_보고서_v2.17.46_2026-08-03.md.
- **TallyOne 1.0**: 검수앱 전수조사 — 결함 52건 수정(ISO 특수컨 오태깅, 실선적 EDI 리퍼→드라이, 베이100 절단, fbCreateVoyage 덮어쓰기, badgeRule departed 등) + 미사용 2,100여 줄 삭제 + 3화면 개편(로그인·역할별 진입·보조기능 #/aux) + 수석 대시보드 보강(수집기 배너·터미널 대조·일정) + 리브랜딩. 커밋 9bc10ae.
- **TallyOne 1.1**: 클로드 메모함(claude_inbox, 오프라인 큐). 1.1-01/-02: 콘앱 번들 캐시키 동반 상향.
- **TallyOne 1.2**: 데미지·실오류·풀리퍼 사진에 "앨범에서 선택" 추가(capture 강제로 찍어둔 사진 등록 불가하던 것 — CLHU2628765 FRONT PANEL TORN 20×30 보고 실패 사건 해결). PORT-MIS 캡처 capture 제거.
- **TallyOne 1.3**: 활동 로그 — 로그인/로그아웃(수동·idle)·화면 열람(30초 스로틀)·끝4 조회어·자연어 질문(음성 포함) 기록, 수석 대시보드 소유자 전용 타임라인, 30일 자동 정리. 커밋 fe52792.
- **ConeOne 1.1**: 약식 파서 F/E 전면 미인식 교정, 마스터 파서 보장 로드+간이 모드 경고, 질문기 트리오 구간 매칭, 픽셀/PDF 사장 코드 285줄 삭제.
- **ConeOne 1.2/1.2-01**: 베이뷰 격자를 검수앱 BayPlan 규칙으로 단일화(OBWH 유령 칸 36 제거) → LOLO 게이트.
- **ConeOne 1.3~1.5-01**: RZOR LOLO 최종 — 덱플랜 뷰(검수앱 LOLO 탭 동일) + 콘 계산기(1단 바닥 콘 없음, (반수−바닥)×4, 바닥 입력식) + 최대 67반 경고.
- 문서 재편: 지침서를 이 한 권으로 대체, 클로드 작업표준·인계함 신설(CLAUDE_SHARED).

> 이후 기록은 이 위에 같은 형식으로 추가한다. 형식: 날짜 — 버전 — 무엇을 왜 — 검증 — 커밋/백업.
