평택항 검수 & Tallyman Master 통합 지침서

> **이 문서는 이 프로젝트의 모든 작업에 우선 적용되는 규칙이다.**
> 이 프로젝트에서 작업하는 모든 Claude는 시작 전에 이 문서를 먼저 읽고 따른다.

최종 업데이트: 2026-06-09
작성자: 성일 (평택항 검수 / Tallyman Master 개발)
현재 버전: V7.45

구성: **0부 = 범용 코딩 지침**(LLM 공통 실수 방지) · **1~6부 = 검수 업무·앱 도메인 원칙** · **7부 = V7.x 최신 작업 원칙** · **8부 = 운영 매뉴얼·실증 데이터(구 MD 흡수)** · **9부 = 2026-06-10 세션 종합** · **부록 = 디버깅 일지(보관)**.

═══════════════════════════════════════════════════════
0부 — 범용 코딩 지침 (모든 작업의 기본 자세)
═══════════════════════════════════════════════════════

caution over speed. 사소한 작업은 판단껏.

## 0-1. 코딩 전에 생각하라 (Think Before Coding)
가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.
- 가정은 명시적으로 말한다. 불확실하면 묻는다.
- 해석이 여러 개면 제시한다 — 혼자 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 불분명하면 멈춘다. 무엇이 헷갈리는지 짚고 묻는다.

## 0-2. 단순함 우선 (Simplicity First)
문제를 푸는 최소 코드만. 추측성 기능 금지.
- 요청 안 한 기능 추가 금지. 일회용 코드에 추상화 금지.
- 요청 안 한 "유연성·설정 가능성" 금지. 불가능한 시나리오 에러처리 금지.
- 200줄 짠 게 50줄로 되면 다시 쓴다.

## 0-3. 외과적 변경 (Surgical Changes)
건드릴 것만 건드린다. 내가 만든 흔적만 치운다.
- 주변 코드·주석·포맷을 "개선"하지 않는다. 안 깨진 걸 리팩터링하지 않는다.
- 기존 스타일을 따른다. 무관한 죽은 코드는 언급만 하고 지우지 않는다.
- 내 변경이 만든 미사용 import/변수/함수만 제거한다.
- 기준: 모든 변경 줄이 사용자 요청과 직접 연결돼야 한다.

## 0-4. 목표 기반 실행 (Goal-Driven Execution)
성공 기준을 정하고, 검증될 때까지 반복한다.
- "버그 수정" → "버그를 재현하는 테스트를 만들고, 통과시킨다."
- 이 앱에선: **실데이터로 재현 → 고침 → 같은 데이터로 PASS 확인**.

## 0-5. 한국어 출력 시 콜론으로 문장 끝내지 말 것
사용자가 한국어면 출력도 한국어. 문장 끝은 `.` `?` `!` — `:` 아님.
(영어 문서 습관으로 콜론이 새어 나옴. 콜론은 코드·키밸류·라벨에만.)

## 0-6. 새 파일 첫 줄에 한국어 헤더 주석
새 소스 파일을 만들면 첫 줄에 역할을 한 줄 한국어로.
예: `// 검수 완료 항차를 archive에 백업하는 함수`. (설정 파일은 제외.)

## 0-7. 큰 작업은 계획·체크리스트·결정 노트 먼저
비자명한 작업은 코드 전에 설계를 글로 정리하고 확인받는다 (예: 화면 구조 변경).

## 0-8. 완료라고 말하기 전에 테스트 (절대 원칙)
코드를 만졌으면 "완료" 전에 검증한다.
- 이 앱에선 **실데이터 시뮬레이션 PASS + `bash build.sh` 성공 + 빌드된 JS 검증**이 곧 테스트다.
- 이것 없이 "완료"라고 말하지 않는다. LLM이 가장 자주 건너뛰는 단계 — 비협상.

## 0-9. 의미 있는 단위로 커밋
한 논리 변경이 끝나면 버전을 올리고 변경 이유를 기록한다.
- 좋음: "archive 백업 추가". 나쁨: "백업 추가하고 UI도 고치고 버그도 수정"(3개로 쪼갬).

## 0-11. 세션 시작 버전 대조 (절대 원칙 — 재발 3건의 교훈)
**작업 시작 전에 반드시 세 가지 버전을 대조한다.**
- ① 프로젝트 지식 지침서의 "현재 버전" ② 작업 베이스(업로드 ZIP/폴더)의 APP_VERSION ③ 사용자가 마지막으로 받은 ZIP 버전.
- 셋이 어긋나면 **코드를 만지기 전에** 사용자에게 보고하고 어느 것이 최신인지 확인한다.
- 배경: 2026-06-09 세션의 수정 3건(deck-only 규칙 제거·MD 정리·tier 라벨 13px)이 별도 라인이라 6-10 세션에 전부 재발. 기록은 있었으나(6/9 ZIP 안 지침서) 프로젝트 지식이 6/6판(V7.28)이라 다음 클로드에게 전달되지 않았음.
- **기록의 전달 경로 (2026-06-10 사용자 확정 운영)**:
  - 프로젝트 지식 교체는 **하루 1회, 그날 마지막 작업 판으로만** (업데이트마다 X — 같은 채팅 안에서는 클로드가 기억하므로 무의미).
  - 새 채팅에서 앱 작업 시에는 **항상 최신 ZIP을 베이스로 업로드** — ZIP 안 지침서가 그 시점의 진실을 전달.
  - **우선순위: 지침서가 둘 보이면 버전 높은 쪽이 진실** (보통 ZIP 안 지침서 ≥ 프로젝트 지식). 프로젝트 지식은 ZIP 없는 채팅(검수 업무·상담)용 베이스라인.
  - **마무리 신호는 사용자가 준다**: 사용자가 "오늘 그만한다 / 지침서 정리해 달라"고 요구하면 그때 클로드가 그날 전체 작업을 반영한 최종판 지침서를 만들어 "프로젝트 지식 교체용"으로 드린다. 중간 ZIP마다 교체를 권하지 않는다.

## 0-10. 에러를 읽어라, 추측하지 마라 (절대 원칙)
실제 에러/로그를 읽는다. 기억으로 패턴 매칭하지 않는다.
- "흔한 수정"을 원인 확인 전에 적용하지 않는다. 불분명하면 로그를 찍어 상태를 확인한 뒤 고친다.
- ⚠️ 이 앱에서 반복된 실수: "캐시 문제겠지", "옛 버전이겠지"로 넘겨짚기. 코드를 끝까지 읽고 실데이터로 확인할 것.

═══════════════════════════════════════════════════════
1부 — 검수 업무 (본업)
═══════════════════════════════════════════════════════

## 1.1 정체성
위치: 대한민국 경기도 평택시 (평택항). 역할: 화물 검수 및 선박 양하 작업 관리. 언어: 한국어.

## 1.2 자료 분석 원칙
- 제출 자료(메인플랜·베이플랜·양하 리스트·XRAY 리스트 등)를 빠짐없이 분석할 것.
- 추론이 아닌 실제 데이터만을 기준으로 답변할 것.
- ⚠️ 가정 금지: "이렇겠지" 추측 작업 X. 실데이터 직접 확인.

## 1.3 응답 워크플로
- 컨테이너 끝자리 4자리 호출 → 실번호 + X-RAY 대상 여부 + 선내 위치.
- 컨테이너 개수("20피트 몇 대?") → 규격별(20ft/40ft) × 상태별(Full/Empty) 상세 산출.
- 특수화물 리스트 → 베이플랜 기본, 리퍼(온도)·FR·O/T·DG 등 포함.

## 1.4 보고 형식
명확하고 간결하며 스캐닝이 쉬운 형식. 실데이터 기반 정밀 수치.

═══════════════════════════════════════════════════════
2부 — Tallyman Master 앱
═══════════════════════════════════════════════════════

## 2.1 현재 상태
앱: Tallyman Master (greenmarinetally). 현재 버전: V7.45.
GitHub: greenmarine26/greenmarinetally. Firebase: greenmarinetally (asia-southeast1, Spark).
작업 디렉토리: /home/claude/v2work/ (또는 세션별 작업 폴더).

## 2.2 검증 원칙 (절대 원칙)
- 검증 안 된 앱은 절대 ZIP으로 주지 않는다.
- 빌드 성공 ≠ 검증. 실데이터로 흐름을 끝까지 돌려 OK일 때만 검증 OK.

## 2.3 작업 순서 (절대 건너뛰지 않음)
```
코드 수정 → 실데이터 시뮬레이션 → PASS → bash build.sh → 빌드된 JS 검증 → ZIP
```

## 2.4 빌드 전 체크리스트
- APP_VERSION 갱신 (src/utils.js 한 곳만 — sw.js·cone.html 라벨은 build.sh가 자동 동기화).
- HelpModal 사용법 등록 (새 기능·변경 시).
- 이 지침서 갱신 (작업 완료 시).

## 2.5 버전 표기
큰 변화/기능 추가 = 마이너 버전업. 작은 수정 = 서브 버전. 모든 변경마다 버전 올림.

## 2.6 ZIP 배포 메커니즘
- 누적본 ZIP을 repo 루트에 덮어쓰기 → commit & push.
- **ZIP 구조**: 단일 디렉토리 `Tallyman_Master_Vx.xx/` 하나로 감싼다 — 풀면 그 폴더 안에 소스 + dist + build.sh + 지침서 등 전체가 들어가게. 파일이 바로 흩어지면 안 됨. (M6.86 합의 — 폴더로 감싼 누적본 통째 전달.)
- 루트 index.html은 빌드본 그대로 (소스형 두면 vite 에러). build.sh가 dist→root로 sw.js·cone.html·manifest까지 복사.
- 캐시 무효화: sw.js는 network-first, VERSION이 바뀌어야 새 버전 인식 + 옛 캐시 삭제.
- **ZIP MD 정리 규칙 (2026-06-09 확정)**: ZIP에는 현재 유효한 `검수앱지침서.md`와 `README.md`만 포함. 옛 핸드오프(HANDOFF_*), 빌드노트(BUILD_NOTES*), 일회성 분석 보고서(*REPORT*, EMBEDDED_*), 구버전 지침서(HANDOFF_인계서.md)는 제외. 살릴 내용은 먼저 이 지침서에 흡수한 뒤 삭제(정보 손실 금지).

═══════════════════════════════════════════════════════
3부 — 컨선 베이 구조 도메인
═══════════════════════════════════════════════════════

## 3.1 짝수 단독 베이 (정상 구조)
BOW / STERN / 선원건물 앞 / 선원건물 뒤에서 정상. 한쪽 홀수만 있어도 정상. 짝수 단독을 비정상으로 일반화 금지.

## 3.2 베이별 자체 row 구조 (핵심)
선박 전체 단일 row max 가정 금지. 같은 선박 내에서도 베이/section마다 row 구조 다름.
deck/hold tier도 베이별로 다름 (예: DJCT BAY 27 = deck only). 단일 rowMaxOdd/Even은 부정확.
예: DJCT BAY 01 = rowCount 7, hasZero true / DJCT BAY 07 = rowCount 10, hasZero false / SWAT BAY 17 = rowCount 11, hasZero true.

## 3.3 베이사전 등록 현황
v2 verified 36척, v5 매트릭스 300척. v2/v5 모두 베이별 row 라벨 정보 일부 누락.

## 3.4 PDF override 사전
src/data/shipBayDict_pdf_override.js. 베이별: rowCount, hasZero, deckTiers, holdTiers, (권장) deckCells, holdCells.

## 3.5 ISO 6346 사이즈
첫 자: 2=20ft, 4=40ft (45R1 = 40ft hi-cube reefer), L/9=45ft. ⚠️ 45R1을 45ft로 분류 금지.

## 3.6 cells = 00 포함 개수 (V7.18 확정 — 핵심)
- 사용자 매트릭스 cells = **00 포함 전체 칸 수**. 데크00 체크 = cells 안에 이미 00 포함.
- ⚠️ `+1` 금지 (00 두 번 세면 없는 09가 생김).
- ⚠️ 개수로 row 번호를 생성하지 말 것. 실제 row 라벨을 쓴다. ("갯수 보지 말고 실제 row 번호를 봐라")

═══════════════════════════════════════════════════════
4부 — 카고플랜 V2 약속
═══════════════════════════════════════════════════════

## 4.1 베이 구조
- **베이플랜은 좌표(absolute) 기반 — 격자(flex) 금지 (V7.0x 확정)**: 격자는 한 칸 빠지면 옆 셀이 밀림. 각 셀이 자기 (row,tier) 좌표 자리를 가져야 함. 데크/홀드는 끝까지 따로 관리.
- **cells 해석을 베이플랜·카고플랜 간 함부로 통일하지 말 것**: 두 화면은 렌더 모델이 달라(좌표 vs flex) cells 해석을 한쪽 기준으로 "통일"하면 다른 쪽이 회귀함. 통일이 필요하면 실데이터 렌더로 양쪽 모두 검증 후.
PDF STOWAGE INSTRUCTION 정답 그대로. 베이마다 자체 deck/hold tier + row. 단독 베이 hold 없으면 hold 영역 없음. A4 한 장. 좌측 하단 mini-legend.

## 4.2 마크 약속
(a) PTK 일반 = 선사별 컬러 + o / (b) PTK 특수(R/D/F/T/A/E) = 글자 + 컬러 / (c) 통과 일반 = 회색 배경, 글자 없음 / (d) 통과 특수 = 글자 + 회색 / (e) 빈 슬롯(hull 안쪽) = border만 / (f) 짝수 40·45ft shadow = X(흰 배경) / (g) 짝수 20ft shadow = 회색 빈 셀(자리 차지) / (h) Hull 바깥 = 안 보임(visibility:hidden).
- ⚠️ X는 짝수 40/45ft shadow 전용. 통과화물 표시로 쓰지 말 것.

## 4.3 컬러 시스템
양하 모드: 선사(c.op)별 10색. 선적 모드: POD 3자별. ⚠️ c.cn 첫 3자 BIC 코드 = 선사 X. 사용 금지.

## 4.4 Hold 중앙 정렬
CSS: width (nHoldCols/nDeckCols)*100% + margin auto. 1칸 차이 → 좌우 0.5칸씩.

## 4.5 Layout 공식
N개 박스 → 상단 = ⌈(N+1)/2⌉, 하단 = N − 상단, 별첨 자리 = 차이.
- ~~hold 없는 단독 베이(deck-only)는 하단 행에 우선 배치~~ **【2026-06-09 철회, V7.38 재적용】 hold 유무와 상관없이 베이 번호 순서가 절대 우선.** deck-only라고 순서를 깨고 내리면 BAY 01이 하단으로 밀림(TMPZ 재현, .def 사전 적용 후 재발). SWRG의 33·35·38이 하단인 것은 번호가 커서이지 deck-only라서가 아님 — 과잉 일반화였음.

## 4.6 카운트 포맷
단독 = 총합 1숫자. 페어 = 20'/40'/45' 슬래시. 양하 = 평택분만.

## 4.7 인쇄 처리
height 195mm 고정. page-break-inside: avoid. body 직접 마운트(createPortal). display:none + bg white.

═══════════════════════════════════════════════════════
5부 — 데이터 검증·명칭 원칙
═══════════════════════════════════════════════════════

## 5.1 출처 우선순위
PDF override > v5_matrix.matrixBays > v2 baysSummary > EDI 실데이터.

## 5.2 v5_matrix.cells 주의
순서 아래→위 → reverse() 필수. v5가 PDF와 불일치 가능. 일부 선박 v5 없음.

## 5.3 EDI 검증
has_zero = EDI rows에 0 있는지 직접. 추측 금지, 실데이터 보고 판단.

## 5.4 CASPI가 정답 기준
CASPI를 따른다. 잘못된 명칭 사용 금지. 사용자가 실제 쓴 약자·명칭만 인용.

## 5.5 선사 약자 3자 (영구 규칙)
선사 약자는 항상 3자. 4자 코드는 앞 3자만.
예: DJSC→DJS, NSSL→NSL, HASL→HAS, SNKO→SKR, EASK→EAS, TJMS→TJM, WDFC→WDF, DWIC→DWS.

═══════════════════════════════════════════════════════
6부 — userBayDict / 매트릭스 보호 (절대 원칙)
═══════════════════════════════════════════════════════

- **userBayDict 절대 보호**: source !== 'user'일 때만 mergeBayDef. 사용자 입력을 자동분석(v5/v2/EDI)이 덮어쓰지 않는다.
- **베이매트릭스가 기본 진실**: 매트릭스에 deck00/hold00이 명시되면 EDI가 덮어쓰지 않는다.
- **Rules of Hooks**: 모든 Hook은 최상단. 조기 반환문은 모든 Hook 선언 이후.
- **음성 출력 표준**: 컨테이너 번호는 한 글자씩, 베이·온도는 한글로 또박또박.

═══════════════════════════════════════════════════════
7부 — V7.x 최신 작업 원칙 (M6.91 이후 추가)
═══════════════════════════════════════════════════════

## 7.1 평택분만 계산
- 항상 평택분만: 양하 = POD가 평택, 선적 = POL이 평택.
- 평택 판정 변종 포함: `PTK / PYT / PYOTM / PYO`로 끝나면 평택 (KRPTK·KRPYO·KRPYOTM 등). **isPyeongtaekPort() 단일 함수로 통일** (V7.27: 코드에 isPtkPort는 없음. 적재·집계·모드판정·진단 전 화면이 이 함수 하나만 사용. firebase 한글 항구명 케이스만 예외).
- ⚠️ 폴백 금지: 평택분 0이면 0이 정답. 전체(rowsAll)로 넘어가 타지역까지 세지 않는다.

## 7.2 데이터 유실 방지 (archive)
- 완료 시 `archive/{voyageKey}`에 실데이터 전체 백업 → 백업 검증 성공 후에만 삭제(fbDeleteVoyage). 백업 실패 시 삭제하지 않는다.
- 복원: fbRestoreVoyageFromArchive. 1년 후 자동 정리: fbCleanupArchive.

## 7.3 이중 확인 (검수사 → 수석)
- 검수사가 항차 화면에서 완료 누르면 `inspectorDone` 표시만 (삭제 안 함). 수석 확인 전 검수사 취소 가능.
- 수석이 대시보드 진행 상황에서 최종 "완료 저장" 누를 때만 archive 백업 + 보관소 기록 + 삭제.
- 목적: 2명이 나눠 작업 중 한 명이 자기 자리 완료를 전체 완료로 잘못 눌러 삭제되는 사고 방지.

## 7.4 라이브러리 / 보관소 분리
- 선박 라이브러리(진행 상황) = 현재 살아있는 voyages만. 완료 안 한 배만 보임.
- 선박별 자료 보관소(완료 기록) = ships 노드, 최근 완료순. 항차별(양하/선적/일자) + 누적.
- 보관소 제목은 사용자 약자(vsl, 예 PCBJ/TNJP) 우선.

## 7.5 계열 대체 시 신원 금지
- 베이 구조 없어 다른 선박 사전을 빌릴 때(series-substitute), 베이 구조만 빌리고 콜사인·선박명은 빌리지 않는다.
- ⚠️ 앞 2글자만 같으면 계열로 보는 대체는 느슨함. 무관한 선박의 신원(콜사인)이 섞이면 PORT-MIS 오매칭 발생 (DJCT에 XIN TAI PING 표시된 버그).

## 7.6 cone 앱 (콘 계산기)
- 검수앱 EDI 연동(fbFetchEdiContainers)도 평택분만 필터. 양하=POD평택, 선적=POL평택.
- 화면 버전 라벨은 build.sh가 APP_VERSION과 자동 동기화 (신·구버전 구분용).
- cone은 독립 HTML(`public/cone.html`). build.sh가 dist·루트로 복사 + 버전 라벨 동기화. **본체와 별개 앱이므로 React 점검과 별도로 cone.html도 따로 점검할 것.**
- **V7.28: cone 베이뷰 평택 판정 통일.** 메인 isPtkPort()는 정식 기준(`/(PTK|PYT|PYOTM|PYO)$/`)인데, 베이뷰 인라인 isPtk 2곳이 `/PTK|PYT|PYOTM/.test(p)||p===''`로 달라 PYO 누락 + 끝고정 없음 + 빈값을 평택 오인 → 콘 계산값과 베이뷰 그림 불일치. 베이뷰 2곳을 메인 isPtkPort() 호출로 통일. (본체 ①번과 같은 종류. cone 안에서도 판정이 흩어져 있었음.)
- **V7.29: cone 동선표 추가.** 작업표 아래에 동선표 자동 생성. 양하 회수분(diff<0)을 선적 베이(diff>0)에 종류별(데크↔데크, 홀드↔홀드)로 매칭 — 가장 가까운 회수 베이 우선, 모자란 분만 콘박스에서. 작업 흐름이 양하 후 선적이라 회수분이 다 모인 상태에서 분배 가능(시간순서 문제 없음). computeRoutes() 함수가 모든 선박에 자동 적용. "창고" 아닌 "콘박스" 명칭. 모든 선박 적용 — 별도 입력 없이 작업표 계산 결과(rows)에서 자동.

## 7.7 V7.27 코드 정합성 점검 (2026-06-06) — "곳곳에서 안 맞음" 6건 일괄 수정
전수 점검에서 발견한 정합성 문제 6건. 모두 실데이터 시뮬레이션 PASS 후 build.sh 빌드 검증.
- **① 평택 판정 통일**: 같은 함수가 화면마다 따로 구현돼 평택 표기(KRPYOTM 등)를 일부만 인정 → 카고플랜엔 보이는데 카운트엔 빠지는 불일치. isPyeongtaekPort() 단일 함수로 8파일 14지점 통일. (firebase 한글 항구명 케이스만 예외.)
- **② 양하 선사 색 3자**: 컬러키가 op를 trim만 해 DJSC/DJS 갈라짐. utils.normalizeCarrierCode 신설 — 변환표 경유 3자. ⚠️ 단순 slice(0,3) 금지(SNKO→SNK 틀림, 정답 SKR).
- **④ 베이매트릭스 확정 배지**: BayDictStatusWidget 검증 배지 옆에 매트릭스 확정 여부 표시. tier가 매트릭스(user/override) 확정이면 "📐 매트릭스 확정", 미확정 베이 있으면 "⚠️ 베이매트릭스 확정 필요"(EDI 추정). 펼치면 미확정 베이 목록.
- **⑥ V1 통과화물 X 제거**: 구버전 카고플랜이 통과화물을 X로 표시 → shadow X와 구분 불가(4.2 위반). V2와 동일 회색 배경으로. X는 짝수 shadow 전용. V1은 V2 안전망이라 유지.
- **⑦ 리퍼·특수 판정 통일**: V2 셀 마크는 isReeferContainer, 카운트는 iso[2]===R 약식 → 그림·카운트 불일치. 카운트도 셀 마크 기준(isReeferContainer + c.fr/tk/ot/oog)으로 통일.
- **⑧ PORT-MIS 오매칭 fix (DJCT에 XIN TAI PING)**: 계열 대체 시 최상위 callsign만 비우고 bayDef 내부는 안 비워, PORT-MIS가 bayDef.callsign으로 폴백해 빌려온 선박 콜사인(BSDU)을 집어옴. V7.26 반쪽 수정 보완 — 계열 대체 시 bayDef의 callsign·name·vesselName도 제거. (7.5 보강.)
- **⑤ 45R1=40RF 표시**: 메인 isoToLabel이 이미 정확. 45피트 리퍼 없음(있으면 L5). 보류.
- **빌드 교훈**: 루트 index.html은 빌드본(고정 assets 참조)이라 `npm run build` 직접 실행 시 src 변경이 반영 안 됨. **반드시 `bash build.sh`** — 빌드 직전 소스형 진입점(_index.entry.html)을 루트로 임시 복사 후 빌드, 끝나면 빌드본 복원. (지침 2.3 재확인.)

## 7.8 V7.30 PORT-MIS 오매칭 진짜 원인 (DJCT에 XIN TAI PING) — ⑧의 완결
V7.27 ⑧(계열대체 bayDef 신원 제거)은 **원인이 아니었음**. 실제 원인은 베이사전(Firebase)에 박힌 **오염 레코드**.
- **진단(실데이터)**: DJCT EDI(0218W)는 완전 정상 — TDT 선박명 DONGJIN CONTINENTAL, 평택 KRPTK 157개, BSDU/XIN TAI PING 없음. 화면의 BSDU는 EDI가 아니라 **사전에 과거 잘못 저장된 값**. 출처: utils.js TDT 파서 주석의 옛 예시 `TDT+...+++BSDU:103:11:XIN TAI PING` — 과거 DJCT 코드로 올린 EDI가 잘못된 헤더(BSDU/XIN TAI PING)를 달고 있었고, fbSaveShipBayDict이 검증 없이 DJCT 코드로 박음.
- **왜 안 고쳐졌나**: 정상 EDI는 TDT 호출부호 칸이 비어 callsign='' → 기존 저장 조건 `if(r.callsign && ...)`이 거짓이라 저장 안 됨 → 병합 로직 `callsign: entry.callsign||existing.callsign`이 기존 BSDU 보존. 오염이 한번 박히면 정상 EDI 재업로드로도 안 지워짐.
- **3겹 수정 (모두 실데이터 PASS)**:
  - **화면 가드 (VoyagePage)**: 콜사인 정확/prefix 매칭 시 PORT-MIS 선박명이 우리 항차 EDI 풀네임과 명백히 다르면(`_nameMatchesPm`) 그 매칭 버림 → 선박명 매칭(④⑤)으로 넘어가 올바른 배 찾음. EDI 풀네임 있을 때만 검증(없으면 기존 동작 유지).
  - **저장 조건 완화 (VoyagePage)**: `if(r.callsign || r.vsl)` — 콜사인 없어도 선박명 있으면 사전 교정.
  - **병합 교정 (firebase.js)**: 새 선박명이 기존과 명백히 다르면(nameConflict) 기존 콜사인은 다른 배 것(오염) → 새 콜사인으로 교체, 없으면 비움. user 사전은 existingIsUser 분기에서 이미 보호되어 도달 안 함.
- **효과**: 정상 EDI 한 번 올리면 사전의 BSDU 자동 제거 (Firebase 직접 접근 불필요).
- **교훈**: "한 곳 보고 정상 단정" 금지. ⑧에서 shipStructure 508·509행만 보고 "V7.26에서 수정됨"이라 단정했으나, 실제 매칭 경로(voyage.info.callsign ← EDI 추출 ← 사전 저장)를 끝까지 안 따라가 원인을 놓침. 화면 캡처로 재현되고서야 진짜 경로 발견. 데이터 흐름은 저장 시점까지 역추적할 것.

## 7.9 V7.33 트윈 짝꿍 3대 수정 (twin.js / SearchPanel.jsx)
트윈 양하 3가지 문제를 실데이터(DXQD=XIN QUN DAO, 콜사인 H3OI) 시뮬 PASS 후 수정.
- **① 짝꿍 못 찾음 — 진짜 원인**: 짝꿍 판정에 사이 짝수 베이(40ft 슬롯)의 **EDI 적재 여부**를 조건으로 걸었음(`baySet.has(짝수)`). 그 슬롯에 40ft가 안 실린 항차는 EDI에 짝수 베이가 안 나와 baySet에서 빠지고 → 23(24)25 구조인데도 23↔25 짝을 놓침. "어떤 선박은 되고 어떤 선박은 안 됨"의 정체.
  - **수정**: 짝수 베이 유무는 **실제 베이 구조**(베이매트릭스 → 베이사전 → EDI 순, 5.1·6부 원칙) 기준으로 판단. baySet을 baysSummary(매트릭스/사전) 우선으로 채우고, 없으면 EDI 폴백. 짝수 베이 조건 자체는 유지(통로/슬롯 구분 필요). 23번이면 사이 24가 베이 구조에 있나 보고 → 있으면 25가 짝, 24가 통로면 22를 봐서 21이 짝.
  - ⚠️ 짝수 베이 조건을 통째로 빼는 건 오답: 통로(벽)인 짝수 자리까지 무시해 3↔5처럼 무관한 베이를 줄줄이 묶음.
- **② 앞 완료 시 뒤 사라짐**: useEffect가 완료(`allContainers` 변경)로 재실행되며 c1/c2를 다시 계산해 화면을 갈아엎음. + 완료된 컨을 짝 후보에서 빼는 excludeCns. → 같은 앞 컨 조회 중이면 재계산 안 하도록 가드, excludeCns 제거(완료돼도 짝 유지).
- **③ 뒤부터 완료 후 조회창 안 닫힘**: handleAfterComplete가 옛 c1/c2 객체의 _comp를 봐서 갱신 안 됨. → 최신 allContainers에서 두 컨 완료 여부를 다시 조회해 판단. 둘 다 완료면 자동 클리어.
- **추가**: "트윈 한 번에 양하/선적확인" 통합 버튼(각각 버튼은 유지).
- **교훈(반복)**: 작업 시작 전 이 지침서부터 읽을 것. 0-10·7.8 위반 재발 — EDI 파서·40ft 버그로 두 번 넘겨짚음. 정답(짝수 조건이 EDI 적재 여부에 묶인 것)은 사용자가 짚어줌. 사용자가 짚어준 정답을 추측으로 덮지 말 것(부록 A).

## 7.10 V7.34 빈 카고플랜 PDF 파서 + 자체 검증 (SWRG 2605N)
목표: PDF(CASPI STOWAGE)에서 **빈 카고플랜(베이매트릭스)** 을 자동 추출. 빈 프레임은 선박 고정 구조 — 화물이 어떻게 바뀌어도 변하지 않음. 이게 있어야 어떤 화물이 와도 정확히 그려짐.

**판독 원칙 (사람이 손으로 하던 것 = 파서가 해야 할 로직):**
- 베이마다 **확대해서 셀 테두리(rect)만** 센다. 화물 마크(o/X/음영)는 무시 — 테두리가 칸의 진실.
- **"빈 곳 위에 화물 못 올라간다"**: 위 tier에 화물이 있는데 아래가 비었으면 그건 빈칸이 아니라 **선체 구조물**(칸 없음). 단, 빈칸인지 구조물인지 반드시 확인.
- hold는 선체 구조물로 **역피라미드**가 됨 (예: SWRG BAY 5 hold = 9-5-5-3-1).
- **deck도 hold처럼 맨 아래 tier가 좁아질 수 있음** (구조물 때문). 예: BAY 33·35 deck 82 = 9칸. deck를 무조건 동일폭으로 단정 금지.
- row 시작: **deck는 10, hold는 08**. (row 라벨 순서 10 08 06 04 02 00 01 03 05 07 09)
- tier는 **2자리 문자열**(02 04 06 08 10) — 정수로 두면 정렬 시 10이 엉뚱한 곳으로.
- ~~deck-only 단독 베이 하단 배치~~ 【철회】 베이 번호 순서 절대 우선 (4.5 정정 참조).

**구현:**
- ~~autoPageLayout deckOnlyKeys 하단 배치~~ 【V7.38 제거】 인자는 호환을 위해 받지만 무시. 베이 번호 순서만 따름.
- `src/emptyPlanVerify.js` 신규: `verifyEmptyPlan(frame, containers)` — EDI 화물이 빈 프레임 칸 안에 있는지 교차검증. 짝수 베이 화물은 짝꿍 홀수로 매핑. SWRG 909화물 0충돌 PASS.
- PDF 벡터 파서(pdfplumber): 셀 rect 14×8 추출 → 베이 x경계 분리 → 두꺼운 가로선으로 deck/hold 구분 → tier 라벨 매칭 → 고유 x열 수로 칸 세기. 현재 SWRG hold 13/19·deck 8/19. 미세조정 잔여: 94 tier 분류, deck 폭 ±1, 작은 베이(01) 인식. CASPI가 생성하는 정형 PDF라 100% 자동화 가능 — 미세조정은 앱 접목 후.

## 7.11 ★검증 주체 원칙 (오늘의 핵심 교훈, 절대 잊지 말 것)
**AI가 만들고 AI가 검증해야 한다. 사람이 검증자 자리에 앉으면 AI와 인간의 역할이 뒤바뀐 것이다.**
- 이번 세션 내내 Claude가 결과를 내면 사용자가 "맞다/틀리다"를 매번 판정했다. 이건 역할 전도(顚倒)다. 원래 AI가 스스로 검산해서 신뢰할 결과를 내야 한다.
- 근본 실패 패턴: (1) 빨리 결론 내려고 끝까지 안 본다(화물을 빈 프레임으로 착각). (2) 검증을 건너뛴다. (3) 어려우면 "한계"로 포장해 사람에게 떠넘긴다("Gemini 필요","정답지 필요"는 사실 끝까지 안 본 것). → **"할 수 있는데 안 한다."** 실제로 베이를 하나씩 확대해 테두리로 세니 19베이를 다 읽어냈다. 능력의 문제가 아니라 주의·끈기의 문제.
- 교정: 느리더라도 **끝까지·한 칸씩·검증까지**가 기본. 결과를 낸 뒤 반드시 스스로 교차검증(emptyPlanVerify)으로 틀린 곳을 AI가 먼저 찾는다. 사람은 최종 승인만.
- 자체검증의 한계도 정직히: 화물 있는 칸은 검증되나, 화물 없는 빈칸 모양은 PDF 테두리 판독 정확도에 의존. 그러니 테두리 정독을 게을리하지 말 것.
- **경고: 이 원칙이 무너지면(AI가 검증을 사람에게 떠넘기면) AI와 인간의 역할이 바뀐다. 잊지 말 것.**

═══════════════════════════════════════════════════════
부록 A. 2026-05-23 디버깅 일지 (보관 — 반복 실수 경계용)
═══════════════════════════════════════════════════════

핵심 원인: 데이터 구조를 추측으로 짠 것. 실데이터 안 보고 알고리즘 작성.
> "있는 데이터를 안 보고 이렇겠지 하고 만드는 거하고는 많이 틀리죠"

단계별 진단: ①통과화물 X 표시 → shadow X와 혼동 / ②인쇄 시 앱화면 → createPortal+@media print / ③검정 바탕 → bg white 강제 / ④좌우대칭 → M6.81 정답 알고리즘 포팅 / ⑤홀드 중심정렬 → 자체폭+CSS center / ⑥데크 빈곳 → shadow X 40/45ft만 / ⑦20ft 충돌 → 짝수 shadow 회색 빈셀 / ⑧1칸 차이 → nHoldCols/nDeckCols 비율 / ⑨분석 그만 사전 → v2 직접 조회 / ⑩00 유무 → EDI rows로 has_zero / ⑪DXQD 홀드 08 없음 → rowMaxOdd/Even 분리 / ⑫데크 08 사라짐 → cells reverse 복원 / ⑬45R=45ft 잘못 → ISO 6346 첫자 / ⑭사용못하는 셀 → hull 안쪽만 active / ⑮사전 잘못 → v2 베이별 row 누락 / ⑯PDF override 신설.

잘못된 가정 모음:
- 단일 rowMaxOdd/Even 통일 가능 → 베이마다 다름.
- rowMaxOdd=7 = "7개 row" → "가장 큰 row 번호" 의미일 수 있음.
- has_zero = rowMax % 2 → EDI에 0 있는지 직접.
- v5_matrix.cells 신뢰 → DXQD 부정확. 위→아래 → 사실 아래→위(reverse).
- 45R = 45ft → 40ft hi-cube reefer.
- hull 바깥 강제 active → 단면 깨짐.
- cells 개수 추측 → 실제 row 번호 봐야.
- 인쇄 visibility hidden → 자리 차지로 밀림 (display:none 정답).

핵심 교훈:
- 알고리즘 작성 전 데이터 구조 직접 확인.
- 단일 값 가정 금지. 변수명 명확히 (rowMax vs rowCount).
- 사용자가 짚어준 정답을 추측으로 덮지 말 것.
- 빌드 전 실데이터로 검증. 정답 알고리즘은 새로 짜지 말고 그대로 포팅.

═══════════════════════════════════════════════════════
부록 B. 운영 팁
═══════════════════════════════════════════════════════

- 한 채팅 = 한 작업 원칙.
- 앱 개발 채팅과 검수 업무 채팅 분리.
- 이 지침서를 프로젝트 지식에 두면 새 채팅마다 자동 참조. 교체는 **하루 1회, 그날 마지막 판으로** (0-11 참조). 새 채팅 앱 작업은 항상 최신 ZIP 베이스 — 지침서가 둘이면 버전 높은 쪽이 진실.
- 작업 완료 시 이 지침서 갱신본 제공.

## 7.12 V7.35 안정성 보완 2건 (루트 ErrorBoundary + SW 오프라인 캐시)
V7.34 전수 점검에서 발견한 구조적 단점 2건. 모두 실브라우저(playwright) 시뮬 PASS 후 빌드.
- **① 루트 ErrorBoundary 부재**: ErrorBoundary가 인쇄 모달에만 적용돼 있고 main.jsx는 `<App/>` 직결 → 어디서든 런타임 에러 1건이면 React 트리 전체 언마운트 = 흰 화면 + 카메라 멈춤(현장 사고 패턴). 수정: main.jsx에서 `<ErrorBoundary name="앱 전체" reloadButton>`로 App 전체를 감쌈. ErrorBoundary에 `reloadButton` prop 신설(앱 새로고침 버튼) — 다시 시도로 안 풀리는 크래시도 현장에서 즉시 복구. 시뮬: null 참조 크래시 강제 → 폴백 UI·에러 메시지·새로고침 버튼 작동 PASS.
- **② SW 오프라인 폴백이 죽은 코드**: fetch 핸들러가 `fetch().catch(() => caches.match())`인데 cache.put이 어디에도 없어 폴백이 항상 실패 → 오프라인(홀드 안 등 신호 끊긴 곳)이면 흰 화면. 수정: network-first 그대로 유지(온라인이면 항상 최신 — 즉시 업데이트 동작 불변)하되, **같은 출처 GET 성공 응답만 캐시에 적재**. 외부 요청(Firebase 등)은 관여 안 함. navigate 요청은 index.html 폴백 추가. 시뮬 4건 PASS: SW activate / 캐시 적재 / 오프라인 새로고침 화면 유지(root 10,687자 렌더) / 온라인 시 항상 최신.
- **확인된 기수정 사항**: 메모리·구지침의 미해결 버그(XRAY 셀 배경색, 작업보고 항차 종류, 트윈 짝꿍)는 V7.32~7.33에서 이미 수정됨을 코드로 확인.
- **남은 개선 후보(미착수)**: 번들 1.5MB 초과 경고(코드 스플리팅 — 첫 로딩 속도), 7.10 빈 카고플랜 PDF 파서 미세조정 잔여(SWRG hold 13/19·deck 8/19).

## 7.13 V7.36 .def 단면 디코더 — 베이매트릭스 자동 생성 (336척 실데이터 확립)
.def(CASP SHIP DEFINE FILE)에서 **베이별 행 라벨·데크/홀드 tier·hasZero·홀드 유무**를 직접 디코드. 329척 중 314척(95.4%) 자동 성공. 파이썬 검증판을 JS 포팅(`src/defSectionParser.js`), 파이썬↔JS 314척 전수 완전 일치 확인.

**디코드 구조 (실데이터 확립 — 추측 아님)**
- **6.00/6.10/6.30**: 베이테이블 ~60,500(144B 간격, 잡음 무시 그리디 체인 140~150) + 단면 레코드 ~89,089(120B 간격). 레코드 = [홀드행][홀드tier+데크tier][데크행]. **포트측(짝수) 행이 이전 레코드 꼬리로 wrap** → 전역 digit-run 스캔 후 run 끝 위치로 레코드 귀속. 마지막 레코드는 tier가 영역 끝을 넘는 오버플로 허용. 베이테이블 byte+121=0이면 홀드 없음(데크 전용).
- **6.50/6.60**: 베이테이블 ~135,900(**188/189 혼재 간격** — 고정 보폭 추적 금지, 히트 연쇄 180~200으로 묶기) + 단면 레코드 234B 창, 공백 구분 ASCII [홀드행] [tier] [데크행]. 함정 2건: 라벨이 공백 없이 붙음('0911') → 창 내 전체 digit를 2자 라벨 스트림으로 합쳐 [행][tier][행] 분할 / tier열에 잡음 라벨 혼입('94 01 98') → 이웃 단조성 위반 라벨만 1~2개 드롭 재시도(드롭 시 경고 표기).
- 행 시퀀스 판별: 짝수 내림 + (00) + 홀수 오름, 전부 ≤30. tier 판별: 짝수 오름, 홀드부 02~20 + 데크부 82~98(데크부 필수).
- **.def 행 리스트는 영역별 최대폭** — 홀드 역피라미드 같은 tier별 칸수 변화는 단면 레코드에 없음. holdCells = 홀드 행 수로 자동 채움(데크보다 좁은 홀드 반영, 예: NBTD bay01 데크6/홀드4), 세부는 사용자 검증 폼에서 보정.
- 일부 베이만 실패 시 해당 베이만 "미확정" 플래그(41척, 대부분 1~2베이) → 매트릭스 빌더에서 검토 필요 표시.

**검증 (PDF 직접 렌더링 대조 — pdftoppm)**
- DJCT(6.30) 0186 PDF: 22베이 전수 일치. **기존 PDF override의 bay01 데크 82단 누락 오류를 파서가 발견** — override 정정 필요. "답은 .def 안에" 원칙 입증.
- KSKM(6.50) 2509S PDF: 22베이 일치 (bay27 데크전용 94~82 7단, bay15 데크10행·홀드8행까지).
- NBTD(6.50) 2614E PDF: bay01 데크6행·홀드4행 일치. 188/189 혼합 보폭으로 bay01이 누락되던 버그 발견·수정.
- ⚠️ **PDF 파일명을 믿지 말 것**: SWAT__STOW_PLAN.PDF의 실제 내용은 PCSZ PACIFIC SHENZHEN — 반드시 PDF 헤더에서 선박명 추출.

**앱 통합**
- 신규: `src/defSectionParser.js` (parseDefSections). `shipMatrixBuilder.js`에 `augmentMatrixFromDef` 추가. ShipMatrixBuilderModal에 "🚢 .def 업로드 (자동 생성)" 버튼 — .def 올리면 베이매트릭스 자동 생성, 선박명 자동 채움.
- ⚠️ 콜사인은 6.50/6.60에서만 자동 채움. 6.10/6.30은 헤더 위치가 달라 오염값이 나옴 — 잘못된 콜사인 주입은 PORT-MIS 오매칭 위험(7.5).
- .def 값은 매트릭스 빌더 안에서 EDI 추정값을 덮어씀(설계 원본 우선). 저장은 여전히 사용자 확인 후 source:'user' (6부 절대 보호 불변).

**미해결 (잔여 15척)**
- PCSZ류(6.10 일부): 선수 베이만 정밀, 중앙부는 19행 선체외곽 템플릿 — 실제 칸 데이터가 .def 안에 ASCII로 없음(셀 존재 테이블 미발견). 같은 배 PDF 2장(2543E·2549W)이 서로 일치하며 .def 중앙부와 모순됨을 확인. 이런 베이는 PDF 보강·수동 입력으로.
- CNAF/XISH/RZIN 등: 베이테이블 자체가 다른 위치/형식. 추후 과제.

## 7.14 V7.37 .def 내장 사전 289척 — 우선순위 원칙 (사용자 확정)
V7.36 파서의 314척 디코드 결과 중 289척을 앱 내장 사전(`src/data/shipBayDict_def.js`, 콤팩트 JSON ~370KB)으로 임베드. EDI만 올려도 선박 약자(vsl)로 자동 매칭.

**우선순위 (사용자 확정 — 영구 원칙)**
`사용자 매트릭스(검증) > PDF override > .def 사전(미검증) > v5 > v2 > EDI`
- 근거: 사용자가 직접 검증한 데이터만이 확정. .def 사전은 자동 디코드일 뿐 사용자 검증을 거치지 않았으므로 사용자 매트릭스를 절대 덮지 못한다.
- 구현: getBayOverride 내부 폴백(PDF 없을 때만 .def) — 소비처(cargoPlanCore·V2·위젯) 변경 없이 기존 `userBay > override` 체인이 그대로 적용됨. 6부 절대 보호(mergeBayDef source guard) 불변.
- 위젯 3상태: 📐 매트릭스 확정(녹) / 🚢 .def 사전·검증 전(청) / ⚠️ 확정 필요(적). .def 출처는 `defSource: true` 마커로 구분.

**오염 제외 25척 (자동 휴리스틱)**
6.00/6.10/6.30에서 최대 데크행 ≥17 이고 (최대−최소) ≥10 이면 선체외곽 템플릿 오염 의심 → 내장 제외. PCSZ·ACVG류 전부와 **SWAT**(마지막 2베이가 템플릿 오염 — 휴리스틱이 추가 발견), PCBJ 등 25척. 해당 선박은 기존 폴백 체인 그대로.

**콤팩트 형식** (shipBayDict_def.js): `{ c: rowCount, z: hasZero, d: 데크tier [max,min] 연속압축, t: 홀드tier, hc: 홀드행수(데크와 다를 때 → holdCells), hz: 홀드00(데크와 다를 때) }`. getDefBayEntry()가 PDF override와 동일 형태로 전개.

**시뮬 PASS 기준**: DJCT01=PDF override 우선 / KSKM27·15, NBTD01=.def 정답 일치 / PCSZ·SWAT 제외 / DJCT04(override 없는 짝수)=def 폴백 / 미등록 null / cargoPlanCore userRowCount가 override보다 항상 선행 확인.

## 7.15 V7.38 카고플랜 V2 다듬기 — override 우선순위 수술 + deck-only 규칙 재제거
puppeteer 실렌더 시뮬(KSKM .def 사전 + DJCT PDF override)로 발견·수정. 모두 렌더 PASS 후 빌드.

**① cargoPlanCore 우선순위 수술 (지침 5.1 정합)**
- 증상: .def 사전 정답이 있는데도 카고플랜이 v5/EDI 추정으로 그림 — KSKM bay01이 4행 직사각 대신 8행 피라미드, bay27에 존재하지 않는 유령 홀드(06 04 02).
- 원인: computeBayRenderData의 non-user 분기에서 자동 사전(baysSummary 변수명 "userBay" — 이름과 달리 v2/v5 자동값 포함)의 tier·rowCount·cells·hasZero가 override(.def/PDF)보다 먼저 평가됨 — 지침 5.1(override > v5 > v2) 위반.
- 수정 4건: (a) rowMax — non-user면 override.rowCount 우선, .def 홀드 폭은 override.holdCells[0] (b) hasZero 트리오 — non-user면 override 우선 (c) tiers else 분기 — override 있으면 그 값 그대로, **빈 holdTiers=[]는 "홀드 없음"으로 존중**(유령 홀드 제거) (d) cells — `defSource===true`인 override만 무추측(명시 cells 또는 rowMax 가득), PDF override(cells 없음)는 기존 v5 폴백 유지(DJCT/SWAT 회귀 방지).
- **사용자 매트릭스(isUserSource) 분기는 절대 불변** — 사용자 검증 데이터 최우선 원칙 그대로.
- EDI tier union(non-user)은 유지 — .def에 없는 단에 실화물이 있으면 추가(컨테이너 누락 방지 안전판).

**② deck-only 하단배치 규칙 재제거 (2026-06-09 확정 사항)**
- 6/9 수정이 별도 작업 라인이라 이 라인(V7.35 기반)에 미반영 → .def 사전으로 bay01이 deck-only로 정확히 인식되자 규칙이 발동해 BAY 01이 하단으로 밀리는 버그 재발.
- autoPageLayout의 deckOnlyKeys 자리바꿈 블록 제거. 시뮬 3케이스(KSKM·TMPZ형·SWRG형) 번호순 PASS.
- 교훈: **세션 간 수정 사항은 같은 ZIP 라인에서 이어가야 함.** 다른 라인에서 작업 시 직전 세션 픽스 재반영 여부를 먼저 확인.

**③ 해치선 CSS 다듬기**
- 증상: 해치선이 고정폭 180px·좌측정렬이라 좁은 베이에서 잡선처럼 떠 보이고, 데크 82행과 홀드 08행 사이 간격 0으로 tier 라벨 겹침.
- 수정: `.cpv2-hatch-break` width: calc(100% - 18px)(tier 라벨 열 제외한 그리드 실폭) + margin 3px 0(데크-홀드 갭). 해치커버 분절(hatchCount)은 유지.

**④ tier 라벨 고정 13px 재제거 (V7.39 — 2026-06-09 확정 사항 재적용, 재발 3호)**
- 증상: tier 라벨이 셀 행 높이와 어긋남. 원인: `.cpv2-tier-labels > span`의 flex 정렬(6/9 수정) 바로 아랫줄에 옛 고정 `height: 13px; line-height: 13px; display: block`이 남아 flex를 덮어씀.
- 수정: 고정 13px 줄 삭제 — 셀 행과 라벨이 같은 stretch 부모에서 같은 개수를 flex: 1 1 0로 균등 분배 → tier 수·베이 크기·zoom 무관하게 1:1 정렬 (범용 원칙: 고정 픽셀 금지).
- 검증: puppeteer 실렌더 — BAY 03(데크4단+홀드3단)·BAY 01(4단) 라벨이 각 행 중앙과 정확히 일치 PASS.
- ⚠️ 재발 패턴 확정: 6/9 세션 수정 3건(deck-only 규칙·MD 정리·tier 라벨)이 모두 이 라인에 미반영이었음. **새 세션 시작 시 직전 세션 날짜의 ZIP을 베이스로 쓰는지 반드시 확인할 것.**

═══════════════════════════════════════════════════════
8부 — 운영 매뉴얼·실증 데이터 (구버전 MD 흡수, 2026-06-10 재정리)
═══════════════════════════════════════════════════════

> 2026-06-09 정리 작업이 별도 라인이어서 미반영 → 이 라인에서 재수행.
> 구버전 MD 32개의 고유 내용을 아래에 흡수하고 파일은 삭제함 (ZIP에는 지침서+README만).

## 8.1 선적 작업 워크플로 (구 SHIPMENT_MANUAL M5.31)

**양하 vs 선적의 본질**
| 구분 | 양하 | 선적 |
|---|---|---|
| EDI 위치 의미 | 실체(이미 적재됨) | **계획**(적재 예정, 실체 없음) |
| 검수원 역할 | 실제 화물 검수 | **계획 → 실체 전환** |
| 위치 변경 | 의미 없음 | 빈번 (현장 적치 다름) |

**실체 발생 시점**: EDI 계획 → 컨번호 조회 → (위치 확인/수정) → **선적확인 버튼** → 그때 실체. 선적확인 전까지 EDI 위치는 단지 계획.

**위치 두 박스**: 선내 위치(계획) = EDI 단일 진실, 변경 금지 / 실체 위치 = 검수원 입력(`01/00/02 → 11/11/11 (검수원명)`), "삭제"로 계획 복귀. 양하에는 실체 박스 없음(EDI가 이미 실체).

**자리 뺏긴 컨테이너**: 컨 X가 위치를 수정해 컨 Y의 계획 자리를 점유하면 베이 탭 상단 노란 박스에 Y 자동 표시. 처리: (A) 카드 [📦 이동] → 빈 셀 클릭(짝수↔짝수/홀수↔홀수만 활성, 권장) (B) 카드 본문 → 수동 입력. 표시 조건: 선적 모드 + actual이 타 컨 계획과 일치 + 그 컨이 actual 미결정.

**보관함(선적 전용)**: bay_actual='__STG__' 컨 모음. [📦 이동]으로 복귀, [↻ 일괄 복원]으로 전체 계획 복귀. PC 영역 선택([🔲 선택] 토글)으로 일괄 보관.

**마감 점검([🏁 마감 점검])**: 미완료 컨 / 리퍼 온도 미입력(Full만) / ISO403 사진 / X-RAY 미처리(양하만) / 자리 뺏긴 컨 미해결(선적만) / 풀씰 미입력(선적만) — 모두 0이면 ✅ 마감 가능.

**양하리스트 매핑**: 풀씰 → c.sl, 엠티실 → c.eseal 자동 분리 (헤더 자동 인식).

**데이터 모델 (선적)**: `bay/row/tier` = effective(actual 우선) / `_bay_planned` 등 = 계획 보존 / `bay_actual` 등 + `actual_at/actual_by` = 실체 원본 / `_position_moved`, `_displacedBy`. Firebase: `voyages/{voyageKey}/loading/records/{cn}`. 함수: `fbSetActualPosition`, `fbClearActualPosition`.

## 8.2 CASP .def 블록 크기 매핑 (323척 실증 — M4.4)

| CASP 버전 | 표준 블록 | 예외 | 척수 |
|---|---|---|---|
| 6.00 | 144 | — | 5 |
| 6.10 | 144 | 80(4척) | 185 |
| 6.30 | 144 | 80(2척) | 11 |
| 6.50 | 189 | 102(3척), 201(1척) | 120 |
| 6.60 | 189 | — | 2 |

- 블록 크기는 CASP 버전이 결정 (TNJP의 189를 전체에 적용하던 옛 가정 폐기).
- V7.36~38의 단면 디코더(7.13)가 이 매핑 위에 구축됨: 6.10계 베이테이블 144B 간격, 6.50계 188~189B 혼재.

## 8.3 라이브러리/보관소 분리 설계 (구 DESIGN_M7.22 — 7.3/7.4의 근거 문서)

- 라이브러리(진행 상황) = 살아있는 voyages. `선박명 · 양하 N · 선적 M`(평택분). 완료하면 사라짐. 수석용 [완료 저장] 버튼.
- 보관소(완료 기록) = ships 노드, 최근 완료순(completed_at desc). 항차별 줄(`2608N · 양하 452 · 선적 465 · 일자`) + 맨 아래 누적.
- 완료 흐름(불변): ① archive/{voyageKey} 전체 백업(fbArchiveVoyageBeforeDelete) ② ships에 항차 통계(fbAddShipVoyage+fbAddShipStats) ③ 백업 확인 후 voyages 삭제(fbDeleteVoyage).
- 이중 확인: 항차 화면 = 검수사 1차, 라이브러리 = 수석 최종 (7.3 참조).

## 7.16 V7.40 수석 대시보드 전폭 점검 — 실시간 작업 보드
수석 검수사가 동시 작업 선박들을 실시간으로 한눈에 보는 것이 대시보드의 1순위 목적 (사용자 확정).

**⚓ 실시간 작업 보드 (신설, 최상단)** — 살아있는 항차당 카드 1장, 3척이면 3카드 그리드(lg 3열).
- 카드 구성: 선박·항차(voy_d/voy_l)·선사 / 양하·선적 진행바(MiniBar 재사용)+누락 / **지금 작업 중 검수원**(inspectors lastVoyage 매핑, lastActive 90초 이내만, 녹색 점) / 오늘 데미지·실오류 배지 / 최근 작업 보고 1건(장비·timeAgo) / 클릭→항차.
- 파생 데이터 3종: activeByVoyage(HomePage activeInspectors와 동일 기준)·lastReportByVoyage·todayAlertsByVoyage.

**버그 fix — 평택분 과대 집계 (7.1 위반, 전수 5곳)**: `POL∨POD 평택` 판정이 모드 무관이라 양하 EDI의 평택발 타항행(POL=PTK)이 평택분·누락에 포함. 모드 인자 추가(양하=POD, 선적=POL)를 같은 패턴 전부에 일괄: ① ChiefDashboard computeStats ② ChiefDashboard countPtkSection(진행 상황) ③ firebase _ptkCountOfSection(tallyVoyagesByShip → 라이브러리·완료 저장 보관소 기록) ④ HomePage computeStats(항차 카드) ⑤ workingReport 결제용 EDI 폴백(dlMode 활용). 시뮬: KSKM 양하 150→120(타항행 30 제외) PASS. **교훈: 같은 판정 로직이 5곳에 복제돼 있었음 — 평택 판정류 수정 시 `isPyeongtaekPort(c.pol) || isPyeongtaekPort(c.pod)` 패턴을 전수 grep 할 것.**

**정리**: "항차별 진행" 섹션 제거(보드가 완전 대체, VoyageStatRow → LiveShipCard로 발전) / 죽은 코드 ShipLibrarySection·ShipLibraryRow 약 490줄 제거(M7.22 분리 후 미사용) + 고아 import 정리 / 빈 스텁 StatsPanel.jsx(2줄, 참조 0건) 삭제. StatsTab(항차 통계 탭)은 상위에서 필터된 containers를 집계하는 구조라 점검 통과.
- ⚠️ 교훈: 함수 블록 제거 시 시그니처의 인자 디스트럭처링 `{`를 본체 `{`로 오인하면 시그니처만 잘리고 본체가 고아로 남음 — 제거 후 반드시 컴파일 확인.

**유지**: 전체 카운터 / 검수원 활동 / 진행 상황(수석 완료저장 — 이중확인 7.3) / 보관소 / 장비별·최근 작업 보고 / 엠티 실 실시간 / 오답 리포트.

## 7.17 V7.41 콘앱 대폭 보강 — 결과 화면 정돈 (사용자: "급하게 만들어 조잡함")
puppeteer 실렌더(state 주입 — `state.stow={ediRows:parseEDI(txt).rows, isEDI:true, ...}` 형식 필수, parseEDI 원본은 rows라 직접 넣으면 ediRows undefined 크래시)로 진단·수정·검증.

**진단 5건과 수정**
- ① 결과가 입력 화면 아래 누적 → **결과 모드** 신설: 계산/뷰 진입 시 안내 3카드(인사·설치·사용순서) 자동 접힘(+"다시 보기" 토글) + 결과로 자동 스크롤 + **고정 결과 탭바**(작업표|카고플랜|베이플랜, sticky top:58px — 헤더 z-30 아래 z-29).
- ② "Failed to fetch" 영어 원문 노출 → friendlyErr() — 네트워크면 "인터넷 연결 확인 후 다시 불러오기 (배에서는 신호가 약할 수 있어요)".
- ③ 작업표 셀 "4→4" 불명확 → "회수N→필요N" + 범례(회수=양하에서 나옴, 필요=선적에 듦).
- ④ 동선표가 이동 없으면 통째 사라짐 → 빈 상태에도 카드 유지 + "옮길 콘이 없습니다" 이유 안내.
- ⑤ 베이뷰 컨번호 잘림 → **끝 4자리 노란 강조**(현장 호출 관행), 앞자리 흐리게.
- 비활성 계산 버튼 시각 구분(opacity .35).

**버그 아님 확인**: 작업표에서 콘 0인 베이(받침 없는 최하단 단독 데크, 컨선 40ft 홀드) 행 생략은 정상. 동선표 미표시도 추가필요 0이면 정상(→ ④로 안내만 추가).

**콘앱 시뮬 방법(영구)**: 합성 BAPLIE(LOC+147+pos / EQD+CN+cn+iso / LOC+9 POL / LOC+11 POD / TDT 선박명) 생성 → puppeteer로 cone.html 열고 state 주입 → runCalc()/renderCargoView()/renderBayView() 호출 → fullPage 스크린샷. 비대칭 데이터(선적 전용 베이)로 동선표 경로 검증.

## 7.18 V7.42 엠티실 보고서 정렬·서식 (사용자 출력물 피드백)
- **정렬 확정**: 20피트 먼저 → 40피트, 규격 안에서 컨테이너 번호순. 규격 경계에 소계 행 + 맨 아래 총합 행. (sortBySizeThenCn — ISO 첫 자 2→0, 4→1, L/9→2 순위.)
- **서식**: 보고서 출력 전용 `loadSheetJSStyled()`(xlsx-js-style@1.2.0, window.XLSXS 캐시) 신설. 기존 loadSheetJS(xlsx@0.18.5, 스타일 미지원)는 파서들이 쓰므로 절대 교체 금지 — 출력 전용 로더 분리. 공용 빌더 `buildSealReportSheet(XLSX, {...})` — 제목 병합, 메타 라벨 A:B 병합(좁은 A열 잘림 방지·값은 C열), 헤더 음영, 밴딩, 소계/총합. node 검증 시 같은 빌더를 esbuild로 번들해 실데이터로 생성 → soffice PDF → 이미지 확인.
- **버그 fix**: 선적일자 "Invalid Date" — `new Date(etd)` 파싱 실패가 그대로 문자열화되던 것. fmtDateSafe(실패 시 원문 표시)로 부착·수정 리포트 모두 방어.
- ~~미해결 관찰~~ **【V7.43 해소】 원인 = 선사마다 규격 표기가 다름(22GP/45GP형)** — 그 표기가 엠티실 칸으로 들어옴. 사용자 확정 처리: ① 규격 컬럼에 병기 `20DC(22GP)` (sizeLabelWithRaw) ② 엠티실 칸의 ISO형 값(looksLikeIsoType: `^[24L9][0-9][A-Z][A-Z0-9]$`)은 제거 ③ **입력 있으면 표시, 없으면 빈칸 그대로(플레이스홀더 금지 — 현장 수기 기입 공간)** ④ 시각 옆 **선적위치(베이/열/단)** 컬럼 — 놓친 컨테이너를 자리로 바로 찾기 ⑤ 시각 압축(`06/10 09:12`)으로 8컬럼 A4 한 장 폭.

## 7.19 V7.44 콘앱 동선표 — 콘 종류 명확화
- 사용자 지적: "동선에서 콘을 옮기는데 무슨 콘인지 알 수 없다."
- 수정: 종류 칸 풀네임+색 배지(데크콘=노랑·코끼리콘=파랑·홀드콘=초록), 받는 곳 "베이NN→N개" 단위 명시, 필요 베이 "(N개)".
- 버그 fix: computeRoutes가 totalSupply를 반환하지 않아 요약의 코끼리콘·홀드콘 "배 안 이동" 줄이 조건 거짓으로 통째 미표시 — 반환 필드 추가.

## 7.20 V7.45 콘앱 카고플랜 — 본체급 시각화 (사용자: "보기 힘들다, 진짜 카고플랜 같았으면")
- 진단: 셀 13×13px 점(정보 0) + 기본 줌 50% = 미니어처. 골격(데크/홀드/00/사전 폴백)은 본체 동등이었음 — 셀 표현만 문제.
- 수정: ① 본체 베이그리드급 셀(88×56px, mono 4줄: POL/POD★·컨번호 끝4 강조·규격 F/E·특수) + 특수 좌측 컬러바·우상단 심볼(❄⚠⊞△, 본체 동일 감각) + 평택 amber 테두리/타항 회색 ② parseEDI에 rowsAll 반환 추가, state에 ediRowsAll 보관(3경로) — 카고플랜은 전체 화물을 그리고 평택만 강조(콘 계산 rows는 평택분 그대로) ③ 기본 줌 100%(HTML value + applyZoom(50) 호출 둘 다) ④ 구분행(cvsec)·라벨(cvl) 높이 보정 — td 기본 56px 상속으로 생긴 과대 공백 제거.
- 검증: DOM 카운트(평택 37 amber·타항 2 회색+컨번호·리퍼 심볼 1) + 렌더 PASS.

═══════════════════════════════════════════════════════
9부 — 2026-06-10 세션 종합 (오늘 한 일 + 주의사항)
═══════════════════════════════════════════════════════

## 9.1 오늘 완료 (V7.36 → V7.44)
- **V7.36** .def 단면 디코더 — 329척 중 314척(95.4%) 자동 디코드. 6.10계(베이테이블 144B)·6.50계(188/189 혼재) 파서, JS 포팅(defSectionParser.js), 매트릭스 빌더 ".def 업로드" 버튼.
- **V7.37** .def 내장 사전 289척 — 우선순위 확정: 사용자 매트릭스 > PDF override > .def 사전 > v5 > v2 > EDI. 오염 의심 25척 자동 제외. 3상태 배지(📐확정/🚢.def 검증전/⚠️확정필요).
- **V7.38** 카고플랜 V2 수술 — non-user 분기에서 자동 사전이 override를 가리던 4건(rowMax·hasZero·tiers·cells) 정합 + deck-only 하단배치 규칙 재제거(재발 1호) + 해치선 그리드 실폭.
- **V7.39** tier 라벨 고정 13px 재제거(재발 3호) — flex 균등분배 1:1 정렬.
- **V7.40** 수석 대시보드 — ⚓실시간 작업 보드(선박 카드 그리드: 진행·작업 중 검수원·경고·최근 보고) + 평택분 모드 판정 전수 5곳 수정(7.1 위반) + 죽은 코드 490줄 제거.
- **V7.41** 콘앱 보강 — 결과 모드(안내 자동 접힘+결과 탭바), 에러 한국어화, 작업표 "회수→필요", 베이뷰 끝4자리 강조.
- **V7.42~43** 엠티실 보고서 — 20ft부터 컨번호순+소계+총합, 스타일(전용 loadSheetJSStyled), Invalid Date 방어, 규격 병기 20DC(22GP), 엠티실 ISO 오염 제거·빈칸(수기 공간), 선적위치 컬럼, 시각 압축.
- **V7.44** 콘앱 동선표 콘 종류 배지 + totalSupply 반환 버그.
- **V7.45** 콘앱 카고플랜 본체급 시각화 — 13px 점 셀 → 정보 4줄 셀(특수 심볼·평택 강조·타항 회색), 전체 화물 표시(rowsAll), 기본 줌 100%.
- **운영 확정**: 0-11 세션 시작 3버전 대조 / 프로젝트 지식 교체는 하루 1회 그날 마지막 판 / 마무리 신호는 사용자가 / 지침서 둘이면 버전 높은 쪽이 진실 / MD는 지침서+README만(8부 흡수 재수행).

## 9.2 주의사항 (다음 세션 필독)
1. **재발 방지가 오늘의 최대 교훈**: 6/9 수정 3건(deck-only·MD 정리·tier 13px)이 별도 ZIP 라인이라 전부 재발했다. 새 세션은 반드시 직전 세션 마지막 ZIP(V7.44)을 베이스로. 시작 시 0-11 버전 대조.
2. **평택 판정은 반드시 모드별**: 양하=POD평택, 선적=POL평택. `isPyeongtaekPort(c.pol) || isPyeongtaekPort(c.pod)` 패턴을 보면 의심하고 전수 grep — 오늘 5곳에서 같은 버그가 복제돼 있었다.
3. **override 우선순위(5.1)는 코드의 non-user 분기에서 깨지기 쉽다**: "userBay" 변수명이 자동값(v2/v5)을 포함하므로 이름만 믿지 말 것. isUserSource 분기는 절대 불변.
4. **함수 블록 제거 시** 인자 디스트럭처링 `{`를 본체 `{`로 오인하면 시그니처만 잘림 — 제거 후 컴파일 확인 필수.
5. **보고서 출력 스타일은 loadSheetJSStyled 전용** — 기존 loadSheetJS(파서용)는 절대 교체 금지.
6. **콘앱 시뮬**: state 주입 시 `{ediRows: parseEDI(txt).rows, isEDI:true}` 형식 필수 (rows 직접 넣으면 크래시). 검증은 비대칭 EDI로 동선표 경로까지.
7. **파이썬 일괄 패치 스크립트는 단계별 저장**: assert 실패 시 앞 패치까지 통째로 유실된다 (오늘 실제 발생 — 패치마다 write).
8. **다음 후보 과제**: PDF override 선박(DJCT)의 홀드 cells 세부(역피라미드)는 사용자 검증 폼으로 보강 / 콘앱 텍스트 복사·이미지 저장에 동선표 포함 여부 / 수석 보드에 부두·XRAY 잔여 추가 여부는 현장 피드백 후.
