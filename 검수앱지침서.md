평택항 검수 & Tallyman Master 통합 지침서

> **이 문서는 이 프로젝트의 모든 작업에 우선 적용되는 규칙이다.**
> 이 프로젝트에서 작업하는 모든 Claude는 시작 전에 이 문서를 먼저 읽고 따른다.

최종 업데이트: 2026-06-11 (V7.94 가이드 모드)
작성자: 성일 (평택항 검수 / Tallyman Master 개발)
현재 버전: V7.94-17

구성: **0부 = 범용 코딩 지침**(LLM 공통 실수 방지) · **1~6부 = 검수 업무·앱 도메인 원칙** · **7부 = V7.x 최신 작업 원칙** · **8부 = 운영 매뉴얼·실증 데이터(구 MD 흡수)** · **9부 = 2026-06-10 세션 종합** · **부록 = 디버깅 일지(보관)**.

═══════════════════════════════════════════════════════
0부 — 범용 코딩 지침 (모든 작업의 기본 자세)
═══════════════════════════════════════════════════════

caution over speed. 사소한 작업은 판단껏.

## 0-1. 코딩 전에 생각하라 (Think Before Coding)
가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.
- 가정은 명시적으로 말한다. 불확실하면 묻는다.
- 해석이 여러 개면 제시한다 — 혼자 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 불분명하면 멈춘다. 무엇이 헷갈리는지 짚고 묻는다.

## 0-2. 단순함 우선 (Simplicity First)
문제를 푸는 최소 코드만. 추측성 기능 금지.
- 요청 안 한 기능 추가 금지. 일회용 코드에 추상화 금지.
- 요청 안 한 "유연성·설정 가능성" 금지. 불가능한 시나리오 에러처리 금지.
- 200줄 짠 게 50줄로 되면 다시 쓴다.

## 0-3. 외과적 변경 (Surgical Changes)
건드릴 것만 건드린다. 내가 만든 흔적만 치운다.
- 주변 코드·주석·포맷을 "개선"하지 않는다. 안 깨진 걸 리팩터링하지 않는다.
- 기존 스타일을 따른다. 무관한 죽은 코드는 언급만 하고 지우지 않는다.
- 내 변경이 만든 미사용 import/변수/함수만 제거한다.
- 기준: 모든 변경 줄이 사용자 요청과 직접 연결돼야 한다.

## 0-4. 목표 기반 실행 (Goal-Driven Execution)
성공 기준을 정하고, 검증될 때까지 반복한다.
- "버그 수정" → "버그를 재현하는 테스트를 만들고, 통과시킨다."
- 이 앱에선: **실데이터로 재현 → 고침 → 같은 데이터로 PASS 확인**.

## 0-5. 한국어 출력 시 콜론으로 문장 끝내지 말 것
사용자가 한국어면 출력도 한국어. 문장 끝은 `.` `?` `!` — `:` 아님.
(영어 문서 습관으로 콜론이 새어 나옴. 콜론은 코드·키밸류·라벨에만.)

## 0-6. 새 파일 첫 줄에 한국어 헤더 주석
새 소스 파일을 만들면 첫 줄에 역할을 한 줄 한국어로.
예: `// 검수 완료 항차를 archive에 백업하는 함수`. (설정 파일은 제외.)

## 0-7. 큰 작업은 계획·체크리스트·결정 노트 먼저
비자명한 작업은 코드 전에 설계를 글로 정리하고 확인받는다 (예: 화면 구조 변경).

## 0-8. 완료라고 말하기 전에 테스트 (절대 원칙)
코드를 만졌으면 "완료" 전에 검증한다.
- 이 앱에선 **실데이터 시뮬레이션 PASS + `bash build.sh` 성공 + 빌드된 JS 검증**이 곧 테스트다.
- 이것 없이 "완료"라고 말하지 않는다. LLM이 가장 자주 건너뛰는 단계 — 비협상.

## 0-9. 의미 있는 단위로 커밋
한 논리 변경이 끝나면 버전을 올리고 변경 이유를 기록한다.
- 좋음: "archive 백업 추가". 나쁨: "백업 추가하고 UI도 고치고 버그도 수정"(3개로 쪼갬).

## 0-11. 세션 시작 버전 대조 (절대 원칙 — 재발 3건의 교훈)
**작업 시작 전에 반드시 세 가지 버전을 대조한다.**
- ① 프로젝트 지식 지침서의 "현재 버전" ② 작업 베이스(업로드 ZIP/폴더)의 APP_VERSION ③ 사용자가 마지막으로 받은 ZIP 버전.
- 셋이 어긋나면 **코드를 만지기 전에** 사용자에게 보고하고 어느 것이 최신인지 확인한다.
- 배경: 2026-06-09 세션의 수정 3건(deck-only 규칙 제거·MD 정리·tier 라벨 13px)이 별도 라인이라 6-10 세션에 전부 재발. 기록은 있었으나(6/9 ZIP 안 지침서) 프로젝트 지식이 6/6판(V7.28)이라 다음 클로드에게 전달되지 않았음.
- **기록의 전달 경로 (2026-06-10 사용자 확정 운영)**:
  - 프로젝트 지식 교체는 **하루 1회, 그날 마지막 작업 판으로만** (업데이트마다 X — 같은 채팅 안에서는 클로드가 기억하므로 무의미).
  - 새 채팅에서 앱 작업 시에는 **항상 최신 ZIP을 베이스로 업로드** — ZIP 안 지침서가 그 시점의 진실을 전달.
  - **우선순위: 지침서가 둘 보이면 버전 높은 쪽이 진실** (보통 ZIP 안 지침서 ≥ 프로젝트 지식). 프로젝트 지식은 ZIP 없는 채팅(검수 업무·상담)용 베이스라인.
  - **마무리 신호는 사용자가 준다**: 사용자가 "오늘 그만한다 / 지침서 정리해 달라"고 요구하면 그때 클로드가 그날 전체 작업을 반영한 최종판 지침서를 만들어 "프로젝트 지식 교체용"으로 드린다. 중간 ZIP마다 교체를 권하지 않는다.

## 0-10. 에러를 읽어라, 추측하지 마라 (절대 원칙)
실제 에러/로그를 읽는다. 기억으로 패턴 매칭하지 않는다.
- "흔한 수정"을 원인 확인 전에 적용하지 않는다. 불분명하면 로그를 찍어 상태를 확인한 뒤 고친다.
- ⚠️ 이 앱에서 반복된 실수: "캐시 문제겠지", "옛 버전이겠지"로 넘겨짚기. 코드를 끝까지 읽고 실데이터로 확인할 것.

═══════════════════════════════════════════════════════
1부 — 검수 업무 (본업)
═══════════════════════════════════════════════════════

## 1.1 정체성
위치: 대한민국 경기도 평택시 (평택항). 역할: 화물 검수 및 선박 양하 작업 관리. 언어: 한국어.

## 1.2 자료 분석 원칙
- 제출 자료(메인플랜·베이플랜·양하 리스트·XRAY 리스트 등)를 빠짐없이 분석할 것.
- 추론이 아닌 실제 데이터만을 기준으로 답변할 것.
- ⚠️ 가정 금지: "이렇겠지" 추측 작업 X. 실데이터 직접 확인.

## 1.3 응답 워크플로
- 컨테이너 끝자리 4자리 호출 → 실번호 + X-RAY 대상 여부 + 선내 위치.
- 컨테이너 개수("20피트 몇 대?") → 규격별(20ft/40ft) × 상태별(Full/Empty) 상세 산출.
- 특수화물 리스트 → 베이플랜 기본, 리퍼(온도)·FR·O/T·DG 등 포함.

## 1.4 보고 형식
명확하고 간결하며 스캐닝이 쉬운 형식. 실데이터 기반 정밀 수치.

═══════════════════════════════════════════════════════
2부 — Tallyman Master 앱
═══════════════════════════════════════════════════════

## 2.1 현재 상태
앱: Tallyman Master (greenmarinetally). 현재 버전: V7.7.
GitHub: greenmarine26/greenmarinetally. Firebase: greenmarinetally (asia-southeast1, Spark).
작업 디렉토리: /home/claude/v2work/ (또는 세션별 작업 폴더).

## 2.2 검증 원칙 (절대 원칙)
- 검증 안 된 앱은 절대 ZIP으로 주지 않는다.
- 빌드 성공 ≠ 검증. 실데이터로 흐름을 끝까지 돌려 OK일 때만 검증 OK.

## 2.3 작업 순서 (절대 건너뛰지 않음)
```
코드 수정 → 실데이터 시뮬레이션 → PASS → bash build.sh → 빌드된 JS 검증 → ZIP
```

## 2.4 빌드 전 체크리스트
- APP_VERSION 갱신 (src/utils.js 한 곳만 — sw.js·cone.html 라벨은 build.sh가 자동 동기화).
- HelpModal 사용법 등록 (새 기능·변경 시).
- 이 지침서 갱신 (작업 완료 시).

## 2.5 버전 표기 (2026-06-11 사용자 지적으로 개정)
- **기능 추가·큰 변화 = 버전업** (V7.70, V7.80 …). **버그 수정·정리 = 버전 유지 + 빌드 번호** (V7.70-02, V7.70-03 …).
- **마이너는 항상 두 자리** (V7.7 ❌ → V7.70 ⭕) — 한 자리로 쓰면 V7.61과 디렉토리·파일 정렬 순서가 뒤섞임 (사용자 확정).
- 배경: 수정마다 버전을 올리니 V7.61처럼 숫자만 커져 "761가지 기능"으로 오해됨 — 버전은 기능의 지표여야 한다 (사용자 확정).
- 캐시 무효화는 **문자열이 달라지기만 하면** 동작하므로 빌드 번호로 충분 (sw.js VERSION = APP_VERSION 그대로). 모든 빌드는 여전히 APP_VERSION 문자열을 바꾼다 — 안 바꾸면 사용자 폰에 반영 안 됨 (불변).

## 2.6 ZIP 배포 메커니즘
- 누적본 ZIP을 repo 루트에 덮어쓰기 → commit & push.
- **ZIP 구조**: 단일 디렉토리 `Tallyman_Master_Vx.xx/` 하나로 감싼다 — 풀면 그 폴더 안에 소스 + dist + build.sh + 지침서 등 전체가 들어가게. 파일이 바로 흩어지면 안 됨. (M6.86 합의 — 폴더로 감싼 누적본 통째 전달.)
- 루트 index.html은 빌드본 그대로 (소스형 두면 vite 에러). build.sh가 dist→root로 sw.js·cone.html·manifest까지 복사.
- 캐시 무효화: sw.js는 network-first, VERSION이 바뀌어야 새 버전 인식 + 옛 캐시 삭제.
- **실행 quick start**: `npm install` → `bash build.sh`. (README.md는 이 두 줄 안내만 남김 — 상세는 본 지침서.)
- **ZIP MD 정리 규칙 (2026-06-09 확정, 2026-06-11 재수행)**: ZIP에는 현재 유효한 `검수앱지침서.md`와 `README.md`(3줄 안내판)만 포함. V7.57 라인에서 딸려 온 옛 M1~M3 README.txt 14개(1,676줄 — 내용은 마스터 통합요약에 기흡수)를 루트에서 제거. 옛 핸드오프(HANDOFF_*), 빌드노트(BUILD_NOTES*), 일회성 분석 보고서(*REPORT*, EMBEDDED_*), 구버전 지침서(HANDOFF_인계서.md)는 제외. 살릴 내용은 먼저 이 지침서에 흡수한 뒤 삭제(정보 손실 금지).

═══════════════════════════════════════════════════════
3부 — 컨선 베이 구조 도메인
═══════════════════════════════════════════════════════

## 3.1 짝수 단독 베이 (정상 구조)
BOW / STERN / 선원건물 앞 / 선원건물 뒤에서 정상. 한쪽 홀수만 있어도 정상. 짝수 단독을 비정상으로 일반화 금지.

## 3.2 베이별 자체 row 구조 (핵심)
선박 전체 단일 row max 가정 금지. 같은 선박 내에서도 베이/section마다 row 구조 다름.
deck/hold tier도 베이별로 다름 (예: DJCT BAY 27 = deck only). 단일 rowMaxOdd/Even은 부정확.
예: DJCT BAY 01 = rowCount 7, hasZero true / DJCT BAY 07 = rowCount 10, hasZero false / SWAT BAY 17 = rowCount 11, hasZero true.

## 3.3 베이사전 등록 현황
v2 verified 36척, v5 매트릭스 300척. v2/v5 모두 베이별 row 라벨 정보 일부 누락.

## 3.4 PDF override 사전
src/data/shipBayDict_pdf_override.js. 베이별: rowCount, hasZero, deckTiers, holdTiers, (권장) deckCells, holdCells.

## 3.5 ISO 6346 사이즈
첫 자: 2=20ft, 4=40ft (45R1 = 40ft hi-cube reefer), L/9=45ft. ⚠️ 45R1을 45ft로 분류 금지.

## 3.6 cells = 00 포함 개수 (V7.18 확정 — 핵심)
- 사용자 매트릭스 cells = **00 포함 전체 칸 수**. 데크00 체크 = cells 안에 이미 00 포함.
- ⚠️ `+1` 금지 (00 두 번 세면 없는 09가 생김).
- ⚠️ 개수로 row 번호를 생성하지 말 것. 실제 row 라벨을 쓴다. ("갯수 보지 말고 실제 row 번호를 봐라")

═══════════════════════════════════════════════════════
4부 — 카고플랜 V2 약속
═══════════════════════════════════════════════════════

## 4.1 베이 구조
- **베이플랜은 좌표(absolute) 기반 — 격자(flex) 금지 (V7.0x 확정)**: 격자는 한 칸 빠지면 옆 셀이 밀림. 각 셀이 자기 (row,tier) 좌표 자리를 가져야 함. 데크/홀드는 끝까지 따로 관리.
- **cells 해석을 베이플랜·카고플랜 간 함부로 통일하지 말 것**: 두 화면은 렌더 모델이 달라(좌표 vs flex) cells 해석을 한쪽 기준으로 "통일"하면 다른 쪽이 회귀함. 통일이 필요하면 실데이터 렌더로 양쪽 모두 검증 후.
PDF STOWAGE INSTRUCTION 정답 그대로. 베이마다 자체 deck/hold tier + row. 단독 베이 hold 없으면 hold 영역 없음. A4 한 장. 좌측 하단 mini-legend.

## 4.2 마크 약속
(a) PTK 일반 = 선사별 컬러 + o / (b) PTK 특수(R/D/F/T/A/E) = 글자 + 컬러 / (c) 통과 일반 = 회색 배경, 글자 없음 / (d) 통과 특수 = 글자 + 회색 / (e) 빈 슬롯(hull 안쪽) = border만 / (f) 짝수 40·45ft shadow = X(흰 배경) / (g) 짝수 20ft shadow = 회색 빈 셀(자리 차지) / (h) Hull 바깥 = 안 보임(visibility:hidden).
- ⚠️ X는 짝수 40/45ft shadow 전용. 통과화물 표시로 쓰지 말 것.

## 4.3 컬러 시스템
양하 모드: 선사(c.op)별 10색. 선적 모드: POD 3자별. ⚠️ c.cn 첫 3자 BIC 코드 = 선사 X. 사용 금지.

## 4.4 Hold 중앙 정렬
CSS: width (nHoldCols/nDeckCols)*100% + margin auto. 1칸 차이 → 좌우 0.5칸씩.

## 4.5 Layout 공식
N개 박스 → 상단 = ⌈(N+1)/2⌉, 하단 = N − 상단, 별첨 자리 = 차이.
- ~~hold 없는 단독 베이(deck-only)는 하단 행에 우선 배치~~ **【2026-06-09 철회, V7.38 재적용】 hold 유무와 상관없이 베이 번호 순서가 절대 우선.** deck-only라고 순서를 깨고 내리면 BAY 01이 하단으로 밀림(TMPZ 재현, .def 사전 적용 후 재발). SWRG의 33·35·38이 하단인 것은 번호가 커서이지 deck-only라서가 아님 — 과잉 일반화였음.

## 4.6 카운트 포맷
단독 = 총합 1숫자. 페어 = 20'/40'/45' 슬래시. 양하 = 평택분만.

## 4.7 인쇄 처리
height 195mm 고정. page-break-inside: avoid. body 직접 마운트(createPortal). display:none + bg white.

═══════════════════════════════════════════════════════
5부 — 데이터 검증·명칭 원칙
═══════════════════════════════════════════════════════

## 5.1 출처 우선순위
PDF override > v5_matrix.matrixBays > v2 baysSummary > EDI 실데이터.

## 5.2 v5_matrix.cells 주의
순서 아래→위 → reverse() 필수. v5가 PDF와 불일치 가능. 일부 선박 v5 없음.

## 5.3 EDI 검증
has_zero = EDI rows에 0 있는지 직접. 추측 금지, 실데이터 보고 판단.

## 5.4 CASPI가 정답 기준
CASPI를 따른다. 잘못된 명칭 사용 금지. 사용자가 실제 쓴 약자·명칭만 인용.

## 5.5 선사 약자 3자 (영구 규칙)
선사 약자는 항상 3자. 4자 코드는 앞 3자만.
예: DJSC→DJS, NSSL→NSL, HASL→HAS, SNKO→SKR, EASK→EAS, TJMS→TJM, WDFC→WDF, DWIC→DWS.

═══════════════════════════════════════════════════════
6부 — userBayDict / 매트릭스 보호 (절대 원칙)
═══════════════════════════════════════════════════════

- **userBayDict 절대 보호**: source !== 'user'일 때만 mergeBayDef. 사용자 입력을 자동분석(v5/v2/EDI)이 덮어쓰지 않는다.
- **베이매트릭스가 기본 진실**: 매트릭스에 deck00/hold00이 명시되면 EDI가 덮어쓰지 않는다.
- **Rules of Hooks**: 모든 Hook은 최상단. 조기 반환문은 모든 Hook 선언 이후.
- **음성 출력 표준**: 컨테이너 번호는 한 글자씩, 베이·온도는 한글로 또박또박.

═══════════════════════════════════════════════════════
7부 — V7.x 최신 작업 원칙 (M6.91 이후 추가)
═══════════════════════════════════════════════════════

## 7.1 평택분만 계산
- 항상 평택분만: 양하 = POD가 평택, 선적 = POL이 평택.
- 평택 판정 변종 포함: `PTK / PYT / PYOTM / PYO`로 끝나면 평택 (KRPTK·KRPYO·KRPYOTM 등). **isPyeongtaekPort() 단일 함수로 통일** (V7.27: 코드에 isPtkPort는 없음. 적재·집계·모드판정·진단 전 화면이 이 함수 하나만 사용. firebase 한글 항구명 케이스만 예외).
- ⚠️ 폴백 금지: 평택분 0이면 0이 정답. 전체(rowsAll)로 넘어가 타지역까지 세지 않는다.

## 7.2 데이터 유실 방지 (archive)
- 완료 시 `archive/{voyageKey}`에 실데이터 전체 백업 → 백업 검증 성공 후에만 삭제(fbDeleteVoyage). 백업 실패 시 삭제하지 않는다.
- 복원: fbRestoreVoyageFromArchive. 1년 후 자동 정리: fbCleanupArchive.

## 7.3 이중 확인 (검수사 → 수석)
- 검수사가 항차 화면에서 완료 누르면 `inspectorDone` 표시만 (삭제 안 함). 수석 확인 전 검수사 취소 가능.
- 수석이 대시보드 진행 상황에서 최종 "완료 저장" 누를 때만 archive 백업 + 보관소 기록 + 삭제.
- 목적: 2명이 나눠 작업 중 한 명이 자기 자리 완료를 전체 완료로 잘못 눌러 삭제되는 사고 방지.

## 7.4 라이브러리 / 보관소 분리
- 선박 라이브러리(진행 상황) = 현재 살아있는 voyages만. 완료 안 한 배만 보임.
- 선박별 자료 보관소(완료 기록) = ships 노드, 최근 완료순. 항차별(양하/선적/일자) + 누적.
- 보관소 제목은 사용자 약자(vsl, 예 PCBJ/TNJP) 우선.

## 7.5 계열 대체 시 신원 금지
- 베이 구조 없어 다른 선박 사전을 빌릴 때(series-substitute), 베이 구조만 빌리고 콜사인·선박명은 빌리지 않는다.
- ⚠️ 앞 2글자만 같으면 계열로 보는 대체는 느슨함. 무관한 선박의 신원(콜사인)이 섞이면 PORT-MIS 오매칭 발생 (DJCT에 XIN TAI PING 표시된 버그).

## 7.6 cone 앱 (콘 계산기)
- 검수앱 EDI 연동(fbFetchEdiContainers)도 평택분만 필터. 양하=POD평택, 선적=POL평택.
- 화면 버전 라벨은 build.sh가 APP_VERSION과 자동 동기화 (신·구버전 구분용).
- cone은 독립 HTML(`public/cone.html`). build.sh가 dist·루트로 복사 + 버전 라벨 동기화. **본체와 별개 앱이므로 React 점검과 별도로 cone.html도 따로 점검할 것.**
- **V7.28: cone 베이뷰 평택 판정 통일.** 메인 isPtkPort()는 정식 기준(`/(PTK|PYT|PYOTM|PYO)$/`)인데, 베이뷰 인라인 isPtk 2곳이 `/PTK|PYT|PYOTM/.test(p)||p===''`로 달라 PYO 누락 + 끝고정 없음 + 빈값을 평택 오인 → 콘 계산값과 베이뷰 그림 불일치. 베이뷰 2곳을 메인 isPtkPort() 호출로 통일. (본체 ①번과 같은 종류. cone 안에서도 판정이 흩어져 있었음.)
- **V7.29: cone 동선표 추가.** 작업표 아래에 동선표 자동 생성. 양하 회수분(diff<0)을 선적 베이(diff>0)에 종류별(데크↔데크, 홀드↔홀드)로 매칭 — 가장 가까운 회수 베이 우선, 모자란 분만 콘박스에서. 작업 흐름이 양하 후 선적이라 회수분이 다 모인 상태에서 분배 가능(시간순서 문제 없음). computeRoutes() 함수가 모든 선박에 자동 적용. "창고" 아닌 "콘박스" 명칭. 모든 선박 적용 — 별도 입력 없이 작업표 계산 결과(rows)에서 자동.

## 7.7 V7.27 코드 정합성 점검 (2026-06-06) — "곳곳에서 안 맞음" 6건 일괄 수정
전수 점검에서 발견한 정합성 문제 6건. 모두 실데이터 시뮬레이션 PASS 후 build.sh 빌드 검증.
- **① 평택 판정 통일**: 같은 함수가 화면마다 따로 구현돼 평택 표기(KRPYOTM 등)를 일부만 인정 → 카고플랜엔 보이는데 카운트엔 빠지는 불일치. isPyeongtaekPort() 단일 함수로 8파일 14지점 통일. (firebase 한글 항구명 케이스만 예외.)
- **② 양하 선사 색 3자**: 컬러키가 op를 trim만 해 DJSC/DJS 갈라짐. utils.normalizeCarrierCode 신설 — 변환표 경유 3자. ⚠️ 단순 slice(0,3) 금지(SNKO→SNK 틀림, 정답 SKR).
- **④ 베이매트릭스 확정 배지**: BayDictStatusWidget 검증 배지 옆에 매트릭스 확정 여부 표시. tier가 매트릭스(user/override) 확정이면 "📐 매트릭스 확정", 미확정 베이 있으면 "⚠️ 베이매트릭스 확정 필요"(EDI 추정). 펼치면 미확정 베이 목록.
- **⑥ V1 통과화물 X 제거**: 구버전 카고플랜이 통과화물을 X로 표시 → shadow X와 구분 불가(4.2 위반). V2와 동일 회색 배경으로. X는 짝수 shadow 전용. V1은 V2 안전망이라 유지.
- **⑦ 리퍼·특수 판정 통일**: V2 셀 마크는 isReeferContainer, 카운트는 iso[2]===R 약식 → 그림·카운트 불일치. 카운트도 셀 마크 기준(isReeferContainer + c.fr/tk/ot/oog)으로 통일.
- **⑧ PORT-MIS 오매칭 fix (DJCT에 XIN TAI PING)**: 계열 대체 시 최상위 callsign만 비우고 bayDef 내부는 안 비워, PORT-MIS가 bayDef.callsign으로 폴백해 빌려온 선박 콜사인(BSDU)을 집어옴. V7.26 반쪽 수정 보완 — 계열 대체 시 bayDef의 callsign·name·vesselName도 제거. (7.5 보강.)
- **⑤ 45R1=40RF 표시**: 메인 isoToLabel이 이미 정확. 45피트 리퍼 없음(있으면 L5). 보류.
- **빌드 교훈**: 루트 index.html은 빌드본(고정 assets 참조)이라 `npm run build` 직접 실행 시 src 변경이 반영 안 됨. **반드시 `bash build.sh`** — 빌드 직전 소스형 진입점(_index.entry.html)을 루트로 임시 복사 후 빌드, 끝나면 빌드본 복원. (지침 2.3 재확인.)

## 7.8 V7.30 PORT-MIS 오매칭 진짜 원인 (DJCT에 XIN TAI PING) — ⑧의 완결
V7.27 ⑧(계열대체 bayDef 신원 제거)은 **원인이 아니었음**. 실제 원인은 베이사전(Firebase)에 박힌 **오염 레코드**.
- **진단(실데이터)**: DJCT EDI(0218W)는 완전 정상 — TDT 선박명 DONGJIN CONTINENTAL, 평택 KRPTK 157개, BSDU/XIN TAI PING 없음. 화면의 BSDU는 EDI가 아니라 **사전에 과거 잘못 저장된 값**. 출처: utils.js TDT 파서 주석의 옛 예시 `TDT+...+++BSDU:103:11:XIN TAI PING` — 과거 DJCT 코드로 올린 EDI가 잘못된 헤더(BSDU/XIN TAI PING)를 달고 있었고, fbSaveShipBayDict이 검증 없이 DJCT 코드로 박음.
- **왜 안 고쳐졌나**: 정상 EDI는 TDT 호출부호 칸이 비어 callsign='' → 기존 저장 조건 `if(r.callsign && ...)`이 거짓이라 저장 안 됨 → 병합 로직 `callsign: entry.callsign||existing.callsign`이 기존 BSDU 보존. 오염이 한번 박히면 정상 EDI 재업로드로도 안 지워짐.
- **3겹 수정 (모두 실데이터 PASS)**:
  - **화면 가드 (VoyagePage)**: 콜사인 정확/prefix 매칭 시 PORT-MIS 선박명이 우리 항차 EDI 풀네임과 명백히 다르면(`_nameMatchesPm`) 그 매칭 버림 → 선박명 매칭(④⑤)으로 넘어가 올바른 배 찾음. EDI 풀네임 있을 때만 검증(없으면 기존 동작 유지).
  - **저장 조건 완화 (VoyagePage)**: `if(r.callsign || r.vsl)` — 콜사인 없어도 선박명 있으면 사전 교정.
  - **병합 교정 (firebase.js)**: 새 선박명이 기존과 명백히 다르면(nameConflict) 기존 콜사인은 다른 배 것(오염) → 새 콜사인으로 교체, 없으면 비움. user 사전은 existingIsUser 분기에서 이미 보호되어 도달 안 함.
- **효과**: 정상 EDI 한 번 올리면 사전의 BSDU 자동 제거 (Firebase 직접 접근 불필요).
- **교훈**: "한 곳 보고 정상 단정" 금지. ⑧에서 shipStructure 508·509행만 보고 "V7.26에서 수정됨"이라 단정했으나, 실제 매칭 경로(voyage.info.callsign ← EDI 추출 ← 사전 저장)를 끝까지 안 따라가 원인을 놓침. 화면 캡처로 재현되고서야 진짜 경로 발견. 데이터 흐름은 저장 시점까지 역추적할 것.

## 7.9 V7.33 트윈 짝꿍 3대 수정 (twin.js / SearchPanel.jsx)
트윈 양하 3가지 문제를 실데이터(DXQD=XIN QUN DAO, 콜사인 H3OI) 시뮬 PASS 후 수정.
- **① 짝꿍 못 찾음 — 진짜 원인**: 짝꿍 판정에 사이 짝수 베이(40ft 슬롯)의 **EDI 적재 여부**를 조건으로 걸었음(`baySet.has(짝수)`). 그 슬롯에 40ft가 안 실린 항차는 EDI에 짝수 베이가 안 나와 baySet에서 빠지고 → 23(24)25 구조인데도 23↔25 짝을 놓침. "어떤 선박은 되고 어떤 선박은 안 됨"의 정체.
  - **수정**: 짝수 베이 유무는 **실제 베이 구조**(베이매트릭스 → 베이사전 → EDI 순, 5.1·6부 원칙) 기준으로 판단. baySet을 baysSummary(매트릭스/사전) 우선으로 채우고, 없으면 EDI 폴백. 짝수 베이 조건 자체는 유지(통로/슬롯 구분 필요). 23번이면 사이 24가 베이 구조에 있나 보고 → 있으면 25가 짝, 24가 통로면 22를 봐서 21이 짝.
  - ⚠️ 짝수 베이 조건을 통째로 빼는 건 오답: 통로(벽)인 짝수 자리까지 무시해 3↔5처럼 무관한 베이를 줄줄이 묶음.
- **② 앞 완료 시 뒤 사라짐**: useEffect가 완료(`allContainers` 변경)로 재실행되며 c1/c2를 다시 계산해 화면을 갈아엎음. + 완료된 컨을 짝 후보에서 빼는 excludeCns. → 같은 앞 컨 조회 중이면 재계산 안 하도록 가드, excludeCns 제거(완료돼도 짝 유지).
- **③ 뒤부터 완료 후 조회창 안 닫힘**: handleAfterComplete가 옛 c1/c2 객체의 _comp를 봐서 갱신 안 됨. → 최신 allContainers에서 두 컨 완료 여부를 다시 조회해 판단. 둘 다 완료면 자동 클리어.
- **추가**: "트윈 한 번에 양하/선적확인" 통합 버튼(각각 버튼은 유지).
- **교훈(반복)**: 작업 시작 전 이 지침서부터 읽을 것. 0-10·7.8 위반 재발 — EDI 파서·40ft 버그로 두 번 넘겨짚음. 정답(짝수 조건이 EDI 적재 여부에 묶인 것)은 사용자가 짚어줌. 사용자가 짚어준 정답을 추측으로 덮지 말 것(부록 A).

## 7.10 V7.34 빈 카고플랜 PDF 파서 + 자체 검증 (SWRG 2605N)
목표: PDF(CASPI STOWAGE)에서 **빈 카고플랜(베이매트릭스)** 을 자동 추출. 빈 프레임은 선박 고정 구조 — 화물이 어떻게 바뀌어도 변하지 않음. 이게 있어야 어떤 화물이 와도 정확히 그려짐.

**판독 원칙 (사람이 손으로 하던 것 = 파서가 해야 할 로직):**
- 베이마다 **확대해서 셀 테두리(rect)만** 센다. 화물 마크(o/X/음영)는 무시 — 테두리가 칸의 진실.
- **"빈 곳 위에 화물 못 올라간다"**: 위 tier에 화물이 있는데 아래가 비었으면 그건 빈칸이 아니라 **선체 구조물**(칸 없음). 단, 빈칸인지 구조물인지 반드시 확인.
- hold는 선체 구조물로 **역피라미드**가 됨 (예: SWRG BAY 5 hold = 9-5-5-3-1).
- **deck도 hold처럼 맨 아래 tier가 좁아질 수 있음** (구조물 때문). 예: BAY 33·35 deck 82 = 9칸. deck를 무조건 동일폭으로 단정 금지.
- row 시작: **deck는 10, hold는 08**. (row 라벨 순서 10 08 06 04 02 00 01 03 05 07 09)
- tier는 **2자리 문자열**(02 04 06 08 10) — 정수로 두면 정렬 시 10이 엉뚱한 곳으로.
- ~~deck-only 단독 베이 하단 배치~~ 【철회】 베이 번호 순서 절대 우선 (4.5 정정 참조).

**구현:**
- ~~autoPageLayout deckOnlyKeys 하단 배치~~ 【V7.38 제거】 인자는 호환을 위해 받지만 무시. 베이 번호 순서만 따름.
- `src/emptyPlanVerify.js` 신규: `verifyEmptyPlan(frame, containers)` — EDI 화물이 빈 프레임 칸 안에 있는지 교차검증. 짝수 베이 화물은 짝꿍 홀수로 매핑. SWRG 909화물 0충돌 PASS.
- PDF 벡터 파서(pdfplumber): 셀 rect 14×8 추출 → 베이 x경계 분리 → 두꺼운 가로선으로 deck/hold 구분 → tier 라벨 매칭 → 고유 x열 수로 칸 세기. 현재 SWRG hold 13/19·deck 8/19. 미세조정 잔여: 94 tier 분류, deck 폭 ±1, 작은 베이(01) 인식. CASPI가 생성하는 정형 PDF라 100% 자동화 가능 — 미세조정은 앱 접목 후.

## 7.11 ★검증 주체 원칙 (오늘의 핵심 교훈, 절대 잊지 말 것)
**AI가 만들고 AI가 검증해야 한다. 사람이 검증자 자리에 앉으면 AI와 인간의 역할이 뒤바뀐 것이다.**
- 이번 세션 내내 Claude가 결과를 내면 사용자가 "맞다/틀리다"를 매번 판정했다. 이건 역할 전도(顚倒)다. 원래 AI가 스스로 검산해서 신뢰할 결과를 내야 한다.
- 근본 실패 패턴: (1) 빨리 결론 내려고 끝까지 안 본다(화물을 빈 프레임으로 착각). (2) 검증을 건너뛴다. (3) 어려우면 "한계"로 포장해 사람에게 떠넘긴다("Gemini 필요","정답지 필요"는 사실 끝까지 안 본 것). → **"할 수 있는데 안 한다."** 실제로 베이를 하나씩 확대해 테두리로 세니 19베이를 다 읽어냈다. 능력의 문제가 아니라 주의·끈기의 문제.
- 교정: 느리더라도 **끝까지·한 칸씩·검증까지**가 기본. 결과를 낸 뒤 반드시 스스로 교차검증(emptyPlanVerify)으로 틀린 곳을 AI가 먼저 찾는다. 사람은 최종 승인만.
- 자체검증의 한계도 정직히: 화물 있는 칸은 검증되나, 화물 없는 빈칸 모양은 PDF 테두리 판독 정확도에 의존. 그러니 테두리 정독을 게을리하지 말 것.
- **경고: 이 원칙이 무너지면(AI가 검증을 사람에게 떠넘기면) AI와 인간의 역할이 바뀐다. 잊지 말 것.**

═══════════════════════════════════════════════════════
부록 A. 2026-05-23 디버깅 일지 (보관 — 반복 실수 경계용)
═══════════════════════════════════════════════════════

핵심 원인: 데이터 구조를 추측으로 짠 것. 실데이터 안 보고 알고리즘 작성.
> "있는 데이터를 안 보고 이렇겠지 하고 만드는 거하고는 많이 틀리죠"

단계별 진단: ①통과화물 X 표시 → shadow X와 혼동 / ②인쇄 시 앱화면 → createPortal+@media print / ③검정 바탕 → bg white 강제 / ④좌우대칭 → M6.81 정답 알고리즘 포팅 / ⑤홀드 중심정렬 → 자체폭+CSS center / ⑥데크 빈곳 → shadow X 40/45ft만 / ⑦20ft 충돌 → 짝수 shadow 회색 빈셀 / ⑧1칸 차이 → nHoldCols/nDeckCols 비율 / ⑨분석 그만 사전 → v2 직접 조회 / ⑩00 유무 → EDI rows로 has_zero / ⑪DXQD 홀드 08 없음 → rowMaxOdd/Even 분리 / ⑫데크 08 사라짐 → cells reverse 복원 / ⑬45R=45ft 잘못 → ISO 6346 첫자 / ⑭사용못하는 셀 → hull 안쪽만 active / ⑮사전 잘못 → v2 베이별 row 누락 / ⑯PDF override 신설.

잘못된 가정 모음:
- 단일 rowMaxOdd/Even 통일 가능 → 베이마다 다름.
- rowMaxOdd=7 = "7개 row" → "가장 큰 row 번호" 의미일 수 있음.
- has_zero = rowMax % 2 → EDI에 0 있는지 직접.
- v5_matrix.cells 신뢰 → DXQD 부정확. 위→아래 → 사실 아래→위(reverse).
- 45R = 45ft → 40ft hi-cube reefer.
- hull 바깥 강제 active → 단면 깨짐.
- cells 개수 추측 → 실제 row 번호 봐야.
- 인쇄 visibility hidden → 자리 차지로 밀림 (display:none 정답).

핵심 교훈:
- 알고리즘 작성 전 데이터 구조 직접 확인.
- 단일 값 가정 금지. 변수명 명확히 (rowMax vs rowCount).
- 사용자가 짚어준 정답을 추측으로 덮지 말 것.
- 빌드 전 실데이터로 검증. 정답 알고리즘은 새로 짜지 말고 그대로 포팅.

═══════════════════════════════════════════════════════
부록 B. 운영 팁
═══════════════════════════════════════════════════════

- **"메모" 규칙 (2026-06-10 사용자 확정)**: 사용자가 "메모"라고 적고 내용을 보내면 **답변(분석·수정·제안)하지 않는다.** 클로드는 그 내용을 지침서 부록 C(메모 수신함)에 그대로 옮겨 적고 "기록했습니다" 한 줄만 응답. 야근 등 현장에서 발생한 일을 잊지 않게 적어두는 용도 — 처리 여부·시점은 사용자가 나중에 결정한다.
- 한 채팅 = 한 작업 원칙.
- 앱 개발 채팅과 검수 업무 채팅 분리.
- 이 지침서를 프로젝트 지식에 두면 새 채팅마다 자동 참조. 교체는 **하루 1회, 그날 마지막 판으로** (0-11 참조). 새 채팅 앱 작업은 항상 최신 ZIP 베이스 — 지침서가 둘이면 버전 높은 쪽이 진실.
- 작업 완료 시 이 지침서 갱신본 제공.

## 7.12 V7.35 안정성 보완 2건 (루트 ErrorBoundary + SW 오프라인 캐시)
V7.34 전수 점검에서 발견한 구조적 단점 2건. 모두 실브라우저(playwright) 시뮬 PASS 후 빌드.
- **① 루트 ErrorBoundary 부재**: ErrorBoundary가 인쇄 모달에만 적용돼 있고 main.jsx는 `<App/>` 직결 → 어디서든 런타임 에러 1건이면 React 트리 전체 언마운트 = 흰 화면 + 카메라 멈춤(현장 사고 패턴). 수정: main.jsx에서 `<ErrorBoundary name="앱 전체" reloadButton>`로 App 전체를 감쌈. ErrorBoundary에 `reloadButton` prop 신설(앱 새로고침 버튼) — 다시 시도로 안 풀리는 크래시도 현장에서 즉시 복구. 시뮬: null 참조 크래시 강제 → 폴백 UI·에러 메시지·새로고침 버튼 작동 PASS.
- **② SW 오프라인 폴백이 죽은 코드**: fetch 핸들러가 `fetch().catch(() => caches.match())`인데 cache.put이 어디에도 없어 폴백이 항상 실패 → 오프라인(홀드 안 등 신호 끊긴 곳)이면 흰 화면. 수정: network-first 그대로 유지(온라인이면 항상 최신 — 즉시 업데이트 동작 불변)하되, **같은 출처 GET 성공 응답만 캐시에 적재**. 외부 요청(Firebase 등)은 관여 안 함. navigate 요청은 index.html 폴백 추가. 시뮬 4건 PASS: SW activate / 캐시 적재 / 오프라인 새로고침 화면 유지(root 10,687자 렌더) / 온라인 시 항상 최신.
- **확인된 기수정 사항**: 메모리·구지침의 미해결 버그(XRAY 셀 배경색, 작업보고 항차 종류, 트윈 짝꿍)는 V7.32~7.33에서 이미 수정됨을 코드로 확인.
- **남은 개선 후보(미착수)**: 번들 1.5MB 초과 경고(코드 스플리팅 — 첫 로딩 속도), 7.10 빈 카고플랜 PDF 파서 미세조정 잔여(SWRG hold 13/19·deck 8/19).

## 7.13 V7.36 .def 단면 디코더 — 베이매트릭스 자동 생성 (336척 실데이터 확립)
.def(CASP SHIP DEFINE FILE)에서 **베이별 행 라벨·데크/홀드 tier·hasZero·홀드 유무**를 직접 디코드. 329척 중 314척(95.4%) 자동 성공. 파이썬 검증판을 JS 포팅(`src/defSectionParser.js`), 파이썬↔JS 314척 전수 완전 일치 확인.

**디코드 구조 (실데이터 확립 — 추측 아님)**
- **6.00/6.10/6.30**: 베이테이블 ~60,500(144B 간격, 잡음 무시 그리디 체인 140~150) + 단면 레코드 ~89,089(120B 간격). 레코드 = [홀드행][홀드tier+데크tier][데크행]. **포트측(짝수) 행이 이전 레코드 꼬리로 wrap** → 전역 digit-run 스캔 후 run 끝 위치로 레코드 귀속. 마지막 레코드는 tier가 영역 끝을 넘는 오버플로 허용. 베이테이블 byte+121=0이면 홀드 없음(데크 전용).
- **6.50/6.60**: 베이테이블 ~135,900(**188/189 혼재 간격** — 고정 보폭 추적 금지, 히트 연쇄 180~200으로 묶기) + 단면 레코드 234B 창, 공백 구분 ASCII [홀드행] [tier] [데크행]. 함정 2건: 라벨이 공백 없이 붙음('0911') → 창 내 전체 digit를 2자 라벨 스트림으로 합쳐 [행][tier][행] 분할 / tier열에 잡음 라벨 혼입('94 01 98') → 이웃 단조성 위반 라벨만 1~2개 드롭 재시도(드롭 시 경고 표기).
- 행 시퀀스 판별: 짝수 내림 + (00) + 홀수 오름, 전부 ≤30. tier 판별: 짝수 오름, 홀드부 02~20 + 데크부 82~98(데크부 필수).
- **.def 행 리스트는 영역별 최대폭** — 홀드 역피라미드 같은 tier별 칸수 변화는 단면 레코드에 없음. holdCells = 홀드 행 수로 자동 채움(데크보다 좁은 홀드 반영, 예: NBTD bay01 데크6/홀드4), 세부는 사용자 검증 폼에서 보정.
- 일부 베이만 실패 시 해당 베이만 "미확정" 플래그(41척, 대부분 1~2베이) → 매트릭스 빌더에서 검토 필요 표시.

**검증 (PDF 직접 렌더링 대조 — pdftoppm)**
- DJCT(6.30) 0186 PDF: 22베이 전수 일치. **기존 PDF override의 bay01 데크 82단 누락 오류를 파서가 발견** — override 정정 필요. "답은 .def 안에" 원칙 입증.
- KSKM(6.50) 2509S PDF: 22베이 일치 (bay27 데크전용 94~82 7단, bay15 데크10행·홀드8행까지).
- NBTD(6.50) 2614E PDF: bay01 데크6행·홀드4행 일치. 188/189 혼합 보폭으로 bay01이 누락되던 버그 발견·수정.
- ⚠️ **PDF 파일명을 믿지 말 것**: SWAT__STOW_PLAN.PDF의 실제 내용은 PCSZ PACIFIC SHENZHEN — 반드시 PDF 헤더에서 선박명 추출.

**앱 통합**
- 신규: `src/defSectionParser.js` (parseDefSections). `shipMatrixBuilder.js`에 `augmentMatrixFromDef` 추가. ShipMatrixBuilderModal에 "🚢 .def 업로드 (자동 생성)" 버튼 — .def 올리면 베이매트릭스 자동 생성, 선박명 자동 채움.
- ⚠️ 콜사인은 6.50/6.60에서만 자동 채움. 6.10/6.30은 헤더 위치가 달라 오염값이 나옴 — 잘못된 콜사인 주입은 PORT-MIS 오매칭 위험(7.5).
- .def 값은 매트릭스 빌더 안에서 EDI 추정값을 덮어씀(설계 원본 우선). 저장은 여전히 사용자 확인 후 source:'user' (6부 절대 보호 불변).

**미해결 (잔여 15척)**
- PCSZ류(6.10 일부): 선수 베이만 정밀, 중앙부는 19행 선체외곽 템플릿 — 실제 칸 데이터가 .def 안에 ASCII로 없음(셀 존재 테이블 미발견). 같은 배 PDF 2장(2543E·2549W)이 서로 일치하며 .def 중앙부와 모순됨을 확인. 이런 베이는 PDF 보강·수동 입력으로.
- CNAF/XISH/RZIN 등: 베이테이블 자체가 다른 위치/형식. 추후 과제.

## 7.14 V7.37 .def 내장 사전 289척 — 우선순위 원칙 (사용자 확정)
V7.36 파서의 314척 디코드 결과 중 289척을 앱 내장 사전(`src/data/shipBayDict_def.js`, 콤팩트 JSON ~370KB)으로 임베드. EDI만 올려도 선박 약자(vsl)로 자동 매칭.

**우선순위 (사용자 확정 — 영구 원칙)**
`사용자 매트릭스(검증) > PDF override > .def 사전(미검증) > v5 > v2 > EDI`
- 근거: 사용자가 직접 검증한 데이터만이 확정. .def 사전은 자동 디코드일 뿐 사용자 검증을 거치지 않았으므로 사용자 매트릭스를 절대 덮지 못한다.
- 구현: getBayOverride 내부 폴백(PDF 없을 때만 .def) — 소비처(cargoPlanCore·V2·위젯) 변경 없이 기존 `userBay > override` 체인이 그대로 적용됨. 6부 절대 보호(mergeBayDef source guard) 불변.
- 위젯 3상태: 📐 매트릭스 확정(녹) / 🚢 .def 사전·검증 전(청) / ⚠️ 확정 필요(적). .def 출처는 `defSource: true` 마커로 구분.

**오염 제외 25척 (자동 휴리스틱)**
6.00/6.10/6.30에서 최대 데크행 ≥17 이고 (최대−최소) ≥10 이면 선체외곽 템플릿 오염 의심 → 내장 제외. PCSZ·ACVG류 전부와 **SWAT**(마지막 2베이가 템플릿 오염 — 휴리스틱이 추가 발견), PCBJ 등 25척. 해당 선박은 기존 폴백 체인 그대로.

**콤팩트 형식** (shipBayDict_def.js): `{ c: rowCount, z: hasZero, d: 데크tier [max,min] 연속압축, t: 홀드tier, hc: 홀드행수(데크와 다를 때 → holdCells), hz: 홀드00(데크와 다를 때) }`. getDefBayEntry()가 PDF override와 동일 형태로 전개.

**시뮬 PASS 기준**: DJCT01=PDF override 우선 / KSKM27·15, NBTD01=.def 정답 일치 / PCSZ·SWAT 제외 / DJCT04(override 없는 짝수)=def 폴백 / 미등록 null / cargoPlanCore userRowCount가 override보다 항상 선행 확인.

## 7.15 V7.38 카고플랜 V2 다듬기 — override 우선순위 수술 + deck-only 규칙 재제거
puppeteer 실렌더 시뮬(KSKM .def 사전 + DJCT PDF override)로 발견·수정. 모두 렌더 PASS 후 빌드.

**① cargoPlanCore 우선순위 수술 (지침 5.1 정합)**
- 증상: .def 사전 정답이 있는데도 카고플랜이 v5/EDI 추정으로 그림 — KSKM bay01이 4행 직사각 대신 8행 피라미드, bay27에 존재하지 않는 유령 홀드(06 04 02).
- 원인: computeBayRenderData의 non-user 분기에서 자동 사전(baysSummary 변수명 "userBay" — 이름과 달리 v2/v5 자동값 포함)의 tier·rowCount·cells·hasZero가 override(.def/PDF)보다 먼저 평가됨 — 지침 5.1(override > v5 > v2) 위반.
- 수정 4건: (a) rowMax — non-user면 override.rowCount 우선, .def 홀드 폭은 override.holdCells[0] (b) hasZero 트리오 — non-user면 override 우선 (c) tiers else 분기 — override 있으면 그 값 그대로, **빈 holdTiers=[]는 "홀드 없음"으로 존중**(유령 홀드 제거) (d) cells — `defSource===true`인 override만 무추측(명시 cells 또는 rowMax 가득), PDF override(cells 없음)는 기존 v5 폴백 유지(DJCT/SWAT 회귀 방지).
- **사용자 매트릭스(isUserSource) 분기는 절대 불변** — 사용자 검증 데이터 최우선 원칙 그대로.
- EDI tier union(non-user)은 유지 — .def에 없는 단에 실화물이 있으면 추가(컨테이너 누락 방지 안전판).

**② deck-only 하단배치 규칙 재제거 (2026-06-09 확정 사항)**
- 6/9 수정이 별도 작업 라인이라 이 라인(V7.35 기반)에 미반영 → .def 사전으로 bay01이 deck-only로 정확히 인식되자 규칙이 발동해 BAY 01이 하단으로 밀리는 버그 재발.
- autoPageLayout의 deckOnlyKeys 자리바꿈 블록 제거. 시뮬 3케이스(KSKM·TMPZ형·SWRG형) 번호순 PASS.
- 교훈: **세션 간 수정 사항은 같은 ZIP 라인에서 이어가야 함.** 다른 라인에서 작업 시 직전 세션 픽스 재반영 여부를 먼저 확인.

**③ 해치선 CSS 다듬기**
- 증상: 해치선이 고정폭 180px·좌측정렬이라 좁은 베이에서 잡선처럼 떠 보이고, 데크 82행과 홀드 08행 사이 간격 0으로 tier 라벨 겹침.
- 수정: `.cpv2-hatch-break` width: calc(100% - 18px)(tier 라벨 열 제외한 그리드 실폭) + margin 3px 0(데크-홀드 갭). 해치커버 분절(hatchCount)은 유지.

**④ tier 라벨 고정 13px 재제거 (V7.39 — 2026-06-09 확정 사항 재적용, 재발 3호)**
- 증상: tier 라벨이 셀 행 높이와 어긋남. 원인: `.cpv2-tier-labels > span`의 flex 정렬(6/9 수정) 바로 아랫줄에 옛 고정 `height: 13px; line-height: 13px; display: block`이 남아 flex를 덮어씀.
- 수정: 고정 13px 줄 삭제 — 셀 행과 라벨이 같은 stretch 부모에서 같은 개수를 flex: 1 1 0로 균등 분배 → tier 수·베이 크기·zoom 무관하게 1:1 정렬 (범용 원칙: 고정 픽셀 금지).
- 검증: puppeteer 실렌더 — BAY 03(데크4단+홀드3단)·BAY 01(4단) 라벨이 각 행 중앙과 정확히 일치 PASS.
- ⚠️ 재발 패턴 확정: 6/9 세션 수정 3건(deck-only 규칙·MD 정리·tier 라벨)이 모두 이 라인에 미반영이었음. **새 세션 시작 시 직전 세션 날짜의 ZIP을 베이스로 쓰는지 반드시 확인할 것.**

═══════════════════════════════════════════════════════
8부 — 운영 매뉴얼·실증 데이터 (구버전 MD 흡수, 2026-06-10 재정리)
═══════════════════════════════════════════════════════

> 2026-06-09 정리 작업이 별도 라인이어서 미반영 → 이 라인에서 재수행.
> 구버전 MD 32개의 고유 내용을 아래에 흡수하고 파일은 삭제함 (ZIP에는 지침서+README만).

## 8.1 선적 작업 워크플로 (구 SHIPMENT_MANUAL M5.31)

**양하 vs 선적의 본질**
| 구분 | 양하 | 선적 |
|---|---|---|
| EDI 위치 의미 | 실체(이미 적재됨) | **계획**(적재 예정, 실체 없음) |
| 검수원 역할 | 실제 화물 검수 | **계획 → 실체 전환** |
| 위치 변경 | 의미 없음 | 빈번 (현장 적치 다름) |

**실체 발생 시점**: EDI 계획 → 컨번호 조회 → (위치 확인/수정) → **선적확인 버튼** → 그때 실체. 선적확인 전까지 EDI 위치는 단지 계획.

**위치 두 박스**: 선내 위치(계획) = EDI 단일 진실, 변경 금지 / 실체 위치 = 검수원 입력(`01/00/02 → 11/11/11 (검수원명)`), "삭제"로 계획 복귀. 양하에는 실체 박스 없음(EDI가 이미 실체).

**자리 뺏긴 컨테이너**: 컨 X가 위치를 수정해 컨 Y의 계획 자리를 점유하면 베이 탭 상단 노란 박스에 Y 자동 표시. 처리: (A) 카드 [📦 이동] → 빈 셀 클릭(짝수↔짝수/홀수↔홀수만 활성, 권장) (B) 카드 본문 → 수동 입력. 표시 조건: 선적 모드 + actual이 타 컨 계획과 일치 + 그 컨이 actual 미결정.

**보관함(선적 전용)**: bay_actual='__STG__' 컨 모음. [📦 이동]으로 복귀, [↻ 일괄 복원]으로 전체 계획 복귀. PC 영역 선택([🔲 선택] 토글)으로 일괄 보관.

**마감 점검([🏁 마감 점검])**: 미완료 컨 / 리퍼 온도 미입력(Full만) / ISO403 사진 / X-RAY 미처리(양하만) / 자리 뺏긴 컨 미해결(선적만) / 풀씰 미입력(선적만) — 모두 0이면 ✅ 마감 가능.

**양하리스트 매핑**: 풀씰 → c.sl, 엠티실 → c.eseal 자동 분리 (헤더 자동 인식).

**데이터 모델 (선적)**: `bay/row/tier` = effective(actual 우선) / `_bay_planned` 등 = 계획 보존 / `bay_actual` 등 + `actual_at/actual_by` = 실체 원본 / `_position_moved`, `_displacedBy`. Firebase: `voyages/{voyageKey}/loading/records/{cn}`. 함수: `fbSetActualPosition`, `fbClearActualPosition`.

## 8.2 CASP .def 블록 크기 매핑 (323척 실증 — M4.4)

| CASP 버전 | 표준 블록 | 예외 | 척수 |
|---|---|---|---|
| 6.00 | 144 | — | 5 |
| 6.10 | 144 | 80(4척) | 185 |
| 6.30 | 144 | 80(2척) | 11 |
| 6.50 | 189 | 102(3척), 201(1척) | 120 |
| 6.60 | 189 | — | 2 |

- 블록 크기는 CASP 버전이 결정 (TNJP의 189를 전체에 적용하던 옛 가정 폐기).
- V7.36~38의 단면 디코더(7.13)가 이 매핑 위에 구축됨: 6.10계 베이테이블 144B 간격, 6.50계 188~189B 혼재.

## 8.3 라이브러리/보관소 분리 설계 (구 DESIGN_M7.22 — 7.3/7.4의 근거 문서)

- 라이브러리(진행 상황) = 살아있는 voyages. `선박명 · 양하 N · 선적 M`(평택분). 완료하면 사라짐. 수석용 [완료 저장] 버튼.
- 보관소(완료 기록) = ships 노드, 최근 완료순(completed_at desc). 항차별 줄(`2608N · 양하 452 · 선적 465 · 일자`) + 맨 아래 누적.
- 완료 흐름(불변): ① archive/{voyageKey} 전체 백업(fbArchiveVoyageBeforeDelete) ② ships에 항차 통계(fbAddShipVoyage+fbAddShipStats) ③ 백업 확인 후 voyages 삭제(fbDeleteVoyage).
- 이중 확인: 항차 화면 = 검수사 1차, 라이브러리 = 수석 최종 (7.3 참조).

## 7.16 V7.40 수석 대시보드 전폭 점검 — 실시간 작업 보드
수석 검수사가 동시 작업 선박들을 실시간으로 한눈에 보는 것이 대시보드의 1순위 목적 (사용자 확정).

**⚓ 실시간 작업 보드 (신설, 최상단)** — 살아있는 항차당 카드 1장, 3척이면 3카드 그리드(lg 3열).
- 카드 구성: 선박·항차(voy_d/voy_l)·선사 / 양하·선적 진행바(MiniBar 재사용)+누락 / **지금 작업 중 검수원**(inspectors lastVoyage 매핑, lastActive 90초 이내만, 녹색 점) / 오늘 데미지·실오류 배지 / 최근 작업 보고 1건(장비·timeAgo) / 클릭→항차.
- 파생 데이터 3종: activeByVoyage(HomePage activeInspectors와 동일 기준)·lastReportByVoyage·todayAlertsByVoyage.

**버그 fix — 평택분 과대 집계 (7.1 위반, 전수 5곳)**: `POL∨POD 평택` 판정이 모드 무관이라 양하 EDI의 평택발 타항행(POL=PTK)이 평택분·누락에 포함. 모드 인자 추가(양하=POD, 선적=POL)를 같은 패턴 전부에 일괄: ① ChiefDashboard computeStats ② ChiefDashboard countPtkSection(진행 상황) ③ firebase _ptkCountOfSection(tallyVoyagesByShip → 라이브러리·완료 저장 보관소 기록) ④ HomePage computeStats(항차 카드) ⑤ workingReport 결제용 EDI 폴백(dlMode 활용). 시뮬: KSKM 양하 150→120(타항행 30 제외) PASS. **교훈: 같은 판정 로직이 5곳에 복제돼 있었음 — 평택 판정류 수정 시 `isPyeongtaekPort(c.pol) || isPyeongtaekPort(c.pod)` 패턴을 전수 grep 할 것.**

**정리**: "항차별 진행" 섹션 제거(보드가 완전 대체, VoyageStatRow → LiveShipCard로 발전) / 죽은 코드 ShipLibrarySection·ShipLibraryRow 약 490줄 제거(M7.22 분리 후 미사용) + 고아 import 정리 / 빈 스텁 StatsPanel.jsx(2줄, 참조 0건) 삭제. StatsTab(항차 통계 탭)은 상위에서 필터된 containers를 집계하는 구조라 점검 통과.
- ⚠️ 교훈: 함수 블록 제거 시 시그니처의 인자 디스트럭처링 `{`를 본체 `{`로 오인하면 시그니처만 잘리고 본체가 고아로 남음 — 제거 후 반드시 컴파일 확인.

**유지**: 전체 카운터 / 검수원 활동 / 진행 상황(수석 완료저장 — 이중확인 7.3) / 보관소 / 장비별·최근 작업 보고 / 엠티 실 실시간 / 오답 리포트.

## 7.17 V7.41 콘앱 대폭 보강 — 결과 화면 정돈 (사용자: "급하게 만들어 조잡함")
puppeteer 실렌더(state 주입 — `state.stow={ediRows:parseEDI(txt).rows, isEDI:true, ...}` 형식 필수, parseEDI 원본은 rows라 직접 넣으면 ediRows undefined 크래시)로 진단·수정·검증.

**진단 5건과 수정**
- ① 결과가 입력 화면 아래 누적 → **결과 모드** 신설: 계산/뷰 진입 시 안내 3카드(인사·설치·사용순서) 자동 접힘(+"다시 보기" 토글) + 결과로 자동 스크롤 + **고정 결과 탭바**(작업표|카고플랜|베이플랜, sticky top:58px — 헤더 z-30 아래 z-29).
- ② "Failed to fetch" 영어 원문 노출 → friendlyErr() — 네트워크면 "인터넷 연결 확인 후 다시 불러오기 (배에서는 신호가 약할 수 있어요)".
- ③ 작업표 셀 "4→4" 불명확 → "회수N→필요N" + 범례(회수=양하에서 나옴, 필요=선적에 듦).
- ④ 동선표가 이동 없으면 통째 사라짐 → 빈 상태에도 카드 유지 + "옮길 콘이 없습니다" 이유 안내.
- ⑤ 베이뷰 컨번호 잘림 → **끝 4자리 노란 강조**(현장 호출 관행), 앞자리 흐리게.
- 비활성 계산 버튼 시각 구분(opacity .35).

**버그 아님 확인**: 작업표에서 콘 0인 베이(받침 없는 최하단 단독 데크, 컨선 40ft 홀드) 행 생략은 정상. 동선표 미표시도 추가필요 0이면 정상(→ ④로 안내만 추가).

**콘앱 시뮬 방법(영구)**: 합성 BAPLIE(LOC+147+pos / EQD+CN+cn+iso / LOC+9 POL / LOC+11 POD / TDT 선박명) 생성 → puppeteer로 cone.html 열고 state 주입 → runCalc()/renderCargoView()/renderBayView() 호출 → fullPage 스크린샷. 비대칭 데이터(선적 전용 베이)로 동선표 경로 검증.

## 7.18 V7.42 엠티실 보고서 정렬·서식 (사용자 출력물 피드백)
- **정렬 확정**: 20피트 먼저 → 40피트, 규격 안에서 컨테이너 번호순. 규격 경계에 소계 행 + 맨 아래 총합 행. (sortBySizeThenCn — ISO 첫 자 2→0, 4→1, L/9→2 순위.)
- **서식**: 보고서 출력 전용 `loadSheetJSStyled()`(xlsx-js-style@1.2.0, window.XLSXS 캐시) 신설. 기존 loadSheetJS(xlsx@0.18.5, 스타일 미지원)는 파서들이 쓰므로 절대 교체 금지 — 출력 전용 로더 분리. 공용 빌더 `buildSealReportSheet(XLSX, {...})` — 제목 병합, 메타 라벨 A:B 병합(좁은 A열 잘림 방지·값은 C열), 헤더 음영, 밴딩, 소계/총합. node 검증 시 같은 빌더를 esbuild로 번들해 실데이터로 생성 → soffice PDF → 이미지 확인.
- **버그 fix**: 선적일자 "Invalid Date" — `new Date(etd)` 파싱 실패가 그대로 문자열화되던 것. fmtDateSafe(실패 시 원문 표시)로 부착·수정 리포트 모두 방어.
- ~~미해결 관찰~~ **【V7.43 해소】 원인 = 선사마다 규격 표기가 다름(22GP/45GP형)** — 그 표기가 엠티실 칸으로 들어옴. 사용자 확정 처리: ① 규격 컬럼에 병기 `20DC(22GP)` (sizeLabelWithRaw) ② 엠티실 칸의 ISO형 값(looksLikeIsoType: `^[24L9][0-9][A-Z][A-Z0-9]$`)은 제거 ③ **입력 있으면 표시, 없으면 빈칸 그대로(플레이스홀더 금지 — 현장 수기 기입 공간)** ④ 시각 옆 **선적위치(베이/열/단)** 컬럼 — 놓친 컨테이너를 자리로 바로 찾기 ⑤ 시각 압축(`06/10 09:12`)으로 8컬럼 A4 한 장 폭.

## 7.19 V7.44 콘앱 동선표 — 콘 종류 명확화
- 사용자 지적: "동선에서 콘을 옮기는데 무슨 콘인지 알 수 없다."
- 수정: 종류 칸 풀네임+색 배지(데크콘=노랑·코끼리콘=파랑·홀드콘=초록), 받는 곳 "베이NN→N개" 단위 명시, 필요 베이 "(N개)".
- 버그 fix: computeRoutes가 totalSupply를 반환하지 않아 요약의 코끼리콘·홀드콘 "배 안 이동" 줄이 조건 거짓으로 통째 미표시 — 반환 필드 추가.

## 7.21 V7.46 콘앱 카고플랜 = 검수앱 카고플랜 V2 그대로 (사용자 확정 — V7.45 방향 정정)
- **V7.45의 오해**: "진짜 카고플랜"을 베이그리드(작업 화면)로 해석해 자체 셀 뷰를 만들었음. 사용자 의도 = **카고플랜 V2** (A4 가로 한 장, o·X 마크, 베이 구조도 검수앱과 동일, 폰 스크롤).
- **해법 = 같은 코드 번들**: `src/coneCargoPlan.entry.jsx` 신규 — 본체 PrintableCargoPlanV2 + cargoPlanCore + 베이사전(.def 내장 289척 포함)을 React째 esbuild iife 번들(`cone-cargoplan.js`, 약 1.6MB, PWA 캐시). build.sh가 빌드마다 재생성 + dist·루트 복사. 콘앱은 카고플랜 버튼/탭 → 번들 동적 로드(loadCargoPlanV2) → `window.ConeCargoPlan.open({containers, shipName, mode})` 한 줄.
- **데이터 변환**: rowsToContainers — parseEDI row → 본체 컨테이너 형식(tier 2자리 패딩, temp→tmp). 카고플랜은 ediRowsAll(전체 화물). 양하·선적 둘 다 있으면 선택 버튼.
- **검증**: SUNNY KALMIA(=KSKM, .def 내장 사전 실선박) 합성 EDI — 사전 매칭으로 15베이 전체 구조(베이별 자체 row/tier·페어 (16)17·해치선) + 화물 F마크 + 별첨 통계 렌더 PASS. 작업표·베이뷰 회귀 없음.
- **정리**: V7.45 자체 카고뷰(renderCargoView·cvBayRender·cvActiveCols·bindCargoTabs ~324줄 + cvtab CSS 28줄) 제거 — V2가 완전 대체.
- ⚠️ 콘앱 EDI에는 선사(op) 정보가 없어 별첨 선사별이 UNK로 묶임 — BAPLIE 한계. 검수앱 연동(fb ediContainers) 경로는 op 포함이라 정상 분류.

## 7.40 V7.90-02 베이 분포 질문 (현장 제보)
- "리퍼가 몇 번 베이에 있어?"가 56개 일치 나열, "어디 어디에 있어?"가 "총 56대"만 — 베이 분포 답변 부재. parseNaturalQuery에 bayDistQuery(`몇 번 베이|어느 베이|무슨 베이|어디 어디|베이별`) 신설 + formatBayDist: 첫 줄 "리퍼 56대 — 베이 3, 7, 20"(음성용, 8개 초과면 개수만) + 베이별 대수(갑판/홀드). posQuery도 결과 >5면 분포로 (개별 56줄 나열은 무의미). 단위테스트 4문항 PASS(기존 질문 회귀 없음).

## 7.53 V7.94-04 X-RAY 미매칭 잔존 키 정리 버튼 (사용자 추적 — "리스트엔 2대뿐인데 1대는 어디서?")
- **잔존 출처 확정**: handleXrayUpload이 **누적(merge)** 방식 — `{...sec.xrayList}` 위에 추가만 하고 새 파일에 없는 옛 키는 안 지움. 이전 업로드(오타/파서 오인/다른 파일)의 키가 영구 잔존.
- **수정**: 업로드 탭 미매칭 표시 옆 **[🧹 미매칭 삭제]** 버튼 — confirm 후 매칭분만 남기고 fbSaveXrayList(교체 저장). 매칭된 X-RAY와 실 입력은 건드리지 않음.
- 단위 검증 PASS (3키 중 미매칭 1키만 제거, 매칭 2키 보존). 누적 업로드 자체는 유지(여러 파일 분할 업로드 지원 목적).

## 7.70 콘앱 C7.64 — 설치 배너 사라짐 원인(scope 충돌) 수정 (2026-06-13)
- **증상**: 콘앱 설치 문구가 사라지고 설치 안 되고 실행만 됨.
- **원인**: 콘앱 매니페스트 scope가 `/greenmarinetally/` — **검수앱과 동일 scope**. 브라우저는 scope+id로 PWA를 식별하는데, 검수앱이 이미 설치된 기기에서 콘앱을 **같은 앱(이미 설치됨)으로 판정** → 설치 배너 억제.
- **수정 (cone.html+public)**: 매니페스트에 **`id: "/greenmarinetally/cone"` 고유 식별자** 추가 + **scope를 `/greenmarinetally/cone.html`로 좁힘** (검수앱과 분리). start_url은 scope 안에 포함돼 설치 조건 충족. 캐시명(cone-v1 vs tallyman-)·SW도 충돌 없음 확인.
- 검증: GitHub Pages 재현 + puppeteer 6항목(id 고유·start_url⊂scope·설치버튼 표시·SW 등록·에러0) PASS.
- **교훈: 같은 도메인 하위에 PWA 2개를 두면 scope·id를 반드시 분리할 것** — 안 하면 한쪽이 다른 쪽을 "이미 설치됨"으로 가려 설치 배너가 안 뜬다.

## 7.69 콘앱 C7.63 — 홈화면 설치(PWA) 지원 (2026-06-13)
- **요구**: 콘앱도 검수앱처럼 홈화면 자동 추가(설치).
- **수정 (cone.html + public/cone.html)**:
  1. 매니페스트 start_url `./cone.html` → **`/greenmarinetally/cone.html` 절대경로**, scope `/greenmarinetally/` (검수앱과 같은 하위경로 설치 404 방지).
  2. 아이콘을 고정 콘(트위스트락) PNG 3종(192·512·512 maskable, base64 인라인)으로 교체. apple-touch-icon도 PNG(iOS 홈화면).
  3. **콘앱 전용 서비스워커 cone-sw.js 신설 + 등록** — 안드로이드 Chrome은 SW가 있어야 "홈화면 추가" 설치 배너가 뜸. 네트워크 우선·캐시 폴백(콘앱은 Firebase 실시간이라 최신 우선).
- 배포 시 **cone-sw.js를 cone.html과 같은 폴더(/greenmarinetally/)에 함께 올릴 것** (검수앱 ZIP에 포함).
- 검증: GitHub Pages 구조 재현 + puppeteer 7항목(start_url·scope·아이콘 3종·apple·SW 등록·진입 200·에러0) PASS.

## 7.68 V7.94-17 — PWA 홈화면 설치 404 수정 + 항만 아이콘 (2026-06-13)
- **증상**: QR로 열면 실행되는데, 홈화면에 설치 후 아이콘으로 열면 404.
- **원인**: 매니페스트 `start_url: "./"` + vite `base: './'`(상대경로). GitHub Pages는 `/greenmarinetally/` 하위 배포라, 홈화면 설치 시 start_url이 루트로 잘못 풀려 404. QR 첫 접속은 전체 URL이라 정상.
- **수정**: manifest start_url·scope = `/greenmarinetally/` (절대경로 고정). 아이콘을 항만 크레인+컨테이너 PNG(192·512·512 maskable, base64 인라인 — 경로 의존 제거)로 교체. iOS는 매니페스트 아이콘 무시 → `apple-touch-icon`(PNG data URI)을 **`_index.entry.html`에 추가** (build.sh가 루트 index.html을 dist 빌드본으로 덮으므로 진입 HTML에 넣어야 생존).
- **빌드 함정**: 루트 index.html에 직접 넣으면 빌드 시 사라짐 — 진입점은 `_index.entry.html`, 최종 루트는 dist 복사본.
- 검증: GitHub Pages 구조 재현(/greenmarinetally/ 하위 서버) + puppeteer 7항목 — start_url 200·scope·아이콘 3종 PNG·apple-touch-icon·설치 후 실행 200·JS 에러 0 PASS. 회귀 66항목 PASS.
- **아이콘 참고**: 움직이는 아이콘은 iOS/안드로이드 모두 홈화면에서 OS가 차단 — 정적 PNG만 가능. 항만 크레인이 컨테이너 인양하는 그림 + 바다.

## 7.67 V7.94-16 — 해치커버 프롬프트 가이드 통합 + 작업보고 항차 오표시 수정 (2026-06-13)
- **사용자 요구**: 해치커버 보고를 별도 화면이 아니라 가이드 작업 흐름의 베이 완료 시점 선택창으로.
- **구현 (GuidedWorkPanel V7.94-16)**:
  1. **양하 — 베이 데크 완료 시** (그룹 데크잔여 0·홀드잔여>0·데크완료>0): "⚓ 이 베이 데크 양하 완료!" → [🔓 해치커버 오픈 → 홀드 진행](오픈 보고 자동 발송 후 홀드 카드 진행) / [다른 데크로 이동].
  2. **선적 — 베이 홀드 완료 시** (기존 V7.94-08 프롬프트 개편): [🔒 해치커버 클로즈 → 데크 계속](클로즈 보고 발송) / [다른 베이 홀드 이동].
  3. **양하 — 그룹(홀드까지) 완료 시** (홀드 작업분 존재): 완료 화면에 [🔒 해치커버 클로즈] / [다른 베이 홀드 이동].
  - 보고: buildHatchMessage(WorkReportModal 해치 보고와 동일 형식) — 그룹 실제 베이 번호들·장비(getEquipNumber)·모드별 항차(voy_d/voy_l), fbAddWorkReport 기록 + 카톡 공유. 그룹 변경 시 프롬프트 플래그 자동 리셋.
- **작업보고 항차 오표시 수정 (메모 #9)**: handleHatch/handleConBox가 `activeByEquip[equip][0].mode` 고정 — 활성 작업 없으면 voy_d 폴백(선적인데 양하 항차), 동시 활성이면 [0] 고정. → `equipModeOf`: 장비 활성 모드 1개면 그것, 아니면 화면 선택 모드(selectedMode).
- 검증: SWRG 실데이터 시뮬 12항목(양하 그룹6 데크44/홀드48 흐름·선적 회귀·메시지 항차·equipModeOf 3케이스) + 기존 회귀 54항목 전부 PASS.

## 7.66 V7.94-15 SWRG(SAWASDEE RIGEL) 풀 테스트 — 실데이터 전수 검증 (2026-06-13)
- **대상**: 양하 2605N(EDI 909대, 평택 402) → 선적 2606S(EDI 890대, 평택 384). IMO 9943803, 표준 SMDG22.
- **전수 교차 결과**:
  - XRAY 엑셀 10건 → 파서 정확 10건·오탐 0, 전건 평택 양하분 존재 ✓
  - 선적 선사 3종(HAS 116 + HSL 1 + SKR 267 = 384) = EDI 평택분 384 **완전 일치** ✓
  - 양하 LIST 404행: EDI 평택 402 + **광양분 1건 혼입(SKLU1981464, EDI상 POD KRKAN @23-00-86)** + 중복 1건 — 현장 주의. (제안: 리스트 업로드→EDI 대조 기능)
  - 가이드: 양하 402 전수 커버·트윈 61쌍 / 선적 384 전수 커버·트윈 12쌍. 컨번호 공백(EQD `DFSU 1452694`)도 정상 처리.
- **수정 2건**:
  1. guidedQueue 선적 "FR·OT 마지막"이 **`oog` 필드 미인식** — SWRG 오픈탑(ISO 2261/4261)은 ot 없이 oog만 참이라 분류 누락. `c.oog || twin?.oog` 추가. 단 **홀드 OT는 기존 물리 제약 예외(위에 데크 적재 필요 → 흐름 내 유지)가 그대로 적용** — 데크 OT만 마지막으로 감 (합성 데크 OT로 검증).
  2. fillEmptyBaysSequential — **사전 없는 신규 선박**에서 EDI 관측 베이에 4의 배수가 전무(트리오 01,02,03/05,06,07… 구조)하면 4의 배수 추정 채움 금지. SWRG 가짜 추정 14개 → 5개(25,27,33,35,37 — 페어 홀수, 합리적).
- 검증: SWRG 시뮬 10항목 + 기존 회귀 44항목 전부 PASS.
- **참고**: SWRG는 사전(V2/v3) 미등록 — 오늘 작업 후 빌더 [저장]하면 v3 등록되어 콘앱(C7.62)에서도 카고플랜 골격 사용 가능.

## 7.65 콘앱 C7.62 — 카고플랜 "새 기기에서 특정 선박 안 보임" 원인·수정 (2026-06-13)
- **증상**: YKTD 카고플랜이 기존 폰은 보이고 새 폰은 "선박 정보를 찾을 수 없습니다 (shipImo=undefined…)". 서버(v3)에 재저장해도 계속 실패.
- **원인 (통합 방안의 빈 구멍)**: 카고플랜 모듈(cone-cargoplan.js, 검수앱 번들)의 사전 탐색 순서 = ① localStorage 사용자 사전(master_user_bay_dict_v1) → ② 임베드 v2 → ③ **window.__fbShipBayDict 전역(검수앱 런타임이 채움)**. **콘앱은 이 전역을 채워주지 않아** Firebase v3 사전이 모듈에 전달되지 않음 → 로컬 사전 보유 기기(기존 폰)만 매칭 성공. 서버 재저장은 콘앱 카고플랜에 무의미했음.
- **수정 (C7.62, cone.html+public 동시)**: openCargoPlanV2에서 모듈 호출 전 `window.__fbShipBayDict = await fbFetchBayDict()` 주입 — 검수앱과 동일 매칭 경로 확보. 번들 수정 불필요.
- 검증 (puppeteer, v3 mock에 실서버 YKTD entry 구조 재현): 주입 전 실패 화면 재현 → 주입 후 사전 주입·실패 화면 소멸·카고플랜 골격 렌더 PNG 확인 — 3항목 PASS.
- 참고: YKTD entry는 name='YKTD'·imo·콜사인 빈값 (약자만) — 매칭은 code 정확 일치로 성공. 추후 빌더에서 실선박명·IMO 채우면 더 견고.
- **운영 처방 갱신**: "안 보이는 선박 = 보이는 폰에서 빌더 재저장" + **콘앱 C7.62 이상 필수**. 로컬 사전 → 서버 일괄 동기화 버튼은 다음 작업 후보.

## 7.64 콘앱 C7.61 — 카고플랜 "못 본다" 원인·수정 (2026-06-13)
- **증상**: 콘앱에서 카고플랜이 안 열림 (사용자: 인원 제한? → 아님, Firebase 무료도 동시 100명).
- **원인 (코드 확정)**:
  1. 카고플랜 모듈 cone-cargoplan.js(1.6MB) 로더가 `?v=&r=Date.now()` — **열 때마다 캐시 무력화로 1.6MB 재다운로드**. 배 안 약신호에서 재시도 1회로도 실패 → "모듈을 내려받지 못했습니다". SW가 프리캐시·런타임 캐시를 갖고 있는데 r= 때문에 매번 다른 URL이라 캐시 미스.
  2. (보조) 항차 목록 fbFetchVoyages가 `voyages.json?shallow=false` — **모든 항차의 EDI 원문·컨테이너 포함 전체 다운로드** (수 MB~수십 MB).
- **수정 (cone.html + public/cone.html 동시)**:
  1. `r=Date.now()` 제거 → `?v=__APPV`만. `window.__APPV='C7.61'` 버전 상수 신설 — **모듈 갱신 시 이 값도 같이 올릴 것**.
  2. 로드 재시도 1회 → 2회 (1.2s, 3s 백오프).
  3. fbFetchVoyages 경량화: shallow 키 목록 + 항차별 info/discharge/loading shallow 병렬 (총 수십 KB).
- 검증: mock fetch 시뮬 5항목(hasD/hasL/정렬/noEdi) PASS + puppeteer 실 렌더(APPV·JS 에러 없음).
- 효과: 카고플랜 모듈은 첫 1회만 다운로드(이후 캐시 즉시), 항차 목록 수십 KB.

## 7.63 V7.94-14 검수원 로그인/작업중 상태 구분 (2026-06-13)
- **증상**: 전부 로그아웃해도 검수원 선택 화면에 "●작업중"이 남음.
- **원인**: 로그아웃이 Firebase에 기록되지 않음(폰에서만 이름 제거) + 배지 판정이 "최근 60초 lastActive"뿐. 로그아웃 인사 모달 동안 30초 heartbeat가 계속 돌아 체감 더 길어짐.
- **수정**:
  1. `fbSetInspector`(로그인)에 `loggedIn:true, loginAt` 마킹, 신규 `fbLogoutInspector`(로그아웃 버튼 즉시 `loggedIn:false, loggedOutAt`) — App.handleLogout에서 호출.
  2. 공용 판정 유틸 `src/inspectorStatus.js`: working(로그인+90초 내 활동) / online(로그인+활동 끊김) / null(로그아웃 또는 이력 없음). loggedIn 필드 없는 과거 데이터는 활동 시각만으로 판정(하위호환).
  3. 표시 3곳 교체: InspectorModal(●작업중 녹색 / ○로그인 하늘색 + 상단 "N명 작업중 · N명 로그인"), StaffManagerModal(작업중/로그인/접속이력 3단), HomePage 항차 카드(로그아웃 마킹 제외).
- 검증: 판정 시뮬 8항목 PASS + JSX 문법 + 빌드.
- 참고: heartbeat(fbSetInspectorActivity)는 loggedIn을 건드리지 않으므로 로그아웃 마킹이 tick에 덮이지 않음.

## 7.62 V7.94-13 MCSN 95베이의 진짜 원인 — BAPLIE 위치 미정 코드 + PDF 워커 404 (2026-06-12)
- **MCSN 실EDI(MCC/MAE BAPLIE) 분석 확정**: 781대 중 단 1대(HASU1411466)가 `LOC+147+0999999` = **99-99-99 '위치 미정' 관례 코드**. 이 1대가 max를 99로 끌어올려 1~max 자동 채움이 95베이를 생성한 것. 나머지 780대는 전부 베이 1~44로 .def 구조와 일치.
- **수정 3종**:
  1. buildMatrixFromEdi: bay·row·tier가 모두 99인 컨은 매트릭스 산정에서 제외 (검수 목록 등 다른 흐름은 유지).
  2. 사전 적용 시 `dictBaySet` 기록 → fillEmptyBaysSequential이 **사전에 없는 베이(4의 배수, 42 등 실선 부재)를 추정으로 채우지 않음**.
  3. PDF 워커 404 수정: Vite new URL 자산 처리가 해시 중첩 파일명(pdf.worker.min-HASH-HASH.mjs) 생성 → **public/pdf.worker.min.mjs 정적 파일 + BASE_URL 참조**로 교체 (pdfjs-dist 4.10.38).
- **최종 검증 (MCSN 실EDI)**: 총 34베이 / 추정 0 / max 45 / 42·4의 배수 없음 — .def 분석과 완전 일치. 게이트·회귀 36항목 PASS.
- 교훈: BAPLIE에 99-99-99(위치 미정) 컨이 존재할 수 있음 — 매트릭스·통계 산정 시 항상 제외할 것.

## 7.61 V7.94-12 MCSN 베이 매트릭스 오염 사건 — 원인·수정 (2026-06-12)
- **증상**: MCSN 622N 빌더가 총 96베이(추정 74개 자동 채움) 생성. 화면 "사전: SPIL NIKEN".
- **진상** (.def 바이너리로 확정):
  - MCSN.def 헤더 = "MCSN / SPIL NIKEN / 콜사인 3E3921 / IMO 9273947" → **MCSN의 실제 선박명이 SPIL NIKEN — 사전 매칭 자체는 정상**.
  - 진짜 원인: V2 사전의 MCSN entry가 **PDF box-region 자동 파싱(M6.71) 오염** — 트리오 3번째 베이(03,07,11…) 전부 누락 + 엉뚱한 bayNo 00, 24베이. 이 결손 구조가 EDI와 결합 + 1~max 자동 채움으로 96베이 쓰레기.
  - **같은 오염 8척**: KKLC, LYTJ, MAMP, MCSN, NBTD, NSFR, SWSP, TNJP (전부 M6.71-box-region-auto).
  - **.def 파서(M4.4)는 정상** — MCSN.def을 34베이(01~45, 42는 실선에 없음, 41 단독+43그룹)로 정확히 읽음. 베이 테이블 = offset 135900, stride 189, "DD+공백5" 패턴 (v6.50 변형도 동일 구조 확인).
- **수정 3종**:
  1. V2 MCSN entry를 .def 분석(M4.4) 결과로 교체 — name 'SPIL NIKEN', imo 9273947, callsign 3E3921, 34베이.
  2. augmentMatrixFromBayDict에 **오염 패턴 자동 거부**: 사전 베이에 00 존재 + 트리오 3번째 결손 ≥2건이면 미적용 + 사유 표시 (나머지 7척 방어, entry 데이터는 보존).
  3. **정합성 게이트**: EDI 실측 최대 베이 > 사전 최대 베이면 미적용(약자 재사용/엉뚱한 사전 방어). V2 필드명 bayNo 폴백(bs.bay ?? bs.bayNo)도 게이트·결합 본문에 적용.
- 빌더 UI에 거부 사유 표시 추가 (⚠ 사전 미적용: 이름 — 사유).
- 검증: 게이트 시뮬 5항목(MCSN 정상 34베이 보강·TNJP 오염 거부·게이트 거부·ATPR 회귀) + 기존 31항목 회귀 전부 PASS.
- **미해결**: 사용자가 본 "EDI에서 99베이"의 출처 — MCSN EDI 미수령. EDI 위치 오독 가능성 확인 필요 (다음 세션, EDI 받으면).

## 7.60 V7.94-11 위치 선택창 — 베이 우선 선택 + 완료 자리 비활성 (사용자 확정 2026-06-12)
- **원칙: 위치 선택창에서 전체 베이의 자리를 한꺼번에 보여주지 말 것 — 오선적 유발.** 선적할 베이를 먼저 선택하고 그 베이 자리만 표시.
- 1단계: 베이 버튼 그리드(B9 / 남은 N자리, 남은 0이면 비활성) → 2단계: 그 베이의 자리 칩 + [← 베이 다시 선택].
- **선적 완료된 자리도 표시하되 선택 불가**: ✓회색 span(✓01-02), 활성 자리만 button. 베이 전체 그림은 보이게.
- 검증: 실 렌더 8항목 PASS(베이 화면 선행/남은 수/크기필터/완료 ✓비활성/타 베이 미노출/복귀/confirm 회귀) + 회귀 28 PASS.

## 7.59 V7.94-10 컨테이너 번호 수정 버튼 + 다른 베이·목적지 구역 경고 (사용자 확정 2026-06-12)
- **두 기능 병행** (사용자 확정): [위치 수정] = 같은 컨, 자리만 변경(7.58 슬롯 피커) / **[컨테이너 번호 수정 (다른 컨이 옴)]** = 신규 — 실제 온 컨 검색·선택 → [위치 선택 →] → 그 컨 대상으로 남은 자리 창(트윈 짝꿍 자동 포함).
- **경고 2종** (confirm 단계):
  - ⚠ **다른 베이에서 오는 컨테이너**: 계획 베이 ≠ 선적 베이 (번호수정 칩에도 미리 ⚠ 다른 베이 표기).
  - 🚨 **목적지 구역 이탈**: 선택 자리의 EDI 계획 컨 POD ≠ 옮기는 컨 POD (슬롯의 원래 계획 컨이 구역 기준 — 실측: BAY 23/24/25=CNWEI, 나머지=CNDLC). 트윈 짝꿍 자리도 동일 검사.
- 구현: BigResultCard posTarget 리팩터(모달 대상 = c 또는 선택 컨), PositionEditModal에 pickedSlotCn·bayWarn/podWarn/partnerPodWarn.
- **React 훅 함정 2건 (렌더 검증으로 적발)**: ① useMemo가 자기보다 아래 선언된 state 참조 → TDZ 크래시. ② 모달의 조기 `return null` 뒤에 훅 추가 → "Rendered more hooks" 크래시. **컴포넌트에 훅 추가 시 반드시 조기 반환 앞·의존 선언 뒤에 둘 것.**
- 검증: 실 렌더 8항목 PASS(두 버튼/칩 경고/그리드/경고①②/짝꿍 25베이/같은 구역 무경고) + 회귀 28 PASS.

## 7.58 V7.94-09 위치 수정 — 남은 자리 선택창 + 트윈 짝꿍 자동 (사용자 요청 2026-06-12)
- **배경**: 선적 중 선내 위치가 바뀌면 기존엔 수정→위치 타이핑→저장→선적완료 4단계.
- **변경**: PositionEditModal 기본 화면을 **남은 자리 그리드**로 — 같은 모드·미완료·크기 일치(20ft↔홀수베이/40ft↔짝수베이) 슬롯을 베이별 칩으로 표시, 탭 한 번에 확인 단계로. 직접 입력/미배정은 접기 폴백으로 유지.
- **트윈 짝꿍 자동**: 대상이 20ft고 트윈 후보(findTwinCandidate)가 있으면, 앞 자리 선택 시 뒤는 bayPairs 짝꿍 베이·같은 row/tier로 자동 배치 (예: 21-00-04 → 23-00-04). confirm에 두 줄 표시.
- **배정 후 바로 선적확인** 체크(기본 켬, 미완료 컨만): 확정 시 reassign(짝꿍 포함, 밀려난 컨 자동 미배정) 후 fbCompleteContainer까지 — 4단계가 탭 2번으로.
- 연결: BigResultCard·ContainerDetailModal 양쪽, 새 props twinPartner/bayPairs/onSavePartner/onCompleteBoth (기존 onSave는 직접 입력 경로로 하위호환).
- 검증: 실 렌더·호출 10항목 PASS(그리드/크기필터/접기/짝꿍 bayPairs 일치/체크 기본값/save·partner·complete 호출/40ft 짝수베이만) + 시뮬 28 회귀 PASS.
- 주의: import 누락은 esbuild가 못 잡음(미정의 전역 취급) — useMemo·twin.js처럼 컴포넌트에 새 식별자 쓰면 import 직접 확인할 것.

## 7.57 V7.94-08 선적 가이드 — 현장 메모 5건 일괄 반영 (사용자 확정 2026-06-12)
모두 **선적 상황** 규칙. 양하는 기존 유지(FR만 우선).
- **① 트윈 스택 연속**: 혼재 베이에서 20ft 트윈은 같은 로우 스택을 바닥부터 연속으로 쌓음(로우는 해상→육상). 트윈 아래 깔린 40ft는 그 직전에 끌어옴(적재 종속). 남은 40ft는 층 순서. 최종: 싱글 → 트윈(스택) → 40ft → 데크 → FR·OT.
- **③ FR·OT 선적 마지막**: 선적의 마지막 단계는 FR+OT (물리 제약 예외는 기존 FR 로직과 동일).
- **② 홀드 완료 프롬프트**: 한 베이 홀드 선적이 끝나면(홀드잔여 0·데크완료 0) 카드 대신 "⚓ 홀드 선적 완료" 배너 — [이 베이 데크 계속]/[다른 베이 선택].
- **④ 수정 시 위치 꼬임 해결**: 선적 수정은 fbReassignContainerPosition으로 실제 컨을 예측 슬롯에 배정 — 그 자리 예측 컨은 **자동 미배정(bay='')+완료취소** (기존 M3.87/M6.72 함수 재사용). 밀려난 컨은 가이드 하단 "⚠ 위치 미배정 N대"에 표시, 탭하면 상세에서 재배정.
- **⑤ 트윈 동시 수정**: 트윈 카드의 수정은 앞·뒤 두 입력 — 바뀐 쪽만 선택하고 [선택한 수정 적용]하면 안 바뀐 쪽은 예측대로 확인.
- 검증: 선적 정렬 시뮬 9항목(스택 연속·종속·FR/OT·양하 회귀) + 기존 11(선적 3번 기대값을 새 규칙 9-06-04로 갱신)+FR 8 회귀 + 실 렌더 7항목(트윈 수정 폼·프롬프트·미배정) 모두 PASS.

## 7.56 V7.94-07 콘앱 카고플랜 "선박 정보를 찾을 수 없습니다" (사용자 현장 스크린샷 — STMJ)
- **원인**: STMJ 베이사전은 Firebase 공유 사전(window.__fbShipBayDict)에만 있는데, 콘앱(cone-cargoplan.js)은 Firebase를 로드하지 않아 사전이 빈 상태로 조회 → dictData null. (콘은 shipImo도 안 넘기지만, 사전만 보이면 code='STMJ' 매칭으로 충분.)
- **수정**: ① 메인 앱이 Firebase 사전 수신 시 localStorage `gm_fb_baydict_cache`에 미러(quota 초과 시 조용히 생략). ② shipStructure의 Firebase 사전 참조 5곳을 getFbBayDict() 헬퍼로 통일 — 메모리(__fbShipBayDict) 우선, 비어 있으면 캐시 폴백. 콘앱은 같은 오리진이라 캐시 공유됨.
- **운영 조건**: 폰에서 메인 앱을 V7.94-07로 한 번 열어야 캐시가 생김 → 그 후 콘앱 카고플랜 정상.
- 검증: 콘앱 상황 재현 시뮬 3항목 PASS(캐시 폴백 조회 fb-code 매칭 / 메모리 우선 회귀 / STMJ entry 정합) + 기존 11+FR 8 회귀 PASS.
- 참고: getShipBayDictData 반환은 평탄화 구조(name/code/bayDef/_v5Matrix가 최상위) — data 래핑 아님.

## 7.55 V7.94-06 가이드 순서 — 40/20 혼재·FR 특수화물 규칙 (사용자 메모 확정 2026-06-12)
- **40ft/20ft 혼재 시 40ft 먼저**: 별도 규칙 아님 — 적재 구조에서 자연 발생. 양하는 위층부터라 트윈 위 40ft가 먼저, 선적은 아래층부터라 바닥 40ft가 먼저. **바닥이 20ft면 일반 순서 그대로** (사용자 확인). 층 단위 정렬이 이미 충족 → 코드 무변경.
- **FR(플랫랙) 특수화물: 우선 양하 / 마지막 선적** — 단계 규칙으로 구현.
  - 물리 제약 예외(층 순서 유지): ① 같은 줄 위에 비FR 작업분 존재 ② 홀드 FR인데 데크 작업 잔여(양하·선적 공통).
  - 트윈 한쪽이 FR이면 카드 전체를 FR로 취급.
  - 최종 단계: 양하 = FR → 일반(40ft+트윈) → 짝없는 20ft 싱글 / 선적 = 싱글 → 일반 → FR.
- 검증: ATPR 실위치 + FR 플래그 시뮬 8항목 PASS(우선/마지막 + 제약 4종 + 무FR 회귀 2종) + 기존 11항목 회귀 PASS.

## 7.54 V7.94-05 가이드 카드 정보 전면 보강 (사용자 요청 — "기본 정보가 다 보여야")
- 카드에 추가: **규격(tp/iso)·FULL/EMPTY·무게(formatWt, kg→t)·선사(op)·항로(POL→POD)·OOG 배지**.
- **실번호 줄**: 현재 값 표시(미입력 시 ⚠경고색), 탭하면 인라인 입력 → fbUpdateRecordSeal 저장. 트윈은 앞·뒤 각각.
- **XRAY 줄** (대상만): 입력값/미입력 표시, 탭하면 XRAY 실번호 + E-실(선택) 입력 → fbSetXraySeal 저장.
- 무게는 반드시 formatWt 사용 — EDI wt는 kg라 직접 표기 시 7200t 오표기 (이번에 렌더 검증에서 적발).
- 검증: ATPR 실데이터 렌더 8항목 PASS (정보 표시 7 + 인라인 입력창 열림).

## 7.52 V7.94-03 X-RAY 상단 0/3 vs 리스트 2 불일치 (사용자 스크린샷 제보 — STMJ 2627E)
- **원인**: VoyageSummaryCard는 `xrayList` 원본 키 전부(3), ListTab은 현재 컨테이너와 매칭분만(2) — 카운트 기준 불일치 (V7.93-02 평택분 패턴과 동류). 미매칭 1건 = EDI/리스트 어디에도 없는 컨번호(오타/타항차 잔존) → **검사 누락 위험이라 숨기지 않고 드러냄**.
- **수정**: ① 요약 칩 = 매칭분 기준 `0/2`, 미매칭 있으면 빨강 `· ⚠N 미매칭`. xraySealed도 매칭분 기준. ② 업로드 탭 X-RAY 섹션에 미매칭 컨번호 전체 나열("⚠미매칭 1대: ZZZU9999999 — 확인 필요").
- **검증**: react-dom/server 실 렌더 시뮬 4항목(스크린샷 상황 재현 3키/1미매칭 + 정상 회귀) PASS.

## 7.51 V7.94 자동 가이드 모드 — 앱이 양하/선적 순서를 예측 제시 (사용자 핵심 비전 1차 구현)
- **목적**: 앱이 크레인 순서대로 "다음 컨테이너"를 카드로 제시 → 검수사는 [확인]/[수정]만. 몸은 검수사, 소프트웨어는 앱.
- **순서 규칙 (사용자 확정, ATPR 2627E/2628W 실데이터 PASS)**:
  - 양하: 데크→해치→홀드, 맨 위 티어부터, 같은 티어는 **육상→해상** 로우.
  - 선적: 홀드→해치→데크, 맨 아래 티어부터, 같은 티어는 **해상→육상** 로우.
  - 육상/해상 판정 = **접안 방향** (우현 접안 = 짝수 로우가 해상쪽). 항차당 1회 입력, `voyage.info.berthSide`('port'|'starboard') 저장.
  - 트윈: 기존 findTwinCandidate 그대로 — 홀수+홀수 짝 한 카드. 40ft는 일반 작업(자기 층 차례 — 트윈 위에 얹힌 40ft도 층 순서로 자연 해결).
  - **싱글모드 = 짝 없는 20ft**: 양하 맨 마지막 / 선적 맨 처음 (크레인 모드 전환 1회). 단 싱글 '아래'에 일반 작업분이 있으면 층 순서 유지(적재 종속 예외).
  - 쉬프팅: 가이드에서 감지하지 않음 — 발생 시 수동 모드 사용 (사용자 결정. 평택분 EDI엔 타지역 컨 정보 없음).
- **UI**: SearchPanel 상단 큰 토글 🤖자동 가이드(보라) / ✋수동(주황) — 한눈에 구분. 자동 = GuidedWorkPanel: 접안 선택 → 베이 그룹(B19·20·21) 선택 → 예측 카드(컨번호 거대·위치·XRAY/리퍼/DG 배지·트윈 앞뒤 동시) + [확인]/[수정]/[수동 전환] + 다음 예정 미리보기 + 음성 안내.
- **수정 흐름**: 실제 나온 컨 입력 → 그 컨 완료, 예측 컨은 큐에 남아 재제시. **수정 3연속 = 플랜대로 진행 안 됨 판단 → 자동 수동 전환** (알림 후).
- **파일**: `src/guidedQueue.js`(순수 정렬 모듈), `src/components/GuidedWorkPanel.jsx`, SearchPanel 토글 통합.
- **검증**: 실데이터 시뮬 11항목 PASS(양하 32장·선적 48장·좌현 뒤집힘) + puppeteer 실 렌더링 3화면(접안/베이선택/카드/트윈/토글) PASS + 빌드 JS 키워드 검증.
- **V7.94-02 (사용자 피드백 — 오선택 방지 + 장비 단계)**:
  - 설정 흐름 확정: **선박 → 양하/선적(기존 탭) → 장비(호기) → 좌/우현 → 베이**. 가이드 진입 시 장비 결정을 항상 한 번 거침(설정돼 있으면 현재 호기 탭 1회로 통과). 장비는 헤더 🏗·작업보고와 동일한 localStorage(gm_equip_no)+equipChanged 이벤트 공유.
  - 접안 방향 **오선택 방지**: 선택 시 confirm("[우현 접안]으로 저장합니다… 맞습니까?") 한 번 더 확인. 변경도 confirm 후 진행 — 한 번 누르면 끝이 아님.
  - **설정 칩 바** 신설: [🏗 N호기][우현 접안][B20 변경] 칩이 모든 단계에 항상 표시, 탭하면 해당 단계로 돌아가 변경 가능.

- **증상 (스크린샷)**: 양하 헤더 "0/909·리퍼 63대" vs 목록 403대·리퍼 32 — V7.92-02 후에도 일부 미수정.
- **전수 grep 결과 (9.2-② 방식)**: ediContainers를 세면서 평택 판정 0회인 곳 — ① **VoyageSummaryCard(현황 요약)** = 909의 범인 ② **WorkClosingChecklist(마감 점검)** ③ **GlobalSearchPage(전 항차 통합 검색)**. 셋 다 평택분 필터 적용 (③은 V7.92-02와 같은 규칙 — 집계는 _ptk, 컨번호 단건 조회는 전체 유지). VoyFixWidget(존재 체크)·BayDictVerifyWidget(구조 검증 — 전체가 정답)은 해당 없음.
- **트윈 무게차 한계 확정 (사용자)**: **부두별 — 동방아이포트(PNCT) 14톤, 평택컨테이너터미널(PCTC) 20톤.** 차이 초과 = 수평이 안 맞아 **불가**(V7.93의 임시 10톤 "주의" 폐지). TWIN_DIFF_LIMITS 상수 + twinDiffLimit(pier). 부두 출처 = voyage.info.pier(M5.82 PORT-MIS 자동 저장). **부두 미상이면 보수적으로 14톤** + "부두 미상·보수 기준" 표기. 질문·브리핑 모두 부두 자동 적용.
- 검증: 단위 14문항(한계 함수·PNCT 차15 불가·PCTC 차15 가능·미상 보수·브리핑 부두별) + 실렌더 4케이스(현황요약 0/5·마감점검 5대·트윈 PNCT 불가·브리핑) + 회귀 3종 — 전부 PASS.

## 7.49 V7.93 트윈 작업 무게 점검 (사용자 도메인 지식 — 현장 안전)
- **도메인 (사용자 제공, 영구)**: ① 카고 크레인 트윈 작업은 **두 컨 합계 55톤 초과 시 불가** ② 두 컨 **무게 차이가 급격하면 수평이 안 맞아 트윈 불가** (불균형).
- **단독 질문**: "20번 베이 트윈 작업이 가능해" / "트윈 무게 확인" (twinCheckQuery — /트윈/+가능·되·불가·확인·무게 등) → 불가 쌍의 **선내 위치(19-02-82 ↔ 21-02-82)+합계 무게**, 불균형 쌍의 차이값, 무게 미상 쌍(EDI 확인 안내), 가능 쌍 수. 짝수 베이로 물어도 양옆 홀수 쌍 포함(N±1). 첫 줄 음성용 한 문장. 대상 = 평택분·현재(또는 지정) 모드·미완료.
- **브리핑 통합**: generateBriefing 4번째 인자 pairsMap(옵션·하위호환) — 초과 쌍 "🏗 싱글 작업 검토", 불균형 쌍 "⚖ 수평 주의"를 ⚠ 주의사항에 자동 포함.
- **구현 원칙**: 짝 판정은 **twin.js getBayPairs 단일 규칙**(홀수 베이+짝수 베이 구조 존재 시 ±2 짝꿍, 같은 row/tier/모드) — 트윈 작업 화면과 동일 보장, 점검 코드가 자체 짝 규칙을 만들지 말 것. nlSearch의 분석 함수(buildTwinPairs/analyzeTwinPairs/generateTwinCheckAnswer)는 pairsMap 주입식 순수 함수 — node 단위테스트 가능.
- **상수**: TWIN_MAX_TOTAL_KG=55000 (사용자 확정) / **TWIN_DIFF_WARN_KG=10000 — ⚠ 임시값, 크레인 실제 불균형 기준 확인 후 이 상수만 변경.** 불균형은 기준 확정 전이라 "불가"가 아닌 "수평 주의"로 표현하고 차이값을 항상 표기.
- 검증: 단위 18문항(파싱·쌍묶기·분석·답변·브리핑, 정상/초과56톤/불균형13톤/미상/단독/40ft 합성) + 실렌더 3케이스(EDI 폴백 짝 구성 경로 포함) + 회귀 4종(챗봇·동의어·평택집계) — 전부 PASS.

## 7.48 V7.92-02 챗봇 집계 = 평택분 통일 (사용자 제보 — 양하 탭과 숫자 불일치)
- **증상**: 양하 탭의 양하 개수와 자연어(검색) 질문 답의 양하 개수가 다름 — 챗봇이 오답.
- **원인 (7.44 잔여)**: SearchPanel allContainers = EDI 전체(통과화물 포함). V7.90-07에서 브리핑만 평택분으로 고치고 **통계·조건 검색 경로는 그대로** — "양하 몇 대야"가 통과화물까지 셈. 양하 탭은 평택분(POD평택)만 세므로 불일치 (9.2-②의 7.1 위반 패턴).
- **수정**: allContainers에 `_ptk` 플래그(양하=POD평택·선적=POL평택, isPyeongtaekPort 단일 함수) — ① 조건 검색·집계 results 평택분만 ② generateLocalAnswer 모집단(capacity/vacant/bayBreakdown/progress) 평택분 ③ 상단 배지(양하/선적 작업대기) 평택분 ④ 검색창 안내 "전체 N대"→"작업 N대"(평택분).
- **⚠ 예외 (V7.53 회귀 방지)**: **컨번호(digits) 단건 조회는 전체 유지** — 통과화물을 스캔했을 때 "없습니다"가 아니라 찾아서 알려주는 게 정답(내리지 말 것 판단 근거).
- **원칙 재확인: 챗봇·배지·탭의 모든 "개수"는 평택분(7.1) 하나의 기준 — allContainers를 새 집계에 쓸 때는 반드시 _ptk를 거칠 것.**
- 검증: 평택 3 + 통과 2 합성 마운트 — 배지 3대 / "양하 몇 대야" 3대 / 리퍼 1대 / 20피트 2대 / 5번 베이 1대 / 통과화물 끝4 조회는 여전히 찾음 — 7/7 PASS + 기존 3종 회귀 PASS.

## 7.47 V7.92 챗봇형 일상 질문 — 자기소개·시간·날씨·입출항 (사용자 요청)
- **요청**: "넌 뭐야"에 자기소개로 답하기 + 시간·날씨·선박 입출항시간도 답하기.
- **검수앱 질문기 4종 신설** (parseNaturalQuery 플래그 + SearchPanel localAnswer):
  - introQuery("넌 뭐야/너 누구야/자기소개") → generateIntroAnswer — 정체 + 현재 선박 + 질문 예시.
  - schedQuery("입항/출항(?!지)/입출항/접안/배 언제") → **portMisMatch.js 신규 모듈** matchPortMis로 PORT-MIS 매칭 — 입항·출항 시각 + 부두. VoyagePage의 화면용 160줄 매칭은 안 건드리고 핵심 규칙만 독립 구현 (7.8 방향: 최신 updatedAt 우선 / V7.30 선박명 가드 포함). portMisData prop: VoyagePage → SearchPanel → SingleSearch.
  - timeQuery("몇 시(?!간)/무슨 요일/오늘 며칠") → generateTimeAnswer — 기기 시간.
  - weatherQuery("날씨/비 와/바람 어때") → Open-Meteo(무키 API, 평택항 36.967/126.822) — 날씨·기온·풍향/초속(현장 작업 관련)·최저최고·강수확률. 비동기(useEffect+state), 실패 시 안내문.
- **⚠ 우선순위: 입출항 > 시간** — "입항 **시간** 알려줘"가 timeQuery에도 걸리므로 schedQuery를 먼저 처리해야 시계 답으로 새지 않음.
- **⚠ 검증이 잡은 버그**: 패치를 SearchPanel(바깥)에만 적용 — 실제 답변 로직은 내부 SingleSearch 컴포넌트라 `portMisData is not defined` 런타임 에러. **"props 추가 시 선언부+모든 호출부 동시 수정" 원칙 재확인** — SearchPanel 파일 안에 SingleSearch·TwinSearch가 별도 컴포넌트로 존재함을 기억할 것. 마운트 검증 없이 컴파일만 믿었으면 출고됐을 버그.
- 게이트(7.40)·KNOWN 어휘(복원 헛호출 방지)·HelpModal 카탈로그 ④ 동시 등록.
- 검증: 단위 27문항(파싱 19·답변 3·매처 5) + SearchPanel 단독 마운트 6케이스(자기소개/입출항/시계/날씨 모킹/일반·베이 질문 회귀) — 전부 PASS.

## 7.46 V7.91-02 질문기 일상 동의어 확장 — 검수앱+콘앱 (사용자 요청)
- **요청**: "전체"는 되는데 "전부", "다"는 안 됨 — 일상 대화에서 통용되는 동의어를 두 앱 질문기가 다 알아듣게.
- **검수앱(nlSearch)**: ① 전체류 — 전부·모두·몽땅·싹 다·죄다·도합·통틀어·합쳐서·다해서 + **단독 "다"**(토큰 경계 매칭 `(?:^|\s)다(?=\s|$|[?.!,])` — "남았다" 속 '다' 오인 방지) ② 구역 — **'데크' 한글이 갑판 패턴에 누락**돼 있던 것 추가 ③ 엠티 — '빈 컨(테이너)' ④ 진행 완료 — 내렸·내린 거·다 했·다 됐 ⑤ 리스트 — 주세요·달라·불러줘·뽑아줘 ⑥ 베이분포 — 어떤 베이.
- **콘앱(coneQaAnswer)**: 전체류 동일 확장 / 남는류 — 남았·남을·뺄·빼야·돌려 / 모자란류 — 필요·가져가야.
- **⚠ 단위 도메인 원칙 (검증에서 발견한 충돌)**: **콘은 '개', 컨테이너는 '대'로 센다.** 콘앱 베이 질문의 컨테이너 대수 분기에 '몇 개'를 추가하면 "데크콘 몇 개"가 컨테이너 질문으로 새서 콘 답이 안 나옴 — 그 분기는 `/컨테이너|몇\s*대|대수/` 유지("컨테이너 몇 개"는 '컨테이너' 단어로 매칭됨).
- **복원 어휘 동기화**: 새 동의어는 검수앱 KNOWN(SearchPanel)·콘앱 CONE_QA_KNOWN에도 등록 — 알아듣는 단어가 AI 복원을 헛되이 트리거하지 않게. **파서 어휘를 늘리면 KNOWN도 같이 늘릴 것 (영구 규칙).**
- 검증: 검수앱 파서 단위테스트 24문항(신규 18+회귀 6, 단독 '다' 오인 방지 2 포함)·콘앱 실렌더 13문항·V7.91 음성 복원 회귀 6케이스 — 전부 PASS.

## 7.45 V7.91 콘앱 음성 질문 자동 복원 + build.sh 라벨 누적 수정 (2026-06-11)
- **콘앱 질문기에 검수앱 V7.80(7.38) 동일 이식** — 7.38의 "다음 후보" 완결. 원칙 동일: **AI는 답하지 않는다 — 질문만 복원**(coneFixQuestionWithAI, temperature 0·60토큰·4초 타임아웃), 답은 복원 문장을 coneQaAnswer(작업표 읽기)에 재투입 — 환각 차단.
- **트리거**: 음성 입력 한정 + (도움말 답변 ∨ 미해석 한글 2자+ 토큰 잔존 — coneQaLeftover, CONE_QA_KNOWN 어휘 제거 후). 부분 인식("5번 베이 잇퍼콘")도 복원 — 7.38 교훈 동일. 숫자만·4자 미만 제외, 직전 문장 1회만(fixTried), 실패·오프라인이면 조용히 원래 답. "🎙 문장 복원 중…" 표시.
- **Gemini 키**: 검수앱과 같은 키 내장 + 같은 localStorage 키('master_gemini_api_key_v1') 우선 — 같은 도메인이라 검수원 본인 키가 콘앱에도 자동 적용.
- **build.sh 버그 fix**: cone.html 라벨 sed 패턴 `V[0-9.]*`가 하이픈 미포함 — 빌드 번호(-07) 도입 후 빌드마다 라벨이 누적(`V7.90-07-06-05…`). `V[0-9.-]*`로 수정 — 오염된 기존 라벨도 다음 빌드에서 자가 치유 확인.
- 검증: puppeteer 실렌더(SR·fetch 모킹) 6케이스 — 정상 즉답(AI 미호출)/오인식 복원→데이터 답/복원 불가 폴백/오프라인 폴백/같은 문장 1회/페어 베이 회귀 없음 — 6/6 PASS. (STT 자체는 헤드리스 불가 — 7.31 방식으로 복원 경로만 검증.)

## 7.44 V7.90-07 브리핑 재구성 (사용자 피드백 — 7.41 정정)
- **문제**: ① 통과화물 포함 전체 집계(7.1 위반 — SearchPanel allContainers는 EDI 전체) ② 일반 통계 나열 — 브리핑의 본질은 "일반적이지 않은 사항의 인지".
- **재구성**: generateBriefing(containers, modeLabel, mode) — 내부에서 평택분(모드별 isPyeongtaekPort)과 통과화물 분리. 구성: 음성 첫 줄(평택 N대 — 주의 M건(핵심 키워드), 잔여) / 📌 작업 한 줄(F/E·규격·베이범위·갑판/홀드) / 📈 진행 / **⚠ 주의사항(행동 지향)**: 리퍼 온도 미입력→"조회 시 입력", 위험물 클래스·베이→"별도 취급", X-RAY(양하만)→"양하 후 별도 처리", FR/OT/OOG→치수·고박, 실번호 의심 요지, **통과화물 작업 베이 혼재→"내리지 말 것"**(양하)/"자리 주의"(선적). 특이사항 없으면 "✅ 특이사항 없음" 한 줄.
- 원칙: **브리핑 집계는 평택분, 통과화물은 숫자가 아니라 주의사항으로.** 일반 통계는 한 줄 압축 — 길게 나열하지 말 것.

## 7.43 V7.90-06 실번호 스왑 즉시 경고 (7.42 보강 — 사용자 요청)
- **시나리오**: 두 컨의 실번호가 리스트에서 서로 바뀜 → 한쪽을 실물대로 수정하면 다른 컨과 중복 발생 — 수정 시점이 스왑이 드러나는 유일한 순간(리스트만으론 둘 다 유효해 사전 탐지 불가).
- **구현**: ContainerDetailModal `checkSealSwap(val)` — 입력값(정규화)이 다른 컨의 sl/eseal/reseal과 일치하면 confirm 경고(상대 컨 끝4·풀번호 명시, "서로 바뀜 가능성 — 상대도 실물 확인"). **기록은 막지 않음**(실물이 진실) — 취소/진행 선택. 적용 3지점: handleSaveSeal(풀씰)·handleSaveEsealWrong(실오류 보고)·handleSaveReseal(리씰).
- auditSeals 중복 사유에 "서로 바뀌어 있을 가능성, 양쪽 모두 실물 확인" 보강.
- 검증: 모달 단독 마운트(simtmp 방식, allContainers 2컨) — A 풀씰에 B 실번호 입력 → 경고 PASS.

## 7.42 V7.90-05 실번호 오류 사전 점검 (사용자 요청 — 브리핑 "주의할 점")
- **auditSeals(containers)** (nlSearch export): 풀씰(sl)·엠티실(eseal) 전수 — ① 중복(정규화 후 같은 씰이 2+ 컨) ② 컨번호 혼입(ISO 형식 `^[A-Z]{4}\d{6,7}$` / 자기 컨번호 숫자부 포함 / 타 컨번호 포함) ③ 특수문자·4자 미만 ④ 자리수 부족(같은 영문 접두 그룹의 최빈 길이 대비 짧음 — 표본 ≥3 + 최빈 ≥70%일 때만 판단, 과잉 경고 방지).
- **브리핑 통합**: 주의 있으면 "🔍 주의할 점" 섹션(최대 12건+안내) + 음성 첫 줄에 "실번호 주의 N건". **단독 질문**: "실번호/씰/실 + 점검/검사/오류/확인/체크" → 전체 목록, 이상 없으면 ✅.
- 정규화: 대문자 + 공백·하이픈 제거 후 비교. 검증: 4종+정상 합성 — 6/6 검출·오탐 0.

## 7.41 V7.90-04 작업 브리핑 (사용자 요청)
- **트리거**: "브리핑/브리핑 해/요약 해/작업 요약" (briefingQuery) — 현재 작업 탭(양하/선적) 컨테이너만 대상.
- **구성**: 첫 줄 = 음성용 한 문장("양하 114대, 리퍼 8, 위험물 2, 엑스레이 2, 잔여 74") / 상세 = 전체(F/E·규격) · 작업 범위(베이 min~max, 갑판/홀드) · 진행률 · 리퍼(베이·온도 분포·온도미입력 경고 — Full만) · 위험물(클래스) · X-RAY(끝4) · FR/OT/TK. `generateBriefing(containers, modeLabel)` (nlSearch export).
- ⚠ 리퍼 판정은 **isReeferContainer 단일 함수**(V7.27 ⑦) — 브리핑 초안의 자체 정규식 `/R[FT1-9]?$/`이 40FR 라벨의 끝 R에 매칭돼 FR을 리퍼로 오인(검증에서 발견). 새 코드에서 리퍼·특수 판정을 직접 짜지 말 것.

## 7.40 V7.90-02/-03 베이 분포 질문 + 상세 확장 (사용자 현장 제보·요청)
- **-02**: "리퍼가 몇 번 베이에 있어?"가 ⚠56개 일치(카드 나열), "어디 어디에 있어?"가 "총 56대"(개별 56줄의 첫 줄)로만 — 베이 분포 질문 유형 신설. parseNaturalQuery `bayDistQuery`(/몇 번 베이|어느 베이|무슨 베이|어디 어디|베이별/) + posQuery인데 결과 >5면 분포로 라우팅. 첫 줄이 음성으로 읽히므로 **첫 줄 = "리퍼 56대 — 베이 3, 7, 20"**(베이 ≤8 나열, 많으면 개수).
- **-03 (상세 확장)**: 베이별 줄에 검수 실무 정보 — 공통: 갑판/홀드·규격(20/40/45, isoToLabel 기준 — V7.53 ISO 원칙). 리퍼: 온도 분포(-18°C×6, '미입력' 명시 — 온도 미입력 점검 겸용). XRAY/DG/FR/OT/TK: 컨번호 끝4 나열(베이당 ≤6). DG: 클래스 분포. formatBayDist(desc, results, parsed).
- 게이트 3곳(hasAnyCondition·SearchPanel localAnswer 2곳)에 bayDistQuery 포함 필수 — 빠지면 답변 자체가 안 나옴.

## 7.39 V7.90 완료 분리 + 조회 오염 해결 + 콘앱 자동 분리 (사용자 설계)
- **완료 분리**: info.inspectorDone(단일) → `dischargeDone/dischargeDoneAt`, `loadingDone/loadingDoneAt` 모드별. VoyageCard에 [⬇ 양하 완료]/[⬆ 선적 완료] 버튼(보유 모드만 표시, ✓ 상태에서 누르면 취소). **보유 모드 전부 완료 = 수석 대기** (구 inspectorDone 데이터는 하위호환으로 계속 인정 — isAllDone/수석 판정 양쪽). 수석 대시보드 진행 줄에 모드별 완료 시각 표시(작업시간 구분).
- **콘앱 자동 분리**: fbFetchVoyages가 dischargeDoneAt(구 inspectorDoneAt 폴백) 수집 → selectVoyage가 state._dischDoneAt 보관 → runCalc에서 양·선 둘 다 로드 + 완료 기록 있으면 **확인창 없이 자동 분리**(기록 시각 안내 카드). 날짜 기반 confirm(7.37)은 완료 기록 없을 때 폴백. — "완료 보고만 했지 실질 완료가 아니던" 문제를 완료 기록이 실제 동작(콘 계산)을 바꾸는 구조로 전환.
- **조회 오염 해결 (V7.53 보완)**: SingleSearch 결과를 main(현재 모드·미완료)과 others(반대 모드+완료분)로 분리 — main만 기본 표시, others는 "다른 작업·완료분에 N건 — 보기" 접이식. main 1건이면 BigResultCard 정상 작동(중복으로 작은 카드 2개 뜨던 문제 해소). V7.53의 "있는 자료를 못 찾는 문제"로는 회귀하지 않음(접혀 있을 뿐 찾을 수 있음). 통계·localAnswer는 전체 기준 유지.
- ⚠️ 검증 교훈: node_modules 없는 상태에서 `esbuild | grep -c "✘"`는 0을 출력해 컴파일 통과처럼 보임 — **컴파일 체크 전 esbuild 실존 확인 필수** (이번 세션에서 가짜 통과 발생, 재검증으로 수습).

## 7.38 V7.80 음성 질문 자동 복원 — AI 질문 번역기 (사용자 확정 아키텍처)
- **원칙: AI는 답하지 않는다 — 질문만 복원한다.** Gemini의 "거짓/모름"은 모델 탓이 아니라 데이터 없이 답을 시킨 역할 설계 탓. AI에게는 음성 오인식 문장의 교정만 시키고(fixQuestionWithAI — temperature 0, 60토큰, 4초 타임아웃), 답은 복원 문장을 로컬 파서(nlSearch)에 다시 넣어 데이터로 계산 — 환각 원천 차단. (콘 질문기 7.30 "계산하지 않는다"와 같은 계열 원칙.)
- **트리거 (핵심)**: 음성 입력(voiceQueryRef) + **미해석 단어가 남았을 때** — 완전 실패(!hasAnyCondition)만 잡으면 "20번 베이 잇퍼 몇대야"가 베이만 잡혀 전체 개수를 답하는 사용자 증상을 못 막음. KNOWN 어휘(파서 어휘+조사+의문어) 제거 후 한글 2자+ 토큰이 남으면 부분 인식도 복원. 같은 문장 1회만(fixTriedRef)·숫자(끝4자리) 제외·실패 시 조용히 기존 흐름(기록 버튼).
- **사용자 동작 불변**: 마이크 한 번 — 복원은 내부 자동(1초 내외, "🎙 문장 복원 중" 표시). 오프라인이면 건너뜀.
- **음성 답변 간결화 (사용자 확정 형식)**: 핵심 한 문장 — "20번 베이 리퍼 10대" / 0대면 "{대상} 없습니다". 상세는 화면에만. (구) 두 줄 낭독 폐지.
- 검증: SearchPanel 단독 마운트 + SpeechRecognition/fetch 모킹 — "잇퍼" 음성 → AI 1회 → "리퍼" 복원 → 즉답 PASS. ⚠ 진단 교훈: 텍스트 직접 입력 16문항은 모두 정답이었음 — "질문을 못 알아듣는다"의 병목은 엔진이 아니라 STT. 증상 재현은 반드시 입력 경로(음성)까지 포함할 것.
- 다음 후보: 콘앱 질문기에도 동일 복원(콘앱은 Gemini 키 인프라 추가 필요).

## 7.37 V7.70 콘앱 양하/선적 분리 작업 자동 분별 — ATPR형 (도메인 원칙 신설)
- **도메인 (사용자 발견)**: 일부 선박(ATPR 등)은 양하와 선적이 **분리 작업** — 양하를 먼저 완료해 콘을 전량 콘박스에 반납한 뒤, 하루 이상 지나 선적. 이 경우 "추가필요 = 선적 소요 − 양하 회수" 상계가 틀림 — 회수분은 이미 배에 없으므로 **선적 콘은 전량 콘박스에서**.
- **자동 분별 + 확인 (사용자 확정 설계)**: 양하·선적 둘 다 로드 + 작업일 차이 ≥ 24h → confirm "양하가 이미 완료되어 콘을 전부 반납했습니까?" — 확인=분리(dg 무시, have 0, 동선표 자연 소멸), 취소=연속(현행 상계). 같은 날이면 묻지 않음(야간 연속작업 오판 방지로 24h 기준). 같은 데이터로 재계산 시 답 기억(_sepAnsKey). 작업표 상단에 판정 안내 카드 표시.
- **작업일 출처**: EDI 작성일 — `extractEdiDocDate`: DTM+137(YYMMDDHHMM) 우선, 폴백 UNB 날짜. parseEDI·parsePositions(비표준) 공용. 검수앱 연동의 파싱 데이터(ediContainers) 경로엔 날짜가 없어 raw EDI를 날짜용으로 자동 보충 fetch.
- 검증: ATPR_017 실 선적(작성 6/10 10:55) + 합성 양하(6/8) — 분리 수락(19번 have 0·전량 추가) / 연속(상계 14) / 같은 날(확인창 없음·현행) 3케이스 PASS.

## 7.36 V7.61 콘앱 홀드콘 ×2 버그 — ATPR 실데이터로 확정 (메모 ③ 완결)
- **진단 (ATPR_017 선적 실 EDI, ATLANTIC PIONEER 445대)**: 19번 홀드 24대(전부 20ft, tier 02~08)·받침 통과 18대 → 콘 18 정답. 작업표는 36 — (20)21도 16→32, 23도 10→20, **전부 정확히 2배**. 그룹 계산(ediToBayGroups)은 정확 — renderResults 작업표 변환의 `g.stow.hold*2`가 범인. "트윈 홀드 두 베이 합산"(4.2: 23콘6+25콘6=12) 규칙을 ×2로 오역 — 두 베이 합산은 각자 센 값의 합이지 자기 값의 2배가 아님.
- 수정: ×2 제거(need/have/diff 모두 원값). 베이뷰 coneByGroup·질문기(perOf.hold=1)·동선표(computeRoutes per=1)는 원값 기반이라 자동 정합 — **작업표만 2배라 베이뷰와 수치가 달랐던 것이 "같이 계산됨"으로 인지된 증상.**
- 검증: ATPR 실 EDI 재계산 — 19번 18 / (20)21번 16 / 23번 10 PASS.
- 교훈: 진단은 "순수 함수 단위 추적(그룹값) → 화면값 비교"로 2배 지점을 좁힘. 실데이터가 오니 10분에 끝남 — 추측 수정 안 한 것(0-10)이 옳았음.

## 7.35 V7.60 메모 2건 수정 — 트윈 중복 선택 + 콘앱 카고플랜 로드 (2026-06-11)
- **① 트윈 끝4자리 중복 시 선택 안 됨 (메모)**: TwinSearch useEffect가 `r1.length > 1`이면 무조건 setC1(null) — 중복 후보 버튼으로 골라도 다음 렌더에서 즉시 무효화. 수정: 고른 c1이 r1 후보 안에 있으면 유지. 검증: SearchPanel 단독 마운트(simtmp/twin_test.entry.jsx — bayplan_test 방식, 선적 탭 클릭 후 트윈) 실렌더 PASS.
  - ⚠️ 잠재 이슈 기록: TwinSearch는 filteredContainers(현재 탭·미완료만)를 받음 — 작업 흐름상 의도지만, 완료 컨이 후보에서 빠지므로 "앞 완료 후 재조회" 시 짝꿍이 안 보일 수 있음. 증상 제보 시 V7.53 SingleSearch 방식(전체+정렬) 검토.
- **② 콘앱 카고플랜 로드 실패 — QR·기기별 (메모)**: 앱은 실행되는데 cone-cargoplan.js(1.6MB) 동적 로드가 누구는 되고 누구는 안 됨. 3중 방어:
  - build.sh 번들에 `--target=es2017` (구형 웹뷰 파싱 호환)
  - loadCargoPlanV2: 실패 시 1.2초 후 1회 자동 재시도 + 원인 구분 안내(네트워크 → "신호가 약할 수 있어요" / onload 후 window.ConeCargoPlan 부재=파싱 실패 → "크롬 최신으로 열어 주세요")
  - sw.js install에 cone-cargoplan.js precache (실패해도 설치 안 막음) — 검수앱 PWA 사용자는 약신호에서도 즉시 로드. QR 단독 사용자는 SW가 없어 재시도가 방어.
- **③ ATRP 홀드 19/(20)21 같이 계산 (메모 — 미해결 대기)**: groupKeyOf상 19는 18 또는 단독만 가능해 코드 경로 특정 불가 — 추측 수정 금지(0-10). **ATRP 실 EDI 확보 후 재현·수정.** 의심 지점: (a) 작업표 그룹 규칙이 "짝수+다음홀수"만 묶음(앞홀수 19 제외) vs ATRP 실구조 (b) V7.59 existBays 확장(사전+전체화물)의 그룹 변화.

## 7.34 V7.59 콘앱 콘 계산 정밀화 3건 (사용자 제보 — 정확도는 생명)
- **① 받침 ±1 = 40ft만**: 짝꿍 베이(±1) 밑 받침은 그 컨이 40ft일 때만 인정(두 베이에 걸쳐 실리는 물리). 옆 베이 20ft는 다른 슬롯 — 받침 오인으로 바닥 빈 컨에 콘이 계산되던 버그. (sizeAt 맵으로 사이즈 인식.)
- **② 전용 데크(20ft 데크콘4) 판정 = 구조 기준**: bayHasHold·이웃 베이 존재를 평택분 화물 유무로 보던 것을 **베이사전(ensureConeBayDict — 베이뷰와 공용·캐시) ∪ 양·선 전체화물(rowsAll)**로. 화물 유무로 구조를 판정하는 V7.33 패턴 재발 — **"구조 판정은 구조 데이터로"를 콘 계산에도 적용.**
- **③ 받침 = 자기 plan 전체화물(ediRowsAll)**: 받침은 우리 화물이든 잔류분(통과화물)이든 성립. 평택분만 보면 통과화물 받침 컨의 콘 누락. (반대 plan은 받침 아님 — 양하 후 선적 시간순.)
- runCalc는 async화(사전 await 후 계산, 실패 시 폴백). buildConeCtx(dictBays) — bays/holdBays/slots 모두 rowsAll+사전 기준.
- 검증: 3케이스 합성 EDI(통과 홀드·옆 20ft·옆 40ft·통과 받침) 재현 — 일반베이 판정/받침 불인정/정당 인정 전부 PASS.

## 7.33 V7.58 카고플랜 해치커버 수평 정렬 (사용자 확정 — 화면 약속)
- **원칙**: 홀드가 있는 베이들은 **해치커버 선이 같은 수평선** — 데크는 아래(82)가 해치선에 붙고(단수 부족분 위 spacer), 홀드는 위가 해치선에 붙음(부족분 아래 spacer). deck-only 베이는 해치선이 없어 제외.
- 구현: PrintableCargoPlanV2 — globalHatch(홀드 보유 베이들의 maxDeck/maxHold) 계산, 모든 해치 보유 박스의 deck:hold 영역 flex를 maxDeck:maxHold로 통일 + 내부 grid는 자기 단수 flex + spacer. 기존 globalMaxTier 말단 spacer는 deck-only에만 유지.
- 검증: KSKM 실사전(데크 4·5·6단 혼재) — 같은 행 해치선 y 전부 일치, 짧은 데크 위 spacer·82 해치선 부착 렌더 PASS.

## 7.32 V7.57 카고플랜 해치커버 등분 복구 (사용자 라인 수정 — 기록)
- V7.05 원복 때 computeBayRenderData return의 hatchCount 누락 → 카고플랜 해치 등분이 항상 1로 폴백. return에 hatchCount 복구. (사용자가 별도 채팅에서 수정한 것을 V7.57 ZIP으로 전달 — 0-11 버전 대조로 이 라인에 승계.)

## 7.31 V7.56 음성인식 항만 용어 보정 (검수앱+콘앱)
- **증상**: "양하"를 6번 말해야 인식 — 크롬 STT(일반 대화 모델)가 항만 용어를 일반어(양아/양화/향하)로 받아 적음. STT 모델 자체는 바꿀 수 없음 — 후처리 보정이 정답.
- **수정 2축**: ① maxAlternatives=5 — 후보 전부에서 항만 용어 점수 최고 후보 채택(첫 후보만 쓰던 것 폐지) ② 오인식 교정 사전(`voice.js fixSpeechDomain` / 콘앱 coneFixSpeech 동일 내장) — 양아·양화·향하→양하, 성적→선적, N번 배→N번 베이, 홀두콘→홀드콘, 댁콘→데크콘, 니퍼→리퍼, 갑반→갑판 등 **보수적 치환만**(일반 대화어 오염 금지 — "풀" 같은 일반어는 치환 안 함).
- **운영**: 현장에서 계속 오인식되는 단어는 🎙 표시에 찍힌 잘못된 글자를 그대로 수집 → 사전에 추가. (STT 검증은 헤드리스 불가 — 교정 함수 단위테스트로 대신.)

## 7.30 V7.55 콘앱 질문기 — 콘 작업 전용 (사용자 확정 범위)
- **범위 확정**: 콘 작업에 관한 것만 — "이 베이는 어떤 콘이 몇 개 남는다/모자란다". 검수앱 질문 엔진(nlSearch) 통째 이식은 과잉이라 폐기, **작업표(rows)를 읽는 초경량 내장 파서**로.
- 지원: 베이별 콘 가감("5번 베이"/"7번 베이 홀드콘") / 모자란·남는 베이 목록 / 전체·종류별 가감 / 베이별 컨테이너 대수. 🎤 음성 입력(SpeechRecognition) + 음성 답변(speechSynthesis, +/− 를 플러스/마이너스로 읽음).
- **원칙: 질문기는 계산하지 않는다 — 작업표 수치(window.__coneQA.rows)를 읽기만.** 계산과 답이 항상 일치, 받침 규칙 등 도메인 로직 자동 반영, 회귀 위험 0.
- 베이 매칭: 페어 라벨("06-07")을 [,\-]로 split해 구성 베이 번호와 비교 — 6으로 물어도 7로 물어도 같은 행.
- X-RAY·실번호·진행률은 콘앱 데이터에 없어 미지원(검수앱 전용) — 질문기 안내 문구로 유도.

## 7.29 V7.54 질문 카탈로그 + 미응답 기록 — 파서 확장 백로그
- 매뉴얼 [AI 질문] 탭 전면 개편: **전수 테스트로 검증된 지원 질문만** 카탈로그로 (찾기/개수·통계/진행·기타). Gemini는 보조로 격하(즉답 우선 명시).
- **미응답 질문 기록**: 인식 실패 또는 일치 0이면 [📌 이 질문을 기록] 버튼 → fbReportWrongAnswer(answerType:'unanswered'). 수집된 질문이 파서 확장의 재료.
- **미지원 백로그 (V7.54 테스트 20문항 — X 10·△ 2)** — 우선순위는 현장 기록 수집 후 결정:
  | 질문 패턴 | 상태 | 필요 작업 |
  |---|---|---|
  | 선사별 몇대 / TJM 몇대 / 동진 컨테이너 | X·△(오답 위험) | parsed.op 신설 + normalizeCarrierCode 매칭 + 선사별 집계 |
  | 온도 미입력 리퍼 / 씰 미입력 / 실번호 없는거 | △(조건 무시) | "미입력/없는/안 찍힌" 부정 필터 (tmp·sl 빈 값) |
  | 시프팅 몇대 | X | shiftingMap 연동 |
  | 통과화물 몇대 | X | 평택 아닌 분(비PTK) 필터 |
  | 제일 무거운 컨 / 가장 큰 | X | 최대/최소 정렬 질의 |
  | 오늘 몇대 했어 | X | completed.at 날짜 필터 |
  | 첫번째 베이 / 두번째 | X | 서수 → 베이 번호 매핑 |
  | 위험물 클래스 N 집계 | △ | c.dgClass/imdg 데이터 채움 확인 후 |
- ⚠️ 오답 위험 2종(선사명·미입력)은 매뉴얼에 경고 표기함 — 지원 전까지 잘못된 답이 나갈 수 있는 패턴.

## 7.28 V7.53 검색/질문(챗봇) 수리 — A방식(규칙 기반) 확정
- **사용자 확정**: 챗봇은 A방식 — AI 없이 있는 데이터로 정확히 답하면 만족. 기존 기능이 "있는 자료도 못 알려주던" 원인 진단(16문항 직접 테스트).
- **진단 결과**: nlSearch 엔진(파서·필터·답변)은 건강. 실결함 2건만 존재:
  - ① **탭 격리**: SearchPanel이 filteredContainers(현재 작업 탭·미완료만)를 검색에 넘김 → 선적·완료 컨테이너가 "없습니다". 수정: 전체 allContainers 대상 + 현재 모드 우선 정렬(rank: 현재모드 미완료 0 → 다른 모드 1 → 완료 2) + 기존 양하/선적/완료 뱃지.
  - ② **45ft 오인**: `/^45/` 매칭 — ISO 45xx는 45피트가 아니라 **40ft 하이큐브**(45GP→40HC, 45R1→40RF). 진짜 45ft는 L5xx. 수정: 45='^L5'∨라벨 45, 40='^4' 전체.
- **ISO 규격 원칙(재확인)**: 첫 자리=길이(2=20, 4=40, L=45), 둘째 자리=높이(2=8'6", 5=9'6" HC). 길이 판정은 라벨(isoToLabel) 기준이 정답.
- **검증법**: nlSearch.js는 순수 모듈 — esbuild cjs 번들 후 node 단위테스트(simtmp/qa_test.cjs 방식, 합성 컨테이너 + 1부 워크플로 질문 16+개). UI 거치지 않고 파서~답변 전체 검증 가능.
- 다음: 현장 실제 질문 10~20개 수집(사용자) → 로컬 파서 패턴 확장. Gemini 자유 질문은 유지하되 로컬 즉답 항상 우선.

## 7.27 V7.52 베이 간 정렬 — 검수앱+콘앱 동시 (자리 표시 4원칙째)
- **증상 (TMPZ 실화면, 사용자 제보)**: BAY 01(deck-only, 4칸 04 02 01 03)의 04가 BAY (02)03(6칸)의 06 위치 위에 그려짐 — 베이 박스마다 좌측 정렬이라 위아래 베이 간 같은 row가 어긋남. **홀드 없는 베이는 데크↔홀드 중앙 보정조차 안 거쳐 특히 어긋남 — 클로드 반복 실수 지점(사용자 힌트).**
- **원칙 ④ 베이 간 정렬**: 기준 폭 = **전체 베이 최대 폭**. 모든 베이를 그 기준 가운데(0.5칸 단위 margin/offset)에 배치 — 베이 내부(데크↔홀드)와 베이 간(위아래 박스)을 같은 공식(4.4)으로. 같은 row는 어느 베이에서나 같은 세로선.
- **검수앱(BayPlan.jsx)**: globalGridCols 신설(사전 전 베이 max(rowCount·deckCells·holdCells) ∨ EDI globalRowRange 폭) — dictBaysSummary 선언 **뒤**에 (앞에 두면 TDZ 에러). pageCoordLayout의 nCols 기준을 globalGridCols로 확장, BayPage prop 전달 4곳.
- **콘앱(cone.html)**: 골격 계산을 computeSkel로 추출해 2패스 — 1패스에서 전 베이 골격·globalMaxW 수집, 2패스 렌더에서 centerMargin 기준을 globalMaxW로.
- 검증: TMPZ 재현(BAY 01 deck-only 4칸 + BAY 03 6칸+홀드) — 콘앱 04 열 x=115 3그리드 일치, 본체 04 라벨 x=76 일치, BAY 01의 02·01이 BAY 03과 일직선 렌더 PASS.
- ⚠️ 본체 검증법(영구): BayPlan을 esbuild 단독 번들 + 합성 containers 마운트(simtmp/bayplan_test.entry.jsx 방식) — Firebase 의존 있어도 마운트 가능, tailwind CDN 필요.

## 7.26 V7.51 콘앱 베이플랜 — 자리 표시 3원칙 (사용자 2회 정정으로 확정 — 영구 원칙)
V7.49(단 생략)·V7.51 1차(투명 공백)가 연속으로 오답 — 사용자가 검수앱 화면 비교(TMPZ)로 정정. **베이 그리기 영구 원칙으로 명문화.**
- **① 자리는 항상 표시**: 컨테이너가 없다고 자리가 없는 것이 아니다. 빈 슬롯=점선, 빈 단(tier)도 통째 표시. 어느 한 곳도 그냥 지나치지 않는다. 반대 작업 슬롯(off)도 보이는 점선(rgba(73,93,120,.55) — 안 보이는 #232f3e 금지).
- **② 가상 칸 금지**: 데크 8·홀드 7이면 홀드는 7칸 그대로. 가상 칸·투명 공백(bv-void)·null 칸 삽입 일절 금지 — "없는 건 없어야 한다. 8과 7은 서로 다르다." 정렬은 좁은 쪽 테이블 좌우를 **0.5칸 단위 margin**으로 보정(4.4 "1칸 차이 → 좌우 0.5칸씩"의 베이플랜 적용, 피치 60px → 0.5칸=30px). row 번호 열 좌표 매핑(union) 방식은 00 유무가 다른 구역 간 물리 위치(반 칸 어긋남)를 표현 못 해 오답.
- **③ 폴백 연속 보간**: 사전 없는 선박의 EDI 폴백 골격은 보인 범위 내 연속 보간 — row 짝수 max→2·(00)·홀수 1→max, tier는 구역 시작(데크 82·홀드 02)부터 max까지 2씩. 컨테이너 있는 칸만 수집하면 빈 row·단이 골격에서 통째 빠짐(① 위반).
- 검증: 데크8(00無·빈 84단 포함)/홀드7(00有) — 풀 골격 점선 + 홀드 margin 30px + 두 테이블 중심 일치(284px) DOM·렌더 PASS.
- **교훈: 정렬 방식을 세 번 갈아엎었다(칸 삽입→margin→union 좌표→0.5칸 margin). 처음부터 지침 4.4와 검수앱 실화면(TMPZ)을 대조했으면 한 번에 끝났다. 화면 약속은 본체 화면이 정답지다.**

## 7.25 V7.50 콘앱 베이플랜 — phantom 00 제거 + 카고플랜식 정중앙 (6부 원칙 재확인)
- **사용자 지적**: 강제 삽입된 00 row(컨테이너 불가 자리) — "초창기 버전 클로드가 실수한 방법". 홀드/데크를 강제로 맞추지 말고 **카고플랜처럼 동적으로 분리**돼야 함. V7.48 후에도 좌우 정렬이 카고플랜만큼 안 됨.
- **원인 2건**: ① bvBaySkeleton이 공통 hasZero를 데크·홀드 양쪽에 적용 + EDI 폴백은 `deckRowPos=holdRowPos=합집합` — 6부 "축 통합 시 phantom 00" 위반 재발 ② centerAlign이 정수 칸 단위(null 칸 삽입) — 홀수 칸 차이면 0.5칸 어긋남.
- **수정 (카고플랜 V2 방식)**: 데크/홀드 row 완전 분리 — 사전 deckHasZero/holdHasZero(없으면 hz→hasZero 폴백) 각자 적용, EDI 폴백도 데크/홀드 row 따로 수집. 칸 삽입(null·bv-empty) 폐지. **정렬 = 좁은 쪽 `<table>` 전체 margin-left = (maxCols−cols)×피치60px÷2** — 0.5칸(30px)도 정확, 두 테이블 중심좌표 일치.
- 검증: 데크9(00有)/홀드6(00無) 재현 — 홀드에 00 미생성 + 데크·홀드 중심 314px 일치 DOM PASS + 렌더 확인.
- **교훈: 6부 "데크/홀드 분리 관리" 원칙은 본체만이 아니라 콘앱 등 모든 화면에 적용. 같은 실수가 앱을 바꿔 재발한다.**

## 7.24 V7.49 콘앱 베이플랜 — 진짜 "빈 화면" 제거 (V7.48 해석 정정)
- **V7.48의 오해**: "빈화면"을 점선 격자로 해석해 구역 단위 압축만 했음. 사용자 의도 = **격자도 없이 공백으로 비는 영역**.
- **진짜 원인**: cells는 양하+선적 합집합 슬롯 — 반대 작업 전용 단(tier)의 슬롯이 이쪽 패널에서 `bv-slot off`(border #232f3e — 배경과 거의 동일색)로 그려져, 사실상 안 보이는 행들이 영역만 차지.
- **수정**: 패널별 tier 필터(tiersWith) — 그 작업의 컨테이너가 있는 단만 그림. 행 생략해도 tier 라벨이 남아 위치 식별 유지. 구역 전체 0이면 기존 "화물 없음" 한 줄.
- 검증: 갑판 92~86=선적 전용·84~82=양하 비대칭 — 양하 패널 [84,82]·선적 패널 [92,90,88,86] DOM PASS + 렌더 확인.
- **교훈: 사용자의 시각 증상 단어("빈화면")는 코드 용어로 번역하기 전에 재현 화면으로 확인할 것.** off 스타일처럼 "그려지지만 안 보이는" 요소는 점선/격자 어휘에 안 잡힌다.

## 7.23 V7.48 콘앱 베이플랜 — 홀드 쏠림 fix + 빈 구역 압축
- **홀드 쏠림**: 데크/홀드가 별도 `<table>`인데 `.bv-tab.box td.bv-rl{width:auto}`라 row 라벨 행이 열폭을 흔들어 두 테이블의 같은 열이 어긋남(내용 폭 따라 베이마다 달라 "일부만" 쏠림). 수정: `.bv-tab.box{table-layout:fixed; width:max-content}` + 모든 셀·라벨 58px 고정(max-width 포함), tier 라벨만 18px. **교훈: 별도 테이블 두 개를 세로로 정렬하려면 table-layout:fixed + 전 칸 고정폭이 필수.**
- **빈 구역 압축**: 그 작업(양하/선적)의 컨테이너가 0인 구역(갑판/선창)은 점선 격자를 통째로 그리지 않고 "갑판 · 화물 없음" 한 줄(hasInSec 판정). 홀드 전용 베이의 휑한 갑판 제거.
- 검증: 데크 9행·홀드 7행 폭 차이 베이 + 양하=홀드만/선적=데크만 비대칭 베이 재현 렌더 — 열 일직선·압축 동작 PASS.

## 7.22 V7.47 콘앱 작업 목록 = 검수앱과 1:1 (숨김 필터 제거)
- 증상: 검수앱 작업선박 목록에는 있는 항차가 콘앱 작업 선택에 안 보임.
- 원인 2건: ① 콘앱 fbFetchVoyages에만 **3일 필터**(createdAt 3일 초과 숨김) — 검수앱은 7일 자동삭제로 관리하므로 불일치 발생 ② **EDI 없는 항차는 목록 제외** — 콘 계산 불가라도 숨기면 사용자는 누락으로 인식.
- 수정: 3일 필터 제거(살아있는 항차 전부) + EDI 없는 항차는 noEdi 플래그로 회색 표시·"EDI 없음" 태그·선택 시 업로드 안내.
- **원칙: 콘앱 목록은 검수앱 voyages와 1:1. 콘앱이 따로 숨기는 조건을 두지 말 것** (숨김은 검수앱의 완료/자동삭제가 담당).

## 7.20 V7.45 콘앱 카고플랜 — 본체급 시각화 (V7.46으로 대체됨 — 기록 보존)
- 진단: 셀 13×13px 점(정보 0) + 기본 줌 50% = 미니어처. 골격(데크/홀드/00/사전 폴백)은 본체 동등이었음 — 셀 표현만 문제.
- 수정: ① 본체 베이그리드급 셀(88×56px, mono 4줄: POL/POD★·컨번호 끝4 강조·규격 F/E·특수) + 특수 좌측 컬러바·우상단 심볼(❄⚠⊞△, 본체 동일 감각) + 평택 amber 테두리/타항 회색 ② parseEDI에 rowsAll 반환 추가, state에 ediRowsAll 보관(3경로) — 카고플랜은 전체 화물을 그리고 평택만 강조(콘 계산 rows는 평택분 그대로) ③ 기본 줌 100%(HTML value + applyZoom(50) 호출 둘 다) ④ 구분행(cvsec)·라벨(cvl) 높이 보정 — td 기본 56px 상속으로 생긴 과대 공백 제거.
- 검증: DOM 카운트(평택 37 amber·타항 2 회색+컨번호·리퍼 심볼 1) + 렌더 PASS.

═══════════════════════════════════════════════════════
9부 — 2026-06-10 세션 종합 (오늘 한 일 + 주의사항)
═══════════════════════════════════════════════════════

## 9.1 오늘 완료 (V7.36 → V7.44)
- **V7.36** .def 단면 디코더 — 329척 중 314척(95.4%) 자동 디코드. 6.10계(베이테이블 144B)·6.50계(188/189 혼재) 파서, JS 포팅(defSectionParser.js), 매트릭스 빌더 ".def 업로드" 버튼.
- **V7.37** .def 내장 사전 289척 — 우선순위 확정: 사용자 매트릭스 > PDF override > .def 사전 > v5 > v2 > EDI. 오염 의심 25척 자동 제외. 3상태 배지(📐확정/🚢.def 검증전/⚠️확정필요).
- **V7.38** 카고플랜 V2 수술 — non-user 분기에서 자동 사전이 override를 가리던 4건(rowMax·hasZero·tiers·cells) 정합 + deck-only 하단배치 규칙 재제거(재발 1호) + 해치선 그리드 실폭.
- **V7.39** tier 라벨 고정 13px 재제거(재발 3호) — flex 균등분배 1:1 정렬.
- **V7.40** 수석 대시보드 — ⚓실시간 작업 보드(선박 카드 그리드: 진행·작업 중 검수원·경고·최근 보고) + 평택분 모드 판정 전수 5곳 수정(7.1 위반) + 죽은 코드 490줄 제거.
- **V7.41** 콘앱 보강 — 결과 모드(안내 자동 접힘+결과 탭바), 에러 한국어화, 작업표 "회수→필요", 베이뷰 끝4자리 강조.
- **V7.42~43** 엠티실 보고서 — 20ft부터 컨번호순+소계+총합, 스타일(전용 loadSheetJSStyled), Invalid Date 방어, 규격 병기 20DC(22GP), 엠티실 ISO 오염 제거·빈칸(수기 공간), 선적위치 컬럼, 시각 압축.
- **V7.44** 콘앱 동선표 콘 종류 배지 + totalSupply 반환 버그.
- **V7.45** 콘앱 카고플랜 본체급 시각화(자체 셀 뷰 — V7.46으로 대체).
- **V7.54** 질문 카탈로그(매뉴얼 AI 질문 탭 개편) + 미응답 질문 기록 버튼 + 파서 확장 백로그(7.29 표).
- **V7.53** 검색/질문 수리 — 탭 격리 해제(전체 자료 검색+우선 정렬)·45ft/40HC 오인 fix. 챗봇 A방식 확정.
- **V7.52** 베이 간 정렬 — 검수앱 BayPlan+콘앱 베이플랜 둘 다, 전체 최대 폭 기준 0.5칸 가운데(④원칙). deck-only 베이 어긋남 해소.
- **V7.51** 콘앱 베이플랜 자리 표시 3원칙 확정 — 빈 자리 전부 점선·가상 칸 금지·0.5칸 margin 정렬·폴백 연속 보간.
- **V7.50** 콘앱 베이플랜 phantom 00 제거(데크/홀드 row 완전 분리) + 카고플랜식 margin 정중앙.
- **V7.49** 콘앱 베이플랜 진짜 빈 화면 제거 — 반대 작업 전용 단(off 슬롯 행) 생략, 패널별 화물 있는 단만 렌더.
- **V7.48** 콘앱 베이플랜 홀드 쏠림 fix(table-layout:fixed·전 칸 고정폭) + 화물 없는 구역 한 줄 압축.
- **V7.47** 콘앱 작업 목록 1:1 — 3일 숨김 필터 제거 + EDI 없는 항차 회색 표시(누락 오인 방지).
- **V7.46** 콘앱 카고플랜 = 검수앱 V2 그대로 — 본체 PrintableCargoPlanV2+core+사전을 React째 번들(cone-cargoplan.js, build.sh 통합), 동적 로드로 100% 동일 화면(A4 가로·베이 구조·별첨·인쇄). 자체 카고뷰 ~350줄 제거.
- **운영 확정**: 0-11 세션 시작 3버전 대조 / 프로젝트 지식 교체는 하루 1회 그날 마지막 판 / 마무리 신호는 사용자가 / 지침서 둘이면 버전 높은 쪽이 진실 / MD는 지침서+README만(8부 흡수 재수행).

## 9.2 주의사항 (다음 세션 필독)
1. **재발 방지가 오늘의 최대 교훈**: 6/9 수정 3건(deck-only·MD 정리·tier 13px)이 별도 ZIP 라인이라 전부 재발했다. 새 세션은 반드시 직전 세션 마지막 ZIP(V7.44)을 베이스로. 시작 시 0-11 버전 대조.
2. **평택 판정은 반드시 모드별**: 양하=POD평택, 선적=POL평택. `isPyeongtaekPort(c.pol) || isPyeongtaekPort(c.pod)` 패턴을 보면 의심하고 전수 grep — 오늘 5곳에서 같은 버그가 복제돼 있었다.
3. **override 우선순위(5.1)는 코드의 non-user 분기에서 깨지기 쉽다**: "userBay" 변수명이 자동값(v2/v5)을 포함하므로 이름만 믿지 말 것. isUserSource 분기는 절대 불변.
4. **함수 블록 제거 시** 인자 디스트럭처링 `{`를 본체 `{`로 오인하면 시그니처만 잘림 — 제거 후 컴파일 확인 필수.
5. **보고서 출력 스타일은 loadSheetJSStyled 전용** — 기존 loadSheetJS(파서용)는 절대 교체 금지.
6. **콘앱 시뮬**: state 주입 시 `{ediRows: parseEDI(txt).rows, isEDI:true}` 형식 필수 (rows 직접 넣으면 크래시). 검증은 비대칭 EDI로 동선표 경로까지.
7. **파이썬 일괄 패치 스크립트는 단계별 저장**: assert 실패 시 앞 패치까지 통째로 유실된다 (오늘 실제 발생 — 패치마다 write).
8. **다음 후보 과제**: PDF override 선박(DJCT)의 홀드 cells 세부(역피라미드)는 사용자 검증 폼으로 보강 / 콘앱 텍스트 복사·이미지 저장에 동선표 포함 여부 / 수석 보드에 부두·XRAY 잔여 추가 여부는 현장 피드백 후.

═══════════════════════════════════════════════════════
부록 C. 메모 수신함 (사용자 메모 — 답변 금지, 기록만)
═══════════════════════════════════════════════════════

> 사용자가 "메모"로 보낸 내용을 날짜와 함께 그대로 적는다. 처리는 사용자가 지시할 때만.

- ~~2026-06-10: 아직도 콘박스 갯수 분석이 제대로 안되면 ATRP 홀드에서 19번베이와 (20)21번베이가 틀린데 같이 계산함~~ → V7.61 수정 완료 (홀드콘 ×2 버그, 7.36)
- ~~2026-06-10: 트윈선적중 끝자리 4자리가 같은게 있을때 선택이 안됨~~ → V7.60 수정 완료
- ~~2026-06-10: 콘앱을 큐알로 사용할때 누구는 되고 누구는 안됨, 무슨 로드실패 뜸~~ → V7.60 수정 완료 (보충: 앱은 실행되나 카고플랜 등 모듈 로드 실패)
