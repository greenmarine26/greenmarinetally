# 선적 작업 메뉴얼 (M5.25)

> 평택항 선박 검수 — 선적 항차 작업 표준 워크플로우

---

## 핵심 원칙

### 양하 vs 선적의 본질적 차이

| 구분 | 양하 (Discharge) | 선적 (Loading) |
|---|---|---|
| EDI 위치의 의미 | **실체** (배에 이미 적재됨) | **계획** (앞으로 적재 예정, 실체 없음) |
| 검수원의 역할 | 실제 화물 검수 | **계획 → 실체 전환** |
| 위치 변경 | 의미 없음 | 빈번 (현장 적치 다름) |
| 엠티 컨 비중 | 거의 없음 | 많음 |

### 선적의 "실체 발생" 시점

```
EDI 계획 (예정) ───→ 컨번호 조회 ───→ [위치 확인 또는 수정] ───→ 선적확인 ───→ 실체
       │                  │                      │                       │              │
   실체 없음           위치 표시            (다른 컨 오면              버튼 누름        그 위치에 컨
                                          수정 위치 입력)                              그려짐 + 진해짐
```

**핵심: 선적확인 누르기 전까지 EDI 위치는 단지 계획. 실제로 거기 컨이 있는 게 아님.**

---

## 작업 흐름

### 1. 선적 항차 생성

1. 메인 → "선적" 모드 선택
2. "+ 새 항차"
3. 선박/항차번호 입력 (TNJP, SWSP 등)
4. 선적 EDI(.EDI) 업로드 → 계획 위치 자동 배치
5. 양하리스트(.xlsx) 업로드 → 풀씰/엠티실 보강
   - 양식 예시: TNJP26334ELIST.xls
   - 헤더: `컨테이너번호` / `Seal No 1` / `엠티실번호` 등 자동 인식
   - **양하리스트의 풀씰은 c.sl, 엠티실은 c.eseal로 자동 분리**

### 2. 베이그리드 화면 — 컨테이너 1개씩 처리

**가장 일반적인 흐름:**

```
A. 베이그리드에서 컨테이너 탭
   → 컨테이너 정보 모달 열림

B. 정보 확인 (컨번호/위치/규격/실번호 등)

C. 선적확인 버튼 (계획대로 정상 적재)
```

**위치가 다르면:**

```
A. 베이그리드에서 컨테이너 탭

B. 모달에서 "실체 위치 (선적확인 시)" 박스 → "수정 위치 입력"

C. 베이/열/단 입력 → 저장
   "01/00/02 → 11/11/11 (성일)" 표시

D. 선적확인 버튼
```

### 3. 위치 영역의 두 박스

**선내 위치 (계획):**
- EDI에서 가져온 계획 위치
- 변경 안 됨 (EDI는 단일 진실)

**실체 위치 (선적확인 시):**
- 계획대로면: "미입력 (계획 위치대로 선적 예정)"
- 수정한 경우: `01/00/02 → 11/11/11 (검수원명)`
- "삭제" 버튼: 수정 위치 지움 → 계획대로 돌아감

### 4. 베이그리드의 자동 반영

위치 수정한 컨테이너는 **즉시 베이그리드에 반영**됩니다:
- 원래 자리 (01/00/02) → 비어 보임
- 새 자리 (11/11/11) → 컨 그려짐
- 검색 화면도 동일하게 새 위치로 표시

### 5. 자리 뺏긴 컨테이너 박스 (자동 표시)

베이 탭 상단에 **노란 박스**가 나타납니다:

```
⚠ 자리 뺏긴 컨테이너 N대
다른 컨이 이 자리로 이동했음 — 새 위치를 결정해 주세요

[CKFU9213883]   F  40HC
01/00/02 → AAAA1 점유

[TBJU7358223]   F  40HC
03/00/02 → BBBB2 점유
```

**상황 설명:**
- 컨 X(예: AAAA1)가 위치를 11/11/11에서 01/00/02로 옮김 (수정)
- 그러면 원래 01/00/02에 계획되어 있던 컨 Y(CKFU9213883)는 자리를 뺏김
- 박스에 자동 표시됨

**처리 (두 가지 경로, M4.9f 신규):**

**A. 빠른 이동 — 카드 우측 [📦 이동] 버튼 (M4.9f, 권장)**
1. 카드 우측 [📦 이동] 버튼 누름 → 카드 노랑 하이라이트(선택중)
2. 화면 상단 큰 노란 안내 바: "본위치 NN/NN/NN → 빈 셀을 누르세요" + [취소]
3. 베이 그리드의 빈 셀이 노랑 테두리 + 📦+ 마크로 활성화 — 그 셀 클릭 → 저장
4. 자동 매칭: 본위치가 짝수(40ft)면 짝수 베이 빈 셀만, 홀수(20ft)면 홀수 빈 셀만 활성화 (잘못된 종류 슬롯 방지)
5. 다른 베이로 옮기려면 페이지 스크롤/점프 — 안내 바 유지됨
6. X마크 셀(다른 컨이 점유)은 비활성 — 안전

**B. 직접 입력 — 카드 본문 클릭 (기존)**
- 카드 본문(왼쪽 큰 영역) 클릭 → 컨테이너 모달 → "수정 위치 입력" → 베이/열/단 수동 입력
- 베이 그리드에서 빈 셀이 안 보이거나 정확한 위치를 알고 있을 때 사용

---

## 자주 하는 질문

### Q1. "선내 위치 (계획)"이랑 "실체 위치"는 왜 둘 다 있나요?

**A.** 선적은 계획에서 시작해 실제 적치로 바뀌는 과정입니다:
- 계획은 EDI 단일 진실 (변경하면 안 됨)
- 실체는 검수원이 결정 (현장 적치 따름)
- 두 값을 분리해 보존하면 보고서에 "계획대로 적재됐는지/얼마나 변경됐는지" 추적 가능

### Q2. 위치 수정한 후 다시 계획대로 돌리려면?

**A.** 모달에서 실체 위치 박스의 "삭제" 버튼 → 미입력으로 돌아갑니다.

### Q3. 양하 항차에서는 왜 "실체 위치" 박스가 안 나오나요?

**A.** 양하 EDI 위치는 이미 실체(배에 적재된 위치)이므로 변경 의미 없음. 양하는 그대로 검수만 진행.

### Q4. 자리 뺏긴 컨테이너 박스가 안 보여요.

**A.** 다음 조건일 때만 표시됩니다:
- 선적 모드 (양하는 표시 안 됨)
- 누군가의 actual 위치가 다른 컨의 계획 위치와 일치
- 그 다른 컨은 아직 actual 위치를 결정 안 함

위치 수정한 컨이 0대면 박스도 0대.

### Q5. 베이상세 인쇄가 좌우 짤려요.

**A.** M4.9e부터 화면 BayPlan과 동일하게 베이마다 컬럼 수가 동적입니다 (`globalRowRange.maxLeft/maxRight` 기반). 큰 베이는 11컬럼, 작은 베이는 7컬럼. 폰 인쇄 미리보기에서 짤리면 인쇄 옵션의 "여백" 또는 "맞춤"을 확인하세요.

### Q6. "검수 완료" 라벨이 사라졌어요.

**A.** M4.9d부터 모드별로 변경됨:
- 양하: "양하확인" / "양하확인 취소"
- 선적: "선적확인" / "선적확인 취소"

---

## 지원 안 되는 기능 (다음 빌드 예정)

다음 빌드(M5.2 또는 그 이후)에 추가될 후보 기능:

1. **HTML5 Drag & Drop** — 보관함 ↔ 베이 그리드 셀 사이 컨 드래그 (PC 전용). 폰은 long-press로 동일 동작
2. 추가 통계/보고서 양식
3. 사용자 피드백에 따른 개선

✅ M5.0 완료: 자료 정리, 항차 요약 카드, 베이 컨트롤 바 산뜻 정리, 영어회화집 도움말 흡수
✅ M5.01(M5.1 흡수) 완료: dictBayList 중복 제거(BAY 01 중복 fix). 짝수 단독 베이는 정상 도메인 그대로 표시 (BOW/STERN/선원건물 앞뒤)
✅ M5.1 완료: G. 작업 마감 체크리스트 / I. 보관함 + 영역 선택(PC 선적 전용) + 일괄 보관

## M5.1 신규 기능 상세

### G. 작업 마감 체크리스트 (양/선적 모두)

**진입**: 항차 페이지 상단 [📤 작업 보고] 옆의 [🏁 마감 점검] 버튼

**한 화면에 모든 미완 항목**:
- 미완료 컨 N대 → 클릭 시 리스트 탭 + 미완 필터
- 리퍼 온도 미입력 (Full만)
- ISO403 사진 미촬영
- X-RAY 미처리 (양하 모드만)
- 자리 뺏긴 컨 미해결 (선적 모드만)
- 풀씰 미입력 (선적 모드만)

**모두 0이면 큰 ✅ "마감 가능" 화면** — 안전한 작업 종료 체크.

### I. 보관함 + 영역 선택 (선적 전용)

**보관함 박스**: 베이 탭 상단 (DisplacedSidebar 아래) — bay_actual='__STG__' 컨들 모음
- 카드 우측 [📦 이동]: 베이 그리드 빈 셀로 다시 보내기
- 헤더 [↻ 일괄 복원]: 모두 계획 위치로 복원

**영역 선택 (PC 전용)**: 베이 탭 컨트롤 바 [🔲 선택] 토글
- 선택 모드 ON: 컨 셀 클릭 → 파란 ring으로 선택 표시
- [📦 보관함으로] 버튼 → 선택분 한 번에 보관함 이동

이 기능들은 PC(마우스) 환경 검증이 필요해서 별도 빌드로 분리.

---

## 데이터 모델 (개발자용 참고)

### 컨테이너 객체 (선적 모드)

```js
{
  cn: 'CKFU9213883',
  bay: '11',           // effective 위치 (변환된 actual 또는 계획)
  row: '11',
  tier: '11',
  _bay_planned: '01',  // 계획 위치 (effective 변환된 경우만 존재)
  _row_planned: '00',
  _tier_planned: '02',
  bay_actual: '11',    // 검수원이 입력한 실체 위치 원본
  row_actual: '11',
  tier_actual: '11',
  actual_at: 1746805200000,
  actual_by: '성일',
  _position_moved: true,
  _displacedBy: '점유자_컨번호',  // displaced 검출 시 부착
  // ... 기타 EDI 필드 (sl, wt, fe, iso 등)
}
```

### Firebase 경로

```
voyages/{voyageKey}/loading/records/{cn}
  ├ bay_actual: '11'
  ├ row_actual: '11'
  ├ tier_actual: '11'
  ├ actual_at: timestamp
  └ actual_by: '검수원명'
```

### 핵심 함수

- `fbSetActualPosition(voyageKey, mode, cn, bay, row, tier, by)` — 실체 위치 저장
- `fbClearActualPosition(voyageKey, mode, cn)` — 실체 위치 삭제 (계획대로 복귀)

---

## 변경 이력

| 버전 | 날짜 | 핵심 변경 |
|---|---|---|
| M4.9e | 2026-05-09 | 선적 실체 위치 1+2+3단계, 자리 뺏긴 컨 박스, 베이 row/tier 동적 |
| M4.9d | 2026-05-09 | "20ft 전용" 라벨 제거, 양하확인/선적확인, 베이 라벨 단순화 |
| M4.9c | 2026-05-09 | 인쇄 표준 패턴(visibility 토글), 양하리스트 매핑 강화 |
| M4.9b | 2026-05-09 | ALLOWED_LIST_FIELDS 픽스 (eseal 등), 인쇄 가로 모드 |
