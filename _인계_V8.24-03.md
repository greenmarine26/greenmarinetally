# 검수앱 V8.24-03 — 다른 선박/항차 EDI 차단 (GitHub 최신에 덮어쓰기)

## 무엇 / 왜
다른 배 EDI나 항차 안 맞는 EDI를 항차에 넣어도 경고 없이 그대로 적용(집계표·베이구조·info·베이사전 덮어씀)되던 위험 버그. 사용자 확정 규칙으로 차단.

## 이 ZIP의 3파일 (GitHub 최신에 그대로 덮어쓰기 — V8.24-02 가드 수정도 포함된 최신본)
- src/pages/VoyagePage.jsx
  - (V8.24-02 유지) PORT-MIS 신원가드 _nameMatchesPm: vslFull 코드형(CNYNT)이면 우회.
  - (V8.24-03 신규) EDI 업로드 적용 루프(파싱 직후, allCns 누적 전)에 가드 추가:
    ① 선박 다름 → 적용 안 함. 콜사인 둘 다 있고 다르거나, 둘 다 '진짜 선박명'(공백/≥7자)인데 다르면 차단. (CNYNT 같은 코드형 쓰레기 vslFull은 비교 제외 → 정당한 재업로드 오차단 방지)
    ② 항차 다름(양하 EDI는 voy_d, 선적 EDI는 voy_l 과 대조, 평택분 기준 ediKind 자동판정) → 적용 안 함.
        - 평택 해당분 있음 → "파일명 항차를 등록 항차로 고쳐 재등록" 유도 메시지.
        - 평택 해당분 없음 → "다른 항차 자료" 차단.
    ③ 첫 EDI(등록 선박/항차 정보 없음) → 통과(선박·항차 확정).
- src/utils.js — APP_VERSION 'V8.24-03'
- public/sw.js — VERSION 'V8.24-03'

## 빌드
bash build.sh → git add -A && git commit -m "V8.24-03 다른 선박/항차 EDI 차단(경고+적용중단)" && git push

## 검증(작성자)
- node 시뮬 9케이스 PASS: 같은배/같은항차 통과, 다른배(콜사인·선박명) 차단, CNYNT 오차단 없음, 양하/선적 항차 불일치 차단(평택분 유무로 재등록유도/다른항차 구분), 첫 EDI 통과.
- esbuild 문법 검증 통과.
