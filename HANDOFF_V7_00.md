# Tallyman Master M6.94.46 — 콘앱 베이플랜 베이사전 골격 + 콘 베이묶음(567) + 해치0

## 버전: M6.94.45 → M6.94.46

## 콘앱 (cone.html)
1. **베이플랜이 베이사전(ship_bay_dict_v3) 전체 골격으로 그려짐** — 컨테이너 없는 빈 슬롯(점선)까지 검수앱처럼 전부 표시.
   - fbFetchBayDict+lookupBayDef로 선박 매칭(code/imo/name/callsign).
   - bvBaySkeleton: baysSummary의 deckTiers/holdTiers/deckCells/holdCells → 검수앱 getRowPositions(bvRowPositions 포팅)로 row 라벨 생성.
   - 골격 + 실데이터 row/tier **합집합** → 베이사전이 좁아도 골격 밖 컨테이너 누락 방지.
   - 베이사전 없으면 컨테이너로 추정(폴백). renderBayView는 async(베이사전 비동기 로드).
2. **콘 작업표 베이 라벨 = 묶인 베이(트리오 규칙)**. 예: 5,6,7 → "05"(단독) + "06-07"(페어). 검수앱 autoPairBays 규칙(짝수 e+다음 홀수 페어, 나머지 홀수 단독).
3. 컨테이너 박스 셀(POL/POD★·컨번호·F/E·온도·특수색) — 이전 버전 유지.
4. 헤더에 버전표시 v46-dict (캐시 확인용).

## 검수앱 — 해치 버그 3건 (M6.94.44 작업, 포함)
저장후 초기화 수정 / 해치 0 옵션 / 홀드없는 베이 자동 0.

## 검증 (시뮬)
- 문법 OK, 실브라우저 초기화 오류 없음, runCalc 보존.
- 베이사전 골격: 컨테이너 6대→deck 4tier 전부+빈슬롯17 표시(캡처). 골격밖 컨테이너 누락 0.
- 베이사전 없을 때 폴백 작동(박스3).
- 567 묶음 → "05|06-07" 정확. 1234→01|02-03|04.
- 빌드: 버전 박힘, 루트=public, 루트 index 빌드본.

## 배포: ZIP을 저장소 루트에 덮어쓰고 push. **올린 뒤 캐시 때문에 옛 화면 보이면 새로고침**(헤더 v46-dict 확인).

## 확인 필요(실폰/실데이터)
- 베이사전이 있는 선박인지(없으면 폴백=컨테이너 추정). 실데이터로 빈 슬롯·골격 정확도 확인.
- EDI의 fe/온도 누락 시 박스 일부 비어보일 수 있음.

## [다음 수정 예정 — 사용자 요청]
- **베이사전 미등록 안내 메시지**: 베이플랜에서 베이사전(ship_bay_dict_v3)이 없어 골격을 못 그리고 컨테이너 좌표로 폴백한 경우, "⚠ 이 선박은 베이사전이 없습니다 — 검수사에게 베이사전 수정/등록을 요청하세요" 안내를 콘앱에 표시.
  - 판별 지점: renderBayView의 bvBaySkeleton 폴백 분기(dictBays.get(bk) 없을 때).
  - 표시 위치: 베이플랜 상단 1회 또는 폴백된 베이별 — 다음에 정함.

## M6.94.47 변경
- **셀 높이 통일**: 빈 슬롯과 컨테이너 박스 크기가 달라 같은 티어인데 양하·선적 높낮이가 어긋나던 문제 수정.
  .bv-tab.box td를 box-sizing:border-box, width 58px, height 38px로 일괄 통일. 슬롯·박스 개별 크기 제거.
  → 같은 티어 좌우 정렬 확인(캡처: 행 높이 모두 38px).
- 버전표시 v47-align.

## [다음 수정 예정 — 사용자 요청 2] 콘 개수를 베이플랜에 통합 표기
- 콘 계산 결과(데크콘·코끼리콘·홀드콘 개수)를 베이플랜의 베이별 상단(BAY 헤더 옆/아래)에 표기 → 화면 하나로 콘 작업.
- **표기 위치 규칙(사용자 확정):**
  - 기본: 콘 개수를 **짝수 베이** 상단에 표시 (40ft는 짝수+홀수 페어 차지 → 대표로 짝수 베이).
  - **20ft 싱글로 실릴 때: 해당 베이(그 베이 자체)** 상단에 표시.
  - 이는 콘 그룹 규칙과 일치: 페어 "06-07"→06(짝수)에, 단독 "05"→05에.
- 콘 계산은 ediToBayGroups(그룹키=짝수 또는 단독 홀수)에서 이미 산출됨. 베이플랜 렌더 시 해당 베이 헤더에 그룹 콘 수 매칭해 표기.
- 단순화 목표: 콘 작업표와 베이플랜 두 화면 → 베이플랜 하나로.

## M6.94.48 변경 (페어 합치기 + 콘 개수 베이플랜 통합)
1. **페어 베이 합치기**: 베이플랜에서 40ft가 차지하는 짝수+홀수(예 06,07)를 **한 격자로** 합쳐 "BAY 06-07"로 그림.
   - 이전: 06,07 따로 → 07 자리가 06 격자에 빈칸으로 떠보이고 개수 혼동.
   - 수집을 groupKeyOf(짝수=페어대표, 홀수는 앞짝수 있으면 그 페어/없으면 단독)로 묶음. 제목=memberBays "06-07".
   - 골격도 그룹 멤버 베이들의 베이사전 합쳐 사용.
2. **콘 개수 베이플랜 통합 표기**: 각 베이 제목 아래 콘 배지.
   - 양하콘(노랑 cone-d)·선적콘(초록 cone-l): "데크 N · 코끼리 N · 홀드 N".
   - 규칙: 페어(40ft)는 짝수 대표 베이(06-07)에, 20ft 싱글은 해당 베이(예 03)에 — 그룹키가 콘 계산 그룹키와 동일해 자동 매칭.
   - planToGroups+distribute 재사용. state.disch/stow는 isEDI:true 보유.
3. 버전표시 v48-cone.

## 검증
- 567→[05,06-07], 20ft단독→[03], 양하선적 배지2, 빈데이터 크래시0, hold 정상.
- 페어 06-07 한 격자에 row 06 04 02 01 03 05 (7번 빈자리 안뜸) 캡처 확인.
- 콘 배지: BAY05="데크1·코끼리2", BAY06-07="데크6" 캡처 확인.
- 셀 높이 통일(38px) 유지. 초기화 오류 없음. 재현 빌드 0에러.

## V7.00 변경 (재산권 표시)
- 버전: M6.94.48 → **V7.00** (utils.js APP_VERSION + sw.js + public/sw.js).
- **두 앱에 회사명·개발자 재산권 표시:**
  - 검수앱: App.jsx 하단 footer "© 2026 (주)그린마린(Green Marine) · 개발 연지아빠" + 버전(V7.00).
  - 콘앱: 헤더 sub "(주)그린마린 · V7.00" + 푸터 "© 2026 (주)그린마린(Green Marine) · 개발 연지아빠".
- 검증: 빌드 JS에 "(주)그린마린" 박힘, 화면 표시 확인(그린마린·연지아빠·V7.00), 초기화 오류 없음.
- 콘앱 헤더 버전표시: V7.00 (기존 v48-cone 대체).
