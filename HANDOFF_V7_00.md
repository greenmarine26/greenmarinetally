# Tallyman Master V7.00

## 콘앱 (cone.html) — 콘 계산 규칙 정밀화 + 표시
1. **선종 분리**: 컨테이너선 / 복합선. 복합선은 홀드콘 곳당 2개/4개 매번 선택(인력 따라). state.multiCount.
2. **홀드콘**: 받침(밑 컨테이너) 있을 때만. 컨테이너선=20ft 1·40ft 0 / 복합선=20·40 구분없이 곳당 multiCount.
   - 핵심 버그 수정: 받침 체크 tier 문자열→2자리 패딩(04-2="2"≠"02" 불일치로 홀드콘 0이던 문제). isDeck도 parseInt.
3. **데크콘**: 40ft=4 / 20ft 트윈(짝꿍 베이 같은 row·tier에 컨 있음)=두 자리 합쳐 데크4+코끼리4 / 20ft 단독(페어 짝꿍 빔)=데크2+코끼리2 / 20ft 전용데크(홀수 단독 베이)=데크4.
4. **코끼리콘 항상 짝수**(곳당 2). distribute ×2 중복 제거 — 홀드콘=대수×곳당개수 그대로.
5. **표시**: 베이플랜 상단 전체 총콘("데크콘 총 N개 (X곳 ×4)" 등, 콘박스에서 가져올 수량) + 베이별 배지(그 베이 콘 총개수).
6. (앞 버전 유지) 페어 베이 한 격자(06-07), 베이사전 골격, 셀 높이 통일, 컨테이너 박스 셀.

## 재산권 표시
- 검수앱: App.jsx footer "© 2026 (주)그린마린(Green Marine) · 개발 연지아빠" + V7.00.
- 콘앱: 헤더 "(주)그린마린 · V7.00" + 푸터 "© 2026 (주)그린마린(Green Marine) · 개발 연지아빠".

## 검증
- 콘 계산 케이스 전부 PASS: 40ft(데크4), 20ft트윈(4+4), 단독페어(2+2), 전용데크(4), 코끼리 짝수, 홀드콘 컨선/복합2/복합4.
- 총콘 표시(데크콘 총 112개 28곳×4 캡처), 베이별 배지(데크콘 28).
- 문법 OK, 초기화 오류 없음, 빌드 V7.00 박힘, 루트=public.

## 배포: ZIP을 저장소 루트에 덮어쓰고 push. 새로고침 후 헤더 V7.00 확인.

## [다음 예정] 베이사전 미등록 안내 메시지 (HANDOFF 이전 기록 참조).

## V7.00 추가 수정 (베이매트릭스 유령 베이)
- **증상**: 4번 베이가 베이사전에 없는데 카고플랜에 (04)05로 잘못 페어링. "1 (2)3" 다음 "5(6)7" 와야 하는데 "(4)5"가 옴.
- **근본 원인**: PrintableCargoPlanV2의 matrixBays가 `_v5Matrix.matrixBays`(raw, v5 자동추출)를 우선 사용. 사용자가 베이사전(baysSummary)에서 4번을 빼도 v5 raw에 4번이 남아 카고플랜에 새어 들어옴.
- **수정**: raw와 baysSummary 둘 다 있으면, baysSummary(사용자 정의)에 있는 bayNum만 남기도록 raw 필터. userBayDict 보호 원칙(사용자 정의 > v5 자동) 준수.
- **검증(Node 시뮬)**: baysSummary 1,2,3,5,6,7 + v5 raw 1~7 → 필터 후 1,2,3,5,6,7 → 트리오 01-(02)03, 05-(06)07. (04)05 안 나옴. 필터 전(버그) 재현도 확인.
- 파일: src/components/PrintableCargoPlanV2.jsx (matrixBays useMemo).

## V7.00 추가 (곳 수 + 가감 표시)
- 베이별 배지: 총개수 + 곳 수 ("데크콘 12개(3곳) · 코끼리콘 N개(M곳) · 홀드콘 N개").
- 전체 총콘 + **가감(빨강)**: 선적-양하 차이. +면 추가, -면 반납. 베이별·전체 둘 다. 클래스 cone-diff/tot-diff (빨강).
- 곳 수: 데크콘÷4, 코끼리콘÷2. 홀드콘은 개수만.
- 검증: 곳수 표시, 가감 +8(추가)/-12(반납) 부호 정확.

## V7.00 추가 (데크/홀드 중앙정렬)
- 증상: 데크·홀드 row 폭이 다르면(데크 7칸·홀드 5칸 등) 홀드가 한쪽으로 치우침.
- 수정: 데크 row + 홀드 row 합집합(commonRowPos)으로 두 구역 모두 그림. 각 단에 없는 row는 빈 슬롯 → 열이 위아래 일치, 가운데 정렬.

## V7.00 추가 (데크·홀드 중앙정렬)
- 증상: 베이플랜에서 데크·홀드 폭이 다르면 홀드가 한쪽으로 치우침.
- 수정: 검수앱 방식 — 합집합(X) 대신, 넓은 폭(max(deck,hold)) 기준으로 좁은 쪽을 가운데 배치(centerAlign).
  row 라벨이 가운데(00/경계) 기준 대칭이라 같은 row가 세로 정렬됨. 여백은 bv-empty(점선 없음), 빈 슬롯은 bv-slot(점선) 구분.
- 검증: 데크8/홀드7 → 갑판 08~07, 선창 06~05가 가운데 정렬(캡처). 폭 같으면 완전 일치.
- 한계: 데크(00없음)·홀드(00있음) 중심 0.5칸 차이는 칸 단위라 미세 오차 가능(검수앱은 % padding). 실사용 문제시 추후 정밀화.
