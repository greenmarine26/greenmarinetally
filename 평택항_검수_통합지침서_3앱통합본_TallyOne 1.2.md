# 평택항 검수 통합 지침서 — 3앱 통합본

> **이 문서는 이 프로젝트의 모든 작업에 우선 적용되는 규칙이다.**
> 이 프로젝트에서 작업하는 모든 Claude는 시작 전에 이 문서를 먼저 읽고 따른다.
>
> 🚀 **배포를 하려면 저장소 루트 `DEPLOY.md`를 먼저 읽는다.**
> 전문은 드라이브 `CLAUDE_SHARED\★배포_표준절차_모든클로드_필독.md`. 배포는 클로드가 끝까지 직접 한다.

최종 버전: **Tallyman Master V9.45** (검수앱 · 콘앱 · 벌크탤리 공통 — 소스 실측) · 수집기 **2.17.42** · 부속도구 **선적 플랜 편집기 V9.14(단독)** · 최종 업데이트: 2026-08-02 (작업일시 = 도선>메일>PORT-MIS>배정 · 자리별 출처 표기 · 수석 대시보드 진입 차단 · 수석 이름 로그인 잠금 · RZOR 덱플랜 자동 수집)
작성자: 성일 (평택항 검수 / Tallyman Master 개발)

> 📌 **출력 규칙 (사용자 확정 2026-06-16)**: 지침서를 사용자에게 전달할 때 **파일명에 최종 버전을 표기**한다. 예: `평택항_검수_통합지침서_3앱통합본_V8.05.md`. 새 버전 발행 시 파일명 버전과 위 "최종 버전" 줄을 함께 갱신한다.

---

## 이 문서의 구성 — 세 앱을 한 권에, 명확히 분리

이 프로젝트에는 **서로 완전히 독립된 네 개의 산출물**이 있다. 코드베이스·배포·점검이 모두 별개이므로, 하나를 고쳐도 다른 것에 자동 반영되지 않는다.

═══════════════════════════════════════════════════════
## ★ 버전 정본표 — 버전을 말하기 전에 반드시 여기를 본다 (2026-07-26 전수 실측)
═══════════════════════════════════════════════════════

> **이 표의 "버전 소스" 열에 적힌 파일을 직접 읽어서 확인한 값만 쓴다.**
> 기억·과거 대화·옛 지침서에 적힌 버전 번호는 전부 무효다. 실제로 44판 뒤처진 값이 기억에 남아 여러 클로드가 헷갈린 사고가 있었다(2026-07-26 확인).

| 앱 | 파일/산출물 | 정체 | 현재 버전 | **버전 소스 (여기를 읽는다)** |
|---|---|---|---|---|
| **A. Tallyman Master (검수앱)** | React/Vite PWA (greenmarinetally) | 컨테이너선 양하·선적 검수 본체 | **V9.37-01** | `src/utils.js` → `export const APP_VERSION` — **단일 진실원** |
| **B. 콘앱 (콘 계산기)** | `public/cone.html` (독립 HTML, 검수앱 ZIP에 동봉) | 콘(고정장치) 가감 계산 | **V9.37-01** (검수앱과 동일) | `cone.html` → `(주)그린마린 · Vx.xx` 라벨 · `__APPV` · `__CONEV` — 전부 build.sh가 A에서 동기화 |
| **C. 벌크탤리** | `bulk_tally.html` (단일 HTML) | 벌크선 철강 코일 검수 | **V9.37-01** (검수앱과 동일) | `bulk_tally.html` → `(주)그린마린 · Vx.xx` 라벨 — build.sh 동기화 대상 |
| **D. 수집기 (TallymanMailCollector)** | `C:\TALLYTEST\TallymanMailCollector_*` (PC 로컬 Python) | 메일 수집·합본·항차 자동등록 | **2.17.32** (2026-08-02) | `app_gui.py` → `VERSION = "..."` — **폴더명이 아니라 이 값이 진실** |

**버전 체계 (혼동 금지)**

- A·B·C는 **하나의 저장소·하나의 build.sh**를 공유하므로 **버전 번호가 항상 같다.** `src/utils.js`의 `APP_VERSION` 한 곳만 올리면 build.sh가 `sw.js`·`cone.html`(라벨/`__APPV`/`__CONEV`)·`bulk_tally.html`·`README.md`를 전부 동기화한다.
- ⛔ **콘앱 독자 버전 체계 `C7.xx`는 V8.98-05에서 폐기됐다.** 그 전까지 `__APPV`가 `C7.67`에 고정돼 있어 콘앱 카고플랜을 새로 배포해도 폰이 옛 번들을 계속 캐시하던 사고가 있었고, build.sh가 `__APPV`를 `APP_VERSION`에 동기화하도록 고치면서 없앴다. **문서·대화에서 `C7.xx`를 현재 버전으로 말하지 않는다.**
- D(수집기)만 독립 체계(`2.x.y`)다. GitHub 배포 대상이 아니다.
- **폴더명·ZIP 파일명은 버전의 근거가 아니다.** 리네임을 빠뜨리면 즉시 어긋난다(실제로 수집기 폴더가 `v2.17.11-02`인 채 내부가 `2.17.15`였다).

**문서 정리 규칙 (2026-07-26 사용자 확정 — 비협상)**

- 저장소·드라이브에 **통합지침서는 최신 1개만 존재**한다. 새 판을 커밋할 때 옛 판 파일을 **같은 커밋에서 지운다.** (이 규칙이 지켜지지 않아 저장소에 48개, 드라이브에 28개가 쌓였다.)
- **`_인계_*.md` · `HANDOFF_*.md` · `변경_*.md`를 저장소에 남기지 않는다.** 내용은 이 지침서의 해당 부에 흡수하고 파일은 지운다.
- **파일명이 `#Uxxxx`로 깨진 파일을 발견하면 즉시 지운다.** Windows 경유 커밋에서 한글 파일명이 이스케이프된 채 커밋돼, 정상 파일과 중복된 **낡은 사본**이 저장소에 남는다. 클로드가 이걸 열면 몇 달 전 내용을 최신인 줄 알고 읽는다(2026-07-26 실제 발생 — 150KB 6월 15일자 지침서가 324KB 최신본과 나란히 있었다).

- **공통부(0~Ⅰ부)**: 세 앱 모두에 적용되는 코딩 원칙·검수 본업·운영 규칙.
- **Ⅱ부 = 검수앱(A)** / **Ⅲ부 = 콘앱(B)** / **Ⅳ부 = 벌크탤리(C)** — 각 앱의 도메인·작업 이력. 수집기(D)는 검수앱과의 연동 계약이라 Ⅱ부에 함께 기록한다.
- **Ⅴ부 = 영구 참조 자료(REF)** — 데이터로 검증 완료된 사실만. 3D 카고플랜·베이 페어링.
- **부록** — 디버깅 일지·운영 팁·메모 수신함.

> ⚠️ **앱 B(콘앱)는 앱 A와 같은 버전 번호를 공유**한다(같은 저장소·같은 ZIP·같은 build.sh). 단 cone.html은 독립 HTML이라 React 점검과 **따로 점검**해야 한다. 콘앱 독자 버전(C7.xx)은 **폐기됐다** — 위 정본표 참조.

> 🔒 **단일 지침서 운영 (사용자 확정 2026-06-15 — 절대)**: 이제 따로 된 지침서는 없다. **이 통합 지침서 한 권만 유지**한다. 세 앱이 모두 완성돼 독립 출시될 때까지는 검수앱·콘앱·벌크탤리를 전부 이 한 권으로 관리하며, **별도 지침서(검수앱지침서.md·BULK_TALLY_지침서.md·REF_*.md 등)를 새로 만들지 않는다.** 모든 새 내용은 이 문서의 해당 부에 흡수한다. 분리는 각 앱이 독립한 뒤 사용자가 지시할 때만.

═══════════════════════════════════════════════════════
# ★ 세션 표준 시나리오 — 모든 Claude 공통 (사용자 확정 2026-06-18, 절대)
═══════════════════════════════════════════════════════

> 이 블록은 이 문서에서 **가장 먼저 읽고 가장 강하게 지키는 절차**다.
> 0부 이하의 원칙들이 "왜"라면, 이 블록은 "어떤 순서로 움직이는가"다.
> 어느 Claude가 와도 **같은 길**을 걷게 하기 위한 것이다. 표현은 흔들려도 되지만, 이 순서는 흔들리면 안 된다.
> 작업 중 각 관문에 들어설 때 **"N관문: …"** 으로 번호를 밝힌다. 사용자가 진행을 그 번호로 추적한다.

---

## 0. 시작 선언 (코드를 만지기 전, 한 번)

작업을 시작하면 **세 줄**을 먼저 적는다. 이걸 적기 전에는 코드·파일을 건드리지 않는다.

1. **대상 앱**: 이번에 건드리는 앱 하나를 특정한다 (A 검수앱 / B 콘앱 / C 벌크탤리). 세 앱은 완전 별개다.
2. **끝낼 범위**: 이번 응답에서 어디까지 끝낼지 한 줄로 못박는다. (범위를 안 적으면 아무 데서나 멈추게 된다 — 작업을 남기는 가장 큰 원인.)
3. **남길 것 명시**: 이번에 안 할 부분이 있으면 "이건 이번에 안 한다, 이유는 X"라고 미리 밝힌다. 슬그머니 미루지 않는다.

---

> 🔴🔴 **미완 이월 금지 (사용자 확정 2026-08-01 — 절대)**
> **그날 할 일은 그날 끝낸다. 원인을 알고 수정 지점까지 특정했으면 그 세션에서 수정·검증·배포까지 간다.**
> - ⛔ "원인은 X입니다. 내일 고치겠습니다" **금지.** 원인 파악 = 작업의 절반이 아니라 **수정 직전 단계**다.
> - 미루면 다음 세션에서 ① 사용자가 상황을 다시 설명하고 ② 클로드가 원인을 다시 찾고 ③ 같은 검증을 다시 한다. **한 번 미루면 세 번 일한다.**
> - 남길 수 있는 것은 딱 둘뿐이다: **(a) 사용자가 명시적으로 "나중에"라고 한 것, (b) 사용자 결정이 있어야 진행되는 것.** 그 외에는 남기지 않는다.
> - "늦었으니 여기까지" · "쉬세요" 같은 배려로 미완을 덮지 않는다. **끝내는 것이 배려다.** 정말 멈춰야 하면 남은 항목·원인·수정 지점을 7관문 인계 3줄에 적고 멈춘다.
> - 배경: 2026-08-01 새벽, 클로드가 원인과 수정 지점을 다 파악한 메일박스 정리를 "퇴근 후에 하죠"로 미루려 했다. 사용자 지적으로 그 자리에서 끝냈다.

---

> 🔴 **답을 아는 질문 금지 (사용자 확정 2026-08-01)**
> 질문은 **사용자만 답할 수 있고, 그 답이 내 다음 행동을 바꿀 때만** 한다.
> - ⛔ 데이터로 알아낼 수 있는 것을 묻지 않는다 — 파일을 열어보면 나오는 것, 로그·state·Firebase를 조회하면 나오는 것.
> - ⛔ 사용자가 이미 답한 것을 다시 묻지 않는다. "전수 검수 바랍니다"는 곧 "정리까지 해달라"이다. 되묻는 순간 사용자가 같은 말을 두 번 한다.
> - ⛔ "진행할까요?" 류의 **승인 구걸 금지.** 되돌릴 수 없는 행위(삭제·발송·과금·외부 게시)만 확인받는다. 이동·수정·배포는 확인 대상이 아니다.
> - ✅ 물어야 하는 것: 업무 규칙(짝 규칙·마감 기준), 우선순위, 표시 위치·형식 취향, 되돌릴 수 없는 행위.
> - 애매하면 **가장 보수적인 선택으로 진행하고 "이렇게 했습니다, 다르면 말씀 주세요"** 로 보고한다. 물어서 세우는 것보다 낫다.

---

> 🔒🔒 **"작업 시작"의 정의 (사용자 확정 2026-06-19 — 절대, 가장 자주 어기는 함정)**
> **작업 시작 = 협의·재분석·질문·확답 확보를 다 거친 "그 다음"이다.** 코드를 만지는 행위(5관문)는 작업의 출발점이 아니라, **사용자의 확답을 받고 나서야 비로소 시작되는 것**이다.
> - 협의·구상·질문·확답은 "작업 전 준비"가 아니라 **작업 시작을 이루는 구성요소 그 자체**다.
> - 0-11b의 "작업 모드 들어가면 중간에 안 되묻기"는 **확답을 받은 5관문 이후에만** 적용된다. 확답 전에 안 묻고 직진하는 것은 "작업 모드"가 아니라 그냥 **협의를 건너뛴 것**이다.
> - 배경: 2관문(소스 확인)을 끝내고 혼자 설계를 정해 5관문(코드)으로 직행하면, 거의 반드시 재작업이 난다(V8.10 규격 20/40 → 20/40/45·F/E 재작업이 실제 사례). 협의를 거쳤으면 처음부터 맞게 짰을 일이다.

---

> ⚠️ **이 7관문은 반드시 지켜진다 (사용자 확정 2026-06-19 — 절대 규칙).** 어느 Claude가 와도 2→3→4관문(점검·구상·협의)을 건너뛰고 코드로 직행하지 않는다. 4관문에서 사용자 확답을 받기 전에는 5관문(작업)에 진입하지 않는다. 통과한 관문은 **"N관문: …"** 으로 번호를 밝혀 사용자가 추적하게 한다.

## 1관문 — 진입 (버전·앱 정합)

- 세 버전을 대조한다. ① 이 지침서의 "최종 버전" ② 작업 베이스(업로드 ZIP/clone)의 `src/utils.js APP_VERSION` ③ 사용자가 마지막으로 받은 ZIP.
- 셋이 어긋나면 **코드를 만지기 전에 보고**하고 어느 게 최신인지 확인받는다. (0-11)
- 통과 조건: 세 버전 일치 확인 또는 불일치 보고 완료.

## 2관문 — 점검 (대상 앱·소스가 제대로 돼 있는지)

- "근거를 본다"가 아니라 **"제대로 되어 있느냐까지 점검한다."** 잘못된 앱·잘못된 베이스로 작업하면 안 되기 때문이다.
- 손댈 소스를 **GitHub에서 직접 확인**하고, 대상이 올바른지·현재 상태가 멀쩡한지·이미 누가 손댔는지(다른 세션 흔적)를 본다. 기억·추측으로 시작하지 않는다.
- 손댈 데이터 구조(EDI 행·매트릭스 cells·firebase 레코드·필드명 등)를 **실제로 본 뒤** 진행한다. (부록 A의 반복 실수는 전부 "안 보고 짠 것"이 원인.)
- "이렇겠지"가 끼면 그 자체로 관문 실패 — 다시 본다.
- 통과 조건: 대상 앱·소스가 올바르고 제대로 돼 있음을 확인. 손댈 소스·데이터 구조·필드명을 실제로 봤다.

## 3관문 — 구상 (방법·시나리오 도출)

- "어떻게 하면 좋을까" **방법을 구상하고, 시나리오를 여러 개 세운다.** 한 가지 안만 들고 오지 않는다.
- 결정이 갈리는 분기점(규격을 어떻게 나눌지, F/E를 구분할지, 표시를 어디에 둘지 등)을 미리 찾아 시나리오로 만든다.
- 통과 조건: 선택지(시나리오)들이 갈리는 지점과 함께 도출됐다.

## 4관문 — 협의·가상시뮬·질문·결정 (← 여기까지 통과해야 "작업 시작")

- **협의 = 구상한 시나리오들 중 나은 걸 찾는 과정.** 사용자와 함께 고른다. 혼자 고르지 않는다. (0-1)
- 나은 안을 고른 다음 **가상 시뮬레이션**을 돌려 "이렇게 하면 이런 결과가 나옵니다"를 데이터로 보여준다. (시뮬은 6관문 검증에만 있는 게 아니라, 작업 전 협의에도 "가상 시뮬"로 들어간다 — 추측이 아니라 데이터로 결정하기 위해.)
- 그 결과를 놓고 **"어떻게 할 것인지" 질문 → 사용자의 결정(확답)** 을 받는다.
- ⛔ **확답 없이는 5관문(작업)에 진입 금지.** 확답이 없으면 아직 "작업 시작"이 안 된 상태다.
- 🔴 **질문은 "답을 아는 질문 금지" 규칙을 통과한 것만.** 데이터로 알 수 있는 것·이미 답을 받은 것·단순 승인 요청은 질문이 아니라 **지연**이다. 그런 건 묻지 말고 데이터로 정하고 진행한 뒤 보고한다.
- 🔴 **버그 수리는 4관문을 축약한다.** 원인이 데이터로 확정됐고 수정이 외과적이면(한 파일·한 규칙) 시나리오 나열 없이 "원인 X, 이렇게 고칩니다" 한 줄 통보 후 바로 5관문. **수리를 협의로 지연시키지 않는다.** 협의가 필요한 건 설계·신규 기능이지 명백한 결함이 아니다.
- 통과 조건: 시나리오 중 하나로 합의 + (필요 시 가상 시뮬로 결과 제시) + 질문에 사용자 확답 확보. (버그 수리는 원인·수정안 통보로 갈음)

## 5관문 — 작업 (외과적 변경)

### 5-0. 파급 지도 — 코드를 고치기 전에 반드시 (사용자 확정 2026-08-01, 비협상)

> **"내가 고칠 곳"이 아니라 "이 수정이 닿는 곳 전부"를 먼저 적는다.** 이걸 건너뛴 수정이 최근 사고의 대부분이다.

1. **호출부 전수** — 고칠 함수·상수·경로를 저장소 전체에서 grep해 **부르는 곳을 전부 나열**한다. 하나만 고치고 나머지를 놓치지 않기 위해서다.
   - 실사례: `merge_all`의 재검색 폭주를 고쳐 배포했는데 **똑같은 코드가 `merge_priority`에도 있었다.** 고친 함수만 보고 사이클 전체를 안 봐서 사용자 작업이 반나절 막혔다 (2026-07-31).
2. **복제본 확인** — 같은 로직이 다른 파일에 복사돼 있지 않은지 본다. (수집기 `terminal.py`/`terminal_v26_backup.py`, 앱 파서 등)
3. **공용 경로 판정** — 내가 넣는 코드가 **특정 선박·특정 케이스 전용**인가? 그렇다면 **게이트를 먼저 걸고** 공용 경로 앞단에 두지 않는다.
   - 실사례: RZOR 전용 덱플랜 감지를 모든 리스트 업로드 앞단에 둬서 **전 선박 업로드가 수 분씩 느려졌다** (V9.22→V9.31).
4. **이 수정으로 값이 바뀌는 하류(下流)** — 내가 바꾸는 필드·노드를 누가 읽는가. 앱 표시·수집기 판정·대시보드 카운트까지 따라간다.
5. 위 1~4를 **한 줄씩 적어 사용자에게 보인다.** 적을 게 없으면 "파급 없음: 이 함수는 여기서만 호출됨"이라고 명시한다.

### 5-1. 변경

- 요청·확답과 **직접 연결된 줄만** 고친다. 주변 코드 "개선"·무관한 리팩터링 금지. (0-3)
- 🔴 **조용한 실패를 만들지 않는다.** 새로 넣는 `await`·동적 import·외부 요청·저장 호출은 **반드시 실패 경로가 화면이나 로그에 드러나게** 한다. 빈 `catch {}`로 삼키지 않는다. 외부 요청에는 **타임아웃과 대체 경로**를 함께 넣는다.
  - 실사례 3연발(2026-07-31): ①동적 import 실패가 try 밖 ②CDN을 타임아웃 없이 대기 ③`undefined` 필드로 Firebase 저장 거부 — 셋 다 예외가 삼켜져 화면은 "처리 중"에 멈춘 채였고, 원인이 증상에서 멀어져 세 판을 헛되이 배포했다.
- 새 파일 첫 줄 한국어 헤더 주석. 한국어 문장은 `.`/`?`/`!`로 끝낸다(콜론 금지). (0-5, 0-6)
- 이 단계부터 0-11b 작업 모드 — 중간에 되묻지 않고 끝까지(단, 4관문 확답 범위 안에서).
- 통과 조건: 변경된 모든 줄이 4관문에서 합의된 내용에 직접 연결된다.

## 6관문 — 검증 (이것이 "완료"의 정의 — 비협상)

**아래 다섯이 전부 끝나야 "완료"라고 말한다. 빌드 성공만으로는 완료가 아니다.**
(2026-08-01 확장: 종전 3종은 "내가 고친 곳"만 봤다. 사고는 **내가 안 본 곳**에서 났다.)

1. **실데이터 시뮬 PASS** — 실제 데이터를 끝까지 돌려 기대값과 일치. 추론·부분 시뮬 금지.
2. **`bash build.sh` 성공** — `npm run build` 단독 금지.
3. **빌드된 JS에서 새 문자열 grep 확인** — 내가 넣은 변경이 번들에 실제로 박혔는지 확인.
   - **새 UI를 추가·변경했으면 시각 렌더로 눈으로 PASS까지.** 화면 확인을 사용자에게 떠넘기지 않는다.
4. 🔴 **파급 검증 — 안 건드린 곳이 멀쩡한지 (신설, 비협상)**
   - **수정 전에 기준 수치를 찍어두고, 수정 후 같은 수치를 다시 찍어 대조한다.** 달라진 항목이 있으면 그게 의도한 것인지 한 줄로 설명한다.
   - 검수앱 기준표: 항차별 `ediContainers`·`records`·`completed` 개수 (Firebase shallow 조회 1회면 전부 나온다).
   - 수집기 기준표: 하트비트 시각·버전, `autoreg` done 건수, 사이클 소요시간, 미수집 건수.
   - 5-0 파급 지도에 적은 호출부·하류를 **하나씩 짚어** 이상 없음을 확인한다.
   - 기준표 뽑는 명령(그대로 복사해 쓴다 — 배포 전 1회, 배포 후 1회):
     ```bash
     python3 - <<'EOF'
     import urllib.request, json
     B='https://greenmarinetally-default-rtdb.asia-southeast1.firebasedatabase.app'
     g=lambda p: (json.load(urllib.request.urlopen(f'{B}/{p}.json?shallow=true')) or {})
     gv=lambda p: json.load(urllib.request.urlopen(f'{B}/{p}.json'))
     print(f'{"항차키":14} {"양하EDI":>7}{"양하rec":>7}{"양하완료":>7} {"선적EDI":>7}{"선적rec":>7}{"선적완료":>7}')
     for k in sorted(g('voyages')):
         r=[k]+[len(g(f'voyages/{k}/{m}/{f}') or {}) for m in ('discharge','loading')
                for f in ('ediContainers','records','completed')]
         print(f'{r[0]:14} {r[1]:7}{r[2]:7}{r[3]:7} {r[4]:7}{r[5]:7}{r[6]:7}')
     h=gv('collector_heartbeat'); print('수집기', h.get('version'), h.get('autoreg'))
     EOF
     ```
5. 🔴 **효과 확인 — "실행됐다"가 아니라 "결과가 그렇게 됐다" (신설, 비협상)**
   - 함수가 성공을 반환한 것, 로그에 "완료"가 찍힌 것은 **검증이 아니다.** 결과 상태를 **다시 읽어** 확인한다.
   - 파일을 옮겼으면 원본이 사라지고 대상에 있는지, 업로드했으면 서버에서 되읽어 개수가 맞는지, 배포했으면 라이브에서 버전·새 문자열이 보이는지.
   - 실사례: 메일박스 정리 스크립트가 "32개 이동 완료"를 찍었는데 **실제로는 30개가 그대로 있었다.** 반환값을 결과로 착각한 것 (2026-08-01).

- 다섯 중 하나라도 못 했으면 "완료"라 부르지 않는다. "검증을 사람에게 떠넘기지" 않는다 — 끝까지 내가 본다. (0-8, Ⅶ부)
- 🔴 **재현 우선 원칙**: 증상을 재현할 수 있으면 **코드 추리보다 재현이 먼저다.** 재현 1판이 추리 3판보다 빠르다 (2026-07-31 업로드 정지 실측 — 추리 세 판 실패, 화면 재현 한 번에 진범 확정).
- 통과 조건: 위 5종(새 UI면 +시각 PASS) 전부 완료 + 결과를 사용자에게 보고.

## 7관문 — 인계 (다음 Claude가 끊김 없이 잇도록)

- **ZIP**은 Ⅱ-2.5 규칙대로 — 최상위 `Tallyman_Master_Vx.xx/` 하나, dist 포함, 제외 목록(node_modules·HANDOFF·구지침서·깨진 파일명 등) 점검. 출력은 `/mnt/user-data/outputs/`.
- 새 발견·결정·검증 결과를 **이 지침서 해당 부에 흡수**한다(별도 파일 만들지 않음). 해결된 항목은 취소선으로 정리.
- **다음 Claude를 위한 3줄**을 남긴다:
  1. 했다 — 이번에 끝낸 것.
  2. 검증됐다 — 무엇이 어떻게 PASS됐는지.
  3. 주의 한 가지 — 다음이 밟지 말아야 할 함정 / 남은 작업.
- 🔴 **미완 점검(신설 2026-08-01)** — 인계 전에 스스로 묻는다: **"원인을 알고 수정 지점까지 아는데 안 고친 것이 있는가?"** 있으면 인계하지 말고 **지금 고친다.** 이월할 수 있는 건 사용자가 미루라고 한 것과 사용자 결정 대기뿐이다.
- 통과 조건: ZIP·지침서 흡수·인계 3줄 완료 + **미완 점검 통과(이월 항목 0 또는 사용자 승인분만)**.

---

## 트리거 단어 (사용자 → Claude, 고정 동작)

| 사용자가 말하면 | Claude는 |
|---|---|
| **메모** | 답변·분석·제안·질문 금지. 부록 C에 원문 그대로 적고 "메모 접수" 한 줄만. |
| **작업해줘** | 쌓인 메모를 몰아서 처리. 0관문부터 정식 시작(2·3·4관문 협의 거쳐 확답 후 코드). |
| **멈춰 / 스톱** | 즉시 중단. 변명·이어쓰기 금지. |
| **검증** | 6관문 결과(3종 PASS 여부, 새 UI면 +렌더)만 보고. |
| **인계** | 7관문 "인계 3줄"만 출력. |

---

## 한 줄 요약 (이 블록의 핵심)

**0 선언 → 1 버전대조 → 2 점검(앱·소스 제대로 됐나) → 3 구상(시나리오 여럿) → 4 협의·가상시뮬·질문·확답 【= 작업 시작】 → 5 외과적 수정 → 6 시뮬·빌드·grep(+새UI면 렌더) 전부 PASS여야 완료 → 7 ZIP·흡수·인계 3줄.**
확답을 받기 전에는 코드를 만지지 않는다. 빨리 끝내려 중간에 멈추지 않는다. 검증을 사람에게 떠넘기지 않는다. 끝낼 범위는 시작에 적고, 못 끝낸 건 인계에 명시한다.

# 0부 — 범용 코딩 지침 (세 앱 공통, 모든 작업의 기본 자세)
═══════════════════════════════════════════════════════

caution over speed. 사소한 작업은 판단껏.

## 0-1. 코딩 전에 생각하라 (Think Before Coding)
가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.
- 가정은 명시적으로 말한다. 불확실하면 묻는다.
- 해석이 여러 개면 제시한다 — 혼자 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 불분명하면 멈춘다. 무엇이 헷갈리는지 짚고 묻는다.

## 0-2. 단순함 우선 (Simplicity First)
문제를 푸는 최소 코드만. 추측성 기능 금지.
- 요청 안 한 기능 추가 금지. 일회용 코드에 추상화 금지.
- 요청 안 한 "유연성·설정 가능성" 금지. 불가능한 시나리오 에러처리 금지.
- 200줄 짠 게 50줄로 되면 다시 쓴다.

## 0-3. 외과적 변경 (Surgical Changes)
건드릴 것만 건드린다. 내가 만든 흔적만 치운다.
- 주변 코드·주석·포맷을 "개선"하지 않는다. 안 깨진 걸 리팩터링하지 않는다.
- 기존 스타일을 따른다. 무관한 죽은 코드는 언급만 하고 지우지 않는다.
- 내 변경이 만든 미사용 import/변수/함수만 제거한다.
- 기준: 모든 변경 줄이 사용자 요청과 직접 연결돼야 한다.

## 0-4. 목표 기반 실행 (Goal-Driven Execution)
성공 기준을 정하고, 검증될 때까지 반복한다.
- "버그 수정" → "버그를 재현하는 테스트를 만들고, 통과시킨다."
- 이 프로젝트에선: **실데이터로 재현 → 고침 → 같은 데이터로 PASS 확인**.

## 0-5. 한국어 출력 시 콜론으로 문장 끝내지 말 것
사용자가 한국어면 출력도 한국어. 문장 끝은 `.` `?` `!` — `:` 아님.
(영어 문서 습관으로 콜론이 새어 나옴. 콜론은 코드·키밸류·라벨에만.)

## 0-6. 새 파일 첫 줄에 한국어 헤더 주석
새 소스 파일을 만들면 첫 줄에 역할을 한 줄 한국어로.
예: `// 검수 완료 항차를 archive에 백업하는 함수`. (설정 파일은 제외.)

## 0-7. 큰 작업은 계획·체크리스트·결정 노트 먼저
비자명한 작업은 코드 전에 설계를 글로 정리하고 확인받는다 (예: 화면 구조 변경).

## 0-8. 완료라고 말하기 전에 테스트 (절대 원칙)
코드를 만졌으면 "완료" 전에 검증한다.
- 이 프로젝트에선 **실데이터 시뮬레이션 PASS + `bash build.sh` 성공 + 빌드된 JS 검증**이 곧 테스트다.
- 이것 없이 "완료"라고 말하지 않는다. LLM이 가장 자주 건너뛰는 단계 — 비협상.
- **빌드 성공만으로 ZIP 제공 금지. 시각적 렌더링이 필요하면 PNG/SVG 출력으로 PASS 확인 후 진행.**

## 0-9. 의미 있는 단위로 커밋
한 논리 변경이 끝나면 버전을 올리고 변경 이유를 기록한다.
- 좋음: "archive 백업 추가". 나쁨: "백업 추가하고 UI도 고치고 버그도 수정"(3개로 쪼갬).

## 0-10. 에러를 읽어라, 추측하지 마라 (절대 원칙)
실제 에러/로그를 읽는다. 기억으로 패턴 매칭하지 않는다.
- "흔한 수정"을 원인 확인 전에 적용하지 않는다. 불분명하면 로그를 찍어 상태를 확인한 뒤 고친다.
- ⚠️ 이 프로젝트에서 반복된 실수: "캐시 문제겠지", "옛 버전이겠지"로 넘겨짚기. 코드를 끝까지 읽고 실데이터로 확인할 것.

## 0-11b. 대화 모드 vs 작업 모드 (절대 원칙 — 사용자 확정 2026-06-16)
- **대화할 때는 대화·질문·대답만.** 작업 모드로 들어가면 중간에 되묻지 말고 끝까지 작업해 결과물로 보여준다. 사용자는 결과물을 보고 수정·보완하는 방식을 선호한다.
- **사용자가 "멈춰/스톱"이라고 하면 즉시 멈춘다 (최우선·비협상).** 진행 중이던 작업이 아깝거나 클로드의 방향이 옳아 보여도, 사용자의 스톱이 우선이다. 멈춘 뒤 하던 것을 밀어붙이지 말고 사용자 말을 먼저 듣는다.
- 배경: 멈추라는데 무시하고 완성한 결과물은 99% 재수정을 거쳐야 했다. 스톱 무시는 사용자 시간을 버리는 최악의 경우다.

## 0-11. 세션 시작 버전 대조 (절대 원칙 — 재발 3건의 교훈)
**작업 시작 전에 반드시 세 가지 버전을 대조한다.**
- ① 프로젝트 지식 지침서의 "현재 버전" ② 작업 베이스(업로드 ZIP/폴더)의 APP_VERSION ③ 사용자가 마지막으로 받은 ZIP 버전.
- 셋이 어긋나면 **코드를 만지기 전에** 사용자에게 보고하고 어느 것이 최신인지 확인한다.
- 배경: 2026-06-09 세션의 수정 3건(deck-only 규칙 제거·MD 정리·tier 라벨 13px)이 별도 라인이라 6-10 세션에 전부 재발. 기록은 있었으나(6/9 ZIP 안 지침서) 프로젝트 지식이 6/6판이라 다음 클로드에게 전달되지 않았음.
- **기록의 전달 경로 (사용자 확정 운영)**:
  - 프로젝트 지식 교체는 **하루 1회, 그날 마지막 작업 판으로만** (업데이트마다 X — 같은 채팅 안에서는 클로드가 기억하므로 무의미).
  - 새 채팅에서 앱 작업 시에는 **항상 최신 ZIP을 베이스로 업로드** — ZIP 안 지침서가 그 시점의 진실을 전달.
  - **우선순위: 지침서가 둘 보이면 버전 높은 쪽이 진실** (보통 ZIP 안 지침서 ≥ 프로젝트 지식). 프로젝트 지식은 ZIP 없는 채팅(검수 업무·상담)용 베이스라인.
  - **마무리 신호는 사용자가 준다**: 사용자가 "오늘 그만한다 / 지침서 정리해 달라"고 요구하면 그때 클로드가 그날 전체 작업을 반영한 최종판 지침서를 만들어 "프로젝트 지식 교체용"으로 드린다. 중간 ZIP마다 교체를 권하지 않는다.

═══════════════════════════════════════════════════════
# Ⅰ부 — 검수 업무 (본업, 세 앱 공통)
═══════════════════════════════════════════════════════

## Ⅰ-1. 정체성
위치: 대한민국 경기도 평택시 (평택항). 역할: 화물 검수 및 선박 양하 작업 관리. 언어: 한국어.

## Ⅰ-2. 자료 분석 원칙
- 제출 자료(메인플랜·베이플랜·양하 리스트·XRAY 리스트 등)를 빠짐없이 분석할 것.
- 추론이 아닌 실제 데이터만을 기준으로 답변할 것.
- ⚠️ 가정 금지: "이렇겠지" 추측 작업 X. 실데이터 직접 확인.

## Ⅰ-3. 응답 워크플로
- 컨테이너 끝자리 4자리 호출 → 실번호 + X-RAY 대상 여부 + 선내 위치.
- 컨테이너 개수("20피트 몇 대?") → 규격별(20ft/40ft) × 상태별(Full/Empty) 상세 산출.
- 특수화물 리스트 → 베이플랜 기본, 리퍼(온도)·FR·O/T·DG 등 포함.

## Ⅰ-4. 보고 형식
명확하고 간결하며 스캐닝이 쉬운 형식. 실데이터 기반 정밀 수치.

═══════════════════════════════════════════════════════
# Ⅱ부 — 앱 A: Tallyman Master (검수앱)
═══════════════════════════════════════════════════════

## Ⅱ-1. 현재 상태
앱: Tallyman Master (greenmarinetally). 현재 버전: V8.09-14.
GitHub: greenmarine26/greenmarinetally. Firebase: greenmarinetally (asia-southeast1, Spark).
작업 디렉토리: /home/claude/v2work/ (또는 세션별 작업 폴더).
스택: React/Vite PWA + Firebase RTDB. EDI 파싱(표준 ISO 9711 BAPLIE D:95B + 비표준 `00:/50:` 페리 포맷) 기반 양하/선적 관리, 카고플랜 PDF 생성, 베이 매트릭스 빌더, PORT-MIS 연동.

## Ⅱ-2. 검증·배포 원칙 (절대 원칙)

### Ⅱ-2.1 검증
- 검증 안 된 앱은 절대 ZIP으로 주지 않는다.
- 빌드 성공 ≠ 검증. 실데이터로 흐름을 끝까지 돌려 OK일 때만 검증 OK.

### Ⅱ-2.2 작업 순서 (절대 건너뛰지 않음)
```
코드 수정 → 실데이터 시뮬레이션 → PASS → bash build.sh → 빌드된 JS 검증 → ZIP
```

### Ⅱ-2.3 빌드 전 체크리스트
- APP_VERSION 갱신 (src/utils.js 한 곳만 — sw.js·cone.html 라벨은 build.sh가 자동 동기화).
- HelpModal 사용법 등록 (새 기능·변경 시).
- 이 지침서 갱신 (작업 완료 시).

### Ⅱ-2.4 버전 표기 — 3단계 통일 규칙 (사용자 확정 2026-06-19, 절대·모든 Claude 통일)

> 배경: Claude마다 버전 올리는 방식이 제각각이었다(누구는 8.1·8.2, 누구는 8.10, 버그는 8.11_01 또는 V8.10-01…). 데이터 보관·정렬·추적이 꼬여 **한 체계로 못박는다.** 어느 Claude가 와도 아래 그대로 따른다.

**형식: `V{메이저}.{마이너 두 자리}-{빌드 두 자리}`** (예: `V8.20`, `V8.21`, `V8.21-01`)

| 변경 성격 | 올리는 자리 | 예 |
|---|---|---|
| **큰 수정**(기능 추가·구조 변화·메이저) | 마이너 **십의 자리** | V8.10 → **V8.20** → V8.30 |
| **작은 변경**(소기능·UI 조정) | 마이너 **일의 자리** | V8.20 → **V8.21** → V8.22 |
| **버그 수정·정리**(기능 불변) | **빌드 번호**(`-NN`) | V8.21 → **V8.21-01** → V8.21-02 |

**절대 규칙**
1. **마이너는 항상 두 자리.** `V8.2 ❌ → V8.20 ⭕`. 한 자리면 사전식 정렬에서 V8.2 < V8.20 < V8.21처럼 큰수정·작은변경이 뒤섞인다. (큰 수정 "8.1"로 가면 다음 작은변경 "8.11"과 정렬·의미가 꼬임 — 그래서 큰 수정은 십의 자리 올림으로 통일.)
2. **빌드 구분자는 하이픈(`-`), 두 자리** (`-01`·`-02`). ⚠️ 언더바(`_`) 금지 — build.sh의 cone.html 라벨 sed가 `V[0-9.-]*` 패턴이라 `_`를 못 잡아 라벨이 누락된다(V7.91에서 겪은 류의 버그). 하이픈이면 build.sh 무수정으로 작동.
3. **버전 기준 = `src/utils.js APP_VERSION` 한 곳.** build.sh가 sw.js·cone.html 라벨을 자동 동기화. 모든 빌드는 APP_VERSION 문자열을 바꾼다 — 안 바꾸면 폰 캐시에 반영 안 됨(불변).
4. **별개 작업은 버전 문자열을 반드시 달리한다 (절대 — 사용자 확정 2026-06-19).** 한 세션에 두 작업을 해도 같은 버전으로 두면 ① 검수원이 앱 화면에서 버전업을 **시각적으로 확인 못 함** ② sw.js VERSION이 같아 **폰 캐시가 새 빌드를 안 받음**. 마지막에 올린 것이 항상 최신 버전이 되도록 올린다(예: 부두·주야간=V8.11, 이어서 LOLO=V8.12). 알파벳 접미사(V8.11b 등) 금지 — 규칙은 일단위(작은 변경)·`-NN`(버그)뿐.
5. 캐시 무효화는 문자열이 달라지기만 하면 되므로 빌드 번호로 충분.

**판단 기준(큰/작은 갈림)**: 새 화면·새 흐름·도메인 규칙 추가 = 큰 수정(십단위). 기존 화면에 항목·옵션·표시 추가 = 작은 변경(일단위). 동작 안 바뀌고 결함만 고침 = 버그(빌드번호).

### Ⅱ-2.4b ZIP 파일명 유일화 (사용자 확정 2026-06-19)
- **압축 풀리는 최상위 폴더명·지침서 파일명은 버전 그대로** (`Tallyman_Master_V8.21/`, `…_V8.21.md`) — 사용자가 늘 같은 방식으로 repo에 덮어쓰므로 폴더명은 바뀌면 안 된다.
- **ZIP 겉파일명은 유일해야 한다** — 같은 버전을 또 받으면 파일이 덮여 데이터 보관이 안 된다. 버전 문자열만으로 부족하면(같은 V8.21을 두 번 빌드) **겉파일명에 시각을 붙인다**: `Tallyman_Master_V8.21_20260619_0130.zip`. 폴더명·지침서명엔 시각 안 붙임(겉파일명에만).
- 버그 빌드번호(`-01`)까지 버전에 박히면 ZIP명도 자연히 유일해지므로, 그 경우 `Tallyman_Master_V8.21-01.zip` 그대로 써도 중복 안 남(시각 생략 가능). 같은 버전·빌드를 재패키징할 때만 시각 추가.

### Ⅱ-2.5 ZIP 배포 메커니즘 — ★이 절차를 글자 그대로 따른다(인스턴스마다 다르게 하지 말 것)

배경: 클로드마다 ZIP 만드는 법이 달라 사용자가 반복 고생함(폴더 구조 오해, 루트 빌드본 누락 등). 아래는 단일 정답 절차다. 임의로 바꾸지 말 것.

**핵심 사실 (먼저 이해할 것)**
- repo는 **평탄 구조**다 — `index.html`·`src/`·`assets/`·`dist/`·`sw.js`가 repo 루트에 바로 있다. GitHub Pages는 `repo루트/index.html`을 직접 서빙한다.
- 그러나 **사용자에게 주는 ZIP은 폴더 한 겹으로 감싼다** — 최상위 `Tallyman_Master_Vx.xx/` 하나. 풀면 그 폴더가 나오고, 사용자는 그 폴더 **안의 내용물**을 repo 루트에 덮어쓴다. (사용자 확정 워크플로 2026-06-18.)
- ★**루트 빌드본을 반드시 ZIP에 포함**한다 — `index.html`(빌드본)·`assets/`·`sw.js`·`manifest.webmanifest`·`cone.html`·`cone-sw.js`·`cone-cargoplan.js`·`bulk_tally.html`·아이콘 PNG들·`_index.entry.html`. 이게 빠지면 사이트가 안 바뀐다(과거 사고: V8.09-06 1차 ZIP에서 루트 `assets/` 누락 → 푸시해도 화면 그대로). dist만 넣고 끝내면 안 됨.

**절차 (build.sh가 루트 빌드본을 이미 만들어 둠 — 그 뒤 그대로 담는다)**
```
# 1) 빌드 (루트 index.html·assets·sw.js·cone.html 등을 dist→root 복사까지 자동 수행)
cd <repo> && bash build.sh

# 2) 폴더로 감싸 담기 — repo 루트의 모든 운영 파일을 그대로 복사. node_modules·.git·잡파일만 제외.
VER=$(grep -oE "V[0-9.]+-[0-9]+" src/utils.js | head -1)   # 예: V8.09-06
STAGE="/home/claude/Tallyman_Master_${VER}"; rm -rf "$STAGE"; mkdir -p "$STAGE"
for d in assets dist public src; do [ -d "$d" ] && cp -r "$d" "$STAGE/"; done
for f in index.html sw.js bulk_tally.html cone.html cone-sw.js cone-cargoplan.js \
         manifest.webmanifest icon-192.png icon-512.png cone-icon-192.png cone-icon-512.png \
         mixerUpload-*.js _index.entry.html build.sh package.json package-lock.json \
         vite.config.js tailwind.config.js postcss.config.js README.md \
         평택항_검수_통합지침서_3앱통합본_${VER}.md; do [ -f "$f" ] && cp "$f" "$STAGE/"; done
rm -rf "$STAGE/node_modules" "$STAGE/src/node_modules"
find "$STAGE" -name '*#U*' -delete   # 깨진 파일명 제거(폴더 통째 복사 시 섞여 들어옴)

# 2b) ★옛 빌드 번들 정리 — repo의 assets/·dist/assets/에 옛 index-*.js가 수십 개 누적돼 있음
#     (매 빌드마다 새 해시 생성, 옛것 미삭제 → 안 지우면 ZIP이 77M까지 부풀음).
#     현재 index.html이 참조하는 번들 + css + manifest + pdf.worker만 남기고 나머지 index-*/manifest-* 제거.
CUR=$(grep -o 'index-[A-Za-z0-9_-]*\.js' index.html | head -1)
CURCSS=$(grep -o 'index-[A-Za-z0-9_-]*\.css' index.html | head -1)
CURMANI=$(grep -o 'manifest-[A-Za-z0-9_-]*\.webmanifest' index.html | head -1)
for dir in "$STAGE/assets" "$STAGE/dist/assets"; do [ -d "$dir" ] || continue
  for fp in "$dir"/index-*.js "$dir"/index-*.css "$dir"/manifest-*.webmanifest; do
    [ -e "$fp" ] || continue; fn=$(basename "$fp")
    [ "$fn" = "$CUR" ] || [ "$fn" = "$CURCSS" ] || [ "$fn" = "$CURMANI" ] || rm -f "$fp"
  done
done   # pdf.worker.min-*.mjs 등 그 외 파일은 유지(필요)

# 3) ZIP
cd /home/claude && zip -rq "/mnt/user-data/outputs/Tallyman_Master_${VER}.zip" "Tallyman_Master_${VER}" -x "*/node_modules/*" "*.git*"
```

**검증 체크리스트 (ZIP 만든 직후 전부 확인 — 하나라도 실패면 다시)**
1. 최상위 단일 폴더: `unzip -l ZIP | ... | cut -d/ -f1 | sort -u` → `Tallyman_Master_Vx.xx` 하나만.
2. 루트 `assets/` 포함: ZIP 안 `Tallyman_Master_Vx.xx/assets/index-*.js` 존재.
3. 루트 `index.html`이 그 번들 참조 + 번들에 새 버전 문자열 박힘.
4. `sw.js` VERSION = 새 버전.
5. MD = 현재 지침서 + README.md만. **깨진 파일명(`#U...`)·HANDOFF_*·구버전 지침서·REF_*·*.zip·*.bak·sim_*·repo정리* 제외.**
6. node_modules 없음.
7. ★`assets/`·`dist/assets/`에 `index-*.js`가 **각 1개**만(옛 번들 정리됨). ZIP 크기 정상(3~5M대 — 70M+이면 옛 번들 안 지운 것).
8. (가능하면) 직전 배포 ZIP과 `diff -rq`로 바뀐 파일이 의도대로인지 대조.

**제외 목록 (절대 ZIP에 넣지 말 것)**: `node_modules/`, `.git*`, `docs/`(작업메모 — 배포 대상 아님), 깨진 파일명(`*#U*` — 한글 파일명이 깨진 중복본, repo에 섞여 있음), `HANDOFF_*`, `BUILD_NOTES*`, `*REPORT*`, `EMBEDDED_*`, 구버전 지침서(`*_V8.09-0[2-5].md`·`*_3앱통합본.md` 등 최신 외 전부), `검수앱지침서.md`, `REF_*.md`(살릴 내용은 지침서에 흡수 후 제외), 이전 `*.zip`, `*.bak`, `sim_*.mjs`, `parse_def_*.py`, `repo정리.*`, `aggregate_stats_*.json`. ★`cp -r`로 폴더를 통째 담을 때 그 안에 깨진 파일명이 섞여 들어올 수 있으니, ZIP 생성 후 반드시 체크5b로 `*#U*` 잔존을 확인할 것.

- 사용자 사용법: ZIP을 풀면 `Tallyman_Master_Vx.xx/` 폴더가 나온다. **그 폴더 안의 내용물**(index.html·src·assets·dist…)을 repo 루트에 덮어쓰기 → `git add -A` → commit & push. (폴더째 repo에 넣으면 루트가 안 바뀌어 "변경 없음"이 됨 — 반드시 안의 내용물을 루트로.)
- 캐시 무효화: sw.js는 network-first, VERSION이 바뀌어야 새 버전 인식 + 옛 캐시 삭제.
- **실행 quick start**: `npm install` → `bash build.sh`. (README.md는 이 두 줄 안내만.)
- **ZIP MD 정리 규칙**: ZIP에는 현재 유효한 지침서와 `README.md`(3줄 안내판)만. **살릴 내용은 먼저 이 지침서에 흡수한 뒤 삭제(정보 손실 금지).**

## Ⅱ-3. 컨선 베이 구조 도메인

### Ⅱ-3.1 짝수 단독 베이 (정상 구조)
BOW / STERN / 선원건물 앞 / 선원건물 뒤에서 정상. 한쪽 홀수만 있어도 정상. 짝수 단독을 비정상으로 일반화 금지.

### Ⅱ-3.2 베이별 자체 row 구조 (핵심)
선박 전체 단일 row max 가정 금지. 같은 선박 내에서도 베이/section마다 row 구조 다름.
deck/hold tier도 베이별로 다름 (예: DJCT BAY 27 = deck only). 단일 rowMaxOdd/Even은 부정확.
예: DJCT BAY 01 = rowCount 7, hasZero true / DJCT BAY 07 = rowCount 10, hasZero false / SWAT BAY 17 = rowCount 11, hasZero true.

### Ⅱ-3.3 베이사전 등록 현황
v2 verified 36척, v5 매트릭스 300척. v2/v5 모두 베이별 row 라벨 정보 일부 누락.

### Ⅱ-3.4 PDF override 사전
src/data/shipBayDict_pdf_override.js. 베이별: rowCount, hasZero, deckTiers, holdTiers, (권장) deckCells, holdCells.

### Ⅱ-3.5 ISO 6346 사이즈
첫 자: 2=20ft, 4=40ft (45R1 = 40ft hi-cube reefer), L/9=45ft. ⚠️ 45R1을 45ft로 분류 금지.
- 둘째 자리=높이(2=8'6", 5=9'6" HC). 길이 판정은 라벨(isoToLabel) 기준이 정답. 45GP→40HC, 45R1→40RF. 진짜 45ft는 L5xx.

### Ⅱ-3.6 cells = 00 포함 개수 (핵심)
- 사용자 매트릭스 cells = **00 포함 전체 칸 수**. 데크00 체크 = cells 안에 이미 00 포함.
- ⚠️ `+1` 금지 (00 두 번 세면 없는 09가 생김).
- ⚠️ 개수로 row 번호를 생성하지 말 것. 실제 row 라벨을 쓴다. ("갯수 보지 말고 실제 row 번호를 봐라")

## Ⅱ-4. 카고플랜 V2 약속

### Ⅱ-4.1 베이 구조
- **베이플랜은 좌표(absolute) 기반 — 격자(flex) 금지**: 격자는 한 칸 빠지면 옆 셀이 밀림. 각 셀이 자기 (row,tier) 좌표 자리를 가져야 함. 데크/홀드는 끝까지 따로 관리.
- **cells 해석을 베이플랜·카고플랜 간 함부로 통일하지 말 것**: 두 화면은 렌더 모델이 달라(좌표 vs flex) 한쪽 기준으로 "통일"하면 다른 쪽이 회귀. 통일 필요 시 실데이터 렌더로 양쪽 검증 후.
- PDF STOWAGE INSTRUCTION 정답 그대로. 베이마다 자체 deck/hold tier + row. 단독 베이 hold 없으면 hold 영역 없음. A4 한 장. 좌측 하단 mini-legend.

### Ⅱ-4.2 마크 약속
(a) PTK 일반 = 선사별 컬러 + o / (b) PTK 특수(R/D/F/T/A/E) = 글자 + 컬러 / (c) 통과 일반 = 회색 배경, 글자 없음 / (d) 통과 특수 = 글자 + 회색 / (e) 빈 슬롯(hull 안쪽) = border만 / (f) 짝수 40·45ft shadow = X(흰 배경) / (g) 짝수 20ft shadow = 회색 빈 셀(자리 차지) / (h) Hull 바깥 = 안 보임(visibility:hidden).
- ⚠️ X는 짝수 40/45ft shadow 전용. 통과화물 표시로 쓰지 말 것.
- ⚠️ **셀 배경색으로 XRAY·선사를 구분하지 말 것 — 구분은 글자(표기) 색으로만** (배경색이 XRAY 색과 혼동돼 작업 중단 유발).

### Ⅱ-4.3 컬러 시스템
양하 모드: 선사(c.op)별 10색. 선적 모드: POD 3자별. ⚠️ c.cn 첫 3자 BIC 코드 = 선사 X. 사용 금지.

### Ⅱ-4.4 Hold 중앙 정렬
CSS: width (nHoldCols/nDeckCols)*100% + margin auto. 1칸 차이 → 좌우 0.5칸씩.

### Ⅱ-4.5 Layout 공식
N개 박스 → 상단 = ⌈(N+1)/2⌉, 하단 = N − 상단, 별첨 자리 = 차이.
- **【확정】 hold 유무와 상관없이 베이 번호 순서가 절대 우선.** deck-only라고 순서를 깨고 내리면 BAY 01이 하단으로 밀림(TMPZ 재현). SWRG의 33·35·38이 하단인 것은 번호가 커서이지 deck-only라서가 아님 — 과잉 일반화였음. (deck-only 하단배치 규칙은 폐기됨.)

### Ⅱ-4.6 카운트 포맷
단독 = 총합 1숫자. 페어 = 20'/40'/45' 슬래시. 양하 = 평택분만.

### Ⅱ-4.7 인쇄 처리
height 195mm 고정. page-break-inside: avoid. body 직접 마운트(createPortal). display:none + bg white.

## Ⅱ-5. 데이터 검증·명칭 원칙

### Ⅱ-5.1 출처 우선순위 (절대 — 사용자 확정)
`사용자 매트릭스(검증) > PDF override > .def 사전(미검증) > v5_matrix.matrixBays > v2 baysSummary > EDI 실데이터`
- 근거: 사용자가 직접 검증한 데이터만이 확정. .def 사전은 자동 디코드일 뿐 사용자 검증을 거치지 않았으므로 사용자 매트릭스를 절대 덮지 못한다.

### Ⅱ-5.2 v5_matrix.cells 주의
순서 아래→위 → reverse() 필수. v5가 PDF와 불일치 가능. 일부 선박 v5 없음.

### Ⅱ-5.3 EDI 검증
has_zero = EDI rows에 0 있는지 직접. 추측 금지, 실데이터 보고 판단.

### Ⅱ-5.4 CASPI가 정답 기준
CASPI를 따른다. 잘못된 명칭 사용 금지. 사용자가 실제 쓴 약자·명칭만 인용.

### Ⅱ-5.5 선사 약자 3자 (영구 규칙)
선사 약자는 항상 3자. 4자 코드는 변환표 경유 3자(단순 slice 금지).
예: DJSC→DJS, NSSL→NSL, HASL→HAS, SNKO→SKR, EASK→EAS, TJMS→TJM, WDFC→WDF, DWIC→DWS.

## Ⅱ-6. userBayDict / 매트릭스 보호 (절대 원칙)
- **userBayDict 절대 보호**: source !== 'user'일 때만 mergeBayDef. 사용자 입력을 자동분석(v5/v2/EDI)이 덮어쓰지 않는다.
- **베이매트릭스가 기본 진실**: 매트릭스에 deck00/hold00이 명시되면 EDI가 덮어쓰지 않는다.
- **Rules of Hooks**: 모든 Hook은 최상단. 조기 반환문은 모든 Hook 선언 이후.
- **음성 출력 표준**: 컨테이너 번호는 한 글자씩, 베이·온도는 한글로 또박또박.
- **데크/홀드는 항상 별도 관리(축 통합 금지)** — 통합 시 phantom 00 생성.

## Ⅱ-7. 운영 핵심 원칙 (V7.x 확립)

### Ⅱ-7.1 평택분만 계산
- 항상 평택분만: 양하 = POD가 평택, 선적 = POL이 평택.
- 평택 판정 변종 포함: `PTK / PYT / PYOTM / PYO`로 끝나면 평택. **isPyeongtaekPort() 단일 함수로 통일** (적재·집계·모드판정·진단 전 화면이 이 함수 하나만 사용. firebase 한글 항구명 케이스만 예외).
- ⚠️ 폴백 금지: 평택분 0이면 0이 정답. 전체로 넘어가 타지역까지 세지 않는다.
- ⚠️ **모드별 판정 필수**: `isPyeongtaekPort(c.pol) || isPyeongtaekPort(c.pod)` 패턴은 모드 무관이라 버그 — 양하 EDI의 평택발 타항행(POL=PTK)이 평택분에 섞임. 이 패턴을 보면 전수 grep (과거 5곳에 복제돼 있었음).

### Ⅱ-7.2 데이터 유실 방지 (archive)
- 완료 시 `archive/{voyageKey}`에 실데이터 전체 백업 → 백업 검증 성공 후에만 삭제(fbDeleteVoyage). 백업 실패 시 삭제하지 않는다.
- 복원: fbRestoreVoyageFromArchive. 1년 후 자동 정리: fbCleanupArchive.

### Ⅱ-7.3 이중 확인 (검수사 → 수석)
- 검수사가 완료 누르면 `inspectorDone` 표시만(삭제 안 함). 수석 확인 전 검수사 취소 가능.
- 수석이 대시보드에서 최종 "완료 저장" 누를 때만 archive 백업 + 보관소 기록 + 삭제.
- 목적: 2명이 나눠 작업 중 한 명이 자기 자리 완료를 전체 완료로 잘못 눌러 삭제되는 사고 방지.
- **완료 권한 보유자 7명**: 이현규·김명보(부장 수석검수), 오승택(차장 수석검수), 성창모·이강익(과장 수석검수), 전우수(대리 수석검수), 김성일(대리 부수석). isChief 정규식은 `/수석검수|부수석/`.

### Ⅱ-7.4 라이브러리 / 보관소 분리
- 선박 라이브러리(진행 상황) = 현재 살아있는 voyages만. 완료 안 한 배만 보임. 수석용 [완료 저장] 버튼.
- 선박별 자료 보관소(완료 기록) = ships 노드, 최근 완료순(completed_at desc). 항차별(양하/선적/일자) + 누적.
- 보관소 제목은 사용자 약자(vsl, 예 PCBJ/TNJP) 우선.

### Ⅱ-7.5 계열 대체 시 신원 금지
- 베이 구조 없어 다른 선박 사전을 빌릴 때(series-substitute), 베이 구조만 빌리고 콜사인·선박명은 빌리지 않는다.
- ⚠️ 앞 2글자만 같으면 계열로 보는 대체는 느슨함. 무관한 선박 신원(콜사인)이 섞이면 PORT-MIS 오매칭(DJCT에 XIN TAI PING 표시된 버그).

### Ⅱ-7.6 알려진 운영 이슈
- **런타임 에러 발생 시 카메라 작동 멈춤** — 에러 핸들링/카메라 복구 작업 시 반드시 고려. (V7.35 루트 ErrorBoundary로 완화: main.jsx에서 `<ErrorBoundary reloadButton>`로 App 전체를 감쌈.)

## Ⅱ-8. 검수앱 작업 이력 (최신 → 과거)

> 콘앱(cone.html) 단독 작업은 Ⅲ부에, 검수앱+콘앱 동시 작업은 양쪽에 요약 표기한다.

### V9.23-01 (2026-07-29) — 재빌드: V9.23 번들이 옛 코드 재포장이었던 사고 교정
- 사고: V9.23 2차 빌드 때 `_pre.sh`(진입 소스형 교체)를 건너뛰고 vite를 돌려, 루트 index.html(빌드본)이 가리키는 **옛 번들을 진입으로 재번들** — 배포본에 mkcon은 있었으나 가상 EDI 병합 수리(OBWH)가 빠짐. 사용자 화면 실측(OBWH 195 전부 미정)으로 발각, 번들 `_virtualEdi` 4회(소스는 5회) 대조로 확정.
- 교훈(비협상): **재빌드는 반드시 진입 교체부터** — `bash build.sh` 전체 흐름이거나 최소 `cp _index.entry.html index.html` 후 vite. 빌드 후 번들 검증은 문자열 유무가 아니라 **소스 대비 발생 횟수 대조**로 한다(있던 문자열은 옛 번들에도 있다).
- 검증: index-fc4jgw2P.js — _virtualEdi 5회·CORE_FILL 조건문 실재 확인.

### V9.23 (2026-07-29) — 제작컨테이너(mkcon) 분류 신설 — 리퍼드라이와 별도 (사용자 확정)
- 발단: R080E 풀리퍼 34 중 HSAP 10대 온도 미입력 경고. 전 자료 교차 대조(EDI 159컨·리퍼리스트 24행(번호 21~30 건너뜀)·CNTR_List·PORT-MIS 신고 리스트) — 어디에도 HSAP 온도 없음. 사용자: "10대는 특수제작 컨테이너입니다. 자체가 상품인 것이죠 내용물은 없는" + "리퍼드라이는 내용물이 상품, 특수제작컨은 컨테이너 자체가 상품 — 따로 분류해야".
- 명칭: **제작컨테이너**(필드 `mkcon`). 리퍼드라이(rfdry)와 완전 별도 분류.
- 온도 미입력 판정 전 지점(diagnostics 빨간배너·StatsTab·VoyageSummaryCard·WorkClosingChecklist·출항배너·reeferTemp 필터·nlSearch 브리핑·CSVExport)에서 `!c.mkcon` 제외 + isISO403(풀리퍼 사진) 제외.
- parseListExcel: REMARK '특수컨/특수제작/제작컨' 자동 감지 → mkcon=true, tmp_missing 억제. ALLOWED_LIST_FIELDS에 'mkcon'.
- UI: BigResultCard 보라 배지 "🏭 제작컨 (컨 자체가 상품)"(리퍼 여부 무관), 상세 모달 표시("온도 없음 정상")+「제작컨 지정/해제」 토글(fbUpdateRecordField 'mkcon'), 항차 요약 카드 "🏭제작컨N", 음성 "제작 컨테이너"(리퍼 낭독 대체), nlSearch 특이사항 줄.
- 시뮬(실데이터): 실제 CNTR_List_R080E_...xlsx(159컨) → parseListExcel: HSAP 10/10 mkcon=true, 오탐 0, 일반리퍼 무영향, 온도경고 제외/일반경고 유지 PASS. 기존 판정식엔 `!c.mkcon` 추가뿐이라 기존 데이터 무변화(구조적 회귀 안전).
- 데이터: R080E 양하 HSAP 10대(HSAP6240018/6240023/6320176/6320181/6320197/6320200/6340110/6340125/6340130/6340146, 42RF, M-B/L SPGRZHPTK053334, 실번호 000578~000590) mkcon=true 패치 → "10대 온도 미입력" 경고 해소.
- 부수 확인: 수집기 v2.17.17-01(EDI 부분정정 가드)·-02(rzdf 수집 키워드) 반영분, RZOR R080E EDI 159컨 복구 사건(2컨 정정본 53303이 최신 채택되던 것 → 가드), 리퍼리스트 수기 투입분 온도 24대 records 패치 — 이번 판에 이력 흡수.
- 후속: 수집기에서 CNTR_List REMARK '특수컨' 자동 감지(현재는 앱 리스트 업로드 시 자동), 덱 플랜 셀 제작컨 표기.
- ⭐ **동판 동승: 가상 EDI 병합 수리 (OBWH 2699E 사용자 신고 "풀엠티·규격 다 있는데 적용 안 됨")**
  원인: 실 EDI 없는 선적(2700WLOADLIST)은 리스트로 가상 EDI 스텁(_virtualEdi, cn/pol/wt뿐)을 만드는데,
  화면 병합에서 fe/iso 등 핵심 필드는 ALLOWED_LIST_FIELDS에 없어 records의 정상값(F15/E180·규격 전수)이 버려짐 → 화면 미정/기타.
  수정: 병합 시 스텁(_virtualEdi)이거나 EDI 값이 빈 핵심 필드(fe/iso/tp/rf/fr/ot/tk/dg/dgc/un/pod/tmp_missing)는 리스트가 채움.
  실 EDI에 값이 있으면 기존 원칙 그대로 EDI가 진실(덮어쓰기 불가 — 구조적 회귀 안전).
  시뮬(실데이터 OBWH_2699E 195컨): F15/E180·미정0·리퍼36 복원 PASS. 잔여: ZXJU0130463 1대는 리스트 원본에 규격 없음(현장 확인 대상).

### V9.22-02 (2026-07-29) — 덱 플랜 빈자리 + 현장 지정 (사용자: "로딩 시 빈자리를 알아야")
- 파서: 데이터 구역 내 글자 없는 병합 = 빈자리(R080E: D101·C79·B14). 회색 solid 채움 = 적재불가 구역 제외
  (cellStyles:true 필요 — 스타일 없으면 빈자리로 안전측). 총계·완료 분모는 컨 전용(빈자리 제외).
- 빈자리 셀: 점선 테두리 '빈자리' — 탭하면 컨번호(전체/끝4) 입력해 📌지정(stowagePlan/assign/{덱-행-열}), 재탭 해제.
- R080E 플랜 재주입(컨 160 + 빈자리 194).

### V9.22-01 (2026-07-29) — 덱 플랜 셀 정보 보강 (사용자: "컨테이너 정보가 부족")
- R080E 리스트 실측: records 160(실번호 160·리퍼온도 24·POD 전수) — 플랜 셀이 안 쓰고 있었음.
- DeckPlanView 셀: 끝4+완료✓+❄온도+⚠DG+긴급/활어 · 규격+F/E(리스트 우선) · 🔒실번호(엠티실 폴백). 행높이 58px.

### V9.22 (2026-07-29) — RZOR 덱 스토우지 플랜: LOLO 선박 카고플랜 신설 (사용자 요청)
- "이걸로 카고플랜을 만들 수 없을까요?" — 선사 rzdf_ship_*.xls(시트 A~E-DECK-PTK, 컨=병합블록
  "컨번호/무게/40 HC F/긴급·활어")를 파싱해 베이 없는 RZOR에 덱 그리드 카고플랜 제공.
- src/rzorPlan.js: SheetJS 병합 기반 파서 — colStops/rowBands 좌표 정규화. R080E 실측 완전 일치:
  D 75(20×4/40×68/45×3)·C 71·B 14 = 160, PDF SUB TOTAL 그대로.
- 업로드 감지: 개별등록에서 -DECK 시트 워크북 → stowagePlan 저장(리스트 파싱 우회). LOLO 탭 상단 DeckPlanView:
  덱 칩·CSS grid·완료 초록·리퍼 청록링·X-RAY 노랑링·긴급/활어 배지·셀 클릭→상세.
- R080E는 파싱분을 Firebase에 직접 주입(총 160 확인) — 배포 즉시 표시.
- 원본 확보처: 수집기가 이미 rzdf 메일 자동 수집(MAILBOX/RZOR/R080E 실측) + 수석 마감 PLAN.xlsx 매 항차.
- 남은 것(후속): 인쇄용 밝은 테마, 수집기 자동 등록 연동(현재는 개별등록 업로드/직접 주입).

### V9.21-04 (2026-07-29) — 페리 주야 분해: 일괄마감 감지 (26353 최종 마감 실측)
- 실측: 양하 완료체크 149대가 18시대 10분 스팬(마감 일괄처리) → 주0/야149 오분해. 선적은 18~22시 실시간(스팬 220분).
- 규칙: 완료 시각 스팬 30분 미만 && 20대 이상 = 일괄마감 → 주/야 빈칸(수기), 총계만 기입. 실시간 체크 항차는 자동 분해 유지.
- 한계 기록: 수석 주야는 크레인 작업시간 기준(16:30~17:00 주간 10대) — 검수 체크 시각과 구조적으로 다를 수 있음(수기 보정 영역).
- 정정: V9.21-03의 '20피트 DG 1대 미결'은 사용자 재확인으로 없음 확정 — 40' 33대가 전부(현재 데이터와 일치).

### V9.21-03 (2026-07-29) — 리스트 REMARK 자유텍스트에서 DG·리퍼온도 감지 (사용자 지적 정정)
- 정정: 수석의 ( DG x 33 ) 출처는 PDF가 아니라 **CNTR LIST의 REMARK 열**("RF +23  IMDG 9 UN_CD 3480" × 33)이었다.
  파서가 IMDG '컬럼'만 찾고 REMARK 자유 텍스트를 안 읽어 dg=0으로 등록되던 것.
- parseListExcel: REMARK/비고 열에서 IMDG·CLASS(→dgc)·UN번호(→un)·RF 온도(→tmp, 빈 경우만) 추출. dg 자동 마킹.
- 26353E는 보관소(archive) 이관 후라 archive/…/discharge 양 노드에 직접 PATCH(있는 컨만·병합 검증).
- ⚠ 사고 기록: 항차가 마감→보관 이관된 줄 모르고 /voyages에 패치 → 빈자리에 33대 유령 노드 생성 + 백업 파일을
  재조회로 덮어씀. 유령 노드 삭제로 정리. **교훈: DB 패치 전 항차 존재(shallow)·보관 여부 확인, 백업은 별도 파일명.**
- 시뮬(보관본): 36 어서션 PASS — OS-IN 40F 'HC x 85 , RH x 40 ( DG x 33 )' 포함.
- 미결: DG 20피트 1대(사용자 확인)는 리스트 REMARK에 표기 없음(연운항 D/G 리스트 PDF에만) — CN 확보 시 반영.

### V9.21-02 (2026-07-29) — 페리 OS-IN/OUT 수석 규칙 반영 (실물 셀 대조)
- 사용자: "수석 텔리와 다르다 — 40피트에서 리퍼/드라이 구분, ( ) 안 DG 수, 구분·순서 틀림."
- 수석 규칙(26353 실물 확정): 행은 **20'/40'/45' 고정, F→E 순** (40HC·43·45Gx는 전부 40'행 합산,
  45'행은 L5/9x만). 분해는 REMARKS: `HC x n , RH x n ( DG x n )` (HC=하이큐브 드라이, RH=리퍼, 일반 40DC는 미언급).
  포트(MARKS)는 첫 행만 — IN=POL, OUT=POD.
- 구현: manifest osFerryIn/Out(고정행) + buildFerry.os(길이 3단×F/E×{n,hc,rh,dg}) + fillFerrySheets OS 채움.
- exceljs 함정 추가 실측: **병합 슬레이브에 쓰면 마스터가 지워진다** — TOTAL H:I에서 9열 null 쓰다 H가 날아감. 마스터만 쓸 것.
- 시뮬: 수석 오라클 12 어서션 PASS (IN 22/127'HC85,RH40'/149 · OUT 13/20/49'HC41,RH8'/159'HC100,RH39'/15'HC15'/256).
- 미결: 양하 DG 33은 연운항 D/G CARGO LIST(PDF)가 앱에 미반영이라 괄호 생략됨 — PDF 파싱은 다음 과제(7부 기록).

### V9.21-01 (2026-07-28) — 통풍컨(Ventilated) 인식 + 미지 규격 터미널 교차확인 버튼
- 사용자 실측(OBWH 2699E): 22V7 2대가 '알 수 없는 규격' 경고 — PNCT 터미널 조회로 20/DC(2210) 확인.
- isoToLabel: V타입 → 20VH/40VH 신설, isUnknownIso 화이트리스트 등재, ContainerList는 DC로 집계(기타 ISO 칩 방지).
- 상세 모달 미지 규격 박스에 「🔎 터미널 조회」 버튼 — 컨번호 클립보드 복사 + PNCT 정보서비스 새창
  (CORS 때문에 앱이 직접 조회는 불가 — 사용자 확인 워크플로를 반자동화).

### V9.21 (2026-07-28) — TNJP 여객선(카페리) 마감텔리: 페리 바우처 변형 신설
- 사용자: "TNJP 마감텔리 리퍼시트 이상·양식 틀림. 여객선은 일반선박과 다르다" — 실물(26353E&W, 오늘 22:17 메일)로 확정:
  선사별이 아니라 **규격군(20 vs 40/HC/45) × F/E × 수화물(Lug) × 주간/야간(0700~1700)** 바우처 + RF 3페이지 + PORTPERFORMANCE 매트릭스.
- 구현: TNJP.xlsx 페리 템플릿(실물서 값만 비움) + manifest variant:'ferry'(FW inRows/outRows·rfFerry pages·ppFerry 좌표) +
  buildFerry(주야=완료시각, Lug=forecast.lugg) + fillFerrySheets. OS-IN/OUT·Seal은 표준 로직 재사용(라벨 기입기 헤더).
- RF 페리 규칙(실물): **양하 F 리퍼만** 기재(공리퍼·선적 리퍼 미기재), 열=CN/실/규격/위치/설정온도/REMARKS(DG).
- ISO 43xx 충돌 실측: 연운항(LYG)은 43=40HC, DXQD 실물은 4300=40' — **전역 규칙 아님**, 페리 전용 분류로 한정
  (리스트 ISO 우선: EDI는 40군을 43으로 통칭). DXQD 회귀 234/1/24 원상 확인.
- 검증: 실물 오라클 24 어서션 PASS — IN 22/2/125=149 · OUT 13/49+20/20/154=256 · RF 40대 3페이지 배치 · PP 매트릭스 · DJCT/DXQD 회귀 5/5.
- 수집기 마감텔리 수확 스크립트에 cp949 로그 함정 2건 기록: 제목의 사설영역 문자·본문 em-dash — log는 errors='replace' 필수.

### V9.20-04 (2026-07-28) — 리퍼드라이 판정처 마감 (경고 잔존 신고)
- V9.20-03 후에도 빨간 배너 잔존 — 원인 2중: ① 배너의 출처는 **diagnostics.js**(진단 위젯)로 별도 판정처
  (V9.20-03은 5곳만 수정). ② records의 rfdry가 VoyagePage **ALLOWED_LIST_FIELDS 화이트리스트**에 막혀 화면 미전달.
- 수정: diagnostics(rfdry·lr.rfdry 제외)·nlSearch 브리핑/특이사항·CSVExport·화이트리스트 'rfdry' 추가.
- **온도 미입력 판정처 전체 지도(9곳 — 재발 방지용)**: diagnostics.js(빨간 배너)·StatsTab·VoyageSummaryCard·
  WorkClosingChecklist·VoyagePage 출항배너·VoyagePage reeferTemp 필터·nlSearch 브리핑·nlSearch 특이사항·CSVExport.
- 수집기 v2.17.17 추가 보강: 컨번호 정규식 경계 추가 — 실메일 BL번호(SNKO0202607…)가 4영문+7숫자에 걸리던 오탐 차단.
- PCSZ 2622E 실컨 3대(TRIU6734869·SEGU9996257·SKRU1900037) rfdry Firebase 직접 반영(전원공급 금지, UN1845 드라이아이스).

### V9.20-03 (2026-07-28) — 리퍼드라이(넌플러그) 개념 신설: 앱 + 수집기 v2.17.17 (PCSZ 2622E 사건)
- 사건: 선사가 본문-only 메일로 "넌플러그 3대" 요청 → 첨부 없는 메일이라 수집기가 통째 무시(수집기록에 없음)
  → 앱은 풀 리퍼 3대 온도 미입력 경고. 사용자: "앱뿐 아니라 수집기에도 리퍼드라이 적용해야".
- 앱(V9.20-03): 레코드 플래그 `rfdry`.
  · 온도 미입력 판정 5곳(Stats·요약카드·마감체크·출항배너·reeferTemp 필터)에서 제외, isISO403(풀리퍼 사진) 제외.
  · 상세 모달 온도 행에 「리퍼드라이 지정/해제」 토글(fbUpdateRecordField 'rfdry') + 🔌 표기.
  · 검수 카드: 경고 대신 청록 「리퍼드라이 (넌플러그)」 배지. 음성: "리퍼드라이 넌플러그". 요약 칩 '🔌드라이n'.
- 수집기 v2.17.17: 리퍼온도(v2.17.13) 패턴 복제 —
  · 본문 키워드(넌플러그·NON-PLUG·리퍼드라이·DRY REEFER) + 컨번호 추출 → {voy}_리퍼드라이.json 합집합 기록.
  · 즉시 push + 합본 사이클 재적용: ediContainers·records 양쪽 '있는 컨만' rfdry=true PATCH (멱등).
  · 파서 시뮬 3케이스 PASS(한글 본문·키워드 없음 오탐 차단·영문 NON PLUG).
- 2622E 3대는 CN 미상(본문 미보존) — 다음 수집 사이클 자동 or 앱 토글 or CN 제보 시 즉시 패치.

### V9.20-02 (2026-07-28) — 합본 등록 F/E·ISO 소실 수리 + 엠티실 표시·음성 (26353W 사건)
- 사건: TNJP 26353E(선적 26353W) 256대 전원 'F/E 미정 + 기타 ISO'. 원인 확정 경로:
  선사 리스트·파서 모두 정상(F62/E194) → Firebase 실데이터 확인 → records 출처가 수집기 합본
  `26353WLOADLIST.xlsx`(_kind='merged')이고, **합본 전용 매핑(V8.32-01)이 컨번호·Seal·Weight만 옮김**.
  합본에는 ISO·F/E·Line·POL·POD 열이 있음(merge_helper.html 실측: Cntr No|ISO|Line|F/E|POL|POD|Seal|Weight|XRAY).
- 수리: ① 합본 매핑에 ISO/F/E/Line/POL/POD 이관 + 엠티면 Seal→eseal 복사.
  ② 26353E는 Firebase REST PATCH로 즉시 복구(1,784필드, 샌드박스 curl 4조각 — blob 검증식 카운트 재확인).
- **엠티실 표시·음성**(사용자 요청): 엠티+eseal(≥4자)이면 검수 카드에 '엠티실'을 청록 대형 표기,
  음성도 "엠티실 …" 낭독(기존: "엠티 실번호 없음 정상"으로 무시). BigResultCard·voice.js.
- 교훈: 리스트 화면 이상은 파서보다 '등록 경로'(자동등록 합본/개별등록/EDI)부터 확인 — 출처는 records._source에 있음.

### V9.20-01 (2026-07-28) — 프로파일 뷰: 40/20 구분 + 특수화물 아이콘 (사용자 피드백)
- "다른 검수원은 40/20 구분이 어렵다" → x축을 20ft 슬롯(홀수 베이)로 깔고 짝수 베이(40ft)는
  **두 슬롯을 차지하는 넓은 칸**으로 — 물리 배치 그대로라 설명 없이 읽힘. 하단 라벨은 홀수 슬롯 번호.
- 상단 여백에 베이별 특수화물 아이콘: ❄RF ⚠DG ⊞FR △OT ▣TK (있는 것만, 최대 3종 쌓기) + 범례.
- 겹침 방지: 빈 격자(슬롯) → 40ft 넓은 칸 → 20ft 순서로 그림. 빈 (베이,티어)는 배경만.
- 시뮬: DJCT EDI — 짝수 베이 스팬 슬롯 자동 보충(4→3·5, 28→27·29) 정합, RF 24·DG 24 집계 PASS.

### V9.20 (2026-07-28) — V1 카고플랜 삭제 + 3D를 '배 옆모습 프로파일 뷰'로 교체
- 사용자 결정: 기존(V1) 카고플랜 완전 삭제 — PrintableCargoPlan.jsx(1,341줄)·BayPlan 인쇄 메뉴 버튼·렌더 블록·
  PrintHub 임포트 제거 (M6.93.11의 폐기 결정을 물리 삭제로 완결). 인쇄는 V2 단일화.
- 3D 카드뷰(BayPlan3D, 원근 2도 기울임)를 **ShipProfileView**(SVG 종단면)로 교체 — 방향은 사용자 선택(프로파일).
  x=베이(선수 오른쪽), y=티어(데크/홀드 해치라인 분리), 셀=(베이,티어) 집계: 숫자=컨 수, 초록=완료,
  모드색=미완료(양하 파랑/선적 주황), 회색=통과화물, 노란 테두리=XRAY 포함. 베이 클릭 → 2D 해당 베이로 점프.
  사전 tiers가 진실, 사전 없으면 tier>=60 데크 폴백. 토글 라벨 '3D' → '⛴ 측면'.
- 함정 기록: pages의 evenBay/oddBay는 문자열(keyBay=String) — 숫자 비교 시 parseInt 필수.
- 시뮬: DJCT 0221W EDI 437컨 전수 셀 배치(합 일치)·데크 82~88/홀드 02~08 분리·평택분 216 일치 PASS.

### V9.19-11 (2026-07-28) — OS-OUT 오버플로 3중 버그 수리 (사용자 실파일 재현)
- 증상(KKAK 실생성): 첫 데이터 행 실종·TOTAL 실종·REMARKS 라벨 이상 위치·리마크 내용 어긋남.
- 원인 3중: ① 원본 배(DJCT)가 숨겨둔 데이터 행(OS-OUT 12행)에 첫 행을 기입 → 안 보임.
  ② exceljs duplicateRow가 아래쪽 REMARKS 병합 행을 파괴(실측 — 텍스트 소실).
  ③ 행 삽입 후 REMARKS 기입 좌표를 보정하지 않아 옛 좌표에 기입.
- 수리: duplicateRow → insertRows(위 행 스타일 상속)로 교체, 기입 구간(dataStart~TOTAL) 숨김 해제,
  리마크 좌표 +ins 보정. 재현 시뮬: 데이터 12~16·TOTAL 17·REMARKS 29·내용 30 정상.

### V9.19-10 (2026-07-28) — 워킹피리어드 '00:00' 유령 표시 정리
- Date 잔재 청소 후에도 =E35 체인·SUM 수식이 빈 입력에 0을 그려 '00:00 HRS'로 보임(PC 육안).
- 빈칸에서 공백인 IF(ISBLANK…) 수식만 남기고 참조 체인·SUM은 제거 — 수석 수기 입력 시 시간 자동계산은 유지.

### V9.19-09 (2026-07-28) — 워킹피리어드 시각 잔재 마감
- PC 육안 확인에서 워킹피리어드 시각(08:30 HRS 등) 잔재 발견 — exceljs가 시간 서식 셀을 **Date 객체**로 읽어
  숫자/문자열 청소를 빠져나감. Date 타입도 청소 대상에 추가.
- 이로써 화면 공유 검수 지적 전항목 완료: FW 구분선·헤더 전시트·RF D:E·Perf 병합/####/잔재·OS B:C·H:I.

### V9.19-08 (2026-07-28) — 화면 공유 검수 반영: 병합 복원 3종 + 헤더 기입기 마감
- 사용자와 화면 보며 시트별 검수 (예시 병합을 직접 만들어 보여줌 — 이 방식이 정확했다):
  1) **RF**: LOCATION D:E 병합 (데이터 행 전부, 실물 2·3페이지와 동일).
  2) **Performance**: 데이터 행마다 선사명 D:G(4칸)·규격별 2칸씩(H:I…V:W) 병합 복원 —
     좁은 한 칸에 숫자가 몰려 ####로 보이던 것과 격자선 어긋남의 원인이 병합 소실이었음.
  3) **OS-IN/OUT**: MARKS(PORT) B:C·MANIFESTED H:I 병합 복원.
  ※ 공통 함정: exceljs mergeCells는 범위에 마스터 스타일을 덮어씀 → 반드시 저장→병합→복원.
- 헤더 기입기 마감(동판): richText 라벨 보호·페이지 사본 헤더 청소
- PC 육안 검증 추가 발견 2건: ① TS의 BERTH 라벨이 richText라 값 셀 탐색이 문자열로 못 보고 덮음(B8 비고 C8 침범).
  ② Seal 41행·RF 50/93행 등 2·3페이지 사본 헤더는 1~9행 스캔 밖이라 원본 배 잔재(DONGJIN·0221W·46222) 잔존.
- 수정: 후보 셀 텍스트 추출에 richText 지원 + 전 행 스캔(콜론 필수·kindOf 패턴·60자 제한으로 오인 차단).
- 최종 검증: 워크북 전 셀 잔재 스윕 0건 · TS B8/C8/D8 정상 · FW C8/F6 정상 · 변형 6/6 · 회귀 5/5 · NaN 0.

### V9.19-07 (2026-07-28) — 헤더 기입기 오작동 즉시 수정 (V9.19-06 사고)
- V9.19-06의 fillAllHeaders가 PC 육안 검증에서 두 곳 오작동: ① FW 표 열머리 'PORT'(B8)를 라벨로 오인해
  C8(FULL/EMPTY)을 'PYEONGTAEK, KOREA'로 덮음. ② PIER 값 셀 탐색이 옆의 'BERTH :' 라벨 셀(F6)을 값 자리로 오인해 덮음.
- 수정: 라벨은 **콜론(:) 포함 필수** + 값 셀 탐색 중 다른 라벨(콜론 포함) 도달 시 즉시 기본 칸(라벨 바로 오른쪽)으로.
- 교훈(재발 방지): 시각 배치가 걸린 변경은 어서션 시뮬만으로 배포 금지 — PC 육안 확인을 관문에 포함할 것.
- 시뮬 17 어서션 + 변형 6/6 + 회귀 5/5 PASS 후 배포.

### V9.19-06 (2026-07-28) — 마감 텔리: 선사 구분선·헤더 전시트 기입·퍼포먼스 잔재 청소
- 사용자 실물 검수 4건 반영:
  1) **FW 선사간 구분선** — 템플릿 선 패턴은 원본 배 블록(DJCT 6행) 기준이라 다른 배(2행 블록)에서 어긋남.
     실물 규칙(실측: F행=hair·짝끝=thin·선사끝=medium)을 실제 블록에 맞춰 다시 그림.
     ⚠ exceljs 함정 추가 실측: 파싱된 셀들은 style 객체를 공유 — 그대로 대입하면 뒤 행 지정이 앞 행을 덮는다. 셀별 딥클론 필수.
  2) **전 시트 헤더 자동 기입**(fillAllHeaders) — 템플릿에 남은 원본 배 잔재(SHIFTING의 DONGJIN 등) 제거.
     1~9행 라벨(M/V·VOY·DATE·PIER·BERTH·PORT) 스캔, 합성 라벨은 콜론 뒤 교체, 값 셀은 라벨 오른쪽 첫 병합 마스터/기존 값 셀
     (Performance O6 라벨↔R6 값처럼 떨어진 레이아웃 대응). 수식 캐시 잔재도 리터럴로 덮음 → 날짜 시리얼(46222)이 좁은 칸에서 ####로 보이던 것도 해소.
  3) **Performance 잔재 청소** — X(TOTAL)열·AA(REMARKS)·SHIFT행·워킹피리어드(크레인·시각·비고) 원본 배 기록 제거.
     X열·S-TOTAL·GRAND TOTAL은 우리 값으로 재계산(병합 슬레이브 중복합산 주의, result:0은 exceljs가 떨굼 → 0은 리터럴).
  4) 워킹피리어드는 틀 유지·내용만 비움(수기 기입) — 빈 자료 공간확보 규칙 준수.
- 시뮬: 변형 6/6·회귀 5/5·헤더/구분선/퍼포먼스 21개 어서션 PASS·NaN 0. FW 선 대조는 재도색 설계상 실물 diff 무의미(시각 검증으로 대체).

### V9.19-05 (2026-07-28) — 마감 텔리: 엑셀 '복구' 경고 근절 (PC 엑셀 실검증 완료)
- 사용자 보고 재조사: "내용 지운 곳 선까지 지워짐" — 실은 **엑셀이 파일을 '복구'하면서 서식을 깎는 것**이 병행 원인.
  사용자 PC의 복구 로그(%TEMP%\error*.xml) 확보 — KKAK·MCAP 실사용 파일 전부 "sheet19.xml 셀 정보" 복구 이력.
- 원인 (PC 엑셀 이분 탐색 12회로 확정): RF 시트 3페이지 수식 셀(H98=H55)의 캐시 결과가 **`<v>NaN</v>`**.
  exceljs가 템플릿 생성 왕복 때 문자열 수식 결과를 NaN으로 캐시 — 엑셀은 NaN을 거부하고 시트를 '복구'한다.
  (파이썬 검사가 놓친 이유: `float('NaN')`이 예외 없이 통과. 교훈으로 기록.)
- 수리: ① 템플릿 6종(DJCT·STANDARD·STMJ·SWAT·SWDN·SWRG)에서 NaN 캐시 zip 수준 제거.
  ② 런타임 스크럽 — writeBuffer 직전 전 시트 순회, 비유한 숫자값·수식 캐시 제거 (재발 원천 차단).
- 검증: 생성본 NaN 0 · 시뮬 6/6·5/5 · 선 대조 전 시트 0손실 · **PC 엑셀에서 KKAK 생성본 직접 열어 복구 경고 없음 + OS/DM/RF 시트 선·병합·정렬 육안 확인**.

### V9.19-04 (2026-07-28) — 마감 텔리: 표 선(테두리) 복원 (사용자 실파일 검증)
- 사용자 보고: 내용 지운 자리의 **선까지 지워져** 보임 (KKAK 실생성 파일로 확인 — STANDARD 폴백 산출물).
- 원인 2가지 (전수 대조로 실측):
  1) 표준형 템플릿 15종 생성 때 값 클리어와 함께 셀 스타일이 소실된 영역 존재 (OS 14곳·Seal 20곳·RF 20곳·Performance 126곳 등).
  2) fill의 FW 재병합 — exceljs `mergeCells`가 범위 전체에 마스터 스타일을 덮어써 아래칸 선이 변형.
- 수리: ① 실물 소스에서 셀 스타일(선·정렬·폰트·배경·행높이) 전면 이식(`repair_std.mjs`, 값은 유지) —
  15종 × 1.2만~1.8만 셀. ② FW 재병합을 병합 전 스타일 보존→병합→복원(`mergeKeepStyle`)으로 교체.
- 검증: 셀 단위 선 전수 대조(`border_diff.mjs`) — DJCT·KKAK(STANDARD 폴백)·DXQD·TMPZ 전 시트 선손실 0.
  변형 6/6·회귀 5/5 시뮬 유지 PASS.

### V9.19-03 (2026-07-28) — 마감 텔리: 변형(cn) 템플릿·표준 폴백·드로잉 보정
- **변형(cn) 템플릿 채우기**: DXQD·TMPZ (중국계 서식 — 선사 반복, 하위선사 괄호 표기 "DLC⏎(SKR)", 선사별 Total·G.Total·헤더가 전부 수식).
  - 쌍 행(F/E)에 값만 쓰고, 수식 셀은 `{formula, result}`로 계산 결과 캐시 갱신 → 재계산 안 하는 모바일 뷰어에서도 숫자 정상 표시.
  - 행 매칭 키 = (하위선사 || 블록선사, 포트). 템플릿에 없는 조합은 빈 쌍 행에 라벨 기입해 배치, 자리 부족 시 노트로 경고.
  - 시뮬: DXQD 2629W 실선적 EDI 345대 → 실물 텔리와 셀 단위 일치(DWS DLC 15/1·234/1/24, EAS DLC 60/10, G.Total 캐시 15/294). 6/6 PASS.
- **표준 폴백**: 전용 템플릿 없는 배 → STANDARD.xlsx(표준 GM 서식)로 생성 + "전용 템플릿 없음" 노트. OBWH(바우처형)만 드로잉 유지.
- **드로잉 폴백 보정**(OBWH·비상용): 전 셀 중앙정렬, DM 시트 MARKS/CONTENTS 병합·EXCEPTION 열 확대, 타임시트 "(작업 보고 기록 없음)" 문구 제거 — 빈 자료도 실물 틀 유지(공간 확보, 내용만 빈 칸. 사용자 확정 규칙).
- 템플릿 17종: 표준 14 + STANDARD + 변형 DXQD·TMPZ. `public/tally_templates/` + Pages 레이스 대응 루트 사본(build.sh 자동 복사).

### V9.19-02 (2026-07-28) — 수석 대시보드: 바로가기 버튼 바 + 항목별 접기

**사용자 보고**: "마감 텔리를 찾는데 한참 아래로 내려야 하고 다른 항목들도 일일이 찾아다녀야 한다. 검수원 로그인 항목 같은 것도 버튼을 누르면 보이게."

- **상단 바로가기 바** (전체 카운터 바로 아래, 3~4열 그리드 13버튼): ⚓작업 보드 · 📋진행 상황 · 📑마감 텔리 · 👷검수원 · 📤작업 보고 · 🏗장비 보고 · 🖐편집 · 📚자료 보관소 · 🗄완료 보관소 · 🔒엠티 실 · 🚛LOLO · ❌오답 · 📢공지. 누르면 **그 항목이 펼쳐지면서 스크롤 이동**(scroll-mt로 상단 여백 확보).
- **항목별 접기(`Fold` 래퍼)** — 13개 섹션 전부 44px 헤더 버튼(▼ 열기/▲ 접기)으로 감쌈. **기본 펼침은 ⚓작업 보드·📋진행 상황 2개뿐**, 나머지(검수원 활동·공지·편집·보관소 2종·마감 텔리·장비/최근 보고·엠티 실·LOLO·오답)는 버튼을 눌러야 보인다 → 첫 화면이 카운터+바로가기+보드로 짧아짐.
- 편집 주의: 기존 섹션 JSX는 손대지 않고 `<Fold>`로 감싸기만(외과적). 조건부 블록(`{cond && (…)}`)은 Fold를 조건 안쪽에 넣어 조건 유지.

**검증** — jsdom 10종 PASS/0: 바로가기 13버튼 · 기본 펼침/접힘 상태 · 바로가기→텔리 펼침 · Fold 토글(검수원 홍길동 표시/숨김) · 보드 접기. 빌드·번들 grep(▲ 접기·▼ 열기·바로가기 라벨) 확인.

### V9.19-01 (2026-07-28) — 마감 텔리: 실물 템플릿 필 전환 + 수석 대시보드 이동

**사용자 피드백 2건** — ① "셀 크기보다 텍스트가 길어 짤리고, 중앙정렬이 안 됨. 간격·셀 크기·글씨 크기가 실물과 조금씩 다름" ② "마감 텔리는 검수원이 보면 안 되는 서류 — 수석 대시보드 안에 있어야."

**① 실물 템플릿 필 방식으로 전환** — 코드가 양식을 그리는 방식(V9.19)을 버리고, **실물 마감 텔리 파일 자체를 서식 틀로 사용**. 빌더(`simtmp_build.mjs`, 1회성)가 각 배 최신 실물 xlsx에서 가변 값만 비워 `public/tally_templates/{CODE}.xlsx` **14척분**(+STANDARD=DJCT) 생성, 좌표 매니페스트는 `src/data/tallyTemplateMap.js`. 앱은 숫자만 채운다 → **열너비·행높이·글꼴(맑은고딕 22pt 제목)·중앙정렬 = 실물 100%**.
- 합계·헤더(DISCH (n))는 원본이 수식이지만 모바일 뷰어 재계산 문제를 피해 **검증된 계산값으로 덮어씀**.
- 템플릿 없는 배(TMPZ=변형 양식 · DXQD/OBWH=.xls) → 기존 드로잉 폴백 + "표준 서식으로 생성" 안내.
- ⚠ exceljs 함정 3개 실측: ⑴ 원본 인쇄영역 정의명이 깨진 채 라운드트립되면 재저장본을 못 연다 → `definedNames.model=[]` + 시트별 `printArea` 삭제 ⑵ **병합 하위 셀에 쓰면 마스터가 덮인다** — A열 선사 블록 병합(A10:A15) 때문에 라벨이 전부 지워졌던 사고. 빌더가 데이터 구역 병합을 풀고, 채우기가 실제 블록 크기로 재병합 ⑶ node ESM은 JSON import에 어트리뷰트 요구 → 매니페스트를 .js 모듈로.

**② 수석 전용 이동** — VoyagePage 결과 탭의 카드 제거. ChiefDashboard에 `TallyExportSection` 신설(보관소 위): 항차 목록 + [엑셀 생성], **비수석은 잠금**("🔒 마감 텔리는 수석검수사만"). 도움말도 위치 갱신.

**⚠ 배포 규칙 정정 (2026-07-28 실측 — V9.10 기록 갱신)**: 이 저장소 Pages는 **워크플로 2개가 경합**한다 — ① 커스텀 Actions(`deploy.yml`, npm build 후 dist 배포) ② 기본 "pages build and deployment"(브랜치 루트 배포). **커밋마다 둘 다 돌고 마지막에 끝난 쪽이 서빙**되므로 라이브 번들 해시가 왔다갔다 한다(V9.10의 "해시 다름" 관찰의 진짜 원인). 따라서 **루트와 dist 양쪽 모두 완전해야 한다** — build.sh가 assets·index.html에 이어 tally_templates도 루트로 복사하도록 보강. 근본 해결(워크플로 하나 제거)은 Pages 설정 확인 후 별도 판에서.

**검증** — DJCT 실선적 216대 템플릿 필 18종 PASS/0(열너비·글꼴·정렬·행높이 실물 대조, SKR 31/89, Total 47/169, 옛 데이터 잔존 0, OS-OUT 216) + ATPR 템플릿 메커니즘 6종(병합 재현 포함) + openpyxl 교차 열람 2파일 OK. dist에 템플릿 15개(1MB) 동봉 확인.

### V9.19 (2026-07-28) — 선박별 마감 텔리(DEP.TALLY REPORT) 엑셀 생성 ★핵심 기능

**사용자 지시**: "선박별 마감 텔리를 만드는 작업. 표기 방법과 순서가 일정해야 한다. 선사 순서 등 모든 것이 선박별·선사별로 다르니 유심히. 참고는 메일 수집기에서 수집."

**1단계 — 실물 수집** (`C:\TALLYTEST\_tally_collect.py`, 1회성): 수집기 모듈(browser/mail/auto) 재사용, 웹메일에서 TALLY REPORT·DEP.TALLY·DEPARTURE REPORT 검색(542통) → 첨부 233개를 `MAILBOX/{선박}/_마감텔리/`에 저장(수집기 탭과 별개 새 탭·NDR 제외·메일별 예외 격리). ⚠ 함정 2개 실측: 새 탭 첫 진입 리다이렉트로 evaluate 끊김(재시도 로직), Windows 폴더명 끝 공백 금지(rstrip).

**2단계 — 양식 분석** (`C:\TALLYTEST\마감텔리_양식_카탈로그_2026-07-28.md`): 20척 분해. 3계열 — **A 표준 워크북 16척**(Final Work→Time Sheet→OS-IN→DM-IN→OS-OUT→DM-OUT→[DAMAGE]→Act.Seal→RF→[Performance]→[SHIFTING]) / **B OBWH 바우처형**(주·야·시간외 요금 배분) / **C PDF형**(RZOR·XTPG — 이번 범위 외). **선사 순서·포트 순서는 배마다 고정**(예: DJCT = SKR→HAS→HSL→DJS→DYS · SHK→HPH→INC / STSE·STMJ = SIT→TJM→EAS→WDG→SKR). 규격 4분류 = 20'/40'/HC/45'(40HC·HC리퍼는 HC열), 빈 셀 공란·Total만 0, OS 정상 NIL, -ditto- 반복 표기.

**3단계 — 구현** (검수앱 V9.19)
- `src/data/tallyFormats.js` — 선박별 양식 사전(선사·포트 순서, DAMAGE 형·SHIFTING·Performance 유무, OBWH variant). 사전에 없는 배 = 등장 순서 + 화면 경고.
- `src/tallyReport.js` — 순수 집계 엔진: Final Work 매트릭스(양하=POL별·선적=POD별 × F/E × 규격4), OS(완료 flag missing/extra), Act.Seal(sl_orig≠sl·리씰), RF, Performance, SHIFTING, Time Sheet(작업 보고 이력→COMMENCED/HATCH/COMPLETED).
- `src/tallyExcel.js` — exceljs 렌더러(테두리·병합·서명란 포함 8~10시트). **동적 import — 918KB 별도 청크**, 버튼 누를 때만 로드.
- UI: 항차 화면 [결과] 탭 맨 위 「📑 마감 텔리」 카드 → [엑셀 생성] → `{code} {voyD}&{voyL} PTK TALLY REPORT.xlsx` 다운로드.

**검증 (실물 대조)** — 샘플 폴더에 실선적 EDI와 실제 발송 텔리가 같이 있어 가능했던 시뮬: **DJCT 0221W 선적 216대 — 실물 Final Work LOAD 매트릭스와 선사·포트·규격 전부 일치**(SKR 31/89·HAS 2/45·HSL 5·DJS 11/25·DYS 3/5), **ATPR 2634E 양하 251대 — SKR DLC 114/20·WEI 41/76·Total 155/96 일치**. 엔진 시뮬 17종 + 엑셀 생성·재독 12종 = PASS 29/0.

**남은 것** — ① OBWH 바우처형 시트(주/야 배분 — 작업 보고 시각으로 계산 가능, 다음 판) ② RZOR·XTPG PDF형 ③ DAMAGE-EACH 상세(사진 보고 연동) ④ Time Sheet의 갱 수·WEATHER는 수기 항목.

### V9.18-02 (2026-07-27) — 선박 정보: 약자(DXQD) 검색 실패 수리

**사용자 보고**: 풀네임(vslFull) 없는 항차는 약자로 검색돼 「"DXQD"라는 이름의 선박에 대한 정보는 검색 결과에서 확인되지 않았습니다」가 나온다 (DXQD = XIN QUN DAO).

**세 겹 수리** (`ShipIntroCard.jsx` + `gemini.js` + `VoyagePage.jsx`):

1. **이름 자동 보완** — `resolveShipDisplayName(info, portMisData)` 신설(순수 함수): vslFull(코드형이면 무시) → **PORT-MIS 선박명**(콜사인 매칭, 6자 이상만 신뢰) → 약자. 카드 헤더·검색 모두 이 이름을 쓴다. VoyagePage가 portMisData를 카드에 전달.
2. **IMO 우선 검색** — 이름이 2~5자 코드형이면 프롬프트에 「사내 약자일 수 있다. IMO·콜사인으로 먼저 실제 선박명을 확정한 뒤 정리하라」를 삽입. IMO가 있으면 약자 상태에서도 검색 허용.
3. **풀네임 수동 입력** — 약자뿐이고 IMO도 없으면 생성 버튼을 잠그고("풀네임 입력 후 검색 가능") 입력칸(placeholder: XIN QUN DAO)을 연다. 입력하면 대문자로 정규화해 그 이름으로 검색.

**검증** — 시뮬 13종 PASS/0: 이름 해석 5(코드형 vslFull 무시·PORT-MIS 보완·짧은 PM명 불신) + 카드 8(약자 경고·버튼 잠금/활성·입력→대문자 검색·IMO 우회·PORT-MIS 헤더). 빌드·번들 grep 확인.

**데이터 작업 (2026-07-27 밤, 사용자 확정 "클로드가 직접 찾아 저장하는 게 정확하고 빠르다")** — 평택 기항 선박 **33척 전량을 Claude가 웹에서 직접 조사**(조사팀 3개 병렬, VesselFinder·MarineTraffic·Vesseltracker·선사 공식 등 출처 검증)해 `ship_intros/`에 **98개 키**(코드+별칭+IMO+콜사인)로 저장. 형식 = 카드와 동일(한 문장 소개 → [선박 제원] → [운항 정보] → [이름 이야기]) + 출처 링크. **현재 항차 12건 전부 실키 조회 시뮬 PASS 12/0** — 검수원은 카드를 열면 바로 보인다(버튼 누를 일 없음). DXQD에 저장돼 있던 실패 문구("선박 풀네임이 필요합니다")도 정상 데이터로 덮어씀. 확인 안 된 값은 "확인 안 됨"으로 명기(NINGBO TRADER TEU, MARSA PRIDE 항로 등). 흥미 발견: TIAN HAI PING ZE의 PING ZE(平澤)가 바로 평택 — 평택 전용선임을 이름에 새긴 배. 새 배가 오면 카드의 [AI로 선박 정보 찾기]로 만들거나 Claude에게 조사를 요청하면 된다.

### V9.18-01 (2026-07-27) — 선박 정보 카드 개편: 실제 제원 + 이름 이야기 + 출처

V9.18 초판은 이름 풀이만 했는데 **사용자 확정이 달랐다**: "KMTC OSAKA는 고려해운 소속의 파나마 국적 컨테이너선… IMO 9848883, 길이 172m, 건조 2019년, 아시아 항로 — 이런 식의 정보". 이어 "이름 풀이도 있긴 합니다 — 배마다 여자 이름·동물 이름이 붙는데 재미있는 유래가 있어서" → **둘 다** 넣는 것으로 확정.

- **Gemini Google 검색 그라운딩** (`gemini.js askShipIntro` 재작성) — `tools: [{google_search}]`로 웹을 실제로 검색해 답한다. 출력 형식 고정: 한 문장 소개 → [선박 제원](선종·IMO·국적·길이×너비·건조년도·GT/TEU) → [운항 정보](선사·항로) → **[이름 이야기]**(언어 풀이·사람/동물/지명 배경·선사 명명 규칙·일화). "확인 안 되는 항목은 '확인 안 됨', 숫자 추측 금지" 강제.
- **출처 링크** — groundingMetadata의 출처(최대 5개)를 카드에 🔗 칩으로 표시하고 Firebase에 함께 저장(`fbSaveShipIntro` 4번째 인자 `sources`).
- **폴백** — 키가 검색 도구를 지원 안 하면(400/403) 검색 없이 생성하고 "⚠ 웹 검색 없이 생성됨 — 수치는 부정확할 수 있습니다"를 본문에 박는다.
- 버튼 문구: [AI로 소개 만들기] → **[AI로 선박 정보 찾기]**. 표기: "AI 생성" → "AI 웹 검색 · 참고용".
- ⚠ 운영 메모: 검색 그라운딩은 Gemini API 무료 한도가 별도(하루 제한)라, 배마다 1회 생성·공유 캐시 구조가 그대로 비용 방어선이다.

**검증** — 시뮬 12종 PASS/0(프롬프트 3섹션·그라운딩·폴백·출처 렌더/저장·새 문구). 빌드 PASS, 번들 grep 7종 확인.

### V9.18 (2026-07-27) — 선박 소개 · 이름 유래 (사용자 요청)

**항차 화면 하단 정보 구역(PORT-MIS 카드 위)에 「🚢 이 배는? — {선박명}」 접이식 카드.**

- **동작**: Firebase `ship_intros/{shipId}` 캐시가 있으면 그대로 표시(전 검수원 공유). 없으면 [✨ AI로 소개 만들기] — Gemini가 **이름의 뜻(언어·한자 풀이) 중심 4문장 이내**로 생성 후 저장. [다시 만들기]로 재생성.
- **환각 방지 설계** — 프롬프트가 "건조연도·크기·사고이력 등 확인 불가한 사실은 절대 지어내지 말 것, 모르면 모른다고 쓸 것"을 강제. 카드에 "✨ AI 생성 · 참고용 · 만든이 · 날짜" 상시 표기. 사실 정보(입출항·물량)는 기존대로 PORT-MIS·EDI가 정본.
- **자연어 연동** (`nlSearch.js` `shipIntroQuery`) — "이 배 뭐야 / 선박 소개 / 배 이름 유래 / 무슨 배야" → 캐시가 있으면 본문을 바로 읽어주고(음성 포함), 없으면 카드 위치 안내. `window.__shipIntroCache`로 카드와 검색이 캐시 공유.
- **새 파일/함수**: `components/ShipIntroCard.jsx`(테스트 주입 가능한 loader/generator/saver), `gemini.js askShipIntro()`, `firebase.js fbGetShipIntro/fbSaveShipIntro`. Firebase 신규 노드 `ship_intros/{shipId} = {text(≤2000자), by, at}`.
- **키 없을 때**: "생성 실패 — 헤더 ⋯ 메뉴에서 AI 검색 키를 확인하세요" 안내.

**검증** — 시뮬 17종 PASS/0: 파서 5문형+회귀 2 · 카드 렌더→생성→Firebase 저장→window 캐시→참고용 표기 · 캐시 즉시 표시 · 생성 실패 안내. 빌드 PASS, 번들 grep(V9.18·이 배는?·ship_intros·환각 방지 문구).

### V9.17 (2026-07-27) — P4 안전망 (전면 점검 4차분 · 패키지 완료)

1. **완료 보관소 열람·복원 UI** (`ChiefDashboard.jsx` `ArchiveRestoreSection`) — archive 백엔드(M7.18b: 목록·복원·1년 정리 함수)가 완성된 채 UI 0으로 방치돼, RZOR 통삭제 같은 실수에 되돌리기가 없었다(전면 점검 §1-5). 수석 대시보드 보관소 아래 신설: [목록 불러오기](필요할 때만 읽음 — 자동 로드 없음) → 항차별 [복원](수석 전용, 확인창 — 복원하면 수집기도 다시 받기 시작) + [🧹 1년 지난 것 정리](수석 전용, 영구 삭제 경고). **자동 정리는 넣지 않았다** — 데이터 삭제는 사람이 누른다.
2. **출항 임박 마감 경고 배너** (`VoyagePage.jsx`) — PORT-MIS ETD 2시간 전인데 미완·X-RAY 미처리·리퍼 온도 미입력이 남아 있으면 항차 화면 맨 위 붉은 배너("🚨 출항 N분 전 — 미완 12대 · X-RAY 3대"). 탭하면 마감 점검이 열린다. 매칭은 콜사인 정확 일치 → IMO 폴백(단순 매칭 — PORT-MIS 카드의 복잡한 매칭과 별개, 못 찾으면 조용히 생략).
3. **PWA 마감** (`public/manifest.webmanifest` + `_index.entry.html`) — 아이콘이 base64 단색 도형이었고 실제 PNG(icon-192/512)는 매니페스트가 참조하지 않았다. 실파일 참조로 교체(+maskable), **길게 눌러 바로가기 3종**(통합 검색 #/search · 수석 대시보드 #/chief · 건강 점검 #/health), `id` 지정, apple-touch-icon도 실파일로. ⚠ 아이콘 PNG를 `public/`에 복사(라이브는 Actions가 dist만 배포하므로 루트 파일은 서빙 안 됨 — V9.10 실측 규칙).

**검증** — 구조 시뮬 12종 PASS/0(매니페스트·배너 게이트·수석 전용·자동 로드 없음), dist에 아이콘 2종 + shortcuts 3종 포함 확인, 빌드 PASS, 번들 grep(V9.17·완료 보관소·1년 정리·마감 점검 열기).

**전면 점검 패키지 P1~P4 완료.** 남은 대형 후보(P5): 오프라인 아웃박스(미전송 N건 표시) · Web Push · 월간 실적 리포트(archive 백필). 보고서(C:\TALLYTEST\검수앱_전면점검_보고서_V9.13기준_2026-07-27.md) 참조.

### V9.16 (2026-07-27) — P3 알찬 기능 (전면 점검 3차분): 쌓이던 데이터를 화면으로

1. **검수원 개인 누적 실적 기록 시작** (`firebase.js`) — 완성된 채 호출 0회로 방치되던 `fbAddShipVoyageInspector`를 `fbCompleteContainer`·`fbCompleteContainersAtomic`에서 호출(비차단·실패 무시 — 완료 흐름을 절대 막지 않음, info는 항차별 1회 캐시). 이제 완료 1건마다 `ships/{shipId}/voyages/{key}/inspectors/{이름}`에 영구 누적 — **수석 완료 저장으로 항차가 지워져도 실적이 남는다.** ⚠ 과거분 소급 없음 · 완료 취소 시 감산 없음(근사 통계).
2. **홈 "오늘의 나" 스트립** (`HomePage.jsx` `computeMyToday()` 순수 함수) — 오늘 내 처리 N대 · 시간당 페이스(최근 20건 간격) · 마지막 완료 몇 분 전. 완료 기록이 있을 때만 표시.
3. **통계 탭 보강** (`StatsTab.jsx` `computeAllStats` 확장) — ① 이상 3종(누락·초과·바뀜)+리퍼 온도 미입력 카운터를 맨 위에 ② 시간대별 처리량 막대(1시간 버킷, 최근 12칸) + "지금 페이스 시간당 N대" ③ 검수원별 완료 ④ 항구별(양하=POL/선적=POD) 분포. 전부 이미 쌓이던 데이터(completed.at/.by/.flag)로만 계산.
4. **결과 탭에 미완 목록** (`ReportTab.jsx` `UndoneSection`) — "결과" 탭에 정작 '아직 안 한 것'이 없었다. 미완 N건 접이식 목록(컨번호+위치), 0건이면 "✅ 전량 완료".
5. **이 항차 작업 보고 이력** (`VoyagePage.jsx` `WorkReportHistory`) — 구독 함수(`fbSubscribeWorkReports`)는 있었는데 호출 0회. 결과 탭 하단에서 시작·중단·해치·콘박스 보고를 확인(최근 50건, 접이식). 필드는 `ts`(fbAddWorkReport 기준).

**검증** — computeMyToday 경계 6종 + StatsTab/ReportTab jsdom 7종 = 13종 PASS/0. 빌드 PASS, 번들 grep(V9.16·시간대별 처리량·미완·보고 이력·검수원별) 확인.

### V9.15 (2026-07-27) — P2 화면 재구성 (전면 점검 2차분)

**① 헤더** (`Header.jsx`) — 우측 6버튼(약 262px)이 선박명을 42px까지 밀어내던 구조 청산.
- 상시 노출: **장비 · 검수원 · ⋯ 메뉴** 3개만. 도움말·AI 검색 키·인원 관리·로그아웃은 ⋯ 드롭다운으로(각 48px 행). Gemini 키 미설정 점멸은 ⋯ 버튼의 빨간 점으로 축소.
- 오프라인 표시: 14px 구름 아이콘 → 헤더 아래 **빨간 띠**("오프라인 — 저장은 연결 복구 후 서버에 반영됩니다").
- 버전 표기는 ⋯ 메뉴의 '사용 매뉴얼' 행 오른쪽.

**② 홈** (`HomePage.jsx`)
- **브랜드 배지 삭제**(헤더와 완전 중복 — 첫 화면 44px 회수), **맛집 배너를 항차 목록 아래로**. 첫 화면에 항차 카드가 먼저 온다.
- 카드 배지 9px 전면 폐기(→11px), 작업일 저대비(slate-600) → slate-500.
- 완료/취소 버튼 24px → **40px**(12px 글씨), 삭제 버튼은 간격을 벌리고 40px 타깃.
- 부두 필터 칩·GPS 좌표 등록 버튼도 확대.
- **색 규칙 재확립**: 카드 테두리 부두색(파랑=PCTC/보라=PNCT) 제거 — 파랑=양하/호박=선적 전용으로 잠금. 부두는 📍배지가 말한다.
- 예보 모달 버튼 순서 [저장][취소] → **[취소][저장]** (앱 표준과 통일).

**③ 항차 화면** (`VoyagePage.jsx`)
- **탭 바 sticky**(top-[52px]) — 리스트 수백 건을 훑다가도 탭 전환 가능.
- **PORT-MIS 카드를 탭 본문 아래로** — 종전엔 탭을 눌러도 이 카드(+진단) 300~400px 때문에 내용이 안 보였다. 진단 경고 패널은 탭 바 위(경고는 눈에 띄어야 하므로).
- BayDictVerifyWidget(자료 진단)을 통계 탭 → **업로드 탭**으로. 통계 탭 첫 화면은 통계.

**검증** — Header jsdom 9종 PASS/0(선박명·⋯메뉴 4항목·비관리자 숨김·오프라인 띠), 구조·홈 검증 14종 PASS/0(배치 순서·sticky·9px 폐기·40px·버튼 순서·P1 회귀), 각 파일 esbuild 문법, 빌드 PASS, 번들 grep.

**교훈(재발 방지)** — 이번 작업 중 python 편집 블록에서 `open().write()`를 빠뜨려 **완료 버튼 확대 수정이 메모리에서만 적용되고 파일에 저장 안 된 채 넘어갈 뻔**했다. 구조 검증 시뮬(14종)이 잡았다. 편집 스크립트는 반드시 마지막에 write + 별도 검증 단계로 확인할 것.

### V9.14 (2026-07-27) — P1 응급 수리 (전면 점검 보고서 1차분) ★버그 7건

전면 점검(C:\TALLYTEST\검수앱_전면점검_보고서_V9.13기준_2026-07-27.md)에서 확정한 응급 패키지.

1. **실오류 신고 화면 크래시 수리** (`ReportTab.jsx`) — `SealErrorReport`가 받지 않은 `voy`를 렌더에 사용해, **실오류가 1건이라도 있으면 결과 탭→실오류 신고를 여는 순간 ReferenceError → 앱 전체가 에러 화면**이 됐다(세관 신고가 가장 급할 때 죽는 경로). `voy` prop 전달로 수리. **구버전 크래시 재현 시뮬로 원인 확정**(`voy is not defined`).
2. **마감 점검 → 리퍼 점프 수리** (`WorkClosingChecklist.jsx` + `VoyagePage.jsx`) — `search:'리퍼'`는 컨번호 검색이라 무시됐다("일단 미지원" 주석). `reeferTemp` 전용 필터(Full 리퍼 & 온도 빈 것) 신설로 교체.
3. **공지 띠가 헤더를 덮던 문제** (`BroadcastMarquee.jsx`) — z-50/top-0이 헤더(z-40)를 가려 공지 떠 있는 동안 홈·검수원 버튼을 못 눌렀다. `top-[52px] z-30`으로 헤더 아래에 고정.
4. **AI 호출 실패 시 검색 결과 소실 수리** (`SearchPanel.jsx`) — 실패 시 `setAiAnswer('오류: …')`를 넣었는데 aiAnswer는 렌더되지 않는 게이트 변수라 **오류도 안 보이고 기존 결과까지 사라졌다**(V9.11 때 기록만 하고 미룬 건). 실패 시 aiAnswer를 세우지 않고 알림만 — 결과 유지.
5. **핀치 줌 허용** (`_index.entry.html` 등 3파일) — `user-scalable=no` 제거. 9~11px 글씨 786곳인 앱에서 확대까지 막혀 있었다. (콘앱 cone.html은 별도 판단 — 이번엔 검수앱만)
6. **alert → 토스트 일괄 전환** (`main.jsx` + `toast.js`) — 완성돼 있던 `toast.js`(연결 0회)를 `installToastAlert()` 1줄로 활성화. alert 254곳 중 alert 계열 전부가 다크테마 논블로킹 토스트로. confirm/prompt는 그대로.
7. **통합 검색 즉답 연결** (`GlobalSearchPage.jsx` + `nlSearch.js` + `WrongAnswerModal.jsx`)
   - `hasAnyCondition`에 챗봇 8종(브리핑·실번호점검·소개·시간·날씨·입출항·트윈·식사) 흡수 → SearchPanel의 8종 수동 나열 게이트(구조적 부채) 제거.
   - 통합검색: 시간·소개는 즉답, 항차 맥락 질문(브리핑 등)은 "항차 화면에서 물어보세요" 안내 카드 — 종전엔 전부 무응답.
   - 오답 신고 parsedSummary에 V7.9x 이후 의도 15종 추가 — 이 계열 오답이 파싱 정보 없이 접수되던 것 수리.

**검증** — 파서·즉답 시뮬 13종 PASS/0, ReportTab jsdom 5종 PASS/0 + **구버전 크래시 재현 PASS**(수리 증명), 빌드 PASS, 번들 grep(V9.14·reeferTemp·AI 호출 실패·안내 카드·toast root) 전부 확인.

**남은 패키지** — P2 화면 재구성(V9.15) · P3 알찬 기능(V9.16) · P4 안전망(V9.17). 보고서의 패키지 표 참조.

### V9.13 (2026-07-27) — 30분 무조작 자동 로그아웃

**사용자 요청**: "30분 이상 로그인 후 신호 없으면 강제 로그아웃." 기준 확정 — **화면 조작**(터치·클릭·키·스크롤)이 없을 때. 앱을 그냥 닫은 경우는 대상 아님. 로그아웃 시 **안내 한 줄 + 검수원 선택창**.

**왜 필요했나** — `fbSetInspectorActivity`는 30초마다 **무조건** 도는 하트비트라 사람이 앱을 만지지 않아도 `lastActive`가 갱신됐다. 그래서 로그인만 해 두면 계속 '작업중/로그인'으로 보였고, 누가 실제로 붙어 있는지 알 수 없었다.

**수정**

- `src/inspectorStatus.js` — `IDLE_LOGOUT_MS = 30분`, `isIdleLogout(lastInputAt, now)` 신설(순수 함수 = 시뮬 대상). 기존 `inspectorStatus()` 배지 판정(90초 작업중)은 손대지 않음.
- `src/App.jsx` — 로그인 상태에서 `pointerdown·keydown·touchstart·wheel·scroll`을 캡처 단계로 듣고 `lastInputRef`만 갱신한다(리렌더 없음). 30초마다 검사해 30분 초과면 `fbLogoutInspector` → 로컬 검수원 해제 → 검수원 선택창. 폰이 잠겨 타이머가 멈추는 경우를 위해 `visibilitychange`(복귀) 때도 즉시 검사. 로그인·수동 변경 시 타이머 리셋.
- `src/components/InspectorModal.jsx` — `notice` prop 추가, 상단에 「⏱ 30분 동안 사용이 없어 자동 로그아웃됐습니다」 한 줄.
- 도움말 — 코스 1단계와 '안 될 때'에 각각 한 줄 안내.

**검증** — 시뮬 13종 PASS/0: 경계(29분59초 유지 / 정확히 30분 로그아웃 / 31분) · 기준시각 없음·미래값 방어 · 기존 배지 판정 회귀 3종 · jsdom 렌더 3종(안내 표시·검수원 선택창 동시·안내 없을 때 미표시). 빌드 PASS, 번들 grep(V9.13 · 자동 로그아웃 문구 · visibilitychange).

**주의** — 로그아웃돼도 작업 기록·완료 처리는 그대로 남는다(로그아웃 마킹만). 대기가 길어질 때 세션을 유지하려면 중간에 화면을 한 번 터치하면 된다.

### V9.12 (2026-07-27) — 양하 홀드 완료 시 해치 클로즈 질문은 "선적이 없을 때만" · 수집기 2.17.16-03/-04

**① 검수앱 V9.12 — 해치커버 클로즈 질문 조건 (사용자 확정)**

> "양하 완료 홀드 완료 후 해치커버 닫을까요? 선적 여부 확인 후 선적이 없을 때 질문. 모를 때나 있을 때는 질문 안 함."

- 종전(V8.09-05): 그 베이 그룹에 **선적 평택분 미완료**가 있으면 안 물음. 그런데 **선적 EDI가 아직 안 온 상태가 `loadRemain=0`으로 읽혀 '선적 없음'으로 오판**하고 닫자고 물었다. 이번에 그 구멍을 막았다.
- 신규 규칙 (`GuidedWorkPanel.jsx` `shouldAskHatchClose()` — 순수 함수로 분리, 시뮬 대상):
  1. 그 그룹 홀드에서 평택 양하분을 한 대라도 쳤을 것(작업한 그룹만 대상)
  2. **선적 자료를 알 것** — 위치(베이) 있는 선적 컨이 하나라도 있어야 한다. 없으면 = 모름 → 묻지 않는다.
  3. **그 그룹에 평택 선적분이 없을 것** — 있으면(완료·미완료 무관) 묻지 않는다. 닫는 건 선적 흐름의 「🔒 해치커버 클로즈 → 데크 계속」이 맡는다.
- 안 물을 때는 그대로 「다른 베이 선택」만 뜬다. 필요하면 [📤 작업 보고] → [해치커버]로 직접 보고한다(도움말 '자동 가이드'에 명시).
- 검증: 순수 함수 시뮬 12종 PASS/0 — 선적 없음(묻는다) · 이 그룹 선적 남음/완료(안 묻는다) · **선적 자료 없음(안 묻는다)** · 리스트만 도착(안 묻는다) · 홀드 미작업 · 비평택 선적뿐(묻는다) · 다른 그룹 · 데크만 완료 · 빈배열/null 방어.

**② 수집기 2.17.16-03 — 첫 자동 시작 5초 → 10초**

`auto_resume`(v2.17.11-22)이 창을 띄우고 5초 뒤 '완전 자동'을 스스로 눌러, **크롬 열기·메일 로그인을 시작할 틈도 없이 사이클이 붙었다**(사용자 보고 2026-07-26). 첫 시작만 10초로 늦췄다. 나머지 주기는 그대로.

**③ 수집기 2.17.16-04 — 완료한 항차 메일을 계속 받던 문제 (ATPR 사건)**

사건: 2026-07-27 06:22 ATPR_2634E(양하 251대)를 수석 완료 저장했는데 수집기가 계속 그 항차 메일을 받았다.

- 원인 ① **완료 저장은 `voyages/{키}`를 `archive/{키}`로 복사하고 원본을 삭제**한다. `_app_voy_done()`은 `voyages/{키}/info`만 읽으므로 완료 표시를 영영 못 본다.
- 원인 ② 판정 캐시 `_APP_DONE_CACHE`가 프로세스 수명 내내 유지되고 한 번도 비워지지 않아, 수집기를 켜 둔 채 완료를 눌러도 **재시작 전까지 반영되지 않았다.**
- 수정 (`collector/auto.py`):
  - 키마다 **살아 있는 항차 우선** → `voyages/{키}/info`가 있으면 그 플래그로 판정(완료 취소·보관소 복원 시 즉시 다시 수집 = 탈출구).
  - `voyages`에 없으면 `archive/{키}/info`를 보되 **leg별로만** 판정한다. ATPR_2634E는 `dischargeDone`만 있으므로 **양하만 막고 선적(2635W)은 계속 받는다** — 사용자 확정 "양하만 완료, 선적은 개별 등록하고 받아야".
  - `collect_week` 시작마다 `_APP_DONE_CACHE.clear()`.
- 정책 확정: **"완료면 끝"** — 완료 저장된 leg는 새 메일이 와도 안 받는다. 되받으려면 검수앱에서 완료 취소 또는 보관소 복원.
- 검증: **실 Firebase 데이터로 실함수 11종 PASS/0** (ATPR 2634E 양하=안 받음 / 2634E·2635W 선적=받음 / 2632E 양하·선적=안 받음 / TMPZ 2023E=받음 / 완료취소=다시 받음 / 통신 실패=판정 생략). 재시작 후 하트비트 `2.17.16-04` 확인.

**남은 관찰** — 완전 자동 [중지]를 눌러도 진행 중인 사이클의 메일 페이지 이동이 계속돼 로그인 화면이 날아간다는 보고(2026-07-26). `should_stop()` 검사 지점이 7곳뿐이라 그 사이 `page.goto`가 계속 도는 것이 원인으로 보인다. **미수정 — 다음 판 후보.**

### V9.11 (2026-07-26) — 도움말(사용 매뉴얼) 전면 재작성 · 관리자 ⚙ 하드코딩 제거

**사용자 요청**: "도움말 기능에서 검수앱 사용법을 다 지우고 자세히 만들어주세요. 예를 들어 가면서, 일일이 설명을 안 해도 될 수 있게."
확정 3건 — ① 하루 작업 순서 코스 + 기능 사전 두 겹 ② 화면 버튼 위치까지 짚기 ③ 사용설명만 교체(용어·회화 유지).

**무엇을 지웠나** — `helpData.js`의 옛 `usage`(9갈래 44섹션 182행 q/a)와 `tips` 전량 삭제. 옛 문구는 기능 나열식이라 "V7.94 신규", "M3.2 신규" 같은 버전 딱지가 본문에 남아 있었고, 화면 어디를 눌러야 하는지가 없어 결국 사람이 옆에서 설명해야 했다.

**새 구조 (`src/data/helpData.js` · `src/components/HelpModal.jsx`)**

- 블록 스키마 하나로 통일: `{ title, step?, where?, lead?, dos?[], says?[{in,out}], warns?[] }`
  - `where` = 📍 화면 위치(예: "항차 화면 [🎤 자연어] 탭 검색창") · `dos` = 번호 매긴 순서 · `says` = **실제 입력 → 앱 답변** · `warns` = ⚠ 막히는 조건
- `HELP_COURSE` (신규) — **하루 작업 순서 10단계**: 검수원 선택 → 장비 지정 → 항차 열기 → 자료 업로드(1·2·3) → 브리핑 → 자동 가이드 → 컨 1대 처리 → 트윈 → 작업 보고 → 마감·인계
- `HELP_DATA.usage` — **기능 사전 13갈래**: 검색 / 물어보기 / 음성 / 자동 가이드 / 완료 처리 / 트윈 / 특수화물 / 항구 검색 / 베이 그림 / 보고·사진 / 자료 업로드 / 출력 / 안 될 때
- 도움말 첫 화면이 2갈래 → **3갈래**(처음이면 여기부터 · 기능 사전 · 용어·회화)

**내용은 코드에서 뽑았다 (추측 금지)** — Header/App/HomePage/VoyagePage의 실제 버튼 문구, `nlSearch.js` 정규식에 실제로 구현된 질의만, `GuidedWorkPanel`·`BigResultCard`·`ContainerDetailModal`의 차단 조건 원문. 그 과정에서 **문서가 잘못 안내하던 함정 3건을 코드로 확인해 매뉴얼에 명시**:

1. `40피트 4777` — 사이즈 맥락이 잡히면 끝자리가 무시된다(`nlSearch.js` skipDigits). 옛 placeholder 예시가 실제로는 동작하지 않았다.
2. `3급` — DG 클래스 정규식이 `클래스|class|급` **뒤**의 숫자를 요구한다. `클래스 3`으로 써야 한다.
3. `홀드 몇 개 남았어`(몇 군데) ≠ `홀드에 몇 개 남았어`(그 단에 몇 대) — 조사 하나로 답이 갈린다.

**같이 고친 것 — 관리자 ⚙ 버튼 (V9.09/V9.10 인수인계의 구멍)**
`App.jsx`가 `inspector === '김성일'`을 하드코딩해 헤더 ⚙(인원 관리) 버튼을 내보내고 있었다. 그래서 **V9.09에서 권한을 받은 관리자는 권한은 있는데 버튼이 안 보여 인원 관리에 못 들어갔다**(인수인계가 반쪽). `fbGetAdminGuard` + `isAdminName`으로 교체 — 베이사전 정본 갱신 배너도 같은 기준으로 통일.

**검증** — 데이터·내용 시뮬 26종 PASS/0(스키마·옛 스키마 잔존 0·함정 3건 수록 확인), jsdom UI 클릭 시뮬 12종 PASS/0(홈→코스 10단계·📍 10개 이상·순서 li 30개 이상·사전 13칸 전부 내용 렌더·뒤로가기·용어·회화집), `bash build.sh` PASS, 번들 grep(V9.11·처음이면 여기부터·하루 작업 순서·기능 사전 / 옛 문구 "V7.94 신규"·"질문 가능 목록"·"M3.2 신규" 0건).

**남긴 참고** — 검색 결과 화면에서 AI(Gemini) 호출이 실패하면 `aiAnswer`가 렌더되지 않는 채 게이트만 걸려 기존 결과까지 사라진다(SearchPanel.jsx). 매뉴얼 "안 될 때"에 ✕(지우기)로 복구하라고 적어 뒀으나, **코드 수정은 미착수 — 다음 판 후보**.

### V9.10 (2026-07-26) — 소유자 권한 영구 고정 · 관리자 퇴사 시 권한 자동 삭제 ★권한 정책 확정

**사용자 원칙 (2026-07-26 확정)**: "제 권한은 앱이 사용되는 한 유지되어야 합니다. 퇴사를 했더라도 그래야 버그를 수정할 수 있습니다. 다른 권한자들은 지금 방식을 따르고, 퇴사하면 권한이 삭제됩니다."

즉 관리자에는 **두 등급**이 있다.

| 등급 | 대상 | 권한 회수 | 퇴사 처리 | 접속 |
|---|---|---|---|---|
| **소유자(owner)** | 김성일 (소스 고정) | ⛔ 불가 | 가능(표시만) | 퇴사여도 항상 가능 |
| **관리자(admin)** | 소유자가 부여한 사람 | 가능 | 가능 — **권한 동시 삭제** | 퇴사 시 차단 |

**수정 (외과적, 4파일)**

1. `src/adminGuard.js`
   - `OWNER_NAME = '김성일'` 신설(소스 고정 = 단일 진실원). `ADMIN_NAME`은 하위호환 별칭으로 유지.
   - `getAdminNames()`가 **항상 소유자를 목록 맨 앞에 넣는다.** Firebase `admins` 노드가 비었거나 지워졌거나 `김성일: {revoked:true}`로 조작돼도 소유자 권한은 되살아난다. DB 사고·타 관리자 조작에 대한 방어.
   - `isOwnerName(name)` · `canRevokeAdmin(guard,name)` 신설 — 회수 가능 판정의 단일 기준(소유자 불가 · 마지막 1명 불가).
2. `src/components/StaffManagerModal.jsx`
   - 소유자 행: 「권한회수」 버튼 대신 **「권한고정」** 표시(회수 불가), 배지는 「관리자」가 아니라 **「소유자」**.
   - `handleRevokeAdmin`: 소유자면 즉시 차단.
   - `handleDeleteStaff`: 관리자도 퇴사 처리 가능해졌다(V9.09에서는 차단). 일반 관리자는 `admins/{name}` 삭제 → 실패 시 퇴사 처리 자체를 중단(권한만 남는 어긋남 방지). 소유자는 퇴사 처리해도 권한·접속 유지(확인창에 명시).
   - `handleKickInspector`: 소유자만 차단(일반 관리자는 접속 기록 제거 허용).
3. `src/components/InspectorModal.jsx` — 화이트리스트에서 소유자 예외. `deletedStaff[김성일]`이 있어도, 심지어 명단이 통째로 비어도 소유자는 항상 검수원 선택이 가능하다(퇴사 후 버그 수정 접속 보장).
4. `src/utils.js` — `APP_VERSION = 'V9.10'`.

**검증** — 로직 시뮬 26종 PASS/0 FAIL: 구버전 하위호환 3 · 소유자 판정 4 · admins 조작 방어 5(소유자 누락·revoked·중복) · 회수 가능 판정 4 · 퇴사 후 상태 2 · 비번 설정 판정 3 · 화이트리스트 5(퇴사여도 접속·일반 퇴사자 차단·명단 소실). 번들 grep(V9.10·권한고정·앱 소유자입니다·관리자 권한도 함께 삭제) PASS. push `0f34012..e94f1e6`(17파일), blob 전수 대조 17/17 일치, 라이브 sw.js `VERSION='V9.10'`·캐시 `tallyman-V9.10` 확인.

**운영 메모** — 복구(재입사)해도 관리자 권한은 자동 복원되지 않는다. 필요하면 다시 「권한부여」한다.

⚠ **이 저장소의 GitHub Pages는 Actions(`.github/workflows/deploy.yml`)가 `npm run build` 결과 `./dist`를 배포한다.** 그래서 **라이브 번들 해시가 저장소의 `assets/index-*.js`와 다를 수 있다**(V9.10 실측: 저장소 `CJrBNLX3` vs 라이브 `Bm2YSPZD`). 라이브 검증은 해시가 아니라 **sw.js의 VERSION·캐시명·번들 내 문자열**로 한다. 해시 불일치를 배포 실패로 오판하지 말 것.

### V9.09 (2026-07-26) — 관리자 인수인계: 다중 관리자 · 관리자별 개별 비밀번호

관리자 이름이 소스에 박혀 있어(`const ADMIN_NAME = '김성일'`) 담당자가 바뀌면 재배포해야만 인수인계가 됐다. Firebase `admin_guard/admins` 목록으로 옮겨 앱 안에서 넘길 수 있게 했다.

- 저장 구조: `admin_guard/admins/{이름} = { pwHash, salt, devices:{...}, grantedBy, grantedAt }`. 구버전 최상위 `pwHash/salt/devices`는 마이그레이션용으로 남기고, 첫 권한 부여 때 소유자분을 목록으로 옮겨 적는다.
- 비밀번호는 **관리자마다 따로** — 권한을 줘도 기존 비밀번호를 알려줄 필요가 없다. 받은 사람이 검수원 선택에서 자기 이름을 처음 고를 때 본인이 정한다(`needsPasswordSetup` → setup 모드). 세션 통과 키도 `gm_admin_session_ok:{이름}`으로 분리.
- 인원 관리 모달에 「권한부여」/「권한회수」 버튼. 마지막 관리자는 회수 불가.
- push `3927f61..0f34012`(17파일). ※ 이 판의 "퇴사·접속기록 제거 차단" 규칙은 **V9.10에서 대체됐다**(위 참조).

### V8.40 + 수집기 v2.15.0 — 품질 게이트·항차 건강 대시보드·하트비트 (2026-07-03, 사용자 확정)

PCSZ 규격 누락 사건의 재발 방지 세트. 사용자 확답 3건: 게이트는 **올리되 '검증 필요' 플래그**(자료는 일단 보아야 한다) · 대시보드는 **홈 요약 + 상세 페이지** · 하트비트 끊김 기준 = **사이클 주기의 2배**.

**수집기 v2.15.0**
- 품질 게이트(autoreg.py `_qc_flags`): 업로드는 하되 이상이면 `voyages/{key}/qc/{mode} = {flags, at, ediFile}` 기록, 정상이면 null PUT으로 지움. 규칙 ① 빈규격 10% 초과(PCSZ) ② 리스트가 EDI보다 15% 초과 '부풂'(XTPG 305) — 리스트 부족은 수집 진행 중 정상이라 플래그 안 함(과잉 경보 방지, 시뮬로 확인).
- 하트비트(`write_heartbeat`): collect_week 끝에서 autoreg 성패 무관(try/finally) `collector_heartbeat = {at, cycleMin, autoreg:{done,flagged}, version}` PUT. cycleMin은 GUI 감시/완전자동 시작 시 `cfg["cycle_min"]`으로 주입.
- 검증: py_compile 3파일 PASS(⚠ C:\TALLYTEST 마운트가 스테일 절단 뷰를 줘 outputs 사본으로 검증 — 마운트 불신 시 호스트 Read가 진실), QC 시뮬 PASS(PCSZ=빈규격만·XTPG=수량불일치만·정상=무플래그).

**검수앱 V8.40**
- `src/health.js`(신규): 항차별 EDI/리스트/빈규격/부풂 계산 — 규칙은 수집기와 동일, 수동 업로드 항차도 같은 기준으로 검사. 수집기 qc 플래그도 병합 표시.
- `pages/HealthPage.jsx`(신규, #/health): 하트비트 카드(정상/끊김/기록없음) + 이상 요약 + 항차별 점검표(클릭→항차 이동).
- 홈 스트립: 3버튼 그리드 아래 — "수집기 정상·N분 전 / 끊김 / 기록 없음" + "검증 필요 N건 / 자료 정상", 클릭→점검 페이지. 30초마다 경과 재계산.
- `firebase.js` fbSubscribeHeartbeat 추가, App.jsx에 health 라우트(해시 #/health).
- 검증: build.sh PASS·번들 grep(V8.40·새 문구 4종)·jsdom 렌더 2종 PASS(HealthPage 9항목, 홈 스트립 끊김 표시·클릭 라우팅).
- ⚠ 주의: 대시보드는 앱이 이미 구독 중인 voyages 전체 트리로만 계산 — 추가 fetch 없음. 모드 키는 discharge/**loading**(load 아님).

### V8.35-01 — 자동 등록 EDI 선택 버그 (사용자 발견: PCSZ 카고플랜 규격 누락)

증상: 카고플랜 일부 규격 누락(빈 규격 180대). 원인: 폴더에 ASC와 BAPLIE가 둘 다 있고 컨 수 동률이면 알파벳순 첫 파일(ASC, 규격 일부 누락)이 채택됨. EDI·파서·카고플랜은 전부 정상 — 자동 등록의 최적 파일 선택 규칙 결함. 수정: 동률 시 규격(iso) 보유 수 우선(사실상 BAPLIE 우선). 시뮬 PASS(규격 없는 파일이 먼저 와도 BAPLIE 채택·빈 규격 0). autoreg_state 초기화로 전 항차 재업로드 유도. 진단 경로 기록: 원본 파싱 정상 → 실렌더 정상 → Firebase 저장본 이상 → raw fileName으로 ASC 확정.

### V8.35 + 수집기 v2.14.1 — 통보(신호) 시스템 폐지 (2026-07-03, 사용자 확정)

자동 항차 등록이 통보를 완전히 대체하므로 제거. **검수앱**: collector_signal 구독(fbSubscribeCollectorSignal)·등록 대기 카드(pendingSignals/dismiss/localStorage gm_dismissed_signals_v1) 전부 삭제. **수집기**: collect_week의 notify_ready 호출 삭제(함수 정의는 사장), GUI 신호 삭제 버튼 제거. **Firebase**: collector_signal 노드 삭제 완료. 검증: 홈 렌더 PASS(신호 카드 부재·자동 배지 정상), 빌드·참조 0건 grep, py_compile. ⚠ XTPG 양하 EDI 도메인: 이 배는 평택 양하 BAPLIE가 따로 없고 **인천 출항 EDI(XTPGnnnnW INC(2.2).EDI, Pan Asia Tally 이승철 nsclee7@naver.com 발신)**가 그 역할 — 제목 "V-05nnW Final Load Documents At KRINC", 수집기가 내용 판정→TDT W→E 교정으로 자동 처리(KKAK INCPA와 동일 계열).

### V8.33 + 수집기 v2.14.0 — RZOR 가상 EDI(IFCSUM) + 업로드 토큰 방어 + 오염 대청소 (2026-07-03 오전, 사용자 확답 4건)

**배경(사용자 신고)**: ①RZOR은 BAPLIE EDI가 없는 배(IFCSUM 매니페스트만) ②MAILBOX 선박 폴더에 안 맞는/지난 항차 자료 혼입. 감사 결과 RZOR/R070E·R070W 폴더에 타선박(KKAK·KKLC·KSKM SI, TNJP 옛 컨리스트) 66개 혼입 — 7/2 00:00 유입(자기치유 라우팅 v2.12.0-42 이전 잔재). Firebase엔 RZOR_R070E에 가짜 EDI 877·리스트 2296 등 오염.

**정리(확답 후 실행)**: Firebase — RZOR_R070E 양하/선적 데이터 삭제, SWCP 선적리스트 1113·TNJP 양하 262·XTPG 양하 160·OBWH_2676E 선적 250 삭제, PCSZ_261E(오타 중복) 항차 삭제(전 항차 검수기록 0 확인 후). MAILBOX — R070E 오분류 64개·R070W 폴더 전체 드라이브 휴지통(30일 복구 가능).

**V8.33(검수앱)**: autoRegApi에 `parseIfcsum` — IFCSUM 12:(POL)·13:(POD)·51:(컨번호/실번호/규격/F\/E/무게) 세그먼트를 가상 EDI 컨테이너로. 실파일 시뮬 124/124 PASS(전량 POD 평택). LOLO라 선내 위치 없음(정상). 헬퍼 재번들.

**V8.33-01 — 가상 EDI에 리스트 동시 생성 (사용자 지적: RZOR 매칭 0)**: IFCSUM은 실번호·무게를 담고 있으므로 ediContainers만 만들면 records가 비어 매칭이 안 잡힘 → 가상 EDI일 때 records(cn/sl/wt)도 함께 생성. RZOR_R070E는 장부의 가상 EDI에서 즉석 생성해 124건 반영 완료(라이브 확인). 헬퍼 V8.33-01 재배포.

**v2.14.0(수집기)**: autoreg 업로드 파일 토큰 검증 — 타선박 코드 토큰 제외, 항차 토큰 불일치 제외, 엑셀류(합본 제외)는 일치 토큰 필수. 시뮬 PASS(진짜만 선택·합본 우선 유지). 상태 v4(전체 재평가). ⚠ 다운로드 라우팅 자체는 v2.12.0-42의 자기치유(_route_foreign_by_filename, 컨번호 실측 대조)가 이미 있음 — 재수정 안 함.

### V8.32 — 수집기 자동 항차 등록 연동 (2026-07-02, 시뮬 3종+렌더 PASS — 사용자 확정 설계)

**설계(사용자 확정)**: 수집기가 MAILBOX 자료 도착 즉시 검수앱에 항차를 **가등록(수집중)** + EDI 업로드하고, 기존 완료 판정(선적 녹색)이 떨어지면 **확인 업로드 후 확정** 전환. 등록 조건은 무조건이 아니라 수집기 신호 조건과 동일(관할 선박만·마감/출항/터미널완료 제외·최신 항차만·등록부 선박만 — 유령 선박 교훈).

**검수앱 쪽(V8.32)**
- `src/autoRegApi.js` 신설 — `window.GMautoPayload(files,{vslCode,voy,mode,phase})` → `{key, info, ediContainers, ediRaw, records, counts}`. 파싱·분류(평택 POD/POL→discharge/loading/transit)는 VoyagePage 재처리 로직과 동일. **Firebase 쓰기는 안 함**(순수 함수) — 쓰기는 수집기 REST 담당.
- HomePage 항차 카드에 배지: `info.autoRegistered` + `autoStatus`(collecting→"🤖 자동(수집중)" 호박색 / confirmed→"🤖 자동(확정)" 초록).
- 검증: 실 EDI(SWCP 2606N, 843대) 페이로드 시뮬 PASS(843/843·평택 238), 빌드 9.9초, 번들 grep(GMautoPayload·배지 문자열), jsdom 렌더 PASS(두 배지+항차쌍 표기).

**V8.32-01 + 수집기 v2.13.0-03 — 라이브에서 잡은 결함 3건 (2026-07-02 밤, XTPG 305 사건)**
- 증상: 터미널 계획 선적 164대인데 자동 업로드 리스트가 305대. 원인 ①: 폴더의 모든 엑셀(VGM·컨번호리스트·MEMO BL 등)을 합집합 — 수집기가 이미 평택 기준으로 검증한 합본(…LOADLIST.xlsx, 140대)은 오히려 제외하고 있었음. → 수집기 -03: 합본이 있으면 합본만 사용.
- 원인 ②: 합본은 GMmerge 산출물(MERGED 시트, 'Cntr No' 헤더)이라 검수앱 parseListExcel이 0건 반환. → V8.32-01: GMautoPayload에 합본 전용 매핑(Cntr No/Seal/EmptySeal/Weight→cn/sl/eseal/wt) 추가, 헬퍼 재번들.
- 원인 ③(OBWH +250 사건): 폴더상 최신 선적이 지난 항차(2658W)면 그 옛 리스트가 현재 항차에 붙음. → 수집기 -03: 터미널 현재 짝보다 _vnum이 작은 폴더 항차는 제외(미래 항차는 허용 — RZOR R070E 같은 선도착).
- 오염 정리: XTPG_0530E/loading/records(305)·OBWH_2679E/loading 삭제(생성 직후라 검수 기록 없음 확인 후, 브라우저 REST DELETE). 상태 _STATE_V=3으로 전체 재평가.

**수집기 쪽(v2.13.0) — 별도 코드베이스지만 연동 계약이라 여기 기록**
- `collector/autoreg.py` 신설 + `collector/autoreg_helper.html`(V8.32 파서 번들, GMautoPayload 노출) + `auto.py` 수집 사이클 끝 훅 + `config auto_register`(기본 켜짐, false로 끄기).
- 키 결정: MAILBOX 폴더가 곧 선박코드/항차. 기존 항차(양하/선적 어느 키든) 있으면 재사용 — 수동 등록과 충돌 없음. 없으면 양하 우선 `{코드}_{양하항차}`.
- **보존 원칙(절대)**: info는 없을 때만 생성(있으면 voy_d/voy_l 빈칸 채움만, 수동 항차에 autoStatus 주입 금지). ediContainers/raw는 EDI 진실이라 교체. records는 **보수 머지**(신규 컨 추가+빈칸 채움만 — 검수 입력 절대 불변). completed 등 다른 경로 불가침.
- 중복 방지: 항차·모드별 파일 지문(autoreg_state.json, %LOCALAPPDATA%\TallymanCollector) — 같은 자료·같은 단계면 재업로드 안 함.
- 검증: 모의 MAILBOX/REST로 run() 시뮬 PASS — 조건 필터(마감·비관할 제외), 단계 판정(녹색→확정), 키 재사용(수동 항차 info 불가침), 보수 머지(기존 실번호·검수 입력 보존), 재실행 중복 0건. **미검증(PC 필요)**: CDP 크롬 실호출·Firebase 실쓰기 — PC 라이브 스모크 필요.
- 범위 제외(다음 과제): XRAY 리스트 자동 업로드(파일 판별 규칙 미확립), records 자동 업로드는 확정 단계에만.

### V8.31 — 작업 보고 해치커버 장수 수동 선택(1~3장) + 빌드 폭주 결함 수정 (2026-07-02, 시뮬·렌더 PASS)

**① 해치커버 자동계산 제거 → 수동 선택 (사용자 지시)**
- 대상: 작업 보고(WorkReportModal)의 해치 화면만. 가이드 흐름(GuidedWorkPanel)의 자동계산은 불변.
- WorkReportModal에서 `hatchPanelsOf`(매트릭스 hatchCount 합산 자동계산, V7.94-22) 제거. 베이 입력칸 아래 "장수 선택" 1장/2장/3장 버튼 추가(기본 1장), 보고 메시지 `총 N장`은 선택값 그대로. fbAddWorkReport 기록에 `panelCount` 포함.
- 검증: 시뮬 PASS(베이 3개 입력+수동 1장 → "총 1장" 등 3케이스), jsdom 실물 렌더 PASS(해치 화면 진입·1→2장 전환·옛 자동 힌트 제거 확인), 번들 grep("장수 선택" 1건).

**② 빌드 수 분 걸리던 진범 수정 — Tailwind가 1.2MB 단일라인 사전을 스캔 (V8.31 동봉)**
- 증상: `bash build.sh`가 3분+ (V8.30 기록 "3m 27s"), 코워크(45초/명령 제한)에서는 빌드 불가 판정까지 났음.
- 원인(프로파일로 확정): CPU 95.6%가 Tailwind 클래스 추출 정규식. content 글롭 `./src/**/*.{js,jsx}`가 `src/data/shipBayDict_v5_matrix.js`(단일 라인 1,248,716자) 등 데이터 사전까지 스캔 → 정규식 파국적 역추적.
- 수정: tailwind.config.js content에 `'!./src/data/**'` 제외 추가(데이터 파일에 Tailwind 클래스 0건 확인 후). 빌드 3분+ → **6.4초**.
- ⚠ 파급: **코워크에서도 검수앱 빌드 가능해짐** — "코워크 빌드 불가(45초 제한)" 결론(2026-07-02 오전)은 이 결함이 원인이었으므로 폐기. 단, 코워크 백그라운드 프로세스는 호출 종료와 함께 죽으므로(die-with-parent) 빌드는 한 호출 안에서 끝내야 함(이제 6초라 문제 없음).

### V8.30 — 수집기 대기 카드 치우기(로컬 숨김) (2026-07-01, 시뮬 5/5 PASS)

**요청**: 홈 상단 초록 대기 카드(`pendingSignals` = 메일수집기 `collector_signal/{이름}` 신호)를 사용자가 임의로 치우는 기능. 지난 항차거나 이미 등록된 항차라 화면에서 안 보이게 하려는 것.
**사용자 확정 규칙**: ① 검수앱 화면에서만 숨김(Firebase 수집기 신호 원본 불변). ② 되살아나도 숨김 유지. ③ **단, 같은 배라도 다른(새) 항차번호로 자료가 오면 다시 보여야 함.**
**설계(핵심)**: 숨김 기준을 신호 하위 키(`_key`)가 아니라 **항차번호 조합 sig**(`norm(dischargeVoy)+'|'+norm(loadVoy)`)로 잡음. `_key`로 숨기면 같은 배 새 항차 자료도 계속 숨겨져 규칙 ③ 위반. sig로 하면 항차번호가 바뀌면 sig가 달라 자동 재노출. `norm`은 기존 `pendingSignals`(340행) 정의 재사용.
**수정** (`HomePage.jsx`만, 외과적): ① `dismissedSigs` useState + `localStorage 'gm_dismissed_signals_v1'` 로드. ② `pendingSignals` useMemo에 `!dismissedSigs.has(sig)` 필터 한 줄 + 신호에 `_sig` 부착(deps에 dismissedSigs 추가). ③ `dismissSignal(sig)` 핸들러(Set에 추가 + localStorage 저장). ④ 대기 카드(410행)에 우상단 X 버튼(`lucide-react` X import 추가). **firebase.js·다른 파일 불변.** 등록 시 숨김(기존 349행 existingVoys 로직)과 OR 공존 — 충돌 없음.
**시뮬(5/5 PASS)**: ①초기 전부표시 ②A치움→B만 ③되살아나도 숨김 유지 ④SITC MOJI 새 항차(2632W→2634W)→sig 다름→재노출 ⑤등록(voy_l 일치)→기존 로직 숨김.
**검증(번들 PASS)**: `bash build.sh` 성공(`✓ built in 3m 27s`), 번들에 "이 대기 카드 치우기"·`gm_dismissed_signals_v1` 박힘, V8.30 index.js·sw.js·cone.html 동기화. HomePage.jsx esbuild JSX 구문 검증 통과.

### V8.29 — 인천발 항차 오프셋 EDI 경고 흡수 (2026-07-01, 가드 시뮬 5/5 PASS)

**배경(실사고)**: PEGASUS PROTO(콜사인 V7A5452) 양하에서 리스트 259대는 붙었는데 EDI 평택=0, 매칭=0. 원인 규명 = 데이터·파서·필터 전부 정상. EDI 항차 `2608S` ↔ 앱 등록 `2607N`. V8.28 `_voyCore`는 방향(N/S/E/W)만 무시하고 **번호는 정확 비교**라 `2608≠2607`에서 EDI가 차단됨 → 그 항차 `ediContainers`가 비어 화면 0. X-RAY·리퍼온도 경고도 전부 이 하나의 하위 증상.
**도메인 규칙(사용자 확정 2026-07-01)**: 인천 출발 선박은 평택 항차가 인천 항차보다 **한 항차 낮고 방향이 바뀐다**(`2608S`→`2607N`). 번호가 정확히 안 맞는 게 정상. **사용자 등록 항차가 진실.** 콜사인이 맞고 EDI에 평택 화물이 보이면 자동 흡수가 아니라 **경고(모달)** 를 띄워 사용자가 흡수 여부를 결정.
**수정** (`VoyagePage.jsx` 1617~ 항차 불일치 분기): 즉시 `continue` 하지 않고, `_ptkLeg > 0`(EDI에 평택분 존재) AND `_csComparable`(양쪽 콜사인 비교 가능)이면 `askChoice` 모달("항차 번호가 다릅니다") 발동. "흡수" 선택 → 가드 통과(정상 병합 흐름 진입, 등록 항차로 흡수), "적용 안 함" → `continue`. **콜사인 불일치·평택분 0·콜사인 비교불가는 기존대로 즉시 차단**(선박명만으로 흡수 승격 안 함 — 오매칭 방지). `_voyCore` 정의·방향 무시 로직은 그대로 유지.
**시뮬(5/5 PASS)**: ①실케이스 `2608S+2607N` 콜사인같음 평택259 → modal ②콜사인 다름 → block(ship) ③항차 같음 `2608==2608` → pass ④항차 다르고 평택분 0 → block(voy_no_ptk) ⑤콜사인 비교불가+항차다름+평택있음 → block(voy_ptk_no_cs).
**검증(번들 PASS)**: `_voyCs`/`_ediCs`(1596)·`askChoice`(1387 컴포넌트 스코프) 접근 정상. 이미 이 업로드 루프에서 `await file.text()`·`await askChoice`(1762) 쓰는 async 컨텍스트. `bash build.sh` 성공(`✓ built in 3m 11s`). 번들에 새 문구 3종("항차 번호가 다릅니다"·"인천발 항차 오프셋"·"로 흡수") 각 1건 박힘, V8.29 index.js·sw.js·cone.html 동기화.
**주의**: EDI 정정본 생성 불필요 — 앱이 등록 항차로 흡수. 파일명·TDT 항차 안 고쳐도 됨.

### V8.28 — EDI 항차 검증을 '번호 기준'으로 완화 (2026-06-28, 시뮬 6/6 PASS)

**배경**: V8.24-03 항차 가드가 방향문자까지 엄격 비교라, 인천 출발 선박처럼 선사가 왕복을 한 항차(예 `0529W`)로 표기하면 평택 양하분(`0529E`)이 "다른 항차"로 오차단됨.
**수정** (`VoyagePage.jsx` 1616~1617): 항차 비교를 '번호 기준'으로 완화. `_voyCore` 헬퍼 신규 — `_norm` 후 끝의 방향문자 `[EWNS]+$` 제거 + 앞 0(`^0+`) 제거, 빈 문자열이면 `_norm` 폴백. 비교를 `_norm` → `_voyCore`로 교체. leg(양하/선적)는 위에서 POD/POL 내용으로 이미 확정하므로 방향문자를 떼도 안전. 같은 번호면 통과(`0529`==`0529`), 번호 다르면(`0530` vs `0529`) 그대로 차단.
**시뮬(6/6 PASS, Claude 교차검증 완료)**: `0529W↔0529E` 통과 · `529E↔0529E` 통과 · `0530E↔0529E` 차단 · `R067W↔R067E` 통과 · `R068E↔R067E` 차단 · `2613S↔2613N` 통과. (추가 경계 `0000E↔0000W`는 폴백 `_norm`으로 떨어져 보수적 차단 — 번호가 통째로 0인 비현실 케이스라 현장 무영향.)
**검증(번들 PASS)**: `_norm`(1594)·`_regVoy`(1612)가 `_voyCore`(1616) 위 정의 — 스코프 정상. esbuild 정합 OK. vite build 성공(`✓ built in 4m 33s`). 번들에 V8.28·핵심 정규식(`EWNS`) 박힘, 누적기능(색맵·앞뒤맞교환·공지) 유지, 줌 회귀 없음(3화면 `useState(1)`). sw.js·cone.html V8.28 동기화.
**주의**: 수집기(TallymanMailCollector)는 미수정(사용자 확정: 검수앱만). 선박명 가드 등 기존 안전장치 그대로 유지.

### V8.27 — 수석 공지(흐르는 띠) + 딥링크 크래시 수정 + 콘앱 캐시 + 범례 (2026-06-24, 번들검증 PASS)

**[④ 수석 공지 — 신규 기능]**: 수석 대시보드에 "검수원 공지" 작성칸(보내기/내리기/읽음 인원·명단). 검수앱은 로그인된 검수원의 모든 화면 상단에 광고판처럼 흐르는 띠(marquee)로 표시, "확인" 누르면 그 사람 화면에서 사라짐(읽음). 새 공지 오면 다시 뜸. 실시간(Firebase `broadcast/current`, `reads/{id}/{검수원}`). 신규 파일 `components/BroadcastMarquee.jsx`, `firebase.js` 함수 5개(`fbSubscribeBroadcast`/`fbSetBroadcast`/`fbClearBroadcast`/`fbMarkBroadcastRead`/`fbSubscribeBroadcastReads`), `ChiefDashboard.jsx` 작성칸, `App.jsx` 마퀴 마운트.
**[① 딥링크 크래시 수정]** (V8.26-01 대기 항목 해결): 항차 주소 직접 진입(`/#/voyage/...`) 시 VoyagePage가 데이터 늦로딩으로 hook 순서 어긋나 React #310 크래시. → `App.jsx`에 `voyagesLoaded` 게이트 — voyages 로드 전엔 VoyagePage 미마운트하고 "불러오는 중/없음" 표시, 로드 후에만 마운트. 정상 경로(홈→항차) 영향 없음.
**[② 콘앱 카고플랜 캐시]**: V8.26 카고플랜 변경(XRAY 붉은별·PC 줌·배치)이 `cone-cargoplan.js`에 빌드되지만 콘앱은 `?v=__APPV`로 캐시. `cone.html` `__APPV` C7.65 → C7.66 상향해 콘앱도 새 카고플랜 받게 함.
**[③ 베이플랜 범례]** (`BayPlan.jsx`): 새 색 체계에 맞게 "흰 배경 · 글자색=선사(양하)/POD(선적) 지정색 · ★붉은별=X-RAY · 검정=비평택"으로 교체(옛 'POL색/노랑ring/X-RAY보라' 문구 폐지). 범례 블록만 변경, 줌·xrayList fix 미변경.

**검증(번들 PASS)**: esbuild App 진입점(신규 BroadcastMarquee→firebase 연결) + Marquee 단독 정합 OK(pdfjs는 외부 패키지). vite build 성공(`✓ built in 3m 41s`), cone-cargoplan.js(1.6M) 재생성·루트/dist 복사. 번들에 V8.27·`fbSetBroadcast`·`검수원 공지`·`흰 배경`(범례)·누적기능(색맵·앞뒤맞교환·붉은별X-RAY) 박힘. 줌 회귀 없음(베이상세·카고플랜V2 `useState(1)` 유지). cone.html C7.66, sw.js·cone.html V8.27 동기화.
**미검증(브라우저 확인 요망)**: 공지 띠 흐름·"확인" 읽음 처리·딥링크 정상 진입·범례 표시는 배포 후 브라우저에서 확인.

### V8.26-02 — 화면별 줌 기본값 정정 (2026-06-24, 번들검증 PASS)

**증상**: V8.26에서 카고플랜·베이상세까지 전부 22%로 시작됨. 본래 기본값은 카고플랜 100%·베이상세 100%·베이플랜만 22%.
**원인**: V8.25 핀치 줌 도입 시 카고플랜(기존·V2)·베이상세 줌 초기 `useState`를 일괄 `0.22`로 둠. 베이플랜(큰 베이가 한 화면에 안 들어와 22% 필요)과 달리 카고플랜·베이상세는 100%가 맞음.
**수정**: 줌 초기값 — `PrintableCargoPlan.jsx`·`PrintableCargoPlanV2.jsx`·`PrintableBayDetail.jsx` `0.22` → `1`(100%). `BayPlan.jsx`는 `0.22`(22%) 유지. (줌 clamp는 세 화면 모두 `0.15~3`이라 100% 정상 범위.)
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 1m 44s`). 소스 — 베이플랜 0.22 유지, 나머지 3개 `useState(1)` 확인. 번들에 V8.26-02·누적기능 박힘, sw.js·cone.html 동기화.

### V8.26-01 — 베이플랜 그리드 크래시 수정 (V8.26 긴급 버그픽스) (2026-06-24, 번들검증 PASS)

**증상**: 양하/선적 항차에서 "베이" 탭 클릭 시 앱 전체 크래시. 에러 `xrayMap is not defined`(React #310은 2차 증상).
**원인**: V8.26에서 XRAY 붉은별 코드를 `BayPage` 컴포넌트 셀 렌더에 넣으며 `xrayMap`을 참조했으나, BayPage는 그 맵을 `xrayList` prop명으로 받음(부모 BayPlan이 `xrayList={xrayMap}`로 전달). → ReferenceError로 렌더 중단.
**수정** (`BayPlan.jsx`): BayPage 셀 렌더의 별표 조건 2곳(1602·1607행) `xrayMap` → `xrayList`로 교정. (BayPlan 자체 스코프의 `getOpColor`·`cellColor` 내 `xrayMap`은 정상 변수라 그대로 둠.)
**검증(번들 PASS)**: BayPage 셀 렌더(1600~1610)에 `xrayMap` 잔존 0 확인. esbuild 정합 OK. vite build 성공(`✓ built in 4m 38s`). 번들에 V8.26-01 박힘, V8.26 기능(색맵·황금각137.5·PC줌 ctrlKey 20·앞뒤맞교환·⇅·bd-r1) 전부 유지. sw.js·cone.html V8.26-01 동기화.
**대기 중(별개 버그)**: 항차 딥링크(`/#/voyage/...`) 직접 진입 시 VoyagePage 데이터 늦로딩으로 hook 순서 어긋나 크래시(React #310). 정상 경로(홈→항차 선택)는 정상. 요청 시 별도 수정. → **V8.27에서 해결**(App.jsx `voyagesLoaded` 게이트).

### V8.26 — 색·PC줌·배치 통합 (V8.25-05 누적 흡수) (2026-06-24, 번들검증 PASS)

V8.25-04(베이상세 5줄 배치) 위에 5개 기능을 한 번에 얹은 기능 추가 버전. (V8.25-05 베이상세 미세조정 — 위치·온도 가운데정렬 — 포함.)

**[1] PC 줌 분리** (`BayPlan.jsx`·`PrintableBayDetail.jsx`·`PrintableCargoPlan(V2).jsx`): 터치 없는 PC에서만 +/−·맞춤 버튼 + Ctrl+마우스휠 확대/축소 노출. 폰(터치)은 기존 핀치 그대로.
**[2] 무중복 지정색** (`utils.buildContainerColorMap`): 기존 12색 유지 + 13번째부터 황금각(137.5°) 균등 분포로 안 겹치는 고유색 생성. 흰 배경 가독성 위해 명도 낮춤. (i%순환 폐지 → 색 중복 없음.)
**[3] XRAY = 붉은 별**: 카고플랜·베이상세 별 색 검정→붉은색·약간 키움. 화면 베이플랜은 보라 셀 배경 폐지 → 흰 배경 + 한쪽 구석 붉은 ★. (도메인 원칙 "셀 배경색 금지, 글자·표기 색으로 구분" 준수.)
**[4] 화면 베이플랜 그리드 흰 배경 + B안 색 + 5줄 분배 배치** (`BayPlan.jsx`, 103라인 대전환): 어두운(slate) 셀 → 흰 배경 + 검정 글자. 셀 배치 줄1 POL좌·*POD우 / 줄2 컨번호 좌 / 줄3 선사좌·무게중앙·규격우 / 줄4 특수(온도·DG) 중앙 / 줄5 위치 중앙, 굵게(노안). B안 색: 양하=선사코드, 선적=POD만 지정색, 특수표시는 그 컨 지정색 따라감, 나머지(컨번호·무게·위치)는 검정, 비평택 전부 검정. (NO TEMP 경고만 빨강 유지.)
**[5] 베이상세(검정)**: V8.25-05까지 포함(5줄 새 배치 + 위치·온도 가운데정렬).

**검증(번들 PASS)**: esbuild 5개 컴포넌트 + utils 색함수 정합 OK. vite build 성공(`✓ built in 3m 26s`). 번들 — V8.26, `buildContainerColorMap`, `137.5`(황금각), `ctrlKey`(PC줌) 20건, 누적기능(앞뒤맞교환·⇅ 각 2건, `bd-r1` 6건) 박힘. sw.js·cone.html V8.26 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 화면 베이플랜이 다크→흰색 대전환이라 변화 큼. 기기에서 색·간격·완료/쉬프팅 표시 확인 필요.

### V8.25-04 — 베이상세 셀 5줄 분배 배치 (검정) 구현 (2026-06-24, 번들검증 PASS)

**목적**: 베이상세 인쇄 셀을 확대 mock대로 5줄 구조로 재배치. 흰 배경·굵게(노안)·셀 안 고르게 분배(space-evenly).
**확정 배치**:
- 줄1 — POL좌(DLC/DLC) · *PTK 우 (양 끝)
- 줄2 — 컨테이너번호 좌측 (자간)
- 줄3 — 선사 좌 · 무게 중앙 · 규격 우 (3등분)
- 줄4 — 온도 / DG 중앙 (없으면 빈 줄)
- 줄5 — 위치 중앙
**수정** (`PrintableBayDetail.jsx`): `formatCellParts()` 신규(토큰 분리), `renderCell`·`mrRenderCellContent` 새 구조, `.bd-cell`/`bd-fill`/`bd-cell-lines` CSS 갱신(`bd-r1`~`bd-r5`).
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 3m 12s`). 번들에 V8.25-04, `bd-r1`/`bd-r5`/`space-evenly` 박힘, 누적 기능(앞뒤 맞교환·⇅) 유지. sw.js·cone.html V8.25-04 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 베이상세 인쇄가 mock과 맞는지(줄 간격·자간) 기기에서 확인 — 미세 조정 가능.
**다음(예정)**: 무중복 지정색 생성 → 카고플랜 B안 색 → 화면 베이플랜 그리드 B안 색 + XRAY 붉은별.

### V8.25-02 — 수동 트윈 앞뒤 맞교환 "화면 미반영" 수정 (2026-06-24, 번들검증 PASS)

**증상**: 수동 모드(양하/선적) 트윈에서 앞뒤 맞교환을 누르면 음성은 "바꿨다"라고 하는데 카드 위치가 그대로라 교환 여부를 화면에서 구분할 수 없음.
**시뮬(4경우)**: 자동·수동 × 양하·선적 모두 DB swap PASS — 앞↔뒤 위치가 실제로 교환됨. 베이플랜·베이상세에도 반영됨. 즉 **데이터는 정상**.
**원인**: 수동 TwinSearch의 `c1`/`c2`는 로컬 `useState`. swap 후 DB(allContainers)는 바뀌지만 `c1`/`c2`가 갱신되지 않아 카드가 옛 위치를 표시. (자동은 card가 allContainers에서 `useMemo`로 매번 재계산돼 정상 반영 — 수동만 해당.)
**수정** (`SearchPanel.jsx`): swap 직후 `c1`/`c2`의 bay/row/tier를 앞↔뒤로 즉시 맞바꿔(`setC1`/`setC2`) 카드에 바로 반영.
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 4m 40s`). 번들에 V8.25-02 박힘, 누적 기능(앞뒤 맞교환·⇅ 각 2건) 유지. sw.js·cone.html V8.25-02 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 수동 트윈 맞교환 시 카드가 즉시 앞↔뒤로 바뀌는지 폰에서 1회 확인.

### V8.25-01 — 베이플랜 핀치 줌 300% 고정 버그 수정 (2026-06-24, 번들검증 PASS)

**증상**: 베이플랜에서 두 손가락 터치하자마자 300%로 확대된 뒤 조정 불가.
**원인**: 핀치 `useEffect` 의존성이 `[zoom]`이라, 핀치 도중 줌이 바뀌면 effect가 재실행되며 `pinchStartDist`를 0으로 리셋 → 다음 이동에서 `ratio = newDist/0 = 무한대` → 줌 상한 300%에 고정. (카고플랜·베이상세는 ref 패턴이라 정상이었음.)
**수정** (`BayPlan.jsx`): 시작 줌을 `zoomRef`로 읽고(`const zoomRef = useRef(zoom); zoomRef.current = zoom;`), `pinchStartZoom = zoomRef.current`로 변경, effect 의존성을 `[]`로 비워 핀치 도중 재실행 차단. 카고플랜·베이상세와 동일한 ref 패턴으로 일관화.
**검증(번들 PASS)**: esbuild 정합 OK. vite build 성공(`✓ built in 3m 46s`). 번들에 V8.25-01 박힘, V8.25 신규기능(앞뒤 맞교환·⇅ 각 2건) 유지 확인. sw.js·cone.html V8.25-01 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 핀치 줌이 손가락 간격대로 부드럽게 따라오는지 폰에서 1회 확인.

### V8.25 — 트윈 앞뒤 맞교환 전구간 확장 + 핀치 전용 줌 + 베이상세 노안 가독성 (2026-06-24, 번들검증 PASS)

**메모1 — 트윈 앞뒤 맞교환을 자동/수동 · 양하/선적 4곳 전부에서 동작.**
- `GuidedWorkPanel.jsx`: `swapTwinPos`의 'loading 전용' 제한 해제(양하도 동작), ⇅버튼을 양하에도 표시.
- `SearchPanel.jsx`: 수동 트윈 패널에 ⇅앞뒤 맞교환 버튼 + `handleSwapPos` 신규(양하·선적 공용), `fbReassignContainerPosition` import 추가.
- 동작: 앞(c1)을 뒤(c2) 자리로 보내면 `fbReassign` swap이 뒤 컨을 앞 자리로 자동 이동. **자리만 교환, 완료 처리 안 함.**

**메모2 — 줌 핀치(핀치/휠) 전용 전환 + 베이상세 텍스트 가독성 강화.**
- `BayPlan.jsx`: 줌 +/− 버튼 제거(핀치/휠 전용), 22% 시작 유지. `ZoomIn`/`ZoomOut` import 제거.
- `PrintableBayDetail.jsx`: 줌 기본 0.22, 핀치 하한 0.4→0.15, +/−·100% 버튼 제거. 텍스트=배경색+흰글자+굵게, 폰트 키움(7→8.5pt 등). 인쇄 무관.
- `PrintableCargoPlanV2.jsx` · `PrintableCargoPlan.jsx`: 핀치 줌 신규(22% 시작), 인쇄 시 scale 무효 처리.

**검증(번들 PASS)**: esbuild 6개 컴포넌트 정합 OK(firebase external 처리 시 오류 0). vite build 성공(`✓ built in 1m 35s`). 번들 grep — `⇅` 2건·`앞뒤 맞교환` 2건(수동/자동 양쪽) 박힘, `fbReassignContainerPosition` 포함 확인. `sw.js`·`cone.html` 라벨 V8.25 동기화, 루트 index.html 빌드본 참조 정상.
**미검증(기기 확인 요망)**: 노안 텍스트 실제 크기감·핀치 동작 손맛은 폰/기기에서 1회 확인 필요.

### V8.24-03 — 다른 선박/항차 EDI 업로드 차단 (2026-06-24, 시뮬 PASS)

**배경**: 다른 배 EDI나 항차 안 맞는 EDI를 항차에 넣어도 경고 없이 적용(집계·베이구조·info·베이사전 덮어씀)되던 위험 버그.

**수정**: VoyagePage EDI 적용 루프(파싱 직후, allCns 누적 전)에 가드 추가. ① 선박 다름 — 콜사인 둘 다 있고 다르면 차단, 콜사인 없으면 '진짜 선박명'(공백/≥7자)끼리만 비교(CNYNT 코드형은 제외해 재업로드 오차단 방지). ② 항차 다름 — 평택분으로 양하(voy_d)/선적(voy_l) 자동판정 후 대조, 평택 해당분 있으면 "파일명 항차 고쳐 재등록" 유도, 없으면 "다른 항차" 차단. ③ 첫 EDI(등록정보 없음)는 통과. continue로 적용만 막음. (V8.24-02 신원가드 유지.)

**시뮬 PASS**: 같은배/같은항차 통과, 다른배(콜사인) 차단, CNYNT 오차단 없음, 항차 불일치 재등록유도/다른항차 구분, 첫 EDI 통과.

### V8.22-01 — 작업보고 카운트 "265" fix 추가 (2026-06-21, 시뮬 PASS)

**배경**: V8.22(협작 테스트본)에 WorkReportModal의 작업보고 평택 카운트가 354 대신 265로 적게 나오던 버그가 남아 있었음(현황요약·마감점검 V8.20-01과 동일 계열, records의 가짜 POL이 EDI 평택 POL을 덮음). V8.20-02에서 이미 고친 fix를 V8.22 베이스에 얹음.

**수정**: WorkReportModal.jsx 머지에서 pol/pod 제외 보강(`{pol:_p,pod:_d,...rEnrich}`), EDI에 없을 때만 리스트 폴백. 시뮬 PASS(265→354). 빌드 후 번들·sw.js·cone.html APP_VERSION V8.22-01 동기화.

**주의**: shipStructure fuzzy 매칭 순서 수정(V8.20-02)은 V8.22가 이미 446줄 콜사인 오염 가드로 DJCT→XTPG를 사후 차단하고 있어 **중복 회피로 미적용**. 두 방식(입구 정확매칭 우선 vs 출구 선박명 검증)이 같은 버그를 다른 지점에서 막음 — 통합 검토는 추후.

### V8.20-01 — 현황요약/마감점검 분모 "265" 버그 수정 (2026-06-21, 실데이터 시뮬 PASS)

**증상**: SWDN 2606N 선적 화면에서 상단 헤더 `선적 0/265`. 같은 화면 하단은 전부 354(EDI 평택 354 / 리스트 354 / 매칭 354 / 규격합 20DC 206+40HC 140+40RF 8 = 354). 헤더 분모만 89 적게 나옴.

**원인(데이터 확정)**: `VoyageSummaryCard.jsx`·`WorkClosingChecklist.jsx`의 EDI↔records 머지가 `{...e, ...recordsAll}`로 records 전체를 덮어씀. 선적 리스트(헤더 없는 엑셀 등)에 POL이 평택 아닌 가짜값으로 든 컨 89개가 있었고, 그 가짜 POL이 EDI의 KRPTK를 덮어 `isPyeongtaekPort(c.pol)` 필터에서 탈락 → 354→265. (도메인: "헤더 없는 엑셀 fallback이 POD를 POL 자리에 넣음"과 동일 계열. Ⅵ M6.94.31 참조.) 화면리스트(`VoyagePage.containers`)는 ALLOWED_LIST_FIELDS로 pol을 막아 354 정상이라, 두 경로가 갈렸다.

**수정**: 두 컴포넌트 머지에서 `pol`·`pod`만 제외하고 보강(`k!=='pol' && k!=='pod'`). EDI를 항구 단일 진실로, 빈 경우만 리스트 폴백.

**점검 결과**: 같은 머지 패턴이 `App.jsx:297`·`VoyagePage.jsx:1141`에도 있으나 둘은 **평택 필터를 안 쓰는** ContainerDetailModal용 전체목록이라 증상 없음 → 미수정(외과적 변경).

**시뮬레이션(실데이터 = SWDN 2607S EDI 354대, 화면 조건 89개 가짜 pol 주입)**: VoyageSummaryCard 버그코드 265 → fix 354 PASS. WorkClosingChecklist 동일 265 → 354 PASS. 빌드 후 번들에 `!=="pol"&&...!=="pod"` 2건 + APP_VERSION V8.20-01 grep 확인.

**교훈(부록 A-2에 상세)**: 여러 Claude가 같은 화면을 보고도 헤맴 — 354 vs 265 차이와 머지 덮어쓰기 코드를 둘 다 봤으면서 연결 안 하고 Firebase export를 요구(Ⅶ부 "할 수 있는데 안 함"). "이미 됐겠지"·"같은 버그겠지"를 확인 없이 단정(Gate 2 생략). **본 데이터는 끝까지 연결하고, 자료 요청으로 도망치지 말 것.**

### V8.20 — 수집기 연동 입구 `mergeApi.js` 추가 (2026-06-20, 실데이터 시뮬 PASS)

**배경**: 수석검수사가 손으로 하던 "메일 검색 → 선적리스트 병합 + EDI 대조 → 검수앱 투입"을 수집기(Tallyman Mail Collector)가 자동화하려면, 검수앱의 검증된 파서를 그대로 재사용해야 함. 수집기 개발 측이 입구 파일 `mergeApi.js`(파서를 감싸는 다리)와 연동안내 MD를 보내옴.

**설계 핵심 (다리 = contract)**
- 파서 자체는 검수앱이 계속 소유(`utils.js`). `mergeApi.js`는 파서를 **감싸기만** 함 — 파서 로직 미포함, 드리프트 방지.
- 폴더의 파일들(EDI + 여러 선사 CLL 엑셀 + XRAY)을 받아 → 병합본 xlsx(base64) + EDI 대조 리포트를 반환.
- `main.jsx`에 `import './mergeApi.js'` 한 줄 → 배포본에 전역 `window.GMmerge(files) → { xlsxBase64, report }` 생성. React/Firebase 의존 없음.
- 분류: `.edi`→BAPLIE, `.asc`→참조, `.xls/.xlsx`→리스트(recap·cbf·cdl·memo 제외, xray 별도). EDI는 **가장 큰(최종) 파일을 진실**로 채택. `mergeWithEdi`: EDI=진실, 리스트=보강(실번호/엠티실/무게), 차이는 CONFLICTS, EDI에 없으면 NOT_IN_EDI.

**Gate 1·2 검증 결과 — 보내온 파일의 확인요청 3건 대조**
- ① `parseXrayList` 반환 형태 → **수정함**. 보낸 파일은 `{records}` 가정이나 실제는 `{ containers:[번호배열], _matchCount }`. 원본은 XRAY를 0개로 처리하던 버그(연동 측 검증표에 XRAY 없어 미발견). xray 처리부 한 줄만 `out.containers` 사용으로 교체(+ records 폴백 유지).
- ② `mergeWithEdi` 인라인 복사 → **그대로 둠**(import로 바꾸면 mixerUpload deps 번들 유입, 드리프트 위험).
- ③ 컨테이너 필드명(`cn/iso/op/sl/eseal/wt/fe/pol/pod`) → **일치**. parseBAPLIE=`{containers}`, parseListExcel=`{records}`, import 5함수 모두 utils.js 존재.

**발견·기록한 주의점**: `asArrayBuffer`가 ArrayBuffer를 그대로 파서에 넘기는데, 파서 내부 `XLSX.read(input,{type:'array'})`는 정석상 **Uint8Array**를 기대. Node/수집기에서 ArrayBuffer 직접 넘기면 엑셀 파싱이 `Cannot read properties of undefined`로 깨질 수 있음 → 회신 MD에 "엑셀류는 `new Uint8Array()`로 감싸 호출" 명시. (브라우저 File.arrayBuffer() 경로는 대체로 무관.)

**시뮬레이션(실데이터, 실제 mergeApi.js import)**: EDI 3대 + 리스트(충돌·미매칭 포함) + XRAY 1건. 결과 전부 기대값 일치 — 병합 3, CONFLICTS 1(MSKU 무게 18000 vs 99999), NOT_IN_EDI 1(XXXU9999999), **XRAY=Y TGHU1000001**(수정 핵심). 빌드 후 번들에 `window.GMmerge` 등록 + V8.20 박힘 grep 확인.

**산출**: `src/mergeApi.js` 신규, `main.jsx` import 1줄, `mergeApi_검수앱측_회신.md`(수집기 측 전달용 — 확인 3건 답변·입력타입 주의·검증표 포함).

### V8.13 — 자연어 텍스트 조회 음성 2버그 수정 (사용자 보고 2026-06-19, 시뮬 15+ PASS)

**증상(사용자 보고)**: 자연어 탭에서 **텍스트로** 끝4자리 조회 시 음성이 ① "끝4자리"를 "끝사자리"로 ② 컨번호 "1250"을 "천이백오십"으로 통째로 읽음.

**원인**: `describeQuery`(nlSearch.js 438행)가 `끝4자리 ${digits}` 문장을 만들어 화면·음성 공용으로 씀(SearchPanel 592·596 등 speak). TTS가 "4"→"사", 붙은 숫자→통째로 읽음. 음성 표준(spellKo/spellDigits)은 있었으나 `speak()` 경로가 좌표(spellPosString)만 변환하고 일반 컨번호 숫자는 안 풀었음.

**수정(2건, 외과적)**
- `nlSearch.js`: `끝4자리`→`끝네자리`(438행). TTS가 한글 "네"로 정확히 읽음. 화면에도 "끝네자리 1250"으로 자연스럽게 표시(숫자는 붙여 둠).
- `voice.js speak()`: 좌표 변환(spellPosString) 뒤 **4자리 이상 연속 숫자만 한 자씩** 풀기 추가 — `replace(/\d{4,}/g, ...NUM_KO)`. "1250"→"일 이 오 공". 모든 speak 경로에 일괄 적용(음성 표준 일원화). 수량(20대)·시각(17시)·온도(18도)·베이(38번)는 1~3자리라 보존 — 부작용 없음.

**검증(시뮬 `speak_digits_sim.mjs`·`voice_final.mjs`)**: 4자리+ 풀림·1~3자리 보존(수량/시각/온도/베이)·BIC+4자리·6자리, describeQuery 화면("끝네자리 1250")과 음성("끝네자리 일 이 오 공") 분리 동작. 15+ PASS. build.sh PASS, 번들에 "끝네자리"·4자리풀기 정규식·V8.13 박힘, sw.js 동기화.

**주의**: 4자리 이상만 푸므로 컨번호 끝3자리(드묾)는 안 풀림 — 끝4자리 조회가 표준이라 문제없음. 단건 조회(announceContainer)는 원래 spellKo라 정상이었고, 이번 수정은 describeQuery 경로(0건·통계·여러건 안내)를 고친 것.

### V8.12 — LOLO 자연어 조회 지원: 베이 게이트 스킵 (사용자 보고 2026-06-19, 시뮬 9/9 PASS)

**증상(사용자 보고)**: LOLO선(RZOR)은 자연어 검색·조회창이 안 됨. 자동/수동 작업 흐름이 베이를 전제하는데 LOLO는 베이가 없어 막힘.

**핵심 도메인(사용자 확정)**: LOLO는 **베이(선내 위치) 정보만 없을 뿐, 나머지(실번호·규격·적공 F/E·온도·X-RAY)는 다 있다.** 그러니 자연어 탭을 막을 게 아니라 **베이 게이트만 건너뛰고 나머지는 그대로 조회**하면 된다. 별도 화면·토글 신설 불필요 — RZOR(`isLoloShip`)면 자동 처리.

**원인(SearchPanel)**: 자연어 탭이 ① 자동/수동 토글 ② 베이 선택 게이트(`manualBay==null`) ③ 단 선택 게이트(`manualTier==null`)를 거쳐야 조회창에 도달. 셋 다 베이 전제라 LOLO는 게이트에서 막힘. `isLoloShip`이 SearchPanel로 전달조차 안 되고 있었음.

**수정(외과적 — 일반선 흐름 무변경)**
- `VoyagePage`: SearchPanel에 `isLoloShip` prop 전달(한 줄).
- `SearchPanel`: prop 수신. LOLO면 ⓐ 자동/수동 토글 숨김(`&& !isLoloShip`) ⓑ GuidedWorkPanel 분기 스킵 ⓒ 베이 게이트 스킵 ⓓ 단 게이트 스킵 → 곧장 조회창. 조회창 상단에 LOLO 안내 배너("베이는 없지만 실번호·규격·적공·온도·X-RAY는 정상 조회").
- 조회 결과 카드(BigResultCard)는 **무수정** — 이미 `c.bay` 있을 때만 위치 표시하므로, LOLO는 위치만 자동으로 빠지고 나머지 정보는 정상 출력(점검으로 확인).

**검증(시뮬 `/home/claude/sim/lolo_gate_sim.mjs`)**: LOLO는 양하·선적·완료·guideMode켜짐 전 상태에서 조회창 직행(게이트·가이드 스킵), 일반선은 베이→단→조회창 게이트 + 자동 가이드 그대로 유지. 9/9 PASS(회귀 0). build.sh PASS, 번들에 "LOLO 선박 (베이플랜 없음)" 박힘. **새 UI(LOLO 배너) 렌더 PNG 눈 확인 PASS**.

**주의**: 자연어 위치 질문(예: "20번 베이 몇 대")은 LOLO에서 결과 없음 — bay가 없으니 당연(applyNLFilter가 못 거름). 끝4자리 단건 조회·규격/실/온도 조회는 정상. LOLO 탭(LoloTab 리스트)은 그대로 병존 — 자연어 탭은 조회, LOLO 탭은 리스트.

### V8.11 — 해치 제외 4척 확장 + 부두별 호기 + 주야간 작업갯수 집계 (사용자 확정 2026-06-19, 시뮬 19/19 PASS) ★메이저

세 가지 신규 도메인 규칙을 한 빌드에 묶음(전부 사용자 확정).

**① 해치커버 계산 제외 4척 (V8.09-17 일반화)**
- V8.09-17이 `isHatchAutoShip=/TMPZ/i`(TMPZ만, 해치 자동·유압식이라 프롬프트 안 띄움)였음.
- 사용자 확정: **TMPZ·TNJP·RZOR·OBWH 4척은 해치커버 계산을 하지 않는다.** 대신 주야간 작업갯수를 기록(③).
- 수정: `isHatchAutoShip` → `isHatchSkipShip = /TMPZ|TNJP|RZOR|OBWH/i.test(vsl+' '+vslFull)`. 참조 3곳(데크완료 오픈·홀드완료 클로즈·닫기 제안) 가드 교체. 4척이면 해치 프롬프트·장수 계산(hatchPanelsOf)·카톡 보고 전부 자동 건너뜀.
- ⚠️ 지침서 과거판이 'TPMZ'로 오기한 곳 있음 — 정답은 **TMPZ**(사용자 확정).

**② 부두별 양적하 장비(호기) 수**
- 사용자 확정: **PCTC = 4대(1~4호기). PNCT = 5대(1~5호기)** — PNCT는 여객석이 RORO 작업을 해 1대 더 잡음.
- 신규 `utils.js equipNumbersForPier(pier)`: PCTC→1~4 / PNCT→1~5 / 미상→1~5(안전 최대). 부두 판정은 기존 `getPierFromBerth(info.berth)`(V8.09-09, 동부두 6~9=PCTC·13~16=PNCT) 재사용.
- 적용 3곳: GuidedWorkPanel 장비선택 UI / Header 장비탭 / WorkReportModal 장비 4곳. 기존 단일 상수 `EQUIPMENT_NUMBERS`(kakaoShare.js, 1~4호기 하드코딩) 대신 부두 동적 목록. 세 컴포넌트의 미사용 `EQUIPMENT_NUMBERS` import 정리(외과적). kakaoShare.js의 상수 자체는 보존(다른 참조 없음·회귀 방지).
- ⚠️ Header는 현재 항차(`route.voyage`)의 berth로 판정, voyage 없으면 1~5 전체. WorkReportModal은 `voyage.info.berth` 기준.

**③ 주야간 작업갯수 집계 → 작업보고(WorkReportModal)로 (①의 4척 전용)**
- 사용자 확정(2026-06-19): **주야간 집계표 = 작업보고의 일종.** 그러므로 가이드 화면 배지가 아니라 **작업보고 안**에 둔다. (최초안은 GuidedWorkPanel 배지였으나, "가이드 모드로는 완벽한 작업이 안 됨 — 현장이 수시로 바뀜"이라는 사용자 지적으로 폐기. 집계 근거는 모드와 무관한 Firebase 완료 레코드이므로 작업 모드에 묶으면 안 됨.)
- **위치/UI**: WorkReportModal `view==='daynight'`. main에 "📋 주야간 작업보고" 진입 버튼(isHatchSkipShip일 때만). 주간/야간 토글 — 진입 시 `reportShiftToShow(now)`로 자동 선택, 검수원이 눌러 수동 전환.
- **표**: 좌측 규격(20/40/45) × F·E·TOTAL, 하단 풀엠티토탈. 양하·선적 각 표.
- **표시 시각 판정(집계 경계와 별개, +30분 여유)**: `reportShiftToShow(ts)` — 주간 표시창 08:00~17:30 / 야간 17:30~익06:00 / 06:00~08:00 다음 주간 미리. 배경: 보고서는 17:00로 정리하지만 실제 보고는 17:30 전, 야간도 동일.
- **보고기준 자동선택(`buildShiftReport`)**: 완료(작업량) vs 잔여 **총합** 중 적은 쪽을 기준으로 + "○○ 기준" 표기. 이유는 비용/정산이 아니라 **세기 편의 — 적은 쪽 카운트가 빠르다**(검수원이 리스트 기준으로 적은 걸 셈). 동수면 작업량(`<=`). 잔여 0이면 "작업 완료 — 보고 제외".
- **완료/잔여 컷(초 단위, 절대)**: 주간 완료 = 완료시각 **≤ 17:00:00**, 야간 = **≤ 05:30:00**. **17:00:01에 완료해도 잔여분**. 잔여 = 전체 − 그 컷까지 완료(17:00 이후 완료분 + 미완료 모두 잔여). 마감 전엔 그 시점까지 실시간 카운트가 그대로(잠정 표시 없음), 17:00 넘으면 컷이 자연 적용돼 고정.
- **대상·모드무관**: 해치 제외 4척, 작업 모드(가이드/수동) 무관. 양쪽 모드 컨은 WorkReportModal 안에서 `voyage.discharge`/`voyage.loading`로 직접 조립(호출부 VoyagePage 미수정 — 외과적 유지). 평택분만(`isPyeongtaekPort` pod/pol).
- 신규 `utils.js`: `reportShiftToShow(ts)`, `buildShiftReport(conts, shift, now)`. (V8.10 1차의 `workShiftOf`/`tallyDayNight`는 보존하나 화면 미사용 — 향후 정리 가능.)
- **기존 코드 변경 기록**: GuidedWorkPanel의 `DayNightBadge`(정의+렌더 2곳)와 `tallyDayNight` import 제거 — 이유: 주야간 집계가 작업보고로 이전돼 가이드 화면 배지가 불필요·부적절(가이드 모드 의존이 잘못). 영향: 가이드 화면에서 배지만 사라짐, 다른 동작 무변경. 주석 1줄 갱신(이전처 명시).

**검증(시뮬 `v810_sim.mjs`·`v810_hatch.mjs`·`report_shift_sim.mjs`·`report_basis_sim.mjs`·`remain_cutoff_sim.mjs`·`v810_dn_final.mjs`)**: 부두별 호기(PCTC4·PNCT5·berth연동), 표시 판정 경계(17:29주간·17:30야간·05:59야간·06:00주간), 보고기준 자동선택(완료>잔여→잔여·완료<잔여→작업량·동수→작업량·전량완료→제외), 잔여 컷 초단위(17:00:00완료·17:00:01잔여·05:30컷), 규격×F/E 표·풀엠티토탈, 해치 제외 4척 정규식. 합계 **57건+ PASS**. `bash build.sh` PASS, 번들에 "주야간 작업보고"·"풀엠티토탈"·"야간보고 19"·"작업 완료" 박힘, **옛 가이드 배지 "주야간 작업갯수" 문구 0(제거 확인)**, sw.js=V8.11 동기화. **새 UI 렌더 PNG로 눈 확인 PASS**(6관문 새UI 렌더 — 토글·표·기준표기·제외 정상).

**다음 주의**: RZOR는 LOLO라 GuidedWorkPanel 경로를 안 타지만, 작업보고(WorkReportModal)는 모드·LOLO 무관하게 열리므로 주야간 보고가 정상 노출됨(1차안의 가이드 배지 우려가 이전으로 해소). V8.10 1차의 `workShiftOf`/`tallyDayNight`/`DayNightBadge`는 제거됐고, `tallyDayNight`는 utils에 남아있으나 미사용 — 다음에 정리 가능.

### V8.09-18 — 트윈 선적: 한 티어 전체 로우 채운 뒤 위 티어로 (메모3 재정의, 사용자 보고 2026-06-18, 시뮬 PASS)

**정확한 증상(사용자 재설명)**: 20ft 트윈 선적 시, 02 티어에 6개 로우가 있는데 다 안 채우고 1개 로우만 완료한 뒤 그 로우의 04·06 티어로 올라가 선적함. (앞서 "같은 규격" 해석은 틀렸음 — 실제는 로우/티어 순서 문제.)

**원인(guidedQueue.js buildStageOrder 트윈 정렬)**: 트윈 sort 비교가 POD → **로우 → 티어** 순이라, 같은 POD 안에서 한 로우를 골라 그 로우의 02·04·06을 먼저 쌓고 다음 로우로 감.

**수정**: 트윈 sort를 POD → **티어(바닥 02부터) → 로우(해상→육상)** 로 변경. 티어를 로우보다 먼저 비교. 같은 티어 안 로우 방향은 기존 rowRank(landToSea:false, 해상→육상) 그대로. → 02 티어 전체 로우(해상부터) → 04 전체 → 06 순서.

**확정 규칙(사용자)**: 각 티어마다 해상 쪽 로우부터, 한 티어를 다 채운 뒤 위 티어로. (02 전체 해상→육상 → 04 전체 → 06.)

**검증**: node 시뮬 PASS — 02티어 6로우 먼저·04 다음·06 마지막, 각 티어 해상(짝수row)부터. 실제 rowRank 방향 일치 확인. build.sh PASS, 번들에 티어 우선 비교 박힘.

**주의**: 40ft·싱글20·FR 순서는 무변경(트윈 스택 내부 순서만 조정). 데크도 동일 규칙 적용(자동 제시, 포트/난이도 보정은 수동).

### V8.09-17 — 메모 5건 일괄 처리 (사용자 메모 2026-06-18, 시뮬+빌드 PASS)

**메모1 — 수동 양하 '선박에 없음' 오류 재발(끝4자리 조회 후 수동 선적 시 발생, 자동은 정상)**: BigResultCard의 "🚫 선박에 없음(누락 처리)" 버튼은 양하 전용 기능인데 노출 조건이 `!isLoading && !isDone`뿐이라 모드 구분이 없었음. 선적에도 떠서 혼란. → `c._mode === 'discharge'` 추가해 양하에서만 노출. (선적은 위치수정·번호수정 버튼이 별도로 있음.)

**메모2 — 수동 싱글 작업 시 숫자패드 안 나옴(양하·선적 동일)**: SingleSearch 입력칸은 자유질문·음성도 받는 칸이라 `inputMode`가 text였음. → 수동 작업 컨텍스트(베이·단 선택 완료, `manualCtx.selectedGroup != null && selectedTier`)일 때만 `inputMode='numeric'` 적용. 자유질문 화면에선 기존 text 유지(한글 질문 보호).

**메모4 — TMPZ는 해치커버 자동이라 보고 안 함**: `isHatchAutoShip = /TMPZ/i.test(vsl+vslFull)` 플래그 신설. 해치 프롬프트 3종(deckDonePromptD·holdWorkedD·holdDonePrompt) 각 시작에 `if (isHatchAutoShip) return false` 추가 → 오픈/클로즈 제안·음성·버튼 모두 미표시. holdCloseD 음성 분기·클로즈 버튼 렌더는 holdWorkedD에 의존하므로 자동 차단.

**메모5 — 자동은 잔여 보이는데 수동은 진행상태 안 보임(자동 가이드처럼)**: 수동 "📍 Bxx번 홀드/데크 작업 중" 배너에 `잔여 N대` 배지 추가. N은 현재 단의 미완료 잔여(manualGroups의 hold/deck, `_comp` 제외 카운트).

**메모6 — 트윈 선적 시 앞뒤 바뀜 많음, 한번에 위치수정**: 트윈 짝꿍 구분선에 "⇅ 앞뒤 맞교환" 버튼(선적만). `swapTwinPos` 신설 — 앞 컨을 뒤 자리로 reassign하면 `fbReassignContainerPosition`의 swap 로직이 뒤 컨을 앞 자리로 자동 이동. confirm 후 자리만 교환(완료 처리 안 함), 음성 안내.

**보류 — 메모3**: ~~의도 확인 후 별도 처리~~ → **V8.09-18에서 해결**(트윈 티어 우선 정렬). 실제 의도는 "규격"이 아니라 로우/티어 순서였음 — 한 티어 전체 로우 채운 뒤 위 티어로.

### V8.09-16 — 실번호 중복 입력 차단 (일반·XRAY, 0000 예외) (사용자 보고 2026-06-18, 시뮬 10/10 PASS)

**보고**: XRAY 실번호를 서로 다른 컨에 같은 번호로 연속 입력했는데 전부 통과됨. 기본 중복 검사가 없었음.

**원인(GuidedWorkPanel.jsx)**: V8.09-06이 "XRAY 실번호 빈값 차단"만 넣었고 중복 검사는 없음. `saveSeal`/`saveXSeal` 둘 다 입력값을 검증 없이 바로 firebase에 저장.

**도메인 규칙(사용자 확정)**: 실번호는 같은 번호가 없다 — 중복은 완전 차단. 예외 단 하나, `0000`(실 부족 시 표기)은 중복 허용.

**수정**: `dupSealOwner(val, selfCn, kind)` 유틸 신설. 같은 항차·**같은 모드(`_mode`)** 안에서 자기 자신(selfCn) 제외하고 같은 실번호를 가진 컨을 찾는다. 일반 실은 다른 컨의 `c.sl`, XRAY 실은 `c._xraySeal.seal`과 **자기 종류끼리만** 비교. 빈값·`0000`은 검사 제외(예외 허용). 중복이면 alert("이미 ㅇㅇㅇㅇ 컨에 입력됨, 실 부족이면 0000")로 저장 차단. `saveSeal`·`saveXSeal` 양쪽 적용.

**검증**: node 시뮬 10/10 PASS — XRAY/일반 중복차단, 신규통과, 0000 연속허용, 자기자신 제외, 모드격리(discharge X100과 loading X100 별개), 빈값 통과. build.sh PASS, 번들에 차단 문구·0000 예외 박힘.

**주의**: 모드 격리이므로 양하에 쓴 실번호와 선적에 쓴 실번호가 같아도 막지 않는다(서로 다른 작업). 같은 모드 안에서만 유일성 강제.

### V8.09-15 — 가이드 모드 해치커버/베이 선택 음성 처리 (사용자 점검 2026-06-18, 시뮬 16/16 PASS)

**점검 배경(사용자 제보)**: 가이드 모드에서 해치커버를 열고닫을지·다음 베이로 갈지 선택하는 갈림길인데, 사람이 아직 고르지 않았는데 앱이 한 템포 빠르게 "다음 컨테이너"를 말해버린다. 어디로 갈지 미정인 상태에서 음성만 앞서감.

**원인(GuidedWorkPanel.jsx 정독)**: 다음 컨테이너 음성 useEffect가 `card`(다음 예측 컨)와 `voiceOn`만 보고 발화한다. 그런데 선택 배너 표시 조건이 `deckDonePromptD && card` / `holdDonePrompt && card`라, **배너가 떠 있는 순간에도 `card`는 살아 있어** 음성이 그대로 다음 컨을 읽어버린다. 또 선택지 자체의 음성 안내는 코드에 전혀 없었다(화면 텍스트만).

**수정(2건)**
1. **선택지 가드**: 선택 배너가 떠 있는 동안(`choicePrompt` 존재) 다음 컨테이너 음성을 막는다. `choicePrompt` useMemo 신설 — 'deckD'(양하 데크완료→홀드) / 'holdL'(선적 홀드완료→데크) / 'holdCloseD'(양하 홀드완료→닫기) 판정. 다음 컨 음성 effect 의존성·조건에 `choicePrompt` 추가.
2. **선택지 음성**: 갈림길을 음성으로 읽는다(사용자 확정 문구 1가). deckD="데크 양하 완료. 해치커버를 열까요, 다른 데크로 갈까요." / holdL="홀드 선적 완료. 해치커버를 닫을까요, 다른 베이 홀드로 갈까요." / holdCloseD="홀드 작업 끝. 해치커버를 닫을까요." **무응답이면 12초마다 1회 반복**(setInterval 12e3), 선택/배너 사라지면 즉시 정지(clearInterval). voiceOn 따름(음성 끄면 선택지 음성도 꺼짐).
- 음성 useEffect 2종을 프롬프트 정의(deckDonePromptD 등) 뒤로 이동 — 정의 순서상 앞에 두면 참조 불가.

**검증**: node 시뮬 16/16 PASS (배너 시 다음컨 차단·선택지 발화·문구 3종 매핑·평상시 정상 발화·voiceOff 무음·닫기완료 후 미표시). build.sh PASS, 번들에 문구 3종·12e3·V8.09-15 박힘 확인.

**다음 주의**: 12초 반복 간격은 현장 청취 후 숫자만 조정 가능. 선택 버튼의 실제 동작(해치 보고·베이 이동)은 무변경 — 이번은 음성 안내 흐름만.

### V8.09-14 — 선박 상태: ETD 지나도 작업 미완료면 '일정 미확정'(입항지연 오표시 수정) (사용자 보고, 시뮬 PASS, 2026-06-18)

**증상(사용자 보고)**: 작업 시작도 안 한 항차인데 "출항함"으로 뜸. 원인은 입항 지연 — 배가 안 왔는데 예정 출항시각(ETD)만 지나버림.

**원인(`getShipStatus`)**: `현재≥ETD`면 무조건 phase='departed'(출항함). ETD는 예정일 뿐인데 출항을 단정 → 입항 지연 시 "작업 0%인데 출항함" 오표시.

**수정**: `getShipStatus(pm, now, work)` 3번째 인자로 작업 진행률 `{done, total}` 받음. ETD 지난 경우: **작업 완료(total>0 && done>=total)면 'departed'(출항함), 아니면 'unsure'(❓ 일정 미확정)**. 출항함은 작업이 실제 끝났을 때만. 호출부(VoyagePage)는 `containers`/`compMap`로 현재 모드 완료수 계산해 전달. unsure tone=amber(❓ 일정 미확정).

**검증(시뮬 `/home/claude/sim/status_v14.mjs`)**: ETD지남+작업0%→일정미확정 / ETD지남+작업100%→출항함 / 절반→미확정 / 입항전→항해중 / 정박중 유지 / 작업데이터없음+ETD지남→미확정. 6케이스 PASS. 빌드 PASS(V8.09-14).

### V8.09-13 — PORT-MIS 같은 선박 여러 항차 중 '다음 작업 항차' 선택 (사용자 보고, 시뮬 PASS, 2026-06-18)

**증상(사용자 보고+화면)**: PACIFIC SHENZHEN(PCSZ, 9V8012)이 실제로는 6/19 19:00 입항 예정(아직 항해중)인데, 앱은 지난 항차(입항 6/17 18:00 / 출항 6/18 12:00)를 매칭해 "📍 PCTC·E6 정박중"으로 잘못 표시. PORT-MIS에 같은 선박의 두 항차(지난 6/17~18 + 다가올 6/19~20)가 공존.

**원인(`VoyagePage.jsx` PORT-MIS 매칭)**: 콜사인 prefix 후보를 **updatedAt(저장 시각) 최신** 순으로만 정렬(V8.01). 실제 입출항 일정(ETA/ETD)과 무관 → 다음 작업할 항차가 아니라 엉뚱한 항차 선택. 게다가 화면 시점이 옛 항차 출항(12:00) 직전이라 "정박중"으로 보임.

**수정**: 후보 정렬을 '다음 작업 항차' 기준으로 변경(09-11의 `parsePortMisDateTime` 재사용). 현재시각 now 대비 ① **아직 안 끝난 항차(ETD≥now) 우선 — 그중 ETA가 가장 늦은(가장 나중) 일정** (옛 항차가 출항 직전이어도 미래 일정이 따로 있으면 그쪽이 다음 작업분) ② 모두 끝났으면 ETD 최신 ③ 시각 없으면 updatedAt. 정렬 후 vesselName/berth 선택 로직이 정렬 순서대로 1순위를 집음.

**검증(시뮬 `/home/claude/sim/pm_match_sim2.mjs`)**: PCSZ old(6/17~18)+new(6/19~20) — 6/18 11:26·6/17 20:00·6/19 20:00·6/21·6/16 전 시나리오에서 new(다음 작업 항차) 선택, 과거+미래 케이스도 미래 우선 PASS. 빌드 PASS(V8.09-13).

**주의(현장)**: 코드는 PORT-MIS에 두 항차가 다 있을 때 올바른 쪽을 고르는 것. 만약 PORT-MIS에 새 항차(6/19)가 아직 안 올라왔으면, 새 PORT-MIS 엑셀을 올려야 새 일정이 잡힘.

### V8.09-12 — 수석대시보드 CNYNT 중복 제거: vsl 자리 항만코드 그룹 제외 (2026-06-18)

**증상(사용자 보고+화면)**: 수석대시보드에서 OBWH(D5MO4)와 CNYNT가 같은 항차(2665E·2663E·2661E)·같은 양하 수치(211·271·224)로 중복 표시. CNYNT는 쓴 적 없음. 사용자 확인: CNYNT=중국 옌타이(연태) 항만코드이며 OBWH의 목적지(POD)와 동일, 작업일자·항차·양하 동일(선적은 0).

**원인**: OBWH 항차의 양하 데이터가 어떤 voyage에 `info.vsl="CNYNT"`(목적항 UN/LOCODE)로 잘못 저장됨. `tallyVoyagesByShip`(firebase.js)이 vsl로만 그룹핑 → 항만코드를 별도 선박으로 오인해 OBWH와 중복 카드 생성. 선적 0은 양하 데이터만 항만코드로 그룹된 정황과 일치.

**수정**
- `utils.js`: `isPortCode(vsl)` 신규 — UN/LOCODE 항만코드 판별. 공백 없는 5자 영문 + 국가 prefix(CN/KR/JP/TW/HK/VN/TH/SG/MY/PH/ID/RU) 또는 알려진 항만 목록(CNYNT·CNTAO·KRPTK 등 30개). 선박명은 공백 포함 또는 4자 이하 약자(OBWH/RZOR/ATRP)라 오검출 없음.
- `firebase.js tallyVoyagesByShip`: vsl이 `isPortCode`면 `continue`(선박 그룹 제외). 데이터는 보존, 표시만 제외.

**검증(시뮬 `/home/claude/sim/cnynt_sim.mjs`, `port_code_sim.mjs`)**: CNYNT/KRPTK/CNTAO→항만코드 true, OBWH/RZOR/ATRP/선박명→false. 수정 후 CNYNT 카드 사라지고 OBWH·RZOR·ATRP 유지 PASS. 빌드 PASS(V8.09-12).

**주의(현장)**: Firebase에 vsl="CNYNT"로 저장된 항차 데이터 자체는 코드로 안 바뀜 — 화면에선 중복이 사라지지만, 근본 정리하려면 그 voyage의 vsl을 OBWH로 고치거나 삭제 필요. 예방: 향후 vsl 입력/EDI 추출 시 항만코드가 vsl로 새지 않게 점검(별도 과제).

### V8.09-11 — PORT-MIS 선박 현재 상태 표시: 항해중/정박/출항 구분 (실데이터 PASS, 2026-06-18)

**요청**: 인천 정박이면 인천, 평택으로 오는중이면 항해중, 평택 정박이면 부두 표시.

**구현(`utils.js`+`VoyagePage.jsx`)**: `getShipStatus(pm, now)` — ETA/ETD vs 현재시각(입출 보조). phase: sailing(현재<ETA) / berthed(ETA≤현재<ETD) / departed(현재≥ETD) / unknown. `parsePortMisDateTime`, `isPyeongtaekPortName`. 표시: 평택+정박중만 부두 배지, 그 외 상태 라벨(🚢 평택 항해중 / ⚓ 인천 정박중 / ↗ 인천 출항함). 검수앱만.
- 검증(시뮬 `status_verify.mjs`, 실데이터): TIAN HAI PING ZE 시각별 전환·인천 선박 구분 6케이스 PASS.

### V8.09-10 — 입력창 키보드 모드: 이름만 한글, 선박명 영문, 나머지 숫자패드 (2026-06-18)

**요청(사용자)**: 검수앱/콘앱/벌크앱에서 이름 칠 때 말고는 한글 자판 안 씀. 일반 텍스트창은 영문, 숫자만 필요한 곳은 숫자 패드.

**확정 기준(사용자 2026-06-18)**: ① 검수원 이름 = 한글 그대로. ② 선박명(+콜사인·CASP코드·컨번호) = 영문 강제(한글 제거 + 대문자). ③ 그 외 전부(조회창·실번호·XRAY실·엠티실·위치 bay/row/tier·개수) = 숫자 패드. ④ 온도 = decimal 패드. ⑤ 데미지·메모·사유 = 이미 한글 텍스트라 그대로(데미지 유무 등은 버튼 클릭이라 입력창 없음). ⑥ 자연어 질문창(검수앱 음성검색, 콘앱 "7번 베이 콘") = 한글 질문 가능하므로 예외로 그대로.

**구현**
- `src/inputUtils.js` 신규: `stripHangul`(자모·완성형 제거), `toEnglishUpper`(한글제거+대문자), `engChange(setter)` onChange 래퍼, `ENG_INPUT_PROPS`(영문 자판 유도: inputMode text·autoCapitalize characters·lang en), `NUM_INPUT_PROPS`(inputMode numeric), `DECIMAL_INPUT_PROPS`. 한글 차단은 onChange가 실시간 제거(강제), 자판 유도는 props가 보조.
- 검수앱 적용: 선박명/콜사인/CASP코드(ShipMatrix) 영문강제, IMO 숫자. 컨번호(ExtraContainer) 영문강제, 온도 decimal, 실번호 숫자. 실번호·XRAY실·E실(GuidedWorkPanel) 숫자. 컨번호 조회창(BigResultCard) 숫자. 위치 bay/row/tier·실번호·엠티실류(ContainerDetailModal) 숫자. SearchPanel 끝4자리 조회창은 기존 inputMode numeric 유지.
- 한글 유지 확인: 검수원 이름(StaffManager·Inspector), 사유(WorkReport), 데미지내용·메모(ExtraContainer) — 변환 없음.
- 콘앱/벌크앱: 콘앱 입력창은 zoom 슬라이더 + 자연어 질문창(cqaInput, 한글)뿐 → 영문/숫자 대상 없음. 벌크앱은 input/textarea 자체가 없음(버튼 위주). 두 앱은 작업 대상 없음.

**검증(시뮬 `/home/claude/sim/kbd_sim.mjs`)**: toEnglishUpper 6케이스(한글섞임·소문자·자모 등) PASS. 빌드 PASS(V8.09-10), 번들에 한글제거 정규식·숫자패드 반영.

### V8.09-09 — PORT-MIS 부두(berth) 파싱 수정: "계선장소" 다중컬럼 다수결 (실데이터 download.xlsx PASS, 2026-06-18)

**증상(사용자 보고+화면)**: PORT-MIS에 새 엑셀을 올렸는데도 "⚠ 부두 정보 없음 (옛 데이터)" 배너가 계속 뜸. 예: TIAN HAI PING ZE(BOPY4), 저장 06.18 10:17, 매칭 callsign 정상인데 berth 없음.

**원인(실엑셀 download.xlsx 분석으로 확정)**: PORT-MIS 엑셀 헤더의 **"계선장소"가 3개 컬럼에 걸침** — 15=시설코드(MBM), 16=선석번호(15), **17=진짜 부두명("동부두 15번선석")**. 파서는 헤더 매칭으로 15(코드)를 berthRaw로 잡고, 17로 보정하는 로직이 두 가지 결함:
- ① **첫 데이터 행 하나로만** 부두명 컬럼을 탐색. 이 엑셀 첫 행은 "백령 153호 / 남항 모래부두".
- ② 보정 정규식이 `[동서남북]부두|N번선석|...`라 **"남항 모래부두"를 못 잡음**("남항"은 "남부두"가 아님). 첫 행 매칭 실패 → 보정 안 됨 → berthRaw가 15(MBM)에 머묾 → `isValidBerth("MBM")=false` → **전 선박 berth 무효** → "부두 정보 없음".
- 데이터 확인: 현재 정규식은 100건 중 34건만 매칭. 17번 부두명 종류 27가지(동부두/남항/신항/북항/송악부두/돌핀/터미널 등).

**수정(`utils.js parsePortMisExcel`)**: berth 컬럼 보정을 "첫 행 정규식" → **"헤더 berthRaw~+4 컬럼을 여러 데이터 행(최대 40개) 스캔, 부두명 최다 컬럼 다수결 선택"**으로 변경. 부두명 정규식도 확장: `[가-힣]+부두|\d+\s*번?\s*선석|\d+\s*선석|[가-힣]+터미널|돌핀|컨테이너|[가-힣]+항\s`. 한 건도 안 잡히면 헤더 위치 유지(안전).

**검증(실엑셀 100척)**: 수정 후 berth 정상 100/100(기존 다수 무효). TIAN HAI PING ZE → "동부두 15번선석" → PNCT 15번 ✅. PCTC 12척/PNCT 10척 자동분류. 컨테이너 외 부두(모래부두·돌핀)는 berth 텍스트 보존 + pier=null → 회색 "📍부두명" 박스(경고 아님). 빌드 PASS(V8.09-09), 번들에 정규식 박힘.

### V8.09-08 — 새 선박 등록에 LOLO 토글 추가 (대체선 대응, 시뮬 PASS, 2026-06-18)

**배경(사용자 요청)**: 새 선박 등록의 3조건(엠티 실 정책 none/verify/attach)에 **LOLO 조건도 추가** — 본선이 수리·고장 시 투입되는 대체선을 검수사가 직접 LOLO로 지정할 수 있게. (V8.09-07은 RZOR만 코드에 박아 한정했는데, 대체선은 코드 수정 없이 현장에서 등록 가능해야 함.)

**수정(`ShipPolicyModal.jsx`)**
- `lolo` state + LOLO 토글 버튼 추가(엠티 mode와 독립, 항상 표시). "LOLO 선박 (베이플랜 없음)" — 대체선 지정 안내 포함.
- 저장 핸들러: 기존엔 `mode==='none'`이면 정책 미저장이었으나, **`mode==='none' && !lolo`일 때만** 미저장으로 변경. LOLO만 켜고 엠티 정책 none인 대체선도 저장됨. policy에 `lolo:true` 포함.
- `mode==='none' && lolo` 케이스 label = 'LOLO 검수 (베이 없는 명세선) · LOLO'.

**연결**: 저장된 정책은 `fbSaveShipPolicy`가 `...policy`로 lolo 보존 → Firebase `shipPolicies` → `extraPolicies` 구독 → `matchShipPolicy({...DEFAULT,...extra})` 병합 → `isLoloShipByPolicy`(V8.09-07)가 읽음. 등록 즉시 LOLO 판정 반영.

**검증(시뮬 `/home/claude/sim/lolo_register_sim.mjs`)**: 대체선 등록 전 false → LOLO 토글 켜 저장 후 true(선박명·코드힌트), 엠티 verify+LOLO 동시 등록 시 둘 다 유지. 회귀 RZOR true·TPMZ false·TNJP false. 빌드 PASS(V8.09-08), 번들에 토글·판정 박힘.

### V8.09-07 — LOLO 검수 리스트를 RZOR 전용으로 한정 (TPMZ 오판 수정, 시뮬 PASS, 2026-06-18)

**증상(사용자 보고)**: LOLO 항목은 RZOR(RIZHAO ORIENT)만 쓰는데, **TPMZ(일반 베이 선박)에도 적용**됨 — 수석대시보드에 TPMZ LOLO 리스트가 잘못 생성됨.

**원인**: LOLO 판정이 `ChiefDashboard.loloVoyages`·`VoyagePage.isLoloShip` 둘 다 **"모든 컨테이너에 bay/row/tier가 없으면 LOLO"**(위치좌표 유무)였음. RZOR는 IFCSUM이라 좌표가 없는 게 정상이지만, 일반 베이 선박(TPMZ)이 위치정보 없이 올라오면 똑같이 LOLO로 오판. 선박 식별이 전혀 없었음.

**사용자 확정(2026-06-18)**: TPMZ는 베이플랜 있는 일반 선박. LOLO는 **선박명으로 한정 — RZOR만**.

**수정**
- `shipPolicies.js`: RIZHAO ORIENT 정책에 `lolo: true` 추가(TPMZ·TNJP·ATRP엔 없음). alias에 항차 prefix `R063`도 추가(선박명 누락 리스트도 인식). 신규 헬퍼 `isLoloShipByPolicy(vsl, extraPolicies, hints)` — 정책 매칭 후 `lolo===true`만 LOLO.
- `ChiefDashboard.jsx`: `loloVoyages` 판정을 "위치좌표 없음" → `isLoloShipByPolicy(vsl, extraPolicies, [voy·voyage·callsign])`로 교체. 의존성에 extraPolicies 추가.
- `VoyagePage.jsx`: `isLoloShip` 동일 교체. LOLO 탭 노출·자동전환이 이 한 값으로 통제되므로 TPMZ는 베이 탭, RZOR만 LOLO 탭.

**검증(시뮬 `/home/claude/sim/lolo_sim.mjs`)**: RZOR(선박명/alias/R063힌트) → LOLO=true / TPMZ·TNJP·ATRP·미등록 → 모두 false PASS. 빌드 PASS(V8.09-07), 번들에 lolo 판정·R063 박힘.

**주의(현장)**: 코드 변경 후 Firebase에 이미 저장된 TPMZ 데이터는 안 바뀜 — 화면에선 LOLO 리스트가 사라지지만(새 판정), 잘못 생성된 옛 LOLO 흔적은 데이터에 남을 수 있음.

### V8.09-06 — 엠티실 완전삭제 + XRAY 실번호 미입력 시 양하확인 차단 (ATRP/RZOR 실데이터 + 시뮬 PASS, 2026-06-18)

**문제1 — 엠티실 테스트 기록 후 완전 삭제 불가 (ATRP/RZOR)**: 공컨 엠티실(eseal)을 테스트로 입력하면 지울 수 없었다. eseal 표시부에 삭제 버튼이 없고(실오류/리씰만 삭제 가능), 저장 핸들러 `handleSaveEseal`이 attach 모드(ATRP)에서 빈값을 막아 1자라도 남겨야 했다.
- 수정(`ContainerDetailModal.jsx`): `handleClearEseal` 신규 — 빈값으로 `fbSetEmptySeal` 저장(완전삭제), eseal_wrong/reseal은 보존. eseal 표시부 헤더에 `c.eseal`일 때만 보이는 "삭제" 버튼 추가. `fbSetEmptySeal`은 빈값 저장을 차단하지 않으므로 firebase 변경 불필요(이력은 자동 누적).
- 검증: 삭제 전 표시=실번호·삭제버튼 보임 → 삭제 후 표시="미입력"·삭제버튼 사라짐(완전삭제) PASS.

**문제2 — XRAY 대상 실번호 미입력인데 양하확인됨**: XRAY 대상 컨은 실 체결 후 XRAY 실번호(seal)를 입력해야 하는데, 완료 핸들러들이 검증 없이 바로 완료 처리했다.
- 사용자 확정(2026-06-18): **완전히 막기**(실번호 입력 전까지 양하확인 불가), 필수 기준은 **XRAY 실번호(seal)만**(eseal 불필요). 양하(discharge)에만 적용, 선적·비XRAY는 영향 없음.
- 수정(완료 경로 5개 파일 전부): `mode==='discharge' && _xray && !seal.trim()`이면 alert 후 완료 중단.
  - `GuidedWorkPanel.jsx`: `handleConfirm`(예측 카드), `handleFixPick`/`handleTwinFixApply`(수정 경로 — 실제 컨은 `allContainers`에서 _xraySeal 조회). `xraySealMissing`·`xrayMissingByCn`·`blockIfXrayMissing` 헬퍼.
  - `ContainerDetailModal.jsx`(상세 완료), `BigResultCard.jsx`(결과카드), `SearchPanel.jsx`(트윈 일괄), `ContainerList.jsx`(목록).
- 검증(시뮬): seal미입력→차단 / seal입력→완료 / 공백→차단 / 비XRAY→완료 / 선적모드→완료 / 트윈 한쪽 미입력→차단 전부 PASS. 번들에 가드 7곳 박힘 확인.

**빌드 PASS(V8.09-06).** 시뮬: `/home/claude/sim/xray_sim.mjs`, `eseal_sim.mjs`.

### V8.09-05 — 해치커버: 양하 후 선적할 게 있으면 닫지 않음 (STSE 2646W+2645E 실EDI PASS, 2026-06-18)

**증상**: 자동(가이드)모드 양하에서 홀드까지 끝내면 선적 여부를 확인하지 않고 해치커버를 닫으려 함. 같은 베이에 선적할 게 있는데도 닫을지 물음.

**원인(`GuidedWorkPanel.jsx`)**: `holdWorkedD`(클로즈 제안 조건)가 양하 홀드 완료분 존재만 판정하고 선적 잔여를 보지 않음.

**수정**: `holdWorkedD`에 "같은 그룹센터에 평택분 선적 잔여(`_mode==='loading' && _ptk && !_comp`)가 있으면 false(close 보류)" 추가. 선적까지 완료되면 true. 선적 EDI 미업로드면 잔여 0 → 기존대로 닫음(안전).

**검증(실EDI 합본 2646W+2645E)**: 양하홀드 있는 4그룹(8·12·16·24) 전부 선적 잔여 존재 → T1(양하완료·선적미완) close 금지, T2(모두완료) close 제안, T3(선적없음) close 제안, T4(선적 1개 잔여) 금지/0개 제안 PASS.

### V8.09-04 — 양하 규칙3: 40ft 풀 일반→풀 리퍼 (베이그룹 단위, 엠티 제외, 물리종속 우선) (STSE 2646W/2645E 실EDI + 합성 PASS, 2026-06-18)

**배경**: V8.09-03이 "40ft 먼저"는 넣었으나 40ft 안의 일반/리퍼 순서 구분이 없었음. 사용자 규칙3 = "40일반 → 40리퍼 → 20트윈 → 20싱글". V8.09-03 양하 데크 40일반/리퍼 전환 70회 → 미구현 확정.

**사용자 확정 규칙(2026-06-18)**: ① 단위 = 베이 그룹, 풀 40일반 전부 → 풀 40리퍼 전부. ② 엠티(공컨) 40ft는 순서 구분 없음 → 풀 뒤 층 순서. ③ 물리 종속 절대 우선(리퍼가 일반 위면 리퍼 먼저). ④ 선적 FR/OT 마지막은 현행 유지(홀드 FR은 데크 작업 전).

**수정(`guidedQueue.js`)**: `reorderFullReeferLast` 신규(베이그룹→로우스택, 스택 맨위 카드 부류로 풀일반→풀리퍼→엠티 정렬, 스택 내부 보존), `cardIsReefer` 로컬 헬퍼.

**검증**: 회귀(2646W) 티어단조 0·물리종속 0, 합성A(독립) 풀일반<풀리퍼, 합성B(충돌) 리퍼위 먼저, 합성C(엠티) 풀<엠티 전부 PASS. 선적 회귀 불변.

### V8.09-03 — 가이드 양하/선적 순서: 40ft 우선·싱글→트윈→40ft (STSE 2645E 실EDI PASS, 2026-06-17)

**배경**: 자동(가이드)모드 양하에서 "홀드 40피트 우선" 규칙 미준수. 같은 단 40·20 혼재 시 층마다 40↔20 순서 → 스프레더 40전용↔20트윈 반복 전환 → 작업 지연. 40ft 다 끝낸 뒤 20ft여야 함.

**원인**: `guidedQueue.js` 양하 cmp가 티어(층)를 40/20 구분보다 우선 → 층마다 40·20 재혼합. 24번 홀드 시뮬에서 t08·t06마다 전환 발생 확인.

**도메인 사실(사용자 확정 2026-06-17)**: 40ft 위에는 20ft가 절대 안 실린다(40ft 중간 콘 구멍 없음). EDI 281개 40ft 전수 — 같은 단 40ft 위 20ft 0건. → 양하 "40ft 먼저 전부", 선적 "트윈 먼저 다 싣고 40ft 나중"이 적재 종속과 충돌 없음.

**수정(`guidedQueue.js`)**
- 양하: `reorder40FirstForDischarge` — 각 단(데크/홀드) 안 40ft를 20ft보다 앞으로. 종속 안전장치 보존. FR 최우선 양하 유지.
- 선적: V7.99-6 "트윈 아래 40ft 끌어오기" 제거(불필요·순서 파괴). 단 내부 20싱글→트윈→40ft(`buildStageOrder`). 트윈은 POD→로우→바닥티어.
- 선적 데크도 홀드와 동일 통일(사용자 확정): 자동은 20싱글→트윈→40ft, 포트분리·난이도 케이스는 수동 보정.

**검증(STSE 2645E 전 베이, FR 제외)**: 양하 40ft우선 13/13, 선적 홀드 6/6, 선적 데크 7/7 PASS. 16·15·17 "FAIL" 의심은 FR(42P0/22P0) 우선양하 정상동작. 빌드 PASS(V8.09-03).

### V8.09-02 — ★긴급 핫픽스: ITEM 빈칸→Full 강제 제거 (전 선박 공컨 오집계, 실데이터 PASS, 2026-06-17)

**증상(현장 보고)**: V8.09-01 배포 후 **전 선박에서 업로드/리스트 집계 이상** — 공컨(Empty) 개수가 틀림. 일반 베이 선박(STSE 등)에서 선적/양하 집계가 어긋남.

**원인(코드+실데이터로 확정)**: V8.09-01에서 RIZHAO 선적용으로 넣은 ITEM 공컨 규칙에 `else if (itemRaw === '') fe = 'F'`(빈칸=Full 강제)가 포함됨. ITEM 헤더가 **품목·순번 등 다른 용도**인 일반 선박 리스트에서, ITEM 빈칸 행이 전부 Full로 덮어써져 그 아래 정상 판정 경로(TYPE/SIZE 끝글자, SOC seal)로 Empty가 될 컨이 모두 Full로 오집계됨. `parseListExcel`은 전 선박 공통 경로라 전체 영향.

**수정**: utils.js 1883행 `else if (itemRaw === '') fe = 'F'` 한 줄 제거. ITEM "공컨테이너"=Empty만 남김. 빈칸은 아래 정상 경로로 흘려보냄(RIZHAO 선적은 끝글자 없어 기본 Full로 동일 동작).

**검증(V8.08-06 정상본 vs V8.09-02 동일성 + RIZHAO 기능 유지)**
- 일반선 3양식(명시 F/E·ITEM 품목용도·SOC) 모두 V8.08-06과 **완전 동일** → 일반선 회귀 0 PASS.
- RIZHAO R063W: EMPTY 71건 유지, 잡시트 차단 170건 유지 → LOLO 선적 기능 보존 PASS.
- 빌드 PASS(V8.09-02), 번들 반영 확인.

**교훈**: 특정 선박(RIZHAO) 전용 규칙을 전 선박 공통 함수(`parseListExcel`)에 넣을 때, "빈칸=기본값 강제" 같은 광범위 분기는 다른 선박을 덮어쓴다. 선박 고유 규칙은 **양성 조건(공컨테이너=E)만** 잡고, 미해당은 기존 경로에 위임할 것.

### V8.09-01 — LOLO 선적 리스트 수석대시보드 표시 (RIZHAO 선적분, 실데이터 PASS, 2026-06-17)

> ⚠️ 이 버전은 ITEM 빈칸→Full 강제 버그로 전 선박 공컨 오집계 발생. V8.09-02에서 핫픽스. 아래 기능 설명은 유효(규격 매핑·잡시트 가드·LOLO 카드·ITEM 공컨=Empty는 정상).

**배경(증상)**: RIZHAO ORIENT(RZOR) 같은 LOLO 선박에서 수석대시보드에 **양하 LOLO 카드만 뜨고 선적이 안 보임**. 검수사 질문 "선적은 어디서 보나". EDI(IFCSUM/BAPLIE) 없이 LOADING LIST 엑셀만 들어오는 선적분이 카드 생성에서 누락됨.

**원인(실데이터 RD-Loading_List_R063W__FIIS.xls 분석으로 확정)**
- ChiefDashboard `loloVoyages`가 카드 생성을 `sec.ediContainers` 유무로만 판정. EDI 없는 선적 리스트는 버려짐.
- 부수 1: RIZHAO 선적 규격 표기(`20'D`/`40'H`/`40'R`/`45'H`/`20'L`)를 `deriveIso`가 인식 못 함 + `type_i`에 `ty/sz` 없음.
- 부수 2: F/E 판정 — RIZHAO는 엠티에도 실 부착 정책이라 실 유무로 못 가림. ITEM "공컨테이너"가 유일 근거.
- 부수 3: 선적 엑셀에 작업자 메모·이전 항차(062W)·낙서 잡시트 4개 → fallback 과집계(170→213).

**수정(최소 수술적)**
- `deriveIso`: RIZHAO 표기 매핑(`20'D`/`20'L`→22G1, `40'H`→45G1, `40'R`→45R1, `45'H`→L5G1, `40'D`→42G1). 작은따옴표 포함이라 타 선사 충돌 없음.
- `type_i`: 헤더 패턴에 `ty/sz`·`ty sz` 추가.
- F/E: ITEM 컬럼 "공컨테이너"=E. (V8.09-01의 빈칸=F 부분은 V8.09-02에서 제거.)
- 잡시트 가드: `formalSheetParsed` — 정식 헤더 시트가 확정되면 이후 헤더 없는 fallback 셀스캔 끔. 헤더 시트 전무 시에만 fallback 유지.
- ChiefDashboard `loloVoyages`: 카드 생성을 EDI∪records 유무 + 위치좌표 없음으로 변경. 일반 베이 선박 영향 0.

**도메인 확정**
- RIZHAO 선적 LOADING LIST: 규격은 `TY/SZ` 따옴표 표기, F/E는 `ITEM` 컬럼 "공컨테이너"가 유일 근거(실 유무 무효 — 엠티도 실 부착).
- 검수사가 올린 선적 엑셀에 작업자 메모/이전항차 잡시트가 섞여 올 수 있음 → 정식 헤더 시트 우선, 헤더 없는 시트는 정식 시트 있으면 무시.

### V8.04-02 초과 모달 스코프 오류 fix — extraModalOpen is not defined (사용자 보고, 렌더 PASS, 2026-06-16)
- **증상**: 초과 컨 버튼 클릭 안 됨 + 작업 베이→데크/홀드 선택 시 "extraModalOpen is not defined" 크래시(앱 전체 격리).
- **원인**: extraModalOpen 상태·버튼은 SearchPanel 본체에 선언했는데, `<ExtraContainerModal>` 렌더를 자식 컴포넌트 SingleSearch 안(HelpModal 옆)에 잘못 넣음. SingleSearch에서 부모 변수 접근 불가 → 미정의. 이 크래시가 SearchPanel 전체를 죽여 베이/단 선택 등 모든 조작에서 에러 화면이 떴음(두 증상 동일 원인).
- **수정**: ExtraContainerModal을 SingleSearch에서 제거하고 SearchPanel 본체 return 안(상태·버튼과 같은 스코프)으로 이동. 정적 분석으로 선언·사용·렌더가 모두 SearchPanel 함수 안에 있고 SingleSearch에 잔존 없음 확인 PASS.
- **교훈(중요)**: 빌드 성공이 런타임 무결성을 보장하지 않는다 — 미정의 변수 참조는 빌드를 통과하고 런타임에만 터진다. 새 상태/모달 추가 시 **선언·사용·렌더가 같은 컴포넌트 스코프인지 빌드 후 정적 점검**할 것.

### V8.04-01 초과 모달 React error #310 fix (사용자 보고, 렌더 PASS, 2026-06-16)
- **증상**: 초과 컨 추가 시 React error #310 (Rendered fewer/more hooks) — 화면 격리됨.
- **원인**: ExtraContainerModal 본문 안에서 Chip/Field를 정의하고 JSX에서 `<Field>`/`<Chip>` 컴포넌트로 사용. 매 렌더 새 컴포넌트 타입이 되고, 조건부 입력(RF 온도·데미지 내용)이 나타났다 사라지며 Hook 트리가 어긋나 #310 유발.
- **수정**: Chip/Field를 모듈 스코프 함수로 분리(재생성·재마운트 제거). react-dom/server 렌더 테스트 PASS(open=true 마운트 정상, #310 없음).
- 교훈: 컴포넌트 본문 안에서 다른 컴포넌트를 정의해 JSX로 쓰지 말 것 — 모듈 스코프로 뺀다(지침서 0-7 연장).

### V8.04 초과 컨테이너 — 신고정보 입력·리스트 포함·별도 집계·삭제 (사용자 요청, 시뮬·PNG PASS, 2026-06-16) ★메이저

**사용자 요구(누적)**: 초과 컨(신고 리스트에 없는데 내려진 것)을 ① 신고용 기본정보와 함께 입력 ② 검수·양하 리스트와 토탈에 포함(색만 강조) ③ 초과분만 따로 집계·제출(검수리스트처럼) ④ 잘못 넣은 것 수정(삭제).

**작업**
- 입력 모달(ExtraContainerModal.jsx 신규): 번호 + 규격(20/40ST/40HC/45)·적공·타입(일반/RF/FR/OT/TK)·리퍼온도·실번호·데미지 유무를 버튼 탭으로. 기존 window.prompt 2단계 → 모달 교체. (V8.04-03: 데미지 라벨에서 "책임소재" 문구 제거 — 검수사에게 불필요한 압박을 주므로. 데미지 기록은 사실 기록이 목적이고 책임소재는 부수적.)
- 저장(firebase.js): `fbAddExtraContainer(voyageKey,mode,cn,by,info)` — info에 size/fe/ctype/temp/seal/damage/note. completed+extras 양쪽 기록. `fbRemoveExtraContainer` 신설(삭제).
- **리스트·토탈 포함(SearchPanel allContainers)**: extras를 정식 컨으로 병합. size→iso 매핑(20→22G1, 40ST→42G1, 40HC→45G1, 45→L5G1), ctype→rf/fr/ot/tk, `_extra=true`, `_ptk=true`(실제 내려진 평택분). EDI/리스트에 이미 있으면 중복 추가 안 함.
- **색 강조만**: 리스트 행에 앰버 테두리 + "초과" 배지 + 데미지 ⚠. 집계엔 일반 컨처럼 들어감.
- **초과분 별도 집계·제출**: `extraList=allContainers.filter(_extra)`. 양하 탭에 "초과분 N건(신고 대상)" + CSV 내보내기(`초과분리스트_날짜.csv`: 번호·규격·적공·타입·온도·실번호·데미지·메모·기록자) + 행별 ✕ 삭제.

**검증**: node 시뮬 — 초과 2건이 전체/양하토탈에 합류(4대), 초과분만 2건 추출, 리퍼·데미지 보존, CSV 형식 PASS. esbuild 컴파일 무에러. 빌드 PASS(V8.04). PNG 모달·집계 UI 확인 PASS.

### V8.03-01 음성/질문 오답 6건 수정 (실사용 오답리포트 기반, 시뮬 PASS, 2026-06-16)

수석 대시보드에서 내보낸 실제 오답리포트(6건) 분석 후 nlSearch.js 수정. 전부 node 시뮬 PASS + 회귀 11건 PASS.
- **[1] "실 번호 점검"**: 띄어쓰기로 인식 실패 → 정규식에 `실\s*번호` 공백 허용.
- **[2] "리스트 점검"**: 전체 목록만 쏟아냄 → `리스트\s*점검`도 sealAuditQuery(실번호 이상 점검)로 연결.
- **[3] "당신이 하는 일이 뭐야"**: 인식 실패 → introQuery에 `당신/하는 일/할 수/무슨 기능` 추가.
- **[4] "몇 시쯤에 끝나"**: 현재 시각만 답함 → etaQuery 정규식에 "쯤에" 허용, "끝/완료" 의도면 timeQuery 끄고 종료 시각 추정(formatEta)으로. timeQuery에 `!etaQuery` 가드.
- **[5] "24번 홀드에 몇 개 남았어" → 0대 (핵심 도메인 버그)**: 짝수 베이 단독만 봐서 0. 검수사 메모대로 **"24번 홀드 = 23·24·25 트리오 전체"**, 홀수(23·25)는 개별. 짝수 베이 + 구역(홀드/데크) 명시 시 `bayTrio=[b-1,b,b+1]`로 확장, applyNLFilter가 트리오 전체 매칭. 통합 시뮬: 24번 홀드 → 23·24·25 홀드 미완료만 정확 집계 PASS.
- **[6] "특이 사항 있어"**: 인식 실패 → customsReportQuery(이상 건 정리)에 `특이\s*(사항|점|건)`, `문제\s*있/발생` 추가. 실오류·데미지 등 발생 문제를 정리.
- 회귀: 지금 몇 시·오늘 며칠·20번 베이·끝4자리 조회·리퍼·넌 뭐야 등 11건 모두 정상 유지(짝수 단일 베이는 zone 없으면 트리오 확장 안 됨).

### V8.03 오답 리포트 '저금통' 내보내기·비우기 (사용자 요청, 시뮬·PNG PASS, 2026-06-16)

**사용자 비유**: "오답을 적으면 저금통에 동전 넣듯 보관 → 저금통(파일)을 클로드에게 전달 → 빈 저금통 비우기." 음성 답변 오답을 앱이 모으긴 하는데(수석 대시보드 오답 리포트, feedback 노드) 파일로 빼서 전달할 방법이 없어 불편.

**작업 (ChiefDashboard.jsx + firebase.js)**
- 오답 리포트 섹션 헤더에 [📥 내보내기] [🧹 비우기] 버튼 추가.
- 내보내기: feedback 전체를 사람이 읽기 쉬운 .txt로 다운로드(`오답리포트_YYYY-MM-DD.txt`) — 질문Q·앱이 한 답·검수사 메모·파싱 요약·선박·검수원·버전 포함. 클로드가 바로 분석 가능한 형식.
- 비우기: **내보낸 시점의 ts 목록만 삭제**(`fbClearFeedback(tsList)`) — 안 본 오답 보호. 내보내기 전엔 비활성. firebase.js에 `fbClearFeedback(tsList=null)` 신설(목록 없으면 전체).
- 흐름: ❌오답 적힘 → 저금통 누적 → 📥 파일로 빼서 클로드 전달 → 🧹 내보낸 것만 비움 → 새 오답 재누적.

**검증**: node 시뮬(내보내기 텍스트 형식·비우기 대상 ts) PASS. 빌드 PASS(V8.03), 번들 반영 확인. PNG UI 시각 확인 PASS.

### V8.02-01 도움말 — 늘어난 기능 보강 (초보 검수사용, PNG PASS, 2026-06-16)

**사용자 지적**: V8.02에서 changelog를 통째로 빼면서, 그 안에 섞인 "신규 기능 사용법"까지 같이 사라졌다. 기능은 많이 늘었는데 도움말 내용은 옛날 그대로. 독자는 초보 검수사 — "어떤 문제도 앱이 해결해준다"는 안내가 필요.

**작업**: 버린 changelog 652항목을 재분석 → 기능 사용법 성격 222건 추출 → 중복·개발용어 제거 후 핵심만 초보 눈높이로 재작성. 도움말 137 → 177항목.
- basic: 브리핑(작업 시작 전 요약), 작업 완료 처리(컨 1개/양하·선적 분리) 추가.
- special: 리퍼·특수화물 베이별 위치 조회 추가.
- voice: 음성 오인식 자동 교정, 교정 사전 추가 안내.
- ai: 콘(고박장비) 계산 질문 추가.
- **신규 카테고리 report(보고·출력)**: 사진 보고(실오류/데미지, 2장 필수), 출력 3종(인쇄/PDF/이미지·CSV), 장비번호(1~4호기).
- **신규 카테고리 newship(신규 선박)**: EDI 자동 등록(95% 성공), 보정중 표시, 베이 그림 맞추기(칸 추가·삭제·복사, 빈 자리 점선).
- tips: "🆘 막혔을 때 — 초보 가이드"(화면 멈춤·카메라·검색·카톡 전송 대처) 추가.
- 카테고리 7 → 9개. 색 추가: report=pink, newship=teal (정적 클래스, purge 확인).

**검증**: node 시뮬 9카테고리·177항목 렌더 PASS(크래시 0). 빌드 PASS(V8.02-01), 신규 색 CSS 포함 확인. PNG 3화면 시각 확인 PASS.

### V8.02 도움말 전면 재구성 — 사용설명/용어 2갈래 + 시각 카드 (사용자 요청, PNG PASS, 2026-06-16) ★메이저

**사용자 요청**: 도움말 '팁' 탭에 개발 변경이력(changelog) 600여건이 쌓여 검수사용으로 부적합. "개발과정은 지침서에 있으니 빼고, ① 앱 사용설명·기능 ② 검수 용어·회화 두 갈래로 구분. 한눈에 눈으로만 봐도 알게."

**구조 재편 (HelpModal.jsx 1966줄 → 202줄)**
- 데이터를 `src/data/helpData.js`로 분리. changelog 612건 제거(통합지침서에 이미 보존 — 정보 손실 없음), 검수 실사용 137항목만 유지 + 검수 용어 18개 신설.
- 첫 화면: 두 갈래 큰 카드(① 사용설명·기능 ② 용어·회화).
- 사용설명: 7개 카테고리 아이콘+색상 카드 그리드(기본검색=sky, AI=violet, 음성=rose, 특수화물=amber, 항구=emerald, 트윈=cyan, 실전팁=yellow). 색만 봐도 구분.
- 용어·회화: 영어회화집(ContainerPhrasebook) 진입 + 용어 풀이 카드(POL/POD/베이/단/리퍼/DG/X-RAY 등 18개).
- 렌더는 `sec.examples || sec.items || sec.rows || []`로 방어(V8.01-01 fix 계승) — 키 혼재해도 안 터짐.

**핵심 안전장치**
- Tailwind 액센트 색은 정적 클래스(ACCENT 맵)로 작성 → purge 회피 확인(빌드 CSS에 7색 모두 포함 검증). 동적 `bg-${color}` 금지(색 누락 사고 예방).
- 미사용 import(Camera/Forklift/BarChart3/Send) 제거(0-3 orphan 정리).

**검증**
- node 시뮬: 전 7카테고리·137항목·18용어 렌더 안전 PASS(크래시 0).
- 빌드 PASS(V8.02), 번들에 신로직 반영, 액센트 7색 CSS 포함 확인.
- PNG 렌더(wkhtmltoimage) 3화면 시각 확인 PASS — 카드 색 구분·가독성 양호.
- props 호환: Header.jsx·SearchPanel.jsx 호출부(open/onClose) 무변경.

### V8.01-01 도움말 '팁' 탭 재로딩 버그 fix (사용자 보고, 시뮬 PASS, 2026-06-16)
- **증상**: 도움말(HelpModal)에서 '팁' 탭을 누르면 화면이 멈춰 재로딩해야 함.
- **원인**: 팁 탭 일부 블록(M6.91~M6.93.0, 5개)이 `examples:` 대신 `items:` 키를 쓰는데, 본문 렌더는 `sec.examples.map(...)`만 호출 → 그 블록에서 `undefined.map` 런타임 에러 → 화면 멈춤(지침서 Ⅱ-7.6 런타임 에러 부류).
- **수정(HelpModal.jsx)**: 렌더를 `const rows = sec.examples || sec.items || []`로 방어 — examples/items 둘 다 수용, 없으면 빈 배열. 향후 같은 키 실수도 무해화.
- **검증(node 시뮬)**: 전 8개 탭·163블록 렌더 안전성 PASS (크래시 0, 빈 블록 0). tips 139블록 전부 정상(items 5개 포함).
- 교훈: 같은 컬렉션을 .map 하는 렌더는 항목 스키마가 섞일 수 있으니 키를 방어적으로 받는다.

### V8.01 자동삭제 기준 = 작업활동 + PORT-MIS stale 날짜 회피 (사용자 확정, 시뮬 PASS, 2026-06-16)

**배경(사용자 의도)**: EDI를 작업 일주일 전에 미리 넣는 운영이 있다. 기존 자동삭제는 `createdAt`(항차 생성=EDI 투입 시각) 기준 7일이라, **작업 시작도 안 한 항차가 사라지는** 위험이 있었다. 또 PORT-MIS에 같은 선박의 지난 항차가 남아 있으면 EDI를 미리 넣어도 항차별 날짜가 stale(지난 날짜)로 찍혔다.

**A. 항차 자동삭제 기준 변경 (HomePage.jsx)**
- 자동삭제 기준을 `createdAt` → **마지막 작업 활동 시각**으로 변경. 공용 헬퍼 `lastWorkAt(v)`(모듈 스코프) 신설 — 양하/선적 완료 기록의 `at`, 선적 실체위치 `actual_at`, `info.lastActive` 중 최댓값.
- **안전장치 ①(핵심)**: 작업 활동 증거가 하나도 없으면(`worked===0`) 절대 삭제 안 함 → EDI만 미리 넣고 작업 안 한 항차는 영구 보호.
- **안전장치 ②**: `createdAt` 없는 옛 항차 보호(기존 유지). 백업 실패 시 삭제 보류(M7.18b 유지).
- "곧 자동삭제" 경고 표시도 같은 기준(작업활동)으로 일치 — 작업 안 한 항차엔 경고 안 띄움(불안 방지). `lastWorkAt`을 HomePage·VoyageCard 둘 다 쓰므로 **모듈 스코프 함수로 정의**(컴포넌트 내부에 두면 VoyageCard에서 미정의 런타임 에러 → 카메라 멈춤 위험. 지침서 Ⅱ-7.6).
- 검증(node 시뮬 8/8 PASS): EDI 8일전+작업안함=보호 / EDI 8일전+어제작업=보호 / 작업 8일전=삭제 / createdAt없음=보호 / 실체위치 6일전=보호·8일전=삭제 / lastActive 9일전=삭제.

**B. PORT-MIS stale 날짜 회피 (VoyagePage.jsx, 지침서 7.8)**
- 콜사인 prefix 후보가 여럿일 때(같은 선박 이번/지난 항차 공존) **최신 `updatedAt` 우선 정렬** 추가 → vesselName/berth 선택 로직이 동조건이면 항상 최신 데이터를 집어 stale 날짜가 안 찍힘.
- 검증(node 시뮬): 같은 선박 2항차(지난 8일전/이번 1일전) → 이번(최신) 선택 PASS.
- **두 번째 방향(vesselName 공유 stale 키 물리 삭제)은 미채택**: 같은 선박이라도 이번/지난·인천/평택 fallback(M5.90) 항차가 정당하게 공존할 수 있어, 물리 삭제는 정상 데이터 유실 위험(Ⅱ-7.2). A 정렬 수정으로 매칭이 최신을 집으면 stale 날짜 문제는 실질 해소되므로 무리한 삭제는 하지 않는다. → **부록 D 6번 백로그 해소.**

### V8.00 인수인계서 — 남은 작업+양하신고+특이사항 + 되묻기 (사용자 요청, 시뮬 PASS, 2026-06-16) ★메이저

**배경(사용자 의도)**: 그날 작업을 마무리 못하고 다음 검수사에게 넘길 때 "인수인계 자료 만들어줘"/"인계 해야돼"라고 하면, 남은 작업·(양하 남으면)양하신고할 것·특이사항을 한눈에 보기 좋게 정리. AI는 부탁받으면 **특이사항/더 전달할 게 있는지 되묻고**, 답을 들어 합쳐 정리.

**구현(nlSearch generateHandover + SearchPanel 2단계 흐름)**
- 파싱: handoverQuery — "인수인계", "인계 자료/서/할/해", "넘겨야", "교대", "다음 검수사", "작업 마무리 못". customs/eta보다 먼저.
- **generateHandover(allContainers, info)**: 평택분(_ptk) 양하·선적 둘 다 _mode로 집계.
  - ① 남은 작업: 양하/선적 각 남은·전체·완료 + 남은 베이 분포(어디가 남았는지).
  - ② 양하신고(양하분 있으면): formatCustomsReport와 동일 판별(누락 flag·초과 flag/_src=edi·바뀜·리씰 sl_orig≠sl·실오류 auditSeals) 요약.
  - ③ 특이사항: 미완료 중 리퍼 온도미입력(isReeferContainer & !tmp & fe≠E)·위험물(dg)·FR/OT 자동 검출.
  - ④ 검수사 메모: info.extraNote(되묻기로 받은 것) 합침.
- **2단계 되묻기(SearchPanel SingleSearch)**: handoverQuery면 localAnswer로 초안+되묻기 문구 표시 + 아래 textarea("더 전달할 내용") + [✓ 인계서 완성]/[특이사항 없음] 버튼. 버튼 누르면 handoverFinalized=true → extraNote 합친 최종본 재생성. 새 질문 시 리셋(useEffect). 음성은 conversational(부드럽게).
- state: handoverNote, handoverFinalized (SingleSearch). localAnswer 의존성에 추가. KNOWN 어휘에 인계·인수·교대·넘겨·특이사항·전달 추가.
- HelpModal 사용법 등록.

**검증(node 실함수)**: 파싱 4종("인수인계 자료/인계 해야돼/넘겨야 해/마무리 못했어") 인식. 합성(양하10[완료6+누락1+미완3]·선적8[완료3]) → 남은 양하3·선적5 + 남은베이(13,14,15/22) + 누락1 + 특이사항(리퍼 온도미입력·위험물) 정확. 메모 반영 최종본에 검수사 메모 추가 PASS.

**V8.00 의의**: V7.92~V8.00에 걸쳐 "AI 대화형 도우미" 축이 완성됨 — 자기소개·시간·날씨·입출항(7.92) → 음성 복원(7.80) → 완료 예정시각 대화체(7.99-15) → 양하신고 점검(7.99-16) → 인수인계(8.00). 모두 **로컬 계산 + 자연어 인식**(AI 답변 생성 없음, 비용 0, 환각 0) 원칙 유지. TTS 자연스러운 목소리 선택(7.99-15)도 포함.

**미연결(다음)**: 인계서 카톡/이미지 공유(kakaoShare 연동), 바뀜(swapped) 자동 기록(번호수정 흐름), 인계서 Firebase 저장해 다음 검수사 로그인 시 자동 표시.

### V7.99-16 양하신고 점검 — 그날 이상 건 정리 (사용자 요청, 시뮬 PASS, 2026-06-16)

**배경(사용자 의도)**: 양하 작업 종료 후 세관 양하신고 시, 그날 발생한 이상(실오류·리씰·컨테이너 바뀜·누락·초과)을 신고서에 적는다. "양하신고할까?" 하면 AI가 그날 이상 건을 자료 화면과 함께 정리해 준다.

**판별 기준(사용자 확정): 신고 리스트(세관 신고 목록)가 기준.**
- 누락 = 신고 리스트엔 있는데 선박에 없는 컨. **검수원이 "선박에 없음"으로 완료**(누락도 완료 상태의 하나).
- 초과 = 신고 리스트에 없는데 실제 내려진 컨. **기록할 장소가 없어 신설.**
- 둘 다 사용자 정의: "누락은 선박에 없는 컨이고 그상태에서 완료, 초과는 이미 내려져 있으므로 더 내릴게 없으면 완료, 기록할 장소는 신설."

**구현(전체 — 데이터+UI+답변)**
1. **데이터(firebase.js)**: `fbCompleteContainer`에 `flag`('normal'|'missing'|'extra'|'swapped')·`note` 인자 추가(기본값 normal → 기존 8개 호출부 회귀 0). 완료 기록이 `{by,at,flag,note}`. 초과 전용 `fbAddExtraContainer` 신설 — completed/{cn}에 flag:'extra' + extras/{cn} 별도 노드.
2. **초과 컨 합치기(VoyagePage)**: 양하 병합부에서 `sec.extras`를 컨 목록에 `_src:'extra'`로 추가(없으면 집계에서 빠짐). 의존성 배열에 sec.extras.
3. **UI**: BigResultCard 양하 미완료에 [🚫 선박에 없음(누락 처리)] 버튼(handleMissing → flag:'missing'). SearchPanel 양하 모드에 [➕ 초과 컨 추가] 버튼(prompt 번호입력 → fbAddExtraContainer). 바뀜은 기존 번호수정 흐름 + flag:'swapped'(향후 연결).
4. **답변(nlSearch formatCustomsReport)**: "양하신고할까?"/"신고할까"/"세관 신고"/"이상 건" 인식(customsReportQuery, 최우선 라우팅). 5종 분류 —
   - 누락 = flag 'missing' (+보조: 리스트에 있는데 미완료 = 작업종료 시 누락 의심 안내)
   - 초과 = flag 'extra' (+보조: _src==='edi'인데 완료)
   - 바뀜 = flag 'swapped'
   - 리씰 = sl_orig ≠ 현재 sl (정규화 비교)
   - 실오류 = auditSeals(중복·혼입·자리수) 재사용
   첫 줄 = 음성 요약("이상 5건 — 누락 1, 초과 1, 바뀜 1, 리씰 1, 실오류 1"), 이후 = 종류별 컨번호·위치·내용 목록. 이상 0건이면 "그대로 신고하세요" + 미완료 컨 있으면 누락 확인 안내.

**데이터 흐름(확인)**: compMap = sec.completed 통째 → `_comp = {by,at,flag,note}`로 주입(SearchPanel 63행). _src: 'edi'|'list'|'both'(VoyagePage 병합) + 'extra'(신설). formatCustomsReport는 평택분(_ptk) 컨에서 flag·_src·sl_orig로 판별.

**검증(node 실함수)**: 합성 양하(정상3+누락1+초과1+바뀜1+리씰1+실오류1) → 5종 정확 분류, 음성 요약 정확. 이상 없음 케이스 PASS. 파싱 회귀 8/8(신고 질문만 customsReport, 베이조회·eta·progress·컨번호 안 뺏김). KNOWN 어휘에 신고·세관·누락·초과·바뀜·리씰·이상·건 추가.

**미연결(다음)**: 바뀜(swapped)을 BigResultCard 번호수정 흐름에 flag로 자동 기록(현재는 함수만 준비). 신고 점검 화면에서 각 건 클릭→해당 컨 카드 이동. 작업 "종료" 신호와 연동해 누락 보조판별 자동 트리거.

### V7.99-15 완료 예정 시각(대화체) + 자연스러운 TTS 목소리 (사용자 요청, 시뮬 PASS, 2026-06-16)

**배경(사용자 의도)**: 검수사는 종일 숫자만 보는 단조롭고 고된 일이라, 앱이 말을 걸어주면 덜 지친다. 정밀 계산보다 "대화감"이 핵심. AI 도입 비용을 길게 논의했으나, 이 기능은 데이터에 이미 완료 타임스탬프가 있어 **AI 없이 로컬 계산으로 충분**(비용 0)하다고 결론.

**A. "몇 시에 끝나?" 완료 예정 시각 (nlSearch.js formatEta)**
- 데이터 출처: 완료 시각은 firebase.js 205행 `set(r,{at:Date.now()})`로 컨마다 기록됨. nlSearch에는 `c._comp = compMap[cn]`(= {at} 객체)로 들어옴. 즉 `c._comp.at`이 완료 시각. **사용자가 속도를 말할 필요 없이 데이터에서 실제 페이스 직접 계산.**
- 파싱: etaQuery — "몇 시에 끝", "언제 끝", "이 속도/페이스면", "얼마나 걸려", "퇴근 몇시", "점심 전에" 등. **progressQuery보다 먼저 평가**(안 그러면 "얼마나 남았고 몇시에 끝나"가 progress로 샘). 단순 "몇 개 남았어"는 그대로 progress='pending'.
- 계산: 완료된 컨의 at을 시간순 정렬 → 최근 20개(없으면 전체)의 (개수-1)÷시간차 = 시간당 페이스(초반 변동 있어 최근 페이스가 체감에 맞음). 남은 = 평택분 전체−완료. 남은시간 = 남은÷페이스. 완료시각 = 현재(기기 시계)+남은시간.
- 답변: **문자열 반환**(다른 답변과 일관 — 객체 반환은 SearchPanel localAnswer.split('\n')에서 깨짐). 첫 줄 = 음성용 대화 한 문장("40대 남았어요. 지금 페이스면 약 1시간 36분, 3시 57분쯤 끝나겠네요. 막바지네요"), 이후 = 화면 상세(예상완료/남은/페이스/남은시간). 남은 양에 따라 응원 한마디(≤10 "거의 다 왔어요" / ≤25% "막바지" / ≥75% "차근차근").
- 경계 방어: 완료0 "아직 시작 전" / 표본<2 "조금 더 진행되면" / 전부완료 "다 끝났어요 수고하셨습니다" / 페이스 0 안내. 크래시 없음.
- 검증(node 실함수): 합성 100대(완료60, 최근20대=시간당25대) → "시간당 약 25대 · 남은 40 · 1시간 36분" 정확. 5개 표현 다 인식, 회귀("몇 개 남았어"·"완료 몇 대"는 progress 유지) PASS. 경계 4종 PASS.

**B. 자연스러운 한국어 TTS 목소리 (voice.js)**
- 문제(사용자 제보 "딱딱하다"): 기존엔 `u.lang='ko-KR'`만 줘서 브라우저가 첫 번째(가장 기계적인) 목소리를 씀. Gemini 탓이 아니라 **폰 내장 TTS 목소리 미선택**이 원인.
- 수정: `getVoices()`에서 향상/네트워크 음성 우선 선택(enhanced/premium/natural/neural/네트워크 → Google → iOS 음성명 → localService=false 가점). 비동기 로드 대응(onvoiceschanged). speak()·speakContainer()에 `u.voice` 적용(없으면 기본 — 회귀 없음).
- 대화 모드: speak에 `opts.conversational` — 검수 호출은 빠르게(rate 1.3 유지), 말 거는 답변(eta)은 느긋·부드럽게(rate 1.0, pitch 1.08). SearchPanel 음성 출력에서 `parsed.etaQuery`면 conversational 적용.
- KNOWN 어휘 동기화: SearchPanel KNOWN 정규식에 끝·끝나·페이스·속도·퇴근·점심·걸려·쯤·예상·마치·종료 추가(파서 어휘 늘리면 KNOWN도 늘려 불필요한 AI 복원 방지 — 영구 원칙).
- HelpModal에 "몇 시에 끝나?" 사용법 등록.
- 비용: 0원(로컬 계산 + 내장 TTS). 향후 더 자연스러운 음성 원하면 클라우드 TTS(유료) 검토 가능하나 현 단계 불필요.

### V7.99-14 베이플랜 기본 줌 22% + 저배율 컨번호 끝4자리 표시 (사용자 제보, PNG PASS, 2026-06-16)

**A. 기본 줌 25% → 22%**
- 사용자 요청: 큰 베이를 한 화면에 보려면 22%가 적절. (V7.99-12에서 25%로 했던 것을 22%로 하향)
- 수정: BayPlan.jsx 46행 기본값·586행 리셋 모두 0.22. 하한 0.15·0.01 단위 조절은 유지.

**B. 저배율에서 컨번호 끝 4자리 표시 (컴팩트 모드)**
- 증상: 22%(셀 24×19px)에서 셀이 너무 작아 컨번호·POL/POD 등 5줄 정보가 다 뭉개져 확인 불가.
- 원인: fontSize가 `max(8, 10*zoom)`이라 저배율에서 8px로 클램프되는데 셀 폭은 줌 따라 작아져 글자가 넘침/안 보임. 게다가 셀 안에 5줄(POL/POD·컨번호·선사/중량·특수·위치)을 다 그림.
- 수정: 셀 폭(cellW)이 42px 미만이면 컴팩트 모드 — 컨번호 **끝 4자리만** 셀 중앙에 꽉 차게(`(c.cn).slice(-4)`). 폰트는 셀 폭에서 역산(`floor((cellW-4)/2.6)`, 6~13px 클램프). 나머지 줄·우상단 타입심볼·시프트·스택 배지·좌측 타입바는 컴팩트에선 숨겨 4자리에 공간 몰아줌.
- XRAY/선사 구분 유지: 컴팩트에서도 XRAY는 셀 배경(getCellBg), 선사는 글자색(getOpColor)으로 그대로 표시 — 배경색 규칙 위반 아님(XRAY 전용 배경은 허용).
- 임계 기준은 줌 값이 아니라 실제 cellW — baseW(폰 110/데스크 140)와 무관하게 일관 동작. 40%↑에선 컴팩트 해제, 기존 5줄 전체 표시.
- 검증(PNG PASS): puppeteer 렌더로 22/25/30/40% 확인. 22%(24px)에서 3147·2640·0028 등 끝4자리 또렷, XRAY 보라배경·선사 글자색 유지, 0028처럼 앞자리 0도 정확. 40%는 전체 컨번호 복귀 확인.

### V7.99-13 자연어 파서 인식 개선 + PORT-MIS 오매칭/삭제 위험 수정 (사용자 제보, 시뮬 PASS, 2026-06-16)

**A. 자연어(nlSearch) 인식 — 현장에서 "할 수 있는 질문도 못 알아듣는다" 제보**
- 증상1: "20번에 몇 개" → "20번"을 베이가 아니라 컨번호 끝4자리(digits)로 오인. "베이" 단어가 없으면 베이 인식 안 됨.
- 증상2: "18번 데크 리퍼 온도" → bay=18을 못 잡고 개수만 답함(온도 위치 미표시).
- 원인: 베이 정규식이 "N번 베이"/"베이 N" 형태만 인식(nlSearch.js 117행). "N번"만으로는 베이로 안 봄.
- 수정: "N번"(1~2자리+'번', '호기' 제외)을 베이로 인식하는 패턴 추가. hasBayCtx에도 "N번" 포함해 digits 오인 차단.
- 검증: 회귀 11케이스 PASS — 끝4자리 조회(3523·SKLU…11자리)는 digits 유지, "N번"은 bay, "2호기"는 베이 아님. KSKM 실데이터 "20번 베이"=="20번에 몇 개"=17대 일치. 합성 평택 리퍼로 "18번 데크 리퍼 온도"→위치+온도([-18°C]) 표시 확인.
- 참고: 온도 표시 필드명은 tmp(temp 아님) — formatLocationList 825행 `c.rf && c.tmp` 정상.

**B. PORT-MIS 오매칭 + 삭제 위험 (V7.30 XIN TAI PING 계열, 위험 버그)**
- 증상: DXQD(name=XIN QUN DAO, 콜사인 H3OI, 19베이 EDI215대)인데 PORT-MIS가 "그린오션호(키 131062)"로 오매칭(dict-fullname). "옛 데이터 삭제"를 누르니 또 다른 틀린 선박명 로딩.
- 원인1(매칭): VoyagePage 풀네임 매칭(673행)의 둘째 조건 `pn.includes(dictNameNorm.slice(4, 4+8))`이 너무 느슨 — 부분 슬라이스가 무관 선박과 우연히 겹쳐 오매칭.
- 원인2(삭제): 삭제 버튼이 `!pm.berth`(부두없음)면 "옛 데이터"로 보고 matchedKey 삭제 → 오매칭된 멀쩡한 다른 배를 지움. 삭제 후 매칭 로직이 또 다른 후보를 잡아 다른 틀린 선박명 로딩.
- 수정1: 풀네임 매칭을 양방향 전체 포함만 인정(`dictNameNorm.includes(pn) || pn.includes(dictNameNorm)`), 양쪽 길이≥5 가드, 부분 슬라이스 제거.
- 수정2: 삭제 confirm에 선박명 명시 — "선박명: {pm.vesselName} / ⚠ 작업 중인 배({vsl})와 다르면 잘못 매칭된 것, 삭제 말고 새 엑셀 업로드".
- 검증: portmis_match.mjs 6케이스 PASS — 정상 풀네임 일치(XIN QUN DAO, DONGJIN CONFIDENT) 유지, 약자/무관 선박(DXQD↔그린오션) 차단.
- 한계(데이터로 확인): DXQD name(XIN QUN DAO)과 "그린오션호"는 영문↔한글이라 슬라이스로도 안 겹침. 즉 실제 오매칭은 콜사인 등 다른 경로일 가능성 — 추가 로그 확인 필요. 단 슬라이스 제거 + 삭제 선박명 명시로 위험은 크게 감소.

### V7.99-12 베이플랜 기본 줌 25%·하한 15%·0.01단위 (메모8 후속, 렌더 PNG PASS, 2026-06-15)
- V7.99-11(스냅 제거)에도 큰 베이(데크 많음+홀드 14칸 등, 예 BAY 11)는 기본 줌 30%에서 한 화면에 안 들어와 홀드가 잘렸다(사용자 제보). 줌 하한이 0.3으로 막혀 더 못 줄였던 것이 원인.
- 수정: 줌 하한 0.3→0.15(휠·핀치·버튼 전부), 버튼 step 0.1→0.01(미세 조정), 기본값 0.3→0.25, 맞춤(리셋) 버튼 모바일 0.3→0.25.
- 검증(폰뷰 412×915, KSKM): 25%에서 BAY (10)11 데크+홀드 전체가 한 화면에, 위 (08)09·아래 (12)13까지 보임. 글자 가독성 유지 PASS.
- 파일: BayPlan.jsx. (콘앱 베이플랜은 자체 구현이라 별개 — 필요 시 동일 조정.)

### V7.99-11 베이플랜 — 베이 선택 시 앞·자기·뒤 한 화면에 (메모8, 렌더 PNG PASS, 2026-06-15)

**왜**
- 검수사가 진행상황을 한눈에 보는 베이플랜(BayPlan)에서, 베이를 고르면 그 베이 하나가 화면을 꽉 채우고 다음 베이는 절반만 잘려 보였다. 아래엔 여백이 남았다. 사용자: "24번 클릭하면 23 (24)25가 다 보이게, 8번이면 7 8 9가 보이게. 아래 여백을 없애면 됨. 단순 작업."
- ⚠ 처음에 메모8을 "짝꿍베이 트리오 19(20)21 미표시"로 오해해 카고플랜 evenBay/oddBay 매핑·자리이동 위험을 길게 진단했으나, 실제 화면은 BayPlan이고 짝꿍은 이미 "(24)25"로 정상 표시됨. 사용자 스크린샷으로 바로잡음 — 매핑·트리오와 무관. (교훈: 화면 증상은 코드 추측 전 실제 스크린샷으로 대상부터 특정.)

**원인**
- 베이 그리드 본체(BayPlan ~822행)가 `scroll-snap-type: y mandatory` + 각 페이지 `scrollSnapAlign:'start'`로 **한 베이 = 한 화면**으로 스냅. 이 스냅이 "여러 베이 같이 보기"를 막고 베이 하나가 뷰포트를 꽉 채워 아래 여백을 만들었다.

**어떻게**
- scroll-snap 제거(snapType·snapAlign·snapStop 삭제) → 베이들이 자연스럽게 이어 그려져 한 화면에 여러 베이가 들어옴. 페이지 간격 space-y-6→space-y-3 축소.
- 베이 점프 select onChange: scrollIntoView 대상을 선택 i가 아니라 **i-1(한 페이지 앞)**으로 → 24 선택 시 그 앞(23/(22)23)이 맨 위로 와서 23·(24)25가 한 화면에. 첫 페이지면 자기 자신(max(0,i-1)).

**검증**(크롬 폰뷰 412×915, KSKM): BAY (24)25 선택 → 화면 맨 위 (22)23, 그 아래 (24)25 전체(데크+홀드), 이어서 (26)27 시작까지 한 화면 PNG PASS. 스냅으로 한 베이가 뷰포트 꽉 채우던 현상·아래 여백 해소.
- 파일: BayPlan.jsx. 시뮬: /home/claude/sim/bayplan_test.* (BayPlan 단독 번들 — 정적 의존만이라 firebase 없이 마운트, react alias dedupe, puppeteer 폰뷰 PNG).

### V7.99-10 수동 작업에도 베이/홀드·데크 선택(A안) + 단 맥락 질문 2종 (메모6 수동, KSKM 실데이터 PASS, 2026-06-15)

**왜**
- 메모6의 가이드(자동)는 V7.99-8에서 베이→홀드/데크 선택을 넣었으나, 수동(SearchPanel)에는 단 지정이 없었다. 수동 작업 중에는 수석이 작업 위치를 알 수 없고, 자유 조회라 선박 전체가 대상이라 끝4자리 중복 오양하 위험이 남았다.
- 사용자 확정(A안): 수동도 가이드와 동일하게 **베이→홀드/데크 반드시 선택**. 그래야 수석에게 전달되고 조회가 그 단으로 좁혀진다. 단, 질문(자연어·날씨·입출항·리퍼·XRAY)은 베이 지정과 별개로 언제든 가능.
- 핵심 질문 의미 구분(사용자): "홀드 **몇 개** 남았어"(에 없음) = 작업 남은 **홀드가 몇 곳**인지(베이 수). "홀드**에** 몇 개 남았어"(에 있음) = 현재 작업 중인 홀드 **안 컨테이너 수**. 앞 답은 되묻지 않게 베이+곳수 한 번에("4, 12, 20 3곳입니다").

**어떻게**
1. SearchPanel(수동) 베이→단 게이트(V7.99-8 가이드 UI 재사용): manualBay·manualTier state, manualGroups 집계(데크/홀드×20/40), 단 선택 후 "📍 N번 홀드 작업 중 [단 변경][베이 변경]" 칩. workFilter 변경 시 리셋.
2. 수석 전달: 수동 선택 시 fbSetInspectorActivity(auto=false)로 호기(수동은 빈값)·베이·단·잔여 기록 — 가이드와 같은 inspectors 노드. ChiefBayEdit 배지(V7.99-8)가 그대로 표시.
3. 오양하 방지: SingleSearch results 정렬에 inManualTier 최우선(rank=-1) — 끝4자리 조회 시 현재 단 컨을 맨 위로. 완전 숨김은 안 함(통과화물·단 밖도 찾아줘야 함 — V7.53 회귀 방지), 우선 노출로 오선택만 방지.
4. 단 맥락 질문 2종(nlSearch): parseNaturalQuery에 tierPlaceCountQuery('홀드 몇 개'='hold'/'deck')·tierInContextQuery('홀드에 몇 개'). "에" 유무 + (개|군데|곳) + 남았 패턴, 숫자 베이 명시("20번 홀드") 제외. generateLocalAnswer가 4번째 인자 ctx(mode·bayPairs·selectedGroup·selectedTier) 받아 formatTierPlaceCount(작업 남은 단 center 모아 "베이 N곳")·formatTierInContext(선택 단 컨 수) 처리. SingleSearch가 manualCtx 전달.

**검증**(KSKM 2611N 양하): "지금 홀드 몇 개 남았어"→"4, 12, 20 3곳입니다" / "지금 홀드에 몇 개 남았어"(ctx 그룹20)→"19·20·21번 홀드에 14개 남았습니다" / 데크 버전 정확 / 맥락 없이 "홀드에"→베이 선택 안내. "에" 유무 구분 PASS.
- 파일: SearchPanel.jsx(게이트·manualCtx·활동기록·조회정렬), nlSearch.js(질문2종 파싱·답변·hasAnyCondition). 시뮬: /home/claude/sim/tierq_test.mjs.

### V7.99-9 해치 프롬프트 자동/수동 전환 시 재등장 버그 (메모10, 시뮬 PASS, 2026-06-15)

**왜 (현장 증상)**
- 해치커버를 열거나 닫은 뒤, 자동 모드 → 수동 모드 → 다시 자동 모드로 가면, 이미 처리한 해치커버를 **또 열거나 닫아야만** 다음 작업이 진행됐다.

**원인**
- SearchPanel의 `guideMode` 토글은 GuidedWorkPanel을 조건부 렌더(삼항)로 켜고 끈다 → 수동 전환 시 GuidedWorkPanel이 **언마운트**, 자동 복귀 시 **재마운트**. 이때 해치 처리 플래그 `hatchOpenDone`/`hatchCloseDone`이 `useState(false)` 초기값으로 돌아감.
- 결과: 이미 해치를 연 베이인데도 재마운트 후 플래그가 false → 데크완료·홀드잔여 조건(deckDonePromptD)이 다시 충족되어 오픈 프롬프트가 재등장. 그걸 눌러야 다음 카드로 넘어갔다.

**어떻게 고쳤나** (GuidedWorkPanel.jsx)
- 해치 처리 여부를 컴포넌트 로컬 state가 아니라 **voyage.info에 영속**(`hatchDone["{mode}_{center}"]='open'|'close'`). voyage prop은 재마운트돼도 유지되므로 프롬프트가 다시 안 뜬다.
- 헬퍼: `isHatchDoneSaved(center, action)`(읽기), `markHatchDone(center, action)`(fbUpdateVoyageInfo로 쓰기). 베이 그룹(center)·모드별로 기록 → 다른 베이는 정상적으로 다시 열어야 함.
- 판정: deckDonePromptD에 `|| isHatchDoneSaved(selectedGroup,'open')`, 양하 클로즈 조건에 `&& !isHatchDoneSaved(selectedGroup,'close')`. 해치 처리 3버튼(오픈·선적클로즈·양하클로즈)이 markHatchDone 호출.

**검증**(node 로직 시뮬): ① 데크완료 첫 도달 프롬프트 뜸 ② 처리 직후 안 뜸 ③ **수동→자동 재복귀(로컬 false 초기화)에도 영속값이 막아 안 뜸 = 메모10 수정 PASS** ④ 다른 베이(영속 없음)는 정상적으로 뜸. 4/4 정확.
- 파일: GuidedWorkPanel.jsx. 시뮬: /home/claude/sim/hatch_persist.mjs.

### V7.99-8 가이드 작업 단(홀드/데크) 선택 + 수석 작업 위치 추적 (메모6, KSKM 실데이터·렌더 PNG PASS, 2026-06-15)

**왜 만들었나 (현장 이유)**
- 수석검수사는 어느 호기가 어느 베이의 홀드/데크를 작업 중인지 알 길이 없었다. 검수사는 화면을 보며 작업하지만, 수석은 일일이 컨번호를 확인해야 작업 위치를 짐작할 수 있었다.
- 동시에 오양하·오선적 위험이 있었다. 자동 가이드 작업 중 예측과 다른 컨이 나와 끝4자리를 칠 때, 후보가 **선박 전체(평택분)**라 같은 끝4자리 중복이 잡혀 엉뚱한 컨을 양하/선적할 수 있었다. (메모4 숫자패드·메모7 선적구분과 같은 오작업 방지 계열.)
- 사용자 요구(원문 정리): 검수사가 베이 선택 후 **반드시 홀드/데크를 눌러야** 한다(자동 판단 금지 — 그래야 수석에게 작업 의도가 전달됨). 그 단의 남은 컨번호 리스트를 보여, 4자리를 치거나 리스트에서 선택해 오작업을 막는다.

**어떻게 고쳤나 (4개 파일)**
1. **단 선택 단계 신설** (GuidedWorkPanel.jsx): 기존 `장비→접안→베이 그룹→예측 카드` 흐름에 **3.5단계 단 선택**을 끼움 → `…→베이 그룹→[🔵데크/🟠홀드 선택]→예측 카드`. `selectedTier` state('hold'|'deck') 추가. 베이 그룹 변경 시 selectedTier 리셋. 단 버튼에 규격별 내역 표시 — `🔵 데크 32개 20FT:24/40FT:8` / `🟠 홀드 36개 20FT:22/40FT:14` (groups useMemo에 deck20/deck40/hold20/hold40 집계 추가).
2. **큐를 선택 단만**: queue useMemo에서 selectedTier='deck'면 t>=80만, 'hold'면 t<80만 필터. 홀드 선택 시 데크 컨은 큐에 안 나옴(검증: 첫 카드부터 끝까지 t<80).
3. **오양하 방지 — 후보 좁히기** (matchFor): "다른 컨이 나옴" 수정 시 후보를 `groupCenterOf===selectedGroup && 선택 단`으로 좁힘. 끝4자리 중복이 다른 베이·다른 단에서 잡히던 것을 차단(합성 검증: 끝4 "1234" 3곳 → 그룹20 홀드 작업 시 1건만). 추가로 q 비었을 때 **그 단 남은 컨 리스트**(tierRemainConts)를 버튼으로 노출 → 4자리 안 쳐도 리스트에서 선택.
4. **수석 작업 위치 전달** (firebase.js·GuidedWorkPanel·ChiefDashboard·ChiefBayEdit):
   - `fbSetInspectorActivity(name, voyageKey, mode, detail)` 확장 — detail={equip, bayLabel, tier, remain, auto}를 `inspectors/{name}`에 기록(workEquip·workBay·workTier·workRemain·workAt). 베이/단 미선택이면 클리어.
   - GuidedWorkPanel: selectedGroup·selectedTier 바뀔 때 활동 기록(검수사가 누른 단 기준 — 자동판단 아님). bayLabel은 그룹 베이 범위(예 19-21).
   - ChiefDashboard activeByVoyage에 위치 필드 추가 → ChiefBayEdit에 activeWorkers prop 전달.
   - ChiefBayEdit 헤더 아래 배지: 현재 모드 작업자 `홍길동 · 2호기 · 01번 홀드 · 남은 5개`(홀드=주황·데크=청색, 글자색 구분).

**검증**
- KSKM 2611N 실데이터: 그룹12(베이11·12·13) 단 선택 화면 = 데크 32(20:24/40:8)·홀드 36(20:22/40:14) 실집계 일치(PNG). 홀드 선택 후 큐 첫 카드~끝 전부 홀드 티어(t<80), 데크 미혼입(PNG). 수정창 남은 리스트 40건 전부 홀드 티어 PASS.
- 합성 중복: 끝4 "1234" 3곳(그룹20홀드·그룹20데크·그룹4홀드) → 단 미적용 3건 → 그룹20 홀드 작업 시 1건만(오선택 후보 제거) PASS.
- ChiefBayEdit 렌더 PNG: 헤더에 "홍길동·2호기·01번 홀드·남은 5개" 배지 표시 PASS.

**미처리(다음)**: 수동(SearchPanel) 작업도 베이·단 선택해 같은 활동 기록 — SearchPanel은 자연어 조회 중심이라 베이/단 선택 UI 신설이 필요(별도 작업). 사용자: "자동이 계획대로 되면 수동을 안 한다"라 가이드 우선.
- 파일: GuidedWorkPanel.jsx, firebase.js(fbSetInspectorActivity), pages/ChiefDashboard.jsx, components/ChiefBayEdit.jsx. 시뮬: /home/claude/sim/gwp_*.* (GuidedWorkPanel 단독 번들 — firebase/voice 임시 stub 교체 후 번들·복원, react alias dedupe, puppeteer PNG), cbe_*.* (ChiefBayEdit).

### V7.99-7 수석 베이상세 선적 전/완료 시각 구분 (메모7, 렌더 PNG PASS, 2026-06-15)
- 증상(PCSZ 실화면): 수석 대시보드 베이상세 편집(ChiefBayEdit)의 **선적 모드**에서 홀드에 컨이 차 있는데, 이건 EDI 계획일 뿐 실제 선적된 게 아님. 선적했는지 안 했는지 구분 불가(지침 Ⅵ-1: 선적 EDI=계획, 선적확인해야 실체).
- 원인: ChiefBayEdit가 baseBay/Row/Tier를 actual 있으면 actual·없으면 EDI로 잡아 셀을 그리는데, **완료(_placed) 구분 없이 전부 filled**로 렌더.
- 수정: ① containers에 `_placed`(선적 실체화 여부 = bay_actual 설정 ∨ completed 등록, 양하는 항상 true) ② cellExtra에 미선적 시 `cbe-unplaced` 클래스 ③ CSS: 미선적 = 점선 테두리 + 흐린 회색 글자(배경색 미사용 — Ⅱ-4.2) ④ renderCellContent 선적완료에 ✓ 접두 ⑤ 헤더에 선적 진행 배지(평택분 `선적 N/M · 점선=선적 전(계획)`).
- 검증(크롬 PNG): PCSZ BAY01 합성 — 선적완료 3컨 진한 실선, 선적 전 5컨 흐린 점선, 상단 "선적 3/8" 배지 정상 표시 PASS.
- 파일: ChiefBayEdit.jsx. 시뮬: /home/claude/sim/cbe_test.* (ChiefBayEdit 단독 번들 마운트 — firebase 정적이라 마운트 가능, puppeteer PNG).

### V7.99-6 가이드 작업 메모 6건 수정 (KSKM 2611N/2612S 실데이터 PASS, 2026-06-15)
퇴근 전 누적 메모 처리. KSKM(3E89, SUNNY KALMIA) 양하 ASC 418컨(평택122)·선적 EDI 406컨(평택110) 실데이터로 재현·검증. 6건 PASS, 4건은 구조변경/렌더검증 필요로 다음 세션 이월.
- **메모1 해치 장수 합산 버그**: `hatchPanelsOf`(GuidedWorkPanel)가 같은 해치 묶음(트리오 11·12·13) 베이의 hatchCount를 단순 합산 → 2+2+2=6장. 한 해치 패널이 여러 베이를 덮으므로 중복. 수정: 같은 groupCenter는 대표값(Math.max), 서로 다른 그룹만 합산. KSKM 트리오 6→2 PASS. ⚠ KSKM 사전엔 hatchCount 미입력 — 빌더에서 베이별 해치 장수 지정해야 정확(합성 hatchCount=2로 재현 검증).
- **메모2 자동 양하 40ft 우선 미적용**: `guidedQueue` cmp에 "같은 티어 40ft 먼저" tiebreak 부재. 짝수 베이 40ft가 양옆 홀수 20ft 트윈 위에 같은 높이로 걸치는 구조(KSKM 베이12 사이 11·13)에서, 같은 티어 안 40ft·20ft가 row 순으로 교대로 나옴. 수정: `is40ft` 헬퍼 + cmp에 **양하 한정** 같은 티어 40ft 우선. t84·t82에서 40ft 전부 선행 PASS. 선적·40단독 베이 회귀 PASS(선적은 holdTwins가 따로 처리하므로 양하에만 적용).
- **메모3 트윈 55톤 경고 미표시**: 55톤 점검 로직(TWIN_MAX_TOTAL_KG·twinDiffLimit)은 nlSearch에 있으나 **질문 답변 전용** — 가이드 트윈 카드엔 자동 경고 없음. 수정: GuidedWorkPanel에 `twinWtWarn` useMemo(합계>55톤=over 불가 / 무게차>부두한계=imbal 수평주의) + 카드에 경고 배지(over=적색, imbal=황색). 부두는 voyage.info.pier(미상 14톤 보수적). KSKM 트윈 33쌍 중 16쌍 55톤초과(최대58.3t)·1쌍 무게차초과(17.5t) 정확 검출 PASS.
- **메모4 수동 컨번호 숫자패드**: 단독 수정칸(587)은 inputMode=numeric인데 트윈 동시 수정칸(621)만 누락된 비대칭. 추가.
- **메모5 해치 포트 무시 전부 오픈**: `groupBaysOf`가 데크만 평택분인 베이까지 해치 대상에 포함. 해치는 홀드 접근 물리작업이므로 홀드 평택분 있는 베이만 열어야 함. 수정: `groupBaysOf(center, holdOnly)` 옵션 + sendHatchReport가 holdOnly=true(홀드 평택 0이면 보고 안 함). KSKM 통과화물 홀드 베이(7·9·15·17·23·25)가 대상에서 제외 PASS.
- **메모9 선적 바닥부터 한 로우 먼저**: 선적 트윈 `holdTwins.sort`가 POD를 안 봐서(cmp엔 podRank 있으나 holdTwins 자체 정렬엔 누락) 포트가 row 순으로 교대로 나옴. 수정: holdTwins를 **POD 우선(그룹 전체 등장순) → row → tier 오름차순(바닥부터)**. KSKM 그룹4·12·20에서 POD 연속블록 + 같은 row 티어 바닥부터 PASS.
- **이월(다음 세션)**: 메모8(베이플랜 "19(20)21" 트리오 3칸 — 렌더 PNG 검증 필요, REF Ⅴ-B 비대칭) · 메모10(수동/자동 전환 시 해치 버그 1409.png — 증상 구체화 필요). (메모6=V7.99-8, 메모7=V7.99-7에서 완료.)
- 파일: guidedQueue.js(is40ft·cmp·twinPodOrder), GuidedWorkPanel.jsx(hatchPanelsOf·groupBaysOf·twinWtWarn·621칸). 시뮬: /home/claude/sim/*.mjs (실파서 parseAscFile·parseBAPLIE + 실 guidedQueue·twin).

### V7.98 통합 (3D 뷰 + 수석 베이상세 편집)
- 깃허브 V7.97(3D뷰·BayDetailEdit·위젯픽스)과 세션 V7.97(ChiefBayEdit) 통합 → V7.98.
- 채택: BayPlan3D(베이탭 3D 토글, 기본OFF, 검증됨) · BayDictVerifyWidget 통계픽스 · ChiefBayEdit(수석 편집). 폐기: BayDetailEdit(거부된 마크 방식).
- 원칙: 편집은 수석 대시보드(ChiefBayEdit), 베이 탭 3D는 뷰. 두 기능 충돌 없음.

### V7.98-02 베이상세 빈칸 도배 픽스 (row99 OOG 오염)
- 증상: 베이상세가 전 베이를 56칸(01~99) 격자로 그려 빈칸 도배(데이터 손실은 없음).
- 원인: bay99 OOG placeholder(row99)가 PrintHubModal globalRowRange.maxRight=99로 오염 → BayDetailPage STD_ROWS가 이 전역값을 전 베이에 적용.
- 해결(방법 A, 최소수술): ① PrintableBayDetail bayList에서 bay>=99 제외(3D와 동일) ② BayDetailPage STD_ROWS를 베이별 매트릭스 row(rowMaxEvenLocal/OddLocal ∪ 이 베이 실제 컨, OOG row>=90 제외)로 ③ ChiefBayEdit도 동일 방어. 인쇄 CSS/tier라벨/해치 무변경.
- 검증(크롬 PNG): 오염된 maxRight=99를 넘겨도 BAY(06)07이 8칸 정상, bay99 페이지 없음. PASS.

### V7.98-07~11 베이상세 BayBoxV2 일원화 + 인쇄 버그 + 페어 표시 (2026-06-15)
페어 모델 상세는 Ⅴ부 REF_베이페어링 참조.
- **V7.98-07/08 일원화**: 카고플랜·편집 베이상세·인쇄 베이상세 모두 BayBoxV2 단일 컴포넌트. 그림=BayBoxV2, 셀 내용만 주입(카고=마크, 편집=컨번호, 인쇄=5줄: POL/POD·컨번호·선사 F/E 무게 타입·특수·위치). 7.98-07이 renderCellContent prop 추가.
  - 절대 주의 ①: BayBoxV2 래퍼는 `display:flex;flex-direction:column` + 높이 확보 필요(셀이 `flex:1 1 0` 높이 상속). **고정 height 금지(넘침 유발)**.
  - 절대 주의 ②: 인쇄용 전체화면 모달은 **document.body로 portal**해야 CARGO_V2_CSS 인쇄 격리를 통과한다.
- **V7.98-09**: 편집 베이상세 격자 찌부러짐 — `cbe-cargo-wrap height:72vh`.
- **V7.98-10**: 인쇄 베이상세 백지·페이지 미분리. print-to-pdf A/B로 확정. 원인 ① CARGO_V2_CSS @media print의 격리 선택자가 portal 없는 `.bd-print-modal`을 숨김 → 백지 ② bd-cargo-wrap 고정 height+page-break가 bd-page와 겹쳐 이중 페이지. 수정 — 격리 선택자에 `:not(.bd-print-modal)` + 모달 createPortal(body) + bd-cargo-wrap 고정높이 제거(flex:1 1 0). pdftoppm 5페이지 PASS.
- **V7.98-11**: 카고플랜 페어 "3 (4)5"→"3 5" 붕괴(가운데 짝수 누락, "7 (8)9"는 정상). node 실함수 시뮬 재현. 원인 — 매트릭스 빌더는 페어를 홀수 엔트리 `pairEven`(짝수번호)로 저장(짝수 별도 키 없음)인데, `autoPairBays`는 트리오를 짝수 베이 별도 존재로만 판단(pairEven 무시) + matrixBays 생성부가 pairEven 미전파. detectMissingBays는 M6.94.36에서 보정받았으나 autoPairBays만 빠진 **비대칭**. 수정 2곳 — autoPairBays에 pairEven 페어 루프(usedEvens 중복가드) + matrixBays map에 pairEven 전파. ALL PASS.
- **ATRP 베이상세가 CASPI와 다르게 큰 박스로 보임 (진단만, 미해결)**: ATRP는 매트릭스가 **cells 없는 PDF 자동본**이라 matrixRender가 hasCells=false→null → 옛 bd-grid 폴백(큰 빈 박스). cells 있는 선박은 BayBoxV2(컴팩트) 정상. 해결 방향 — cells 없을 때 EDI 실데이터로 단면 골격 생성(BayPlan 패턴)해 BayBoxV2 렌더 → 전 선박 CASPI형 통일. [다음 세션]
- **교훈(절대)**: ① ZIP은 Ⅱ-2.5대로 dist 포함, HANDOFF_* 제외(내용은 지침서 흡수, 정보 손실 금지). ② 페어 함수는 여러 개 — 한 곳만 고치면 비대칭 재발. 페어 수정 시 REF_베이페어링 §2 표 전부 점검.

### V7.94-24 자리 교환(swap) + 위치수정창 작업 단 필터 (2026-06-13)
- **현장 규칙**: 위치 수정창은 작업 중인 베이의, 작업 중인 단(홀드/데크)의 빈자리만 순서대로 표시. 자리 교환: A가 B 자리로 선적되면 B는 A의 원래 자리로 이동해 대기(미배정 떠돌이 X). 같은 포트 엠티끼리 베이 교환 가능.
- 수정: ① fbReassignContainerPosition swap화(A 원위치 캡처 → displaced B를 A 원자리로, 반환에 swappedTo) ② PositionEditModal workTier 필터('hold' tier<80 / 'deck' tier>=80). 검증: swap·workTier 10항목 + BAY38 선적 큐 순서 대조 + 회귀 127 PASS.
- 미처리: 연쇄 자리 교환(다단계), 조회 시 예측 위치 자동 제안 강화, 수동 선적 베이→홀드/데크 게이트(SearchPanel).

### V7.94-23 가이드 베이 홀드/데크 구분 + 장비선택 1회화 + 선적 POD별 묶기 (2026-06-13)
- 현장 지적 4건(SWRG 2605N): ① 베이 선택 화면에 홀드/데크 구분 추가(데크=하늘·홀드=주황 잔여) ② 장비 선택 매번 나옴 → equipStep 초기값 `!getEquipNumber()` ③ 자동 선적이 단일 포트처럼 → buildGuidedQueue 선적 시 같은 베이 안 POD별 묶기(podRank) ④ 수동 선적 베이→홀드/데크 선택 후 조회 = **미착수(다음 작업)**.
- 검증: 10항목(SWRG BAY2·10·14) + 회귀 117 PASS.

### V7.94-22 해치커버 보고 "장" 수 = 매트릭스 hatchCount 합 (2026-06-13)
- 버그: 베이 그룹인데 장수가 베이 개수로 나옴(매트릭스는 2장 지정). 원인: buildHatchMessage가 `총 {bays.length}장` — 매트릭스 hatchCount 무시.
- 수정: buildHatchMessage에 panelCount 파라미터(없으면 베이 개수 폴백) + hatchPanelsOf(bays)로 사전 hatchCount 합산(페어 흡수 짝수는 대표 홀수에 포함) + fbAddWorkReport에 panelCount 기록. 검증 10항목 + 회귀 107 PASS.
- 주의: 사전에 hatchCount 지정돼야 정확. 신규 선박은 빌더에서 베이별 해치 장수 지정 권장.

### V7.94-21 컨번호 조회 숫자 키패드 (2026-06-13, 메모 처리)
- 끝4자리 조회 입력칸 3곳에 `inputMode="numeric"`(SearchPanel·GuidedWorkPanel). 자유질문칸은 음성·한글 받으므로 text 유지. 검증: 빌드 JS 6곳 + 회귀 97 PASS.
- 미처리 메모: 로우 목록 화면 스크린샷(타 검수앱) — 신설인지 단순 참고인지 **사용자 확인 필요**.

### V7.94-20 끝4자리 중복 시 수동작업 오선택 + 위치창 베이 자동선택 (2026-06-13)
- 현장 발견(SWRG 2606S BAY38): 끝4자리 3523 2대. 선적 평택 전체 끝4자리 중복 8종.
- 문제1: matchFor가 끝4자리로 후보 찾는데 같은 베이 중복 시 엉뚱한 컨 선택 → 원래 주인 displaced 잔존. 수정: 현재 카드 위치(card.pos) 같은 컨 우선 정렬 + 2개↑면 ⚠ 중복 경고 배지.
- 문제2: 미배정 재배정 시 작업 베이 또 골라야 함. 수정: PositionEditModal workBay prop(작업 베이 자동 선택). 검증 8항목 + 회귀 97 PASS.

### V7.94-19 베이매트릭스 페어(짝수) 인라인 변경 (2026-06-13)
- 짝수(페어) 지정을 베이 목록에서 바로 변경 가능하게. updatePairEven(빈값=단독, 짝수=짝꿍, 홀수·범위밖 경고) + 인라인 number 입력(placeholder "단독", step 2). 검증 9항목 + 회귀 89 PASS.

### V7.94-18 수석검수 완료 권한 + 비수석 경고 (2026-06-13)
- 버그: 비수석도 완료 저장 가능(권한 체크 없음). 수정: isChief 확장(`/수석검수|부수석/`), ChiefDashboard에 inspector 전달, doComplete 권한 차단 + 경고, 버튼 권한별 분기([✓ 수석 완료 저장] / [🔒 수석 전용]). 검증 14항목 + 회귀 80 PASS.

### V7.94-17 PWA 홈화면 설치 404 수정 + 항만 아이콘 (2026-06-13)
- 증상: 홈화면 설치 후 아이콘으로 열면 404(QR 첫 접속은 정상). 원인: manifest `start_url:"./"` + vite `base:'./'` 상대경로 → GitHub Pages `/greenmarinetally/` 하위라 루트로 잘못 풀림.
- 수정: manifest start_url·scope = `/greenmarinetally/`(절대경로). 아이콘을 항만 크레인+컨테이너 PNG(base64 인라인). iOS는 `apple-touch-icon`(PNG data URI)을 `_index.entry.html`에 추가(build.sh가 루트 index를 dist로 덮으므로 진입 HTML에 넣어야 생존).
- 빌드 함정: 루트 index.html 직접 넣으면 빌드 시 사라짐 — 진입점은 `_index.entry.html`. 검증 7항목 + 회귀 66 PASS.
- 참고: 움직이는 아이콘은 iOS/안드로이드 모두 OS가 차단 — 정적 PNG만.

### V7.94-16 해치커버 프롬프트 가이드 통합 + 작업보고 항차 오표시 수정 (2026-06-13)
- 해치커버 보고를 가이드 작업 흐름의 베이 완료 시점 선택창으로 통합(GuidedWorkPanel): 양하 데크 완료 시 [🔓 오픈→홀드], 선적 홀드 완료 시 [🔒 클로즈→데크], 양하 그룹 완료 시 [🔒 클로즈]. 보고는 buildHatchMessage(실제 베이번호·장비·모드별 항차) + fbAddWorkReport + 카톡.
- **작업보고 항차 오표시 수정**: handleHatch/handleConBox가 `activeByEquip[equip][0].mode` 고정 → 활성 없으면 voy_d 폴백(선적인데 양하 항차). → equipModeOf: 장비 활성 모드 1개면 그것, 아니면 화면 선택 모드(selectedMode). 검증 SWRG 12항목 + 회귀 54 PASS.

### V7.94-15 SWRG(SAWASDEE RIGEL) 풀 테스트 — 실데이터 전수 검증 (2026-06-13)
- 양하 2605N(909대, 평택 402) → 선적 2606S(890대, 평택 384). IMO 9943803, SMDG22.
- 전수 교차: XRAY 10건 정확·오탐0 / 선적 선사 3종(HAS116+HSL1+SKR267=384) EDI 완전 일치 / 양하 LIST 404행 = EDI 402 + **광양분 1건 혼입(SKLU1981464, POD KRKAN)** + 중복 1건 → 현장 주의 / 가이드 양하 402·트윈 61쌍, 선적 384·트윈 12쌍.
- 수정 2건: ① guidedQueue 선적 "FR·OT 마지막"이 oog 필드 미인식 → `c.oog||twin?.oog` 추가(홀드 OT는 물리 제약 예외 유지) ② fillEmptyBaysSequential — 사전 없는 신규선박 4의배수 전무(트리오 구조)면 추정 채움 금지(SWRG 가짜 14→5). 검증 10 + 회귀 44 PASS.
- 참고: SWRG는 사전 미등록 — 작업 후 빌더 [저장]하면 v3 등록.

### V7.94-14 검수원 로그인/작업중 상태 구분 (2026-06-13)
- 증상: 로그아웃해도 "●작업중" 남음. 원인: 로그아웃이 Firebase 미기록 + 배지가 "최근 60초 활동"뿐.
- 수정: fbSetInspector에 loggedIn/loginAt, 신규 fbLogoutInspector. 공용 유틸 inspectorStatus.js(working/online/null, loggedIn 없는 과거데이터 하위호환). 표시 3곳 교체. 검증 8항목 PASS.

### V7.94-13 MCSN 95베이의 진짜 원인 — BAPLIE 위치 미정 코드 + PDF 워커 404 (2026-06-12)
- MCSN 실EDI 781대 중 1대(HASU1411466)가 `LOC+147+0999999` = **99-99-99 위치 미정 관례 코드**. 이 1대가 max를 99로 끌어올려 95베이 생성. 나머지 780대는 1~44로 .def와 일치.
- 수정 3종: ① buildMatrixFromEdi에서 bay·row·tier 모두 99인 컨 제외 ② dictBaySet 기록 → fillEmptyBaysSequential이 사전 없는 베이 추정 금지 ③ PDF 워커 404 — public/pdf.worker.min.mjs 정적 파일 + BASE_URL 참조(pdfjs 4.10.38). 최종 검증: 34베이·추정0·max45 .def 일치, 회귀 36 PASS.
- 교훈: BAPLIE에 99-99-99(위치 미정) 컨 존재 가능 — 매트릭스·통계 산정 시 항상 제외.

### V7.94-12 MCSN 베이 매트릭스 오염 사건 (2026-06-12)
- 증상: MCSN 622N 빌더가 96베이(추정 74). 화면 "사전: SPIL NIKEN".
- 진상(.def 바이너리): MCSN.def 헤더 = MCSN / SPIL NIKEN / 3E3921 / IMO 9273947 → **MCSN 실선박명이 SPIL NIKEN, 매칭 자체는 정상**. 진짜 원인 = V2 사전 MCSN entry가 PDF box-region 자동파싱(M6.71) 오염(트리오 3번째 베이 누락 + bayNo 00, 24베이). 같은 오염 8척: KKLC, LYTJ, MAMP, MCSN, NBTD, NSFR, SWSP, TNJP. .def 파서(M4.4)는 정상(34베이).
- 수정 3종: ① V2 MCSN entry를 .def 결과로 교체 ② augmentMatrixFromBayDict에 오염 패턴 자동 거부(00 존재+트리오 3번째 결손≥2) ③ 정합성 게이트(EDI 실측 max 베이 > 사전 max면 미적용). 검증 5 + 회귀 31 PASS.
- 미해결: 사용자가 본 "EDI에서 99베이"의 출처 — MCSN EDI 미수령(다음 세션).

### V7.94-11 위치 선택창 — 베이 우선 선택 + 완료 자리 비활성 (2026-06-12)
- 원칙: 전체 베이 자리를 한꺼번에 보이지 말 것(오선적 유발). 베이 먼저 선택 → 그 베이 자리만. 완료된 자리도 표시하되 선택 불가(✓회색). 검증 8 + 회귀 28 PASS.

### V7.94-10 컨테이너 번호 수정 버튼 + 다른 베이·목적지 구역 경고 (2026-06-12)
- 두 기능 병행: [위치 수정](같은 컨 자리만) / [컨테이너 번호 수정](다른 컨이 옴 — 검색·선택 후 남은 자리 창).
- 경고 2종: ⚠ 다른 베이에서 오는 컨 / 🚨 목적지 구역 이탈(슬롯 EDI 계획 컨 POD ≠ 옮기는 컨 POD). 트윈 짝꿍 자리도 검사.
- React 훅 함정 2건(렌더 검증 적발): useMemo가 아래 선언 state 참조→TDZ / 조기 return null 뒤 훅 추가→"Rendered more hooks". **훅 추가 시 조기 반환 앞·의존 선언 뒤.** 검증 8 + 회귀 28 PASS.

### V7.94-09 위치 수정 — 남은 자리 선택창 + 트윈 짝꿍 자동 (2026-06-12)
- PositionEditModal 기본 화면을 남은 자리 그리드로(같은 모드·미완료·크기 일치). 트윈 짝꿍 자동(20ft+findTwinCandidate → 앞 선택 시 뒤 자동: 21-00-04→23-00-04). 배정 후 바로 선적확인 체크. 검증 10 + 회귀 28 PASS.
- 주의: import 누락은 esbuild가 못 잡음 — 새 식별자 쓰면 import 직접 확인.

### V7.94-08 선적 가이드 — 현장 메모 5건 (2026-06-12)
선적 상황 규칙. 양하는 기존 유지(FR만 우선).
- ① 트윈 스택 연속(혼재 베이 20ft 트윈은 같은 로우 바닥부터 연속, 아래 깔린 40ft는 직전에 끌어옴). 최종: 싱글→트윈(스택)→40ft→데크→FR·OT ② 홀드 완료 프롬프트 ③ FR·OT 선적 마지막 ④ 수정 시 위치 꼬임 = fbReassignContainerPosition(예측 컨 자동 미배정+완료취소) ⑤ 트윈 동시 수정(바뀐 쪽만 선택). 검증 9 + 회귀 11+FR8 + 실렌더 7 PASS.

### V7.94-06 가이드 순서 — 40/20 혼재·FR 특수화물 규칙 (2026-06-12)
- 40/20 혼재 시 40ft 먼저 = 적재 구조에서 자연 발생(양하 위층부터라 트윈 위 40ft 먼저, 선적 아래층부터라 바닥 40ft 먼저). 바닥 20ft면 일반 순서 — 코드 무변경.
- FR(플랫랙): 우선 양하 / 마지막 선적. 물리 제약 예외(같은 줄 위 비FR 존재 / 홀드 FR인데 데크 잔여). 트윈 한쪽 FR이면 카드 전체 FR. 검증 8 + 회귀 11 PASS.

### V7.94-05 가이드 카드 정보 전면 보강 (2026-06-12)
- 카드에 규격·FULL/EMPTY·무게(formatWt)·선사·항로·OOG 배지. 실번호 줄(미입력 ⚠경고, 탭→인라인 입력 fbUpdateRecordSeal). XRAY 줄(대상만, 탭→XRAY 실번호+E-실 fbSetXraySeal). 무게는 반드시 formatWt(EDI wt는 kg — 직접 표기 시 7200t 오표기). 검증 8 PASS.

### V7.94-04 X-RAY 미매칭 잔존 키 정리 버튼 (2026-06-13)
- 잔존 출처: handleXrayUpload이 누적(merge) — 새 파일에 없는 옛 키 안 지움. 수정: [🧹 미매칭 삭제] 버튼(매칭분만 남기고 교체 저장). 누적 업로드 자체는 유지(분할 업로드). 검증 3키 중 미매칭 1키만 제거 PASS.

### V7.94-03 X-RAY 상단 0/3 vs 리스트 2 불일치 (STMJ 2627E)
- 원인: VoyageSummaryCard는 xrayList 원본 키 전부(3), ListTab은 매칭분만(2). 미매칭 1건 = EDI/리스트 어디에도 없는 컨번호(검사 누락 위험) → 숨기지 않고 드러냄. 수정: 요약 칩 매칭분 기준 `0/2` + ⚠N 미매칭, 업로드 탭에 미매칭 전체 나열. 검증 4 PASS.

### V7.94 자동 가이드 모드 — 앱이 양하/선적 순서를 예측 제시 (핵심 비전 1차 구현)
- 목적: 앱이 크레인 순서대로 "다음 컨테이너" 카드 제시 → 검수사는 [확인]/[수정]만. 몸은 검수사, 소프트웨어는 앱.
- **순서 규칙 (ATPR 2627E/2628W 실데이터 PASS)**:
  - 양하: 데크→해치→홀드, 맨 위 티어부터, 같은 티어는 육상→해상 로우.
  - 선적: 홀드→해치→데크, 맨 아래 티어부터, 같은 티어는 해상→육상 로우.
  - 육상/해상 = 접안 방향(우현 접안 = 짝수 로우가 해상쪽). 항차당 1회 입력, `voyage.info.berthSide` 저장.
  - 트윈: findTwinCandidate 그대로(홀수+홀수 한 카드). 40ft는 자기 층 차례. 싱글모드(짝 없는 20ft) = 양하 맨 마지막 / 선적 맨 처음. 쉬프팅은 감지 안 함(수동).
- UI: SearchPanel 상단 🤖자동/✋수동 토글. GuidedWorkPanel(접안→베이 그룹→예측 카드+트윈 동시+배지+음성). 수정 3연속 = 자동 수동 전환.
- 파일: guidedQueue.js(순수 정렬), GuidedWorkPanel.jsx. 검증 11 + puppeteer 3화면 PASS.
- **V7.94-02 (오선택 방지+장비 단계)**: 설정 흐름 = 선박→양하/선적→장비(호기)→좌/우현→베이. 접안 방향 confirm 재확인. 설정 칩 바([🏗 N호기][우현 접안][B20 변경]).

### V7.93 트윈 작업 무게 점검 (현장 안전 도메인)
- 도메인: ① 카고 크레인 트윈 두 컨 합계 **55톤 초과 불가** ② 무게 차이 급격하면 수평 안 맞아 불가.
- **트윈 무게차 한계(V7.94 확정)**: 부두별 — **동방아이포트(PNCT) 14톤, 평택컨테이너터미널(PCTC) 20톤.** 초과 = 불가. TWIN_DIFF_LIMITS + twinDiffLimit(pier). 부두 출처 = voyage.info.pier. 미상이면 보수적 14톤.
- 단독 질문: twinCheckQuery → 불가 쌍 위치+합계 무게, 불균형 차이값. 짝 판정은 twin.js getBayPairs 단일 규칙. 상수 TWIN_MAX_TOTAL_KG=55000. 검증 18 + 실렌더 3 PASS.

### V7.92 챗봇형 일상 질문 — 자기소개·시간·날씨·입출항
- 4종 신설: introQuery(자기소개) / schedQuery(입출항 — portMisMatch.js 신규, 최신 updatedAt 우선·V7.30 선박명 가드) / timeQuery(기기 시간) / weatherQuery(Open-Meteo 무키, 평택항 36.967/126.822 — 풍향·초속).
- ⚠ 우선순위: 입출항 > 시간("입항 시간"이 timeQuery에도 걸림). ⚠ 검증 적발 버그: 패치를 SearchPanel(바깥)만 적용 → 답변 로직은 내부 SingleSearch라 런타임 에러. **props 추가 시 선언부+모든 호출부 동시 수정**(SearchPanel 안에 SingleSearch·TwinSearch 별도 존재). 검증 27 + 마운트 6 PASS.

### V7.92-02 챗봇 집계 = 평택분 통일
- 증상: 양하 탭 개수 ≠ 자연어 질문 답. 원인: SearchPanel allContainers = EDI 전체(통과화물 포함). 수정: `_ptk` 플래그(양하 POD평택·선적 POL평택). ⚠ 예외: **컨번호 단건 조회는 전체 유지**(통과화물 스캔 시 찾아주는 게 정답). 검증 7 PASS.

### V7.91 검수앱 음성 질문 자동 복원 (콘앱 동시 — Ⅲ부 참조)
- (검수앱 측은 V7.80에서 구현, 7.91은 콘앱 이식 — Ⅲ부.)

### V7.90 완료 분리 + 조회 오염 해결 + 콘앱 자동 분리
- 완료 분리: info.inspectorDone(단일) → dischargeDone/loadingDone 모드별. VoyageCard에 [⬇ 양하 완료]/[⬆ 선적 완료]. 보유 모드 전부 완료 = 수석 대기(구 데이터 하위호환).
- 조회 오염 해결(V7.53 보완): SingleSearch 결과를 main(현재 모드·미완료)과 others(반대 모드+완료분)로 분리. main만 기본, others 접이식.
- ⚠ 검증 교훈: node_modules 없는 상태 `esbuild | grep -c "✘"`는 0 출력(가짜 통과) — **컴파일 체크 전 esbuild 실존 확인.**

### V7.90-02/-03 베이 분포 질문 + 상세 확장
- "리퍼가 몇 번 베이에 있어?" 유형 신설. bayDistQuery + posQuery 결과>5면 분포 라우팅. 첫 줄 음성용("리퍼 56대 — 베이 3, 7, 20", ≤8 나열). 베이별 줄에 갑판/홀드·규격·온도 분포·끝4 나열. 게이트 3곳에 포함 필수.

### V7.90-04 작업 브리핑
- briefingQuery("브리핑/요약"). 첫 줄 음성 한 문장 + 상세(F/E·규격·베이범위·진행률·리퍼 온도·위험물·XRAY·FR/OT/TK). generateBriefing.
- ⚠ 리퍼 판정은 isReeferContainer 단일 함수(자체 정규식 `/R[FT1-9]?$/`이 40FR 끝 R에 매칭돼 오인).

### V7.90-05 실번호 오류 사전 점검
- auditSeals: 풀씰·엠티실 전수 — 중복 / 컨번호 혼입 / 특수문자·4자 미만 / 자리수 부족(같은 접두 그룹 최빈 길이 대비, 표본≥3·최빈≥70%). 브리핑 통합 + 단독 질문. 검증 6/6 검출·오탐0.

### V7.90-06 실번호 스왑 즉시 경고
- 시나리오: 두 컨 실번호가 리스트에서 서로 바뀜 → 수정 시점이 스왑 드러나는 유일한 순간. checkSealSwap(입력값이 다른 컨 sl/eseal/reseal과 일치 시 confirm 경고). **기록은 막지 않음**(실물이 진실). 검증 PASS.

### V7.90-07 브리핑 재구성 (7.41 정정)
- 문제: 통과화물 포함 전체 집계(7.1 위반) + 일반 통계 나열. 재구성: 평택분/통과화물 분리. **⚠ 주의사항(행동 지향)** — 리퍼 온도 미입력→"조회 시 입력", 통과화물 혼재→"내리지 말 것". 일반 통계는 한 줄 압축. **브리핑 집계는 평택분, 통과화물은 숫자가 아니라 주의사항으로.**

### V7.80 음성 질문 자동 복원 — AI 질문 번역기 (검수앱)
- **원칙: AI는 답하지 않는다 — 질문만 복원한다.** 음성 오인식 교정만 AI(fixQuestionWithAI, temp0·60토큰·4초), 답은 복원 문장을 로컬 파서(nlSearch)에 재투입 — 환각 차단.
- 트리거: 음성 입력 + 미해석 단어 잔존(KNOWN 어휘 제거 후 한글 2자+). 부분 인식("20번 베이 잇퍼 몇대야")도 복원. 같은 문장 1회만. 음성 답변 핵심 한 문장.
- ⚠ 진단 교훈: 텍스트 직접 입력 16문항은 다 정답 — 병목은 엔진이 아니라 STT. 증상 재현은 반드시 입력 경로(음성)까지 포함.

### V7.61 검수앱 — (콘앱 홀드콘 ×2 버그, Ⅲ부 참조)

### V7.57 카고플랜 해치커버 등분 복구
- V7.05 원복 때 computeBayRenderData return의 hatchCount 누락 → 해치 등분 항상 1로 폴백. return에 hatchCount 복구.

### V7.58 카고플랜 해치커버 수평 정렬
- 원칙: 홀드 있는 베이들은 해치커버 선이 같은 수평선(데크는 아래 82가 해치선에 붙고 부족분 위 spacer, 홀드는 위가 붙고 부족분 아래 spacer). deck-only는 해치선 없어 제외. globalHatch 계산. 검증 KSKM PASS.

### V7.53 검색/질문(챗봇) 수리 — A방식(규칙 기반) 확정
- 사용자 확정: 챗봇은 A방식 — AI 없이 있는 데이터로 정확히. 실결함 2건: ① 탭 격리(filteredContainers→ allContainers 전체 + 현재 모드 우선 정렬) ② 45ft 오인(`/^45/`→ 45='^L5'∨라벨45, 40='^4'). nlSearch.js는 순수 모듈 — node 단위테스트.

### V7.54 질문 카탈로그 + 미응답 기록 — 파서 확장 백로그
- 매뉴얼 [AI 질문] 탭 개편(검증된 질문만 카탈로그). 미응답 [📌 기록] 버튼 → fbReportWrongAnswer. **미지원 백로그(20문항 중 X10·△2)**: 선사별 집계 / 미입력 필터(온도·씰) / 시프팅 / 통과화물 / 최대최소 정렬 / 날짜 필터 / 서수→베이 / 위험물 클래스. 오답 위험 2종(선사명·미입력) 매뉴얼 경고 표기.

### V7.52 베이 간 정렬 (검수앱 BayPlan + 콘앱 동시 — Ⅲ부도 참조)
- 증상(TMPZ): BAY01(deck-only 4칸)의 04가 BAY(02)03(6칸)의 06 위에. **홀드 없는 베이가 특히 어긋남 — 반복 실수 지점.**
- 원칙 ④ 베이 간 정렬: 기준 폭 = 전체 베이 최대 폭. 모든 베이를 그 가운데(0.5칸 margin)에. 같은 row는 어느 베이에서나 같은 세로선.
- 검수앱(BayPlan.jsx): globalGridCols 신설(dictBaysSummary 선언 뒤 — TDZ). 검증 TMPZ 렌더 PASS.
- ⚠ 본체 검증법(영구): BayPlan을 esbuild 단독 번들 + 합성 containers 마운트(bayplan_test.entry.jsx) — Firebase 의존 있어도 마운트 가능.

### V7.42~43 엠티실 보고서 정렬·서식
- 정렬: 20ft 먼저 → 40ft, 규격 안 컨번호순. 규격 경계 소계 + 맨 아래 총합(sortBySizeThenCn).
- 서식: 출력 전용 loadSheetJSStyled(xlsx-js-style@1.2.0). **기존 loadSheetJS(파서용)는 절대 교체 금지.** 공용 빌더 buildSealReportSheet.
- 버그: 선적일자 Invalid Date → fmtDateSafe.
- **V7.43 해소**: 원인 = 선사마다 규격 표기 다름(22GP/45GP) → 엠티실 칸 오염. ① 규격 컬럼 병기 `20DC(22GP)` ② 엠티실 ISO형 값 제거 ③ 입력 있으면 표시·없으면 빈칸(수기 기입 공간) ④ 선적위치(베이/열/단) 컬럼 ⑤ 시각 압축 → 8컬럼 A4 한 장.

### V7.40 수석 대시보드 전폭 점검 — 실시간 작업 보드
- **⚓ 실시간 작업 보드(신설, 최상단)**: 살아있는 항차당 카드 1장. 진행바+누락 / 지금 작업 중 검수원(lastActive 90초) / 오늘 데미지·실오류 배지 / 최근 작업 보고 / 클릭→항차.
- 버그 fix — 평택분 과대 집계(7.1 위반, 전수 5곳): 모드 인자 추가(양하=POD, 선적=POL)를 5곳 일괄. **교훈: `isPyeongtaekPort(c.pol)||isPyeongtaekPort(c.pod)` 패턴을 전수 grep.**
- 정리: "항차별 진행" 섹션 제거(보드 대체), 죽은 코드 490줄 제거. ⚠ 함수 블록 제거 시 시그니처 `{`를 본체 `{`로 오인 주의.

### V7.38 카고플랜 V2 다듬기 — override 우선순위 수술 + deck-only 규칙 재제거
- ① cargoPlanCore 우선순위 수술(5.1 정합): non-user 분기에서 자동 사전(변수명 "userBay"지만 v2/v5 포함)이 override보다 먼저 평가됨 → KSKM bay01이 4행 직사각 대신 8행 피라미드, bay27에 유령 홀드. 수정 4건(rowMax·hasZero·tiers·cells override 우선, 빈 holdTiers=[]는 "홀드 없음" 존중). **isUserSource 분기는 절대 불변.**
- ② deck-only 하단배치 규칙 재제거(6/9 확정 — 재발 1호). autoPageLayout deckOnlyKeys 자리바꿈 제거. **교훈: 세션 간 수정은 같은 ZIP 라인에서 이어가야 함.**
- ③ 해치선 CSS(calc(100%-18px)+margin). ④ tier 라벨 고정 13px 재제거(V7.39 — 재발 3호, flex 균등분배 1:1).

### V7.36 .def 단면 디코더 — 베이매트릭스 자동 생성 (336척 실데이터 확립)
- .def(CASP SHIP DEFINE FILE)에서 베이별 행 라벨·데크/홀드 tier·hasZero·홀드 유무 직접 디코드. 329척 중 314척(95.4%) 자동 성공. 파이썬↔JS(defSectionParser.js) 314척 전수 일치.
- **디코드 구조(실데이터 확립)**:
  - 6.00/6.10/6.30: 베이테이블 ~60,500(144B 간격) + 단면 레코드 ~89,089(120B 간격). 레코드=[홀드행][홀드tier+데크tier][데크행]. 포트측(짝수) 행이 이전 레코드 꼬리로 wrap. 베이테이블 byte+121=0이면 홀드 없음.
  - 6.50/6.60: 베이테이블 ~135,900(188/189 혼재 간격 — 고정 보폭 금지) + 단면 234B 창. 함정: 라벨 공백없이 붙음('0911'), tier열 잡음 라벨.
  - 행 시퀀스: 짝수 내림+(00)+홀수 오름, ≤30. tier: 짝수 오름, 홀드 02~20+데크 82~98.
  - .def 행 리스트는 영역별 최대폭. holdCells = 홀드 행 수로 자동 채움.
- **검증(pdftoppm 대조)**: DJCT(6.30) 22베이 일치(기존 override bay01 82단 누락 발견) / KSKM(6.50) 22베이 / NBTD(6.50) bay01 데크6·홀드4. ⚠ **PDF 파일명 믿지 말 것**(SWAT__STOW_PLAN.PDF 실내용은 PCSZ — 헤더에서 추출).
- 앱: defSectionParser.js + augmentMatrixFromDef + ShipMatrixBuilderModal ".def 업로드" 버튼. ⚠ 콜사인은 6.50/6.60만 자동(6.10/6.30은 오염값 — PORT-MIS 오매칭 위험).
- 미해결(잔여 15척): PCSZ류(중앙부 셀 데이터가 .def에 없음 — PDF 보강), CNAF/XISH/RZIN(베이테이블 다른 위치).

### V7.37 .def 내장 사전 289척 — 우선순위 원칙
- 314척 중 289척 앱 내장(shipBayDict_def.js, ~370KB). EDI만 올려도 vsl 자동 매칭. 우선순위 Ⅱ-5.1대로(사용자 매트릭스 > PDF override > .def 사전 > v5 > v2 > EDI).
- 오염 제외 25척(자동 휴리스틱): 최대 데크행≥17 且 (최대−최소)≥10 = 선체외곽 템플릿 의심(PCSZ·ACVG류·SWAT·PCBJ 등). 위젯 3상태: 📐매트릭스 확정 / 🚢.def 검증전 / ⚠확정 필요.

### V7.35 안정성 보완 2건 (루트 ErrorBoundary + SW 오프라인 캐시)
- ① 루트 ErrorBoundary 부재(인쇄 모달에만) → main.jsx에서 App 전체를 `<ErrorBoundary reloadButton>`로. 런타임 에러 1건이 트리 전체 언마운트 = 흰 화면 + 카메라 멈춤(현장 사고 패턴) 방지.
- ② SW 오프라인 폴백 죽은 코드(cache.put 없음) → network-first 유지하되 같은 출처 GET 성공만 캐시. 검증 4건 PASS.
- 확인된 기수정: XRAY 셀 배경색·작업보고 항차·트윈 짝꿍은 V7.32~33에서 이미 수정됨을 코드로 확인.

### V7.34 빈 카고플랜 PDF 파서 + 자체 검증 (SWRG 2605N)
- 목표: PDF(CASPI STOWAGE)에서 빈 카고플랜(베이매트릭스) 자동 추출. 빈 프레임은 선박 고정 구조.
- 판독 원칙: 베이마다 확대해 셀 테두리(rect)만 센다(화물 마크 무시). "빈 곳 위에 화물 못 올라간다"(위 화물 있는데 아래 비면 선체 구조물). hold는 역피라미드. deck도 맨 아래 좁아질 수 있음. row 시작: deck 10·hold 08. tier는 2자리 문자열.
- 구현: emptyPlanVerify.js(verifyEmptyPlan — EDI 화물이 빈 프레임 칸 안에 있는지 교차검증, SWRG 909화물 0충돌). PDF 벡터 파서(pdfplumber). 미세조정 잔여: 94 tier 분류, deck 폭 ±1.

### V7.33 트윈 짝꿍 3대 수정 (twin.js / SearchPanel.jsx)
실데이터(DXQD=XIN QUN DAO, H3OI) 시뮬 PASS.
- ① 짝꿍 못 찾음 진짜 원인: 짝꿍 판정에 사이 짝수 베이의 **EDI 적재 여부**를 조건으로(`baySet.has(짝수)`) → 40ft 안 실린 항차는 짝수가 baySet에서 빠져 23(24)25 구조인데도 23↔25 놓침. 수정: 짝수 베이 유무는 실제 베이 구조(매트릭스→사전→EDI) 기준. 짝수 베이 조건 자체는 유지(통로/슬롯 구분). ⚠ 통째로 빼면 3↔5 무관 베이를 묶음.
- ② 앞 완료 시 뒤 사라짐: useEffect 재실행 + excludeCns. → 같은 앞 컨 조회 중 재계산 가드, excludeCns 제거.
- ③ 뒤부터 완료 후 조회창 안 닫힘: 최신 allContainers에서 두 컨 완료 여부 재조회. 둘 다 완료면 자동 클리어.
- 교훈: 사용자가 짚어준 정답(짝수 조건이 EDI 적재 여부에 묶임)을 추측으로 덮지 말 것.

### V7.30 PORT-MIS 오매칭 진짜 원인 (DJCT에 XIN TAI PING)
- V7.27 ⑧(계열대체 신원 제거)은 원인이 아니었음. 실제 = 베이사전(Firebase) **오염 레코드**. 과거 DJCT 코드로 올린 EDI가 잘못된 헤더(BSDU/XIN TAI PING)를 달고 있었고 검증 없이 박힘. 정상 EDI는 callsign='' → 저장 조건 거짓 → 병합이 기존 BSDU 보존 → 재업로드로도 안 지워짐.
- 3겹 수정(실데이터 PASS): ① 화면 가드(VoyagePage — PORT-MIS 선박명이 EDI 풀네임과 다르면 그 매칭 버림) ② 저장 조건 완화(`if(r.callsign||r.vsl)`) ③ 병합 교정(firebase.js — 새 선박명이 기존과 다르면 오염 콜사인 교체).
- 교훈: "한 곳 보고 정상 단정" 금지. 데이터 흐름은 저장 시점까지 역추적.

### V7.27 코드 정합성 점검 — "곳곳에서 안 맞음" 6건 일괄 수정
- ① 평택 판정 통일(isPyeongtaekPort 단일 함수로 8파일 14지점) ② 양하 선사 색 3자(normalizeCarrierCode — 변환표 경유, ⚠ 단순 slice 금지: SNKO→SKR) ④ 베이매트릭스 확정 배지 ⑥ V1 통과화물 X 제거(shadow X와 구분 불가) ⑦ 리퍼·특수 판정 통일(isReeferContainer + c.fr/tk/ot/oog) ⑧ PORT-MIS 오매칭(계열 대체 시 bayDef callsign·name·vesselName 제거).
- 빌드 교훈: 루트 index.html은 빌드본이라 `npm run build` 직접 시 src 미반영 — 반드시 `bash build.sh`.

═══════════════════════════════════════════════════════
# Ⅲ부 — 앱 B: 콘앱 (콘 계산기, cone.html)
═══════════════════════════════════════════════════════

> 콘앱은 **검수앱 ZIP에 동봉되는 독립 HTML**(`public/cone.html`). build.sh가 dist·루트로 복사 + 버전 라벨을 APP_VERSION과 자동 동기화한다. **검수앱과 별개 앱이므로 React 점검과 별도로 cone.html도 따로 점검할 것.**

## Ⅲ-1. 콘앱 정체·목적
- 콘(고정장치)을 컨테이너 구멍 위치에 미리 갖다 놓기 위한 가감 계산기. 입력 = 양하/선적 플랜(EDI/PDF/사진), 출력 = 베이별 추가/반납 개수.
- 검수앱 EDI 연동(fbFetchEdiContainers)도 평택분만 필터(양하=POD평택, 선적=POL평택).
- Gemini 키는 검수앱과 같은 키·같은 localStorage('master_gemini_api_key_v1') 공유(같은 도메인).

## Ⅲ-2. 콘 계산 도메인 규칙 (정확도는 생명)

### Ⅲ-2.1 콘 규칙 (컨 1개당)
- Deck 40ft = 데크콘4 / Deck 20ft = 데크콘2 + 코끼리콘2 / Hold 40ft = 일반선4·컨테이너선0 / Hold 20ft = 홀드콘1.

### Ⅲ-2.2 받침 판정
- 콘은 2단부터. 바로 아래 단 같은 row에 컨(우리 화물이든 잔류분이든)이 있으면 받침 성립.
- **받침은 자기 plan 전체화물(ediRowsAll) 기준** — 평택분만 보면 통과화물 받침 컨의 콘 누락(반대 plan은 받침 아님, 양하 후 선적 시간순).
- **받침 ±1(짝꿍 베이) = 40ft만** — 두 베이에 걸쳐 실리는 물리. 옆 베이 20ft는 다른 슬롯(받침 오인 금지).

### Ⅲ-2.3 트윈 합본 계산
- 트윈 1쌍 = 데크콘4 + 코끼리콘4. 홀드콘은 두 베이 합산(23콘6 + 25콘6 = 12). ⚠ **합산은 각자 센 값의 합이지 자기 값의 2배가 아님**(V7.61 ×2 버그 교훈).

### Ⅲ-2.4 최종 계산
- 추가필요 = 선적 소요 − 양하 회수(양수=장착, 음수=반납). 곳당 + 총개수 둘 다 표시(데크콘 4곳, 코끼리콘 2곳, 홀드콘 1곳).
- 입력 3종 자동감지: EDI(.edi/.txt, 가장 정확)·PDF·사진. 양하=POD평택, 선적=POL평택만.

### Ⅲ-2.5 양하/선적 분리 작업 (ATPR형 도메인)
- 일부 선박(ATPR 등)은 양하 완료 후 콘 전량 반납 → 하루 이상 지나 선적 = **분리 작업**. 이 경우 상계가 틀림(회수분이 배에 없음) → 선적 콘 전량 콘박스에서.
- 자동 분별: 양하·선적 둘 다 로드 + 작업일 차이 ≥24h → confirm. 완료 기록 있으면 확인창 없이 자동 분리(V7.90).

## Ⅲ-3. 콘앱 핵심 원칙

### Ⅲ-3.1 질문기는 계산하지 않는다
- 콘앱 질문기는 작업표(window.__coneQA.rows)를 **읽기만** 한다. 계산과 답이 항상 일치, 도메인 로직 자동 반영, 회귀 위험 0.

### Ⅲ-3.2 단위 도메인 (검증에서 발견한 충돌)
- **콘은 '개', 컨테이너는 '대'로 센다.** 베이 질문 컨테이너 대수 분기에 '몇 개'를 추가하면 "데크콘 몇 개"가 컨테이너 질문으로 샘 — `/컨테이너|몇\s*대|대수/` 유지.

### Ⅲ-3.3 콘앱 시뮬 방법 (영구)
- 합성 BAPLIE(LOC+147+pos / EQD+CN / LOC+9 POL / LOC+11 POD / TDT 선박명) 생성 → puppeteer로 cone.html 열고 state 주입 → runCalc()/renderCargoView()/renderBayView() 호출 → fullPage 스크린샷.
- ⚠ state 주입 형식 필수: `state.stow={ediRows:parseEDI(txt).rows, isEDI:true, ...}` (parseEDI 원본은 rows라 직접 넣으면 ediRows undefined 크래시). 비대칭 데이터(선적 전용 베이)로 동선표 경로 검증.

### Ⅲ-3.4 콘앱 카고플랜 = 검수앱 카고플랜 V2 그대로
- 콘앱 카고플랜은 본체 PrintableCargoPlanV2 + cargoPlanCore + 베이사전(.def 내장 289척)을 React째 esbuild iife 번들(`cone-cargoplan.js`, ~1.6MB). build.sh가 빌드마다 재생성·복사. 콘앱은 동적 로드(loadCargoPlanV2) → `window.ConeCargoPlan.open(...)` 한 줄. 자체 카고뷰 만들지 말 것.

## Ⅲ-4. 콘앱 작업 이력 (최신 → 과거)

### C8.34 작업표 트리오 합본 표시 (2026-07-03 — 사용자 확정, DPRT 실EDI 검증)
- 문제(사용자 제보): 콘 작업자는 트리오(예 33·34·35)를 "34번"으로 보는데, 작업표 그룹 규칙(짝수 e↔e+1 페어, 홀수는 앞 짝수 없으면 단독)이 **선행 홀수(33·25·17·13·09)를 별도 행으로 고아화** → 작업자가 짝수 행(34-35 필요 26)만 보고 홀수 몫(33 필요 2)을 누락.
- 수정: 계산·그룹은 불변, **표시 계층만 병합** — mergeTrioRows(표시·질문기·동선표 공용). 큰 숫자 = 트리오 합산(합 방식: 28=2+26, 사용자 확정 — 행별 반올림 합이라 잉여 방향으로 안전), 작은 글씨 = 소그룹 분해("33:2 · 34-35:26"), 필요 숫자 강조(need-hl). 홀수 단독 베이(21 등)는 그대로.
- 검증: DPRT 2607N/2608S 실EDI jsdom 시뮬 — 33-35 필요 D28·E8, 분해·강조 렌더 4항목 PASS. 컨별 판정 계측으로 34번 콘대상 24대(1단 7대 제외)·트리오 32대 확인.


### C7.64 설치 배너 사라짐 원인(scope 충돌) 수정 (2026-06-13)
- 증상: 콘앱 설치 문구 사라지고 실행만 됨. 원인: 콘앱 매니페스트 scope가 `/greenmarinetally/` — **검수앱과 동일 scope** → 검수앱 설치된 기기에서 콘앱을 같은 앱으로 판정 → 배너 억제.
- 수정: 매니페스트 `id:"/greenmarinetally/cone"` 고유 식별자 + scope를 `/greenmarinetally/cone.html`로 좁힘. 검증 puppeteer 6항목 PASS.
- **교훈: 같은 도메인 하위 PWA 2개는 scope·id를 반드시 분리.**

### C7.63 홈화면 설치(PWA) 지원 (2026-06-13)
- 콘앱도 검수앱처럼 홈화면 추가. 매니페스트 start_url 절대경로 + 고정 콘(트위스트락) PNG 3종 + apple-touch-icon. **콘앱 전용 SW cone-sw.js 신설**(안드로이드 Chrome은 SW 있어야 설치 배너). 배포 시 cone-sw.js를 cone.html과 같은 폴더에 함께 올릴 것. 검증 7항목 PASS.

### C7.62 카고플랜 "새 기기에서 특정 선박 안 보임" (2026-06-13)
- 증상: YKTD 카고플랜이 기존 폰은 보이고 새 폰은 "선박 정보 없음". 원인: 카고플랜 모듈 사전 탐색이 window.__fbShipBayDict 전역(검수앱 런타임이 채움)에 의존하는데 **콘앱은 이 전역을 안 채움** → Firebase v3 사전 미전달. 로컬 사전 보유 기기만 성공.
- 수정: openCargoPlanV2에서 모듈 호출 전 `window.__fbShipBayDict = await fbFetchBayDict()` 주입. 검증 puppeteer 3항목 PASS.
- 운영 처방: "안 보이는 선박 = 보이는 폰에서 빌더 재저장" + 콘앱 C7.62 이상 필수.

### C7.61 카고플랜 "못 본다" (2026-06-13)
- 원인: ① 모듈 로더가 `?v=&r=Date.now()` — 열 때마다 캐시 무력화로 1.6MB 재다운로드, 약신호에서 실패 ② 항차 목록 fbFetchVoyages가 전체 다운로드(수 MB).
- 수정: `r=Date.now()` 제거 → `?v=__APPV`만(window.__APPV='C7.61' 상수, 모듈 갱신 시 같이 올릴 것), 재시도 2회(백오프), fbFetchVoyages 경량화(shallow 병렬, 수십 KB). 효과: 첫 1회만 다운로드. 검증 5 + puppeteer PASS.

### V7.46 콘앱 카고플랜 = 검수앱 카고플랜 V2 그대로 (V7.45 정정)
- V7.45 오해: "진짜 카고플랜"을 베이그리드로 해석해 자체 셀 뷰 제작. 사용자 의도 = 카고플랜 V2(A4 가로 한 장, o·X 마크, 검수앱과 동일 구조).
- 해법 = 같은 코드 번들: coneCargoPlan.entry.jsx 신규 → cone-cargoplan.js(~1.6MB) 동적 로드. 검증 SUNNY KALMIA(=KSKM) 합성 EDI 15베이 PASS. 자체 카고뷰 ~350줄 제거.
- ⚠ 콘앱 EDI에는 선사(op) 없어 별첨 선사별이 UNK(BAPLIE 한계). 검수앱 연동(fb ediContainers)은 op 포함이라 정상.

### V7.45 콘앱 카고플랜 본체급 시각화 (V7.46으로 대체 — 기록 보존)
- 자체 셀 뷰(88×56px, mono 4줄+특수 컬러바). parseEDI rowsAll 반환, 기본 줌 100%. V7.46이 본체 번들로 완전 대체.

### V7.44 콘앱 동선표 — 콘 종류 명확화
- 종류 칸 풀네임+색 배지(데크콘=노랑·코끼리콘=파랑·홀드콘=초록), 받는 곳 "베이NN→N개". 버그 fix: computeRoutes가 totalSupply 미반환 → 요약 코끼리콘·홀드콘 "배 안 이동" 줄 미표시.

### V7.41 콘앱 대폭 보강 — 결과 화면 정돈 ("급하게 만들어 조잡함")
- ① 결과 모드 신설(안내 3카드 자동 접힘 + 고정 결과 탭바 작업표|카고플랜|베이플랜, 자동 스크롤) ② "Failed to fetch" → friendlyErr 한국어 ③ 작업표 "회수N→필요N" + 범례 ④ 동선표 빈 상태에도 카드 유지 ⑤ 베이뷰 컨번호 **끝 4자리 노란 강조**(현장 호출 관행). 비활성 계산 버튼 opacity .35.

### V7.61 콘앱 홀드콘 ×2 버그 — ATPR 실데이터로 확정
- 진단(ATPR_017 선적 실EDI, 445대): 19번 홀드 콘 18 정답인데 작업표는 36 — 전부 정확히 2배. 그룹 계산(ediToBayGroups)은 정확, renderResults 작업표 변환의 `g.stow.hold*2`가 범인. "트윈 두 베이 합산"을 ×2로 오역.
- 수정: ×2 제거(need/have/diff 원값). 베이뷰·질문기·동선표는 원값 기반이라 자동 정합. 검증 ATPR PASS.
- 교훈: 진단은 "순수 함수 단위 추적 → 화면값 비교"로 2배 지점 좁힘. 실데이터 오니 10분에 끝남.

### V7.60 콘앱 카고플랜 로드 실패 + 트윈 중복 (검수앱 동시 — Ⅱ부도)
- 콘앱 카고플랜(1.6MB) 동적 로드 누구는 되고 안 됨 → 3중 방어: build.sh 번들 `--target=es2017`(구형 웹뷰), loadCargoPlanV2 실패 시 1.2초 후 1회 재시도 + 원인 구분 안내, sw.js install에 cone-cargoplan.js precache.
- (검수앱 측) 트윈 끝4자리 중복 시 선택 안 됨 — Ⅱ부 참조.

### V7.59 콘앱 콘 계산 정밀화 3건 (정확도는 생명)
- ① 받침 ±1 = 40ft만(sizeAt 맵, 옆 베이 20ft 받침 오인 제거) ② 전용 데크(20ft 데크콘4) 판정 = **구조 기준**(베이사전 ∪ 전체화물, 화물 유무로 구조 판정하는 패턴 재발 — "구조 판정은 구조 데이터로") ③ 받침 = 자기 plan 전체화물(통과화물 받침 인정). runCalc async화. 검증 3케이스 PASS.

### V7.56 음성인식 항만 용어 보정 (검수앱+콘앱)
- 증상: "양하"를 6번 말해야 인식(크롬 STT가 양아/양화/향하로). 후처리 보정이 정답. 수정 2축: maxAlternatives=5(후보 전부서 항만 용어 최고 채택) + 오인식 교정 사전(coneFixSpeech 동일 내장 — 양아·양화·향하→양하, 홀두콘→홀드콘, 댁콘→데크콘, 니퍼→리퍼 등 보수적 치환만). 운영: 현장 오인식 단어는 🎙 표시 글자 그대로 수집 → 사전 추가.

### V7.55 콘앱 질문기 — 콘 작업 전용
- 범위 확정: 콘 작업에 관한 것만("이 베이는 어떤 콘이 몇 개 남는다/모자란다"). 검수앱 질문 엔진 통째 이식은 과잉 → 작업표(rows) 읽는 초경량 내장 파서. 🎤 음성 입출력(+/−를 플러스/마이너스). 베이 매칭: 페어 라벨("06-07") split. X-RAY·실번호·진행률은 콘앱 데이터에 없어 미지원.

### V7.52 콘앱 베이플랜 — 베이 간 정렬 (검수앱 동시 — Ⅱ부도)
- 콘앱(cone.html): 골격 계산을 computeSkel로 추출해 2패스 — 1패스 전 베이 골격·globalMaxW 수집, 2패스 centerMargin 기준 globalMaxW. 검증 TMPZ 일직선 PASS.

### V7.51 콘앱 베이플랜 — 자리 표시 3원칙 (영구 원칙)
V7.49·V7.51 1차 연속 오답 — 사용자가 검수앱 화면(TMPZ) 비교로 정정. **영구 명문화.**
- ① 자리는 항상 표시: 컨테이너 없다고 자리가 없는 게 아니다. 빈 슬롯=점선, 빈 단도 통째 표시. 반대 작업 슬롯(off)도 보이는 점선.
- ② 가상 칸 금지: 데크8·홀드7이면 홀드 7칸 그대로. 가상 칸·투명 공백·null 칸 삽입 금지. 정렬은 좁은 쪽 0.5칸 margin(피치 60px → 30px).
- ③ 폴백 연속 보간: 사전 없는 선박 EDI 폴백 골격은 보인 범위 내 연속 보간. 컨테이너 있는 칸만 수집하면 빈 row·단 누락(① 위반).
- **교훈: 정렬 방식을 세 번 갈아엎었다. 처음부터 Ⅱ-4.4와 검수앱 실화면(TMPZ)을 대조했으면 한 번에 끝났다. 화면 약속은 본체 화면이 정답지다.**

### V7.50 콘앱 베이플랜 — phantom 00 제거 + 카고플랜식 정중앙
- 사용자 지적: 강제 삽입 00 row(컨테이너 불가 자리). 원인: 공통 hasZero를 데크·홀드 양쪽 적용 + EDI 폴백 deckRowPos=holdRowPos=합집합(Ⅱ-6 축 통합 phantom 00 재발). 수정: 데크/홀드 row 완전 분리(deckHasZero/holdHasZero 각자), 칸 삽입 폐지, 정렬 = 좁은 쪽 table margin-left=(maxCols−cols)×60÷2. 검증 PASS.
- **교훈: 데크/홀드 분리 관리 원칙은 본체만 아니라 콘앱 등 모든 화면에 적용. 같은 실수가 앱을 바꿔 재발.**

### V7.49 콘앱 베이플랜 — 진짜 "빈 화면" 제거
- V7.48 오해: "빈화면"을 점선 격자로 해석해 압축만. 사용자 의도 = 격자도 없이 공백으로 비는 영역. 진짜 원인: cells가 양하+선적 합집합 — 반대 작업 전용 단의 슬롯이 `bv-slot off`(배경과 거의 동일색)로 안 보이게 그려져 영역만 차지. 수정: 패널별 tier 필터(tiersWith — 그 작업 컨 있는 단만). 검증 PASS.
- **교훈: 시각 증상 단어("빈화면")는 코드 용어 번역 전 재현 화면으로 확인. "그려지지만 안 보이는" 요소는 점선/격자 어휘에 안 잡힌다.**

### V7.48 콘앱 베이플랜 — 홀드 쏠림 fix + 빈 구역 압축
- 홀드 쏠림: 데크/홀드 별도 `<table>`인데 row 라벨 행이 열폭 흔듦. 수정: `table-layout:fixed; width:max-content` + 전 칸 58px 고정(tier 라벨 18px). **교훈: 별도 테이블 두 개 세로 정렬엔 table-layout:fixed + 전 칸 고정폭 필수.** 빈 구역(컨 0)은 점선 격자 대신 "갑판 · 화물 없음" 한 줄.

### V7.47 콘앱 작업 목록 = 검수앱과 1:1 (숨김 필터 제거)
- 증상: 검수앱엔 있는 항차가 콘앱에 안 보임. 원인: 콘앱 fbFetchVoyages에 3일 필터 + EDI 없는 항차 제외. 수정: 3일 필터 제거(살아있는 항차 전부) + EDI 없는 항차 noEdi 회색 표시. **원칙: 콘앱 목록은 검수앱 voyages와 1:1. 콘앱이 따로 숨기는 조건 두지 말 것**(숨김은 검수앱 완료/자동삭제 담당).

### V7.91 콘앱 음성 질문 자동 복원 + build.sh 라벨 누적 수정
- 검수앱 V7.80 동일 이식(coneFixQuestionWithAI, temp0·60토큰·4초). **AI는 답하지 않는다 — 질문만 복원**, 답은 coneQaAnswer(작업표 읽기)에 재투입. Gemini 키 검수앱과 공유.
- build.sh 버그: cone.html 라벨 sed `V[0-9.]*`가 하이픈 미포함 → 빌드 번호(-07) 도입 후 라벨 누적(`V7.90-07-06-05…`). `V[0-9.-]*`로 수정(오염 라벨 자가 치유). 검증 6케이스 PASS.

### V7.91-02 질문기 일상 동의어 확장 (검수앱+콘앱)
- "전체"는 되는데 "전부/다"는 안 됨. 검수앱: 전부·모두·몽땅·죄다 + 단독 "다"(토큰 경계 매칭 "남았다" 오인 방지) + '데크' 추가. 콘앱: 남았·남을·뺄 / 필요·가져가야.
- ⚠ 단위 도메인: 콘앱 베이 질문 컨테이너 대수 분기에 '몇 개' 추가 금지(Ⅲ-3.2). **복원 어휘 동기화: 파서 어휘 늘리면 KNOWN(콘앱 CONE_QA_KNOWN)도 같이 늘릴 것(영구).**

### V7.70 콘앱 양하/선적 분리 작업 자동 분별 — ATPR형 (도메인 신설)
- Ⅲ-2.5 도메인 구현. 양하·선적 둘 다 로드 + 작업일 차이 ≥24h → confirm "양하 이미 완료해 콘 전부 반납?". 작업일 출처 = EDI 작성일(extractEdiDocDate: DTM+137 우선, 폴백 UNB). 검증 ATPR_017 3케이스 PASS.

═══════════════════════════════════════════════════════
# Ⅳ부 — 앱 C: 벌크탤리 (벌크선 코일 검수, bulk_tally.html)
═══════════════════════════════════════════════════════

> 최종 업데이트: 2026-06-12 (양하 모드+펄프 / 시작 화면 2026-06-13)
> **이 앱은 검수앱(Tallyman Master)·콘앱과 완전 별개 — 따로 점검.** 범용 코딩 원칙(0부)은 동일하게 적용한다.

## Ⅳ-1. 앱 개요

### Ⅳ-1.1 정체
- 벌크선(예: WESTWOOD 계열) **철강 코일 검수** 단일 HTML 앱. 파일 1개(bulk_tally.html)로 동작.
- React 18.3.1 + Tailwind를 CDN(esm.sh, cdn.tailwindcss.com) importmap으로 로드. **빌드 없음.**
- 기능: 바코드 스캔(html5-qrcode) + 한글자판 오타 복원, 수동/블루투스 입력, PORT MIX 차단, 취소 모드, 통계, 특수화물(데미지·HR·탑중량·Eye-to-Sky), 적부(베이플랜), 보고서(PDF/엑셀/CSV/공유), Supabase 실시간 다검수자 동기화 + 오프라인 큐, 모니터 탭.

### Ⅳ-1.2 진입 흐름 (2026-06-11 확정)
- **시작 화면 → ① 검수자명 입력 → ② 항차 선택(또는 + 새 항차 생성) → [시작하기] → 작업 화면.**
- 검수자명·활성 항차가 둘 다 있으면 시작 화면 생략. **헤더 선박명 탭** = 시작 화면 재진입.
- 신규 설치 기기에 데모 항차 자동 생성 안 함(과거 WESTWOOD VICTORIA 935코일 임베드 노출 문제). 새 항차는 빈 마스터(0 COILS)로 시작 → ⚙ 데이터 관리에서 엑셀 업로드.

## Ⅳ-2. 항차 보관함 (항차별 분리 관리)

### Ⅳ-2.1 저장 구조 (localStorage)
| 키 | 내용 |
|----|------|
| `bulk_voyages_v1` | 항차 레지스트리 {항차키: {vessel, voyage, pol, pod, updatedAt}} |
| `bulk_active_voyage_v1` | 활성 항차 키 |
| `bulk_master_<항차키>` | 항차별 마스터 (coils, bls, damaged) |
| `bulk_scans_<항차키>` | 항차별 스캔 기록 |
| `tally_settings_v1` | 전역 설정(검수자명·shift·hold/raw/tier/seq·pod·autoSeq) — 항차 무관 |
| `supabase_config_v1` / `supabase_queue_v1` | 클라우드 설정 / 오프라인 전송 큐 |

- 항차키 = `선박명-항차` 대문자·공백제거(예: WESTWOODVICTORIA-148E). Supabase voyage 컬럼과 동일 키.
- **레거시 자동 이전**: 구버전 키(voyage_v1 등)가 실제 존재할 때만 첫 실행 시 항차 1건으로 복사(원본 보존). 빈 "[]"는 부재로 취급(마이그레이션 가드).

### Ⅳ-2.2 항차 운영 규칙
- **새 항차 엑셀 업로드 자동 분기**: 업로드 파일의 선박·항차가 현재와 다르면 "새 항차로 등록?(현재 항차 보관)" confirm → 확인 시 새 항차 생성·전환. **이전 항차 스캔 섞임 원천 차단.**
- 항차 정보 수정으로 키가 바뀌면 마스터·스캔 데이터 키도 함께 이동. 활성 항차는 삭제 불가. 삭제는 기기에서만(클라우드 기록 유지).
- 초기화·스캔삭제 버튼은 현재 항차 범위만. 보고서·모니터·현황은 항차별 키를 읽음(구 단일 키 `tally_scans_v1` 잔존 참조 3곳 수정 완료).

## Ⅳ-3. 스캔·동기화 핵심 동작

### Ⅳ-3.1 스캔 흐름
- handleScan 전체가 try/catch 보호 — 처리 중 예외가 나도 **스캔 루프/카메라가 죽지 않음**(검수앱 "에러 시 카메라 멈춤" 패턴 예방).
- 중복 디바운스 2초. 마스터 미매칭/중복/PORT MIX는 진동·비프·플래시로 구분.
- **Seq 자동+1 토글**(기본 꺼짐): 켜면 스캔 성공마다 SEQ +1. ※ 증가 규칙이 현장과 다르면(TIER 우선, HOLD별 리셋) 규칙 확인 후 조정 — 미확정.
- 스캔 카드 time 필드 방어 처리(time 없는 비정형 레코드도 크래시 없음).

### Ⅳ-3.2 클라우드 큐 (데이터 유실 방지 — 절대 보존)
- 전송 실패 항목은 큐에 **보존**(과거: 실패해도 통째 clearQueue → 유실). 중복(23505)은 성공 취급.
- **삭제도 큐잉**: 오프라인/실패 시 `{_op:"delete", voyage, pid}` 큐 보관(과거: 취소 코일이 재접속 머지로 부활).
- 30초 주기 자동 재시도(선상 네트워크 플랩 대비) + 동시 실행 가드. 원격 DELETE 수신 시 항차 검증. CSV escape 처리.

## Ⅳ-4. 작업·검증 원칙 (이 앱 전용)

### Ⅳ-4.1 수정 원칙
- 파일 안 JS는 **컴파일된 jsx()/jsxs() 호출 형태** — UI 추가·수정도 같은 형태(JSX 문법 금지).
- 배포본은 CDN importmap(esm.sh 9곳 + tailwind) 원본 유지. 수정 후 md5로 출력본 동일성 확인. 새 localStorage 키는 `bulk_` 접두사.

### Ⅳ-4.2 검증 방법 (CDN 차단 환경 실 브라우저 테스트)
1. **검증용 사본**(bulk_tally_TEST.html)만 importmap을 로컬 번들로 치환 — 배포본은 손대지 않음.
2. 로컬 번들: react/jsx-runtime/react-dom-client/lucide를 하나의 all.mjs로 함께 번들(esbuild, react 단일 인스턴스) 후 specifier별 shim 분배. CJS `export *` 재수출은 named export 누락 → 명시적 export 필수. external 방식 금지(ESM throw).
3. Tailwind는 @tailwindcss/browser(npm) 대체.
4. python http.server + puppeteer-core(`--no-proxy-server`). 첫 로드 60초+ 가능(워밍업 후 재시도).
5. **테스트 레이스 주의**: localStorage.clear() 후 곧바로 reload하면 구 페이지 effect가 키 재오염 → clear 전후 1초+ 대기.
6. 회귀 스위트(27항목): 시작 화면 12 + 항차 흐름 8 + 스캔 기능 7. 수정 시 전체 재실행.

## Ⅳ-5. 작업 이력

### 버그 수정 9건 (2026-06-11)
1. 오프라인 큐 유실(실패 시 통삭제) → 실패분 보존 / 2. 취소 코일 부활(오프라인 삭제 미전파) → 삭제 큐잉 / 3. 스캔 콜백 예외로 카메라 정지 → 전체 보호 / 4. CSV 콤마 깨짐 → escape / 5. 큐 재시도 부재 → 30초 타이머 / 6. 타 항차 원격 삭제 전파 → 항차 검증 / 7. 보고서/현황 구 단일 키 참조 3곳 → 항차별 키 / 8. time 없는 레코드 크래시 → 방어 / 9. 마이그레이션 가드가 빈 "[]"를 데이터 오인 → 부재 취급.

### 기능 추가
- Seq 자동+1 토글 / 항차 보관함(분리 저장·전환·삭제·새 항차·자동 마이그레이션·업로드 자동 분기) / 시작 화면(검수자명+항차 게이트, 헤더 탭 재진입, 신규 설치 데모 차단).

### 양하 모드 + 펄프 화물 (2026-06-12)
- 항차에 **workMode**('loading'|'discharge')·**cargoType**('coil'|'pulp') 추가. 새 항차 폼에 토글.
- **양하 항차 키 = `선박-항차-DIS`**(선적은 무접미 — 하위호환). Supabase voyage도 같은 키라 자동 분리. 항차 정보 수정 시 병합 보존.
- 라벨 동적화: WORK_MODE/CARGO_TYPE/MODE_KO/UNIT_EN(COILS↔UNITS)/CARGO_KO(코일↔펄프) — VOYAGE 로드 시 결정.
- **마스터 0건 자유 기록**: 0건 항차는 스캔·수동 입력을 자유 기록(_free, 펄프면 품명 PULP). 1건 이상이면 기존대로 미매칭 차단.
- 미확정: **펄프/양하 마스터 엑셀 형식 미확보** — 샘플 수령 시 parseFile에 파서 추가. (WW_COLUMBIA DONGBU PPG 시트는 제품번호 없는 PO 수량이라 대조 부적합.) 검증 12항목 PASS.

### 시작 화면 상시 표시 (2026-06-13)
- (신) 앱을 새로 켜면 항상 시작 화면(검수자·항차 확인) — 검수앱과 동일한 의식적 출근 체크. 기존 검수자명·항차는 미리 채워져 [시작하기] 한 번. 구현: enter()에서 sessionStorage `bulk_entered_session=1` → 세션 플래그 없으면 게이트 true. 같은 세션 리로드는 게이트 생략, 재시작이면 표시. 검증 5항목 PASS.

## Ⅳ-6. 알려진 한계 / 다음 후보
- **CDN 의존**: 첫 로드에 인터넷 필요(브라우저 캐시 후 대체로 동작, SW 오프라인 보장 없음) → PWA화 후보.
- Seq 자동 증가 규칙 미확정(TIER/RAW/HOLD 연동 확인 후).
- 항차별 클라우드 과거 항차 복원(기기 교체 시 Supabase 불러오기) 미구현.
- 항차 자동 보관 한도/정리 정책 없음.

═══════════════════════════════════════════════════════
# Ⅴ부 — 영구 참조 자료 (REF, 검증 완료 사실만)
═══════════════════════════════════════════════════════

> 이 부는 **데이터로 PASS 검증된 사실만** 담는다(추측·미확정은 Ⅱ~Ⅳ부 작업 이력에). 해당 작업 전 이 부를 먼저 읽는다.

## Ⅴ-A. 3D 입체 카고플랜 구현 기준
작성: 2026-06-14 (MCSN 624S 실데이터 검증). 상태: 기반 검증 완료 / 3D 프로토타입 미완.

### A-1. 절대 원칙 — 3D 격자의 진실은 `buildEmptyBayRenderData` 하나뿐
3D 좌표를 EDI 슬롯에서 임의 역산하지 **않는다**. 카고플랜·베이플랜·매트릭스 미리보기가 전부 `src/cargoPlanCore.js`의 `buildEmptyBayRenderData(bayEntry, bayKey, isPair)`를 쓴다. 3D도 반드시 이 출력을 좌표로 써야 화면 3자(카고플랜/베이플랜/3D)가 100% 일치.
- 과거 단독 프로토타입(EDI LOC+147에서 임의 매핑)은 **폐기**(빈 슬롯 안 보이고 tier/row 어긋남).
- 출력 구조: `{ deckRows:[{tier,invisible,cells:[{active,rowLbl,mark,isXray,colorKey,...}]}], holdRows:[…], deckRowPos:[…], holdRowPos:[…], nDeckCols, nHoldCols, deckTiers, holdTiers, hatchCount, deckAlign, holdAlign }`.
- 3D 매핑: 각 active 셀 = 컨테이너 한 자리. **bay=Z축(선수~선미), 칸 index(또는 rowLbl)=X축, tier=Y축(데크=갑판 위, 홀드=데크선 아래)**. EDI/Firebase 슬롯이 (bay, rowLbl, tier)에 매칭되면 색 채움. 빈 active 셀 = 아직 안 실린 자리.
- 검증: MCSN 3E39 34베이 active 셀 합 = cells 합 100% PASS.

### A-2. 베이매트릭스 데이터 — 어디서 오나
- 저장 경로: Firebase `ship_bay_dict_v3/{code}` → `window.__fbShipBayDict` + `localStorage['gm_fb_baydict_cache']` 미러.
- 정적 사전(v2/v5/def)에는 deck/hold tier 분리 구조 없음(v2=rowsMax만, v5=cells 합본·deckRows:0). 3D에 쓸 건 사용자가 매트릭스 빌더로 입력해 Firebase 저장한 것뿐.
- 빌더 저장 베이 필드: `{bayNo, deckTiers[], deckCells[], holdTiers[], holdCells[], deckHasZero, holdHasZero, hasZero, hatchCount, pairEven, deckAlign, holdAlign, deckPadLeft/Right, holdPadLeft/Right, rowCount, source}`.

### A-3. 정렬 규칙 (화면 = 진실)
- 정상 저장값은 전 베이 `deckAlign='center' / holdAlign='center'`(MCSN·KSKM 확인). center는 % 단위라 홀수 diff도 0.5칸씩 좌우 균등 = 정중앙.
- **중앙 기준은 베이마다 다름**: 홀수 row만→01 중심 / 00 있으면→00 중심 / 짝수 폭→01·02 사이. `getRowPositions(nCols, hasZero)`가 처리하므로 3D는 `rowPos` 그대로 따름(재계산 금지).
- "우측 정렬" 규정 언급이 나와도, 실제 카고플랜 표시(center)가 진실.

### A-4. 페어(트윈) 베이 — 카고플랜 묶음 vs 베이 나열
- 카고플랜은 짝수+다음 홀수 한 페이지로 묶어 그림(02-01). 단, **베이 나열 자체는 개별 번호**(1,2,3,5,6,7…). 3D 베이 선택 UI는 개별, 격자 렌더만 페어.

### A-5. EDI 슬롯 좌표 — LOC+147 BBBRRTT
- `LOC+147+{7자리}` = bay(3)+row(2)+tier(2). 컨번호는 같은 그룹 `EQD+CN`(curLoc을 EQD까지 유지). tier≥80=데크, <80=홀드.
- ⚠ **미해결 검증 포인트**: EDI row는 선형(1,2,3…), 격자 rowPos는 중앙정렬(…02,01,03…). 단순 숫자 비교 매칭은 11.9%만 맞음 — **BayPlan의 EDI→좌표 변환 로직을 그대로 가져와 재검증**(다음 세션 1순위, 목표 100% PASS).

### A-6. 검증된 테스트 선박
| 코드 | 선박명 | 구조 | 용도 |
|---|---|---|---|
| 3E39 | MCSN (SPIL NIKEN) | 34베이 완전 | 대형선 |
| 3E89 | KSKM | 22베이 완전 | 일반선 비교 |
| 3E47 | MAMP | 36베이 완전 | 추가 |

- 전체 89키 중 deck/hold tier 구조 완비 = 27키(3E39 3E47 3E89 3EHZ 5LRE 9HA3 BSDU D5MO D5MP D5QW D5RR D7XF DCON H3OI PCSZ SWCP SWDN SWRG TMPZ TNJP V2EE V7A2 V7A4 V7A5 VRKS VRSC YKTD). 나머지는 ASC/자동본이라 구조 없음 → 3D 불가.

### A-7. 발견 문제 (MCSN 624S 세션)
- **MCSN 두 키 분열(해결)**: 같은 배가 `MCSN`(ASC 자동본 0/22)·`3E39`(콜사인·matrix_builder 34베이) 두 키. EDI는 MCSN으로 들어와 빈 자동본과 충돌. 해결: 조회 시 source='user'/sourceFile='matrix_builder' 우선(DXQD↔H3OI 패턴과 동일).
- **3D 좌표 EDI 역산 금지(원칙 신설)**: A-1로 확정.
- **정렬 = 화면이 진실(확정)**: A-3.
- **페어 묶음 vs 베이 나열(확정)**: A-4.

### A-8. 검증 환경
- 매트릭스 데이터: `fb_baydict_full.json`(콘솔 export — `window.__fbShipBayDict` 또는 캐시를 JSON 다운로드).
- 시뮬: `node --input-type=module`로 cargoPlanCore.js의 buildEmptyBayRenderData 직접 import. 렌더 PASS: SVG → puppeteer-core(`/opt/google/chrome/chrome`, `--no-proxy-server`) PNG 캡처 → view 확인. 빌드/생성만으로 완료 금지.

---

## Ⅴ-B. 베이 페어링 (pairEven)
근거: V7.98-11 카고플랜 페어 "3 (4)5"→"3 5" 붕괴 수정(node 실함수 시뮬 ALL PASS).

### B-1. 페어 저장 모델 (진실)
40ft 컨테이너는 짝수 베이 1개를 차지하고 물리적으로 양옆 홀수 2개 자리를 덮는다. 표기는 "홀 (짝)홀" 트리오("3 (4)5") 또는 "(짝)홀" 페어("(04)05").
- 매트릭스 빌더는 페어를 **홀수 베이 엔트리 1개**로 저장하고 `pairEven` 필드에 짝수 번호. **짝수 베이는 별도 키를 만들지 않는다.**
  - 예: "(04)05" → 키 `005`, pairEven='04'. `004` 키 없음. 트리오 "3 (4)5" → 홀수 `003`(top) + 홀수 `005`(pairEven='04').
- 단, .def/v5 PDF 파싱본은 **짝수를 별도 엔트리**로 갖는 경우가 있음 → 같은 배 안에서도 두 표현(pairEven 흡수 vs 짝수 별도) **공존 가능**. 이게 비대칭의 근원.

### B-2. 페어를 다루는 모든 함수 (수정 시 전부 점검)
| 함수 | 파일 | pairEven 처리 |
|---|---|---|
| createEmptyBayEntry | shipMatrixBuilder.js | 생성자(정의처) ✓ |
| matrixToBayDictEntry | shipMatrixBuilder.js | 전파 ✓ |
| detectMissingBays | shipMatrixBuilder.js | 인식 ✓ (M6.94.36) |
| autoPairBays | cargoPlanCore.js | 인식 ✓ (V7.98-11, 짝수-별도 루프 뒤 pairEven 루프) |
| matrixBays 생성부 | PrintableCargoPlanV2.jsx | 전파 ✓ (V7.98-11) |
| pairEven 리졸버 헬퍼 | cargoPlanCore.js (~880행) | 인식 ✓ |
| buildBayPages | PrintableBayDetail.jsx / PrintableCargoPlan.jsx | (n-1,n) 휴리스틱 — **점검 권장** |
| splitForeAft | PrintableBayDetail.jsx | 짝수 별도 기준 — **점검 권장** |
| getBayPairs / findTwinCandidate | twin.js | **점검 필요**(실데이터 미확인) |

### B-3. 절대 원칙
1. **페어 함수는 여러 개다. 한 곳만 pairEven 보정하면 비대칭 버그 재발.**(V7.98-11: detectMissingBays는 보정됐으나 autoPairBays가 빠져 "7 (8)9"는 되고 "3 (4)5"는 붕괴.) 수정 시 §B-2 표 전부 점검.
2. **짝꿍 판정을 짝수 베이의 EDI 적재 여부에 의존시키지 말 것.** 그 슬롯에 40ft 안 실린 항차는 EDI/baySet에 짝수가 안 나타나 페어 붕괴. 판정은 매트릭스 구조 / pairEven 기준만.
3. pairEven은 항상 해당 홀수의 좌측 짝수(=홀수-1). 트리오의 top(반대편 홀수)은 짝수-1.

### B-4. 검증 (V7.98-11, node 실함수)
- pairEven 저장 페어(`005` pairEven='04') → 트리오 `["03","(04)05"]` PASS / 짝수-별도 저장 → `["07","(08)09"]` 불변 PASS / 단독 "03"/"05" 잔존 안 함 PASS / 레거시 회귀0·중복0 PASS.

═══════════════════════════════════════════════════════
# Ⅵ부 — 운영 매뉴얼·실증 데이터 (검수앱)
═══════════════════════════════════════════════════════

## Ⅵ-1. 선적 작업 워크플로

**양하 vs 선적의 본질**
| 구분 | 양하 | 선적 |
|---|---|---|
| EDI 위치 의미 | 실체(이미 적재됨) | **계획**(적재 예정, 실체 없음) |
| 검수원 역할 | 실제 화물 검수 | **계획 → 실체 전환** |
| 위치 변경 | 의미 없음 | 빈번(현장 적치 다름) |

- **실체 발생 시점**: EDI 계획 → 컨번호 조회 → (위치 확인/수정) → **선적확인 버튼** → 그때 실체.
- **위치 두 박스**: 선내 위치(계획) = EDI 단일 진실, 변경 금지 / 실체 위치 = 검수원 입력(`01/00/02 → 11/11/11 (검수원명)`), "삭제"로 계획 복귀. 양하에는 실체 박스 없음.
- **자리 뺏긴 컨테이너**: 컨 X가 위치 수정해 컨 Y의 계획 자리 점유 → 베이 탭 상단 노란 박스에 Y 자동 표시. 처리: (A) [📦 이동] → 빈 셀 클릭(짝수↔짝수/홀수↔홀수만) (B) 수동 입력.
- **보관함(선적 전용)**: bay_actual='__STG__' 컨 모음. [📦 이동] 복귀, [↻ 일괄 복원]. PC 영역 선택([🔲 선택]).
- **마감 점검([🏁 마감 점검])**: 미완료 컨 / 리퍼 온도 미입력(Full만) / ISO403 사진 / X-RAY 미처리(양하만) / 자리 뺏긴 컨(선적만) / 풀씰 미입력(선적만) — 모두 0이면 ✅.
- **양하리스트 매핑**: 풀씰 → c.sl, 엠티실 → c.eseal 자동 분리(헤더 자동 인식).
- **데이터 모델(선적)**: `bay/row/tier`=effective(actual 우선) / `_bay_planned`=계획 보존 / `bay_actual`+`actual_at/by`=실체 / `_position_moved`,`_displacedBy`. Firebase: `voyages/{voyageKey}/loading/records/{cn}`. 함수: fbSetActualPosition, fbClearActualPosition.

## Ⅵ-2. CASP .def 블록 크기 매핑 (323척 실증)
| CASP 버전 | 표준 블록 | 예외 | 척수 |
|---|---|---|---|
| 6.00 | 144 | — | 5 |
| 6.10 | 144 | 80(4척) | 185 |
| 6.30 | 144 | 80(2척) | 11 |
| 6.50 | 189 | 102(3척), 201(1척) | 120 |
| 6.60 | 189 | — | 2 |
- 블록 크기는 CASP 버전이 결정(TNJP 189를 전체 적용하던 옛 가정 폐기). V7.36~38 단면 디코더가 이 매핑 위에 구축됨.

## Ⅵ-3. 라이브러리/보관소 분리 설계 (Ⅱ-7.3/7.4 근거)
- 라이브러리(진행 상황) = 살아있는 voyages. `선박명 · 양하 N · 선적 M`(평택분). 완료하면 사라짐. 수석용 [완료 저장].
- 보관소(완료 기록) = ships 노드, 최근 완료순. 항차별 줄(`2608N · 양하 452 · 선적 465 · 일자`) + 누적.
- 완료 흐름: ① archive/{voyageKey} 전체 백업 ② ships에 항차 통계 ③ 백업 확인 후 voyages 삭제. 이중 확인(항차 화면=검수사, 라이브러리=수석).

## Ⅵ-4. [REF] 신규 선박 복제 파이프라인 (V7.99~V7.99-5, 검증 완료)
진본 .def 없는 신규 선박을 기존 등록 선박 복제·보강해 베이매트릭스를 만드는 4단계. 실데이터(EVER CONFORM EDI·fb_baydict 89척·Define.zip 329척) 검증 PASS.

- **4단계 흐름**: ① 기존 선박 매칭에서 구조 우선 추천 → ② 그 매트릭스에 현재 화물(EDI) 실어봄 → ③ 중력 무시 셀 처리 → ④ 100% 확인 후 제시(그대로 저장 / 수정 후 저장).
- **우선순위(절대)**: 확정 베이매트릭스 > 복제(보정중) 구조 > EDI. 복제본도 source='user'(EDI보다 우선·자동분석 차단). 단 "🛠 보정중" 표시(provisional 플래그, 동작 영향0).
- **매칭 = 구조 우선(절대 교훈, 2회 반복 실수)**: 수용률만으로 정렬 금지(더 큰 배는 작은 배 컨테이너를 다 받아 99~100%지만 빈 베이·단 다수로 카고플랜 어긋남). 정렬 = 구조점수 우선, 수용률은 동률 tiebreak. 구조 지문(shipMatchFinder.js extractFingerprint): ① 홀짝 정규화 베이집합(40ft 페어 정규화 — 안 하면 같은 배도 0.68로 깎임) ② 부분집합 ③ capacity 수용성 ④ deckonly 패턴 ⑤ 데크높이등급 ⑥ 끝베이 근접. 검증: EVER CONFORM → SAWASDEE 계열·CNC SATURN 91% 공동최상위. 교차검증 89척 top-1 74%·top-3 96%.
- **화물 실어보기(verifyMatrixFit)**: tier 기준 수용률, 누락 시 missByBay. "실린 컨테이너 다 보이면 복제 성공."
- **중력 위반 셀 보정(detectBlockedCells, 확실한 것만)**: 기준 A만 — 한 베이·한 row에서 적재 최저단보다 아래 빈 단 = 채울 수 없음 = blocked. 위가 비면 미적재일 뿐(손대지 않음, 오탐0). 기준 B(여러 베이 같은 위치)는 단일 항차 오탐 위험으로 제외. 검증: EVER CONFORM 40곳 탐지, R9·R10 T2 막힘률 100%(구조적)·R1·R5 14~20%(정상) 정확 구분.
- **EVER CONFORM 구조 규칙(실측 확정)**: 베이 28개(1,2,3,5,6,7,9…38), 풀빔 06,10,14,18,22,26,30,38. **홀드 00 전혀 없음 / 데크 00은 베이별로 있음**(deckHasZero·holdHasZero 분리 필수). 홀드 있는 베이 데크 시작 84 / 선미 데크전용(33,35) 시작 82. 진본 .def는 Define.zip에 없음(동형: SAWASDEE 계열·CNC SATURN).
- **관련 파일**: shipMatchFinder.js, shipMatrixBuilder.js(blockedCells/provisional 직렬화), cargoPlanCore.js buildEmptyBayRenderData, ShipMatrixBuilderModal.jsx(🔁 복제 버튼).
- ⚠ 빌드 주의: `npm run build` 단독 금지 — 반드시 `bash build.sh`. 후 번들에 신규 한글 문자열 grep.

═══════════════════════════════════════════════════════
# Ⅶ부 — 검증 주체 원칙 (절대, 잊지 말 것)
═══════════════════════════════════════════════════════

**AI가 만들고 AI가 검증해야 한다. 사람이 검증자 자리에 앉으면 AI와 인간의 역할이 뒤바뀐 것이다.**
- 실패 패턴: ① 빨리 결론 내려고 끝까지 안 본다 ② 검증을 건너뛴다 ③ 어려우면 "한계"로 포장해 사람에게 떠넘긴다("정답지 필요"는 사실 끝까지 안 본 것). → **"할 수 있는데 안 한다."** 능력이 아니라 주의·끈기의 문제.
- 교정: 느리더라도 **끝까지·한 칸씩·검증까지**가 기본. 결과 후 반드시 스스로 교차검증(emptyPlanVerify 등). 사람은 최종 승인만.
- **경고: 이 원칙이 무너지면 AI와 인간의 역할이 바뀐다.**

═══════════════════════════════════════════════════════
# 부록 A. 디버깅 일지 (2026-05-23, 보관 — 반복 실수 경계)
═══════════════════════════════════════════════════════

핵심 원인: 데이터 구조를 추측으로 짠 것. 실데이터 안 보고 알고리즘 작성.
> "있는 데이터를 안 보고 이렇겠지 하고 만드는 거하고는 많이 틀리죠"

**잘못된 가정 모음**:
- 단일 rowMaxOdd/Even 통일 가능 → 베이마다 다름.
- rowMaxOdd=7 = "7개 row" → "가장 큰 row 번호"일 수 있음.
- has_zero = rowMax % 2 → EDI에 0 있는지 직접.
- v5_matrix.cells 신뢰 → DXQD 부정확. 위→아래 → 사실 아래→위(reverse).
- 45R = 45ft → 40ft hi-cube reefer.
- hull 바깥 강제 active → 단면 깨짐(hull 안쪽만).
- cells 개수 추측 → 실제 row 번호 봐야.
- 인쇄 visibility:hidden → 자리 차지로 밀림(display:none).
- 베이플랜 격자(flex) → 한 칸 빠지면 옆 밀림(좌표 absolute).

**핵심 교훈**: 알고리즘 작성 전 데이터 구조 직접 확인. 단일 값 가정 금지(rowMax vs rowCount 명확히). 사용자가 짚어준 정답을 추측으로 덮지 말 것. 정답 알고리즘은 새로 짜지 말고 그대로 포팅.

═══════════════════════════════════════════════════════
# 부록 B. 운영 팁
═══════════════════════════════════════════════════════

- **"메모" 규칙 (절대)**: 사용자가 "메모"로 시작하는 글을 보내면 **즉시 작업·분석·제안·질문하지 않는다.** 기억만 하고 부록 C에 그대로 옮겨 적은 뒤 "메모 접수"처럼 한 줄로만 응답한다(사용자가 연속으로 메모를 적는 중이라 흐름이 끊기면 안 됨). 사용자가 "작업해줘"(주로 퇴근 전)라고 하면 그때 쌓인 메모를 몰아서 처리한다.
- **문제 누적 규칙 (사용자 지시)**: 작업 중 발견된 문제는 반드시 해결법과 함께 이 지침서에 항시 누적할 것. 완벽하게 구현·검증 완료된 것은 영구 참조 자료(Ⅴ부 REF)로 승격해 다른 Claude가 참고하게 할 것. 추측·미확정은 작업 이력에, 데이터로 PASS 확인된 것만 REF로.
- **세 앱 완전 별개 원칙 (머리말급 — 2026-06-15 확정)**: 검수앱(A)·콘앱(B)·벌크탤리(C)는 코드베이스·배포·점검이 모두 독립이다. 한 앱을 고쳐도 다른 앱에 자동 반영되지 않는다. 이 원칙은 운영 팁이 아니라 **문서 머리말에 명시된 최상위 전제**이며(문서 첫머리 "세 앱을 한 권에, 명확히 분리" 표 참조), 모든 작업은 어느 앱을 건드리는지 먼저 특정하고 시작한다. 특히 콘앱(cone.html)은 검수앱과 버전 번호(V7.xx)를 공유하지만 독립 HTML이라 React 점검과 **반드시 따로 점검**한다.
- **콘앱 이력 분리 기록 규칙 (2026-06-15 확정)**: 콘앱(B) 작업은 앞으로도 **검수앱(A) 작업 이력(Ⅱ-8)과 섞지 말고 Ⅲ부에 별도 기록**한다. 검수앱+콘앱 동시 작업이면 양쪽에 요약하고 "(상대 부 참조)"로 교차 표기한다. 과거 지침서가 7부에 두 앱을 시간순으로 뒤섞어 추적이 어려웠던 것을 이번 통합에서 바로잡았으므로, 같은 혼선을 재발시키지 않는다.
- **단일 지침서 운영 (사용자 확정 2026-06-15 — 절대)**: **이 통합 지침서 한 권만 유지한다. 별도 지침서를 새로 만들지 말 것.** 세 앱이 모두 완성돼 독립 출시될 때까지는 검수앱·콘앱·벌크탤리 전부 이 한 권으로 간다. 과거처럼 `검수앱지침서.md`·`BULK_TALLY_지침서.md`·`REF_*.md`를 따로 분리·생성하지 않는다(따로 된 지침서는 이제 없음). 새 발견·결정·검증 결과는 모두 이 문서의 해당 부(Ⅱ~Ⅵ·Ⅴ부 REF)에 흡수한다. 앱별 분리는 각 앱이 독립한 뒤 사용자가 지시할 때만.
- 한 채팅 = 한 작업 원칙. 앱 개발 채팅과 검수 업무 채팅 분리. 세 앱(검수앱·콘앱·벌크탤리)은 완전 별개 — 따로 점검.
- 이 지침서를 프로젝트 지식에 두면 새 채팅마다 자동 참조. 교체는 하루 1회 그날 마지막 판으로(0-11). 지침서가 둘이면 버전 높은 쪽이 진실.
- 작업 완료 시 이 지침서 갱신본 제공. 세션 종료 시 당일 픽스·결정사항·새 운영 흐름을 지침서 업데이트 항목으로 제안.
- Firebase 베이사전 export 경로: `ship_bay_dict_v3` → `window.__fbShipBayDict`/`localStorage['gm_fb_baydict_cache']`.

═══════════════════════════════════════════════════════
# 부록 C. 메모 수신함 (사용자 메모 — 답변 금지, 기록만)
═══════════════════════════════════════════════════════

> 사용자가 "메모"로 보낸 내용을 날짜와 함께 그대로 적는다. 처리는 사용자가 지시할 때만.

- ~~2026-06-10: ATRP 홀드 19번·(20)21번 같이 계산~~ → V7.61 수정 완료(홀드콘 ×2, Ⅲ부)
- ~~2026-06-10: 트윈 선적 중 끝4자리 같은 게 선택 안 됨~~ → V7.60 수정 완료
- ~~2026-06-10: 콘앱 QR 사용 시 누구는 되고 안 됨(로드 실패)~~ → V7.60 수정 완료

═══════════════════════════════════════════════════════
# 부록 D. 미해결 백로그 (2026-06-15 기준)
═══════════════════════════════════════════════════════

**검수앱(A)**
1. **앱 내 신규 선박 입력 메뉴 + PDF/.def 자동 파싱** (핵심 목표) — 빈 카고플랜 PDF 파서(V7.34)·.def 디코더(V7.36)·복제 파이프라인(Ⅵ-4)이 기반. 통합 메뉴 미완.
2. 끝자리 4자리 조회를 베이상세/3D 하이라이트.
3. ATRP 등 cells 없는 PDF 자동본 — EDI에서 단면 골격 생성해 BayBoxV2 렌더(전 선박 CASPI형 통일).
4. 3D 카고플랜 EDI row↔격자 rowPos 매칭 100% PASS(Ⅴ-A-5) 후 프로토타입.
5. .def 미해결 15척(PCSZ류 중앙부·CNAF/XISH/RZIN 베이테이블 위치).
6. ~~PORT-MIS 날짜 동결 패턴 영구 수정 2종~~ → V8.01에서 해소. VoyagePage 콜사인 후보 최신 updatedAt 우선 정렬로 stale 날짜 회피(시뮬 PASS). fbSavePortMisBatch vesselName 공유 키 물리 삭제는 정상 데이터 유실 위험으로 미채택(정렬 수정으로 실질 해소). 추가로 항차 자동삭제 기준을 createdAt→작업활동으로 바꿔 EDI 미리 투입 항차 보호.
7. 미지원 질문 백로그(V7.54 표): 선사별 집계·미입력 필터·시프팅·통과화물·최대최소·날짜·서수·위험물 클래스.

**콘앱(B)**
8. 로컬 사전 → 서버 일괄 동기화 버튼. 콘앱 텍스트 복사·이미지 저장에 동선표 포함 여부(현장 피드백 후).

**벌크탤리(C)**
9. 펄프/양하 마스터 엑셀 형식 확보 후 parseFile 파서 추가. PWA화(SW+의존성 인라인). 과거 항차 클라우드 복원.

---

## 부록 Z — 세션 흡수 로그 (V8.41 이후 · 본문 미반영분 포인터 + 최신 세션)

> 본 지침서 본문 최종 반영 = **V8.40**. V8.41~V8.98-09 상세 이력은 프로젝트 `클로드_공유_완료_*` 문서에 개별 기록돼 있다. 아래는 최신 세션(V8.98-10) 흡수분이다.

### V8.98-10 (2026-07-15) — isoToLabel 공컨/45ft 숫자ISO 규격판정 오탐 수정

**증상.** ATPR 2633W 완성본에서 "알 수 없는 규격 표기 82대 - 사진 촬영 + 현장 확인 필요" 위험 패널. 각 컨테이너가 `453E`로 표시되고 파생 규격이 `403`으로 나옴.

**원인 (실데이터 확정).** 완성본 EDI(ATPR_027_RE RE.EDI)의 ISO **4530 공컨 82대**가 앱 공컨정규화(utils.js 993/1131/2051, `iso.slice(0,-1)+'E'`)로 `4530 → 453E`가 되고, isoToLabel이 `453E`를 해석 못 해 최종 fallback `'40'+p[2] = '40'+'3' = '403'`을 반환한다. `403`은 known 화이트리스트에 없어 isUnknownIso=true. 4500 공컨(→450E)은 p[2]='0' fallback이 40HC로 잡아 정상, 4530만 p[2]='3' 케이스 부재로 깨진다.

**수정 (외과적 — isoToLabel 상단 2줄).**
- `if (/^\d{3}[EF]$/.test(p)) p = p.slice(0, 3) + '0';` — 공컨/풀 마커(450E·453E·220E·950E·453F 등)를 원 숫자로 복원. loadingEdiExport numericIso/ediIso의 `/^\d{3}E$/` 복원과 동일 소스.
- `if (/^95\d\d$/.test(p)) return '45HC';` — 45ft 숫자표기(9500/9530, numericIso 대응) 규격판정 추가(복원된 950E 포함).

**검증 (PASS).**
- 25종 코드 회귀 — 오탐 4종(453E·453F·9500·950E)만 정상화, 나머지 21종 무변화.
- 실 EDI — TNJP_26349W 미지 11→0, SWDN_2608S 0→0.
- ATPR 2633W 완성본 앱 파이프라인 재현(공컨정규화 포함) — **미지 82(453E) → 0**. 화면 "82대"와 정확히 일치.
- `bash build.sh` 성공(V8.98-10). 번들 grep — APP_VERSION·두 정규식 확인. 충돌마커 0건. cone-cargoplan.js 1.6M.

**미확정 (추후 검토, Ⅶ부/부록감).** 4530을 isoToLabel은 40HC, numericIso는 40ft 리퍼(RF→4530)로 봄 — 내부 불일치. 이번 항차 BAPLIE는 45GP(드라이)라 40HC가 맞다. isISO403의 `/^45[3]\d$/`(4530류 리퍼 사진의무) 규칙은 공컨정규화값(453E)에는 매칭 안 됨 — 공컨 리퍼 사진의무 정책은 별도 결정 필요.

### 지침서 업데이트 제안
- **부록 A(반복 실수) 추가** — 규격/ISO 표시 버그는 원시 EDI 코드가 아니라 앱 후처리(공컨정규화 등)를 거친 값을 봐야 재현된다. 원시 파일만 grep하면 0으로 나온다(ATPR 2633W 453E 사례).
- **Ⅱ부(검수앱) 규격판정** — isoToLabel의 엠티/풀 마커(`\d{3}[EF]`) 복원 + 45ft 숫자ISO(95xx) 규칙을 정식 반영.
- **본문 버전 동기화** — 본문이 V8.40에 멈춰 있음. 차기 세션에서 V8.41~V8.98 주요 결정(카스피 문법 3단 수렴, 쉬프팅 자동검출, 실선적 EDI/ASC 왕복 등)을 본문에 일괄 흡수 권장.

---

### V8.98-11 (2026-07-15) — 엠티 실 리스트: 부킹슬롯 제외 + 규격 20E/45GE/45RE

**증상.** 수석대시보드 "엠티 실 작업 실시간 현황" — ①실번호 없는 `__BOOK_` 부킹슬롯(예상 EDI 빈자리)이 별도 가상 카드로 뜸 ②규격 칸이 원시 코드(220E/450E/453E) 노출.

**수정 (외과적 — 엠티 실 리스트 표기 전용, 전역 미변경).**
- `ChiefDashboard.jsx` sealVoyages targets 루프: `if (isBookingSlot(c)) return;` — 부킹슬롯 제외.
- `utils.js` `emptySealSpec(c)` 신설 + ChiefDashboard 규격 렌더(777행) 교체: DC20→`20E`, DCHC→`45GE`, RFHC→`45RE`. 리퍼 판별은 숫자 ISO 냉동코드(3번째 자리='3', 예 4530). 전역 isoToLabel·리퍼 카운트 미변경.

**근거 (메인플랜 교차대조 — 데이터 확정).** ATPR 2633W 메인플랜 TpSz ⟺ 완성본 EDI: **RFHC=4530(리퍼)** · DCHC=4500 · DC20=2200. 82개 4530 = 중국(WEI)행 빈 리퍼.

**검증 (PASS).** emptySealSpec 실함수 전수 — 공컨(리스트 대상): 20E=272·45GE=77·45RE=82. 풀(F, 정책이 제외): 20E=14·45GE=1(HLHU6411725). 즉 리스트 45GE=77(4500 78개 중 풀1 제외 — 사용자 확인). F/E 제외는 기존 applyPolicyToContainer. build.sh 성공(V8.98-11). 번들 grep 45RE/45GE·냉동정규식 확인. 충돌0. cone 1.6M.

**미확정.** 전역 리퍼 인식(isoToLabel 4530→40RF, 리퍼 카운트 반영)은 이번 미적용 — 리스트 표기만 45RE, 카운트는 40HC 유지. 필요 시 별도 결정.

---

### V8.98-12 (2026-07-15) — 카고플랜 공컨 리퍼 R/E 복구 (isReeferIso 숫자 냉동코드 인식)

**증상.** 카고플랜에서 공컨 리퍼(4530/453E)가 `R/E`가 아니라 일반 공컨 `E`로 표시 — 리퍼 구분 소실.

**원인.** 카고플랜 V2 마크(PrintableCargoPlanV2 getMarkV2)는 `isReeferContainer(c)`로 R/E 판정. 그런데 `isReeferIso`가 숫자코드 `453E`(3번째='3')를 못 잡아 false → 공컨 리퍼가 그냥 E. (알파 45RF는 [2]='R'로 잡히나 완성본 EDI는 숫자 4530.)

**수정 (단일 소스 — isReeferIso 1줄).**
```js
if (/^(22|45|95)3[0-9EF]$/.test(upper)) return true;  // ISO6346 냉동군(3번째='3'): 4530/2230/9530 + 엠티 453E
```
- numericIso(40RF→4530) 대응. 4583/4584(FR)는 3번째='8'이라 미해당. isoToLabel(size) 미변경.

**효과.** isReeferContainer(453E)=true → ①카고플랜 R/E 복구 ②리퍼 카운트 정상(+82) ③emptySealSpec 일관(45RE). 메인+cone 번들 공통(카고플랜 3진입).

**검증 (PASS).** 회귀: 4530/453E/453F/9530/2230만 false→true(전부 진짜 냉동군), 드라이 코드(2200/4500/22GP/45GP/45G1/4583-FR) 무변화. 카고플랜 마크 전수: **R/E=82·E=77·e=272·F=15**, 리퍼 카운트 **82**. build.sh(V8.98-12) 메인+cone 번들 냉동정규식 grep 확인. 충돌0. cone 1.6M.

**참고.** 이번은 리퍼 '판별'(isReeferContainer)만 수정 — 규격 라벨(isoToLabel)은 4530→40HC 유지(리퍼는 별도 플래그). 규격 라벨까지 40RF로 바꾸려면 별도.

---

### V8.98-13 (2026-07-15) — 규격 라벨 리퍼 통일 (isoToLabel 4530→40RF)

**요청.** 규격 라벨(isoToLabel)이 4530을 40HC(드라이)로 표시 — 리퍼 규격까지 40RF로 통일.

**수정 (isoToLabel 2줄).**
```js
if (/^453/.test(p)) return '40RF';   // 4530류 = 40ft 리퍼(ISO6346 냉동군, 메인플랜 RFHC)
if (/^223/.test(p)) return '20RF';   // 2230류 = 20ft 리퍼
```

**효과.** 규격 라벨·규격 카운트·PDF 라벨(isoToPdfLabel 40RF→RFHC)·카테고리(RF) 전부 리퍼로 통일. isReeferIso·카고플랜 R/E·emptySealSpec와 완전 일치.

**검증 (PASS).** isoToLabel 회귀 6종만(4530/453E/453F/4535→40RF, 2230/223E→20RF), 나머지 무변화. 완성본 규격 카운트: **20DC=286 · 40HC=78 · 40RF=82**(리퍼 82대 정확 분리). isUnknownIso(453E)=false(403 오탐 미재발). emptySealSpec 45RE/45GE 유지. build.sh(V8.98-13, 해시 index-qVB467eE.js) 메인+cone 번들 40RF·^453 확인. 충돌0. cone 1.6M.

**참고.** 9530(45ft 리퍼 숫자코드)는 앱 관례상 45HC 유지(45피트=HC only) — 리퍼 플래그(isReeferIso)만 true. 이 항차엔 미해당.

---

### V8.98-14 (2026-07-15) — 베이상세 인쇄 셀 균일화 (카스피식 고정격자)

**증상.** 베이상세 인쇄물 셀 크기 제각각 — ①점유 셀이 빈 셀보다 8~10px 넓음(셀 padding·border가 flex 분배에 가산) ②같은 행 빈 셀 폭이 행마다 출렁임 ③홀드 %패딩이 부모(wrap) 기준이라 tier라벨 16px 몫만큼 데크와 계통차 ④격자가 페이지를 flex로 채워 베이(페이지)마다 셀 크기 상이. 카스피 양식(전 셀 동일 크기 고정격자) 참조 요청.

**수정.**
- `PrintableBayDetail`: 항차 전체(모든 매트릭스 페이지)의 최대 칸수(gcMax)·최대 행수(rowsMax)로 **단일 셀 폭·행 높이 계산**(`uniformCell`, w=clamp(78~118, 1046/gcMax), h=clamp(46~84, 655/rowsMax)) → 각 페이지 `.bd-cargo-wrap.bd-uniform`에 CSS 변수(--bdc-w/--bdc-h/--bdc-gc) 주입.
- CSS(bd-uniform 스코프): 행 `flex: 0 0 var(--bdc-h)` 고정, 모든 칸(점유/빈/비활성) `flex: 0 0 var(--bdc-w)` 고정, 점유 셀 padding을 내용 래퍼(bd-cell-lines)로 이동, 빈칸 투명 border로 폭 통일, 격자·라벨·해치 중앙정렬(세로는 auto 마진 safe center), 영역 flex 채움 폐기.
- `BayBoxV2`(공용): **옵트인 prop `fixedCellVar`** 추가 — 전달 시 %패딩 대신 `calc(셀폭×칸수)` 패딩(라벨 16px 계통차 소멸). **미전달(카고플랜 본체) 시 기존 % 로직 그대로 = 회귀 0.**

**함정 (재발 방지).**
- `.cpv2-bay-content`가 `flex: 1 1 0`(basis 0)라 부모 section 높이가 4px로 붕괴 → 중앙정렬이 4px 박스를 중앙에 놓아 내용이 페이지 밖으로 흐름. bd-uniform에서 content도 `flex: 0 0 auto` 필수.
- flex `basis 0` 분배에서 아이템 자체 padding·border는 분배 폭 위에 **가산**됨(Chrome 실측) — 셀 크기 균일이 필요하면 grow 분배가 아니라 고정폭이 정답.
- 시뮬 esbuild 번들 시 엔트리가 리포 밖이면 react가 다른 복사본으로 해석돼 hooks null 크래시 — 엔트리는 반드시 리포 안에.

**검증 (실렌더 계측 PASS).** 실 STSE 2654W 선적 EDI(331개)+CASP 형상 사전으로 실제 컴포넌트를 헤드리스 크롬 렌더 계측: BEFORE 홀드 셀 폭 108~124px 6종·데크≠홀드·페이지별 상이 → **AFTER 전 페이지·전 셀 116×81 단일값**. 인쇄 PDF 육안: 카스피와 동일 구도(균일 격자·컬럼 정렬·중앙 배치·여백). build.sh(V8.98-14) 성공, 번들 grep(bd-uniform 18·fixedCellVar·--bdc-w), 충돌 0, cone 1.6M(카고플랜 경로 무변경).

**적용 범위.** 매트릭스(사전 deckCells) 페이지만 — 폴백(STD_ROWS) 페이지·화면 미리보기 외 카고플랜 본체는 기존 그대로. 미리보기 화면도 인쇄와 동일 고정격자로 표시됨(의도).

---

### V8.98-15 (2026-07-15) — 베이사전 '공유 사전 가져오기' 동기화 버튼

**배경 (크롬≠엣지 사고).** 베이사전 조회는 기기별 localStorage 사본(master_user_bay_dict_v1)이 절대 우선(§6.3)이고 FB→로컬 자동 병합이 없어, 브라우저/기기마다 사전이 어긋남 — 실사례: 같은 PC에서 크롬과 엣지의 STSE/SWDN 구조가 서로 다름. 외부(Firebase 직접) 수정도 로컬에 가려짐.

**구현.**
- `userBayDict.js` `mergeUserBayDictFrom(sharedDict)`: 공유본으로 같은 키 덮어쓰기 + 로컬 전용 선박 유지, {ok,updated,added,kept,total} 반환.
- `BayDictLibraryWidget`: "📥 공유 사전 가져오기 (이 기기 동기화)" 버튼(확인 다이얼로그 → 병합 → 결과 알림). 위젯이 이미 구독 중인 FB 사전(state) 사용 — 네트워크 추가 0.
- 자동 병합은 넣지 않음(§6.3 로컬 우선 원칙 유지) — 명시적 버튼일 때만.

**검증.** 실함수 단위시뮬(localStorage shim) — 오늘 실사례 재현: 덮어쓰기 3(STSE 88단·SWDN bay10·BSDU 80단)/신규 1/로컬전용 유지 1/빈입력 안전 전부 PASS. build.sh(V8.98-15), 번들 grep, 충돌 0.

**사용법 (지침).** 사전이 기기마다 다르게 보이면: 항차 화면 → 베이사전 라이브러리 위젯 펼침 → "공유 사전 가져오기" → 새로고침. 사전 외부 수정 후에는 각 작업 기기에서 이 버튼 1회.


---

### V8.99 ~ V9.04 (2026-07-16~17) — 요약 (상세는 프로젝트 공유 MD)

선석배정예정(V8.99) → 맛집수첩·일일점검(V9.00) → 홈 정렬 작업시간 근접순(V9.01) → 카톡 물량예보(V9.02) → 긴급·수화물 마커 + 선적 가상엠티(DUME) 분리 표기·E확정(V9.03/-01) → 매트릭스 사용불가 셀(V9.04). 각 판 상세는 `claude/클로드_공유_완료_검수앱V*.md` (프로젝트 문서) 참조.

---

### V9.04-01 (2026-07-18) — 가상 컨번호 판정 ISO 규칙화: CASP 가상엠티 오집계(실 177·총 364·누락 187) 수정

**사건 (MCSN 629S 선적).** 홈 카드가 "선적 평택 287 · 매칭 100 · 실 177+가상E110 · E확정 187 → 총 364 · 누락 187"로 표시 — 터미널 실측은 실 100 + 엠티 187 = 총 287. BAPLIE 실측: 평택 287 = 풀 실번호 100 + 가상엠티 187(**DUME 110 + CASP 77**). CASP0000001~95는 CASP69 플래너가 만든 가상번호인데 V9.03의 `/^DUME/` 프리픽스 판정이 CASP를 실번호로 오집계 → 실 177(+77 허수)·총 364(77 이중계상)·누락 187(가상 자리를 누락으로 오판). 수집기 대조리포트("실 177 + E확정 187 = 총 364"·"1: 기대 77 부족")도 동일 원인.

**핵심 규칙 (원칙 개정 — 프리픽스 열거 폐기).** 실번호는 ISO 6346 규칙상 **4번째 글자가 항상 U/J/Z**(MSKU·TCLU…). 플래너·수집기의 가상번호(DUME…, CASP…)는 이 규칙을 안 따른다. 판정 함수 단일화:
`isVirtualCn(cn) = /^[A-Z]{4}\d{7}$/ && !/^[A-Z]{3}[UJZ]/` — utils.js export. 새 가상 프리픽스가 나타나도 자동 대응(기존 "프리픽스 발견 시 필터에 추가" 원칙은 폐기).
검증: 4개 선사(MSK·카스피·DONGJIN·연태훼리) EDI 5본 3,728컨 + 리스트 실번호 974컨 → 가상 판정 = DUME 110·CASP 77뿐, 오탐 0.

**수정 지점 (검수앱 3 + 수집기 2).**
1. `src/utils.js`: `isVirtualCn()` 신설 + APP_VERSION V9.04-01.
2. `src/pages/HomePage.jsx` computeStats: `dummyE`를 isVirtualCn으로(선적만) + **`missing = ptk − matched − dummyE`** (가상 자리는 누락 아님, floor 0). 629S: 실 100+가상E 187 · E확정 187 → 총 287 · 누락 0.
3. `src/components/ValidationBox.jsx`: ①가상 자리를 '리스트에 없음(누락)'에서 제외 ②가상>0이면 EDI밖 리스트분 중 fe≠'F'를 **E확정(리스트 엠티)**으로 분리(경고 아님, 보라 정보줄 "실 100 + 가상E 187 · E확정 187 → 총 287") — '누락 187 + 추가 187' 이중 경고 소멸.
4. 수집기 `merge_helper.html` (v2.17.11-17): `isDummyE`를 동일 ISO 규칙으로 → 대조리포트 "실 100 + E확정 187 = 총 287", CASP 77 '부족' 소멸. + 엠티 출처 파일(파일명 /empty/i)인데 F/E 공란이면 `fe='E'` 채움(합본 F/E 공란 287행 실측 — 앱 E확정 판정 근거 확보).
5. 수집기 `autoreg_helper.html` (V8.84-04): EDI 선정 cnCount 필터 `!/^DUME…/` → `/^[A-Z]{3}[UJZ]\d{7}$/` (CASP도 실번호 집계 제외).

**검증 (전부 실데이터 PASS).**
- computeStats 시뮬: 구판 재현(실177·총364·매칭100·누락187) → 신판(실100·총287·누락0·가상E187). 회귀: 627N 양하(297, 가상0)·DJCT 0221W(진짜 누락 3 유지)·OBWH 2690W(284) 불변.
- ValidationBox 헤드리스 크롬 실렌더 9/9 PASS(가상E 줄·E확정·경고 소멸·쉬프팅 29·총 작업 316 + 회귀 2종).
- 수집기 GMmerge 실파일 재현(구판: dummy110·부족77 → 신판: dummy187·부족0·E확정187) + OBWH 2690W 회귀 구=신 동일.
- build.sh 성공(assets/index-a2xuI90E.js, CSS 해시 불변) + 번들 grep(V9.04-01·[A-Z]{3}[UJZ]·E확정 문자열).

**함정·참고.**
- 가상번호는 수집기(DUME)만이 아니라 **선사 플래너(CASP69)도 EDI 원문에 직접 넣는다** — EDI 원문에 있다고 실번호가 아니다.
- 시뮬 esbuild 번들 시 엔트리가 리포 밖이면 react가 다른 복사본으로 해석돼 hooks null 크래시 — alias로 리포 react 고정(V8.98-14 함정 재확인).
- 합본 xlsx의 F/E 컬럼이 공란일 수 있음(629S 287행 전부) — v2.17.11-17부터 엠티 출처는 E로 채워지지만, 앱 판정은 fe≠'F' 기준(공란 허용)으로 방어.


#### V9.04-02 (2026-07-18, 같은 날 추가) — 진단 경고(diagnostics.js)도 가상 인식

V9.04-01 배포 후 사용자 재보고: 항차 페이지 **🟡 주의 배너**에 여전히 "EDI 287대 중 리스트 매칭 100대 (187개 부족) — CASP0000001…"과 "리스트에 EDI 평택과 매칭 안되는 컨 187개"가 표시. 원인 = `src/diagnostics.js`(runDiagnostics, 주의 배너·음성 경고 담당)가 isVirtualCn 미적용 — 홈 카드(computeStats)·검증 카드(ValidationBox)와 별개 경로임.

수정: ①`list_short`(부족) — 기준을 EDI 실번호(realEdiCount = ptk − 가상)로, 부족 목록에서 가상 제외, 문구 "EDI 실번호 N대 중…" ②`list_extra`(리스트가 EDI에 없음) — 가상>0이면 fe≠'F' 리스트분을 **info `empty_confirmed`**("가상E 187자리 · E확정 187 — 실 100 + E확정 = 총 287")로 분리, 경고에서 제외.

검증(실데이터): 629S = 경고 0 + info 1(총 287) / DJCT 0221W 진짜 부족 3 유지 / OBWH 2690W 경고 0 — 전부 PASS. **교훈: 같은 수치(매칭·부족)를 계산하는 경로가 검수앱에 3곳(computeStats·ValidationBox·diagnostics) — 집계 규칙 수정 시 3곳 전부 점검할 것.**


#### V9.04-03 + 수집기 v2.17.11-17 (2026-07-19, 같은 사건 3차) — 실번호 EDI 역선정·되덮기 근절 + E확정 중복가산 방지

사용자 재보고: "어제 (실번호 LOAD EDI를) 받아서 등록했는데 수집기가 다르게 읽는다" — 수동 등록이 사이클마다 BAPLIE(가상 엠티)로 되덮임.

**원인 (실측).** `autoreg.py _newest_per_edi_kind`(v2.17.11-09, RZOR 갱신본 역선택 방지)가 같은 EDIFACT 종류 파일 중 **mtime 최신 1개만** 후보로 남김 → 629S에선 최신 BAPLIE GF23(13:48, 실812+가상187)이 카스피 실번호 완성본 'MCSN-629S LOAD EDI FILE.EDI'(09:14, 실999 — 수동 수신이라 _수집기록에 없음)를 탈락시켜 헬퍼의 '실번호 최다' 규칙까지 못 감. 헬퍼(V8.84-04) 자체는 전체 폴더를 주면 LOAD EDI를 정확히 선정함(시뮬 확인).

**수정.**
- 수집기 `autoreg.py` (v2.17.11-17): _newest_per_edi_kind — 최신본에 **가상번호(4번째 글자 U/J/Z 아님)가 있으면**, 실번호가 더 많은 구본도 후보로 남겨 헬퍼가 판정. 컨번호 토큰 카운트는 **카스피 내부 공백('MNBU 0678265') 허용** 정규식. RZOR 갱신본(가상 없음)은 기존 '최신만' 유지 — 합성 회귀 PASS. app_gui VERSION 2.17.11-17 (.py 수정이라 재시작 필요).
- 검수앱 V9.04-03: computeStats에 `realEPtk`(EDI 내 엠티 실번호) → **총 = 실 + max(0, E확정 − realEPtk)**. 실번호 EDI가 등록되면 E확정 187이 이미 실에 포함되므로 "총 474" 중복가산 방지, 표기 "E확정 187 ✓EDI반영". BAPLIE(가상만) 등록 시엔 기존 총287 유지.

**검증(실데이터).** 후보 필터 시뮬: 구판 = LOAD 탈락 / 신판 = GF23+LOAD 생존·GF22 이하 탈락, RZOR 합성 회귀 PASS. GMautoPayload 실행(헤드리스 크롬, 실파일): rawName = 'MCSN-629S LOAD EDI FILE.EDI', ediContainers 999(가상 0), ptk 287. computeStats: LOAD 등록 = 실287·매칭287·총287 / BAPLIE 회귀 = 실100·총287 / OBWH 회귀 PASS.

**함정.** ①수동으로 받은 EDI를 MAILBOX 항차 폴더에 넣어도 자동등록 후보에는 들어가지만(폴더 스캔), '같은 종류 최신만' 규칙에 걸릴 수 있었음 — 이번 수정으로 해소. ②카스피(PCTC) EDI는 컨번호 내부 공백 표기 — 정규식 카운트·grep 시 항상 공백 허용할 것(콘앱 V8.98-04 공백 교훈의 재출현). ③fp는 후보 names 기준이라 필터 변경만으로 재등록이 자동 트리거됨(상태파일 수동 조작 불필요).


#### V9.05 (2026-07-21) — SWAT 2607S 사건: 관리자 이름 보호 + 베이사전 쓰기 전(全) 경로 게이트 + 자동머지 승인제 + 출력물 사전출처 표기

**사건 (2026-07-21, SWAT 2607S 선적).** 아침 SWAT 카고플랜 출력물이 별첨(합 273)과 안 맞음 — 페어가 (04)05·(08)09…(28)29로 그려지고 베이 카운트 합이 62뿐. 조사 결과 **FB·로컬의 SWAT 매트릭스는 6/16 등록 후 줄곧 정상**(페어 (02)03·(06)07…(34)35, 짝수 02·06·…·38)이었고, 데이터 무단 수정도 없었음. 아침 출력물의 선체는 SWAT가 아니었음(뒷베이 92/94단 없음 = 제네릭 폴백 구조) → **사전 조회(매칭)가 정본을 못 찾고 폴백 구조로 그린 것**. 저녁에 사용자가 빌더 재저장(21:41~42, V7A2 키 생성 + SWAT 키 신원 보강)하자 매칭이 붙어 수정본 정상 (베이 카운트 합 = 별첨 = 273 재확인). 판별법: **별첨 합계 vs 베이 카운트 라벨 합계 대조** + 선체 단수(92/94단 유무) 확인.

**사용자 원칙 확정: "베이매트릭스는 관리자(김성일)와 관리자가 허락한 작업만 수정 가능. 앱이 스스로 수정 금지."**

**수정 4건 (V9.05).**
1. **관리자 이름 보호** (`src/adminGuard.js` 신설 + InspectorModal + StaffManagerModal): 김성일 선택은 신뢰 기기(최대 3대 — PC+폰2)만 무비번. 그 외 기기는 비밀번호(SHA-256+salt, FB `admin_guard` 노드, 평문 저장 없음) — 통과 시 그 세션(탭)만 허용, "기기 등록" 체크 시 신뢰 기기 등록(3대 한도). 최초 설정: 김성일 클릭 → 비번 설정 → 그 기기가 1호. 기기 목록·해제·비번 변경은 인원 관리 모달. 분실 시 FB에서 admin_guard 노드 삭제 = 초기화.
2. **베이사전 쓰기 전 경로 게이트** (`src/bayDictGuard.js` 신설): 기존 matrix_editors 게이트는 빌더 1곳뿐 — 실제 쓰기 경로는 6곳(빌더/BulkStowage/BulkAsc/StowageReview/VoyagePage .def/머지). 이제 `fbSaveShipBayDict`·`fbDeleteShipBayDict`·`addToUserBayDict`·`removeFromUserBayDict`·`mergeUserBayDictFrom`·`applyApprovedSync` 전부 저장 직전 게이트 통과 필수(권한자 명단 = matrix_editors, App.jsx가 `window.__gmMatrixEditors` 캐시, 미로딩 폴백 = 김성일만). 차단 시 alert 1회/10초 + console.
3. **자동 머지 승인제 전환** (App.jsx): M6.94.20 조용한 FB→로컬 자동 덮어쓰기 제거. 결함 2건 — ①타임스탬프 비교가 `Number(ISO문자열)`=NaN으로 무작위 동작(수리: `entryTimestamp()` = Number(updatedAt) || Date.parse(parsedAt)) ②알림·이력 없는 로컬 덮어쓰기. 이제 "공유 정본이 로컬 사본보다 최신"인 항목을 **탐지만** 하고, 관리자 화면 상단 배너에서 승인해야 `applyApprovedSync`로 반영. 오프라인 조회는 `gm_fb_baydict_cache` 폴백이 있어 자동 머지 없이도 동작.
4. **출력물 사전 출처 표기** (PrintableCargoPlanV2 헤더): `사전:SWAT·user·2026-06-16` 형식. user 정본이 아니면 빨간 `⚠비정본`, 미매칭 폴백이면 `⚠미매칭(폴백 구조)` — 이번 사건의 재발 판별 장치.

**검증.** Node ESM 시뮬 28/28 PASS (/tmp/sim/v905_sim.mjs — SWAT 실 타임스탬프 값으로 NaN 수리·게이트 차단/허용·승인 반영·회귀(로컬 최신 보존)·해시/신뢰기기). bash build.sh EXIT 0, 번들 grep: V9.05·베이사전 저장 차단·신뢰 기기·정본으로 갱신·비정본·admin_guard 전부 확인.

**함정·주의.**
- 이름 가드는 화면 수준 잠금 — 직원 오클릭·사칭 방지용. 개발도구 우회까지 막으려면 Firebase 보안 규칙 별도 작업 필요(미착수).
- 비권한자가 .def 업로드/일괄 등록을 시도하면 이제 저장이 조용히 실패하고 경고 1회 표시됨 — 정상 동작.
- InspectorModal 가드는 FB `admin_guard`를 읽어야 동작 — 완전 오프라인 첫 실행 시 "로딩 중" 안내 후 재시도 필요.
- 조회(매칭) 레이어가 정본을 못 찾는 근본 원인(항차 신원 vs 사전 키 매칭)은 이번에 표기(4번)로 가시화만 함 — ⚠비정본/⚠미매칭이 뜨면 빌더에서 해당 선박 열어 재저장(신원 보강)이 응급처치. 근본 수정은 차기 과제.


#### V9.05-01 (2026-07-21) — ISO403 사진 알람 평택분 필터 (통과 리퍼 오알람)

사용자 보고: SWAT 2607S 선적(평택 273대, 리퍼 0)인데 베이 탭 사진 배지 ⚠16. 원인 = BayPlan.jsx iso403Stats가 containers 전체(EDI 942대, 통과 포함)에서 풀 리퍼를 집계 — 평택 필터 부재(9.2-② 패턴 재발). 알람 16대 = 전부 VNSGN행 통과 리퍼.

수정: iso403Stats 필터에 평택 판정 추가(선적=_inList||POL평택, 양하=POD평택 — isPtk와 동일 규칙, TDZ로 지역 정의) + deps에 mode. 시뮬 5/5 PASS (SWAT 16→0, 필터 무 대조 16, DJCF 0148N 양하 53 유지, _inList 리퍼 포함, L5R1 회귀).

교훈: containers 전체를 도는 집계는 신설·기존 불문 평택 필터 유무를 먼저 본다 — XRAY·쉬프팅·사진 등 배지류가 특히 재발 지점.


#### V9.05-02 (2026-07-22) — 공유 사전 가져오기 게이트 해제 (V9.05 과잉 차단 수정) + 가져오기 후 새로고침 안내

사용자 보고: 개인 폰에서 베이사전 '공유사전 불러오기' 후 저장이 안 되고, 김성일로 검수원 선택(신뢰 기기/비번 통과)해야만 저장돼 카고플랜이 제대로 보임. 폰마다 관리자 로그인을 해줄 수 없음.

원인: V9.05 중앙 게이트가 쓰기 6경로 전부를 막으면서 **FB 정본→로컬 복사 경로 2곳**(`mergeUserBayDictFrom` 공유 사전 가져오기, `applyApprovedSync` 정본 승인 반영)까지 차단. 이 두 함수는 원본(FB)을 전혀 건드리지 않고 자기 기기 localStorage에 복사만 하는데도 차단됨 → 비권한자 폰은 로컬 사본이 안 생기고, 조회는 로컬 우선(§6.3)이라 카고플랜이 폴백/구본으로 틀리게 표시.

사용자 원칙 확정: "수정 권한은 안 주고, 관리자가 저장한 매트릭스 정보만 불러오게" — 즉 **원본 쓰기·로컬 직접 수정은 계속 권한자 전용, 정본 내려받기(읽기 성격)는 전원 허용.**

수정 2건 (`src/data/userBayDict.js`, `src/components/BayDictLibraryWidget.jsx`).
1. `mergeUserBayDictFrom`·`applyApprovedSync`에서 `gateBayDictWrite` 제거 — FB→로컬 복사는 모든 검수원 허용. `addToUserBayDict`·`removeFromUserBayDict`(로컬 직접 수정)와 `fbSaveShipBayDict`·`fbDeleteShipBayDict`(원본 쓰기) 게이트는 그대로 유지.
2. 라이브러리 위젯 '공유 사전 가져오기' 성공 시 "카고플랜에 반영하려면 새로고침이 필요합니다. 지금 새로고침할까요?" confirm 추가 — 확인 시 즉시 `location.reload()`. (카고플랜이 로컬 사본을 앞서 읽으므로 새로고침 전에는 반영 안 됨.)

검증. Node ESM 시뮬 6/6 PASS — ①비권한자 가져오기 성공 ②로컬 사본 생성 확인 ③비권한자 승인반영 성공 ④비권한자 직접 저장 차단 유지 ⑤비권한자 삭제 차단 유지 ⑥권한자(김성일) 직접 저장 회귀 정상. bash build.sh EXIT 0, 번들 grep: V9.05-02 1건 · 새로고침 안내 문구 1건 · 차단 경고 문구 유지 확인.

운영 안내(검수원 공지용). 각자 폰에서 ①베이사전 라이브러리 → '📥 공유 사전 가져오기' → 완료 창에서 '확인'으로 새로고침. ②카고플랜이 이상하게 보이면 먼저 공유 사전 가져오기 → 새로고침. 그래도 이상하면 관리자에게 보고 (⚠비정본/⚠미매칭 표기 확인).

함정·주의.
- 게이트 해제는 '내려받기' 2곳뿐 — 비권한자 .def 업로드/빌더 저장은 여전히 조용히 차단+경고 1회 (V9.05 정상 동작).
- 가져오기는 같은 키를 공유본으로 덮어씀 — 관리자 폰에서 로컬에만 있는 미업로드 확정본이 있으면 먼저 FB 저장 후 가져오기.


#### V9.05-03 (2026-07-23) — 버그픽스 3건: 섹션 삭제 voy 필드 잔존 · 콜사인 없는 EDI 흡수 모달 · 콘앱 파서 parseBAPLIE 통합

오래 걸려 있던 활성 버그 3건을 한 번에 처리. 세 건 모두 실데이터 시뮬 PASS 후 빌드.

**① 한쪽 섹션 삭제 시 voy 필드 잔존 (HomePage.jsx performDelete)**
원인: `fbDeleteSection`은 `voyages/{key}/{mode}`만 지우고 `info.voy_d/voy_l`은 남김 → 목록 표시(859행)·검색(599행 some)·수집기 재등록 판정이 유령 항차를 계속 봄.
수정: 양하 삭제 시 `fbUpdateVoyageInfo(key,{voy_d:null})`, 선적 삭제 시 `{voy_l:null}` 추가(RTDB update의 null=키 삭제). 전체 삭제(all)는 기존 그대로.
시뮬: performDelete 실코드 추출 실행 — 양하→delSection+voy_d 제거, 선적→delSection+voy_l 제거, 전체 삭제 회귀 없음 PASS.

**② 콜사인 없는 선박 흡수 모달 미작동 (VoyagePage.jsx V8.29 게이트)**
원인 실측 두 갈래: (a) DJCT 계열 EDI TDT 식별자가 '9891' 같은 숫자 코드 → parseBAPLIE r.callsign='' (실데이터 DJCT-0221W LOAD EDI FILE.EDI 확인). (b) 자동등록(수집기) 항차 info에는 callsign 자체가 저장 안 됨(autoreg.py info 구조 실측) → 등록 측이 항상 공란. 어느 쪽이든 `_csComparable=false` → 흡수 모달 없이 즉시 차단.
수정: 모달 트리거를 `_ptkLeg>0 && _csComparable` → `_ptkLeg>0`로 확대. 안전 근거 — 이 지점 도달 = 상단 선박 가드 통과(콜사인·진짜 선박명이 '비교 가능한데 다른' EDI는 이미 차단됨) = 불일치 증거 없음. 모달 문구에 식별 근거 표시(`_idBasis`): 콜사인 일치 / 선박명 대조 통과 / ⚠식별자 비교 불가—직접 확인. 옛 '비교불가+평택분 있음 즉시 차단' 분기는 모달로 대체(삭제).
시뮬: DJCT 실 TDT — 수정 전 모달 불가 재현, 수정 후 트리거 PASS. ATPR(콜사인 D5RR5 있음)은 기존 경로 유지.

**③ 콘앱 내부 약식 파서 → parseBAPLIE 단일 소스 통합 (cone.html + coneCargoPlan.entry.jsx)**
원인 실측: 콘앱 parseEDI는 EQD 상태(+5=Full/+4=Empty)를 읽지 않아(fem 변수를 만들고 사용 안 함) fe가 항상 공란 — ATPR 029_RE.EDI 실측 세그먼트로 재현. ISO도 약식.
수정: ⓐ cone-cargoplan.js 번들 진입점에 `window.ConeParse={parseBAPLIE,parseAscFile}` 노출. ⓑ cone.html parsePositions를 본체 파서 우선으로 — 번들 로드돼 있으면 parseBAPLIE/parseAscFile 사용(masterRowsAdapter가 콘앱 rows 형식으로 변환, kind 규칙은 내부 파서와 동일), 로드 전/실패(약신호)면 기존 내부 파서 폴백(회귀 없음). ⓒ 시작 2.5초 후 번들 조용히 선로드(버전 캐시 재사용, 실패 무해). ⓓ 1343행 EDI 업로드 경로도 parsePositions로 통일 → 숫자코드 BAPLIE(CASP)·IFCSUM(RIZHAO)·ASC도 그 경로에서 처리됨.
시뮬(실데이터): ATPR 029_RE 실측 세그먼트 — 내부 파서 fe 전부 공란(버그 재현) vs 본체 파서 Full/Empty·DG(UN1866)·20/40 사이즈 전부 정확 PASS. NBTD2614E.ASC 602행 — 내부/본체 rows·kind 동일(회귀 없음) PASS. jsdom 로드 스모크 — 스크립트 파스 에러 0.

**부수 정리**: README 제목이 V8.09-03에 멈춰 있던 불일치 수정 + build.sh에 README 버전 자동 동기화 추가(재발 방지). 검증 — bash build.sh EXIT 0, 번들 grep: APP_VERSION V9.05-03 1건 · "식별자 비교 불가" 1건 · voy_d:null 1건 · cone-cargoplan.js ConeParse 1건 · sw.js/__APPV/__CONEV/README 동기화 확인.

**함정·주의**
- ②로 인해 '항차 불일치+평택분 있음'은 이제 항상 모달이 뜬다 — 식별자 비교 불가 문구(⚠)가 뜨면 검수사가 EDI 선박명을 직접 확인하고 결정할 것.
- ③ 콘앱 fe는 번들 로드 후부터 정확(F/E) — 약신호로 번들이 안 내려오면 기존처럼 fe 공란(폴백). 콘 계산엔 영향 없고 표시 정보만 달라짐.
- 시뮬 환경 참고: 코워크 샌드박스에서 마운트 폴더는 파일 삭제가 금지라 build.sh의 rm이 실패함 — VM 내부($HOME/buildrepo)로 복사해 빌드하고 산출물만 되가져오는 방식 확립.


#### 수집기 v2.17.11-23 (2026-07-22) — 콜번호 항차 해석: 프루닝 전 원본으로 폴백 (ATPR 029 미수집 사건)

사건: "ATPR 029 선적 EDI 수정분"(평택신항플레너 16:26) 반복 미수집. 진단로그 실측 — 매 사이클 `건너뜀: ATPR 029 선적 — 지난/오류 항차(현재 ['2633E'])`.
원인(실데이터 확정): PNCT 선석배정이 ATPR을 **콜별 딴 행**으로 기재(028(2633E/-)·029(-/2634W)·030(2634E/-), state.json 실측 — KKLC007형 한 셀 병기가 아님). collect_week 프루닝이 `tpairs[c]=[min(_alive)]`로 가장 이른 한 쌍만 남김 → 028(2633E)만 잔존(E는 W/S 마감 프루닝 비대상). v2.17.11-20 콜번호 해석이 수축된 tpairs를 봐서 029 불일치 → maybe_typo → 첨부(2634W)도 현재(2633E)와 불일치라 구제 실패 → 드롭.
수정(외과 2곳, collector/auto.py): 프루닝 직전 `_pairs_full` 원본 보존 + 게이트에서 `_voy_by_call(tpairs…) or _voy_by_call(_pairs_full…)` 폴백.
검증: 실데이터 시뮬 PASS(029→2634W→수용·비현재, KKLC 007 회귀 없음) → 실기기 재시작 → 18:22:52 풀 사이클 실측 `콜번호 해석: ATPR 029→2634W 선적` + `수집: … 신규 EDI=O` + 파일 저장 확인(ATPR 029.edi/029_RE.EDI). 09:09 원본 029 메일도 같이 회수됨.
잔여 관찰: 분리 기재형에서 '현재 대상' 표시가 마감된 양하콜로 남는 문제(화면·경보 기준) — E-only 쌍의 E 마감 프루닝 확대는 v2.17.11-09 취지 검토 후 별도 판단. [7부 후보]
수정 전 백업: TallymanMailCollector_v2.17.11-02/_backup_v21711_23_prev/. 인계: _인계_v2.17.11-23.md.

#### V9.06 (2026-07-23) — 예정 항차 선등록 (수집기 v2.17.11-24 연동, 사용자 요청)

요청: PCTC·PNCT 선석배정표의 다가오는(예비) 기항을 메일 도착 전에 메일박스에 선기록하고
검수앱에도 '예정'으로 미리 등록 — 메일이 오면 그대로 저장·승격. 예정작업일자 함께 표기.

**수집기 v2.17.11-24** (`collector/autoreg.py` register_expected 신설, `auto.py` 연결)
- _terminal_pairs가 sched(기항별 eta/etd — v2.17.11-14부터 파싱만 되고 미사용이던 것)를 반환하도록 확장.
- 매 사이클(신착 게이트 이전) register_expected 실행:
  ① MAILBOX/{코드}/{항차}/_예정.txt — 예정작업일자(ETA~ETD)·터미널·콜번호 선기록. 메일 도착 시 기존
     흐름이 같은 폴더에 저장. 부수효과: 짝 폴더가 처음부터 존재해 '폴더 생성 타이밍' 오등록 여지 축소
     (7부의 2620E/2620W 사건 계열 완화 — 완전 해결 주장 아님).
  ② voyages/{코드}_{항차}: 없을 때만 autoStatus='expected' + planDate(예정작업일자) 가등록. 키 규약은
     _voy_key와 동일(양하 우선·짝 한 키). 기존 실등록 키는 절대 불변. expected 키는 planDate만 보강.
- 대상 안전망: 관할 작업대상(tpairs)∩선석배정 · etd 미경과 기항만 · 마감(DEP.TALLY) leg 제외.
- 실자료 도착 시 _register_voyage가 expected→collecting(수집)/confirmed(확정) 승격.
- 빈폴더 정리(_rm_empty_chain)는 _예정.txt 폴더를 건너뜀(메일 대기 중 보호).

**검수앱 V9.06** (`HomePage.jsx`)
- autoStatus='expected' 항차에 하늘색 배지 「📅 예정 · MM-DD HH:MM」(planDate) 표시.
  수집중(🤖) 배지는 expected 제외 조건 추가 — 예정↔수집중↔확정 3단계가 배지로 구분됨.

검증: 실코드 임포트 시뮬 7항목 PASS(마감·과거·비관할 제외/신규 예정+일자/기존 등록 불변/멱등/
일자 PATCH/승격 2경로/빈폴더 보호+기존 정리 유지) · py_compile OK · bash build.sh EXIT 0 ·
번들 grep V9.06=1·예정 배지 문자열 확인.

#### V9.06-01 (2026-07-23) — 홈 정렬: 작업(예정)일자 순 (planDate 폴백)

요청: 항차 목록을 작업(예정)일자 순으로. 기존 V9.01 근접 정렬은 PORT-MIS eta/etd만 봐서
PORT-MIS 레코드 없는 예정 항차가 '일정 미상'(④)으로 밀렸음.
수정(HomePage.jsx): _etaMs/_etdMs 계산에 planDate("ETA ~ ETD") 폴백 추가 — PORT-MIS가 있으면
그대로 우선, 없을 때만 planDate 파싱. 정렬 결과 = 작업중 → 예정(일자 가까운 순, PORT-MIS·planDate 혼합)
→ 지남 → 미상(등록 최신순). PNCT 계열 planDate '미상'건은 여전히 ④ — PNCT 일시 파싱이 되면 자동 개선.
검증: 실함수(parsePortMisDateTime) 시뮬 7행 순서 PASS + PM 우선 유지 확인, build EXIT 0.

또한 수집기 v2.17.11-25/-26 (같은 날): 자동등록 항차 빈 짝 배정표 보강(사용자 확답) +
예정 순회 기준 sched→tpairs 교정(PNCT 계열 누락 실측 구멍) — 상세는 수집기 _인계_v2.17.11-24.md.

#### V9.06-02 (2026-07-23) — 리퍼 리스트 온도 미반영 픽스 (冷箱清单 사건)

사용자 보고: 수집기가 TMPZ 2022E의 냉동 리스트(冷箱清单.xls — 리퍼 2대, -18C/-20C)를 받아놓고 온도를 앱에 안 알림.
원인(실측 2겹): ① 리스트 파서(parseListExcel) 온도 열 후보에 'Degre(e)' 없음 — 이 파일의 온도 헤더가 "Degre".
② 값이 "-18C"(단위 접미)라 정규화 regex가 못 받음. 합본(merge_helper)·자동등록(autoreg_helper)이 같은 파서
번들이라 어느 경로로도 온도 유실. (파일 자체는 무토큰 화이트리스트 "清单"으로 합본 입력에 정상 포함되고 있었음.)
수정: utils.js tmp 헤더 후보 +/^degre/·/℃|°c/, 값 정규화에 [cC]|℃|°C 접미 허용. 수집기 helper 번들 2종은
동일 regex 문자열 치환으로 패치(열 1건·정규화 2건씩 — 재빌드 없이 안전).
검증: 실파일 冷箱清单.xls을 실함수 parseListExcel로 시뮬 — TTNU8312071=-18, TTNU8311959=-20 추출 PASS.
재처리 유도: 해당 폴더 xls touch(수집기 지문 변경) → 다음 사이클 재합본·재업로드 → records 보수 머지가 tmp 빈칸 채움.

#### V9.06-03 (2026-07-23) — 카고플랜 긴급▲ · XRAY★ 동시 표기 (::after 충돌 제거)

사용자 질문(긴급∩XRAY 표기)에 코드 실사 — V9.03 주석은 "동시 표기 가능"이나 실제로는 XRAY ★와
긴급 ▲가 둘 다 CSS ::after 채널이라, 겹친 셀에서 뒤 규칙(▲)이 ★를 지우는 구현 버그.
수정(PrintableCargoPlanV2): ▲를 ::after → 실요소 <i class="cpv2-um">로 전환(셀 렌더 4곳 + CSS).
이제 셀 네 모서리 완전 공존 — 좌상 ◆쉬프팅(::before) · 우상 ★XRAY(::after) · 좌하 ▲긴급(실요소) ·
보라 테두리 수화물(box-shadow). 콘앱 카고플랜도 같은 번들(cone-cargoplan.js)이라 동일 적용.
리스트 화면은 원래 배지 나열이라 겹침 문제 없음(🔍 XRAY + ▲ 긴급 나란히).

같은 날 수집기 v2.17.11-29~-32: EBAY 항차 붙음 제목(-29) · 엑셀 수집 키워드에 container list/
RF/reefer/긴급/수화물(-30) · 첨부 엑셀형 긴급 리스트→json 병합+양하도 즉시 push(-31) ·
forecast.mode 기본값 항차 접미 기준(-32) — 상세는 수집기 _인계_v2.17.11-24.md.

---

### V9.07 (2026-07-25) — 선적 플랜 편집기(단독) 신설 · 베이상세 빈 칸 드롭 누락 확인

**발단.** "베이상세편집에서 위치를 바꾸려는데 꼭 임시창고에 넣어야 하나?"

**진단 (코드 확정 — ChiefBayEdit.jsx).**
- 격자 렌더 경로가 둘이다. 구 격자 `renderCell`(274행)은 빈 칸에 `onDragOver`+`onDrop`이 붙어 직접 이동이 되지만, 현 격자 `cellExtra`(305행)는 **빈 슬롯에 `return {}`** 이라 드롭 핸들러가 없다.
- 396~404행에서 `view.matrixRender`가 잡히면 무조건 BayBoxV2(현 격자) 경로 → **베이사전 등록 선박 = 실사용 전부**가 빈 칸 드롭 불가. 임시창고 경유가 유일해 보였던 원인.
- 찬 칸은 `onDrop`이 있으나 `placeAtCell`(218행)에 점유 검사가 없어 좌표만 덮어씀 → **같은 bay/row/tier 중복 2대**. 스왑 로직 부재.
- 다른 베이로 이동 시 임시창고 경유는 구조상 불가피(베이 탭 전환 시 드래그 단절) — 정상.

**결정 (사용자 확정 2026-07-25).**
- 확정 플랜 작업을 `ChiefBayEdit`로 하면 안 된다. 저장이 `fbSetActualPosition`(실체 위치)로 들어가 검수사 화면과 `loadingEdiExport` 실선적 BAPLIE까지 오염된다.
- 편집기를 **먼저 단독으로 만들어 테스트한 뒤** 검수앱에 붙인다.
- 대상은 **선적(loading) 전용**. 이동 가능 = 작업대상(평택 선적화물 + 쉬프팅), 통과 고정분은 잠금.
- 산출물 4종: 베이플랜 인쇄/PDF · 선적 EDI(BAPLIE) · 변경내역 JSON · 엑셀 변경표.
- 검수앱 부착 시 [확정] 버튼이 곧 검수앱 선적 플랜이 된다(PCTC/PNCT 송부 → 회신 재수신 흐름 대응). 저장 계층은 3단으로 분리: `planDraft`(초안) → 확정 시 `ediContainers` bay/row/tier 갱신 → `records.bay_actual`(실체)는 불가침. 최초 확정 시 `bay_edi0/row_edi0/tier_edi0`에 EDI 원본 백업 + 원본 복원 버튼.

**신설 파일.**
- `src/planEditCore.js` — 순수 로직(배치·스왑·임시창고·이동권한·검증·변경내역·BAPLIE 제자리 재작성). Firebase/DOM 무의존, Node 단독 시뮬 가능.
- `src/planedit.entry.jsx` · `_planedit.entry.html` · `vite.planedit.config.js` · `_inline.cjs` · `build_planedit.sh`
- 산출물 `planedit.html` — 단일 HTML 1.69MB, 외부 참조 0, Firebase 미포함(쓰기 물리적 불가).

**핵심 설계.**
- **파서 단일 소스 유지**: 콘앱 실패(별도 약식 파서 → Full/Empty·ISO 불일치)를 반복하지 않도록 `parseBAPLIE`·`getShipBayDictData`·`cargoPlanCore`를 검수앱 소스에서 그대로 번들.
- 페이지 구성은 `autoPairBays` 트리오 그대로: 홀수(e-1) 단독 박스 + `(짝수 e)홀수(e+1)` 합본 박스. 페어 박스 드롭 시 목적 베이는 컨 사이즈로 결정(40/45→짝수, 20→홀수).
- BAPLIE 내보내기는 **원문 `LOC+147`만 제자리 교체**. 나머지 세그먼트는 바이트 보존 → 선사 수신 호환 유지. 임시창고 컨은 블록 삭제.

**검증 (실데이터 PASS — DPRT-2609S LOAD EDI FILE.EDI, 836대).**

| 항목 | 결과 |
|---|---|
| 분류 | 이동가능 293 / 통과 고정 543 |
| 잠금 화물 이동 시도 30건 | 30건 전부 차단 |
| 잠금 칸으로 교환 시도 | 거부 |
| 무작위 스왑 300회 후 좌표중복 | **0건** |
| BAPLIE 재생성 → parseBAPLIE 왕복 | 796건 좌표 불일치 **0** |
| 무변경 시 원문 바이트 | 112,406 = 112,406 완전일치 |
| 브라우저 렌더(Chromium) | 베이 10페이지·28베이 전수, JS 에러 0 |
| 내보내기 4종 | BAPLIE·JSON·XLSX·인쇄PDF 전부 정상 |

**빌드 사고 기록 (재발 방지).** 단일 HTML 인라인 시 `html.replace(re, jsString)`을 쓰면 번들 안의 `"$&"` 문자열이 **매치 전체로 확장**되어 `<script>` 태그가 조기 종료된다. 반드시 함수형 치환 `replace(re, () => jsString)`을 쓸 것 (`_inline.cjs`에 주석 고정).

**미결 (다음 세션).**
1. `ChiefBayEdit.cellExtra` 빈 칸 드롭 핸들러 부여 + `placeAtCell` 점유 검사·스왑 — 편집기 검증 후 동일 규칙으로 이식.
2. 편집기 검수앱 부착: `planDraft` 계층 신설 + [확정] 버튼 + [EDI 원본 복원].
3. 쉬프팅 자동 식별은 현재 양하 EDI 추가 업로드 시에만 동작. 검수앱 부착 시 `computeShiftingMapCached`로 대체.
4. 베이사전 미등록 선박(PEGASUS PROTO 등)은 EDI 좌표 격자로 그림 — 신규 선박 등록 메뉴 완성 시 해소.

---

### V9.08 (2026-07-25) — 플랜 편집기 격자를 베이매트릭스 기준으로 교체 · 카고플랜 보기

**사용자 지적.** "베이가 틀립니다. 베이매트릭스를 이용해야 할듯합니다." / "빈 슬롯이 화면에 없습니다. 빈자리도 다 보여줘야 이동이 가능합니다. 맞게 옮겨졌는지 한눈에 보려면 카고플랜을 볼 수 있어야 합니다."

**원인 1 — 사전 조회 오염 (실데이터 확정).**
`extractShipInfo`가 DPRT-2609S EDI에서 `imo:'V7A5452'`(콜사인, `imoIsNumeric:false`)를 반환하는데 이것을 `getShipBayDictData(imo, ...)`의 imo 자리에 그대로 넘겨 조회가 null이 됐다. 같은 배를 `getShipBayDictData('', 'PEGASUS PROTO', {})`로 부르면 `name-fuzzy(DPRT)` → 28베이 정상 매칭된다.
→ **수정**: `imoIsNumeric`이 false면 imo를 비우고 선박명으로만 조회. 실패 시 **베이 번호 집합 일치**로 2차 판정(`matchShipByBays`), 그래도 실패하면 사전 36척 목록에서 **직접 선택**.

**원인 2 — 격자를 EDI 좌표로만 그림.**
컨이 실린 좌표만 그려서 실제 존재하는 빈 슬롯이 화면에 없었다. 빈 자리가 없으면 이동 자체가 불가능하다.
→ **수정**: 검수앱 카고플랜과 동일 경로로 교체. `enrichBayDef` → matrixBays prep → `autoPairBays` → `generatePdfBays` → `computeBayRenderData` → `BayBoxV2`. 페이지 구성도 트리오 그대로(홀수 단독 박스 + `(짝수)홀수` 합본 박스).

**핵심 수정 — 빈 슬롯 드롭 (검수앱 ChiefBayEdit 버그의 정답).**
`cellExtra`에서 **`cell.active && cell.rowLbl`이면 컨이 없어도** `onDragOver`/`onDrop`을 부여한다. 검수앱은 `if (!c) return {}` 이라 이 핸들러가 없어 빈 칸 이동이 막혀 있다 — 이식 시 이 규칙을 그대로 쓸 것.

**레이아웃 함정.** `CARGO_V2_CSS`는 전 계층이 `flex:1 1 0`이라 **높이가 고정된 부모** 안에서만 펼쳐진다. 높이 없이 넣으면 격자가 한 줄로 눌린다(1차 빌드 실사고). `.pe-sheet{height:calc(100vh - 190px)}` + `.pe-boxwrap{flex:1 1 0;min-height:0}`로 해결. BayDetailEdit의 `.bde-box{height:min(72vh,620px)}`와 같은 처방.

**카고플랜 보기.** `PrintableCargoPlanV2`에 편집 반영 좌표(`editedContainers`)를 그대로 넘겨 전체 28베이를 한 화면에서 확인. 임시창고 컨은 격자에서 제외. 사전 `name` 필드가 문서제목('CARGO DISCHARGING PLAN')인 엔트리가 있어 표시 선박명은 **EDI 선박명 우선**.

**검증 (PASS — DPRT 2609S, 836대).**

| 항목 | 결과 |
|---|---|
| 사전 매칭 | DPRT 28베이 (EDI 베이 28개와 완전일치) |
| 10페이지 전수 순회 컨 합계 | **836 = EDI 전량, 누락 0** |
| 잠금 셀 합계 | **543 = 통과 고정분과 정확히 일치** |
| 표시된 빈 슬롯 | **832개** (기존 0개) |
| 빈 슬롯 드롭 | 이동 성공 + 변경 셀 주황 표시 |
| 카고플랜 보기 | 28베이 1,857셀 + 별첨(POD별·선사별·규격별 F/E) 렌더 |
| JS 에러 | 0 |

---

### V9.09 (2026-07-25) — 격자 기하 고정 · 선택분 여러 대 동시 이동

**사용자 신고.** "이동을 하면 셀이 틀어지고, 컨테이너 3개를 동시에 옮기려고 3개를 드래그하니 전체가 움직입니다. 드래그된 부분만 움직여야 하는데."

**원인 1 — 격자 기하가 컨 위치에 종속 (실측 확정).**
`computeBayRenderData`는 `posMap`(현재 컨 좌표)으로 행·단을 넓힌다. 편집 중 posMap이 바뀌면 격자 자체가 다시 계산돼 전체가 밀린다.

```
원본 (06)07 deckTiers : 90,88,86,84,82
컨 1대를 92단으로 이동 → 92,90,88,86,84,82   ← 행이 하나 늘며 전체 이동
```

→ **수정**: `basePosMap = buildPosMap(containers)` — **편집 시작 시점 배치로 기하를 고정**하고, 편집 중에는 셀 내용(`cellMap`)만 갱신한다. 셀 내용은 `renderCellContent`/`cellExtra`가 전담하므로 기하만 고정하면 충분하다.
**검증**: 189셀의 시트 기준 상대좌표(left/top/width/height)를 이동 3회 전후로 문자열 비교 — **픽셀 단위 완전 동일**.

**원인 2 — 여러 대 선택해도 1대만 이동.**
`dropCell`이 `dataTransfer`의 컨 1개만 처리했다.
→ **수정**: `planEditCore.placeMany()` 신설 + UI에서 격자 **인덱스 델타**로 목적지 계산. 행 라벨은 물리 배열(큰짝수…02·00·01…큰홀수)이라 숫자 연산이 아니라 열 인덱스 차이로 옮겨야 한다.

**그룹 이동 규칙 (확정).**
- 선택 안의 컨을 끌면 선택 전체가, 선택 밖의 컨을 끌면 그 1대만 이동
- 상대 위치 유지 (기준 컨의 이동량만큼 전원 평행 이동)
- 데크↔홀드를 넘는 이동은 거부 (섹션마다 열 수가 달라 상대 위치가 깨진다)
- **원자적** — 잠금 화물 포함 / 격자 밖 이탈 / 슬롯 없음 / 선택 밖 컨이 목적 칸 점유 / 선택분끼리 충돌 중 하나라도 걸리면 **전부 취소**. 부분 이동으로 플랜이 깨지지 않게.
- 1대 이동은 종전대로 빈 칸이면 이동, 컨이 있으면 자리 맞교환

**부수 수정.** 섹션 열 라벨을 첫 행에서만 뽑아 `cols`가 통째로 `null`이 되던 문제(최상단 단이 전부 비어 있는 베이). 전 행을 인덱스별로 합쳐서 뽑도록 수정.

**검증 (PASS).**

| 항목 | 결과 |
|---|---|
| 이동 3회 전후 격자 189셀 픽셀 좌표 | 완전 동일 |
| 영역 선택 3개 | 정확히 3개 선택 (통과 고정분 제외) |
| 그룹 이동 | 선택 3대만 이동, 변경 +3, 상대 위치 유지 |
| 잠금 화물 포함 그룹 이동 | 거부 + 원상복구 |
| 선택 밖 컨이 목적 칸 점유 | 거부 + 원상복구 |
| 선택분끼리 같은 칸 | 거부 + 원상복구 |
| 그룹 이동 200회 반복 후 좌표중복 | 0건 |
| JS 에러 | 0 |

---

### V9.10 (2026-07-25) — 선택 개별 추가·취소 · 격자 흔들림 완전 제거

**사용자 신고 ①.** "드래그해서 2개를 지정했다가 하나를 취소하려면? 5개 지정했다가 2개만 취소하려면?"
→ 종전엔 [해제]로 전체 해제만 가능했다.

**추가 (선택 조작 4종).**
- **셀 클릭** = 선택 토글 (넣고 빼기). 드래그와 충돌 없음(클릭은 이동 없는 mousedown+mouseup)
- **Shift + 영역 드래그** = 기존 선택에 추가
- **Ctrl(⌘) + 영역 드래그** = 영역 안의 것을 선택에서 제외
- **선택 탭** 신설 — 선택된 컨 목록 + 항목별 `×` 개별 취소 + [전체 해제]
- 영역 선택 시 선택 탭으로 자동 전환. 베이를 옮겨도 선택이 유지된다(그룹 이동에서 타 베이 컨은 걸러짐).

**사용자 신고 ②.** "옮기고 나면 격자 크기가 같아지긴 하는데 움직일 때는 격자가 흔들립니다. 드래그된 상태에서도요."

**원인 3종 (전부 확정·수정).**

1. **상단 바 줄바꿈.** `.pe-head`/`.pe-stats`가 `flex-wrap:wrap`이라 상태 메시지·선택 표시 길이가 바뀔 때마다 줄바꿈되며 아래 격자가 통째로 밀렸다.
   → 세 바를 **높이 고정**(44/30/44px)·`nowrap`, 메시지는 말줄임 처리, 선박 select는 폭 250px 고정. 시트 높이도 `calc(100vh - 138px)`로 확정.

2. **dragover마다 React 리렌더.** 드래그 하이라이트를 `overKey` 상태로 두어 초당 수십 번 189셀이 리렌더됐다.
   → 하이라이트를 **DOM 클래스 직접 토글**(`e.currentTarget.classList.add('pe-over')`)로 변경. 드래그 중 리렌더 **0회**.

3. **★ CSS 클래스 이름 충돌 (핵심).** 선박 선택 `<select className="pe-sel">`와 셀 선택 표시 `.pe-sel`이 **같은 이름**이라, 셀을 선택하면 select용 `padding:4px 7px`가 그 셀에 먹었다. flex 행에서 한 셀이 103px→116px로 커지고 나머지가 1px씩 줄며 행 전체가 재분배 = 흔들림.
   → select 클래스를 `pe-shipsel`로 분리. **셀 상태 클래스는 크기에 영향을 주는 속성을 절대 갖지 않는다**(테두리 1px 통일·색만 변경, 선택/변경 표시는 inset box-shadow)는 규칙 고정.

**검증 (PASS).**

| 항목 | 결과 |
|---|---|
| 드래그 중 25칸 통과 — 서로 다른 격자 상태 | **1개** (= 변화 없음) |
| 10개 베이 전수: 선택 5 → 이동 1 전후 격자 픽셀 | **전 베이 동일, 흔들림 0건** |
| 셀 클릭 6회 → 선택 6대 → 2개 클릭 취소 → 4대 | 정상 |
| 선택 목록 개별 `×` | 4 → 3 정상 |
| JS 에러 | 0 |

**교훈 (검수앱 이식 시 주의).** 카고플랜 격자에 편집 오버레이를 얹을 때는 ①상태 클래스가 padding/border/margin 등 **크기 속성을 절대 갖지 않게** 하고, ②클래스 이름 접두사를 화면 요소별로 분리하며(`pe-shipsel` vs `pe-sel`), ③고빈도 이벤트(dragover)는 React 상태로 올리지 말 것.

---

### V9.11 (2026-07-25) — 드래그 하이라이트 잔존 수정

**사용자 신고.** "셀을 클릭하면 클릭한 셀만 보여야 하는데 다른 것까지 그림자로 보입니다. 한 개를 선택하든 여러 개를 선택하든 같습니다."

**원인 (확정).** V9.10에서 드래그 하이라이트(`pe-over`)를 React 상태에서 DOM 클래스 직접 조작으로 바꿨는데, **React는 자기 VDOM에 없던 클래스를 지우지 않는다.** 직접 붙인 클래스는 직접 걷어야 한다. 게다가 정리를 `dragleave`에 맡겼는데, `dragleave`는 셀 안의 자식 `<span>`(컨번호·규격)을 드나들 때도 발생해 짝이 맞지 않는다. 결과적으로 드래그가 지나간 칸마다 하이라이트가 남아 **선택과 구분이 안 됐다**.

**수정.**
- `clearOver()` — 시트 전체에서 `pe-over`를 걷는 전역 정리 함수. `dragover`에서 "이미 내가 하이라이트면 통과, 아니면 전체 정리 후 나만 표시" → **항상 정확히 한 칸**.
- `dragleave` 의존 제거. `drop` · `dragend` · 스테이지 이탈에서도 정리(드래그를 취소해도 안 남게).
- 색 분리: 드래그 중 지나가는 칸 = **주황**(임시), 선택된 칸 = **파랑 테두리 + 연파랑 배경**(지속). 역할이 다른 표시를 같은 색으로 쓰지 않는다.

**검증 (PASS).**

| 항목 | 결과 |
|---|---|
| 20칸 통과 중 동시 하이라이트 | **1개** |
| 드롭 후 잔존 하이라이트 | 0 |
| 드래그 취소(dragend만) 후 잔존 | 0 |
| 셀 1개 클릭 | 선택 표시 정확히 1개 |
| 셀 3개 클릭 | 선택 3개, 잔존 하이라이트 0 |
| JS 에러 | 0 |

**교훈 (검수앱 이식 시).** 성능을 위해 DOM 클래스를 직접 만질 때는 **정리 책임도 같이 진다**. React가 치워줄 거라 기대하면 안 된다. `dragleave`는 자식 요소 때문에 신뢰할 수 없으므로, `dragover` 진입 시 전역 정리 후 한 칸만 표시하는 방식이 안전하다.

---

### V9.12 (2026-07-25) — 카고플랜 페어 붕괴 · 인쇄 백지 수정 ★검수앱 공용파일 1건 수정

**사용자 신고.** "카고플랜이 짝꿍베이끼리 연결되지 않고 개별베이별로 되어 있습니다. `1 (2)3 … 13(14)15`가 맞는데 `1 2 … 13 14`로 나옵니다. 검수앱은 제대로 됩니다. 인쇄도 안 됩니다." (TEN JUPITER 26352W)

**원인 ① — 사전 오매칭 (사용자 화면 캡처에서 확정).** 헤더의 `사전:LYTJ-v2-fuzzy`가 결정적 단서. 같은 배가 **두 코드로 중복 등록**돼 있고 선박명이 둘 다 `TEN JUPITER`라 name-fuzzy가 아무거나 잡는다.

| 코드 | v2 baysSummary | v5 matrixBays | 유효 페어 |
|---|---|---|---|
| TNJP | 19베이 (3·7 있음) | **없음** | 2 |
| LYTJ | 18베이 (3·7 없음) | **25베이** | **8** |

**원인 ② — v5 매트릭스를 불완전한 v2 목록으로 걸러냄 (실원인).** matrixBays 준비 단계의 V7.00 보호 규칙이

```js
if (raw.length > 0 && baysSummary.length > 0) {
  const allowed = new Set(baysSummary.map(s => Number(s.bayNo)));
  bays = raw.filter(b => allowed.has(Number(b.bayNum)));   // ← 홀수 3·7·11·15… 전멸
}
```

원래 의도는 **"사용자가 직접 고친 베이사전이 정답"**(유령 베이 제거)인데 **자동추출 사전(v2/v5/fuzzy)에까지** 적용되고 있었다. LYTJ는 v2가 v5보다 불완전해서 v5의 홀수 베이가 전부 잘리고 트리오가 0이 된다.

**수정.**
- `source === 'user'`일 때만 v2 목록으로 거른다. 자동 사전이면 **v5 ∪ v2 합집합**.
- 적용처 2곳: 단독 편집기 `planedit.entry.jsx`, **그리고 검수앱 공용 `src/components/PrintableCargoPlanV2.jsx`**.
  → 검수앱은 Firebase user 사전을 쓰므로 `source==='user'` 경로를 타 **동작 불변**. 자동 사전일 때만 개선.
- 사전 자동선정을 `pickBestDict()`로 교체: EDI 베이를 모두 담는 사전 중 **트리오 수 → 베이 수** 순 최적 선택(선박명 일치 우선). 선박 드롭다운에 `(N베이 · 페어 M)` 표시.

**원인 ③ — 인쇄 백지.** `CARGO_V2_CSS`의 `body > *:not(.cpv2-overlay):not(.bd-print-modal){display:none !important}` 때문에, V9.08에서 이 CSS를 편집기에 넣은 뒤로 인쇄 시 `#root`가 통째로 숨겨졌다.
→ `@media print{ body > #root{display:block !important} body.pe-planopen > #root{display:none !important} }` + 카고플랜 열림을 body 클래스로 표시.

**검증 (PASS).**

| 항목 | TEN JUPITER 26352W | DPRT 2609S (회귀) |
|---|---|---|
| 선정 사전 | LYTJ (페어 8) | DPRT (페어 9) |
| 편집 베이 탭 | `01·(02)03 05·(06)07 09·(10)11 13·(14)15 17·(18)19 21·(22)23 25·(26)27 29·(30)31 33` | 정상 |
| 카고플랜 페어 표기 | 1개 → **9개** | 10개 유지 |
| 카고플랜 중복 베이 | 0 | 0 |
| 베이플랜·카고플랜 인쇄 PDF | 정상 출력 | 정상 |
| JS 에러 | 0 | 0 |

**교훈.** ①사용자 화면 캡처의 진단 문자열(`사전:LYTJ-v2-fuzzy`)이 로그보다 빠른 단서였다. ②"사용자 정의 보호" 규칙은 **적용 범위를 좁혀야 한다** — 자동 데이터에까지 적용되면 보호가 아니라 손실이다. ③단독본은 Firebase user 사전을 못 읽으므로 번들 사전 중 **가장 완전한 것을 스스로 고르는 로직**이 필수다.

---

### V9.13 (2026-07-25) — 인쇄 배경색 강제 출력 (브라우저별 차이 대응)

**사용자 질문.** "이게 크롬이랑 엣지랑 틀리게 보이나요?"

**사실 관계.** 크롬·엣지 모두 Chromium/Blink 엔진이므로 **같은 버전·같은 창 폭이면 화면 렌더링은 동일**하다. 실측으로 확인된 "달라 보이는" 요인은 브라우저 자체가 아니라 아래 셋이다.

| 요인 | 영향 | 대응 |
|---|---|---|
| **인쇄 '배경 그래픽' 기본값** | 꺼져 있으면 통과 고정분 회색·선택 파랑이 전부 날아가 흑백 격자로 인쇄됨 | **수정** — `.pe-sheet, .pe-sheet *`에 `print-color-adjust:exact` 강제 |
| **창 폭** (엣지 Copilot 사이드바 등) | 셀 글자가 `clamp(7px,0.68vw,10px)` — 1600px→10px, 1180px→8.02px. 셀 폭 103px→81px | 의도된 반응형. 두 브라우저를 같은 폭으로 놓고 비교할 것 |
| **브라우저별 확대/축소 기억값** | 사이트별 zoom을 각자 저장 | Ctrl+0으로 100% 맞춤 |

**실측 (동일 빌드, 폭만 변경).**

```
1600px : 폰트 10px    · 셀 103×36 · 잠금배경 rgb(203,213,225) · 셀 189개
1180px : 폰트 8.02px  · 셀  81×36 · 잠금배경 rgb(203,213,225) · 셀 189개
인쇄모드 두 경우 모두 print-color-adjust = exact, 배경색 유지 확인
```

격자 구조(셀 189개·행 수)는 폭과 무관하게 동일 — 레이아웃이 깨지는 것은 아니다.

**참고.** 카고플랜(`CARGO_V2_CSS`)에는 이미 `print-color-adjust:exact`가 있었고, 편집 화면(베이플랜) 인쇄 경로에만 빠져 있었다. 검수앱에 이식할 때 **편집 오버레이 인쇄 경로에도 반드시 넣을 것.**

---

### V9.14 (2026-07-25) — 베이사전 JSON 불러오기 (⚠비정본 해소)

**사용자 질문.** "카고플랜이 제대로 보이는데 왜 `사전:LYTJ·v2-fuzzy ⚠비정본`으로 나오죠? 선박명을 LYTJ에서 TNJP로 바꾼 지 한참 됐는데."

**원인 (구조적).** `⚠비정본` = `source !== 'user'`, 즉 **사용자가 직접 고친 사전이 아니라는 표시**다. 성일님이 고치신 TNJP는

- Firebase `ship_bay_dict_v3/TNJP`
- 브라우저 localStorage `master_user_bay_dict_v1`

두 곳에 있는데, 단독 편집기는 **설계상 Firebase에 연결하지 않고**, 파일로 열리므로(file://) 검수앱의 localStorage도 공유하지 않는다. 따라서 번들 사전(`shipBayDict_v2.js` 등, 저장소 스냅샷)만 보이고 거기엔 아직 옛 LYTJ 엔트리가 남아 있다. 격자가 맞았던 것은 V9.12에서 LYTJ의 완전한 v5 매트릭스를 살렸기 때문이지 사전이 정본이어서가 아니다.

**해결 — 검수앱 내보내기 파일을 그대로 받는다.**
검수앱 **[선박목록]** 화면에 이미 내보내기가 있다 (`ShipList.jsx handleExport` → `{코드}_baydict_{날짜}.json`, `toTallymanFormat` 형식 `{imo,code,name,callsign,bayDef:{baysSummary}}`). 이 파일을 단독본 로드 화면 **④번 칸** 또는 상단 **[📖 사전]** 버튼으로 넣으면

- 이 페이지의 localStorage(`master_user_bay_dict_v1`)에 `source:'user'`, `_userOwned:true`로 등록
- `getShipBayDictData`가 user 경로로 잡아 **★정본** 표시, 카고플랜 헤더도 `사전:TNJP·user·<날짜>`
- 선박 드롭다운이 **정본 / 번들(비정본)** 두 그룹으로 분리

형식은 단일 entry·배열·`{코드:entry}` 맵 모두 허용한다. `addToUserBayDict()`는 관리자 게이트(`gateBayDictWrite`)를 타므로 단독본에서는 저장소에 **직접 쓴다**.

**검증 (PASS — TEN JUPITER 26352W).**

| 항목 | 사전 불러오기 전 | 불러온 뒤 |
|---|---|---|
| 배지 | ✓ 번들 LYTJ · 18베이 | **★정본 TNJP · 25베이** |
| 카고플랜 헤더 | `사전:LYTJ·v2-fuzzy ⚠비정본` | **`사전:TNJP·user·2026-07-25`** |
| 편집 베이 탭 | 정상 | 정상 (동일) |
| 카고플랜 페어 | 8개 | 8개 (동일) |
| JS 에러 | 0 | 0 |

**운영 메모.** 단독 편집기를 쓸 때는 **검수앱 [선박목록]에서 해당 선박 JSON을 한 번 내보내 같이 넣는 것**을 기본 절차로 삼는다. 그래야 격자가 성일님이 확정한 정본과 완전히 일치한다. 배가 바뀔 때마다 한 번씩만 하면 되고, 같은 브라우저에서는 다음에 열 때도 유지된다(localStorage).

**배포 메모.** 이번 세션부터 산출물 파일명에 버전을 넣는다 (`planedit_V9.14.html`). 같은 이름으로 계속 보내 다운로드 폴더에 `planedit(1).html`… 형태로 쌓이면서 옛 버전을 열어 "고쳤는데 안 고쳐졌다"고 오인하는 사고가 실제로 발생했다.

---

### V9.07 (2026-07-25) — 베이상세 편집 전면 개편 + 선적 확정 플랜 편집 검수앱 내장

**사용자 지시.** "검수앱에 있는 베이상세 편집 방법을 지금의 선적플랜 편집기 형식대로 바꿔주시고, 그 옆에 지금 만든 컨펌용 선적플랜 편집기를 넣어주세요."

**구조 — 편집기 1개, 저장 대상 2개.**
단독본에서 검증된 조작 방식을 `BayGridEditor.jsx`(신설, 공용)로 뽑고, 저장 경로만 다르게 두 화면이 공유한다.

```
BayGridEditor  ──┬── ChiefBayEdit     → fbSetActualPosition / fbBatchMoveToStorage  (실체위치)
                 └── LoadingPlanEdit  → planDraft → [확정] → ediContainers          (선적 플랜)
                 (공통 로직 planEditCore.js)
```

**① ChiefBayEdit 전면 개편 (파일 교체).**
종전 자체 격자(cbe-cell) + 부분 BayBoxV2 혼용을 버리고 BayGridEditor로 교체. 이로써 아래 미해결 버그가 해소된다.
- **빈 슬롯 이동 불가** — `cellExtra`의 `if(!c) return {}`로 빈 칸에 드롭 핸들러가 없던 문제 (V9.12 진단분)
- **좌표 중복** — `placeAtCell`에 점유 검사가 없어 같은 bay/row/tier에 컨 2대가 배치되던 문제 → **자리 맞교환**으로 대체
- 추가: 여러 대 동시 이동(상대 위치 유지·원자적), 셀 클릭 토글 선택, 격자 기하 고정, 드래그 하이라이트 1칸 보장, 인쇄 배경 강제
- **저장 경로는 불변** — `records.bay_actual` 의미와 검수사 화면 반영 방식은 종전 그대로.

**② LoadingPlanEdit 신설 (선적 확정 플랜).** 수석 대시보드 [베이상세 편집] **바로 아래** 보라색 버튼군.

| 계층 | 경로 | 이 화면의 동작 |
|---|---|---|
| 초안 | `voyages/{key}/loading/planDraft` | **[초안 저장]** — 검수사 화면 영향 0 |
| 계획 | `loading/ediContainers` bay/row/tier | **[확정]** 시에만 갱신 = 검수앱 선적 플랜 |
| 실체 | `loading/records` bay_actual | **불가침** |

- 확정 시 최초 1회 `bay_edi0/row_edi0/tier_edi0`에 EDI 원본 백업 → **[↩ 원본 복원]** 가능
- `planHistory/{ts}`에 일시·검수원·건수·전후 좌표 기록
- 창고로 뺀 컨은 계획 위치를 비우고 `plan_unassigned=true`
- 이동 가능 = 평택 선적분(리스트 등록 또는 POL 평택) + 쉬프팅(`computeShiftingMapCached`). 통과 고정분 잠금
- **[🗺 카고플랜 보기]** — 편집 반영 좌표로 전체 베이 확인 · JSON/엑셀 변경표 내보내기

**신설·수정 파일.**
- 신설 `src/components/BayGridEditor.jsx`, `src/components/LoadingPlanEdit.jsx`, `src/planEditCore.js`
- 교체 `src/components/ChiefBayEdit.jsx`
- 수정 `src/firebase.js` (fbSavePlanDraft/fbClearPlanDraft/fbCommitPlan/fbRestorePlanFromEdi), `src/pages/ChiefDashboard.jsx`, `src/components/PrintableCargoPlanV2.jsx`(v5∪v2)

**검증 (실데이터 PASS — DPRT 2609S 836대).**

| 항목 | 결과 |
|---|---|
| 실체편집 분류 | 이동가능 293 / 통과화물 543 |
| 저장 payload 분기 | 창고/좌표 정상 분리, 좌표 형식 2자리 정합 |
| 확정 플랜 스왑 199회 + 창고 15건 | 좌표중복 **0** |
| 확정 반영 후 | 배치 821 / 미배정 15 / 좌표중복 **0** |
| 원본 복원 왕복 | 일치 |
| UI(하네스): 베이 탭 | 10페이지 `01·(02)03 … 38` |
| UI: 빈 슬롯 | 95개 표시·이동 성공 |
| UI: 맞교환 / 잠금 draggable=false / 클릭 토글 4→2 | 전부 정상 |
| UI: 격자 픽셀 고정 | PASS |
| UI: 저장 호출 변경건수 | 3건 정확 |
| 빌드 | `bash build.sh` 성공 · APP_VERSION V9.07 번들 2건 |
| 번들 grep | 선적 확정 플랜 2 · planDraft 2 · fbCommitPlan 1 · bay_edi0 1 · 자리 맞교환 1 |
| JS 에러 | 0 |

**주의 (운영).** [확정]은 되돌릴 수 있지만(원본 복원) 검수사 화면에 즉시 반영된다. 일항사 협의 중에는 **[초안 저장]**만 쓰고, 합의가 끝난 뒤 [확정]을 누른다.

---

#### V9.07 실행 검증 (Claude 직접 구동, 사용자 지시 "직접 해보세요")

빌드본을 로컬 서버로 띄워 실제 부팅을 확인하고(V9.07 배지 정상), Firebase가 샌드박스에서 차단되므로 **firebase 모듈을 스텁으로 alias 교체**한 하네스로 두 화면을 끝까지 몰아봤다. 호출 payload는 `window.__fb`에 기록해 검증.

**① 베이상세 편집**

```
배지     🖐 베이상세 편집 | PEGASUS PROTO · 선적 | ⚠비정본 DPRT · 28베이
베이 탭  01·(02)03 05·(06)07 … 33·(34)35 38   (10페이지)
05·(06)07  컨 94 · 빈 슬롯 95 · 잠금 66
빈칸 이동   SKLU2319787 → 10열 88단 이동
맞교환      SKLU2319787 ↔ BEAU2430245 자리 맞교환
잠금칸 교환 거부 — "대상 칸 SEKU1379947 통과 고정분 — 교환 불가"
임시창고    1대 보관
저장 →  fbBatchMoveToStorage(TEST_2609S, loading, ["BEAU2430245"], 검증검수원)
        fbSetActualPosition(TEST_2609S, loading, SKLU2319787, …, 검증검수원)
        알림 "저장 완료 — 2건 반영. 검수사 화면에 표시됩니다."
JS 에러 0
```

**② 선적 확정 플랜**

```
헤더     🗺 카고플랜 | ✅ 확정 | 🖨 | ↩ | 초안 저장 | ×
영역 선택 3대 → 그룹 이동 "선택 3대 동시 이동 (상대 위치 유지)"
초안 저장 → fbSavePlanDraft, payload 3건
확정      → fbCommitPlan, payload 3건 · 검수원 전달 확인
            샘플 BEAU2355727 → {bay:"05",row:"03",tier:"86"}
            알림 "확정 완료 — 3건 반영 (미배정 0건)"
호출 순서  fbSavePlanDraft → fbCommitPlan  (실체위치 API 호출 0건 = 실선적 불가침 확인)
카고플랜   19박스 · 페어 9개 (01·(02)03 … (26)27)
JS 에러 0
```

**실행 중 발견해 고친 결함 1건.** 확정 플랜 헤더에 버튼을 7개 넣었더니 **가로로 넘쳐 클릭이 막혔다**(`.bge-head`는 높이 고정을 위해 `nowrap`). → 헤더는 [카고플랜]·[확정] 2개만 두고 JSON·엑셀·원본 복원은 **옆 패널 하단**으로 이동. 인쇄·되돌리기는 아이콘만 표시.

**알아둘 동작.** 저장/확정 후 편집기가 새 기준으로 재시작한다(`key` 변경). 실제 앱에서는 Firebase 구독이 갱신된 항차를 밀어주므로 곧바로 새 기준이 잡히지만, 네트워크가 느리면 잠깐 이전 기준으로 보일 수 있다. 저장은 절대 좌표를 쓰므로 데이터가 어긋나지는 않는다.


---

### V9.07-01 (2026-07-26) — 컨펌용 플랜편집 개칭 + 카고플랜 z-index + 인쇄 대상 오조준

배포 당일 사용자 실사용 신고 3건. 커밋 `5ca9e05`, 17파일.

**① 개칭.** "선적 확정 플랜" → **"컨펌용 플랜편집"** (사용자 확정 — 화면 표시 전부).
바뀐 곳: `ChiefDashboard.jsx` 대시보드 섹션 라벨 · `LoadingPlanEdit.jsx` 편집기 상단 제목 · 옆 패널 안내문 · `planedit.entry.jsx`(단독본 시트 제목).
`[확정]` 버튼 명칭은 그대로 둔다 — 동작 이름이지 화면 이름이 아니다.

**② 카고플랜이 안 뜨던 원인 = z-index 역전.**
`.bge-overlay` = `z-index:10000`, `.cpv2-overlay` = `z-index:50`. 둘 다 `document.body` 포털이라
[🗺 카고플랜]을 눌러도 카고플랜이 편집기 **아래에 깔려** 화면이 그대로였다.
→ `BGE_CSS`에 `.cpv2-overlay{z-index:10050 !important}` 추가. 이 CSS는 편집기가 떠 있는 동안에만
주입되므로 다른 화면의 쌓임 순서에는 영향이 없다.

**③ 인쇄가 뒤 화면을 잡던 원인 = CSS 선택자 우선순위 (본질적 함정).**
`BayGridEditor`는 셀 레이아웃을 위해 `CARGO_V2_CSS`를 함께 주입한다. 그 안에 인쇄 규칙이 있다.

```
body > *:not(.cpv2-overlay):not(.bd-print-modal){display:none !important}   (0,2,1)
```

이 규칙이 편집기 오버레이(=body 직속 포털)까지 지운다. V9.07의 대응은

```
body > #root{display:block !important}                                      (1,0,1)
```

였는데, id가 들어가 우선순위가 더 높아 **본화면만 되살아나** 그게 인쇄됐다. 편집기 시트는 애초에 안 찍혔다.

→ 클래스를 겹쳐 우선순위를 결정적으로 확보한다. 소스 주입 순서에 기대지 않는다.

```
body.bge-open.bge-open > *:not(.bge-overlay):not(.cpv2-overlay){display:none !important}  (0,4,1)
body.bge-open.bge-open > .bge-overlay{display:flex !important}                            (0,3,1)
body.bge-planopen.bge-planopen > .bge-overlay{display:none !important}                    (0,3,1)
```

`bge-open`은 `BayGridEditor`가 마운트 동안 body에 붙인다(언마운트 시 제거).
`bge-planopen`은 종전대로 `LoadingPlanEdit`가 카고플랜 열림 동안 붙인다.

**실측 검증 (추론 아님).** 두 파일의 `@media print` 블록에서 display를 건드리는 규칙만 추출해
실제 Chrome에서 `getComputedStyle`로 캐스케이드를 측정했다.

| 케이스 | `#root` | `.bge-overlay` | `.cpv2-overlay` |
|---|---|---|---|
| 수정 전 · 편집기만 | **block** | **none** | block |
| 수정 후 · 편집기만 | none | **flex** | — |
| 수정 후 · 편집기+카고플랜 | none | none | **block** |

수정 전 첫 줄이 사용자 신고("인쇄하면 다른 화면이 잡힘")를 그대로 재현한다.

**확립 원칙 (다른 Claude 주의).** `CARGO_V2_CSS`를 주입하는 화면은 그 인쇄 규칙
`body > *:not(.cpv2-overlay)`를 함께 떠안는다. 새 전체화면 오버레이를 body 포털로 만들 때는
**인쇄 대상 규칙을 반드시 같이 설계한다.** id 선택자로 되받으면 (1,0,1)이라 항상 이겨버려
엉뚱한 화면이 인쇄된다 — 클래스 중복으로 (0,3,1) 이상을 쓸 것.

**검증.** 시뮬 PASS(위 표) · `bash build.sh` 성공 · 번들 `index-BBm6QcF1.js` grep
(컨펌용 플랜편집 3 / bge-open 6 / z-index:10050 1 / V9.07-01 1 / 선적 확정 플랜 0) ·
push `d1b4a0b..5ca9e05` · blob 해시 17파일 전수 일치 · 라이브 sw.js `V9.07-01` 확인.
누적 ZIP = `Downloads\Tallyman_Master_V9.07-01.zip` (+`C:\TALLYTEST` 사본).

---

### V9.07-02 (2026-07-26) — 베이 편집 시트 분리 (페어 베이 한 장씩)

사용자 신고. `1 (2) 3`을 누르면 **01 한 장 · (02)03 한 장 = 두 장**이 나와야 하는데
한 시트에 합쳐져 있어 베이 높낮이가 눌려 보이고, 인쇄도 베이별로 따로 못 뽑았다. 커밋 `5d198a1`.

**원인.** `BayGridEditor`가 페이지 하나당 시트(`.bge-sheet`) 하나만 만들고,
그 안에서 박스들이 `.bge-boxwrap{flex:1 1 0}`으로 **높이를 나눠 가졌다.**
페어 페이지는 박스가 2개라 각자 절반만 차지한다.

**수정.** 시트를 **박스마다 하나씩** 만든다. `boxes.map`이 `.bge-sheet`째로 반복하도록 바꾸고
시트 제목도 페이지 라벨(`page.label`)이 아니라 박스 라벨(`b.label`)을 쓴다.
`.bge-sheet`에 `margin:0 auto 12px`(마지막은 0)를 줘 세로로 쌓이게 하고,
인쇄에서는 `break-after:page` + `page-break-after:always`(마지막 시트만 `auto`)로 한 장씩 끊는다.

부수 수정 하나. 시트가 쌓이며 `.bge-stage`가 기본으로 스크롤되므로,
절대배치 러버밴드가 스크롤량만큼 어긋난다 → `stageDown`에서 `scrollLeft/scrollTop`을 더한다.
(`stageUp`의 선택 판정은 뷰포트 좌표를 쓰므로 원래 스크롤 안전했다.)

**실측 검증.** 실제 Chrome에서 수정 전/후 DOM을 만들어 `.bge-boxwrap` 높이를 쟀다.

| | 시트 수 | 박스 높이 | 스테이지 스크롤 |
|---|---|---|---|
| 수정 전 | 1 | 244px · 244px | 없음 |
| 수정 후 | 2 | **497px · 497px** | 있음 |

인쇄 `breakAfter` 계산값 = 첫 시트 `page` · 마지막 시트 `auto`.

**적용 범위.** `BayGridEditor` 한 곳만 고쳤고 **베이상세 편집과 컨펌용 플랜편집 양쪽에 동시 반영**된다.
단독 편집기(`planedit.entry.jsx`)는 별도 번들이라 이번 판에 포함하지 않았다.

**검증.** 시뮬 PASS(위 표) · `bash build.sh` 성공 · 번들 `index-ByUL3x0Q.js` grep
(break-after:page 1 / page-break-after:always 1 / margin:0 auto 12px 1 / V9.07-02 1) ·
push `47e7fb6..5d198a1` · blob 해시 14파일 전수 일치 · 라이브 sw.js `V9.07-02` 확인.
누적 ZIP = `Downloads\Tallyman_Master_V9.07-02.zip`.

---

### V9.07-03 (2026-07-26) — 편집기에 통과화물 표시 (raw EDI 기준 전체 적부도)

사용자 신고. 베이상세 편집·컨펌용 플랜편집의 **격자와 카고플랜 양쪽에서 다른 항에서 실린 화물이 안 보인다.**
그래서 옮겨도 되는 자리인지 분간이 안 된다. 커밋 `db708c3`.

**원인.** 두 편집기가 `sec.ediContainers`를 봤는데 **거기엔 통과화물이 없다.**
이건 이미 확인돼 저장소 주석에도 적혀 있던 사실이다 (`PrintHubModal.jsx:32,48` — V8.98-02/01).
인쇄 허브는 `ediMapFromRaw(sec)`로 raw EDI 전문을 파싱해 이미 해결해 뒀는데 편집기에만 그 처리가 빠져 있었다.

**수정.** 인쇄 허브 안에 있던 병합 로직을 `utils.fullEdiMapOf(sec)`로 **승격**하고
`ChiefBayEdit` · `LoadingPlanEdit` · `PrintHubModal` 세 곳이 같은 함수를 쓰게 했다.
(콘앱 파서 불일치와 같은 종류의 분기를 미리 없앤 것 — 단일 소스 원칙.)

```
raw 있으면 raw가 단일 진실 → 저장본 필드는 병합해 보존
raw에 없는 실번호 형식(4영문+7숫자) 저장본 키는 구판/가상 잔재로 제외 (V8.98-04 판정 그대로)
raw 없으면 종전대로 ediContainers
```

곁들여 `ChiefBayEdit`의 `_placed` 판정을 보정했다. 통과화물은 이미 실려 있으므로
선적 모드의 미배치 표식(`·`)을 달지 않는다 — 평택 선적분에만 적용한다.

**표시 규칙 (사용자 확정).** 통과화물은 **회색 잠금** 그대로. 별도 표식은 붙이지 않는다.
`lockedCns` 판정이 이미 통과분을 잠그므로 보이기만 하고 움직이지 않는다.

**실데이터 시뮬 PASS.** 수집기 저장본(=평택분만)을 재현한 뒤 `fullEdiMapOf`를 태웠다.

| 선박 | raw 전체 | 수정 전 편집기가 보던 수 | 수정 후 | 새로 보이는 통과화물 | 이동가능으로 새는 것 | 숨어 있던 점유 슬롯 |
|---|---|---|---|---|---|---|
| KSKM 2610S | 279 | **54** | 279 | 225 | 0 | **225** |
| XTPG 0522W | 240 | **175** | 240 | 65 | 0 | **65** |

"숨어 있던 점유 슬롯"이 곧 사용자가 말한 "이동해도 되는 곳인지 분간이 안 되는" 자리 수다.

**시뮬 중 잡은 함정.** 처음에 `TNJP_26349W_ACTUAL_BAPLIE.edi` · `SWDN_2608S_ACTUAL_BAPLIE.edi`로
재현하려 했으나 두 파일은 POL이 전부 KRPTK인 **평택분 추출본**이라 통과화물이 0이었다.
통과화물이 실제로 든 전선 BAPLIE(`KSKM-2610S LOAD EDI FILE.EDI`, `RE) XTPG-0522W LOAD EDI FILE.EDI`)로
바꿔서야 재현됐다. **EDI 파일이라고 다 전선 적부도가 아니다 — POL 분포를 먼저 보고 고를 것.**

**검증.** 실데이터 시뮬 PASS(위 표) · `bash build.sh` 성공 · 번들 `index-D0JqBfGd.js` grep
(V9.07-03 1 / fullEdiMapOf 1) · push `819382e..db708c3` · blob 해시 19파일 전수 일치 ·
라이브 sw.js `V9.07-03` 확인(V9.07-01·02 반영분도 번들에 유지 확인).
누적 ZIP = `Downloads\Tallyman_Master_V9.07-03.zip`.

---

### V9.07-04 (2026-07-26) — 편집기에 옆 베이 점유 표시 (40ft ✕ · 20ft 회색) + 드롭 차단

사용자 신고. 짝수 베이에 40ft가 있으면 홀수 베이에 ✕가 떠야 하는데
**카고플랜엔 나오고 베이 격자엔 안 나온다.** 그래서 이동 가능/불가 분간이 안 된다. 커밋 `1925daa`.

**원인.** 단독 홀수 박스에서 `cargoPlanCore.buildBayMarks`는 인접 짝수 베이를 이렇게 태그한다.

```
40ft/45ft → marks 에 'X'          (cargoPlanCore.js:397)
20ft      → shadow20s (회색 빈 칸)  (cargoPlanCore.js:406)
```

카고플랜은 `BayBoxV2`의 일반 분기를 타서 `cell.mark`와 `cpv2-shadow20`을 그대로 그린다.
그런데 편집기는 `renderCellContent`가 있는 **편집기 전용 분기**를 타는데, 그 분기는
`cell.mark`를 아예 쓰지 않고(자기 컨만 그린다) `className`도 `cellExtra`가 통째로 덮어쓴다.
→ 두 종류 모두 **빈 칸과 똑같이** 보였다.

**수정.** `BayGridEditor.makeExtra`에서 처리한다(`BayBoxV2`는 건드리지 않음 — className을 cellExtra가 이기므로).

```
blockedBy40 = !cn && cell.mark === 'X'      → class bge-x      (회색 + ✕)
blockedBy20 = !cn && cell.isShadow20        → class bge-shadow (회색)
둘 다 dropProps 제거 → 끌어다 놓을 수 없다
```

**`!cn` 조건이 핵심이다.** `'X'`는 `defaultGetSelfMark`가 **자기 컨의 마크**로도 쓴다
(`c.pod !== pod`이면 'X' — cargoPlanCore.js:781). 자기 베이 컨은 항상 `box.cellMap`에 있으므로
`cn`이 잡혀 막히지 않는다. 이 조건이 없으면 **타 목적지 컨이 전부 ✕로 덮여 사라진다.**

표시 규칙은 사용자 확정 — 40ft는 회색+✕, 20ft는 카고플랜과 같은 회색. 드롭은 둘 다 차단.
`::after` 절대배치라 셀 크기에 영향 없음(격자 기하 고정 규칙 준수).

**실데이터 시뮬 PASS.** 매트릭스 01·02·03에 40ft(bay02 row02 tier82)·20ft(bay02 row04 tier82)·
자기 컨(bay01 row02 tier84)을 넣고 `computeBayRenderData`를 돌렸다.

```
BAY 01  → tier82 row02 mark 'X'      (인접 40ft)      → bge-x
          tier82 row04 isShadow20     (인접 20ft)      → bge-shadow
          tier84 row02 mark 'X'       (자기 컨, POD 불일치) → cn 있으므로 정상 표시
BAY (02)03 → 페어 박스라 인접 표시 없음 (정상)
```

**빌드에서 걸린 함정 하나.** `BGE_CSS`는 템플릿 리터럴이라 CSS의 `content:'\2715'`가
**8진 이스케이프로 해석돼 빌드가 죽는다**(`Legacy octal escape sequences cannot be used in template literals`).
→ 유니코드 이스케이프 대신 문자 `✕`를 직접 쓴다. 템플릿 리터럴 안의 CSS에 백슬래시 이스케이프 금지.

**검증.** 시뮬 PASS(위) · `bash build.sh` 성공 · 번들 `index-CI71LdOe.js` grep
(bge-x 4 / bge-shadow 3 / 배치 불가 3 / V9.07-04 1 / fullEdiMapOf 1=V9.07-03 유지) ·
push `163fec3..1925daa` · 라이브 sw.js `V9.07-04` 확인.
누적 ZIP = `Downloads\Tallyman_Master_V9.07-04.zip`.

---

### V9.07-05 (2026-07-26) — 옆 베이 점유 표기를 카고플랜과 일치 + adjMap 직접 판정

사용자 후속. V9.07-04에서 "회색은 보이는데 ✕가 안 보인다", 그리고
**"카고플랜의 각 베이처럼 베이 플랜도 표기가 같아야 한다"**. 커밋 `72e7852`.

**실자료로 확인한 것 (MCSC-PRELOAD EDI, 1077대).**
짝수 베이(06·10·14·18·22·26·30·34·38·44)가 전부 40ft라, 편집기 판정을 그대로 돌리면
`bge-x` **290칸**, `bge-shadow` **0칸**이었다. 즉 사용자가 본 회색이 곧 ✕ 칸이고
**배경만 칠해지고 글자가 안 찍히던 상태**였다. `::after` 방식을 버리고 카고플랜과 같은
**셀 안 X 글자**(`renderCellContent`)로 바꿨다.

**같이 잡은 잠재 버그 — `mark === 'X'` 판정 금지.**
`defaultGetSelfMark`는 `c.pod !== pod`인 **자기 컨에도 'X'**를 준다(cargoPlanCore.js:781).
V9.07-04은 `!cn && cell.mark==='X'`로 막았는데, 컨을 옮기거나 임시창고로 빼면
그 자리는 `cn`이 사라지고 마크만 남아 **방금 비운 자리가 잘못 막힌다.**
→ `boxes` 메모에서 **현재 위치(`state.pos`) 기준 `adjMap`을 직접 계산**한다.

```
단독 홀수 박스 k에 대해, bay ∈ {k-1, k+1} 인 컨을 훑어
  sizeOf(c) === '20' → adjMap[tier-row] = '20'   (회색)
  그 외(40/45)      → adjMap[tier-row] = '40'   (X 글자)
페어 박스 "(EE)OO"는 인접 판정 없음 — 두 베이를 함께 그리므로.
```

**표시 규칙 (사용자 확정 — 카고플랜과 동일).**
40ft 점유 = 흰 배경 + **X 글자**. 20ft 점유 = 회색 빈 칸. 둘 다 드롭 차단.

**실데이터 시뮬 PASS (MCSC-PRELOAD EDI).**

| | X(글자) | 회색 | 빈칸 | 컨 |
|---|---|---|---|---|
| 기본 | 290 | 0 | 306 | 842 |
| 홀수베이 컨 1대 창고로 이동 | 290 | 0 | **307** | 841 |

두 번째 줄이 회귀 검증이다. 뺀 자리가 **빈칸으로 늘어난다**(막히지 않는다).
옛 `mark==='X'` 방식이었다면 그 컨의 POD가 대표 POD와 달라 자기 마크가 'X'이므로 잘못 막혔다.

**검증.** 실데이터 시뮬 PASS(위 표) · `bash build.sh` 성공 · 번들 `index-CLJIXbjU.js` grep
(adjMap 3 / bge-x-mark 2 / 차지한 자리 2 / V9.07-05 1) · push `3ae5312..72e7852` ·
blob 해시 14파일 전수 일치 · 라이브 sw.js `V9.07-05` 확인.
누적 ZIP = `Downloads\Tallyman_Master_V9.07-05.zip`.

---

### V9.07-05 후속 — 버전 정본화 + 문서 대청소 (2026-07-26, 사용자 지시)

사용자 지시: **"검수앱 콘앱 메일수집기의 모든 자료들 버전을 일치시켜 주고 지침서들도 최신버전으로 통일. 클로드들이 헷갈려 합니다."**
앱 코드 동작은 바꾸지 않았다. 버전 번호도 V9.07-05를 유지한다(문서·정리 작업이라 캐시 무효화가 불필요하고, `bulk_tally.html`은 `sw.js` precache 대상이 아님을 확인).

**전수 실측 결과 — 기억이 전부 틀렸다.**

| 항목 | 기억(CLAUDE.md) | 실제 | 근거 파일 |
|---|---|---|---|
| 검수앱 | V8.84-02 | **V9.07-05** | `src/utils.js` APP_VERSION |
| 콘앱 | C7.67 | **V9.07-05** | `cone.html` 라벨·`__APPV`·`__CONEV` |
| 벌크탤리 | (기록 없음) | 버전 문자열 자체가 **없었음** | `bulk_tally.html` |
| 수집기 | v2.17.7 | **2.17.15** (2026-07-25) | `app_gui.py` VERSION |

검수앱은 기억이 **23판** 뒤처져 있었다. 콘앱은 폐기된 체계(`C7.xx`)를 현재 버전으로 알고 있었다.

**헷갈림의 실제 원인 7가지 (추정 아님 — 전부 파일로 확인).**

1. ⭐ **파일명이 `#Uxxxx`로 깨진 파일 7개**가 저장소에 있었다. 그중 2개가 **2026-06-15/16자 통합지침서(150KB)** — 최신본은 324KB다. 클로드가 이걸 열면 6주 전 문서를 최신인 줄 알고 읽는다. 가장 치명적이었다. (`검수앱지침서.md`·`평택항_..._3앱통합본.md`·`REF_베이페어링.md`·`repo정리.bat/ps1`·`docs/checklist_*`·`docs/context-notes_*`의 깨진 사본)
2. **저장소에 통합지침서 48개 누적** (V8.09-16 ~ V9.07-05). 배포절차 9장 "옛 버전 파일명은 지운다"가 한 번도 이행되지 않았다.
3. **드라이브 `내 드라이브\새 폴더\tallyman-hub`에 통합지침서 28개** (V8.09-16 ~ V8.40). 저장소보다 더 낡았고, 드라이브 커넥터로 검색하면 이쪽이 먼저 잡혔다.
4. **지침서 헤더의 3앱 표가 자기 자신과 불일치.** 최종 버전 줄은 V9.07-05인데 표는 검수앱 V9.04-03 / 콘앱 C7.64 / 벌크탤리 "2026-06-12판"이었고, **수집기는 표에 아예 없었다.**
5. **수집기 폴더명 `TallymanMailCollector_v2.17.11-02` ≠ 내부 VERSION `2.17.15`.** 폴더명으로 버전을 읽으면 13판 틀린다.
6. **`_인계_*.md` 14개 + `HANDOFF_*.md` 2개 + `변경_*.md` 2개**가 저장소 루트에 산재. 내용은 이미 지침서에 흡수돼 있었다(V8.25-04·V8.28·V8.41 표본 대조로 확인).
7. **`★배포_표준절차` 이력이 V9.07-01에서 멈춤** (실제 배포는 -05까지).

**조치.**

- **지침서 최상단에 "버전 정본표" 신설** — 4개 산출물 전부, 각 버전의 **소스 파일 경로를 명시**했다. 이제 "버전이 뭐냐"는 물음에 기억으로 답하지 않고 그 파일을 읽는다.
- **`C7.xx` 체계 폐기를 명문화.** V8.98-05에 이미 죽은 체계였는데 문서에 살아 있었다.
- **저장소에서 73개 파일 삭제** — 깨진 파일 7 + 구지침서 48 + 인계문서 16 + 옛 ZIP 2. (git 이력에는 남는다.)
- **드라이브 `tallyman-hub`의 구지침서 28개 삭제.**
- **수집기 폴더 리네임** `_v2.17.11-02` → `_v2.17.15`.
- **`bulk_tally.html`에 버전 라벨 신설** (`<meta name="app-version">`) + build.sh 동기화 대상에 추가.
- ⭐ **`build.sh`에 재발 방지 2종 추가** — 빌드마다 ① 최신 통합지침서 1개만 남기고 나머지 자동 삭제, ② `#Uxxxx` 깨진 파일 자동 삭제. **사람이 기억해서 지우는 규칙은 48번 실패했으므로 스크립트에 넣었다.**
- **문서 정리 규칙을 지침서 상단(정본표 아래)에 비협상 조항으로 못 박음.**

**검증.** 버전 문자열 4종을 소스에서 직접 grep해 정본표와 대조 · `bash build.sh`로 동기화 동작 확인 · 커밋 후 fresh clone에서 지침서 1개·깨진 파일 0개 확인.

---

### 수집기 2.17.42 (2026-08-02 밤) — 선박 단위 일정을 엉뚱한 항차에 붙이지 않는다

**증상(사용자 지적)**: *"항차가 틀린데 한선박이 두항차 동시에 들어 오나요?"* — TMPZ 2025E 카드가
오늘 도선 시각(08-02 18:30)을 달고 있었다. 실제 2025E 배정은 **08-09 19:00**(콜번호 029)이고,
오늘 들어오는 건 2023E(콜 027)다.

**원인**: 도선 예보·PORT-MIS는 **선박당 레코드 1개**라 항차 구분이 없다. `decide_plan`이 그 값을
그 선박의 **모든 활성 항차**에 붙였다. 배정표는 항차별로 오는데 대조하지 않았다.

**수정**: `_same_call()`/`_voy_scoped()` 신설 — 선박 단위 값은 **그 항차 배정과 36시간 안쪽일 때만**
채택. 어긋나면 배정값 유지. 배정이 없으면 종전대로(없는 근거로 막지 않는다). 도선 양쪽·도선 조각·
PORT-MIS 폴백·부분 합성 후보까지 **네 경로 모두**에 같은 게이트를 걸었다(3금지① 호출부 전수).

**검증**: 시뮬 7건(오늘 기항 채택 · 일주일 뒤 거부 · 배정 없음 · 3시간 차 · 40시간 차 · 자력선 회귀 2)
전부 기대대로 · 라이브 20항차 전/후 대조에서 **바뀐 건 3건**(TMPZ_2025E · RZOR_R083E · OBWH_2705E) —
셋 다 다음 기항 카드에 이번 기항 도선값이 잘못 붙어 있던 것으로, **같은 결함이 세 척에서 나고 있었다.**
나머지 17항차 불변. 이번 기항(TMPZ_2023E · RZOR_R082E · OBWH_2703E)은 도선값 그대로 유지 확인.

---

### 수집기 2.17.41 (2026-08-02 저녁) — RZOR 덱플랜 자동 수집·업로드

**증상(사용자 질문)**: *"RZOR도 카고플랜을 볼수있게 하지 않았나요?"* — V9.22에서 만든 덱 플랜 뷰가
R081E·R082E에서 안 보였다.

**조사로 확정한 사실**
- RZOR EDI는 IFCSUM이라 **선내 위치가 전무**(R082E 191건 전부 `bay`/`row`/`tier` 빈칸) →
  덱플랜이 카고플랜의 유일한 원천.
- 덱플랜을 앱에 넣는 경로가 **업로드 화면 한 곳뿐**이었다(`VoyagePage.jsx` → `fbSetStowagePlan`).
  R080E에 플랜이 있는 건 V9.22 만들던 날 손으로 올렸기 때문이고, **R081E는 파일이 7/31에 도착하고도
  아무도 안 올려 그 항차 내내 카고플랜이 없었다.**
- 이번 항차 파일은 수집기에 **아예 들어오지도 않았다.** 원인 2겹:
  ① rzdf 메일은 제목·파일명에 항차가 없다 ② 내용에는 `R082E`가 있는데 `_voy_in_excel()`이
  **확장자로 형식을 판단**해, 이름만 `.xls`인 xlsx(zip)를 바이트로 훑다 아무것도 못 읽었다.

**수정(외과적)**
- `auto.py::_voy_in_excel` — 형식 판별을 **확장자 → 시그니처(`PK`)** 로. 앞말(VSL/VOY) 없이 항차만
  적힌 자료를 위해 **코드형 항차가 파일 전체에서 유일할 때만** 채택하는 폴백 추가(오배치 방지).
- `collector/deckplan.py` **신설** — `src/rzorPlan.js`의 파이썬 이식본. 출력 형식(voy/decks/slots)이
  같아야 앱 화면과 어긋나지 않는다. openpyxl에 **경로 대신 바이트(BytesIO)** 를 넘긴다(확장자 거부 회피).
  파일명 게이트(`looks_like_deck_plan_name`)로 후보를 좁힌 뒤에만 연다 — V9.31 지연 사고 재발 방지.
- `merge.py::_push_deck_plan` **신설** — 리퍼온도 훅 옆에 배치. 양하/선적은 **덱플랜 컨과 실제로
  겹치는 쪽**으로 정한다(방향 추측 금지). 같은 파일·같은 대수면 재전송 안 함(멱등).
  예외는 로그로 드러낸다(조용한 실패 금지).

**검증**
- 파싱 정확도 — 시트 안 **SUB TOTAL**을 정답지로 전 덱 대조: R080E 160(D75/C71/B14) ·
  R081E 159 · R082E 192(D102/C71/B19) **전부 일치**. R080E 160은 **V9.22 JS 파서 결과와 동일**(두 벌 동등성).
- 파급검증 — 메일함 **엑셀 618개 전수**로 `_voy_in_excel` 전/후 대조: 값이 바뀐 것 **0** ·
  못 읽게 된 것 **0** · 새로 읽은 것 22(전부 RZOR, 전부 폴더 항차와 일치).
- 드라이런 — 지난 항차(R080E·R081E)는 검수앱에 항차가 없어 **아무것도 쓰지 않음** 확인.
  R082E는 discharge 겹침 191/191로 정확히 판정.
- 효과 확인 — R082E `stowagePlan` 서버 되읽기 total 192(D102/C71/B19).

**남은 것** — 덱플랜이 PDF(`2甲/3甲/4甲.PDF`)로만 오는 경우는 아직 파싱 못 한다(xls가 같이 오면 무관).

---

### V9.43~V9.45 / 수집기 2.17.39~2.17.40-02 (2026-08-02 저녁) — 자리별 출처 표기 · 수석 권한 잠금

**V9.43 · 수집기 2.17.39~2.17.40-02 — 작업일시 출처를 자리별로 보여준다**
- 사용자 확정: *"출처 두군데면 다보여주면 됩니다. 입항에 도선 출항에 포트미스"* /
  *"도선 2개가 붙으면 확정으로 알면 되고 신고가 붙으면 예정으로 알면 되니까요"*
- `planSrc`가 `"입항출처|출항출처"` 형태가 됐고, 앱이 각 시각 뒤에 `⚓도선/📧메일/🚢신고/📋배정`을 붙인다.
- 지난 기항 배제(2.17.39-01): `port_mis_data`·`pilot_forecast`는 **선박당 레코드 1개**라 옛 값이 남는다.
  파급검증에서 8항차 중 4건이 7월 날짜였다 → **미래 etd만 인정**.
- 출처 누락 수리(2.17.40-02): 보완 결과가 배정과 글자까지 같으면 patch가 스킵돼 출처가 안 붙었다(KSKM).
  → **값이 같아도 `planSrc`는 따로 기록**한다.

**V9.44 — 수석 대시보드 진입 차단**
- 사용자: *"아......... 이제야 알았네요 일반 검수원이 수석대쉬보드 진입이 가능 하다는걸."*
- 홈에서 버튼을 숨기고(`isChief`), `ChiefDashboard` 첫머리에 가드를 세워 직접 진입해도
  "🔒 수석 검수원만 출입이 가능합니다"로 막는다.

**V9.45 — 수석검수·부수석 이름 로그인 잠금 (V9.44와 짝)**
- 사용자: *"이제 로그인시 타인의 이름으로 못들어 가게 해야 합니다."* → 범위는 **수석검수·부수석까지**.
- 화면만 막고 이름을 안 잠그면 수석 이름으로 들어와 그대로 열린다 — **문 잠그고 창문 열어둔 꼴**이었다.
- 잠금 대상: 관리자 + `isChief()` 7명. 일반 검수원 22명은 종전대로 즉시 통과(현장 속도).
- **저장 노드를 나눈 이유(반드시 지킬 것)**: `getAdminNames()`가 `admins`의 키를 그대로 관리자로 읽는다.
  수석 비번을 `admins/`에 저장하면 **관리자 권한이 딸려 붙는다.** → 관리자 `admins/{이름}`,
  그 외 잠금 대상 `locks/{이름}`. 신설 `lockEntry/lockPath/isLockedName/ownerCanUnlock`.
- 소유자 마스터 해제: 비번을 잊었을 때 김성일 비번으로 **이번 접속만** 열어준다(남의 기기를 신뢰기기로
  등록하지 않음). 그 자리에서 초기화하면 다음에 본인이 새로 정한다.
- **같이 잡은 잠복 결함 2건**
  - `handlePick`이 `guardLoaded` 검사를 **잠금 판정 뒤에** 했다 → guard 로딩 중에는 비번이 "미설정"으로
    읽혀 **남의 비밀번호 설정 화면이 뜨는** 경로가 있었다. 로딩 검사를 맨 앞으로 옮김.
  - 신뢰기기 카운트가 최상위 `guard.devices`(구버전 단일 관리자 필드)를 세어 **사람이 바뀌어도 같은 숫자**가
    나왔다 → `lockEntry(guard, 이름).devices`로 교정.
  - `fbGetAdminGuard()` 실패 시 `guardLoaded`가 영원히 false가 될 수 있어 `.finally`로 보장(조용한 실패 금지).

**검증.** 시뮬 24항목 전체 PASS(잠금 7/열림 22 명단 대조 · 노드 분리로 관리자 수 불변 · 본인/틀린/남의 비번 ·
소유자 해제 · 관리자 회귀 · guard=null 안전성) · 라이브 RTDB에 `locks/` 왕복 쓰기·삭제로 **보안 규칙 통과 실증** ·
빌드 후 번들 grep(V9.45 · `locks/` · 게이트 문자열 4종) · 커밋 blob 22/22 일치 · 라이브 sw.js `V9.45` ·
라이브 번들 md5 = 로컬 번들 md5.

---

### V9.39~V9.42 / 수집기 2.17.34~38 (2026-08-02 오후) — 작업일시 판단 + 자력선 안전장치

> 상세 경위는 드라이브 `CLAUDE_SHARED\★작업계획서_수집기개편_20260801_상세.md` 판 D.

**★★ 작업일시 순위가 바뀌었다 — V9.35 확답을 사용자가 직접 뒤집었다**

```
(1) 도선 예보   (2) 선사 일정 메일   (3) PORT-MIS 신고   (4) 선석배정
```

⛔ 폐기: V9.35 "작업일시의 진실은 선석배정이다. 도선만 해놓고 대기하는 경우도 있으니까요."
   → 이 문서 뒤쪽 V9.35 항목에 그 문장이 그대로 남아 있다. **옛 문장을 근거로 되돌리지 말 것.**

**근거(TMPZ 2023E 실측)**: 도선 18:30~16:00 · 메일 ETB 20:30~ETD 16:00 · 배정 21:30~12:00.
도선과 메일이 소수점까지 일치하고 배정만 4시간 어긋났다. 사용자 설명 — *"선박이 조금 일찍 도착해서
작업시작이 빨라지고 더불어 출항시간이 빨라집니다. 도선시간과 메일이 맞아 떨어지죠."*

**★ 자력선 안전장치(사용자 확정)** — *"자력선입니다. 도선예보에 올라오지 않는 선박 반드시 메일로
확인해야 합니다. 안전장치는 포트미스 입니다."* 자력선은 예보에 안 뜨니 메일이 사실상 1순위이고,
메일마저 없으면 PORT-MIS 신고가 받친다.

> ⚠ **PORT-MIS 는 지난 기항을 걸러야 한다.** 콜사인당 레코드가 하나라 옛 신고가 남는다.
> 안 거르면 8월 항차에 7월 날짜가 박힌다(실측: MCAT·NSDC 07-05, STMJ·SWSP 07-21 — **파급검증이 잡았다**).
> 출항(etd)이 안 지난 신고만 인정. 앱에서도 같은 사고가 있었다(V9.34).

**사용자 실험이 원인을 갈랐다.** 클로드가 "메일 파싱이 왜 안 되나"를 추측으로 파고 있을 때
사용자가 *"배정목록에서 일시를 가져오는 기능을 테스트로 잠시 닫고 도선이 잡히는지 보자"* 고 제안했다.
임시 스위치(`plan_from_berth`)로 끄자 카드가 `18:30 ~ 16:00 도선`으로 바뀌었고, 그 한 번으로
**후보 사슬·도선 파싱·±12h 가드·앱 표시가 전부 정상**임이 증명됐다(테스트 후 원복).

**판별 요약**

| 판 | 버전 | 내용 |
|---|---|---|
| D-2 | 수집기 2.17.34 | 선사 일정 메일 파싱(`VESSEL MOVEMENT`) — 수집 조건은 안 건드리고 본문만 |
| D-3 | 수집기 2.17.36 | **일정 전용 루프 분리**(3분) — 전수 검색 사이클 19~20분 동안 일정이 멈추던 문제 |
| D-4 | 수집기 2.17.35→37→38 | 작업일시 판단 `decide_plan()` — 규칙이 두 번 바뀌어 최종 4단 순위 |
| D-5 | V9.40 · V9.41 | `info.planSrc` 표시 — 도선/메일/신고/배정 |
| D-6 | V9.42 | 홈 상단 3카드 정리 |
| — | V9.39 | 베이플랜 슬롯 Set 오염 수리 + 흡수 모달 IMO 근거 |

**D-3 일정 루프 — 크롬 경합을 구조적으로 막았다.** 두 루프가 같은 크롬(9222)을 만지면 CDP가
엉킨다(08-01 실측 사고). 그래서 **크롬을 안 쓰는 것만** 뗐다 — 도선예보·터미널작업현황(둘 다 urllib).
선석배정·메일은 컨테이너 사이클에 남겼다. 효과확인: 도선 갱신 12:02:43 → 12:27:11(3분 주기).

**V9.42 홈 정리(사용자 지시)** — 상단 3카드 삭제, 수석 대시보드는 '진행 중인 항차' 줄 가운데로,
통합검색·PORT-MIS 캡처는 수석 대시보드 바로가기 그리드 빈칸으로(모달·prop 함께 이동).

**미완(다음 판)**
- 선사 일정 메일이 **실제 메일에서 아직 안 걸린다.** 파서는 실메일로 검증 PASS인데 `{voy}_일정.json`이
  안 생긴다. 진단 로그를 넣었으나 `log()`는 GUI 창에만 뜨고 **진단 파일에는 안 남는다**(`_dg`만 파일).
  → 진단을 파일로 남기면 "수집 목록에 없음 / 본문을 못 읽음" 중 어느 쪽인지 확정된다.
- 메일 서식은 두우·TJM만 지원. 다른 선사는 실물 확인 후 추가.

---

### V9.38 / 수집기 2.17.33 (2026-08-02) — 출항 배지 기준 교체 + 판정 단일화

> 상세는 드라이브 `CLAUDE_SHARED\★작업계획서_수집기개편_20260801_상세.md` 판A·판B.

**원래 계획(판11 = 판정을 수집기로 이관)을 착수 직전에 되돌렸다.** 사용자 지적:
*"그걸 굳이 수집기가 할 필요가 있습니까? 수집기 하나로 다 할려고 하죠?"* /
*"검수앱이 잘 돌아가면 그걸 볼 필요가 없습니다. 실시간 작업중이니 잔여분도 바로 볼 수가 있으니까요."*
출항 배지의 1순위 근거는 **검수앱 자신의 잔여**(총 − completed)이고 검수하는 순간 앱이 안다.
수집기가 5~20분 뒤 계산해 돌려주면 실시간 값을 일부러 늦추는 것이다.
→ **판정은 앱, 수집기는 앱이 모르는 재료만.** `collector/badge.py` 폐기(경위만 남김).
※ 이관 자체는 착수 전 검증했다 — 실 17항차 JS↔파이썬 **밀리초까지 일치**, 합성 10케이스 10/10.
  그 로직은 그대로 `badgeRule.js`로 옮겼다.

**① V9.38 — 기준 교체 + 규칙 1벌**

- ⛔ `pct >= 90%` 폐지. *"600개의 10%는 60개, 2갱이면 한 시간 이상 / 100개의 10%면 10개, 2갱이면 5분."*
  같은 10%가 한 시간도 5분도 되므로 준비 시간 확보에 쓸 수 없다.
- ✅ **선적 잔여 <= 20개**. 갱당 시간당 20개이고, **갱수로 나누지 않는다** —
  마무리엔 갱이 하나씩 빠지고 **마지막 한 갱**이 처리하므로(사용자 교정). 잔여 20개 = 약 1시간.
- 선적 잔여만 본다(양하는 먼저 끝난다). 선적 없는 항차는 양하 잔여.
- **DEPARTED면 무조건** 출항(완료 버튼 미입력 보완) · **한 번 뜨면 유지**(`info.departBadgeAt`).
- 신설 `src/badgeRule.js` — 검수앱·콘앱 **2벌 -> 1벌**. 콘앱은 판정하지 않고 결과만 읽는다
  (콘앱은 `completed`를 안 읽어 잔여를 모르므로 판정 자체가 불가능).
- 검증: 모듈 12케이스 · 실 17항차 전/후 **표시 변화 0건** · 번들 grep · blob 20/20 · 라이브 3파일.

**② 수집기 2.17.33 — 터미널 항차 상태 공급**

- `terminal.voy_status(page)` 신설 — **PCTC(tr 클래스 done) · PNCT(상태 열 DEPARTED)** 두 형식.
  종전 `_row_status`는 키가 `XTPG-0028` 형식이라 PNCT(`TMPZ027(2023E/2023W)`)를 못 읽었다.
- `terminal.push_voy_status` — **실재하는 항차 키에만** `info.terminalStatus` PATCH,
  값이 같으면 쓰지 않는다(쓰기 폭주 방지).
- 검증: py_compile 4 · PNCT 9항차·PCTC 6항차 PASS · 오탐 방지 PASS ·
  재시작 후 하트비트 `2.17.33` · **라이브 `terminalStatus` 0건 -> 7건**(`MCAP_629N=working` 실제와 일치).

> **교훈**: *"계획된 것부터 하시죠. 그래야 윤곽이 완전히 잡히고 그 안에서 수정을 해야 큰 변화가 없을 거 같은데."*
> 두 곳에 복사된 채 기준을 바꿨다면 두 곳을 똑같이 고쳐야 했다. 구조 먼저, 기준은 그 안에서.

---

### V9.37-02 / 수집기 2.17.32-01 (2026-08-02 아침) — 플랜 자리 카드 집계 + 하트비트 데몬

> 상세는 드라이브 `CLAUDE_SHARED\★작업계획서_수집기개편_20260801_상세.md` 판12·판13.

**① V9.37-02 — 플랜 자리 카드 집계 (사용자 화면: TMPZ `선적 평택 1 · 매칭 0 · 누락 1`)**

- 라이브 데이터는 정상이었다 — 슬롯 370 전부 `pol=KRPTK`, POD는 KRPUS 211 / CNSHA 159(인계서와 일치).
- **원인**: `computeStats` 가 `ptkCns.add(c.cn)` 인데, 플랜 가상 EDI 슬롯은 확답 ④
  ("컨번호 임의 배정 금지 — `__SLOT_` 키")대로 **`cn` 필드가 없다.** `Set`에 `undefined` 370개가
  **1개로 합쳐져** `ptk=1` / `matched=0` / `missing=1` 세 숫자가 한꺼번에 나왔다.
- **복제본 전수(3금지①)**: `HomePage`(수리) · `ChiefDashboard`(**같이 수리** — 안 고치면 홈 370 /
  수석 1로 어긋난다) · `BayPlan.jsx:107 s.add(c.cn)`(**미수리, 다음 판** — 그림 렌더라 증상·검증이 다름).
- **수정**: `Object.entries` + `c.cn || key` / 슬롯은 누락에서 제외(`planSlots`) /
  `planOnly` 분기 신설 — 슬롯엔 컨번호가 없어 매칭·누락이 성립하지 않으므로 표시하지 않고
  `평택 370자리 · 리스트 301 · [플랜 자리]` 로 보여준다.
- **검증**: 실 Firebase 17항차 전수 시뮬 전/후 — **TMPZ 선적 한 줄만 변경**(홈·수석 각각) ·
  번들 grep(`planOnly`·`__SLOT_`·`플랜 자리`) · origin blob **20/20 일치** ·
  라이브 sw.js `V9.37-02` · index.html이 가리키는 번들 = 빌드 정본.

**② 수집기 2.17.32-01 — 하트비트 데몬 60초 (홈 '끊김' 오표시)**

- 아침 홈에 빨간 `수집기 끊김 · 11분 전`. **클로드가 확인 없이 "죽었다"고 단정**했고 사용자가
  *"수집기 정상 아닌가요? 밤새 돌았는데"* 라고 되물어 재확인 → **살아 있었다**(40초 뒤 갱신). 답지 4-A2 재발.
- **실측**(진단_20260802, 00:02~08:02 57사이클): 보통 6분이지만 **약 20분마다 19~20분짜리
  전수 검색 사이클**이 돈다. 하트비트를 사이클 시작·끝에만 찍으니 그 동안 무신호이고,
  홈 기준은 `cycleMin*2 = 10분` → **정상인데 20분마다 반드시 '끊김'.**
  v2.17.19-01의 '사이클 시작 하트비트'로는 못 막는다(시작↔끝 간격이 곧 20분).
- **수정**: `autoreg.touch_heartbeat` 신설(**`collector_heartbeat/at` 한 필드만 PUT** — 전체 PUT이면
  `autoreg`·`cycleMin`·`version`이 지워진다) + `app_gui` **60초 데몬 스레드**
  (`watching or auto_running`일 때만 — 켜두고 자동을 끈 상태를 '정상 수집 중'으로 보이면 안 되므로).
- **검증**: py_compile · 실 Firebase 왕복(at 갱신 · **타 필드 보존**) · 게이트 4조합 ·
  재시작 후 하트비트 `2.17.32-01` · 라이브 **정확히 60초 간격 갱신** 확인.

> **교훈(오늘 두 번째)**: 빨간 표시를 보고 원인을 단정하기 전에 **한 번 더 읽어라.**
> 40초짜리 재조회 하나가 "죽었다"와 "20분 사이클"을 갈랐다.

---

### 2026-08-02 새벽 세션 — 판7·9·10·8-B (수집기 2.17.30 -> **2.17.32**)

> 상세 기록은 드라이브 `CLAUDE_SHARED\★작업계획서_수집기개편_20260801_상세.md` 7-D절.

| 판 | 내용 | 검증 |
|----|------|------|
| 7 | **부족 표시 단일화** — `merge.effective_gap/gap_short` 단일 소스. 리포트·사이클 로그·번개메시지 3사본이 각자 계산하던 것 | TMPZ `부족 -301` 재발 경로 제거 확인 |
| 9 | `collector/master.py` — 생산(`produce`)/등록(`register`)/`cycle()` 분리. app_gui 두 루프가 3·6개 호출을 나열하던 것을 한 줄로 | 호출 순서 기준표 전/후 대조 · 스텁 5종 PASS · 실사이클 42항차 완주 |
| 10 | 예약 자동등록 성공률 실측(읽기 전용 리포트) | 4주 86항차 — 도달률 83%, **예정분 제외 시 72/73 = 99%** |
| 8-B | **`.txt` EDI 합본 인식 + 파서 블록화(LOC-first)** | 아래 |

**판 8-B (수집기 2.17.32) — 판10이 남긴 `DXQD_2630E` 결함 후보의 원인**

- **발단**: 클로드가 "DXQD는 EDI가 없다"고 단정 -> 사용자가 폴더의 `XUD2630E(UN22).txt`를 직접 제시.
  분석 스크립트가 `.edi/.asc`만 스캔했다. *"재료가 아무리 좋아도 무슨 소용 있나요, 요리사가 요리를 못하면?"*
- **실측(메일박스 전수)**: `.txt` EDI **7파일 · 2척**(DXQD 4 · STMJ 3). **전부 LOC-first**
  (`LOC+9/LOC+11`이 `EQD+CN` **앞**).
- **결함 2개(독립)**: (1) `EXTS_MERGE`에 `.txt`가 없어 **합본이 기준 EDI를 영영 못 찾음**(5항차 합본채택 0) —
  "기대 미확보·판정 보류"의 원인. (2) `_edi_leg_of_text`가 EQD-first 전제라 LOC-first에서 **매 파일 1대씩 밀림**.
- **수정(외과적 2곳)**: `fileselect.EXTS_MERGE`에 `.txt` 추가 + `_`접두·대조리포트 제외 + `looks_like_edi()`
  **내용 확인**(우리 기록 파일도 `.txt`이므로) / `auto._edi_leg_of_text`를 **블록 단위 집계**로 교체
  (LOC-first·EQD-first 양쪽 정확, 파일 끝 잔여 블록 처리).
- **검증**: 파급 기준표 651파일·1156줄 전/후 **18줄만 변경**(CLASS 변화 0) · 재시작 후 하트비트 **2.17.32** ·
  1사이클 완주 · 17항차 x (ediContainers·records) **34개 지표 전부 동일** · 항차 목록 동일.

**부수 확인 — TMPZ 플랜은 규칙대로 작동 중(수정 없음).**
클로드가 "merge 경로에 플랜 보호가 없다"는 위험을 제기했으나 **인계서에 이미 답이 있었다**
(2026-07-11 사용자 확정). "합본 대체 보호"는 *업로드* 경로에서 LOADLIST가 낱개 엑셀을 대체할 때
플랜을 예외로 살린다는 뜻이지 merge에 넣으라는 뜻이 아니다 — 플랜엔 컨번호가 없어 합본 재료가 아니고,
가상 EDI는 `autoreg_helper.parsePlanGrid`가 만든다. 실측: 2023W는 LOADLIST가 있어도 플랜 채택,
게이트는 컨번호 0인 파일을 배제할 수 없음, 라이브 `TMPZ_2023E/loading` 370자리(인계서와 일치).
-> **규칙 위반 0건.** 상세는 답지 1-4B절.

> **교훈**: 이 건은 **묻기 전에 인계서를 찾았으면 질문 자체가 없었다.**
> 사용자: *"TMPZ 플랜 지침서 찾아 보세요, 아님 인계서 안 찾아 보셨죠?"* — 답지 규칙 그대로, **문서 먼저.**

---

### 2026-08-01 저녁 세션 — 수집기 개편 판1~6 + 클로드 자신이 낸 사고 3건

**한 일(수집기 2.17.23 → 2.17.29-02, 검수앱 V9.36-02 → V9.37-01)**

| 판 | 내용 | 검증 |
|----|------|------|
| 1 | 파일선택 규칙 단일화 `collector/fileselect.py` | 항차폴더 160개×2종 전/후 **md5 동일** · 죽은 사본 3개 제거 |
| 2 | 합본 사이클 전제·재검색 판정 단일화(`_cycle_ctx`·`_research_decision`) | 행동추적 486줄 **md5 동일**(분기 4갈래 전부 태움) |
| 3 | 기대 총계 기준을 **선석배정 확정수량**으로(PCTC) | 사고 재현 시 `부족 9` 자동 표시 |
| 3-01 | **예정(하늘)/확정(황·초록) 구분** — 사용자 지적 | 표 `tr` 클래스 `plan/work/done` 사용, 예정 미저장 |
| 4 | 3반 자동 배정·재검증 `class_rules.py` | 24척 소급 배정 · 통과화물 나오면 자동 2반 이동 |
| 5 | Firebase 쓰기 단일 창구 `fbwrite.py` | 8곳 전환 · 손 구현 잔존 0 · 실제 왕복 검증 |
| 6 | 즉시 처리 신호 | ⚠ **요건 미확인 기능**(아래) |

**⚠ 판6은 클로드의 가정에서 나왔다.** "작업 중엔 5분도 길다"를 스스로 전제하고 만들었으나,
사용자 답은 *"지금 처리는 필요 없다. 순차적으로 도니 **작업 시작 전까지만** 완성되면 된다"*.
수집기는 단일 스레드 순차 실행이라 이름을 바꿔도 한 바퀴 돌고 대기 때나 처리된다 —
**문구 수정조차 이득이 없어 손대지 않기로 확정**(사용자 판단). 기능은 배포된 채 둔다.
→ **교훈: 신규 기능 전에 '언제까지 끝나야 하는가'를 묻는다. '빠르면 좋다'는 요건이 아니다.**

**클로드가 직접 낸 사고 3건(전부 같은 날, 전부 검증 부족)**

1. **차트 조회가 사용자의 선석배정목록 탭을 가로챔**(v2.17.26). `page_for("pctc21.com")`이
   사용자가 띄워둔 목록 탭을 잡아 차트로 이동시켜, 매 사이클 "터미널 항차 0건 → 탭을 열어
   주세요"가 반복됐다. 사용자는 열어뒀는데 앱이 스스로 딴 데로 돌려놓고 없다고 한 것.
   → **차트 조회 자체를 폐지**(v2.17.28-03). 상태는 목록 표 `tr` 클래스에서 같이 읽는다.
   사용자가 *"배정목록에서도 색 구분이 가능하다"*고 알려줘 근본 제거가 가능해졌다.
2. **수집기 전용 크롬(9222)에 브라우저 자동화로 접속** → CDP 엉킴으로 수집기가 사이클 진입 불가.
   `<ws connected>` 후 `connect_over_cdp` 타임아웃. → 답지 4-A3에 금지 조항.
3. **판1 검증의 빈틈** — 전/후 목록이 "같은지"만 보고 "옳은지"는 안 봤다. 틀린 채로 같으면
   그것도 PASS가 된다. KSKM/2615S 오염(타선박 90·타항차 105)은 그 검증을 통과했다.

**응답 태도에서 교정된 것(사용자 지적)**
- *"맞나요?"는 틀렸다는 신호가 아니라 **확인했느냐**는 신호다.* 반사적 사과·번복 금지.
  ATPR 2635W 건에서 확인 없이 3회 연속 오판하고, 그 때문에 사용자가 자료를 다시 찾았다.
- **대조 전에 '같은 대상인가'부터.** 화면은 2635W인데 DB는 2635E(다른 기항 키)를 꺼내 비교했다.
- **부정에는 더 무거운 근거가 필요하다** — 부정은 사용자를 움직이게 만든다.

---

### 수집기 2.17.22 (2026-08-01) — **합본이 정상 자료를 잘라내던 결함** (사용자 신고 "수량 자료는 다 있었는데 합산이 안 돼서 생긴 문제")

- **증상**. XTPG 534W 대조리포트가 `EDI 기대 총계 0 / 수집 합본 108 / 총계 부족 -108`. 사용자가 받은 자료를 앱에 직접 다 넣으니 **117**이 됐다. 즉 **9대가 합본에서 빠져 있었다.**
- **원인 (재현으로 확정)**. `merge._inputs`의 **무토큰 리스트 교집합 게이트**(v2.17.16, MCSC 628N 허수 차단용)가 판정 기준 EDI를 **폴더 안 아무 EDI/ASC**로 잡는다. 선적 EDI가 오기 전 그 폴더엔 **인천 적재분(`RE)XTPG 534W KRINC PRELOAD.ASC`, `XTPG 534W KRINC DEP.ASC`)** 과 **타항차 프리로드(`XTPG 027 - Preload.ASC`)** 뿐이었다. **평택에서 실을 컨이 인천 적재분과 겹칠 리가 없다.** 그래서 정상 자료 `cntr_no_list.xls`(9대)·`XTPG534WCN_CONTAINERLIST38713.xls`(16대)가 "타선박 의심"으로 배제됐다.
  - 두 파일은 **7/30에 이미 폴더에 있었다**(`_수집기록.txt` 17:50 / 13:48). 메일이 늦게 온 것이 아니라 **하루 넘게 안 세고 있었다.**
- **복제본도 같이 고쳤다**(3금지 ①). 같은 결함이 `autoreg._edi_cns_of_folder`에도 그대로 있었다. `merge_all`만 고치고 `merge_priority`를 놓쳤던 7/31 사고와 같은 계열.
- **수정**. 기준 EDI를 **내용상 평택 선적(POL=KRPTK)인 EDI/ASC**로 한정(`auto._edi_leg_of_file` == '선적'). 평택 선적 EDI가 하나도 없으면 **판정 근거 자체가 없는 것**이므로 게이트를 걸지 않는다(fail-open — 막아서 자료를 잃는 쪽이 더 나쁘다는 기존 원칙 유지).
- **리포트 모순 제거**. 기대 총계가 0(근거 없음)인데 `108 − 0 = -108`을 "부족"으로 찍던 것을 **`판정 보류 — 기준 EDI가 없어 부족을 셀 수 없다`** 로 바꿨다. 사용자가 예전에 지적한 *"완성이라고 녹색 표시하고 부족하다고 또 표시되는 건 모순"* 과 같은 계열.
- **검증**(2필수 준수).
  - *재현* — 534W 폴더에서 선적 EDI만 뺀 상태를 만들어 돌리니 **현행 108**(실제 고장값과 정확히 일치) → **수정안 117**(정답). 가설이 아니라 재현으로 확정.
  - *효과 확인* — 내 재구현이 아니라 **고친 `merge._inputs`를 직접 호출**해 임시 복제 폴더에서 확인: 채택 14파일 → 합집합 **117 PASS**, `autoreg._edi_cns_of_folder` = 0(평택 선적 EDI 없음 → 정상).
  - *회귀* — 현재(선적 EDI 있는) 534W: 117 유지, `_edi_cns_of_folder` = 227.
  - *파급* — 메일박스 **선적 폴더 94개 전수** 수정 전/후 대조, 무토큰 후보가 있는 30개 중 **수가 바뀌는 곳 0개**. 즉 이 수정은 고장 상황에서만 작동하고 정상 폴더는 건드리지 않는다.
- **가설이 틀렸던 기록**(남겨둔다). 처음엔 "기준 EDI를 항차 일치분으로 좁히고, 기준이 리스트와 안 겹치면 게이트 해제"를 수정안으로 잡았다. **재현해보니 그래도 9대가 안 들어왔다**(그 9대는 폴더 안 **어느 파일과도** 교집합 0이라 교집합 계열 규칙으로는 구제 불가). 시뮬이 없었으면 안 고쳐진 코드를 배포할 뻔했다. → **교집합은 '남의 배'를 가르는 근거가 못 된다. 적재항(POL)이 근거다.**

### 남은 과제 — 기대 총계의 진짜 기준은 **선석배정목록**
사용자 규칙(2026-08-01 확답): **PCTC는 배정목록의 황색 표시가 양하/선적 확정 수량**(pctc21 배정표에 `XTPG-0028-(P) 61/117/0` 형태로 그대로 나온다 — 양하 61 / 선적 117). **PNCT는 지금처럼 EDI에서 평택분을 찾는 것부터 시작.**
현재 수집기 `state.json`은 **양하 수량(`disc_count`)만** 저장하고 **선적 확정수량은 아예 안 읽는다.** 이걸 읽어오면 기준 EDI가 늦게 와도 `기대 117 / 합본 108 → 9 부족`이 즉시 뜬다. 다음 판 과제(한 판에 하나 원칙).

---

### V9.36 / 콘앱 C7.69 / 수집기 2.17.21 (2026-08-01) — 작업 마무리 시 카드가 '출항시간'으로 전환
- 요청(사용자): "작업이 마무리 될 시점 출항시간만 찾아내서 지금 작업예정시간을 지우고 그자리에 검수앱 콘앱에 표기. 부득이한 사정으로 작업이 늦어지거나 출항 시간이 바뀌면 그때 상황에 맞춰 적용 가능해야 함." 참조 자료원으로 트레드링스 터미널 작업현황을 지정.
- **자료원 확정**: `https://api.tradlinx.com/terminal-work` — 인증 불필요 JSON, `refreshMin=5`. **선박코드(vesselCd)가 우리 코드와 동일**해 매칭이 정확하다(voyage는 선사 시퀀스: `OBWH-088` = 메일박스 `OBWH088(2702W).EDI`). 제공 필드: 작업 시작/종료, 출항 ETD, 양하·적하 진행수, 합계 진행률, `expectHour`에 `(지연중)`.
- **사용자 확정 3가지**: ① 출항시각은 **도선 예보 우선**, 없으면 터미널 ETD ② 전환 시점은 **터미널 진행률 기준**(상수 `WORK_DONE_PCT = 90`) ③ 대상은 **검수앱 항차 목록에 있는 선박만**(터미널에 뜨는 배 전부가 아님).
- 수집기 `collector/terminal_work.py` 신설: API 전체(113건) → Firebase `voyages` 목록의 선박코드로 필터 → `terminal_work/{코드}`에 PUT. run_autoreg·도선예보 뒤에서 호출, 실패해도 사이클 무영향.
- 앱: `fbSubscribeTerminalWork` 구독 → 홈 카드 배지가 `📅 작업일시` → `🚢 출항 <날> <시각> ⚓도선|🏭터미널`로 교체(지연 중이면 `· 지연`). 콘앱 작업 선택 카드도 동일.
- **⚠ 시뮬에서 잡은 결함(가드 신설)**: 터미널 자료는 **선박코드로만** 오기 때문에 그대로 쓰면 **직전 항차의 출항이 다음 기항 카드에 붙는다** — 실제로 OBWH 2703E(예정)에 "출항 어제 19:00"이 찍혔다. → 터미널 작업 **시작시각이 그 항차 작업창(±12시간) 안**일 때만 인정하도록 막았다. 재시뮬 결과 직전 항차 자료 3건 전부 정상 무시.
- 검증: 파서 단위(_dt·_pct·pick) PASS · 실API 113건 중 우리 선박 3척 필터·기록 PASS · 실데이터 15항차 배지 시뮬(오적용 0) · 전환 로직 5종 시뮬(초반/마무리·도선/지연/도선없음/직전항차) 전부 의도대로 · 연막검사 2종 · 콘앱 스크립트 문법.

### V9.35 (2026-08-01) — 홈 카드 작업일시 항상 표시 + 작업일시 기준은 선석배정 (사용자 확정)
> ⛔ **아래 '작업일시의 진실은 선석배정' 규칙은 2026-08-02 사용자가 직접 뒤집었다(폐기).**
> 현행은 **도선 예보 > 선사 일정 메일 > PORT-MIS > 선석배정** + 자력선 안전장치다.
> 이 항목은 경위 기록으로만 남긴다 — **여기 문장을 근거로 되돌리지 말 것.** 위 V9.39~V9.42 항목 참조.
- 증상(사용자 신고): "포트미스 자료가 들어오면 예정일시(작업일시)가 안 보입니다. 그래서 금일 작업인지 명일 작업인지 배정목록에서 찾아 보는 경우가 있습니다."
- 원인: `📅 예정` 배지가 `autoStatus === 'expected'`일 때만 렌더됐다. 수집기가 자료를 받아 '수집중/확정'으로 승격시키는 순간 배지가 사라지고 **등록일만** 남았다(등록일도 같은 📅라 혼동 가중).
- **업무 규칙 확정 — 작업일시의 진실은 선석배정이다.**
  > "작업일시는 선석배정목록이 먼저 입니다. 도선만 해놓고 대기 하는 경우도 있으니까요."
  → V9.34에서 정렬 우선순위를 **도선 예보 1순위**로 뒀던 것을 교정한다. 작업일시·정렬 모두 **선석배정(planDate) > 도선 예보 > PORT-MIS**. 단 항차 상세의 `⚓ 도선 예보` 줄(출항 시각)은 그대로 — 거기선 도선이 확정에 가깝다.
- 수정: ① 배지를 **항상** 표시(기존 예정 배지 자리) ② 입항~출항 둘 다 + 오늘/내일/요일 라벨 ③ 출처 항상 표기(📋배정·⚓도선·🚢신고) ④ 출항 지난 항차는 회색 ⑤ 등록일 줄은 `🗂 등록`으로 라벨 구분.
- 파급 지도: `_etaMs/_etdMs`는 HomePage 한 곳에서 생성돼 정렬과 이 배지가 함께 쓴다(복제본·타 화면 없음). `_etaSrc` 추가는 가산이라 기존 동작 불변.
- 검증: 실데이터 15항차 전건 배지 생성 확인(전부 📋배정), 정렬은 MCAP이 도선 04:30 대신 배정 08:00 기준 3위로 이동 — 규칙대로. 연막검사 2종 통과, 번들 APP_VERSION 확인.
- 교훈(사용자 지적): **표시 취향은 착수 전에 반드시 확답을 받는다.** 이 건도 클로드가 확인 없이 구현에 들어갔다가 우선순위가 반대임을 뒤늦게 알았다 — 물었기에 재작업을 면했다.

### V9.34 (2026-08-01) — 홈 정렬이 '지난 기항' 레코드에 오염되던 문제
- 증상: MCAP 629N이 홈 맨 아래(16위). 실제로는 **08-02 04:30 입항 예정**인 다가오는 항차였다.
- 원인(`HomePage.jsx` `_etaMs/_etdMs`): 종전 식은 `(pm ? pm.eta : null) ?? planDate` — **PORT-MIS 레코드가 있으면 그것만** 봤다. MCAP은 같은 콜사인(V2EE9)의 **지난 기항 레코드(6/28~6/29)** 가 남아 있어 "출항 지남"(rank 2)으로 판정됐다. OBWH 2703E·RZOR R082E·TNJP 26355E 같은 신규 예정 키는 PORT-MIS 매칭이 없어 planDate로 정상 정렬됐다 — **MCAP만 틀렸던 이유가 이 대비로 확정된다.**
- 수정: 후보를 셋(도선예보 > 선석배정 planDate > PORT-MIS) 모아 **① 아직 안 끝난 것(etd 없음 또는 미래)을 우선순위 높은 순으로 채택 ② 전부 지났으면 가장 최근 것**. 도선 예보를 1순위로 둔 근거는 "포트미스는 예보 성격, 도선사협회는 확정과 비슷"(사용자 확정 2026-08-01).
- 파급 지도: `_etaMs/_etdMs`는 HomePage 정렬 한 곳에서만 생성·소비(229·230행 → 238행). 복제본·타 화면 사용 없음. VoyagePage의 `parsePortMisDateTime`은 별도 계산이라 무관.
- 파급 검증(실데이터 전/후 대조): MCAP 15위 → **2위**, 나머지 14개 항차의 상대 순서는 그대로. 의도한 것만 바뀜.
- 교훈(사용자 지적): **근거 하나로 단정하지 말 것.** 이 건에서 클로드는 PORT-MIS 한 줄만 보고 YKTD를 "지난 항차"로, `completed 0` 한 칸만 보고 "검수 기록 없음"으로 단정해 두 번 틀렸다(실제로는 어제 작업 완료, 실번호 213/213 기록됨). **서로 다른 출처의 근거 2개 이상**을 확인하고, 하나뿐이면 불확실성을 밝힌다. 사용자가 "맞습니까?"라고 되물으면 그건 신호이니 같은 답을 반복하지 말고 근거를 더 찾는다.

### V9.33 / 콘앱 C7.68 / 수집기 2.17.20 (2026-08-01) — 평택도선사회 도선 예보 표시
- 요청(사용자): "포트미스 예약해서 하는 것처럼 도선 예보 1-2일치를 매일 검수앱·콘앱 항차 카드에 보여달라."
- **원칙 확정(사용자 확답)**: "포트미스는 예보 성격이고 도선사협회는 확정과 비슷하다." → PORT-MIS 값(port_mis_data)은 **덮지 않고** 별도 노드 `pilot_forecast/{선박코드}`에 기록, 카드에는 두 줄로 나란히 표시.
- 자료원: `http://www.ptpilot.co.kr/forecast` (예보현황조회). 표 15컬럼 고정 — 번호·도선상태·도선사·일자·시간·선명·구간from·구간to·접안·톤수·홀수·대리점·예선·강취·**호출부호**. 한 페이지에 당일+익일이 함께 나온다(1-2일치 자동 충족).
- **방향 판정 규칙(확정)**: 외항 도선점 = `IPA`·`JANG`. to가 외항점 = **출항**, from이 외항점 = **입항**, 둘 다 선석 = **이동(shifting)**. 선석은 출항이면 from, 입항이면 to.
- 수집기 `collector/pilot.py` 신설: 크롬(CDP)으로 페이지 열어 `호출부호`가 든 표를 통째로 긁고 파싱 → **관할 선박만**(VESSEL_CODE 매칭, 호출부호 우선) → 지난 시각 제외 후 `pilot_forecast/{code}`에 PUT(nextDep/nextArr/berth 포함). run_autoreg 뒤에서 호출, 실패해도 사이클 무영향(자료 보존).
- 검수앱: `fbSubscribePilotForecast` 구독 → VoyagePage PORT-MIS 카드 아래 `⚓ 도선 예보 입항 08-02 04:30 (E7) · 출항 08-02 23:00 (E7)` 줄. 콘앱: 작업 선택 카드 각 항차에 같은 한 줄.
- 시뮬(실데이터 57행, 사용자 제공 08-01 예보 원문): 파싱 57/57 PASS · 방향 판정 3종 PASS · 관할 5척 매칭(TNJP·RZOR·XTPG·PCSG·MCAP) PASS · 카드 문자열 5/5 PASS · 콘앱 스크립트 문법 PASS · 렌더 연막검사 2종 PASS.
- 주의: 사이트가 간헐적으로 응답하지 않는다(작업 당시 클로드 쪽 접속 전부 타임아웃, 사용자 PC에서는 정상). 실패 시 그 사이클만 건너뛰고 기존 값 보존한다.

### 수집기 2.17.19-02 (2026-07-31) — 재검색 폭주 차단(합본·완성우선 모두)
- 증상: 사이클이 몇 시간째 안 끝나 run_autoreg(검수앱 등록)에 도달 못 함 → 메일박스에는 자료가 있는데 앱은 0건.
- 원인: `merge_all`·`merge_priority` 둘 다 **부족 항차마다** 받은편지함 전체(700여 통)를 재검색. TNJP는 옛 항차 폴더가 40개라 한 선박에서만 수십 번.
- 수정: 재검색을 ① **선석배정 현재 짝 항차에만** ② 사이클당 **선박별 1회**로 제한(두 함수 동일 규칙). 로그 `· 26332W 부족 229개 — 현재 짝 아님(지난 항차), 재검색 생략`.
- 결과: 사이클 40분에 완주 → autoreg done 14건. DXQD 2630E/2630W(0→241·55), TMPZ 2023W(36→301), KSKM 2615S(0→EDI 278), TNJP 26354W(0→128) 등 등록 완료.

### V9.32-02 (2026-07-31) — 리스트 저장 undefined 거부 수리 (업로드 "처리 중" 정지의 진범, 재현으로 확정)
- 증상: V9.31·V9.32·V9.32-01을 거쳐도 OBWH 2702W 선적 리스트("CLL Data_vgm")가 "1개 파일 처리 중..."에서 멈춤.
- 재현: 사용자 승인 하에 클로드 크롬 탭에서 라이브 앱에 실물 파일 업로드 → 동일 정지 재현 → unhandledrejection 후킹으로 포착:
  `set failed: value argument contains undefined in property 'voyages...records.SPSU2020025.dgc'`
- 원인: V9.21-03의 `dgc: rmkDgc || undefined`(un·mkcon 동일) — REMARK에 위험물 표기가 없으면 **모든 신규 레코드에 undefined**가 박힘. Firebase RTDB `set()`은 undefined가 든 객체를 통째로 거부한다. 평소엔 ① 기존 리스트와의 스마트 병합(`v == null`이면 skip)이 undefined를 걸러 주고 ② EDI 불일치가 있으면 충돌검토 경로로 빠져서 숨어 있었다. 이번엔 **빈 리스트 + 전건 신규 + EDI 283대 전건 일치(충돌 0)** → 직행 저장 → 거부 → `await`가 던진 예외를 아무도 안 받아 함수가 조용히 죽고 "처리 중" 잔류.
- 수정: ① `parseListExcel`의 dgc/un `|| undefined` → `|| ''`, mkcon → `|| false` ② `firebase.js chunkedReplace`에 JSON 왕복 sanitize(전 저장 경로 안전망 — 수집기 REST의 JSON.stringify와 동일 의미론) ③ VoyagePage 직행 저장을 try/catch로 감싸 실패 시 "❌ 리스트 저장 실패 — 사유" 표시.
- 시뮬: 실물 3종(vgm 2차 283 · PCSZ 619 · TMPZ 35) 레코드 undefined 0건 · sanitize 동작 확인 · 연막검사 2종 통과.
- 교훈: **화면 재현이 가능하면 코드 추리보다 재현이 빠르다.** V9.31(덱플랜 전수 파싱)·V9.32(동적 import)·V9.32-01(CDN 무한대기)은 실존하던 결함이지만 이번 정지의 진범은 아니었다. async 핸들러의 마지막 `await`까지 전부 실패 경로를 화면에 드러낼 것.

### V9.32-01 (2026-07-31) — 엑셀 라이브러리 번들 우선 로드 + CDN 타임아웃 (업로드 정지 최종 방어)
- 증상: V9.32 배포 후에도 리스트 업로드가 "처리 중"에서 멈춤(EDI는 정상, 엑셀만).
- 원인: `loadSheetJS()`가 **CDN(jsdelivr) 한 곳을 타임아웃 없이** 기다렸다. 새로고침으로 캐시가 비워진 뒤 CDN이 무응답이면 `await`가 영원히 안 풀린다 — 에러도 없다.
- 수정(`src/utils.js`): ① 번들 xlsx를 동적 import로 **1차 사용**(의존성에 이미 있음, planedit.entry와 동일 경로 — 네트워크 불필요, 오프라인 가능) ② 실패 시 CDN 2곳(jsdelivr→cdnjs)을 **10초 타임아웃**으로 순차 시도 ③ 전부 실패 시 명확한 에러를 던져 화면에 표시. `mixerUpload.js`의 PDF.js CDN 로더도 동일 계열이라 15초 타임아웃 + 실패 프라미스 캐시 비움(종전엔 한 번 실패하면 재시도 불가).
- 시뮬: 실물 4종(TMPZ CLL 35 · PCSZ CLL 619 · R068W 185 · MSC 양하 216) 번들 경로 파싱 PASS(30~192ms) · 폴백 경로 CDN 2곳 시도 후 에러 PASS · CDN 성공 경로 PASS.
- 교훈: **외부 CDN `await`는 반드시 타임아웃 + 대체 경로.** 번들에 이미 있는 라이브러리를 CDN으로 다시 받지 말 것.

### 수집기 2.17.19-01 (2026-07-31) — 합본 재검색 사이클당 선박별 1회 제한 + 사이클 시작 하트비트
- 증상: 검수앱 홈 "수집기 끊김 · 174분 전". 실제로는 수집기 생존(state.json 계속 갱신) — **합본(merge) 단계가 몇 시간째 안 끝나** run_autoreg·하트비트에 도달 못 함.
- 원인(`merge.py merge_all`): 부족 항차**마다** 받은편지함 전체 재검색(메일 700여 건 분석, 수 분). TNJP처럼 미도착(LYG 리스트) 항차가 여럿이면 항차 수 × 수 분 = 사이클이 사실상 무한.
- 수정: ① `merge_all`에서 재검색 수집은 **사이클당 선박별 1회만**(같은 선박 나머지 항차는 재검색 후의 합본 결과 그대로, 다음 사이클 재시도) ② `app_gui` 감시/완전자동 루프 **시작 시에도 하트비트** 기록 — 합본이 길어도 홈 '끊김' 오표시 방지.
- 적용: 디스크 반영 완료. **수집기 재시작 시 적용**(실행 중 프로세스는 옛 코드).

### V9.32 (2026-07-31) — 업로드 "처리 중" 조용한 정지 수리 (OBWH 2702W, 원인 확정)
- 증상: 리스트 업로드가 "2개 파일 처리 중…"에서 무한 정지. 에러도 없음. **실물 파일 파싱은 정상**(2702W CLL 256·283대, 각 50~80ms — 파서·덱플랜 검사 모두 무죄).
- 원인: 업로드 함수 앞부분의 **동적 import(mixerUpload/gemini/utils)가 try 밖**에 있었다. 하루에 여러 판을 배포하면 브라우저 캐시가 옛 청크를 가리켜 `Failed to fetch dynamically imported module`이 나고, 예외가 함수를 끊어 상태 문구가 "처리 중"에 그대로 남는다 — 사용자에겐 앱이 죽은 것처럼 보인다.
- 수정: 동적 import를 try로 감싸 실패 시 "앱이 새 버전으로 갱신되었습니다 — 새로고침 후 다시 올려 주세요" + 원인 메시지 표시.
- 교훈: **await 동적 import는 반드시 try로 감싸고 실패를 화면에 드러낸다.** 잦은 배포 환경에서 이 부류는 반드시 재발한다.

### V9.31 (2026-07-31) — 리스트 업로드 "처리 중" 정체 수정 (OBWH 2702W 신고)
- 증상: 선적 리스트 2파일 업로드가 "2개 파일 처리 중…"에서 멈춘 듯 오래 걸림("이런 적 없이 바로 읽었는데").
- 원인: V9.22에서 넣은 **RZOR 덱 플랜 감지**가 모든 리스트 파일을 `XLSX.read(cellStyles:true)`로 통째 파싱한다. 스타일 전체 파싱은 큰 CLL(.xls)에서 수십 초~분이 걸린다 — RZOR 전용 기능이 전 선박 업로드를 느리게 만들고 있었다.
- 수정: 파일명이 `rzdf`/`deck`을 포함하거나 선박이 RZOR일 때만 덱 플랜 검사. 그 외는 곧바로 리스트 파싱.
- 교훈: 특정 선박 전용 감지를 공통 경로 앞단에 두지 말 것 — 게이트를 먼저 건다.

## V9.30 — 메일박스·항차 데이터 전수 점검 (2026-07-31, 사용자 지시 "세밀하게 점검하고 완벽하게 수정")

**점검 범위.** MAILBOX 1,630개 파일(선박 폴더 전수) + 등록 16항차 × 양하/선적의 ediContainers·records·raw EDI 3중 대조.

**① MCAP 629N 정정 (앞선 V9.29 진단 수정).** "POD가 천진이라 TS 62대가 빠졌다"는 진단은 **틀렸다**. BAPLIE 원문은 `LOC+11+KRPTK`로 **평택을 양하항으로 적는다**(환적이어도 이번 배에서 내리는 항이 POD). 현재 파서로 원문을 파싱하면 평택 278 정확. 진짜 원인은 **Firebase ediContainers가 구본 EDI로 저장된 것** — 62대 POD를 CNTXG로 갖고 있었다. 62대 POD 갱신 → 278 일치. (V9.29의 "리스트 등재분도 평택분" 규칙은 EDI 지연·결손 대비 안전망으로 유지.)
   - 도메인 규칙 확정: **환적(TS) = 배를 갈아탐(양하 1 + 선적 1, 부두 경유)** ↔ **쉬프팅 = 같은 배 안 자리 옮김(SHIFT 별도 계상)**.

**② 리스트 규격 헤더 `SzTp` 미인식 (신규 결함).** 천경 CDL(`CntrNo,OPR,POL,POD,SzTp,Wgt,F/E`)의 규격 컬럼을 파서가 못 읽어 XTPG 534E 양하 **57대 전부 규격미상**. type_i에 `sztp|tpsz|sz tp` 추가 → 57/57 인식, records 패치.

**③ LOADLIST 옛 등록분 규격 누락.** 수집기 합본(LOADING_LIST 시트)에 ISO/F/E가 있는데 V9.20-02 이전 등록분은 컨번호만 저장돼 있었다 — XTPG 108·TMPZ 36·MCAP 16. 현재 코드로 재파싱해 패치(XTPG 규격미상 108→13, TMPZ 36→0). 남은 것은 원본에도 빈 칸.

**④ TMPZ F/E 'L' 18건.** IFCSUM의 Laden 표기가 그대로 저장돼 앱 F/E 체계와 어긋남 → 'F'로 교정(현 파서는 이미 정규화).

**⑤ 메일박스 오배정 9건 이동.** DXQD/2630W에 ATPR 3건, KSKM/2615S에 TNJP 6건. (UNKNOWN/_마감텔리의 타선박 파일 19건은 마감텔리 보관함 성격이라 유지.)

**⑥ 수집기 v2.17.18 동반 수리** — 항차 판독 3단(파일명 뒤글자 허용·엑셀 내용 VSL/VOY·메일 제목), JDJC/DJCN 별칭, MCAP 629N 폴더 69건 정리.

**남은 관찰 대상(정상이거나 원본 결손).** MCAP 631S 선적 16대(선사 파일에 규격·F/E 자체가 없음, EDI 대기) · XTPG 선적 13/26(원본 빈칸) · YKTD 리퍼 온도 미입력 12·KSKM 1(현장 확인) · KSKM·TNJP raw는 ASC 형식이라 parseBAPLIE로는 0(앱은 parseAscFile 사용 — 실害 없음).

## V9.29 — 양하 평택분에 TS(환적) 화물 포함 (2026-07-31, MCAP 629N — 오늘 최대 발견)

**증상.** 터미널 화면 **278**/350, 선사 양하리스트 **278**대인데 앱은 **216**대. 매칭 0·누락 216.

**원인 (실데이터).** 양하 EDI 960대 중 POD=KRPTK가 216. 차이 **62대**는 POD가 **CNTXG(천진)·엠티**인 화물 — 선사 메일(L77-629N-KRPYOTM-Final discharge, 머스크) 지시: *"declare 62 x 40'HDRY empty ex L77-629N as **TS** in customs manifest ... create **load plan in KRPYOTM** for TS"*. **평택에서 내렸다가 다시 싣는 환적 화물**이라 POD는 천진이지만 평택 검수 대상이다. 앱의 평택분 판정이 `양하=POD 평택`뿐이라 62대가 통째로 누락됐다.

**수정.** `isPtk` 확장 — 양하도 **리스트(records) 등재분을 평택분으로 인정**(선적에 이미 있던 원칙과 통일). 선사가 양하리스트에 올린 컨은 평택에서 내리는 것이 진실. 진단 배너 필터도 같은 판정으로 통일(단일 소스).

**데이터.** StandardDischargeList(머스크, 278행) 파싱해 records 278 등록(ISO·F/E·실번호·리퍼 온도 포함, 리퍼 175). 매칭 0 해소.

**검증.** 실데이터: 구규칙 216 → 신규칙 **278** = 터미널·선사리스트 일치 PASS. 연막검사 2종 통과.

**도메인 규칙 갱신.** 평택분 카운트: 양하 = **POD 평택 ∪ 양하리스트 등재(TS 포함)** · 선적 = 리스트 등재 ∪ POL 평택. (종전 지침서 "양하=POD 평택" 단독 규칙은 이 판으로 교체.)

### V9.28-09·10 (2026-07-31) — TNJP 리퍼 실종 사건 전모 (사용자 신고의 진짜 선박 — 세 겹 구멍)
- 사용자 단서: "메일 내용에 리퍼 언급이 있는데도 이런 실수" — 수집기는 정상(첨부 전부 수집). 구멍은 앱 판정 세 곳.
- ① **isReeferIso**: 높이코드 클래스 [024568L9]에 '3' 누락 → 43R*(연운항 40리퍼) 리퍼 판정 탈락. '3' 추가(-09, FR 충돌 없음 — 4583/4584는 [2]='8').
- ② **isoToLabel**: /^43/→40HC 일괄 규칙이 43RF까지 삼킴 → 필터 칩에 40RF 없이 40HC 101로 뭉개짐. 43R→40RF 선행 규칙(-09).
- ③ **parseListExcel SZ 매핑**: 연운항 CNTR LIST의 축약 SZ(5R/4J/2D/4D/2T)를 몰라 **리스트 118대 전부 iso 미인식** — REMARK의 RF온도·UN·IMDG는 뽑아놓고 rf=false. 매핑 추가(-10): 5R→45R1·4J→45G1·2D→22G1·4D→42G1·2T→22T1.
- 검증(실물 리스트): 118대 전부 iso 인식(45G1/22G1/45R1/42G1), 리퍼 24대 rf+온도(20/23)+UN3480+IMDG9 완비 PASS. ISO 단위시험 7/7(43DC/4300은 여전히 드라이 40HC — 연운항 43=HC 규칙 보존).
- 데이터 백필: TNJP 26354E records 142항목 패치(iso 전수 + 리퍼 24 rf) — 검증 완료.
- ⚠ 교훈 재확인: 큰 base64 손 전사 금지(V8.85 규칙) — 이번에 1글자 어긋나 zip 훼손, computer-use 파일 복사로 우회.

### V9.28-08 (2026-07-31) — 📍 리퍼 위치미상 표시 (TMPZ 2023E — 스캔 중 발견한 별건)
- 실데이터: TMPZ 2023E 양하 리퍼 6대(40RH, -18/-21℃)가 **선사 냉동리스트(冷箱清单.xls)에만 존재** — EDI raw 전문(121컨)에 컨번호 자체가 없음(선사 EDI 누락). 위치가 없으니 카고플랜에 못 그림 — 앱 버그가 아니라 선사 자료 결손.
- 문제는 앱이 이를 **숨기던 것**: 요약은 리퍼 6대 인식하는데 어디에도 "위치를 모른다"는 표시가 없었다.
- 수정: 요약 카드 리퍼 칩에 **📍위치미상 N**(EDI·실체 위치 모두 없는 리퍼) 추가, 있으면 칩 빨강. 검수원이 현장에서 찾아야 할 대상임을 드러낸다.
- 같은 날 YKTD 4530 건(-07)과 별개 사안 — 하나는 표시 판정 버그, 하나는 선사 자료 결손의 가시화.

### V9.28-07 (2026-07-31) — 카고플랜 R마크: 비표준 리퍼 코드 인정 (YKTD 2612E 사용자 신고)
- 신고: "리퍼가 있다고 인식한 것 같은데 리스트 항목에도 없고 카고플랜에도 표시가 없다."
- 실데이터: YKTD 2612E 양하 12대 — EDI ISO '4530'(비표준 40ft 리퍼 HC, 온도 -18~-25 동봉, rf=true), 세관 CLL엔 45R1로 실재. **진짜 리퍼 맞음.**
- 원인: cargoPlanCore.defaultGetSelfMark가 iso[2]==='R' 문자만 판정 — '4530'은 '3'이라 R마크 탈락. 카운트(isReeferContainer: rf 플래그 인정)와 카고플랜 판정이 서로 달랐다.
- 수정: R마크 판정을 typeChar 'R' ∥ c.rf ∥ isoToLabel 라벨 'RF' 종결로 확장 — 카운트·리스트·카고플랜 단일 기준.
- 참고: 4530류=40ft 리퍼 HC 판정은 V9.04-04 각주·DJCF 0148N 실측(4530 46대=냉동군)과 일치.

### V9.28-06 (2026-07-31) — POD 구역: 검수원 경로 차단 (사용자 역할 규칙 확정 — 이번 포트 섞임 건 한정)
- 사용자 확정: "포트가 섞일 때도 있다. 다만 **계획된 것이 아니라면 실리면 안 된다.** 불가피한 수정은 **수석검수사가 베이상세편집 또는 EDI 수정**으로. **일반 검수사는 계획에 없는 걸 하면 안 된다.**"
- 적용(이번 건 한정): 검수원 경로(🅿 빈자리 배치 커밋·상세 모달 좌표 입력)의 POD 구역 불일치 = ⛔ **차단** + "불가피한 변경은 수석검수사가 베이상세편집 또는 EDI 수정으로" 안내. -05의 경고-허용에서 격상.
- 수석 경로(베이상세편집 placeAt)는 그대로 자유 — 수석의 교정 권한.

### V9.28-05 (2026-07-31) — POD 구역 경고 (사용자: "한 건이 잘못됐지만 잡을 기회가 여러 번 있었는데 다 놓쳤다")
- 사고의 전모(사용자 판독): FBIU5086535(40ft·CNTAO)와 SEGU3445581(20ft·CNSHD)의 **자리 맞바꿈 한 건** — 잡을 기회 넷: ①20ft 자리에 40ft(→ V9.28-04 인접검사) ②40ft 자리에 20ft(→ V9.28-04 콘홀검사) ③④서로 포트가 다른 구역(→ 이번 판).
- utils **podZoneMismatch(c, bay, tier, others)**: 같은 구역(±1베이·데크/홀드 동일)의 다수 POD(3대 이상 근거)와 컨 POD가 다르면 경고 재료 반환. **차단 아님 — 확인 후 허용**(포트 섞임은 물리 불가가 아니고, 실물이 이미 거기 실린 오선적을 기록해야 할 때도 있다).
- 적용: 빈자리 배치 커밋·상세 모달 좌표 입력 — "⚠ 이 구역은 CNSHD 화물 자리입니다(주변 35대). ○○의 포트는 CNTAO입니다."
- 실데이터 검증: FBIU@20구역 경고(CNSHD 35)·SEGU@24구역 경고(CNTAO 46)·각자 제자리(24-06-90/21-03-82)에선 침묵 — 4/4 PASS.
- 남은 재배치(미배정 대기): FBIU→24-06-90, SEGU→21-03-82가 데이터상 정답 — 새 3중 그물(규격·인접·포트) 흐름으로 재배치 예정.

### V9.28-04 (2026-07-31) — 인접 슬롯·40위20 물리 검사 (사용자 실테스트가 잡아낸 구멍 2개)
- 실측 ①: FBIU5086535(45RE)를 20-03-82에 받아줌 — 40ft는 19·21의 03열 슬롯을 차지하는데 **19-03-82엔 이미 20ft(SITU2542374)** → 물리 충돌. ②: SEGU3445581(22GE)이 25-06-90 — 아래 24-88-06이 40ft 지붕 → **40피트 위 20피트 불가(콘 홀 없음)** 위반.
- utils **slotAdjacencyError(c, bay, row, tier, others)** 신설: ⓐ 40/45ft@짝수 — 양옆 홀수 같은 열·단 점유 시 차단 ⓑ 20ft@홀수 — 인접 짝수 40ft가 그 자리 차지 시 차단 ⓒ 20ft@홀수 — 자기 받침 없고 아래 단이 인접 짝수 40ft면 차단.
- 적용: onCommitMove(빈자리 배치)·상세 모달 좌표 입력 + **BayPlan 📦+ 후보 표시가 같은 함수 사용**(표시-저장 단일 소스 — 표시는 되는데 저장이 막히거나 그 반대인 어긋남 원천 봉쇄).
- 데이터: FBIU·SEGU 미배정 복귀(새 가드 흐름으로 재배치). 검증: 두 사고 좌표 차단·정상 좌표(24-06-90) 허용·실렌더 후보 37→35칸·합성 4→2칸 PASS.

### V9.28-03 (2026-07-31) — 베이 탭 크래시 긴급수정 + BayPlan 연막검사 신설
- 사고: V9.28-02의 occupiedTierSet이 BayPlan **페이지 하위 스코프**에 들어가 `containers`가 없음 → 베이 탭 여는 순간 "containers is not defined" 앱 전체 크래시. 연막검사가 BayGridEditor만 그려서 통과됨(V9.23-06과 같은 부류 — 그리는 컴포넌트만 잡는다).
- 수정: 같은 스코프의 bayGroups로 계산. 실데이터(STSE 405컨) BayPlan 실렌더 — 오류 0 · 📦+ 후보 37칸.
- 재발 방지: build.sh에 **BayPlan 연막검사 추가**(합성 사전+컨, 미배정 pendingMove 상태로 실렌더 — 오류/빈 화면/📦+ 0이면 빌드 실패). 문턱값 함정: 합성 화면은 282자라 300자 기준이 오탐 — 200자로.

### V9.28-01·02 (2026-07-31) — "빈자리" 정의 2회 교정 (사용자 정정 — 도메인 규칙로 승격)
- V9.28 오류: 격자의 모든 빈 칸에 📦+ — "계획에 없는 빈곳이 아니다"(사용자). → V9.28-01: EDI 계획 좌표 중 빈 곳으로 제한 — 실데이터 검산 1곳뿐(계획 좌표는 거의 다 차 있음) = 쓸모없음. 사용자: "미배정이 14대면 빈 곳도 14곳. 검산 필요합니까? 빈 자리를 찾아야 하는 거죠."
- **확정 정의 (V9.28-02): 빈 자리 = 화물이 실리는 단(tier)의 빈 칸.** 88단이 만적인데 86단이 비어 있으면 그 86단 빈 칸들이 곧 (계획이 엠티용으로 비워 둔) 빈 자리다. 같은 베이·같은 단에 화물(계획·실체)이 하나라도 있으면 그 단의 빈 칸을 📦+ 후보로 켠다. 완전히 빈 단(92단 등)·계획 밖 공간은 제외. 후보가 실물보다 몇 칸 많을 수 있으나 검수원이 실물 보고 고른다 — 후보를 못 내놓는 것(V9.28-01)보다 낫다.
- 검증: 어제 강제입력 교정 자리 12/12가 후보로 표시됨 (16-86 일곱·28-82 셋·24-90 둘).

## V9.28 — 🅿 미배정→빈자리 배치 흐름 (2026-07-31, 사용자 역할 정의 확정)

**사용자 확정 원칙 (도메인 규칙로 승격).** "미배정 = 자리 지정이 안 됐다 = 어딘가 빈자리가 있다. **앱이 빈자리를 보여주고 그 빈자리에 맞는 컨을 넣는 건 검수원 작업 화면의 일**이다. 수석 베이상세편집은 **오입력 교정용**(1234↔4567 맞바꿈처럼 검수사가 잘못 입력한 걸 제자리 찾아주는 기능)이다." — "작업할 베이 없음+미배정 N"을 정상 상태라 한 클로드 판단을 사용자가 정정.

**구현.** 기존 pendingMove 배관(M4.9f: 빈 셀 📦+ 클릭→fbSetActualPosition) 재사용:
- 작업 패널 미배정 목록(자동 가이드 2곳·수동 미지정 그룹 화면)에 **「🅿 배치」 버튼** — 누르면 pendingMove 설정 + 베이 탭으로 이동, 빈 칸(📦+)을 골라 배치.
- BayPlan moveTargetBay: 본위치 없는 미배정 컨은 **규격으로 짝/홀 판정** — 40/45ft→짝수 베이 페이지만, 20ft→홀수(단독 짝수 허용), 규격 미상→배치 불가(안전측).
- onCommitMove에도 bayParityError 가드(V9.27 원칙 — 이 경로도 좌표 기록처).
- 문구 정리: "위치는 베이상세편집에서 지정" → 배치 흐름 안내로 교체 (역할 분리).

**검증(실데이터 STSE 2658W 미배정 14대).** 수동→미지정 그룹→🅿 배치 14개 노출·콜백 정상 PASS · 연막검사 통과.

**부수 기록.** 좌표중복 2건(28-07-82 SKHU/BMOU · 11-03-82 LYGU/TIIU)과 SITU2466905의 11-08-86(11베이는 4열뿐 — 존재하지 않는 열)은 데이터로 확정 불가 — 재선적 검증에서 실물대로 재배치 대상.

## V9.27 — ⛔ 40/45피트 홀수 베이 기록 원천 차단 (2026-07-31, 사용자 확정 "코끼리를 냉장고에")

**사용자 판정.** "경고도 그렇고 아예 들어갈 수 없는 자리다. 어제 같은 일들이 벌어진 것이고, 사용자 잘못이 아니고 앱의 버그다." — 물리 불가 좌표는 확인창(허용)이 아니라 **차단**. 중복 자리(V9.24, 판단 여지 있음 — 경고 후 허용)와 구분되는 원칙.

**전수 감사 (전 15항차 × 양하/선적, 유효 위치 기준).** 위반 = 40/45ft가 홀수 베이: **STSE 2658W 선적 11건뿐** — 전부 어제 강제 입력분. EDI 계획엔 위반 0 (파서 정상). 오탐 1(ISO 'DC2E' 역순 표기 — 20ft).

**데이터 교정 (전부 빈자리·스택 지지 대조로 확정).** 15베이 86단 7대→16베이(16-86 빈 열 01·02·03·04·05·07·09가 일곱 대 열과 정확 일치, 88단 만적 스택 받침) · 27베이 82단 3대→28베이(01/03/05 빈자리) · 23-04-90 1대→24베이(04 빈자리). PATCH 후 재감사 잔존 위반 0.

**차단 지점 (bayParityError(c, bay) 단일 유틸 — isoToLabel 40/45 판정, 미상 ISO는 차단 안 함).**
- planEditCore.placeAt/placeMany — 베이상세편집·빈자리 픽커·플랜 편집 전부의 관문 (pair 경로는 사이즈로 짝수 강제라 기존대로).
- ContainerDetailModal 좌표 직접 입력 — alert ⛔ 후 저장 불가 (어제 사고의 실제 통로).
- PositionEditModal 확정 — 직접 입력·슬롯 그리드 공통 최종 관문.
- GuidedWorkPanel 재배정은 EDI 예측 자리로만 가므로 해당 없음.

**시뮬 PASS.** bayParityError(45GE@27=차단/45GE@28=통과/22GP@27=통과/45RE@15=차단) · placeAt 40@27 거부·40@28 허용·20@27 허용 · placeMany 40@15 거부. 함정 기록: buildState는 listCns 미지정 시 전 컨을 통과 고정으로 잠근다(시뮬 시 listCns 필수).

## V9.26 — 검증 모드 ✂️ 부분 취소 (2026-07-31, 사용자: "일부분이 잘못되었을 때 전체를 수정할 필요는 없다")

- 검증 패널에 부분 취소 격자 신설: 모드(양하/선적) → 베이 칩(완료 수 표기) → 완료 컨만 셀로 그린 미니 격자(단×열).
- 선택: PC는 누른 채 **드래그**(mousedown+mouseover), 폰은 탭 토글. 초록=완료, 빨강=선택.
- 취소 정책은 단건 취소(fbCancelComplete)와 동일 — 완료 해제 + 위치 원계획(bay_orig) 원복 (2026-07-08 사용자 확정 정책 일관).
- 검증(실데이터 STSE 2658W 선적 391완료): 베이 칩 17개(B1 26 ~ B28 46) 정확, B1 격자 26셀, 드래그 3셀 선택 → "선택 3건 취소" 버튼 PASS. 연막검사 통과.
- 시뮬 함정 기록: jsdom에서 onMouseEnter는 dispatch 'mouseenter'로 안 걸린다(비버블) — React가 mouseover로 구현하므로 'mouseover'를 쏴야 한다. 실브라우저 드래그는 정상.

## V9.25 — 🧪 검증 모드(테스트 랩) 신설 (2026-07-31, 사용자 요청 "저만 보이게")

**목적.** STSE 2658W를 아카이브에서 복원해 새 기능(빈자리 흐름·중복 가드)을 실항차로 재검수하려는 요구. "선적을 한번에 취소시키고 다시 테스트."

**구현.** 항차 화면 하단(출력 버튼 아래)에 🧪 검증 모드 버튼 — **검수원 '김성일' 선택 시에만 렌더**(다른 검수원에겐 코드상 노출 자체가 없음). 패널 기능:
- 모드별(양하/선적) **검수확인 전체 취소** — completed 노드 삭제 + 마감 플래그(loadingDone/dischargeDone·At) 해제.
- 옵션(기본 켬): **수동 배치·임시창고 기록 원복** — records의 bay_actual/row_actual/tier_actual/actual_at/actual_by 일괄 null (임시창고 __STG__ 포함, 원계획 복귀).
- 리스트·EDI·실번호·X-RAY 기록은 보존. 이중 confirm + 실행 결과 건수 로그.

**구조.** firebase.js `buildBulkCancelPatch(records)`(순수 함수) + `fbBulkCancelComplete(voyageKey, mode, {resetActuals})` 멀티패스 update 1회(원자적). components/TestLabModal.jsx 신설.

**검증(실데이터).** 복원된 STSE 2657E(2658W 선적) records로 빌더 시뮬 — 수동 배치 16건 감지, 패치 경로 80개 정확 · 빌드 연막검사 통과 · 번들 grep(검증 모드·김성일 게이트·전체 취소 문자열) 확인.

## V9.24-01 — 베이플랜 X 규칙 통일 (2026-07-30, TNJP 26354E 사용자 신고)

**신고.** "(22)23 베이플랜에서 컨 없는 곳(82단 01·03·05열)에 X. 40ft 짝꿍 홀수베이엔 X가 없다. 베이플랜·카고플랜·베이상세 동일해야 하는 것 아닌가."

**원인 (실데이터 재현).** 베이 탭 BayPlan.jsx의 xMarks(M6.2~M6.7)만 "짝수 **열**(row)의 40ft가 옆 홀수 열(row-1)을 차지"라는 추정 규칙 — 열은 배 폭 방향이라 물리적 잠식이 없다. TNJP 22베이 82단 40ft(00/02/04/06열)가 01/03/05열에 가짜 X 3개를 만든 것(00열은 -1 없음이라 제외 — 스크린샷과 정확히 일치). 카고플랜(cargoPlanCore)·베이상세편집(adjMap)은 이미 올바른 규칙(단독 홀수 박스에서 인접 짝수 베이 40ft 잠식 X).

**수정.** BayPlan xMarks를 동일 규칙로 교체 — 페어 페이지 X 없음(40ft가 병합 박스에 그려짐), 단독 홀수 페이지에서 인접 짝수 베이(±1) 40/45ft의 같은 단·열에 X(자기 컨 점유 칸 제외).

**검증 (TNJP 26354E 실데이터).** (22)23 X 0(가짜 3개 소멸) · BAY 21 단독 X 19 — 베이상세편집 렌더 실측(bge-x 19개)과 정확 일치. 세 화면 통일.

## V9.24 — 자리 중복 가드 (2026-07-30, STSE 2658W 마감 후 사고 복기)

**사고.** 마감에 몰린 현장에서 미배정 15대를 좌표 강제 입력으로 완료 처리. 결과: **BAY 28/07열/82단에 두 컨(SKHU9831210 실체 · BMOU5940993 계획, 둘 다 완료)** — 아카이브 실데이터 전수 감사로 확인(중복 1곳, 그 외 404대 정상). 사용자: "두 컨테이너가 같은 자리에 있을 수 있습니까? 안 맞는다고 블럭 처리하면 수정이 안 됩니다."

**재현으로 가른 것.** 사고 직전 상태(수동 배치 16건 제거)를 실데이터+실사전(STSE 22베이)으로 -08 코드에 태워 실렌더 — 임시창고 14 · 🅿빈자리 329곳 · 베이 목록 전부 정상. **-08 로직 결함이 아니라 현장 폰이 20:02 배포를 못 받은 상태(강제 입력 20:12~14)였다.** 남은 진짜 결함은 중복을 막지도 보여주지도 않는 것.

**중복의 통로.** planEditCore.placeAt/placeMany는 점유 시 맞교환/거부라 안전. **ContainerDetailModal 좌표 직접 입력(fbSetActualPosition 직행)** 이 점유 검사 없이 저장 — 사고의 SKHU(15:07)가 이 경로.

**수리 3종 (하드 블럭 금지 — 사용자 원칙 "블럭하면 수정이 안 된다").**
1. 모달 좌표 저장 전 점유 확인 — 대상 자리를 다른 컨이 실체/계획으로 쓰고 있으면 ConfirmModal "⚠ 자리 중복 … 그래도 저장" 확인 후에만 저장. 임시창고(__STG__) 점유자는 제외.
2. 요약 카드 🔴 자리 중복 N곳 칩 — 자리키+컨번호 나열. 실체 우선 유효 위치 기준, '00' 좌표 제외.
3. 마감 점검 체크리스트 '🔴 자리 중복 — 즉시 정리' 항목 — 마감 전에 반드시 눈에 걸리게.

**검증.** 실데이터 시뮬: 사고 후 상태에서 중복 28-07-82 [BMOU·SKHU] 정확 검출, 모달 가드가 BMOU(계획) 경고 PASS · 연막검사 통과 · 재현 렌더(405대·임시창고14·빈자리329) 유지.

**부수 함정 수정.** build.sh 연막검사 출력이 고정 /tmp/_smoke.js — 타 세션 잔재(다른 uid 소유)가 있으면 쓰기 실패로 검사가 통째로 건너뛰어졌다(⚠ 건너뜀 로그 실측). mktemp로 교체.

**남은 숙제(기록).** 현장 마감 시각과 배포 시각이 겹칠 때 — 배포 직후엔 폰 새로고침을 사용자에게 명시 요청할 것. 강제 입력분 BAY 28/07/82는 다음 접안 시 실물 확인 대상.

## V9.23-02 ~ V9.23-05 — 베이상세편집 신고 4건 종결 (2026-07-30, 항차 STSE2658W)

**신고 원문.** ① 선적된 베이 편집이 안 됨 ② 베이 탭이 설명 세 줄에 가려 잘림 ③ 28번 데크 X 기호 3개가 수정 안 됨 ④ "잘못된 컨테이너면 놓이질 않아야 되는데 놓였다는 것도 이상하고, 놓였다 하더라도 수정을 할 수 있어야 되는데 보이질 않습니다" ⑤ "16번 베이부터 28번 베이까지 … 검은색 처리와 X표 처리한 곳 전부 다 컨이 중복되거나 잘못 놓였다고 판단한 것 같은데, 다른 곳에 실린 게 이동했을 수도 있습니다. 그럴 때 임시창고에 있다가 호출되면 해당 베이에 선적이 되어야 하는 건데 아무 메시지도 없고 수정도 못 하게 되어 있습니다."

### 실데이터 판정 — 16~28베이는 **깨끗했다** (추정 아님, RTDB 전수 대조)

파이어베이스 `voyages/STSE_2657E/loading` 실데이터로 검사했다 (항차키는 `STSE_2657E`, `voy_l=2658W`. 양하 E와 선적 W가 **한 레코드**에 들어 있다 — 끝자리로 항차를 찾을 때 주의).

| 항목 | 결과 |
|---|---|
| 16~28베이 컨 | 205대 (전량 POL=KRPTK 평택분) |
| 같은 좌표 중복 | **0건** |
| 20ft↔40/45ft 자리 겹침 | **0건** |
| 베이사전 격자 밖 | **0건** |
| 28베이 | 46대 전량 45ft·CNTAO — 이상 없음 |

**결론: 중복도 오적재도 없었다.** 화면의 검은칸·X는 오류 표시가 아니라 **정상 표기**였고, 표기의 뜻을 화면이 말해 주지 않은 것이 문제였다.

### 검은칸·X의 정체 (셋을 구분해야 한다)

| 표기 | 코드 | 뜻 |
|---|---|---|
| **X 글자 + 흰 배경** (`bge-x`) | `adjMap[k]==='40'` | **옆 베이의 40/45ft가 그 자리를 먹었다.** 홀수 베이 화면에서 이웃 짝수 베이 컨이 만든다 |
| **검은 그림자** (`bge-shadow`) | `adjMap[k]==='20'` | 옆 베이의 20ft가 먹은 자리 |
| **셀 마크 'X'** | `defaultGetSelfMark`: `c.pod !== pod` | 그 컨의 **POD가 페이지 대표 POD와 다름**. 2658W는 대표 POD가 CNTAO(279대)라 **CNSHD 126대가 전부 X**로 찍힌다 |

⚠ 앞의 둘과 셋째는 **완전히 다른 X**다. `cell.mark`로 이웃 점유를 판정하면 안 된다(V9.07-05에 이미 명문화). 이번 신고의 "28데크 X 3개"는 첫째 유형이었다.

### 진짜 결함 — 미배정 컨 15대가 가짜 좌표에 갇혀 있었다

`buildState()`가 좌표 없는 컨(`bay`/`tier`가 빈 문자열)에도 `pad2('')='00'`을 먹여 **전원 `00-00-00`에 몰아넣었다.** 결과:

- `validate()`가 이들을 **"좌표중복 1건"**으로 보고 → 사용자가 본 "중복" 경고의 정체
- 어느 베이 페이지에도 안 그려짐 → **보이지도, 손대지도 못함** (신고 ④의 실체)
- 2658W 실측: **15대** (SKR 45ft 11대 + SIT 20ft 4대, 전량 KRPTK 발)

**수정 (V9.23-05, `src/planEditCore.js`).** 좌표 없는 컨은 `{storage:true}`로 만들어 **임시창고에 넣는다.** 사용자가 말한 업무 흐름 그대로다 — 임시창고에 있다가 **호출되면 해당 베이에 선적**. `state.unplaced` Set을 함께 반환해 임시창고 칩에 노란 **미배정** 배지를 단다.

시뮬 결과: 중복 1건 → **0건**, `validate().ok=false` → **true**, 임시창고 15대, 호출→배치 `ok=true`, 이미 찬 자리로 호출하면 자리 맞교환.

### 화면이 이유를 말하게 했다 (폰에는 풍선말이 안 뜬다)

`title` 속성에만 이유가 있어서 폰에서는 아무 설명도 안 보였다. 상태줄(`setMsg`)로 전부 끌어냈다.

| 누른 곳 | 나오는 말 |
|---|---|
| 옆 베이가 먹은 칸 | `이 자리는 28베이 SKHU6414573(40ft)이 차지하고 있습니다 — 그 컨을 먼저 옮기면 이 자리가 납니다 (선택됨)` |
| 〃 (통과 고정분) | `… — 통과 고정분이라 옮길 수 없습니다` |
| 슬롯 없는 칸 | `이 자리는 이 배에 슬롯이 없습니다 (베이사전 기준)` |
| 컨이 있는 칸 | `SKHU6414573 · 28베이 05열 84단 (45GP) — 끌어서 옮기거나 보관하십시오` |
| 빈 칸 | `빈 자리 05열 84단 — 임시창고나 다른 칸에서 컨을 끌어 놓으십시오` |

막힌 칸에 떨어뜨려도 이제 조용히 무시하지 않고 이유를 말한다(`onDragOver`+`onDrop`).

### 판올림 내역

| 버전 | 내용 | 파일 |
|---|---|---|
| V9.23-02 | 선적 탭 복원 — `ediContainers`만 보던 판정을 `raw.edi.text`(50자 초과)도 인정하도록. 빈 섹션은 `(자료 없음)`으로 **비활성 표시**(탭 자체는 항상 렌더) | `ChiefBayEdit.jsx` |
| V9.23-03 | 베이 탭 잘림 해소(`.bge-nav` 고정 44px → `min 34 / max 78`, 모바일 64px·제목 말줄임) + **옆 베이 점유 칸에 컨번호 표시·드래그 허용**(`adjCnMap`/`adjBayMap`) | `BayGridEditor.jsx` |
| V9.23-04 | 격자에 안 그려지는 컨을 찾는 `⚠ 안 보임 N` 사이드 탭 + `sendCns()` 일괄 보관 | `BayGridEditor.jsx` |
| V9.23-05 | **미배정 컨 → 임시창고**(`unplaced` Set·노란 배지) + **막힌 칸 사유를 화면에 표시** | `planEditCore.js`, `BayGridEditor.jsx` |

### 이번에 새로 확인한 것 (다음 클로드가 헤매지 말 것)

1. **항차키는 방향 접미사가 붙은 한 개다.** `STSE_2657E` 하나에 `voy_d=2657E`(양하)·`voy_l=2658W`(선적)가 들어 있다. `voyages.json?shallow=true`로 목록을 먼저 본다.
2. **RTDB는 REST로 바로 읽힌다** — `https://greenmarinetally-default-rtdb.asia-southeast1.firebasedatabase.app/<경로>.json`. 큰 EDI를 컨텍스트에 올리지 않고 샌드박스에서 `curl`+`node`로 전수 분석할 수 있다. **실데이터 판정은 이 경로로 한다.**
3. **STSE 베이사전은 `source:"edi"`(자동추출)**, 22개 베이만 있다(01,03,04,05,07,08,09,11,12,13,15,16,17,19,20,21,23,24,25,27,28,29). 짝수 18·22·26이 없는 건 화물이 없어서다. 27~29는 92단이 있고 82단은 8열까지다.
4. **배포 실측(2026-07-30)**: computer-use가 응답하지 않을 때는 **방법 B(브라우저 업로드)**로 끝까지 간다. 디렉터리별 `/upload/main/<경로>`로 나눠 7커밋. 커밋 버튼은 좌표 클릭 말고 `javascript_tool`로 `[...document.querySelectorAll('button')].find(x=>/변경 사항을 커밋/.test(x.textContent)).click()` — GitHub UI가 한국어라 영문 라벨로는 안 잡힌다. 대용량 번들 업로드 후 커밋 버튼이 살아나기까지 **12초** 기다려야 한다.
5. **검증**: 원격 blob 해시 전수 대조(`git rev-parse origin/main:<파일>` vs `git hash-object`) 9/9 일치 · 라이브 `sw.js` VERSION=V9.23-05 · 라이브 번들에서 `미배정` 14건·`이 자리는` 3건 확인.

### 남은 것

- **28베이 X 3개가 정말 해소됐는지 사용자 화면 확인 필요.** 표기 자체는 정상이므로 "X가 사라지는" 게 아니라 **눌렀을 때 무엇이 막고 있는지 말해 주고 그 컨을 잡을 수 있게** 된 것이다.
- `defaultGetSelfMark`의 POD 불일치 'X'가 이웃 점유 X와 글자가 같아 계속 혼동을 부른다. **다른 글자로 분리하는 것을 검토**할 것(예: POD 불일치는 소문자 `x` 또는 색만).

---

## V9.23-06 — 배포본 전체 크래시 긴급수정 + **렌더 연막검사 도입** (2026-07-30)

**사고.** V9.23-05를 배포한 직후 사용자 화면에 `앱 전체 크래시 / Cannot access 'ge' before initialization`. 앱이 아예 안 뜬다.

**원인.** V9.23-04에서 넣은 `hidden` useMemo(326행)가 **346행에 선언된 `issues`를 참조**했다. `const`는 선언 전 접근이 TDZ 에러다. `useMemo` 본문은 렌더 중 즉시 실행되므로 컴포넌트가 마운트되는 순간 죽는다. `ge`는 `issues`의 minify 결과.

**수정.** `issues` 선언을 `drawable` 위로 올렸다(302행). 덤으로 발견한 결함 하나 더 — 베이사전이 아직 없으면 `drawable`이 빈 집합이라 **전 컨이 '격자 밖'으로 잡혔다(실측 390/405)**. `hidden`에 `!matrixBays.length` 가드를 넣었다.

### ⭐ 이 사고가 남긴 교훈 — 빌드 성공은 아무것도 보장하지 않는다

이번에 통과한 관문: 실데이터 시뮬 PASS · `bash build.sh` 성공 · 번들 grep으로 APP_VERSION·새 문자열 확인 · 원격 blob 해시 9/9 일치 · 라이브 sw.js VERSION 확인. **다 통과했는데 앱이 죽었다.**

이유는 명백하다 — **위 검사 중 컴포넌트를 실제로 그려 본 것이 하나도 없다.** 시뮬은 `planEditCore`(순수 함수)만 돌렸고, grep은 문자열이 들어갔는지만 봤다. 렌더 중에만 터지는 오류는 렌더해야 잡힌다.

**조치: `build.sh`에 렌더 연막검사를 넣었다** (`tools/smoke_entry.jsx` + `tools/smoke_render.cjs`).

- esbuild로 `BayGridEditor`를 합성 픽스처(48대 + 미배정 3대)와 함께 번들 → **jsdom에 올려 실제로 렌더**
- 렌더 중 `console.error`·`window.onerror`가 **한 건이라도** 나오면 `exit 1`로 **빌드를 실패시킨다**
- 렌더 결과가 500자 미만이면(=아무것도 안 그림) 실패
- 미배정 3대가 임시창고로 갔는지까지 확인
- 검증: 버그 있는 코드로 돌리면 `✗ 렌더 중 오류 … Cannot access 'issues' before initialization`로 정확히 잡힌다(재현 확인함)

> **정적 스캐너는 버렸다.** 선언 순서를 정규식으로 훑는 검사도 만들어 봤는데 다른 파일에서 오탐 60건이 나왔다(중첩 스코프·구조분해 가림을 구분 못 함). 잡음이 많은 검사는 안 보게 되므로 넣지 않았다. **실제로 그려 보는 것 하나면 충분하다.**

### 관문 6 갱신 (비협상)

기존: 시뮬 + 빌드 + 번들grep + UI렌더 → **"UI렌더"가 선택이 아니라 `build.sh`가 강제하는 단계가 됐다.** 새 컴포넌트를 손대면 `tools/smoke_entry.jsx`에 렌더 대상을 추가한다.

---

## V9.23-07 — **빈자리 우선** 선적 흐름 (2026-07-30, 사용자 요구)

**사용자 원문.** "일단 선적을 다 임의로 해봤습니다. 15대가 선적이 안되었는데 안된 위치를 보여 줘야 하는데 임의로 좌표를 입력하라고 나옵니다. 순서가 바뀐듯 합니다. 좌표를 입력하는것보다 선적이 안된 위치를 보여주고 그곳을 누르면 대상 컨테이너들을 보여 주는것이 작업 하기가 편합니다."

**진단.** V9.23-05까지의 흐름은 **컨 → 자리**였다. 임시창고에서 컨을 집어 격자의 좌표로 끌어다 놓는다. 폰에서 긴 드래그는 실패하기 쉽고, 무엇보다 **"어디가 비었는지"를 먼저 알 수 없다.** 사용자는 반대 순서를 원한다 — **자리 → 컨**.

### 뒤집은 흐름

1. **`🅿 빈자리 N` 탭 신설.** 전 페이지의 빈 자리를 베이별로 묶어 보여 준다 (`BAY 11 — 빈 자리 33곳 / 데크 10열 90단 · 데크 08열 90단 …`). 누르면 그 베이 페이지로 바로 넘어간다.
2. **놓을 수 있는 빈 칸을 초록으로 칠한다** (`.bge-open`). 임시창고에 대기 컨이 있을 때만 켜진다 — 놓을 게 없으면 초록이 안 뜨므로 헷갈리지 않는다.
3. **빈 칸을 누르면 고르기 창**이 뜬다. `BAY (04)05 · 06열 88단` 머리글 아래 임시창고 컨 목록(미배정 배지 포함). 컨을 누르면 그 자리에 선적되고 창이 닫힌다. **좌표를 외우거나 입력할 일이 없다.**

빈 자리 계산은 **점유 + 옆 베이 40/45ft 잠식**을 모두 뺀다. 실제로 못 놓는 자리는 애초에 목록에도 초록에도 안 나온다.

**실데이터 검증 (2658W, 405대).** 렌더 오류 0 · 임시창고 15 · 빈자리 330곳 · 안 보임 0 · 현재 페이지 초록칸 7. 베이별 목록 정상(BAY 01 7곳 / 03 18곳 / (04)05 18곳 / 07 10곳 / (08)09 10곳 / 11 33곳 …).

### 연막검사 강화 — 이제 **눌러 본다**

V9.23-06의 연막검사는 껍데기만 그리고 있었다(실측: `.cpv2-cell` 0개). **베이사전이 없으면 격자가 아예 안 그려지기 때문**이다. 합성 사전(`window.__fbShipBayDict`)을 픽스처에 넣어 진짜 격자가 나오게 고쳤고, 검사 항목도 늘렸다:

1. 렌더 중 오류 0건 · 렌더 결과 500자 이상
2. 미배정 3대가 임시창고로 갔는가
3. **초록 빈 칸이 그려졌는가** → 없으면 실패
4. **빈 칸을 클릭하면 고르기 창이 뜨는가** → 안 뜨면 실패
5. **컨을 고르면 창이 닫히고 임시창고가 3→2로 줄었는가** → 아니면 실패
6. 상호작용 중에도 오류 0건

즉 **화면을 실제로 눌러 보는 것까지 빌드가 강제**한다. 통과 출력: `✓ 렌더 연막검사 통과 (18271자 · 임시창고 3→2 · TEST1000048 배치 · 오류 0)`.

> **교훈 하나 더.** 연막검사를 넣었다고 안심하면 안 된다. **검사가 실제로 무엇을 그리고 있는지 세어 봐야 한다.** 이번엔 셀 0개를 그리면서 "통과"를 내고 있었다.

---

## V9.23-08 — **리스트에만 있는 컨 14대가 통째로 사라지던 문제** (2026-07-30, 2658W)

**사용자 지적.** "선적 14대가 남았는데 선적할곳이 없으면 어떻게 하라는거죠?" → "선적할 베이가 안뜬다는것은 선적이 다 되었다는 뜻입니다. 한대라도 비었다면 그 베이는 표시가 되어야 합니다. 그리고 베이상세 편집에서도 배정이 안된 14대를 보여줘야 빈곳에 넣어 수정을 할텐데 검수원도 수석 검수사도 수정을 할수가 없다는 이야기 입니다."

화면은 **선적 391/405 · 대기 14대**인데 작업 패널은 **"남은 선적 작업이 없습니다"**. 맞는 지적이다 — 한 대라도 남았으면 어디선가는 보여야 한다.

### 실데이터 추적 (RTDB `voyages/STSE_2657E/loading`)

| 확인 | 값 |
|---|---|
| `records`(리스트, `_source: 2658WLOADLIST.xlsx`) | 405 |
| `completed` | 391 |
| 대기 = 405 − 391 | **14** |
| 그 14대의 적부 좌표 | **전원 없음** (`bay=""`) |
| 그 14대가 raw BAPLIE에 있는가 | **전원 없음** |

선사가 **엑셀 로드리스트로만 준 엠티 14대**(SKR 45ft 10 · SIT 20ft 4)다. BAPLIE에 슬롯이 없다.

### 원인 — 네 군데가 각자 이 컨들을 버렸다

1. ⭐ **`fullEdiMapOf()` (utils.js)** — `if (!/^[A-Z]{4}\d{7}$/.test(k)) m[k] = v;` 즉 **raw에 없는 "실번호 형식" 키는 구판·가상 잔재로 보고 제외.** 진짜 화물 14대가 여기서 통째로 증발했다. 베이상세편집·인쇄허브·선적플랜편집이 전부 이 함수를 쓰므로 **수석도 검수원도 손댈 방법이 없었다.**
   → **수정**: raw에 없어도 **`records`나 `completed`에 있으면 실재하는 화물**이므로 살린다.
2. **`planEditCore.buildState()`** — 자리 없음 판정이 `bay === ''`였는데 **`ChiefBayEdit`는 `pad2()`로 넘겨 빈 값이 `'00'`이 된다.** V9.23-05의 임시창고 이관이 이 경로에선 작동하지 않았다. → 베이·단의 `'0'`·`'00'`도 자리 없음으로 본다(01단부터 시작하므로 안전).
3. **`SearchPanel` 수동 `manualGroups`** — `if (… || !c.bay) return;`로 제외 → 고를 베이가 0개 → "남은 작업 없음". → **`⚠ 자리 미지정` 묶음**을 만들어 베이 선택 화면 맨 위에 낸다. 누르면 데크/홀드 게이트를 건너뛰고 바로 조회·작업으로 간다.
4. **`GuidedWorkPanel`** — `위치 미배정` 패널이 **이미 있었지만**(V7.94-08) 베이를 고른 뒤에야 렌더된다. 고를 베이가 없으면 그 앞의 early return에 걸려 **영원히 안 보인다.** → 베이 선택 화면에도 같은 블록을 낸다.

### 검증 (실데이터, ChiefBayEdit와 같은 경로)

`fullEdiMapOf` → `pad2()` → `buildState()`로 그대로 태워 확인:

- `fullEdiMapOf` 결과 **405대** (종전 391) — 14대 전원 복귀
- `buildState` → **임시창고 14대 · 미배정 표시 14대** · 좌표중복 0 · `validate.ok = true`
- 임시창고에 들어갔으므로 V9.23-07의 **빈자리 → 컨 고르기**로 곧바로 자리를 줄 수 있다

### 연막검사 보강

픽스처의 미배정 컨을 **`bay:'00', tier:'00'`(pad2 통과 형태)**으로 바꿨다. 이번 사고의 급소가 바로 그 경로였는데 종전 픽스처는 `''`만 써서 못 잡았다.

> **교훈.** 컴포넌트 하나만 데이터로 태워 보고 "검증 완료"라 하면 안 된다. V9.23-05에서 `BayGridEditor`에 **원본 EDI 값을 직접** 넣어 "임시창고 15대"를 확인했지만, 실제 앱은 `ChiefBayEdit`가 `fullEdiMapOf`로 거르고 `pad2`로 가공해 넘긴다. **호출부까지 포함한 실제 경로로 태워야 검증이다.**

---

# [TallyOne 1.0 — 2026-08-03 재탄생판] 전수조사 수리 + 3화면 개편 + 리브랜딩

## 이름 변경 (사용자 확정 2026-08-03)
- 검수앱 Tallyman Master → **TallyOne 1.0** / 메일수집기 → **MailPilot 1.0** / 콘앱 → **ConeOne 1.0**
- 콘앱 버전은 cone.html의 window.__CONEV가 단일 소스(검수앱 버전과 분리, build.sh가 라벨만 동기화).

## 판1 (V9.57 커밋): 전수조사 수리·삭제
- 결함 52건 수정: 숫자 ISO 특수컨 오태깅(4582 계열 tk 오판), normalizeCntrType 충돌(실선적 EDI 리퍼→드라이), 플랜편집 베이 100번대 절단, fbCreateVoyage가 수집기 info 덮어쓰기, 보관소 평택분 옛 규칙(POL∨POD), 약식 IFCSUM 파서 제거(parseNumericIFCSUM 단일화), badgeRule departed 무조건 전환, 흡수 가드 구멍(자동등록 항차 무검증 통과), gemini API 키 하드코딩 제거(⚠ AI 기능은 설정에서 키 등록 필요), 수석 장비표 5호기 누락·external_pause 미표시 등.
- 삭제 약 2,100줄: 고아 서브시스템(ShipList/BayBuilder/CargoPlanView/BayBox/lib), MixerUploadModal 껍데기, BayDictStatusWidget(기능은 VerifyWidget 이관), emptyPlanVerify, 죽은 export 다수.

## 판2 (TallyOne 1.0): 3화면 개편
- 시작은 항상 **로그인 화면**(#/login). 로그인 후 역할별 진입 — 검수사→홈(#/), 수석·소유자→수석 대시보드(#/chief).
- 권한: #/chief·#/search는 수석·소유자 전용. **소유자(김성일)만 전 화면 열람**.
- 보조기능 화면 #/aux 신설: 사용 매뉴얼·검수 용어집·영어 회화집(1급 승격)·맛집·건강점검·AI 키·오늘의 브리핑·장비번호.
- 수석 대시보드 보강: 수집기 하트비트 배너·healthSummary, 터미널 실적 대조(disDone/disPlan 등)·출항 표시, planDate/planSrc·도선 예보, 10px 버튼 44px화, alert 21곳 인라인 통지화.
- 라우팅 수리: 양하/선적 mode 해시 유실(B-1), 뒤로가기 정리, 해시 파서 단일화(backHandler.parseHash).

## MailPilot 1.0 (수집기, 같은 날)
- v2.17.47 전수조사 수리판(치명 6·높음 19·중간 20·낮음 11 수정, 미사용 790줄 삭제) 위에 이름 변경.
- 상세: C:\TALLYTEST\메일수집기_전수조사_보고서_v2.17.46_2026-08-03.md

---

# [ConeOne 1.1 — 2026-08-03] 콘앱 전수조사 수리

- 약식 parseEDI F/E 전면 미인식 교정(EQD 역방향 스캔, 마스터 parseBAPLIE와 동일 규칙) · parseASC에 cn/iso/F,E 포함.
- 마스터 파서 단일 소스 보장 — 항차 선택 시 최대 8초 로드 대기, 폴백 시 "⚠ 간이 해석 모드" 경고 카드(조용한 격하 금지).
- 질문기 트리오 베이 구간 매칭 — "6번 베이"(트리오 05-07의 가운데) 조회·집계 실패 수정.
- 픽셀/PDF 판독 사장 파이프라인 285줄 삭제. 미배치 컨(bay='') 차단.
- 백로그 "ATRP 홀드 콘 계산 오류": 실EDI 3항차(2632W/2633W/2634W) 전수 계산이 지침 Ⅲ-2 규칙과 전부 일치 — 재현 불가. 재현에는 ATPR 양하(E항차) EDI와 틀린 수치(항차·베이·표시값vs실제값)가 필요. 트리오 조회 실패(수정됨)가 체감 원인이었을 가능성.

---

# [TallyOne 1.1 — 2026-08-03] 클로드 메모함

- 앱 어디서든 헤더 ⋯ 메뉴 또는 보조기능 카드에서 "클로드에게 메모" — Firebase `claude_inbox`에 저장(검수원·화면·항차·모드·버전 자동 첨부).
- 오프라인이면 폰에 보관(tallyone_claude_memo_queue) 후 연결 시 자동 전송. 성공/보관/실패 전부 화면 문구로 표시.
- **모든 클로드 세션 규칙**: 작업 시작 시 `GET {firebase_db}/claude_inbox.json` 확인 → 메모 규칙(즉시 작업 금지, "작업해줘" 시 일괄 처리)에 따라 처리 → 처리 후 사용자 보고, 정리는 사용자가 앱에서 삭제.

---

# [TallyOne 1.2 — 2026-08-03] 사진 앨범 선택 + ConeOne 1.5 LOLO 규칙

- 데미지·실오류·풀리퍼 사진 입력에 "🖼 앨범에서 선택" 추가 — capture 강제로 찍어둔 사진 등록이 불가하던 문제 해결(2026-08-02 CLHU2628765 FRONT PANEL TORN 20×30 보고 실패 사건). PORT-MIS 캡처는 capture 제거(갤러리 선택 가능).
- ConeOne 1.2~1.5-01: 베이뷰 격자 검수앱 단일화(유령 칸 제거) → RZOR LOLO 덱플랜 뷰(검수앱 LOLO 탭 동일) + 콘 규칙 확정 — 1단 바닥 콘 없음, 2·3단 대당 4개, 콘=(반수−바닥)×4(바닥 입력식, 기본 ⌈N/3⌉), 최대 작업량 67반 초과 경고. 최대 8열 3단.
