# HANDOFF_TO_NEXT_CHAT.md — M4.5 인계 지침서

> **현재 상태:** M4.5 빌드 완료 (현장 버그 fix 3건 + v2 사전 109척 임베드 + 퍼지 매칭)
> **이전 버전:** M4.4 (.def 런타임 파서, userBayDict)
> **인계 일자:** 2026-05-09
> **빌드 결과:** 1.17MB / gzip 247KB (v2 사전 109척 포함)

---

## 1. M4.5 핵심 변경 — 현장 버그 fix 3건

성일님이 M4.4 현장 사용 중 발견하신 세 가지 버그 수정:

### 1-1. 빈 베이 미표시 → 수정
- **이전**: BayPlan이 EDI `maxBay`까지만 페이지 → 마지막 컨 이후 빈 베이(예: TNJP 33) 안 보임
- **수정**: `.def` 사전 우선 → 등록된 모든 베이 무조건 페이지 추가

### 1-2. 통로/건물 오인 → 수정
- **이전**: 1~maxBay 모든 짝수가 페이지로 → 통로/건물도 빈 페이지로 표시됨
- **수정**: `.def`에 등록된 베이만 페이지로 → 갭(통로/건물)은 자동 생략

### 1-3. 짝수 단독 베이 = 20ft 전용 식별 → 수정 (사용자 정정)
- **이전**: 양쪽 홀수 모두 누락된 짝수 베이를 단독 40ft로 처리
- **수정**: **양쪽 홀수가 .def에 모두 없으면 = 20ft 전용 베이**
  - 예: `[..25, 26, 28, 30..]`에서 베이 28의 양쪽(27, 29) 모두 누락 → 20ft 전용
  - 40ft 슬롯 만들 인접 홀수가 없으니 40ft 컨이 못 들어가는 구조

### 1-4. v2 사전 109척 임베드 + 퍼지 매칭 (확장)
- **이전**: 임베드 사전 11척 (v1.1, 미검증)만
- **수정**:
  - v2.0 사전 **109척 verified** 추가 (이전 채팅 일괄 분석 결과)
  - 정규화된 선박명 부분 매칭 — `"TJTEN JUPITER"` ↔ `"TEN JUPITER"` 자동 매칭

---

## 2. 페이지 로직 동작 (검증된 시나리오)

### 시나리오 검증 결과 (5종 + 사용자 정정 케이스 3종)

```
TNJP (균등 트리오 + 단독 33):
  베이 25개 / 통로 8개 (04,08,12,16,20,24,28,32) → 17 페이지
  마지막 BAY 33 (20ft) 정상 표시 ✓

AKEIGA (단독 베이 09, 19 = 선원동 인접):
  베이 23개 / 갭 8개 → 16 페이지
  베이 09, 19 단독으로 정상 표시 ✓

ASFD (연속 갭 = 건물):
  베이 24개 / 건물 [08-10], [26-28] / 통로 5개 → 16 페이지
  건물 위치 페이지 생성 안 됨 (자동 생략) ✓

BIJG (베이 02 없음):
  베이 19개 / 베이 01이 단독으로 시작 → 13 페이지

CASN (대형선 37 베이):
  베이 37개 / 13 섹션 → 25 페이지

[사용자 정정 #1] 232425 → 30:
  BAY 23 (20ft) / BAY 24 (40ft)+25 / BAY 30 (20ft 전용)  ✓

[사용자 정정 #2] 232425 → 303132:
  BAY 23 (20ft) / BAY 24+25 / BAY 30+31 / BAY 32 (단독 40ft)  ✓

[사용자 정정 #3] 25, 26, 28, 30, 31, 32:
  BAY 25 (20ft) / BAY 26 (40ft) / BAY 28 (20ft 전용) / BAY 30+31 / BAY 32  ✓
```

---

## 3. 변경된 파일 (M4.5)

| 파일 | 변경 내용 |
|------|----------|
| `src/utils.js` | `APP_VERSION = 'M4.5'` |
| `src/data/shipBayDict_v2.js` | **신규** (109척 verified 임베드, 415KB) |
| `src/shipStructure.js` | v2 import + `fuzzyLookupAcrossDicts` 추가 + `getShipBayDictData` v2/user/v1 우선순위 + 정규화 부분매칭 |
| `src/components/BayPlan.jsx` | `.def` 베이 리스트 우선 사용 + 양쪽 홀수 검사로 20ft 전용 식별 |
| `src/pages/VoyagePage.jsx` | BayPlan에 `shipImo`, `shipName` props 전달 |

---

## 4. 매칭 우선순위 (M4.5 통합)

```
shipImo + shipName
       ↓
fuzzyLookupAcrossDicts() 실행
       ↓
1. userBayDict (M4.4 사용자 .def 업로드, localStorage)
2. SHIP_BAY_DICT_V2 (109척 verified, 임베드)
3. SHIP_BAY_DICT v1.1 (11척 미검증, 폴백)
       ↓ (모두 실패 시)
4. v2 사전 정규화 부분 매칭 — 선박명에서 공백/특수문자 제거 후 양방향 substring
   예: EDI "TJ TEN JUPITER" → "TJTENJUPITER"
       v2 키 "TEN JUPITER"  → "TENJUPITER"
       후자가 전자에 포함됨 → 매칭 ✓
       ↓ (실패)
5. EDI 기반 폴백 (M3.89.1 동작) — 통로 처리 안 됨, 빈 페이지 표시 가능
```

---

## 5. 정의 정리 (사용자 통찰 반영)

### .def 베이 리스트에서:
- **존재하는 베이** → 페이지 생성, 그리드 표시
- **단일 짝수 누락** (예: 04 하나만) → 통로(gangway), 페이지 생성 안 함
- **연속 누락** (2개 이상) → **건물** (선원 숙소, 사무실, 조타실, 선장실), 페이지 생성 안 함
- **양쪽 홀수가 모두 없는 짝수** → **20ft 전용 베이** (40ft 슬롯 불가)
- **단독 홀수** → 일반 20ft 베이

### 베이 플랜에서 보이는 것:
- 트리오 (홀+짝+홀) → 표준 페어 페이지
- 단독 홀수 → 단독 20ft 페이지
- 양쪽 홀수 있는 짝수 → 표준 40ft + 우측 20ft 페어
- 한쪽 홀수만 있는 짝수 → 단독 40ft 페이지
- **양쪽 홀수 없는 짝수 → 20ft 전용 페이지** (M4.5 신규)

### 베이 플랜에서 안 보이는 것:
- 통로 (단일 짝수 갭)
- 건물 (연속 갭) = 선원 숙소/사무실/조타실/선장실 등

---

## 6. 미해결 / 다음 세션 후보

### 6-1. 우선순위 최상 (현장 검증)
1. **M4.5 배포 후 폰에서 실제 동작 확인**:
   - TNJP: 베이 33 + 통로 자동 생략
   - 임베드 109척 중 실제 입항 선박 매칭 성공률
   - 매칭 실패 시 .def 직접 업로드 (M4.4 기능)로 등록

### 6-2. 우선순위 상 (메서드 강화 — 이전 채팅 발견)
2. **CASP 6.10 메타 위치 캘리브레이션**:
   - 평택 운항선의 56%가 6.10 + block 144 (이전 채팅 일괄 분석)
   - 메타 위치(byte 89/121/153) 6.50과 다른지 검증 필요
   - 검증 시 semi-verified 163척 → verified 승격 가능 → 임베드 척수 109 → 272

### 6-3. 우선순위 중 (UI 보강)
3. **건물/통로 시각 표시**: 베이 페이지 사이에 "🏢 건물 (28-30)" 또는 "│ 통로 (04)" 같은 구분선
4. **20ft 전용 베이 시각 강조**: "(20ft 전용)" 텍스트 외에 색상/아이콘 구분

### 6-4. 오래된 숙제 (M3.86부터)
5. ISO 변경 후 화면 미반영 (ediContainers 동기화)
6. 리퍼 온도 직접 수정 UI 부재

---

## 7. 빌드 정보

```
빌드 명령: npx vite build
M4.5 빌드 결과:
  index.html                    1.16 kB
  assets/index-CW1byNsl.js    1,170.19 kB (gzip 246.51 kB)
  assets/index-D0MqRUGE.css      55.80 kB
  assets/mixerUpload-...js        6.83 kB

번들 크기 변화:
  M4.4: 860KB / gzip 228KB
  M4.5: 1170KB / gzip 247KB (v2 사전 109척 임베드로 +310KB / +19KB gzip)

배포 사이트: https://greenmarine26.github.io/greenmarinetally/
```

---

## 8. 다음 세션 시작 시 권장 첫 메시지

성일님이 다음 세션에서 이 ZIP을 다시 올리실 때:

- **"M4.5 폰 테스트 결과"** + 화면 스크린샷 (가장 가치 큼)
- **"6.10 spot check 시작"** (semi-verified 163척 verified 승격)
- **"건물/통로 시각 표시 추가"**
- **"미해결 버그 2건 처리"** (ISO 변경, 리퍼 온도)

---

## 9. 빠른 재시작 명령

```bash
cd /home/claude/m44_build

# M4.5 핵심 변경 빠르게 보기
grep -A 10 "M4.5" src/components/BayPlan.jsx | head -20
grep -A 10 "fuzzyLookupAcrossDicts" src/shipStructure.js | head -30

# 재빌드
npx vite build
cp dist/index.html ./ && cp -r dist/assets ./ && rm -rf dist
```

---

## 10. 자체 평가

| 항목 | 점수 | 비고 |
|------|------|------|
| 사용자 정정 반영 | 10/10 | 3가지 모두 시뮬레이션으로 검증 |
| 코드 안전성 | 9/10 | 폴백 보존, 사전 없으면 기존 동작 |
| 매칭 견고성 | 9/10 | 퍼지 매칭으로 EDI vsl 변형 대응 |
| 사전 통합 | 10/10 | 109척 verified 임베드 (11척 → 109척) |
| 검증 충실도 | 7/10 | 시뮬레이션 OK, 실제 폰 테스트 미실행 |
| **종합** | **9/10** | M4.5 배포 가치 큼, 현장 검증 필수 |

---

*M4.5 빌드 완료 / 현장 버그 3건 fix + v2 사전 109척 + 퍼지 매칭 / 다음: 폰 테스트 후 6.10 캘리브레이션*
