# 인계 — 검수앱 V8.41 (가상 선적 EDI, RZOR·OBWH)

## 했다 (무엇을)
- RZOR·OBWH는 선적 EDI가 늦거나(OBWH) 안 와서(RZOR) 대시보드 선적이 0으로 뜨던 것 보정.
- `autoRegApi.js buildAutoPayload`: 선적(mode='loading')인데 진짜 EDI가 없고(best=null) 선적 리스트(records)가 있으면, **RZOR·OBWH만**(`VIRTUAL_LOAD_SHIPS`) 리스트를 가상 선적 ediContainers(POL=KRPTK, `_virtualEdi`·`_virtualFromList`)로 승격. → 선적 카운트(`_ptkCountOfSection`·POL평택)가 리스트 수만큼 잡힘.
- OBWH: 나중에 실 EDI 도착 → `best`가 실 BAPLIE로 잡혀 ediContainers를 덮음(마무리=실 EDI, autoreg PUT). RZOR: 실 EDI 없어 가상이 최종.
- `HomePage.jsx`: `computeStats`에 `virtual` 플래그, `SectionBar`에 '가상/리스트' 배지(보라).
- `utils.js`: APP_VERSION V8.40 → V8.41.

## 왜
- 선적 카운트는 loading.ediContainers(POL평택)만 셈(firebase.js `_ptkCountOfSection`). 리스트(records)는 안 셈 → EDI 늦거나 없으면 선적 0. 리스트를 가상 EDI로 승격해 0을 없앰. (사용자 확답 2026-07-04: RZOR 가상으로 마무리 / OBWH 가상으로 채우다 실 EDI로 마무리.)

## 바뀐 파일 (repo 상대경로)
- src/autoRegApi.js
- src/utils.js
- src/pages/HomePage.jsx

## 빌드법
1. 위 3개 파일을 repo src/ 같은 경로에 덮어쓰기.
2. `bash build.sh` (node_modules 있으면 ~6초 — sw.js·cone.html 버전 동기화 + vite build + dist→루트 복사).
3. commit & push → GitHub Pages 반영.

## 검증됨
- 로직 시뮬 PASS: RZOR 리스트4→선적4(가상), OBWH 리스트227→227(가상)·실EDI2 오면 2(대체), DXQD(대상외)→0 유지, RZOR 양하→미적용.
- `node --check` 문법 OK (autoRegApi.js·utils.js).

## 주의 하나
- '가상/리스트' 배지는 새 UI라 렌더를 아직 눈으로 못 봄(샌드박스 빌드 미완). 배포 후 화면에서 배지 확인 필요.
