# 검수앱 V8.24-02 — PORT-MIS 미매칭(vslFull 코드형 오염) 수정 · GitHub 최신에 덮어쓰기

## 무엇 / 왜
OBWH 항차 "PORT-MIS 매칭 미확인(콜사인 D5MO4)". 실제 Firebase 확인 결과:
- voyages/OBWH_2671E/info.callsign="D5MO4"(정상), port_mis_data/D5MO4 존재(OCEAN BLUE WHALE) — 둘 다 멀쩡.
- voyages/OBWH_2671E/info.vslFull="CNYNT"(항구코드 CN-옌타이가 EDI 자동추출로 잘못 박힘).
- VoyagePage 신원가드 _nameMatchesPm 이 vslFull "CNYNT" vs "OCEAN BLUE WHALE" 비교 → 불일치 → 정확 콜사인 매칭을 버림 → 미매칭.

## 이 ZIP의 3파일 (GitHub 최신 V8.24-01에 그대로 덮어쓰기)
- src/pages/VoyagePage.jsx — _nameMatchesPm 수정: vslFull이 공백없는 코드형(또는 빈값·짧음)이면 검증 불가→통과. 진짜 풀네임(공백 다단어 또는 ≥7자)만 가드 적용. (DJCT 오염콜사인 차단은 보존)
- src/utils.js — APP_VERSION 'V8.24-02'
- public/sw.js — VERSION 'V8.24-02'

## 빌드
bash build.sh → git add -A && git commit -m "V8.24-02 PORT-MIS vslFull 코드형 오염 시 신원가드 우회" && git push
검증: 빌드 JS에 V8.24-02 + looksLikeName 박힘. OBWH 항차 열면 PORT-MIS 키 D5MO4로 자동 매칭.

## 검증 완료(작성자)
node 시뮬 6케이스 PASS: OBWH(vslFull="CNYNT") 매칭 성공 + DJCT(DONGJIN CONFIDENT→XIN TAI PING) 오염콜사인 차단 보존.
